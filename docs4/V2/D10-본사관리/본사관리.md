# 본사관리

> **docs4 Scope 문서**
> - 원본: `docs2/D10-본사관리/본사관리.md`
> - 범위: `V2`
> - 생성 기준: `lastspr/v1checklist.xlsx` `3.CRM (admin)_제로스트_0518논의` 시트, V1=`4~751`행
> - 원칙: 원본 기획 내용을 임의 축소하지 않고, 섹션 단위로 V1/V2에 재배치했습니다.

> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


# D10-본사관리 본사관리

> 개발사용 정리 문서입니다. 이 파일만으로 해당 도메인의 화면·다이얼로그 구현 기준을 확인할 수 있도록 재구성했습니다.


> **docs4 Scope**: V2
> **분류**: `V2_UNMAPPED`
> **분류 근거**: 직접 매칭 코드 없음. V1 행 범위에 포함되지 않은 기타/보류 섹션입니다.
> **주의**: 체크리스트 V1 코드와 직접 매칭되지 않아 V2/보류 영역으로 배치했습니다.


## 문서 메타
- 전체 화면 수: 15
- 전체 다이얼로그 수: 6
- 연결 기능 수: 15
- 공통 권한: `_공통/권한매트릭스.md`
- 공통 예외: `_공통/에러_예외_표준.md`
- 공통 자동화: `_공통/자동화_크론.md`
- 공통 디자인: `_공통/디자인_시스템.md`


> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


## 도메인 개요
- 화면/다이얼로그 구현 시 이 파일을 먼저 확인합니다.
- 본 문서가 해당 도메인 화면·다이얼로그 구현 기준입니다. 구현 판단을 위해 다른 폴더를 확인하지 않습니다.
- API/DB 상세는 백엔드 설계 범위이므로 제외하고, 화면/기능/UX/운영정책 기준만 남겼습니다.


> **docs4 Scope**: V2
> **분류**: `V2_UNMAPPED`
> **분류 근거**: 직접 매칭 코드 없음. V1 행 범위에 포함되지 않은 기타/보류 섹션입니다.
> **주의**: 체크리스트 V1 코드와 직접 매칭되지 않아 V2/보류 영역으로 배치했습니다.


## 화면


> **docs4 Scope**: V2
> **분류**: `V2`
> **분류 근거**: `HQ-10`: 1032행 `HQ-10-01` 위젯 라이브러리; `HQ-10-01`: 1032행 `HQ-10-01` 위젯 라이브러리; `HQ-10-02`: 1031행 `HQ-10` 커스텀 대시보드 빌더; `HQ-10-03`: 1031행 `HQ-10` 커스텀 대시보드 빌더; `HQ-10-04`: 1035행 `HQ-10-04` 레이아웃 옵션; `HQ-10-05`: 1036행 `HQ-10-05` 레이아웃 저장; 외 3개


## SCR-H1002 커스텀 대시보드 빌더

> 유형: 화면
> URL 경로: `/dashboard/builder`
> 연결 기능: `HQ-10`

### 1. 목적
위젯을 드래그앤드롭 방식으로 배치하여 자신만의 대시보드를 구성하는 화면입니다.
자주 확인하는 지표와 정보를 원하는 레이아웃으로 배치하여 업무 효율을 높입니다.

### 2. UI 구성
### 위젯 라이브러리 패널 (좌측)
- 추가 가능한 위젯 목록 (카테고리별 분류: 매출 / 회원 / 출석 / 수업 / 공지 / 할 일 등)
- 각 위젯 썸네일 미리보기
- 위젯 검색

### 캔버스 영역 (우측)
- 현재 대시보드 레이아웃 편집 영역
- 위젯 드래그앤드롭으로 배치 및 이동
- 위젯 크기 조절 (모서리 드래그)
- 위젯별 설정 버튼 (위젯 내 표시 기간, 지점 등 세부 설정)
- 위젯별 제거 버튼

### 레이아웃 옵션
- 그리드 열 수 선택 (2 / 3 / 4열)
- 그리드 선 표시/숨김 토글

### 저장 및 관리
- 현재 레이아웃 저장 버튼
- 저장된 레이아웃 불러오기
- 기본값으로 초기화 버튼
- 레이아웃 이름 지정 및 저장 (여러 레이아웃 프리셋 관리)

### 3. 액션 / 흐름
1. 화면 진입 시 현재 저장된 대시보드 레이아웃을 불러온다.
2. 좌측 라이브러리에서 위젯을 드래그하여 캔버스에 배치.
3. 배치된 위젯을 드래그로 이동하거나 모서리를 드래그로 크기 조정.
4. 위젯 설정 버튼 클릭 → 해당 위젯의 데이터 범위·필터 설정 패널 표시.
5. 저장 버튼 클릭 → 레이아웃 저장 → 대시보드에 즉시 반영.

### 4. 핵심 상태
| 상태 | 설명 |
|---|---|
| 기본 | 저장된 레이아웃 편집 모드 |
| 위젯 드래그 중 | 드래그 대상 위젯 강조, 배치 가능 위치 표시 |
| 수정 중 | 변경 사항 있을 때 저장 버튼 활성 |
| 저장 완료 | 완료 메시지 후 대시보드 반영 |

### 5. 권한
| 계정 | 볼 수 있는 것 | 할 수 있는 것 |
|---|---|---|
| 슈퍼관리자 | 전체 위젯 라이브러리 | 레이아웃 구성·저장·공유 |
| Owner(지점장) | 지점 관련 위젯 | 레이아웃 구성·저장 |
| 매니저 이하 | 접근 불가 | — |

### 6. 필수 예외처리
| 케이스 | 표시 | 후속 조치 |
|---|---|---|
| 빈 캔버스 | "위젯을 추가하여 대시보드를 구성해보세요" + 라이브러리 강조 | 위젯 추가 안내 |
| 위젯 최대 개수 초과 | 토스트 "캔버스 위젯 최대 개수(20개)를 초과했습니다" | 추가 차단, 기존 위젯 제거 안내 |
| 개별 위젯 데이터 로드 실패 | 위젯 내 "데이터를 불러오지 못했습니다 [재시도]" | 나머지 위젯 정상 표시 유지 |
| 저장 미완료 이탈 | "저장되지 않은 변경 사항이 있습니다" 다이얼로그 | [저장 후 이동] / [그냥 이동] / [취소] |
| 프리셋 최대 개수 초과 저장 | "프리셋을 더 이상 저장할 수 없습니다(최대 10개)" | 기존 프리셋 삭제 후 재시도 안내 |
| 프리셋 이름 중복 | "같은 이름의 레이아웃이 이미 있습니다. 덮어쓰시겠습니까?" | 확인 시 덮어쓰기, 취소 시 새 이름 입력 |
| 전사 공유 레이아웃 수정 시도(Owner(지점장)) | "본사에서 배포한 레이아웃은 수정할 수 없습니다" | 복사 후 편집 유도 |
| 권한 외 위젯 드래그 시도 | 라이브러리에서 해당 위젯 숨김 | 드래그 자체 불가 |
| 손상된 프리셋 불러오기 | "레이아웃을 불러올 수 없습니다" 오류 | 기본 레이아웃 자동 폴백 |
| 기본값 초기화 확인 미완료 | 초기화 미실행 | 확인 다이얼로그 취소 처리 |
| 탭 최대 개수 초과 | "탭은 최대 5개까지 생성할 수 있습니다" | 탭 추가 차단 |
| 동일 위젯 중복 추가 (제한 위젯) | 토스트 "이미 추가된 위젯입니다" | 추가 차단 |
| 매니저 이하 접근 | 403 화면 | 접근 불가 안내 |

### 7. 연동 / 자동화 참조
| 연계 대상 | 트리거 | 영향 |
|---|---|---|
| 메인 대시보드(SCR-090) | 레이아웃 저장 | 저장 즉시 대시보드 반영 (캐시 무효화) |
| 전사 공유 레이아웃 배포 이벤트 | superAdmin [전사 공유] 저장 | 전체 Owner(지점장) 계정 기본 대시보드 레이아웃 교체 트리거 |
| 캐시 무효화 이벤트 | 수동 새로고침 버튼 클릭 | 전체 위젯 캐시 즉시 만료 + 재요청 |
| 레이아웃 내보내기 배치 | [PNG/PDF 내보내기] 클릭 | 캔버스 스냅샷 + 실시간 데이터 취합 후 파일 생성 다운로드 |
| NFR-19 알림 센터 | 전사 공유 레이아웃 배포 완료 | 대상 Owner(지점장) 계정 인앱 알림 "대시보드 레이아웃이 업데이트되었습니다" |
| 프리셋 FIFO 관리 배치 | 프리셋 최대 한도 초과 저장 시도 | 사용자에게 기존 프리셋 삭제 요청 (자동 삭제 안 함) |

### 8. 핵심 디자인 요청사항
- 공통 색상, 간격, 폰트, 토스트 규칙은 `_공통/디자인_시스템.md`, `_공통/토스트_메시지.md`를 따른다.
- 본 섹션에 별도 명시된 UI 상태와 권한 차이가 있으면 도메인 공통보다 화면 정의를 우선한다.

---


> **docs4 Scope**: V2
> **분류**: `V2`
> **분류 근거**: `HQ-11`: 914행 `HQ-11-03` 비교 막대 차트; `HQ-11-01`: 912행 `HQ-11-01` 비교 기준 설정; `HQ-11-02`: 913행 `HQ-11-02` 분석 실행; `HQ-11-03`: 914행 `HQ-11-03` 비교 막대 차트; `HQ-11-04`: 915행 `HQ-11-04` 백분위 표시; `HQ-11-05`: 916행 `HQ-11-05` 인사이트 요약; 외 1개


## SCR-H1003 벤치마크 비교

> 유형: 화면
> URL 경로: `/benchmark`
> 연결 기능: `HQ-11`

### 1. 목적
유사한 규모·업종의 다른 센터와 자사 지점의 KPI를 익명으로 비교 분석하는 화면입니다.
업계 평균 대비 우리 센터의 위치를 파악하여 경쟁력 강화 전략을 수립합니다.

### 2. UI 구성
### 비교 기준 설정
- 비교 대상 센터 규모 선택 (회원 수 기준 소 / 중 / 대)
- 비교 대상 업종 선택 (노출 순서 고정: 헬스 / 필라테스 / PT샵 / 골프 / 요가 / 크로스핏 / 복싱 / 수영 / 태권도 / 스피닝 / 기타)
- 비교 지표 선택 (매출 / 회원 유지율 / 출석률 / 신규 회원 등)
- 기간 선택 (월간 / 분기 / 연간)

### 벤치마크 차트
- 우리 지점 수치 vs 업계 평균 vs 상위 25% 비교 막대 차트
- 지표별 우리 지점의 백분위 표시 (예: "상위 35%")
- 개선 여지가 큰 지표 강조 표시

### 인사이트 요약
- 강점 지표 목록 (업계 평균 대비 우수한 항목)
- 개선 필요 지표 목록 (업계 평균 대비 미달 항목)
- 개선을 위한 액션 아이템 제안

### 3. 액션 / 흐름
1. 비교 기준(규모, 업종, 지표, 기간)을 선택한다.
2. 분석 실행 버튼 클릭 → 벤치마크 데이터 로드 및 차트 표시.
3. 지표별 차트를 확인하고 인사이트 요약을 검토한다.
4. 내보내기 버튼으로 결과를 PDF로 저장한다.

### 4. 핵심 상태
| 상태 | 설명 |
|---|---|
| 초기 | 비교 기준 설정 패널 표시, 차트 비어있음 |
| 로딩 | 벤치마크 데이터 수집 중 (처리 중 표시) |
| 결과 표시 | 차트 및 인사이트 표시 |
| 데이터 부족 | 비교 가능한 동일 규모 센터 데이터 부족 안내 |

### 5. 권한
| 계정 | 볼 수 있는 것 | 할 수 있는 것 |
|---|---|---|
| 슈퍼관리자 | 전체 지점 벤치마크 비교 | 비교 기준 설정, 내보내기 |
| Owner(지점장) | 소속 지점 벤치마크 | 비교 기준 설정 |
| 매니저 이하 | 접근 불가 | — |

### 6. 필수 예외처리
| 케이스 | 표시 | 후속 조치 |
|---|---|---|
| 비교 샘플 부족 | "비교 가능한 센터 데이터가 부족합니다. 비교 기준을 완화해보세요" | 기준 완화 권고, 결과 미노출 |
| 지표 미선택 분석 시도 | 인라인 "비교 지표를 1개 이상 선택해주세요" | 분석 차단 |
| 지표 최대 개수 초과 | 토스트 "지표는 최대 6개까지 선택 가능합니다" | 추가 선택 차단 |
| 분석 타임아웃(30초) | "데이터 수집에 시간이 걸리고 있습니다. 잠시 후 다시 시도해주세요" + 재시도 버튼 | 재시도 |
| 전월 데이터 없음 | 전월 비교 컬럼 "-" 표시 | 현재 결과 정상 표시 유지 |
| PDF 내보내기 실패 | 토스트 "PDF 생성에 실패했습니다. 다시 시도해주세요" | 재시도 안내 |
| 매니저 이하 접근 | 403 화면 | 접근 불가 안내 |
| 업종/규모 선택 없이 분석 시도 | 인라인 "규모와 업종을 선택해주세요" | 분석 차단 |
| 데이터 기준일 표시 누락 | 결과 화면 상단 "데이터 기준일" 자동 삽입 | 시스템 자동 처리 |

### 7. 연동 / 자동화 참조
| 연계 대상 | 트리거 | 영향 |
|---|---|---|
| 분석 결과 캐시 | 동일 조건 재실행 | 1시간 TTL 캐시 반환, 강제 새로고침 시 캐시 무효화 |
| 개선 여지 하이라이트 배치 | 분석 결과 생성 시 | 업계 평균 대비 20% 미달 지표 플래그 자동 설정 |
| PDF 생성 배치 | [PDF 다운로드] 클릭 | 차트 스냅샷 + 인사이트 텍스트 + 헤더 메타 취합 → PDF 파일 생성 |
| 샘플 임계치 검증 이벤트 | 분석 실행 전 | 풀 내 센터 수 카운트 → 5개 미만 시 분석 차단 플래그 |
| NFR-19 알림 센터 | 샘플 부족으로 분석 차단 | 운영 담당자에게 "벤치마크 풀 데이터 부족" 알림 |
| 전월 비교 데이터 조회 | 분석 실행 시 | 동일 조건 전월 결과 조회 → 증감 산출 → 결과 함께 렌더링 |
| 액션 아이템 매핑 배치 | 하이라이트 지표 확정 시 | 시스템 정의 액션 아이템 텍스트 매핑 → 인사이트 요약 패널 갱신 |

### 8. 핵심 디자인 요청사항
- 공통 색상, 간격, 폰트, 토스트 규칙은 `_공통/디자인_시스템.md`, `_공통/토스트_메시지.md`를 따른다.
- 본 섹션에 별도 명시된 UI 상태와 권한 차이가 있으면 도메인 공통보다 화면 정의를 우선한다.

---


> **docs4 Scope**: V2
> **분류**: `V2`
> **분류 근거**: `HQ-13`: 922행 `HQ-13-04` 응답 목록; `HQ-13-01`: 919행 `HQ-13-01` NPS 요약 카드; `HQ-13-02`: 920행 `HQ-13-02` NPS 추이 차트; `HQ-13-03`: 921행 `HQ-13-03` 응답 분포; `HQ-13-04`: 922행 `HQ-13-04` 응답 목록; `HQ-13-05`: 923행 `HQ-13-05` 키워드 분석; 외 3개


## SCR-H1005 NPS 설문

> 유형: 화면
> URL 경로: `/nps`
> 연결 기능: `HQ-13`

### 1. 목적
회원 대상 NPS(순추천지수) 설문 결과를 조회하고 분석하는 화면입니다.
회원 만족도 추이를 파악하고, 부정적 피드백을 빠르게 식별하여 서비스 개선에 활용합니다.

### 2. UI 구성
### NPS 요약 카드
- 전체 NPS 점수 (-100 ~ +100)
- 응답자 분류 비율 (추천자 / 중립자 / 비추천자)
- 전기간 대비 NPS 점수 변화

### NPS 추이 차트
- 기간별 NPS 점수 변화 선 차트
- 응답자 수 추이

### 응답 분포
- 0~10 점수별 응답 수 막대 차트
- 지점별 NPS 비교 테이블

### 응답 목록
- 설문 응답 목록 (점수, 응답일, 자유 의견, 회원 정보)
- 점수 범위 필터 (추천자 9~10 / 중립 7~8 / 비추천 0~6)
- 자유 의견 키워드 분석 (자주 등장하는 키워드 태그 클라우드)

### 설문 발송 관리
- NPS 설문 발송 주기 설정 (월 1회 / 분기 1회)
- 발송 대상 설정 (활성 회원 전체 / 특정 이용권 보유 회원)
- 발송 채널 선택 (Push / 카카오톡 / SMS)
- 최근 발송 이력 목록

### 3. 액션 / 흐름
1. 화면 진입 시 최근 분기 기준 NPS 결과를 불러온다.
2. 기간 필터 변경 → NPS 점수 및 차트 자동 갱신.
3. 비추천자 필터 선택 → 비추천 응답 목록만 표시하여 빠른 대응 가능.
4. 자유 의견 키워드 클릭 → 해당 키워드가 포함된 응답 목록 필터링.
5. `설문 발송` 탭에서 대상과 채널을 선택하고 발송한다.
6. 발송 후 최근 발송 이력에 캠페인을 추가하고 결과 탭으로 복귀한다.

### 4. 핵심 상태
| 상태 | 설명 |
|---|---|
| 기본 | NPS 요약 및 응답 목록 표시 |
| 로딩 | 데이터 불러오는 중 (스켈레톤 표시) |
| 응답 없음 | 선택 기간에 응답 없음 안내 |
| 설문 미발송 | 아직 설문을 발송한 이력이 없을 때 설문 설정 안내 |

### 5. 권한
| 계정 | 볼 수 있는 것 | 할 수 있는 것 |
|---|---|---|
| 슈퍼관리자 | 전체 지점 NPS 통합 및 지점별 비교 | 설문 발송 설정, 내보내기 |
| Owner(지점장) | 소속 지점 NPS 결과 | 설문 발송 설정 |
| 매니저 이하 | 접근 불가 | — |

### 6. 필수 예외처리
| 케이스 | 표시 | 후속 조치 |
|---|---|---|
| 선택 기간 내 응답 없음 | "선택한 기간에 응답이 없습니다" | 빈 상태 표시, 기간 조정 안내 |
| 설문 발송 이력 없음 | "발송 이력이 없습니다. 설문을 설정하고 발송해보세요" CTA | 발송 관리 패널 강조 |
| 발송 대상 0명 | "발송 대상 회원이 없습니다. 발송 조건을 확인해주세요" | 즉시 발송 차단 |
| 키워드 분석 응답 부족(10건 미만) | "키워드 분석에 필요한 응답이 부족합니다" | 클라우드 미노출 |
| 지점별 NPS 응답 0건 지점 | "-" 표시 | 전체 평균 산출 제외 |
| 낮은 점수 알림 발송 실패 | 재시도 3회 후 실패 시 관리자 인앱 알림 | 수동 대응 안내 |
| 엑셀 다운로드 대용량(1만 건 이상) | "다운로드에 시간이 걸릴 수 있습니다" 안내 | 백그라운드 처리 + 완료 알림 |
| 발송 주기 변경 스케줄 충돌 | "기존 발송 스케줄이 새 주기로 교체됩니다" 확인 다이얼로그 | 확정 시 기존 스케줄 교체 |
| FC가 회원 식별 정보 접근 시도 | 회원명·연락처 숨김 | 점수·의견만 표시 |
| PDF 내보내기 실패 | 토스트 "PDF 생성에 실패했습니다. 다시 시도해주세요" | 재시도 안내 |
| 중복 응답 제출 시도 | "이미 응답하신 설문입니다" (회원 앱 측 처리) | 최초 응답만 유효 처리 |
| 매니저 이하 접근 | 403 화면 | 접근 불가 안내 |

### 7. 연동 / 자동화 참조
| 연계 대상 | 트리거 | 영향 |
|---|---|---|
| 설문 발송 스케줄러 | 설정된 주기(월 1회/분기 1회) 도달 | 발송 대상 조회 → 선택 채널로 설문 링크 발송 |
| 낮은 점수 자동 알림 | 0~3점 응답 접수 시 | NFR-19 경유 Owner(지점장)·FC 인앱 알림 즉시 발송. 실패 시 재시도 3회 |
| 키워드 추출 배치 | 신규 자유 의견 응답 누적 시 | 형태소 분석 + 빈도 계산 → 키워드 클라우드 데이터 갱신 |
| NPS 점수 산출 배치 | 응답 수집 후 | 추천자/중립/비추천 분류 → NPS 공식 적용 → 요약 카드·추이 차트 데이터 갱신 |
| 응답 익명화 배치 | 응답 보존 기간(3년) 만료 | 회원 식별 정보 자동 익명화 처리 |
| 엑셀 다운로드 배치 | 대용량(1만 건 이상) 다운로드 요청 | 백그라운드 처리 → 완료 후 인앱 알림 + 다운로드 링크 |
| PDF 생성 배치 | [PDF 내보내기] 클릭 | 요약·차트·키워드 클라우드 스냅샷 + 헤더 메타 취합 → PDF 생성 |
| 세그먼트별 NPS 배치 | 세그먼트 필터 변경 시 | 해당 세그먼트 응답 재집계 → NPS 독립 산출 |
| 후속 조치 알림 | 비추천 응답 후속 조치 미등록 48시간 경과 | 담당 FC·Owner(지점장) 리마인드 알림 발송 |
| 발송 채널 응답률 통계 배치 | 매월 1일 03:00 | 채널별 발송 수·응답 수·응답률 집계 → 발송 관리 패널 통계 갱신 |

### 8. 핵심 디자인 요청사항
- 공통 색상, 간격, 폰트, 토스트 규칙은 `_공통/디자인_시스템.md`, `_공통/토스트_메시지.md`를 따른다.
- 본 섹션에 별도 명시된 UI 상태와 권한 차이가 있으면 도메인 공통보다 화면 정의를 우선한다.


> **docs4 Scope**: V2
> **분류**: `V2_UNMAPPED`
> **분류 근거**: 직접 매칭 코드 없음. V1 행 범위에 포함되지 않은 기타/보류 섹션입니다.
> **주의**: 체크리스트 V1 코드와 직접 매칭되지 않아 V2/보류 영역으로 배치했습니다.


## 다이얼로그
