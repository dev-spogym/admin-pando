# 운영정책

> **docs4 Scope 문서**
> - 원본: `docs2/D09-설정관리/운영정책.md`
> - 범위: `V2`
> - 생성 기준: `lastspr/v1checklist.xlsx` `3.CRM (admin)_제로스트_0518논의` 시트, V1=`4~751`행
> - 원칙: 원본 기획 내용을 임의 축소하지 않고, 섹션 내부 표/목록/문단 항목까지 V1/V2에 재배치했습니다.

> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


# D09-설정관리 설정관리 — 운영정책

> 개발사용 운영정책입니다. 이 파일만으로 해당 도메인의 운영 기준, 정책값, 예외 기준을 확인할 수 있도록 재구성했습니다.


> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT_SPLIT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 V2 전용 용어가 포함된 보조 섹션이라 항목 단위로 자동 분리했습니다. V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 1. 핵심 원칙

- 키오스크 단말 운영 화면, 장애 대응 콘솔, 원격 재시작, 로그 다운로드는 KIOSK 기획문서 범위다.


> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


## 3. 권한·책임 분리
- 본사 권한자는 통합 모드에서 전 지점 조회·정책 관리를 수행한다.
- 지점 권한자는 소속 지점 데이터의 일상 운영 처리를 수행한다.
- 변경·삭제·복구·정산·권한 변경은 화면별 최소 권한을 따른다.
- 공통 권한 기준은 `_공통/권한매트릭스.md`를 우선 참조하고, 화면별 차이는 도메인 문서에 따로 표기한다.


> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


## 4. KPI / 성과 측정
| KPI | 산식/의미 | 측정 주기 |
|---|---|---|
| 처리 완료율 | 완료 건수 / 대상 건수 | 일/주 단위 |
| 예외 발생률 | 예외·차단 건수 / 전체 시도 건수 | 주 단위 |
| 정책 준수율 | 권한·상태·필수값 위반 없이 처리된 비율 | 월 단위 |
| 운영 응답률 | 알림·요청 후 운영자가 처리한 비율 | 주 단위 |


> **docs4 Scope**: V2
> **분류**: `V2_SPLIT`
> **분류 근거**: `SET-05`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-01`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-02`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-03`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-04`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-05`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); 외 1개
> **주의**: 원본 섹션에 V1/V2 항목이 함께 있어 표/목록/문단 단위로 자동 분리했습니다. V1 코드와 V2 전용 용어가 같은 블록에 함께 있어 해당 블록 전체를 V2로 이동했습니다. / V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 5. 자동화 정책

| 자동화 | 실행 시점/트리거 | 처리 동작/결과 | 연결 기능 |
|---|---|---|---|
| 회원앱 / 키오스크 push | 센터명·로고·운영시간 저장 | 즉시 동기화 | `SET-01` |
| 알림 정책 적용 | 알림설정 저장 | 다음 알림부터 신규 정책 적용 | `SET-01` |
| 메시지 플랫폼 메타데이터 조회 | 알림설정 탭 진입/재조회 | 발신 프로필·템플릿 상태·예상 비용 기준 갱신 | `SET-01` |
| 물품 잔여 자동 계산 | 발급 트랜잭션 | 잔여 = 재고 - 오늘 발급 | `SET-01` |
| NFR-19 알림 | 물품 잔여 0개·운영시간 변경 | 운영자 알림 | `SET-01` |
| 테마 적용 | 화면 모드 저장 | 저장한 운영자 계정 화면에 즉시 반영. 센터 전체 브랜딩 컬러 변경은 센터 기본정보 저장 이벤트로 별도 반영 | `SET-01` |
| NFR-19 알림 센터 | 역할 삭제·민감 기능 부여 | 본사 슈퍼관리자에게 알림 | `SET-02` |
| SCR-097 히스토리 로그 | 역할 생성·삭제, 권한 템플릿 저장, 민감 권한 부여·회수 | 변경자, 대상 역할, 변경 전/후 권한, 영향 직원 수, 변경 사유 기록 | `SET-02` |
| 플랜 한도 임박 알림 | 회원 수 플랜 한도 90% 도달 | Owner(지점장)에게 업그레이드 권유 알림 | `SET-03` |
| 다운그레이드 데이터 초과 정리 배치 | 다운그레이드 적용일 자정 | 한도 초과 데이터(비활성 회원 우선) 자동 아카이빙 + 운영자 리포트 | `SET-03` |
| SCR-081 권한 설정 | 커스텀 역할 매트릭스 변경 | 양 화면 동기화 (단일 source-of-truth) | `SET-04` |
| 직원 활성 세션 | 권한 변경·재배정 | 다음 페이지 로드부터 신규 권한 적용 | `SET-04` |
| NFR-19 알림 센터 | 역할 삭제·민감 기능 부여 | 본사 슈퍼관리자에게 알림 | `SET-04` |
| SCR-097 히스토리 로그 | 커스텀 역할 매트릭스 변경, 역할 삭제·교체, 직원 재배정 | 변경자, 대상 역할, before/after 차이, 영향 직원 수, 변경 사유 기록 | `SET-04` |
| DLG-087-001 재배정 | 삭제 시 배정 직원 ≥ 1명 | 일괄 재배정 트랜잭션 | `SET-04` |
| 키오스크 언어 동기화 | 언어 설정 저장 이벤트 | 연결된 모든 키오스크 기기에 WebSocket 또는 polling으로 언어 설정 즉시 푸시 | `SET-05` |
| 관리자 화면 i18n 리로드 | 언어 설정 저장 후 | 클라이언트 i18n 번들 새로 로드 (페이지 리로드) | `SET-05` |
| 타임존 기반 UI 변환 | 타임존 설정 저장 이벤트 | 이후 모든 날짜·시각 표시를 새 타임존 offset으로 계산 | `SET-05` |
| 키오스크 설정 동기화 | SCR-082/SCR-082A 저장 | 출석 방식·화면 구성·출입 규칙 설정을 연결된 키오스크가 다음 동기화 시점에 수신 | `SET-IOT-01` |
| IoT 기기 설정 동기화 | SCR-083 저장 | 출입 게이트 / 키오스크 / 락커 컨트롤러 / InBody 측정기 4종의 설정값과 상태 표시 기준 갱신 | `SET-IOT-02` |
| 락커/사물함/운동복 자동 회수 정책 적용 | SCR-080A 저장 | D06 락커/사물함/운동복 화면의 자동 회수 실행 여부 갱신. 만료 경과 자동 회수 OFF, 이용권 만료 이벤트 자동 회수 OFF, 일일 사물함 22:00 상태 갱신 ON 고정, 일일 사물함 실제 일괄 회수 OFF, 운동복 일일 자동 만료/강제 회수 OFF가 기본값 | `SET-EXT-01` |
| SCR-097 히스토리 로그 | 지점 정책/스텝 ON/OFF, 지점 추가 step, 기기 설정, 직원 근태 기준, 백업 설정 저장 | 변경자, 대상 지점, 정책 유형, 변경 전/후 값, 변경 사유 기록 | `SET-EXT-01`, `SET-IOT-02`, `SET-EXT-05`, `SET-06` |


> **docs4 Scope**: V2
> **분류**: `V2_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: V2 구현에도 공통으로 필요한 보조 섹션입니다.


## 6. 정책 결정 근거
- 화면별 구현 판단은 `UI 구성`, `액션 / 흐름`, `권한`, `필수 예외처리` 순서로 확인한다.
- 여러 항목 값이 충돌하면 docs2 문서 안에서 하나의 기준값으로 보정해 반영한다.
- 키오스크 운영 화면은 D09에 만들지 않는다. D09는 설정값 관리만 담당하며, 운영 관제는 KIOSK 기획문서 범위다.
- IoT 기기 종류는 4종으로 고정하고, 문서 내 다른 표현이 있으면 출입 게이트 / 키오스크 / 락커 컨트롤러 / InBody 측정기로 정규화한다.
- 클라이언트 운영 정책값이 확정되지 않은 항목은 `docs2/_정책미확정_확인필요.md`에 별도 관리한다.


> **docs4 Scope**: V2
> **분류**: `V2_SPLIT`
> **분류 근거**: `SET-05`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-01`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-02`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-03`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-04`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); `SET-05-05`: 1024행 `SET-05` 다국어 설정 (시스템 언어·국가·자동 번역); 외 1개
> **주의**: 원본 섹션에 V1/V2 항목이 함께 있어 표/목록/문단 단위로 자동 분리했습니다. V1 코드와 V2 전용 용어가 같은 블록에 함께 있어 해당 블록 전체를 V2로 이동했습니다. / V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 7. 운영 예외 처리

| 케이스 | 처리 방식 | 후속 조치 | 연결 기능 |
|---|---|---|---|
| 센터명 빈값 | "센터명은 필수입니다" 인라인 | 저장 차단 | `SET-01` |
| 사업자번호 형식 오류 | "10자리 숫자 입력" | 저장 차단 | `SET-01` |
| 사업자번호 외부 검증 실패 | "검증 실패, 형식만 확인됨" | 진행 가능 | `SET-01` |
| superAdmin 선택 | 매트릭스 잠금 + "수정 불가" 안내 | 저장 버튼 hidden | `SET-02` |
| 플랜 없음 (미가입) | "플랜을 선택해주세요" + 플랜 카드 CTA | 플랜 선택 유도 | `SET-03` |
| 결제 수단 없음 | "결제 수단을 등록해주세요" 인라인 안내 | 결제 수단 등록 유도 | `SET-03` |
| 결제 실패 (grace period 중) | 상단 경고 배너 + D-day 카운트다운 | 결제 수단 교체 유도 | `SET-03` |
| 다운그레이드 데이터 한도 초과 | DLG-084-001 경고 + 초과 데이터 정리 링크 | 사용자가 정리 후 재시도 또는 경고 수락 | `SET-03` |
| 이름 빈값 | "역할 이름은 필수입니다" 인라인 | 저장 차단 | `SET-04` |
| 이름 30자 초과 | "30자 이내 입력해주세요" | 저장 차단 | `SET-04` |
| 기본 역할명 중복 | "기본 역할명과 중복됩니다" | 저장 차단 | `SET-04` |
| 지점 내 이름 중복 | "이미 사용 중인 이름입니다" | 저장 차단 | `SET-04` |
| 복사 베이스 변경 시 편집 폐기 | "현재 편집 내용이 사라집니다" 모달 | 확인 / 취소 | `SET-04` |
| 키오스크 기본 언어 비활성화 시도 | "기본 언어는 비활성화 불가 — 먼저 기본 언어 변경" 인라인 에러 | 기본 언어 변경 유도 | `SET-05` |
| 키오스크 활성 언어 전체 제거 시도 | "최소 1개 언어 필요" 에러 | 제거 차단 | `SET-05` |
| 타임존 변경 | "기존 예약 시각 재표시됩니다" 확인 모달 | 확인 후 저장 처리 | `SET-05` |
| 번역 부족 키 존재 | 설정 화면 상단 경고 배지 "{개수}개 키 미번역" | 번역 데이터 보완 안내 | `SET-05` |
| 텍스트 길이 초과 (키오스크 미리보기) | 해당 언어 배지에 경고 아이콘 + "레이아웃 검토 필요" | 번역 문구 단축 유도 | `SET-05` |
| 지원하지 않는 기기 종류 등록 | "지원 기기 종류는 4종입니다" | 출입 게이트 / 키오스크 / 락커 컨트롤러 / InBody 측정기 중 선택 | `SET-IOT-02` |
| 키오스크 운영 콘솔 진입 요청 | D09에서는 제공하지 않음 | KIOSK 기획문서 기준 화면 안내 | `SET-IOT-01` |
| 본사 정책 세트 0개 | 드롭다운 비활성 + "본사 관리자에게 문의" 안내 | 정책 세트 등록 요청 | `SET-EXT-01` |
| 정책 충돌 감지 | 충돌 행 빨간 배지 + DLG-080A-001 충돌 목록 | 본사 기준일은 유지하고 지점 허용값만 조정 | `SET-EXT-01` |
| 시뮬레이션 발송 대상 0명 | "발송 대상 없음 — 정책 조건 확인" 안내 | 조건 재검토 유도 | `SET-EXT-01` |
