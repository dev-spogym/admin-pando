# 운영정책

> **docs4 Scope 문서**
> - 원본: `docs2/D09-설정관리/운영정책.md`
> - 범위: `V1`
> - 생성 기준: `lastspr/v1checklist.xlsx` `3.CRM (admin)_제로스트_0518논의` 시트, V1=`4~751`행
> - 원칙: 원본 기획 내용을 임의 축소하지 않고, 섹션 내부 표/목록/문단 항목까지 V1/V2에 재배치했습니다.

> **docs4 Scope**: V1
> **분류**: `V1_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 같은 원본 파일의 V1 구현 이해에 필요한 공통/운영 보조 섹션입니다.


# D09-설정관리 설정관리 — 운영정책

> 개발사용 운영정책입니다. 이 파일만으로 해당 도메인의 운영 기준, 정책값, 예외 기준을 확인할 수 있도록 재구성했습니다.


> **docs4 Scope**: V1
> **분류**: `V1_SUPPORT_SPLIT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 V2 전용 용어가 포함된 보조 섹션이라 항목 단위로 자동 분리했습니다. V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 1. 핵심 원칙

- 본사와 지점, 관리자 웹과 회원앱의 권한 구조를 실제 운영 기준으로 정리한다.

- 문서마다 달랐던 역할 명칭을 운영 기준으로 하나로 맞춘다.

- 관리자 웹 표준 역할 코드는 `superAdmin`, `primary`, `owner`, `manager`, `fc`, `trainer`, `staff`, `readonly` 8개를 기준으로 한다. `owner`의 화면 표시명은 `Owner(지점장)`으로 통일한다.

- `trainer`는 관리자 웹 기본 역할이다. PT트레이너, GX강사, 골프강사는 직원 직무값이며 기본 권한 템플릿은 `trainer`로 지정한다.

- 카운터/현장 지원 담당자는 별도 표준 역할로 두지 않고 `staff`에 통일한다.

- 권한은 "페이지 접근", "조회", "수정", "승인", "일괄처리"로 나눠서 본다.

- 전사 권한 템플릿

- 공통 상품 마스터

- 자동화 정책 세트

- 알림 템플릿

- 공지 템플릿

- KPI 및 감사 기준

- 회원/리드 실제 운영

- 현장 결제와 미수금 처리

- 수업 개설과 스케줄 운영

- 시설 배정

- 본사 정책 범위 안에서의 적용 설정

- 전사 환불 규모는 환불 집중 지점을 조기 포착하는 경고 지표로 본다.

- 키오스크와 IoT의 관리자 설정 화면은 D09가 책임진다.

- CRM에서 설정 가능한 기기 종류는 출입 게이트 / 키오스크 / 락커 컨트롤러 / InBody 측정기 4종으로 고정한다.

- 카드 리더와 얼굴인식 카메라는 별도 기기 종류가 아니라 출입 게이트 또는 키오스크의 인증 수단/부속 모듈로 관리한다.

- 수업/회원 출석 지각 기준과 직원 근태 지각 기준은 별도 정책값이다. 회원·수업 출석 기준값은 미설정을 허용하며, 값이 비어 있으면 해당 자동 분류·자동 알림·자동 노쇼 처리를 실행하지 않고 수동 처리 대상으로 남긴다. 직원 지각 기준은 소속 지점 Owner(지점장)이 `SCR-086 출석 관리 설정`에서 설정한 직원 지각 허용 시간을 따른다. 기본값은 10분이며, 정규 출근 시각 + 10분까지는 정상, 10분 초과 출근부터 지각으로 분류한다.

- 락커 만료 알림 step과 실물 자산 자동 회수 실행 정책은 분리한다. 알림 step은 본사 정책 + 지점 추가 step 구조이고, 락커/사물함/운동복 자동 회수 실행은 `SCR-080A 지점 자동화 적용`의 지점 운영 정책값으로 Owner(지점장)이 제어한다.

- 권한, 역할, 정책, 자동화, 기기, 백업 설정 변경은 모두 `SCR-097 히스토리 로그` 기록 대상이다. 변경 이력에는 변경자, 대상 지점/역할/정책, 변경 전 값, 변경 후 값, 변경 사유, 변경 일시를 남기며, D09에서는 변경을 수행하고 D10 `SCR-097 히스토리 로그`에서 조회한다.


> **docs4 Scope**: V1
> **분류**: `V1`
> **분류 근거**: `SET-01`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-05`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품)


## 2. 상태 / 운영 정의
- 앱 푸시 | 입장 알림·만료 임박·결제 완료·수업 예약·수업 취소 (5종) |
- 이메일 | 주간 리포트·만료 예정 안내 (2종) |
- SMS | 결제 완료·만료 임박 (2종) |
- 변경 사항이 있을 때만 활성화. 미저장 + 탭 이동 시 DLG-080-001.
- | SET-01-05-02 | 저장 버튼 활성 |
- **신규 매니저 영입 시 권한 점검**: 신규 매니저가 회원 영구 삭제·환불 권한이 활성인지 확인 후 회수 → 사고 사전 차단
- **직원 사고 발생 후 권한 즉시 잠금**: 특정 역할의 위험 메뉴 일괄 거부 → 저장 전 영향 안내로 영향 직원·담당 회원 수 확인
- **권한 초기화 (DLG-081-001)**: 잘못 변경한 역할을 마지막 저장 상태로 복원
- [초기화] (DLG-081-001 마지막 저장 상태로)
- [변경 사항 저장] (변경 시 활성)
- **신규 센터 온보딩**: 무료 체험 기간 종료 전 플랜 선택 → 결제 수단 등록 → 자동 갱신 활성
- **결제 실패 grace period 관리**: 결제 수단 만료 등으로 자동 갱신 실패 → grace period(7일) 내 수단 교체 유도
- **프랜차이즈 본사 다중 지점 관리**: 본사 superAdmin이 지점별 구독 상태 일괄 조회 + 플랜 일괄 변경 요청
- 현재 플랜 강조 | 배지 |
- 상태 | 결제 완료 / 실패 / 환불 / 미수 |
- **시간제 강사 역할**: trainer 복사 후 수업·출석만 활성 → 재무 정보 차단
- **신입 인턴 한정 권한**: 기본 매트릭스에서 [조회]만 활성 → 1개월 후 정식 역할로 전환
- **프로젝트 PM (한시적)**: 메시지·공지만 활성 → 프로젝트 종료 시 역할 삭제 + 직원 재배정
- 본사 슈퍼관리자 | 시간제 강사 표준 역할 | 복사 베이스 trainer → 수업·출석만 활성 → 재무 그룹 [전체 거부] → 저장 | 시간제 강사용 표준 역할 보급 |
- 본사 슈퍼관리자 | 신입 인턴 1개월 한정 | 매트릭스 [조회]만 전체 활성 → 저장 → 1개월 후 [삭제] → DLG-087-001로 정식 매니저 재배정 | 인턴 권한 통제 + 정식 전환 |


> **docs4 Scope**: V1
> **분류**: `V1_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 같은 원본 파일의 V1 구현 이해에 필요한 공통/운영 보조 섹션입니다.


## 3. 권한·책임 분리
- 본사 권한자는 통합 모드에서 전 지점 조회·정책 관리를 수행한다.
- 지점 권한자는 소속 지점 데이터의 일상 운영 처리를 수행한다.
- 변경·삭제·복구·정산·권한 변경은 화면별 최소 권한을 따른다.
- 공통 권한 기준은 `_공통/권한매트릭스.md`를 우선 참조하고, 화면별 차이는 도메인 문서에 따로 표기한다.


> **docs4 Scope**: V1
> **분류**: `V1_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 같은 원본 파일의 V1 구현 이해에 필요한 공통/운영 보조 섹션입니다.


## 4. KPI / 성과 측정
| KPI | 산식/의미 | 측정 주기 |
|---|---|---|
| 처리 완료율 | 완료 건수 / 대상 건수 | 일/주 단위 |
| 예외 발생률 | 예외·차단 건수 / 전체 시도 건수 | 주 단위 |
| 정책 준수율 | 권한·상태·필수값 위반 없이 처리된 비율 | 월 단위 |
| 운영 응답률 | 알림·요청 후 운영자가 처리한 비율 | 주 단위 |


> **docs4 Scope**: V1
> **분류**: `V1_SPLIT`
> **분류 근거**: `SET-01`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-01`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-02`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-03`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-04`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-05`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); 외 33개
> **주의**: 원본 섹션에 V1/V2 항목이 함께 있어 표/목록/문단 단위로 자동 분리했습니다. V1 코드와 V2 전용 용어가 같은 블록에 함께 있어 해당 블록 전체를 V2로 이동했습니다. / V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 5. 자동화 정책

| 자동화 | 실행 시점/트리거 | 처리 동작/결과 | 연결 기능 |
|---|---|---|---|
| 일별 물품 잔여 리셋 배치 | 매일 00:00 | 1일 발급 카운터 리셋 | `SET-01` |
| SCR-087 커스텀 역할 | 커스텀 역할 권한 편집 | 양 화면 동기화 (단일 source-of-truth) | `SET-02` |
| SET-04 영향 분석 | 역할 삭제 트리거 | 배정 직원·담당 회원 카운트 자동 산정 | `SET-02` |
| primary/superAdmin 권한 부여 | 저장 시점 | 운영팀 메일·슬랙 알림 | `SET-02` |
| 동시 편집 lock | 진입 시 30초 lock 획득 | 다른 운영자 진입 차단 | `SET-02` |
| 일별 권한 점검 배치 | 매일 04:00 | 비활성 직원의 잔여 권한 회수 후보 보고 | `SET-02` |
| 결제 실패 알림 | 자동 갱신 실패 이벤트 | Owner(지점장)에게 이메일·SMS·앱 푸시 발송 (grace period D-day 포함) | `SET-03` |
| grace period 만료 배치 | 결제 실패 후 7일 경과 | 서비스 상태 → "일시 정지" 전환, 전역 배너 활성 | `SET-03` |
| 미수 복구 이벤트 | 결제 수단 교체 + 재결제 성공 | 서비스 상태 → "정상" 복구, 배너 해제 | `SET-03` |
| 데이터 삭제 배치 | 구독 해지 후 90일 경과 | 센터 데이터 영구 삭제 (삭제 7일 전 경고 메일) | `SET-03` |
| 체험 종료 알림 배치 | 무료 체험 만료 D-7, D-3, D-1 | 플랜 선택 유도 이메일 + 앱 알림 | `SET-03` |
| 동시 편집 lock | 진입 시 30초 lock | 다른 운영자 진입 차단 | `SET-04` |
| 일별 미사용 역할 점검 | 매일 04:00 | 30일 이상 배정 0명 역할 보고 | `SET-04` |
| 역할 사용 통계 배치 | 매주 월요일 04:00 | 역할별 배정 추이 집계 | `SET-04` |
| 직원 근태 기준 적용 | SCR-086 직원 지각 허용 시간 저장. 기본값 10분 | D07 직원 근태 관리의 신규 출퇴근 자동 분류 기준 갱신 | `SET-EXT-05` |


> **docs4 Scope**: V1
> **분류**: `V1_SUPPORT`
> **분류 근거**: 직접 매칭 코드 없음. 같은 원본 파일의 구현 기준을 이해하기 위한 보조 섹션입니다.
> **주의**: 직접 매칭 코드는 없지만 같은 원본 파일의 V1 구현 이해에 필요한 공통/운영 보조 섹션입니다.


## 6. 정책 결정 근거
- 화면별 구현 판단은 `UI 구성`, `액션 / 흐름`, `권한`, `필수 예외처리` 순서로 확인한다.
- 여러 항목 값이 충돌하면 docs2 문서 안에서 하나의 기준값으로 보정해 반영한다.
- 키오스크 운영 화면은 D09에 만들지 않는다. D09는 설정값 관리만 담당하며, 운영 관제는 KIOSK 기획문서 범위다.
- IoT 기기 종류는 4종으로 고정하고, 문서 내 다른 표현이 있으면 출입 게이트 / 키오스크 / 락커 컨트롤러 / InBody 측정기로 정규화한다.
- 클라이언트 운영 정책값이 확정되지 않은 항목은 `docs2/_정책미확정_확인필요.md`에 별도 관리한다.


> **docs4 Scope**: V1
> **분류**: `V1_SPLIT`
> **분류 근거**: `SET-01`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-01`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-02`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-03`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-04`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); `SET-01-05`: 57행 `SET-01` 센터 설정 (기본 정보 + 알림 + 테마 + 물품); 외 27개
> **주의**: 원본 섹션에 V1/V2 항목이 함께 있어 표/목록/문단 단위로 자동 분리했습니다. V1 코드와 V2 전용 용어가 같은 블록에 함께 있어 해당 블록 전체를 V2로 이동했습니다. / V2 전용 용어가 포함된 블록을 V2로 이동했습니다.


## 7. 운영 예외 처리

| 케이스 | 처리 방식 | 후속 조치 | 연결 기능 |
|---|---|---|---|
| 센터명 50자 초과 | 인라인 카운터 | 저장 차단 | `SET-01` |
| 로고 5MB 초과 | "5MB 이내" | 업로드 차단 | `SET-01` |
| 권한 충돌 감지 | 인라인 경고 + 저장 시 DLG-081-002 | 충돌 무시 저장 / 수정 | `SET-02` |
| 민감 기능 체크 (superAdmin/primary 외) | 셀 색·아이콘 강조 + 저장 시 강조 카드 | 민감 권한 저장 차단 + superAdmin/primary 권한 부여 요청 안내 | `SET-02` |
| DLG-081-006 영향 직원 0명 | "영향 없음" 안내 | [그대로 진행] 즉시 통과 | `SET-02` |
| 역할 삭제 시 직원 배정 | DLG-081-006 → 재배정 강제 | 재배정 역할 선택 후 [삭제] | `SET-02` |
| grace period 만료 → 서비스 정지 | 전역 "서비스 일시 정지" 배너 | 결제 수단 교체 후 즉시 복구 | `SET-03` |
| Owner(지점장) 외 스텝 OFF 시도 | 토글 disabled + "Owner(지점장)만 변경 가능" 툴팁 | Owner(지점장) 권한 필요 안내 | `SET-EXT-01` |
| 테스트 발송 채널 오류 | "테스트 발송 실패 — 플랫폼 연동 상태 확인" 토스트 | 플랫폼 연동 링크 제공 | `SET-EXT-01` |
