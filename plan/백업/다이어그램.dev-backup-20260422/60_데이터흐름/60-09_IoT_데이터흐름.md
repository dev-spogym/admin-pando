---
diagramId: DF09
title: IoT(출입/체성분) 데이터 흐름
type: erDiagram+flowchart
scope: IoTDevice, AccessLog, BodyComposition
actors: [member, IoT, system]
tcMappings: [TC-DF09-01, TC-DF09-02]
lastUpdated: 2026-04-20
---

## 1. 엔티티 개요

IoT 장치(`IoTDevice`: 키오스크/밴드/카드/InBody)가 이벤트를 발행하면 출입/출석/체성분 데이터가 자동 적재된다. 외부 시스템 연동.

## 2. ER 다이어그램

```mermaid
erDiagram
  IoTDevice ||--o{ AccessLog : 발행
  AccessLog }o--|| Member : 매칭
  BodyComposition }o--|| Member : 소유
  BodyComposition }o--|| IoTDevice : 측정장치
  AccessLog }o--|| Branch : 지점
```

## 3. 쓰기 경로 (출입 이벤트)

```mermaid
flowchart LR
  Device[[IoT 장치]] -->|Webhook/MQTT| Gateway[IoT Gateway]
  Gateway -->|E_DF09_01| API[POST /api/iot/access]
  API --> Verify{장치 인증}
  Verify -->|실패| Reject[/403/]
  Verify -->|OK| Match[(회원 매칭: card_id/band_id)]
  Match -->|매칭| DB1[(INSERT access_logs)]
  DB1 --> DB2[(UPDATE members SET last_visit)]
  DB2 --> DB3[(INSERT attendances)]
  DB3 --> Evt[이벤트: access.granted]
  Evt --> RT[실시간 대시보드 push]
  Match -->|미매칭| Anon[(INSERT access_logs type=ANONYMOUS)]

  classDef external fill:#ECEFF1,stroke:#455A64,stroke-dasharray:3 3
  classDef api fill:#EDE7F6,stroke:#5E35B2
  classDef db fill:#E8F5E9,stroke:#2E7D32
  class Device,Gateway external
  class API api
  class DB1,DB2,DB3 db
```

## 4. 체성분 자동 반영

```mermaid
flowchart LR
  InBody[[InBody 측정기]] -->|Webhook| API[POST /api/iot/body-composition]
  API --> DB1[(INSERT body_compositions)]
  DB1 --> Thresh{임계치 체크}
  Thresh -->|초과| Notif[FC 자동 알림 + 상담 트리거]
  DB1 --> UI[Tab08 체성분 갱신]
```

## 5. 주요 필드

| 필드 | 비고 |
|------|------|
| iot_devices.type | KIOSK/BAND/CARD/INBODY |
| iot_devices.public_key | 장치 인증 |
| access_logs.gate_id | 출입문 식별 |
| body_compositions.weight/bfr/smm | InBody 지표 |

## 6. 인덱스/제약

- `INDEX(access_logs.member_id, recorded_at DESC)`
- `INDEX(body_compositions.member_id, measured_at DESC)`
- 장치 JWT/공개키 인증 필수

## 7. TC 후보

| TC ID | 타입 | 설명 |
|-------|:----:|------|
| TC-DF09-01 | positive | 밴드 태깅 → 출입 + 출석 동시 기록 |
| TC-DF09-02-NEG | negative | 미등록 밴드 → ANONYMOUS 로그 |
| TC-DF09-03-EXC | exception | 장치 인증 실패 → 403 + 경보 |
| TC-DF09-04 | positive | 체성분 임계치 초과 → FC 알림 (X22) |
