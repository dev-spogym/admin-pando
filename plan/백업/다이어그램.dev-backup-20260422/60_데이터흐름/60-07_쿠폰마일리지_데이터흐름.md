---
diagramId: DF07
title: 쿠폰/마일리지 데이터 흐름
type: erDiagram+flowchart
scope: Coupon, CouponIssue, MileageTxn
actors: [manager, member, system]
tcMappings: [TC-DF07-01, TC-DF07-02]
lastUpdated: 2026-04-20
---

## 1. 엔티티 개요

쿠폰(`Coupon`)은 템플릿 기반으로 회원에게 발행(`CouponIssue`)되고 사용 시 결제에 할인 적용. 마일리지(`MileageTxn`)는 적립/사용 트랜잭션 로그. S10 CouponStatus 참조.

## 2. ER 다이어그램

```mermaid
erDiagram
  Coupon ||--o{ CouponIssue : 발행
  CouponIssue }o--|| Member : 수령
  CouponIssue }o--o| Sale : 사용
  Member ||--o{ MileageTxn : 거래
  MileageTxn }o--o| Sale : 연결
```

## 3. 쓰기 경로 (쿠폰 발행)

```mermaid
flowchart LR
  UI[SCR-073 쿠폰관리] -->|E_DF07_01| API[POST /api/coupons/issues]
  API --> DB1[(INSERT coupon_issues x N)]
  DB1 --> DB2[(UPDATE coupons.remaining_quota)]
  DB2 --> Notif[일괄 메시지 발송]
```

## 4. 사용 플로우

```mermaid
flowchart LR
  POS[SCR-S002 POS] -->|쿠폰 적용| API[POST /api/coupons/:issueId/apply]
  API --> Valid{유효성}
  Valid -->|만료| Rej[/410/]
  Valid -->|사용완료| Rej2[/409/]
  Valid -->|OK| DB[(UPDATE coupon_issues SET used_at, sale_id)]
  DB --> PayCalc[할인 반영]
```

## 5. 주요 필드

| 필드 | 비고 |
|------|------|
| coupon.discount_type | FIXED / PERCENT |
| coupon.min_amount | 최소 결제액 |
| coupon.valid_from / valid_to | 기간 |
| coupon_issue.status | S10 |
| mileage_txn.kind | EARN / USE / EXPIRE / ADJUST |

## 6. 인덱스/제약

- `INDEX(coupon_issues.member_id, status)`
- `INDEX(mileage_txns.member_id, created_at DESC)`
- 합계 계산은 뷰/머터리얼라이즈드 뷰 권장

## 7. TC 후보

| TC ID | 타입 | 설명 |
|-------|:----:|------|
| TC-DF07-01 | positive | 쿠폰 발행 → 배포 → 사용 전 생명주기 |
| TC-DF07-02-NEG | negative | 만료된 쿠폰 사용 시 거부 |
| TC-DF07-03 | boundary | 마일리지 음수 잔액 방지 |
