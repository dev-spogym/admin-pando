---
diagramId: DF04
title: 수업 데이터 흐름
type: erDiagram+flowchart
scope: Lesson, Reservation, Schedule, Signature
actors: [fc, member, manager, system]
tcMappings: [TC-DF04-01, TC-DF04-02, TC-DF04-03]
lastUpdated: 2026-04-20
---

## 1. 엔티티 개요

수업(`Lesson`)은 반복 스케줄(`Schedule`)로부터 생성되며, 예약(`Reservation`)-체크인-진행-서명-횟수차감 플로우를 거친다. S5/S6 상태 참조.

## 2. ER 다이어그램

```mermaid
erDiagram
  Schedule ||--o{ Lesson : 인스턴스생성
  Lesson ||--o{ Reservation : 예약
  Lesson ||--o| Signature : 서명
  Lesson }o--|| Staff : 강사
  Reservation }o--|| Member : 예약자
  Reservation ||--o| Waitlist : 대기열🆕
  Signature }o--|| Membership : 횟수차감
  Lesson }o--|| LessonTemplate : 템플릿
```

## 3. 쓰기 경로 (서명→차감)

```mermaid
flowchart LR
  DLG[DLG-C006 서명] -->|E_DF04_01| API[POST /api/lessons/:id/sign]
  API -->|E_DF04_02| Txn[(BEGIN)]
  Txn --> DB1[(INSERT signatures)]
  DB1 --> DB2[(UPDATE lessons SET status=COMPLETED)]
  DB2 --> DB3[(UPDATE memberships SET remaining_count -= 1)]
  DB3 --> Evt[이벤트: lesson.signed]
  Evt --> Audit[(audit_logs)]
  Evt --> Notif[회원 알림]
  Txn --> Commit[(COMMIT)] --> Resp[200 OK] --> UI[Tab10 레슨 갱신]

  classDef api fill:#EDE7F6,stroke:#5E35B2
  classDef db fill:#E8F5E9,stroke:#2E7D32
  class API api
  class DB1,DB2,DB3,Audit db
```

## 4. 읽기 경로 (캘린더)

```mermaid
flowchart LR
  UI[SCR-C001 캘린더] -->|GET /api/lessons?from,to,staffId| DB[(SELECT lessons JOIN reservations JOIN members)]
  DB --> Resp[응답: 일정 목록]
  Resp --> UI
  UI -->|드래그앤드롭| API[PATCH /api/lessons/:id]
  API --> DB2[(UPDATE lessons)]
  DB2 --> Notif[관련자 알림]
```

## 5. 주요 필드

| 필드 | 테이블 | 비고 |
|------|--------|------|
| id | lessons | PK |
| schedule_id | lessons | FK (반복 스케줄) |
| staff_id | lessons | FK 강사 |
| start_at / end_at | lessons | timestamp |
| status | lessons | S5 |
| type | lessons | PT/GROUP/OT |
| capacity | lessons | 정원 |
| reserved_count | lessons | 예약수 |

## 6. 인덱스/제약

- `INDEX(staff_id, start_at)` — 강사별 일정 조회
- `INDEX(start_at, status)` — 캘린더 쿼리
- `UNIQUE(lesson_id, member_id)` on reservations — 중복 예약 방지

## 7. TC 후보

| TC ID | 타입 | 설명 |
|-------|:----:|------|
| TC-DF04-01 | positive | 서명 시 이용권 횟수 차감 |
| TC-DF04-02-NEG | negative | 만료 이용권으로 서명 시도 시 거부 |
| TC-DF04-03-EXC | exception | 동시 서명 경쟁 → 한 번만 차감 |
| TC-DF04-04 | positive | 노쇼 발생 시 페널티 자동 생성 (A05 연결) |
