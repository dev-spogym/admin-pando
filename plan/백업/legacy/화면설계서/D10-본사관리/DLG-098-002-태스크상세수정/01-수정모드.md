# DLG-098-002 태스크상세수정 — 상태: 수정 모드 (manager 이상)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-098-002 |
| 상태 코드 | `edit-mode` |
| 트리거 | SCR-098 태스크 행 클릭 (primary/owner/manager) |
| 역할 | primary, owner, manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-098 정상 |
| 다음 상태 | [저장] → `03-수정성공` / [삭제] → `04-삭제확인` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-098-002 태스크 상세/수정 — 수정 모드

E_D98_02_M1_02: primary/owner/manager → 수정 모드 (전체 필드 편집 가능)

Modal 레이아웃 (max-w-lg):
- 헤더: "태스크 수정" + [X]
- 우측 상단: [삭제] Button (variant="ghost", text-red-500)

[폼 필드 7개 — 기존 값 프리필]
1. 제목 * — Input (기존 값)
2. 카테고리 * — Select (기존 값)
3. 우선순위 * — RadioGroup (기존 값)
4. 상태 * — Select:
   옵션: pending(대기) / in_progress(진행중) / completed(완료)
   ⚠️ completed → pending/in_progress 변경 시 경고 토스트:
   "완료된 태스크는 되돌릴 수 없습니다." (E_D98_02_M2_04)
5. 마감일 — DatePicker (기존 값, 날짜 변경 가능)
6. 담당자 — Combobox (기존 값, 변경 가능)
7. 메모 — Textarea (기존 값, 최대 500자)

프리필:
useEffect(() => {
  if (task) {
    setValue('title', task.title)
    setValue('category', task.category)
    setValue('priority', task.priority)
    setValue('status', task.status)
    setValue('dueDate', task.due_date)
    setValue('assignedTo', task.assigned_to)
    setValue('memo', task.memo)
  }
}, [task])

푸터:
- [취소] (variant="outline")
- [저장] (primary)

TC-098-DLG-005: fc 역할 클릭 → 읽기 전용 (02-읽기전용 상태로 분기)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D98_02_M1_02`: primary/owner/manager 역할로 태스크 행 클릭

### 역할별 분기
- primary/owner/manager → 수정 모드 (`01-수정모드`)
- fc/staff → 읽기 전용 (`02-읽기전용`)

### 필드 검증 (M2)
| 필드 | 규칙 |
|------|------|
| 제목 | 필수, 100자 |
| 카테고리 | 필수 |
| completed→pending | 경고 toast |
| 메모 | 500자 |

### 인터랙션 (User Actions)
1. [저장] → `03-수정성공`
2. [삭제] → `04-삭제확인` 다이얼로그
3. [취소] / ESC → 닫힘

### 연결 화면
- 이전: SCR-098 정상
- 다음: `03-수정성공` / `04-삭제확인` / 취소 → SCR-098
