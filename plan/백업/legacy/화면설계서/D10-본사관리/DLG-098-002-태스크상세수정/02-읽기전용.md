# DLG-098-002 태스크상세수정 — 상태: 읽기 전용 (fc/staff)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-098-002 |
| 상태 코드 | `view-mode` |
| 트리거 | SCR-098 태스크 행 클릭 (fc/staff) |
| 역할 | fc, staff |
| 우선순위 | P1 |
| 이전 상태 | SCR-098 정상 |
| 다음 상태 | 완료 체크 → `05-완료처리` / 닫힘 → SCR-098 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-098-002 태스크 상세 — 읽기 전용 (fc/staff)

E_D98_02_M1_03: fc/staff → ViewMode (완료 체크만 가능)

Modal 레이아웃 (max-w-lg):
- 헤더: "태스크 상세" + [X]
- (수정/삭제 버튼 없음)

[필드 — 모두 disabled, 읽기 전용]
- 제목: text-lg font-semibold (Input 아닌 텍스트)
- 카테고리 배지 + 우선순위 도트
- 상태 배지 (pending/in_progress/completed)
- 마감일, 담당자, 메모 텍스트 표시

[완료 체크 — 유일한 인터랙션]
{task.status !== 'completed' && (
  <div className="border-t pt-4 mt-4">
    <Button
      onClick={handleComplete}
      disabled={isCompleting}
      className="w-full"
      variant="outline"
    >
      {isCompleting
        ? <><Loader2 className="w-4 h-4 animate-spin mr-2" />처리 중...</>
        : '✅ 완료 처리'}
    </Button>
    <p className="text-xs text-gray-400 text-center mt-1">
      완료 후에는 되돌릴 수 없습니다
    </p>
  </div>
)}

E_D98_02_M1_10: [완료 처리] 클릭 → PATCH /api/tasks/{id}/complete

TC-098-DLG-005: fc 역할 → 다이얼로그 열림 → 읽기 전용 + 완료 체크만 노출
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D98_02_M1_03`: fc/staff 역할로 태스크 행 클릭

### TC 연결
- TC-098-DLG-005: fc 역할 → 읽기 전용 + 완료 체크만 노출

### 인터랙션 (User Actions)
1. [완료 처리] → `E_D98_02_M1_10` → `05-완료처리`
2. [X] / ESC → 닫힘

### 비즈니스 룰
- 이미 completed 상태 → 완료 처리 버튼 숨김
- 수정/삭제 버튼 미노출

### 연결 화면
- 이전: SCR-098 정상
- 다음: `05-완료처리` / 닫힘 → SCR-098
