# DLG-098-002 태스크상세수정 — 상태: 수정 성공

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-098-002 |
| 상태 코드 | `edit-success` |
| 트리거 | PUT /api/tasks/{id} 200 OK |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-수정모드` |
| 다음 상태 | 닫힘 → SCR-098 목록 갱신 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-098-002 — 수정 성공 처리

E_D98_02_M3_04: PUT /api/tasks/{id} 200 OK

수정 로직:
const onSubmit = async (data: TaskFormData) => {
  setIsSubmitting(true)
  try {
    const { error } = await supabase
      .from('today_tasks')
      .update({
        title: data.title,
        category: data.category,
        priority: data.priority,
        status: data.status,
        due_date: data.dueDate,
        assigned_to: data.assignedTo,
        memo: data.memo,
        updated_at: new Date().toISOString()
      })
      .eq('id', task.id)

    if (error) throw error

    toast.success("태스크가 수정되었습니다.") // Toast_Edit
    onClose()
    onTaskUpdated(task.id, data) // 목록 해당 행 갱신

  } catch (err) {
    if (err.status === 400) {
      setError('title', { message: err.message })
    } else if (err.status === 500) {
      toast.error("저장에 실패했습니다.")
    }
  } finally {
    setIsSubmitting(false)
  }
}

TC-098-DLG-006: 완료 태스크 상태 되돌리기 → 경고 toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D98_02_M3_04`: PUT 200 OK

### TC 연결
- TC-098-DLG-006: completed → pending 시도 → 경고 toast

### 결과 분기 (M3)
| 액션 | 응답 | 처리 |
|------|------|------|
| 수정 | 200 | 닫힘 + 목록 갱신 + toast.success |
| 수정 | 400 | 인라인 에러 |
| 수정 | 500 | toast.error |

### 연결 화면
- 이전: `01-수정모드`
- 다음: SCR-098 목록 갱신
