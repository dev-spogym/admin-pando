# SCR-103 예측분석 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-103 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/prediction/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-데이터있음` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-103 예측분석 — 로딩

파일: src/app/(admin)/prediction/page.tsx

레이아웃:
- AppLayout + PageHeader title="예측 분석"

로딩 스켈레톤:
- 예측 차트 skeleton (h-64 bg-gray-200 rounded-xl animate-pulse)
- 위험 회원 목록 skeleton (5행 h-12 bg-gray-100 rounded animate-pulse)
- 인사이트 카드 skeleton (h-24 bg-gray-200 rounded-xl animate-pulse)

E_F6_103_01: 예측 데이터 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_103_01`: 예측 분석 데이터 API 호출 중

### 비즈니스 룰
- primary, owner만 접근 가능

### 연결 화면
- 이전: 진입
- 다음: `02-데이터있음` / `03-빈상태` / `04-에러`
