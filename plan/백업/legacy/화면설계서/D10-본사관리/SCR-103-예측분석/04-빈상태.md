# SCR-103 예측분석 — 상태: 빈 상태 (데이터 부족)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-103 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/prediction/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 데이터 축적 후 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-103 예측분석 — 데이터 부족

조건: E_F6_103_03 — 예측 모델에 필요한 최소 30일 데이터 없음

빈 상태 UI:
<div className="flex flex-col items-center justify-center min-h-[60vh] gap-5">
  <div className="text-6xl">📈</div>
  <h2 className="text-xl font-semibold text-gray-700">예측 데이터가 부족합니다</h2>
  <p className="text-sm text-gray-500 text-center max-w-sm">
    예측 분석을 사용하려면 최소 30일 이상의 운영 데이터가 필요합니다.<br />
    현재 {currentDays}일치 데이터가 누적되었습니다.
  </p>
  <div className="bg-gray-100 rounded-full h-3 w-64">
    <div className="bg-blue-500 h-3 rounded-full"
      style={{ width: `${Math.min((currentDays / 30) * 100, 100)}%` }} />
  </div>
  <p className="text-xs text-gray-400">{currentDays} / 30일</p>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_103_03`: 운영 데이터 30일 미만

### 연결 화면
- 이전: `01-로딩`
- 다음: 30일 경과 후 → `02-정상` 또는 `03-정상`
