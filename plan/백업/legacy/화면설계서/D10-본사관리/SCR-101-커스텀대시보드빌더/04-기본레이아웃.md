# SCR-101 커스텀대시보드빌더 — 상태: 기본 레이아웃 (저장된 레이아웃 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `default-layout` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (localStorage 없음) |
| 다음 상태 | 커스터마이징 후 → `07-미저장` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 기본 레이아웃 (첫 진입)

조건: E_F6_101_04 — localStorage에 저장된 레이아웃 없음

기본 레이아웃 안내 배너:
<div className="bg-blue-50 border border-blue-200 rounded-lg px-4 py-3 text-sm text-blue-700 mb-4 flex items-center gap-2">
  <span>💡</span>
  <span>추천 위젯이 배치되었습니다. 드래그하여 자유롭게 재배치하세요.</span>
</div>

기본 위젯 배치 (추천):
const defaultLayout = [
  { id: 'stat-members', x: 0, y: 0, w: 3, h: 2 },
  { id: 'stat-revenue', x: 3, y: 0, w: 3, h: 2 },
  { id: 'stat-pt', x: 6, y: 0, w: 3, h: 2 },
  { id: 'stat-attendance', x: 9, y: 0, w: 3, h: 2 },
  { id: 'chart-monthly', x: 0, y: 2, w: 6, h: 4 },
  { id: 'notice-list', x: 6, y: 2, w: 6, h: 4 },
]

[저장하기] 버튼 강조 (ring-2 ring-blue-500 animate-pulse 1회):
"이 레이아웃이 마음에 드시면 저장하세요"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_04`: localStorage 저장값 없음 → 추천 위젯 배치

### 인터랙션 (User Actions)
1. 위젯 드래그 → `05-드래그중`
2. [저장] → `06-저장완료`

### 연결 화면
- 이전: `01-로딩`
- 다음: `05-드래그중` / `06-저장완료` / `07-미저장`
