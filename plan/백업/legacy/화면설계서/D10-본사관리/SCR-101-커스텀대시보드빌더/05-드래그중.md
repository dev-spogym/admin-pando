# SCR-101 커스텀대시보드빌더 — 상태: 드래그 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `dragging` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` 또는 `04-기본레이아웃` |
| 다음 상태 | 드롭 → `02-정상` (미저장 변경) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 드래그 중

조건: E_F6_101_05 — 위젯 드래그 시작

드래그 UI:
- 드래그 중인 위젯: opacity-60 + scale-105 + shadow-2xl
- 드롭 가능 영역: bg-blue-50 border-2 border-dashed border-blue-300
- 그리드 가이드라인 표시 (점선 격자)
- 다른 위젯들: 이동 미리보기 (ghost position)

헤더 힌트:
<p className="text-xs text-gray-400 text-center">원하는 위치에 놓으세요</p>

E_F6_101_06: 드롭 → 레이아웃 업데이트 → 02-정상 (미저장 상태)
onDragEnd → setLayout(newLayout) → setHasUnsaved(true)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_05`: 위젯 드래그 핸들 마우스다운

### 인터랙션 (User Actions)
1. 드롭 → `E_F6_101_06` → 레이아웃 업데이트 → `07-미저장`

### 연결 화면
- 이전: `02-정상` / `04-기본레이아웃`
- 다음: 드롭 후 → `07-미저장`
