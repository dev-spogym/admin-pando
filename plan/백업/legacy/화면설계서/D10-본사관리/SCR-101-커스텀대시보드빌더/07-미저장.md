# SCR-101 커스텀대시보드빌더 — 상태: 미저장 변경

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `unsaved` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (변경 후) |
| 다음 상태 | [저장] → `06-저장완료` / 이탈 시 경고 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 미저장 변경 상태

조건: E_F6_101_08 — 레이아웃 변경 있음, 아직 저장 안 됨

미저장 UI:
- [저장] 버튼 강조: ring-2 ring-blue-500 + "저장하기" 텍스트 (bold)
- 페이지 상단 미저장 배너:
  <div className="bg-amber-50 border-b border-amber-200 px-6 py-2 flex items-center justify-between">
    <span className="text-sm text-amber-700">변경사항이 저장되지 않았습니다.</span>
    <div className="flex gap-2">
      <Button size="xs" variant="ghost" onClick={resetLayout}>되돌리기</Button>
      <Button size="xs" onClick={saveLayout}>저장하기</Button>
    </div>
  </div>

페이지 이탈 방지:
useEffect(() => {
  if (hasUnsaved) {
    const handleBeforeUnload = (e: BeforeUnloadEvent) => {
      e.preventDefault()
      e.returnValue = ''
    }
    window.addEventListener('beforeunload', handleBeforeUnload)
    return () => window.removeEventListener('beforeunload', handleBeforeUnload)
  }
}, [hasUnsaved])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_08`: 위젯 이동/추가/제거 후 아직 저장하지 않음

### 인터랙션 (User Actions)
1. [저장하기] → `06-저장완료`
2. [되돌리기] → 마지막 저장 상태로 복원 → `02-정상`
3. 페이지 이탈 시 → 브라우저 이탈 확인 다이얼로그

### 연결 화면
- 이전: `02-정상` / `05-드래그중` (드롭 후)
- 다음: [저장] → `06-저장완료` / [되돌리기] → `02-정상`
