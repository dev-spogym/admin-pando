# SCR-101 커스텀대시보드빌더 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `04-기본레이아웃` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 로딩 (레이아웃 복원 + 위젯 API 호출)

파일: src/app/(admin)/dashboard-builder/page.tsx

E_F6_101_01: 레이아웃 복원 + 위젯 API 호출 중

레이아웃:
- AppLayout + PageHeader title="커스텀 대시보드 빌더"
- 우측: [저장] Button (비활성)

로딩 UI:
- 그리드 레이아웃 영역 skeleton (다양한 크기):
  · 대형 위젯 h-48 animate-pulse bg-gray-200 rounded-xl
  · 소형 위젯 h-24 animate-pulse bg-gray-200 rounded-xl
- 사이드바 위젯 패널 skeleton (우측)

로딩 순서:
1. localStorage.getItem('dashboard-layout-{userId}') → 저장된 레이아웃 복원
2. API 호출로 각 위젯 데이터 fetch

const [isLoading, setIsLoading] = useState(true)
const [savedLayout, setSavedLayout] = useState<Layout | null>(null)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_01`: 진입 시 레이아웃 복원 + 위젯 데이터 로드

### 비즈니스 룰
- 저장된 레이아웃 없으면 → `04-기본레이아웃` 상태
- 저장된 레이아웃 있으면 → `02-정상` (전체 성공) 또는 `03-부분로드` (일부 실패)

### 연결 화면
- 이전: 진입
- 다음: `02-정상` / `03-부분로드` / `04-기본레이아웃`
