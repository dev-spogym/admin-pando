# SCR-097 감사로그 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `blocked` |
| 경로 | `src/app/(admin)/audit-log/page.tsx` |
| 역할 | manager, fc, staff, readonly (차단) |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 대시보드 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 권한없음

조건: E_097_S_05 — manager 이하 역할

권한없음 UI:
if (!hasPermission(user, 'audit_log:read')) {
  return (
    <AppLayout>
      <div className="flex flex-col items-center justify-center min-h-[70vh] gap-4">
        <div className="text-5xl">🔒</div>
        <h2 className="text-xl font-semibold text-gray-700">접근 권한이 없습니다</h2>
        <p className="text-sm text-gray-500">감사 로그는 최고관리자 또는 대표만 접근 가능합니다.</p>
        <Button onClick={() => moveToPage('/dashboard')} variant="outline">
          대시보드로 이동
        </Button>
      </div>
    </AppLayout>
  )
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_097_S_05`: manager 이하 역할로 URL 접근

### TC 연결
- TC-097-F6-002: manager → 진입 → 접근 차단 (P0)

### 비즈니스 룰
- 감사 로그는 보안 민감 데이터 — primary, owner만 열람 가능

### 연결 화면
- 이전: 진입 시도
- 다음: 대시보드 이동
