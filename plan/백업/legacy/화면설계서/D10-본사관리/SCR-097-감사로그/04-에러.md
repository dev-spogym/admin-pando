# SCR-097 감사로그 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `error` |
| 경로 | `src/app/(admin)/audit-log/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 에러

조건: E_097_S_04 — audit_log API 실패

에러 UI:
<div className="flex flex-col items-center justify-center h-80 gap-4">
  <p className="text-gray-500">감사 로그를 불러오지 못했습니다.</p>
  <Button variant="outline" onClick={loadAuditLog}>새로고침</Button>
</div>

toast.error("감사 로그를 불러오지 못했습니다.")
E_097_S_07: 재시도 → loadAuditLog()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_097_S_04`: audit_log 테이블 API 오류

### 인터랙션 (User Actions)
1. [새로고침] → `E_097_S_07` → `01-로딩`

### 연결 화면
- 이전: `01-로딩`
- 다음: 새로고침 → `01-로딩`
