# SCR-097 감사로그 — 상태: 필터 결과 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `empty-filter` |
| 경로 | `src/app/(admin)/audit-log/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (필터 적용 후) |
| 다음 상태 | 필터 초기화 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 필터 결과 없음

조건: E_097_S_03 — 필터 조건에 맞는 로그 0건

레이아웃:
- AppLayout + PageHeader title="감사 로그"
- 필터 바 유지 (활성 상태 — 현재 적용된 필터 표시)

[빈 상태 — DataTable 자리]
<div className="flex flex-col items-center justify-center h-64 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-3">
  <p className="text-gray-500 font-medium">검색 결과가 없습니다.</p>
  <p className="text-sm text-gray-400">다른 조건으로 다시 검색해 보세요.</p>
  <Button variant="outline" size="sm" onClick={resetFilter}>
    필터 초기화
  </Button>
</div>

TC-097-F6-001: 필터 적용 → 결과 없음 → 빈 상태 메시지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_097_S_03`: 필터 적용 후 조회 결과 0건

### TC 연결
- TC-097-F6-001: 필터 적용 → 결과 없음 → 빈 상태 메시지

### 인터랙션 (User Actions)
1. [필터 초기화] → 필터 리셋 → `02-정상`
2. 필터 직접 수정 → 재조회

### 연결 화면
- 이전: `02-정상` (필터 적용 후)
- 다음: 필터 초기화 → `02-정상`
