# SCR-098 오늘의할일 — 상태: 태스크 있음 (목록 + 통계카드)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-098 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/tasks/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 태스크 클릭 → DLG-098-002 / 추가 → DLG-098-001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-098 오늘의할일 — 태스크 있음

파일: src/app/(admin)/tasks/page.tsx

레이아웃:
- AppLayout + PageHeader title="오늘의 할일"
- 우측: [+ 태스크 추가] Button → DLG-098-001 모달

[StatCardGrid — 4개 카드]
- 전체: tasks.length
- 대기: tasks.filter(t => t.status === 'pending').length
- 진행중: tasks.filter(t => t.status === 'in_progress').length
- 완료: tasks.filter(t => t.status === 'completed').length

TC-098-006: 전체27/대기15/진행6/완료6 → 4개 카드에 각 수치

[태스크 목록]
각 태스크 행 (bg-white border rounded-lg px-4 py-3 flex items-center gap-3):
- 체크박스 (완료 처리)
- 우선순위 도트 (high=red, medium=yellow, low=green)
- 제목 (text-sm font-medium)
  · completed: line-through text-gray-400
- 카테고리 배지 (text-xs bg-gray-100)
- 담당자 아바타 (w-6 h-6 rounded-full)
- 마감일 (text-xs text-gray-400, 오늘=orange, 지남=red)
- 클릭 → DLG-098-002 모달

[태스크 상태별 스타일]
pending: 기본 흰색
in_progress: bg-blue-50 border-blue-200
completed: bg-gray-50 opacity-70 line-through

데이터:
supabase.from('today_tasks')
  .select('id, title, status, priority, category, assigned_to, due_date')
  .eq('branch_id', branchId)
  .order('priority', { ascending: false })
  .order('created_at', { ascending: true })

사용 유틸: useAuthStore, supabase, DataTable, toast, formatNumber
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_098_S_02`: today_tasks 1건 이상

### TC 연결
- TC-098-006: 전체 27 / 대기 15 / 진행 6 / 완료 6 → 4개 카드 수치

### 개별 태스크 상태 (E_098_S_08~10)
| 상태 | 스타일 |
|------|--------|
| pending | 기본 흰색 |
| in_progress | bg-blue-50 파랑 테두리 |
| completed | 회색 배경 + 취소선 |

### 인터랙션 (User Actions)
1. 체크박스 클릭 → PATCH /tasks/{id}/complete → `03-전체완료` 가능
2. 행 클릭 → DLG-098-002
3. [+ 태스크 추가] → DLG-098-001

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-098-001 / DLG-098-002 / `03-전체완료`
