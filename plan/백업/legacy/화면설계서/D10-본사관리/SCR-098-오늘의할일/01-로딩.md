# SCR-098 오늘의할일 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-098 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/tasks/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-태스크있음` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-098 오늘의할일 — 상태: 로딩

파일: src/app/(admin)/tasks/page.tsx (또는 today-tasks)

레이아웃:
- AppLayout + PageHeader title="오늘의 할일"
- 우측 상단 [+ 태스크 추가] 버튼 (skeleton 또는 표시 유지)

로딩 스켈레톤:
- StatCardGrid skeleton (카드 4개: 전체/대기/진행중/완료)
  · animate-pulse bg-gray-200 rounded-xl h-24
- 태스크 목록 skeleton (5행):
  · 각 행: h-14 bg-gray-100 rounded-lg animate-pulse

E_098_S_01: 태스크 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_098_S_01`: today_tasks API 호출 중

### 비즈니스 룰
- 전 역할 접근 가능 (읽기 — 자기 할당 태스크만)
- primary/owner/manager는 전체 태스크 조회

### 연결 화면
- 이전: 진입
- 다음: `02-태스크있음` / `03-태스크없음` / `03-전체완료` / `04-에러`
