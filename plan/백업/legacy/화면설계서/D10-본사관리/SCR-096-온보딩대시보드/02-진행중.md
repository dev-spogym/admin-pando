# SCR-096 온보딩대시보드 — 상태: 진행 중 (일부 완료)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-096 |
| 상태 코드 | `in-progress` |
| 경로 | `src/app/(admin)/onboarding/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-초기상태` |
| 다음 상태 | 필수 4단계 완료 → `03-필수완료` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-096 온보딩대시보드 — 진행 중 (일부 완료)

조건: E_096_S_02 — 1개 이상 완료, 전체 미완료

[진행률 바]
- completedCount / totalCount * 100 % 계산
- 예: 2/4 필수 완료 → 50%
<div className="bg-gray-100 rounded-full h-3">
  <div className="bg-blue-500 h-3 rounded-full transition-all duration-500"
    style={{ width: `${progress}%` }} />
</div>
<p className="text-sm text-gray-500 mt-1">{progress}% 완료 ({completedCount}/{totalCount}단계)</p>

[단계 목록]
완료된 단계 카드:
- 초록 체크 아이콘 (bg-green-100 text-green-600)
- 단계명 + "완료" 배지 (bg-green-100 text-green-700 text-xs)
- 완료 일시 표시 (text-xs text-gray-400)
- 버튼 없음 또는 [수정] 버튼

현재 진행 단계 카드 (border-2 border-blue-500):
- 파랑 원형 배지 + 단계 번호
- [이어하기] 버튼 (primary)

미완료 단계 카드:
- 회색 배지
- [시작하기] 버튼 (비활성 disabled)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_096_S_02`: 1개 이상 완료, 미완료 단계 존재

### 인터랙션 (User Actions)
1. [이어하기] → 현재 미완료 단계 설정 화면
2. 완료 단계 [수정] → 해당 설정 화면 (재진입)

### 연결 화면
- 이전: `01-초기상태`
- 다음: 필수 4단계 모두 완료 → `03-필수완료`
