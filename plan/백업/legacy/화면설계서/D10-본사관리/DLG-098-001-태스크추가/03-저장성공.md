# DLG-098-001 태스크추가 — 상태: 저장 성공

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-098-001 |
| 상태 코드 | `success` |
| 트리거 | POST /api/tasks 201 Created |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 닫힘 → SCR-098 목록 맨 위 추가 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-098-001 태스크 추가 — 저장 성공

E_D98_01_M3_01: POST /api/tasks 201 Created

저장 로직:
const onSubmit = async (data: TaskFormData) => {
  setIsSubmitting(true)
  try {
    const { data: newTask, error } = await supabase
      .from('today_tasks')
      .insert({
        title: data.title,
        category: data.category,
        priority: data.priority,
        due_date: data.dueDate,
        assigned_to: data.assignedTo,
        memo: data.memo,
        status: 'pending',
        branch_id: branchId,
        created_by: user.id
      })
      .select()
      .single()

    if (error) throw error

    // M3 성공 처리
    toast.success("태스크가 추가되었습니다.") // Toast_OK
    onClose()
    onTaskAdded(newTask)  // 목록 맨 위 추가 + 통계 카드 갱신

  } catch (err) {
    if (err.status === 403) {
      toast.error("권한이 없습니다.")
      onClose()
    } else if (err.status === 400) {
      // 인라인 에러 처리
      setError('title', { message: err.message })
    } else {
      toast.error("태스크 추가에 실패했습니다.")
    }
  } finally {
    setIsSubmitting(false)
  }
}

TC-098-DLG-001: 저장 성공 → 목록 맨 위 추가 + toast.success
TC-098-DLG-004: 태스크 추가 성공 → 목록 갱신 + 통계 카드 갱신
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D98_01_M3_01`: POST /api/tasks 201 Created

### TC 연결
- TC-098-DLG-001: 저장 성공 → 목록 맨 위 추가 + toast.success
- TC-098-DLG-004: 태스크 추가 성공 → 목록 갱신 + 통계 카드 갱신

### 결과 분기 (M3)
| 응답 | 처리 |
|------|------|
| 201 Created | 닫힘 + 목록 맨 위 추가 + 통계 갱신 + toast.success |
| 400 Bad Request | 인라인 에러 표시 |
| 403 Forbidden | toast.error + 닫힘 |
| 500 | toast.error |

### 연결 화면
- 이전: `01-오픈`
- 다음: SCR-098 목록 + 통계 카드 갱신
