# SCR-100 로그인 — 상태: 입력 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-100 |
| 상태 코드 | `editing` |
| 경로 | `src/app/login/page.tsx` |
| 역할 | 비인증 사용자 |
| 우선순위 | P0 |
| 이전 상태 | `01-기본` |
| 다음 상태 | 로그인 클릭 → `03-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-100 로그인 — 입력 중 상태

조건: E_F6_100_02 — 사용자가 이메일/비밀번호 입력 중

상태 변화:
- 이메일 Input: border-blue-500 (focus ring)
- 비밀번호 Input: border-blue-500 (focus ring)
- [로그인] 버튼: 이메일 + 비밀번호 모두 입력 시 disabled=false (활성화)

실시간 유효성:
- 이메일 형식 체크 (blur 시): 잘못된 형식이면 text-red-500 "올바른 이메일 형식을 입력하세요"
- 비밀번호 최소 1자 이상 입력 시 버튼 활성화

const [email, setEmail] = useState('')
const [password, setPassword] = useState('')
const isValid = email.length > 0 && password.length > 0

E_F6_100_03: [로그인] 클릭 → 03-로딩
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_100_02`: 이메일 또는 비밀번호 입력 시작

### 인터랙션 (User Actions)
1. 이메일 blur → 형식 체크
2. [로그인] 클릭 (isValid=true) → `03-로딩`

### 연결 화면
- 이전: `01-기본`
- 다음: `03-로딩`
