# SCR-I003 IoT연동관리 — 상태: 빈 상태 (등록 장비 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I003 |
| 상태 코드 | `empty` |
| 경로 | `/settings/iot` |
| 역할 | superAdmin, primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (장비 0건) |
| 다음 상태 | 장비 추가 → `01-로딩` 재실행 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I003 IoT연동관리 — 상태: 빈 상태

파일: src/app/settings/iot/page.tsx (조건부 렌더링)

조건: iotDevices.length === 0 (로딩 완료 후)

[빈 상태 UI]
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center mb-5">
    <svg ... className="w-10 h-10 text-gray-300" /> {/* IoT/wifi 아이콘 */}
  </div>
  <p className="text-lg font-semibold text-gray-600">등록된 IoT 장비가 없습니다</p>
  <p className="mt-2 text-sm text-gray-400 max-w-sm">
    입장 게이트, 얼굴인식 단말기, QR 리더 등 IoT 장비를 등록하면
    자동 출석 처리가 가능합니다.
  </p>
  {hasPermission('iot:write') && (
    <button
      onClick={openAddDeviceModal}
      className="mt-6 px-5 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700"
    >
      첫 장비 등록하기
    </button>
  )}
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 지점 최초 접근 (장비 미등록)
- 모든 장비 삭제 후

### 인터랙션
1. "첫 장비 등록하기" → 장비 등록 모달
2. `iot:write` 없으면 버튼 숨김 + 안내 텍스트만 표시

### 비즈니스 룰
- IoT 미등록 시 SCR-I001에서 얼굴인식 출석 채널 비활성화 연동
- 관리자에게 장비 등록 알림 (향후)

### 연결 화면
- 이전: `01-로딩`
- 다음: 장비 추가 모달 → 완료 후 `02-정상`
