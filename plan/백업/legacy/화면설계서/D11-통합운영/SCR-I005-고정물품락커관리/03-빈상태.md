# SCR-I005 고정물품락커관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I005 |
| 상태 코드 | `empty` |
| 경로 | `/(facilities)/locker/fixed` |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (배정 0건) |
| 다음 상태 | 새 배정 → 배정 폼 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I005 고정물품락커관리 — 상태: 빈 상태

파일: src/app/(facilities)/locker/fixed/page.tsx (조건부)

조건: assignments.length === 0

[빈 상태 UI]
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center mb-5">
    <svg ... className="w-10 h-10 text-gray-300" /> {/* 자물쇠/상자 아이콘 */}
  </div>
  <p className="text-lg font-semibold text-gray-600">배정된 락커가 없습니다</p>
  <p className="mt-2 text-sm text-gray-400 max-w-sm">
    고정 물품 보관 락커를 회원에게 배정하면 여기에 표시됩니다.
  </p>
  {hasPermission('locker:write') && (
    <button
      onClick={openAssignModal}
      className="mt-6 px-5 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700"
    >
      첫 배정 시작하기
    </button>
  )}
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 운영 시작 또는 전체 반납 완료 후

### 인터랙션
1. "첫 배정 시작하기" → 배정 모달/폼
2. `locker:write` 없으면 버튼 숨김 + 안내만 표시

### 연결 화면
- 이전: `01-로딩`
- 다음: 배정 완료 후 `02-정상`
