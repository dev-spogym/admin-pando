# SCR-I004 옷락커운영관리 — 상태: 권한 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I004 |
| 상태 코드 | `forbidden` |
| 경로 | `/(facilities)/locker` |
| 역할 | (fc — 읽기 전용 / 미인증 차단) |
| 우선순위 | P1 |
| 이전 상태 | 인증 체크 실패 |
| 다음 상태 | 대시보드로 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 컴포넌트를 작성하라.
화면: SCR-I004 옷락커운영관리 — 상태: 403 권한 없음

파일: src/app/(facilities)/locker/page.tsx

권한:
- locker:read: superAdmin, primary, owner, manager, staff, fc (전체)
- locker:write: superAdmin, primary, owner, manager, staff (fc 제외)
- 읽기는 가능하나 배정/반납 버튼 숨김 (fc)
- 미인증: /login 리다이렉트

[FC 역할 — 읽기 전용 모드]
- 락커 그리드: 정상 표시
- 빈 락커 클릭: 차단 (cursor-not-allowed)
- 배정 버튼 없음
- 상단 안내: <p className="text-xs text-gray-400">읽기 전용 모드입니다</p>

[미인증]
useEffect(() => {
  if (!session) router.replace('/login?returnUrl=/locker')
}, [session])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- fc 역할: 읽기 전용 (배정/반납 불가)
- 미인증: 로그인 리다이렉트

### 비즈니스 룰
- fc는 락커 현황 조회만 가능 (배정 불가)
- 빈 락커 클릭: 안내 토스트 "배정 권한이 없습니다"

### 연결 화면
- 미인증: `/login`
- fc 읽기 전용: `02-정상` UI (배정 불가 버전)
