---
id: DLG-I002
kind: dialog
domain: D11-통합운영
title: 옷락커 배정
priority: P0
roles:
  - super
  - primary
  - owner
  - manager
  - staff
  - front
---

# DLG-I002 옷락커 배정 — 기본화면 (마스터)

> 이 문서는 **다이얼로그 마스터 스펙**. 상태 델타 파일은 이 문서를 상속.
> SCR-I004 옷락커 운영에서 빈 락커 클릭 시 트리거. RFID 밴드 스캔 연동.

## 0. 메타
| 항목 | 값 |
|---|---|
| 화면 ID | DLG-I002 |
| 화면명 | 옷락커 배정 |
| 도메인 | D11-통합운영 |
| 트리거 | SCR-I004 빈 락커 셀 클릭 |
| 파일 | `src/components/dialogs/ClothingLockerAssignDialog.tsx` |
| 역할 | super/primary/owner/manager/staff ● / front ● |
| 우선순위 | P0 |

## 1. 목적
방문 회원에게 옷락커를 배정. 회원 정보 확인 + RFID 밴드 스캔 후 IoT 락커 자동 잠금.

## 2. 레이아웃
```
┌──────────────────────────────────────┐
│ 락커 101 배정                [✕]      │
├──────────────────────────────────────┤
│ 회원 검색 [_______________] [검색]    │
│ 선택: 김철수 (회원권: 유효)           │
├──────────────────────────────────────┤
│ RFID 밴드 스캔                        │
│ [스캔 대기 중...]  또는 [수동 입력]   │
├──────────────────────────────────────┤
│ 성별 구역 확인: 남성 ✓               │
├──────────────────────────────────────┤
│              [취소]  [배정]           │
└──────────────────────────────────────┘
```

## 3. 디자인 토큰
| 토큰 | 클래스 |
|---|---|
| overlay | `fixed inset-0 bg-black/50 z-50` |
| dialog | `bg-white rounded-2xl shadow-xl w-full max-w-md mx-auto p-6` |
| scan.waiting | `bg-blue-50 border-2 border-dashed border-blue-300 rounded-lg p-4 text-center` |
| scan.success | `bg-emerald-50 border-2 border-emerald-300 rounded-lg p-4 text-center` |
| gender.warn | `bg-amber-50 text-amber-800 rounded-lg p-2 text-sm` |

## 4. 🔐 RBAC
| 액션 | super | primary | owner | manager | fc | trainer | staff | front |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 배정 | ● | ● | ● | ● | — | — | ● | ● |

## 5. 컴포넌트 트리
```
<Dialog open={open} onClose={onClose}>
  <DialogHeader title={`락커 ${locker.number} 배정`} />
  <MemberSearch onSelect={setMember} />
  {member && <MembershipStatus member={member} />}
  <RfidScanInput value={rfid} onChange={setRfid} onScan={handleScan} />
  {genderMismatch && <GenderWarning />}
  <DialogFooter>
    <CancelButton onClick={onClose} />
    <SubmitButton onClick={assign}
      disabled={!member || !rfid || genderMismatch}
      loading={isPending} />
  </DialogFooter>
</Dialog>
```

## 6. 데이터 계약
```ts
interface LockerAssignInput {
  lockerId: string;
  memberId: number;
  bandRfid: string;
  branchId: number;
}
```

### API
| 메서드 | 경로 |
|---|---|
| POST | `/clothing-lockers/:id/assign` |
| GET | `/members/search?q=&branchId=` |

## 7. 비즈니스 룰
1. RFID 밴드 스캔 필수 (수동 입력 대체 가능)
2. 성별 구역 불일치 시 경고 (강제 진행 불가 — 관리자만 override)
3. 배정 완료 → IoT 락커 잠금 신호 전송
4. 회원권 만료 시 경고 표시 (배정은 가능)
5. 배정 후 SCR-I004 그리드 실시간 갱신

## 8. 상태 목록
| 파일 | 코드 |
|---|---|
| `01-기본.md` | `dlg-assign-idle` |
| `02-스캔대기.md` | `dlg-assign-scanning` |
| `03-제출중.md` | `dlg-assign-submitting` |
| `04-성공.md` | `dlg-assign-success` |
| `05-에러.md` | `dlg-assign-error` |

## 9. 접근성
- `role="dialog" aria-modal="true" aria-labelledby="dialog-title"`
- RFID 스캔 영역 `aria-live="polite"` (스캔 결과 공지)
- 포커스 트랩, ESC 닫기

## 10. 🧩 바이브코딩 프롬프트
```
Next.js 15 + TS + Tailwind + React Query

파일: src/components/dialogs/ClothingLockerAssignDialog.tsx

<Dialog open={open} onOpenChange={onClose}>
  <DialogContent className="max-w-md">
    <DialogHeader>
      <DialogTitle>락커 {locker.number} 배정</DialogTitle>
    </DialogHeader>
    <div className="space-y-4 py-2">
      <MemberSearchInput onSelect={setMember} branchId={branchId} />
      {member && (
        <div className="rounded-lg bg-gray-50 p-3 text-sm flex items-center justify-between">
          <span className="font-medium">{member.name}</span>
          <span className={member.membershipValid ? 'text-emerald-600' : 'text-amber-600'}>
            {member.membershipValid ? '회원권 유효' : '회원권 만료'}
          </span>
        </div>
      )}
      <div>
        <label className="text-sm font-medium mb-1 block">RFID 밴드 스캔</label>
        <div aria-live="polite"
          className={cn('rounded-lg p-4 text-center text-sm border-2 border-dashed',
            rfid ? 'bg-emerald-50 border-emerald-300 text-emerald-800'
                 : 'bg-blue-50 border-blue-300 text-blue-700')}>
          {rfid ? `✓ ${rfid}` : '밴드를 스캐너에 가져다 대세요'}
        </div>
        <button onClick={() => setManualInput(true)}
          className="mt-1 text-xs text-gray-500 hover:underline">수동 입력</button>
      </div>
      {genderMismatch && (
        <div className="bg-amber-50 text-amber-800 rounded-lg p-2 text-sm">
          ⚠ 성별 구역 불일치. 관리자 승인 필요.
        </div>
      )}
    </div>
    <DialogFooter>
      <button onClick={onClose} className="h-10 px-6 rounded-lg border border-gray-300 text-sm">취소</button>
      <button onClick={assign}
        disabled={!member || !rfid || (genderMismatch && !isManager) || isPending}
        className="h-10 px-6 rounded-lg bg-blue-600 text-white text-sm font-medium disabled:opacity-50">
        {isPending ? '배정 중...' : '배정'}
      </button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

## 11. QA 체크리스트
- [ ] RFID 스캔 → 자동 입력
- [ ] 성별 구역 불일치 경고
- [ ] 배정 후 IoT 잠금 신호
- [ ] 그리드 실시간 갱신
- [ ] 회원권 만료 경고 (배정은 가능)
- [ ] ESC 닫기, 포커스 트랩
