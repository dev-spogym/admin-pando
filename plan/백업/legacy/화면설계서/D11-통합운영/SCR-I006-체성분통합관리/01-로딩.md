# SCR-I006 체성분통합관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I006 |
| 상태 코드 | `loading` |
| 경로 | `/body-composition` |
| 역할 | superAdmin, primary, owner, manager, fc |
| 우선순위 | P0 |
| 이전 상태 | 인증/권한 확인 완료 직후 |
| 다음 상태 | `02-정상` / `03-빈상태` / `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I006 체성분통합관리 — 상태: 로딩 (스켈레톤)

파일: src/app/body-composition/page.tsx

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "체성분 통합 관리" + 우측 버튼 영역 스켈레톤

스켈레톤 구성 (animate-pulse):

[요약 KPI — 4칸]
각 카드 (h-24 bg-gray-100 rounded-xl):
- 라벨 바 (h-3 w-20 bg-gray-200)
- 숫자 바 (h-8 w-16 bg-gray-300 mt-2)

[필터 바]
- DateRangePicker 스켈레톤 (h-9 w-60 bg-gray-100 rounded)
- 입력소스 Select 스켈레톤 (h-9 w-32 bg-gray-100 rounded)
- 검수상태 Select 스켈레톤 (h-9 w-32 bg-gray-100 rounded)
- 검색창 스켈레톤 (h-9 w-48 bg-gray-100 rounded)

[측정 결과 DataTable — 10행]
- 헤더 7컬럼 바 (h-4 bg-gray-200)
- 행 10줄 × 7셀 (h-4 bg-gray-100 rounded)

구현:
- useEffect(() => { loadBodyCompositionData() }, [])
- Promise.all([fetchMeasurements, fetchKPI, fetchInBodyStatus]) 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I006 최초 진입 시
- 필터 변경 후 재조회 시
- 수기 등록(DLG-I003) 완료 후 목록 갱신 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| KPI | `bodyComposition` | `branch_id=?`, 집계 |
| 측정 결과 | `bodyComposition` JOIN `members` | `branch_id=?`, 필터 |
| InBody 상태 | `iot_devices` | `type='inbody'` |

### 연결 화면
- 이전: 사이드바 통합운영 메뉴
- 다음: `02-정상` / `03-빈상태` / `06-에러`
