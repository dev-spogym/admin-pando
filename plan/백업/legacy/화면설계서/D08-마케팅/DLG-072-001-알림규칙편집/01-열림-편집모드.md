# DLG-072-001 알림규칙편집 — 상태: 열림 (편집 모드)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-072-001 |
| 상태 코드 | `open-edit` |
| 트리거 | SCR-072 카드 편집 아이콘 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-072 |
| 다음 상태 | `03-저장성공` 또는 `04-유효성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-072-001 알림 규칙 편집 모달 — 편집 모드

파일: src/components/marketing/AlarmRuleModal.tsx

모달 구조:
- title: "{rule.name} 알림 규칙 편집"
- size: xl (max-w-2xl)

폼 레이아웃 (grid grid-cols-2 gap-4):
1. 채널 * (Select):
   옵션: SMS / 카카오 / 앱푸시
2. 발송 시점 * (Select):
   옵션: 이벤트 즉시 / 1일 전 / 3일 전 / 7일 전 / D-day
3. 대상 (Select):
   옵션: 전체 회원 / 활성 회원 / 특정 등급
4. 활성 여부 (Toggle switch)

메시지 내용 (col-span-2):
- label: "메시지 내용 *" + 글자수 카운터 "{현재}/{최대}"
- Textarea rows=5
- 변수 삽입 버튼 (flex gap-2 mt-2):
  · "{이름}" Button variant=ghost size=xs
  · "{만료일}" Button variant=ghost size=xs
  · "{상품명}" Button variant=ghost size=xs
  → 클릭 시 textarea cursor 위치에 삽입

테스트 발송 (col-span-2):
<div className="flex items-center gap-3 p-3 bg-gray-50 rounded-lg">
  <Input placeholder="테스트 수신 번호" className="flex-1" />
  <Button variant=outline size=sm onClick={handleTestSend}>테스트 발송</Button>
</div>

하단 버튼:
- "취소" variant=outline
- "저장" variant=primary onClick={handleSave}

구현:
- 기존값 주입: useEffect(() => { form.reset(rule) }, [rule]) (TC-072-004)
- handleSave: localStorage.setItem(`settings_${branchId}_auto_alarm`, JSON.stringify(rules))
- 성공: toast.success("알림 규칙이 저장되었습니다.") (TC-072-005)
- handleTestSend: POST /messages/test → toast.success("테스트 발송 완료")

사용 컴포넌트:
- Modal, Button, Select, Textarea
- toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-072 카드 편집 아이콘 클릭 (TC-072-004)

### 인터랙션 (User Actions)
1. 채널/시점 변경
2. 메시지 수정 + 변수 삽입 (TC-072-006)
3. 테스트 발송
4. "저장" → localStorage 업데이트

### 비즈니스 룰
- 채널 필수 (TC-072-M2-001-01)
- 메시지 내용 필수 + 글자수 제한
- 저장 즉시 SCR-072 카드 갱신

### 에지 케이스
- localStorage 용량 초과: toast.warning

### 연결 화면
- 다음: `03-저장성공` 또는 `04-유효성오류`
