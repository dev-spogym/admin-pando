# DLG-073-002 쿠폰발급 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-073-002 |
| 상태 코드 | `open` |
| 트리거 | SCR-073 Send 아이콘 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-073 정상 |
| 다음 상태 | `02-발급성공` 또는 `03-발급오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-073-002 쿠폰 발급 모달

파일: src/components/marketing/CouponIssueModal.tsx

모달 구조:
- title: "쿠폰 발급 — {coupon.name}"
- size: xl (max-w-2xl)

쿠폰 정보 요약 (bg-blue-50 rounded-lg p-3 mb-4):
- 쿠폰 코드: {coupon.code} (font-mono)
- 할인 내용: {coupon.discountValue}{type=rate?'%':'원'}
- 유효기간: {validFrom} ~ {validUntil}
- 잔여 발급 가능: {totalIssued - totalUsed}/{totalIssued}건

발급 방식 (TabNav):
- "즉시 발급" | "예약 발급"

[즉시 발급 탭]:
회원 검색/선택 (DLG-071-001과 동일 구조):
- SearchInput + 회원 목록 + 체크박스
- 선택된 회원 수 칩

[예약 발급 탭]:
- 회원 선택 (동일)
- DateTimePicker "발급 예정일시"

하단 버튼:
- "취소" variant=outline
- "발급하기 ({N}명)" variant=primary disabled={selectedCount===0} onClick={handleIssue}

구현:
- POST /coupon-issues (supabase 가상)
  body: { couponId, memberIds: selectedIds, isScheduled, scheduledAt }
- 201: toast.success(`${N}명에게 쿠폰이 발급되었습니다.`) (TC-073-004)
- 410 만료: toast.error("만료된 쿠폰입니다.")
- 409 소진: toast.error("발급 한도를 초과했습니다.")

사용 컴포넌트:
- Modal, Button, TabNav
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-073 활성 쿠폰의 Send 아이콘 클릭 (TC-073-004)

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 쿠폰 정보 | `coupons` | id = couponId |
| 회원 목록 | `members` | branchId = ? |

### 인터랙션 (User Actions)
1. 회원 검색/선택
2. 발급 방식 선택 (즉시/예약)
3. "발급하기" → API 호출

### 비즈니스 룰
- 만료/소진 쿠폰: Send 아이콘 비활성 (SCR-073에서 사전 차단)
- 발급 후 totalIssued 갱신 (TC-073-004)
- 0명 선택 시 버튼 비활성

### 연결 화면
- 다음: `02-발급성공` 또는 `03-발급오류`
