# SCR-074 마일리지관리 — 상태: 소멸 예정 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-074 |
| 상태 코드 | `expire-warning` |
| 경로 | `/mileage` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (서브 상태) |
| 다음 상태 | DLG-074-002 (수동 처리) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-074 마일리지관리 — 상태: 소멸 예정 경고 (02-정상의 서브 상태)

파일: src/app/(marketing)/mileage/page.tsx

이 상태는 02-정상과 동일하되, 소멸 예정 마일리지가 있을 때 추가 UI:

1. 통계 카드 아래 경고 배너:
   <div className="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-4 flex items-center justify-between">
     <div className="flex items-center gap-2">
       <AlertTriangle className="w-5 h-5 text-amber-600" />
       <div>
         <p className="text-sm font-semibold text-amber-700">이번 달 소멸 예정 마일리지</p>
         <p className="text-lg font-bold text-amber-800">{expireAmount:,}P</p>
       </div>
     </div>
     <Button variant=ghost size=sm onClick={() => setActiveTab('history')}>
       이력 확인
     </Button>
   </div>

2. 회원별 현황 탭에서 소멸 예정 회원:
   - 현재 잔액 셀 옆에 "소멸 예정: {amount}P" 배지 (text-xs text-amber-600)

구현:
- const expireSoon = mileageLogs.filter(l => {
    const daysLeft = differenceInDays(new Date(l.expireAt), new Date())
    return daysLeft >= 0 && daysLeft <= 30
  })
- expireSoon.length > 0 && expireAmount > 0 → 배너 표시 (TC-074-F6-02)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 이번 달 소멸 예정 마일리지 > 0 (TC-074-F6-02)

### 비즈니스 룰
- 소멸 예정 기준: `expire_at` 이번 달 말일 이내
- 배너 클릭 → 거래 이력 탭으로 이동 + 소멸 필터 자동 적용

### 연결 화면
- 이전: `02-정상` (서브 상태)
- 다음: DLG-074-002 또는 거래 이력 탭
