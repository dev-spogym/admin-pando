# DLG-071-001 수신자검색 — 상태: 0명 오류

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-071-001 |
| 상태 코드 | `zero-selection-error` |
| 트리거 | 선택 0명 상태에서 완료 시도 |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 선택 후 `02-선택완료` |

## 🧩 바이브코딩 프롬프트

```
0명 선택 오류 처리 (RecipientSearchModal.tsx):

방식 1 (권장): 버튼 비활성
- disabled={selectedIds.length === 0}
- "선택 완료 (0명)" 버튼 비활성화 상태
- cursor-not-allowed + opacity-50

방식 2: 클릭 시 에러 표시
- "최소 1명을 선택해주세요" 인라인 에러
  <p className="text-xs text-red-500 mt-1">최소 1명을 선택해주세요.</p>

TC-071-M2-001-01 구현:
- selectedIds.length === 0 → 완료 버튼 disabled
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 수신자 0명 상태 (TC-071-M2-001-01)

### 인터랙션 (User Actions)
1. 회원 선택 → 버튼 활성화

### 연결 화면
- 다음: 선택 후 `02-선택완료`
