# DLG-076-001 캠페인생성 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-076-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-076 "캠페인 생성" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-076 |
| 다음 상태 | `02-저장성공` 또는 `03-저장오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-076-001 캠페인 생성 모달 (미구현 화면 — newFeature 스타일)

파일: src/components/marketing/CampaignFormModal.tsx

모달 구조:
- title: "캠페인 생성"
- size: lg (max-w-lg)
- 상단 newFeature 배지: "개발 예정 기능" (bg-purple-100 text-purple-700 text-xs)

폼 필드:
1. 캠페인명 * (Input)
2. 캠페인 유형 (Select): 할인 / 이벤트 / 시즌 / 기타
3. 시작일 * (DatePicker)
4. 종료일 * (DatePicker)
5. 대상 (Select): 전체 / 신규 회원 / 장기 미방문 / 특정 등급
6. 설명 (Textarea, rows=3)

하단 버튼:
- "취소" variant=outline
- "생성" variant=primary onClick={handleSave}

구현:
- POST /campaigns (supabase.from('campaigns').insert)
- 성공: toast.success("캠페인이 생성되었습니다.") + onClose() + refreshCampaigns()
- 실패: toast.error("생성에 실패했습니다.")

사용 컴포넌트:
- Modal, Button, Select, Textarea
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-076 "캠페인 생성" 버튼 (TC-DLG-076-001-M1-01)

### 인터랙션 (User Actions)
1. 필드 입력
2. "생성" → POST /campaigns

### 비즈니스 룰
- 종료일 ≥ 시작일
- 캠페인명 필수

### 연결 화면
- 다음: `02-저장성공` 또는 `03-저장오류`
