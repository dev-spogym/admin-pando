# SCR-071 메시지발송 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-071 |
| 상태 코드 | `blocked` |
| 경로 | `/message` |
| 역할 | readonly (차단) |
| 우선순위 | P1 |
| 이전 상태 | 진입 (권한 확인) |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-071 메시지발송 — 상태: 권한없음

파일: src/app/(marketing)/message/page.tsx

권한없음 UI:
- AppLayout + PageHeader("메시지 발송")
- 본문 중앙 flex flex-col items-center py-24:
  · Lock 아이콘 (w-16 h-16 text-gray-300 mb-4)
  · "접근 권한이 없습니다" (text-xl font-bold text-gray-600)
  · "메시지 발송은 스태프 이상 역할에서 접근할 수 있습니다." (text-sm text-gray-400 mt-2)
  · Button "대시보드로 이동" onClick={() => moveToPage('/dashboard')} variant=outline mt-6

구현:
- hasPermission(user.role, 'message') 검사
- readonly 역할 차단
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `hasPermission(role, 'message') === false`
- readonly 역할 접근 시

### 인터랙션 (User Actions)
1. "대시보드로 이동" → `/dashboard`

### 연결 화면
- 다음: `/dashboard`
