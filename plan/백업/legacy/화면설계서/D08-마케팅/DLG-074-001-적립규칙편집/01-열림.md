# DLG-074-001 적립규칙편집 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-074-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-074 규칙 수정 아이콘 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-074 적립 규칙 탭 |
| 다음 상태 | `02-저장성공` 또는 `03-유효성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-074-001 적립 규칙 편집 모달

파일: src/components/marketing/MileageRuleModal.tsx

모달 구조:
- title: "{rule.eventName} 적립 규칙 편집"
- size: md (max-w-md)

이벤트 정보 (읽기 전용):
<div className="bg-gray-50 rounded-lg p-3 mb-4">
  <p className="text-sm font-medium text-gray-700">{rule.eventName}</p>
  <p className="text-xs text-gray-500 mt-1">{rule.description}</p>
</div>

폼 필드:
1. 적립 유형 (Select, 읽기 전용):
   - 결제금액 비율 / 건당 고정 금액
2. 적립률/금액 * (Input, number):
   - 비율: "결제금액의 N%" → suffix "%"
   - 고정: "N원" → suffix "원"
   - min=0 초과 (양수만)
3. 월 한도 (Input, number, 0=무제한):
   - placeholder="0 (무제한)"
4. 활성 여부 (Toggle switch):
   - 활성: text-blue-600 "활성"
   - 비활성: text-gray-400 "비활성"

하단 버튼:
- "취소" variant=outline onClick={onClose}
- "저장" variant=primary onClick={handleSave}

구현:
- 기존값 주입 (TC-074-F2-01): form.reset(rule)
- PATCH /mileage-rules/:id
- 성공: toast.success("규칙이 저장되었습니다.") + onClose() + refreshRules()
- 음수/0 에러: toast.error 또는 인라인 에러

사용 컴포넌트:
- Modal, Button, Select
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-074 규칙 수정 아이콘 클릭 (TC-074-F2-01)

### 인터랙션 (User Actions)
1. 적립률/금액 변경 (TC-074-M1-001-01)
2. Toggle 활성/비활성 전환
3. "저장" → PATCH API

### 비즈니스 룰
- 음수 불가 (TC-074-M2-001-01)
- 0 불가 (0은 무제한 한도용 예외)
- 적립률 100% 초과 불가

### 에지 케이스
- 500: toast.error + 모달 유지

### 연결 화면
- 다음: `02-저장성공` 또는 `03-유효성오류`
