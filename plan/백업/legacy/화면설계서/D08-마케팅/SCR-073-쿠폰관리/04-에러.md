# SCR-073 쿠폰관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-073 |
| 상태 코드 | `error` |
| 경로 | `/message/coupon` |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 새로고침 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-073 쿠폰관리 — 상태: 에러

파일: src/app/(marketing)/message/coupon/page.tsx

에러 UI:
- AppLayout + PageHeader("쿠폰 관리")
- 본문 중앙 flex flex-col items-center py-20:
  · AlertCircle (w-16 h-16 text-red-400 mb-4)
  · "데이터를 불러오지 못했습니다" (text-lg font-semibold text-gray-700)
  · "서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요." (text-sm text-gray-400 mt-1)
  · Button "새로고침" onClick={() => window.location.reload()} variant=outline mt-4

구현:
- catch (err) → setError(true)
- toast.error("쿠폰 데이터를 불러오지 못했습니다.") 1회
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `coupons` 쿼리 500 응답

### 인터랙션 (User Actions)
1. "새로고침" → `window.location.reload()`

### 연결 화면
- 다음: 재시도 → `01-로딩`
