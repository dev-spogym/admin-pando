# SCR-072 자동알림설정 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-072 |
| 상태 코드 | `blocked` |
| 경로 | `/message/auto-alarm` |
| 역할 | fc, staff, readonly (차단) |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-072 자동알림설정 — 상태: 권한없음

파일: src/app/(marketing)/message/auto-alarm/page.tsx

권한없음 UI:
- AppLayout + PageHeader("자동 알림 설정")
- 본문 중앙 flex flex-col items-center py-24:
  · Lock 아이콘 (w-16 h-16 text-gray-300 mb-4)
  · "접근 권한이 없습니다" (text-xl font-bold text-gray-600)
  · "자동 알림 설정은 매니저 이상만 변경할 수 있습니다." (text-sm text-gray-400 mt-2)
  · Button "메시지 발송으로 이동" onClick={() => moveToPage('/message')} variant=outline mt-6

구현:
- hasPermission(user.role, 'auto-alarm-settings') 검사
- owner/manager만 허용
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- fc, staff, readonly 역할 접근

### 인터랙션 (User Actions)
1. "메시지 발송으로 이동" → `/message`

### 연결 화면
- 다음: `/message`
