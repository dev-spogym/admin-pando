# SCR-072 자동알림설정 — 상태: 기본값 (localStorage 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-072 |
| 상태 코드 | `default` |
| 경로 | `/message/auto-alarm` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-072-001 (규칙 편집) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-072 자동알림설정 — 상태: 기본값 (첫 진입 또는 초기화)

파일: src/app/(marketing)/message/auto-alarm/page.tsx

레이아웃:
- AppLayout + PageHeader("자동 알림 설정")
- 상단 안내 배너 (bg-yellow-50 border border-yellow-200 rounded-lg p-3 mb-4):
  "기본 알림 규칙이 적용되어 있습니다. 필요에 따라 편집하세요."

알림 규칙 카드 그리드:
- 02-복원됨과 동일한 레이아웃
- 기본값: 13종 모두 isActive=true, 채널=SMS, 기본 메시지 적용
- "기본값" 라벨 배지 (text-xs text-gray-400) 각 카드 하단

구현:
- localStorage 없을 때 DEFAULT_ALARM_RULES 상수 사용
- DEFAULT_ALARM_RULES: 13종 규칙 기본값 정의
- 최초 편집 시 localStorage에 저장 시작
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- localStorage에 해당 키 없음 (최초 진입)
- "전체 초기화" 후

### 인터랙션 (User Actions)
1. 카드 편집 → DLG-072-001 (빈 폼 신규 모드)
2. 저장 후 → localStorage에 기록 → `02-복원됨` 상태 전환

### 비즈니스 룰
- 기본값 13종은 코드 상수로 정의 (DB 불필요)
- 첫 저장 시 localStorage 키 생성

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-072-001 편집 → `02-복원됨`
