# SCR-072 자동알림설정 — 상태: 복원됨 (localStorage 값 존재)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-072 |
| 상태 코드 | `restored` |
| 경로 | `/message/auto-alarm` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-072-001 (규칙 편집) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-072 자동알림설정 — 상태: 복원됨 (저장된 규칙 적용)

파일: src/app/(marketing)/message/auto-alarm/page.tsx

레이아웃:
- AppLayout + PageHeader("자동 알림 설정", 우측: "전체 초기화" Button variant=ghost)
- max-w-5xl mx-auto px-6 py-6

상단 안내:
- "저장된 설정을 불러왔습니다." 배지 (bg-blue-50 text-blue-600 text-xs px-3 py-1 rounded-full mb-4)

알림 규칙 카드 그리드 (grid grid-cols-3 gap-4):
13개 알림 규칙 카드 각각:
- 활성 카드 (isActive=true):
  · border-2 border-blue-500 bg-blue-50 rounded-xl p-4
  · 우상단 ping dot (w-2 h-2 bg-green-400 rounded-full animate-ping)
  · 규칙명 (text-sm font-semibold text-blue-800)
  · 채널 배지 (SMS/카카오/앱푸시)
  · 발송 시점 (text-xs text-blue-600)
  · 편집 아이콘 (DLG-072-001 트리거)
- 비활성 카드 (isActive=false):
  · border border-gray-200 bg-white opacity-75 rounded-xl p-4
  · 규칙명 (text-sm font-semibold text-gray-500)
  · 편집 아이콘

13종 규칙: 회원가입환영/이용권만료7일전/이용권만료1일전/이용권만료당일/
결제완료/수업예약확인/수업취소/수업리마인더/리드후속/장기미방문/생일축하/
포인트적립/계약체결

구현:
- const saved = localStorage.getItem(`settings_${branchId}_auto_alarm`)
- if (saved) setRules(JSON.parse(saved))
- isRestored = true → 안내 배지 표시

사용 컴포넌트:
- AppLayout, PageHeader, Button
- useAuthStore, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- localStorage에 `settings_{branchId}_auto_alarm` 키 존재
- JSON 파싱 성공

### 필수 데이터
| 블록 | 설명 |
|------|------|
| 규칙 설정 | localStorage JSON (채널/발송시점/메시지/활성여부) |

### 인터랙션 (User Actions)
1. 카드 편집 아이콘 → DLG-072-001 (기존값 주입)
2. 카드 토글 → isActive 전환 + localStorage 저장
3. "전체 초기화" → 기본값 13종으로 리셋 + toast.info

### 비즈니스 룰
- 활성 카드: accent border + ping dot (TC-072 규칙)
- 비활성 카드: opacity-75 + 회색 스타일
- 변경 즉시 localStorage 저장 (자동 저장)

### 에지 케이스
- JSON 파싱 오류 → `03-기본값` 상태로 폴백 + toast.warning

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-072-001 → 저장 후 카드 갱신
