# SCR-072 자동알림설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-072 |
| 상태 코드 | `loading` |
| 경로 | `/message/auto-alarm` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-복원됨` 또는 `03-기본값` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-072 자동알림설정 — 상태: 로딩

파일: src/app/(marketing)/message/auto-alarm/page.tsx

레이아웃:
- AppLayout + PageHeader("자동 알림 설정")
- max-w-5xl mx-auto px-6 py-6

Skeleton 구성:
- 설명 텍스트 영역: h-6 w-64 bg-gray-100 animate-pulse rounded mb-6
- 알림 규칙 카드 그리드 (grid grid-cols-3 gap-4):
  · 13개 카드 각각: h-40 bg-gray-100 animate-pulse rounded-xl

구현:
- useEffect(() => { loadSettings() }, [])
- loadSettings: localStorage.getItem('settings_{branchId}_auto_alarm') 읽기
- 동기 작업이므로 50ms timeout 후 렌더 (UX 일관성)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/message/auto-alarm` 진입 직후
- localStorage 읽기 전 초기 상태

### 연결 화면
- 다음: `02-복원됨` (localStorage 값 존재) 또는 `03-기본값` (localStorage 없음)
