# SCR-107 화면설계서 오버레이 — 상태: 빈 상태 (설계서 없음)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-107 |
| 상태 코드 | `spec-overlay-empty` |
| 경로 | 모든 인증 경로 (오버레이) |
| 역할 | superAdmin / primary / owner / manager |
| 우선순위 | P2 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-107_화면설계서오버레이/F6_상태별.md` |
| 이전 상태 | `01-로딩` (파일 없음) |
| 다음 상태 | `02-정상` (업로드 후) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-107 화면설계서 오버레이 — 상태: 빈 상태

파일: src/components/common/SpecOverlay.tsx (specContent=null 분기)

레이아웃:
- 동일 오버레이 패널

컴포넌트 구조:
1. 오버레이 헤더 (닫기 버튼만)
2. 빈 상태 블록 (flex-1 flex items-center justify-center flex-col):
   - FileX 아이콘 (w-16 h-16 text-gray-300 mx-auto)
   - "등록된 화면설계서가 없습니다" (text-base text-gray-500 mt-4)
   - 안내: "이 화면에 대한 설계서를 업로드하세요." (text-sm text-gray-400)
   - "파일 업로드" Button (outline, mt-6) → 파일 선택 input
   - 지원 형식: "MD, PDF, PNG, JPG (최대 20MB)"

데이터:
- specContent: null
- screenId: string
- 업로드: POST /spec-files { screenId, file }

인터랙션:
- "파일 업로드" → input[type=file] 열기
- 파일 선택 → 업로드 + `02-정상` 전환

사용 유틸:
- supabase, toast, lucide-react (FileX, Upload)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 해당 screenId의 설계서 파일 없음

### 필수 데이터
- screenId

### 인터랙션 (User Actions)
1. 파일 업로드 → 설계서 등록 → `02-정상`
2. 닫기 → 오버레이 닫힘

### 비즈니스 룰
- 업로드 권한: manager 이상
- 지원 형식: MD, PDF, PNG, JPG
- 최대 크기: 20MB

### 에지 케이스
- 업로드 실패 → 에러 토스트

### 접근성 (A11y)
- `role="status"` `aria-label="설계서 없음"`
- 업로드 버튼 `aria-label="화면설계서 파일 업로드"`

### 연결 화면
- 업로드 후: `02-정상`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-107_화면설계서오버레이/F6_상태별.md` → STATE_EMPTY 노드
