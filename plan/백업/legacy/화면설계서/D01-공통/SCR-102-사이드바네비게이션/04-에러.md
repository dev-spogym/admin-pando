# SCR-102 사이드바 네비게이션 — 상태: 에러

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-102 |
| 상태 코드 | `sidebar-error` |
| 경로 | 모든 인증 경로 (공통 레이아웃) |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-102_사이드바_네비게이션/F6_상태별.md` |
| 이전 상태 | `01-로딩` (메뉴 로드 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-102 사이드바 네비게이션 — 상태: 에러

파일: src/components/layout/AppLayout.tsx (isMenuError 분기)

레이아웃:
- 좌측 고정 w-64
- 메뉴 영역에 에러 UI 표시

컴포넌트 구조:
1. 로고 영역 (정상)
2. 에러 메뉴 영역:
   - AlertCircle 아이콘 (text-red-400, w-8 h-8, mx-auto mt-8)
   - "메뉴를 불러오지 못했습니다" (text-sm text-gray-500 text-center mt-2)
   - 재시도 Button (outline, text-sm, mt-4) → 메뉴 재로딩
3. 하단 프로필 (정상, 기본 정보만)

데이터:
- isMenuError: boolean = true
- retryLoadMenu: () => void

인터랙션:
- 재시도 버튼 → `01-로딩` 재진입
- 성공 시 → `02-펼침`

사용 유틸:
- useAuthStore, lucide-react (AlertCircle, RefreshCw)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 역할별 메뉴 구성 API 실패
- 권한 정보 로드 실패

### 필수 데이터
- retryLoadMenu 함수

### 인터랙션 (User Actions)
1. 재시도 버튼 → 메뉴 재로딩
2. 하단 프로필 클릭 → SCR-105 (기본 이동 가능)

### 비즈니스 룰
- 메뉴 에러 시에도 페이지 콘텐츠는 표시 (사이드바만 에러)
- 재시도 횟수 제한 없음

### 에지 케이스
- 재시도 반복 실패 → 에러 상태 유지
- 페이지 새로고침 → 전체 재시도

### 접근성 (A11y)
- 에러 영역 `role="alert"`
- 재시도 버튼 `aria-label="메뉴 다시 불러오기"`

### 연결 화면
- 재시도: `01-로딩` → `02-펼침`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-102_사이드바_네비게이션/F6_상태별.md` → STATE_ERROR 노드
