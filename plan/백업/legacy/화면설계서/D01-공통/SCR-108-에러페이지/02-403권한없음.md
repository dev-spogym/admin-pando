# SCR-108 에러 페이지 — 상태: 403 권한 없음

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-108 |
| 상태 코드 | `error-403` |
| 경로 | `/forbidden` |
| 역할 | all (접근 시도한 역할) |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-108_에러페이지/F6_상태별.md` |
| 이전 상태 | 권한 없는 화면 접근 |
| 다음 상태 | SCR-101 대시보드 |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 컴포넌트를 작성하라.
화면: SCR-108 에러 페이지 — 상태: 403 권한 없음

파일: src/app/(error)/forbidden/page.tsx

레이아웃:
- AppLayout 포함 (인증 상태)
- min-h-[calc(100vh-4rem)] flex items-center justify-center

컴포넌트 구조:
1. 에러 코드 (text-8xl font-black text-gray-200): "403"
2. ShieldOff 아이콘 (w-16 h-16 text-red-400 mx-auto)
3. 제목: "접근 권한이 없습니다" (text-2xl font-bold text-gray-700 mt-4)
4. 설명: "이 페이지에 접근할 수 있는 권한이 없습니다.\n필요한 경우 관리자에게 문의하세요." (text-sm text-gray-500)
5. 버튼:
   - "대시보드로 이동" Button (primary) → moveToPage('/')
6. 현재 역할 표시: "현재 역할: {role}" (text-xs text-gray-400 mt-6)

데이터:
- 에러 코드: 403
- user.role (현재 역할 표시용)

인터랙션:
- "대시보드로 이동" → /

사용 유틸:
- useAuthStore, moveToPage, lucide-react (ShieldOff, Home)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 미들웨어 권한 검사 실패 → `/forbidden` 리다이렉트
- API 403 응답 수신 후 리다이렉트
- 메뉴에 없는 URL 직접 접근 (권한 부족)

### 필수 데이터
| 항목 | 출처 | 비고 |
|------|------|------|
| 현재 역할 | useAuthStore | 표시용 |

### 인터랙션 (User Actions)
1. "대시보드로 이동" → SCR-101

### 비즈니스 룰
- 403 페이지에서 요청한 URL 노출 금지 (보안)
- 역할 정보는 참고용으로만 표시
- 관리자 문의 방법 별도 링크 없음 (내부 운영 정책)

### 에지 케이스
- 비인증 상태에서 403 접근 → SCR-100 리다이렉트 (미들웨어 처리)

### 접근성 (A11y)
- `<h1>` "접근 권한이 없습니다"
- 에러 코드 `aria-hidden="true"`

### 연결 화면
- 대시보드: SCR-101

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-108_에러페이지/F6_상태별.md` → STATE_FORBIDDEN 노드
