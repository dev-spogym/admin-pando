# SCR-109 로그아웃 — 상태: 로컬 데이터 클리어 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-109 |
| 상태 코드 | `logout-clearing` |
| 경로 | `/logout` |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` |
| 이전 상태 | `01-처리중` (API 완료) |
| 다음 상태 | `03-완료` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-109 로그아웃 — 상태: 로컬 데이터 클리어 중

파일: src/app/(auth)/logout/page.tsx (step='clearing' 분기)

레이아웃:
- 동일 전체 화면 로딩

컴포넌트 구조:
1. Loader2 스피너 (w-12 h-12 animate-spin text-blue-500)
2. "세션을 정리하는 중..." 텍스트 (text-sm text-gray-500 mt-4)

데이터:
클리어 대상:
- useAuthStore.reset() (Zustand store 초기화)
- localStorage.clear() (검색 기록, 사이드바 상태 등)
- sessionStorage.clear()
- supabase 캐시 초기화

인터랙션:
- 클리어 완료 → `03-완료`
- 동기 작업이므로 즉시 완료 (시각적 표시용)

사용 유틸:
- useAuthStore, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `01-처리중` 완료 후 자동 진행

### 필수 데이터
- 없음 (로컬 스토리지 클리어)

### 인터랙션 (User Actions)
- 모든 상호작용 차단
- 동기 작업 완료 → 즉시 `03-완료`

### 비즈니스 룰
- API 성공/실패 무관하게 반드시 클리어 실행 (보안)
- 클리어 목록: Zustand store, localStorage, sessionStorage, 쿠키

### 에지 케이스
- localStorage 접근 불가(Safari 프라이빗) → 에러 무시, 진행

### 접근성 (A11y)
- `aria-live="polite"` `role="status"`

### 연결 화면
- 완료: `03-완료`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` → STATE_CLEARING 노드
