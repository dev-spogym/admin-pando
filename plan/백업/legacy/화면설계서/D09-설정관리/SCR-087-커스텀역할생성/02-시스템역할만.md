# SCR-087 커스텀역할생성 — 상태: 시스템 역할만 (커스텀 역할 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-087 |
| 상태 코드 | `system-only` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-커스텀역할있음` (역할 생성 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-087 커스텀역할생성 — 상태: 시스템 역할만 + 새 역할 생성 CTA (TC-087-007)

파일: src/app/settings/permissions/page.tsx

레이아웃:
- PageHeader + "커스텀 역할" 탭 활성

시스템 역할 목록 (수정/삭제 불가):
{SYSTEM_ROLES.map(role => (
  <div key={role.code} className="flex items-center gap-3 px-4 py-3 rounded-lg border border-gray-100 bg-gray-50/50">
    <div className="w-8 h-8 rounded-full bg-gray-200 flex items-center justify-center">
      <Lock className="w-4 h-4 text-gray-400" />
    </div>
    <div className="flex-1">
      <p className="text-sm font-medium text-gray-700">{role.name}</p>
      <p className="text-xs text-gray-400">{role.description}</p>
    </div>
    <span className="text-xs text-gray-400 bg-gray-100 px-2 py-0.5 rounded-full">시스템</span>
  </div>
))}

커스텀 역할 없음 안내 + CTA (E_F6_087_04):
<div className="mt-6 flex flex-col items-center justify-center py-12 gap-4 border-2 border-dashed border-gray-200 rounded-xl">
  <UserCog className="w-10 h-10 text-gray-300" />
  <div className="text-center">
    <p className="text-sm font-medium text-gray-600">커스텀 역할이 없습니다</p>
    <p className="text-xs text-gray-400 mt-1">조직에 맞는 역할을 직접 만들어 보세요.</p>
  </div>
  <Button onClick={openCreateRole}>
    <Plus className="mr-2 h-4 w-4" />
    새 역할 만들기
  </Button>
</div>

SYSTEM_ROLES = ['최고관리자', '원장', '매니저', 'FC', '직원', '조회전용']
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- custom_roles = [] (커스텀 역할 없음)
- 이벤트: `E_F6_087_04`
- TC-087-007: 커스텀 역할 없음 → 시스템 역할만 + 생성 CTA

### UI 구성
| 요소 | 내용 |
|------|------|
| 시스템 역할 | Lock 아이콘, 수정/삭제 불가, "시스템" 배지 |
| 커스텀 영역 | 점선 테두리 + "새 역할 만들기" CTA |

### 비즈니스 룰
- 시스템 역할 행: Lock 아이콘, 수정/삭제 버튼 없음 (E_F6_087_06)
- primary, owner만 커스텀 역할 생성 가능

### 연결 화면
- 생성: DLG-081-003
