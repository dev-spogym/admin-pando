# SCR-089 데이터백업복원 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-089 |
| 상태 코드 | `loading` |
| 경로 | `/settings/backup` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-이력없음` / `03-이력있음` / `07-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-089 데이터백업복원 — 상태: 로딩

파일: src/app/settings/backup/page.tsx

Skeleton (animate-pulse, space-y-6):
- 자동 백업 카드: h-32 w-full bg-gray-200 rounded-xl
- 섹션 헤더: h-5 w-24 bg-gray-200 rounded
- 백업 이력 행 5개: h-16 bg-gray-200 rounded-lg

데이터 페치:
Promise.all([
  supabase.from('backup_settings').select('*').eq('branch_id', branchId).single(),
  supabase.from('backup_history').select('*').eq('branch_id', branchId).order('created_at', {ascending: false}).limit(20)
])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 이벤트: `E_F6_089_01`

### 연결 화면
- history=[] → `02-이력없음`
- 있음 → `03-이력있음`
- 실패 → `07-에러`
