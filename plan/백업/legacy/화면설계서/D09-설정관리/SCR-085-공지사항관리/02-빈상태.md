# SCR-085 공지사항관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-085 |
| 상태 코드 | `empty` |
| 경로 | `/notices` |
| 역할 | primary, owner, manager, fc, staff, readonly |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-목록-고정공지` (공지 등록 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-085 공지사항관리 — 상태: 빈 상태 (TC-085-006)

파일: src/app/notices/page.tsx

빈 상태 UI (notices.length === 0):
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <div className="w-16 h-16 rounded-full bg-gray-100 flex items-center justify-center">
    <Megaphone className="w-8 h-8 text-gray-300" />
  </div>
  <div className="text-center">
    <p className="text-base font-semibold text-gray-700">등록된 공지가 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">새 공지를 등록하여 직원과 회원에게 알려보세요.</p>
  </div>
  {hasPermission(role, 'notices:write') && (
    <Button onClick={openNoticeForm}>
      <Plus className="mr-2 h-4 w-4" />
      첫 공지 등록하기
    </Button>
  )}
</div>

TC-085-006: 공지 없음 → 빈 상태 + 등록 CTA
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- notices API 성공 + 결과 빈 배열
- 이벤트: `E_F6_085_04`

### UI 구성
| 요소 | 내용 |
|------|------|
| 아이콘 | Megaphone, gray-300, bg-gray-100 원형 |
| 주 메시지 | "등록된 공지가 없습니다" |
| 부 메시지 | "새 공지를 등록하여..." |
| CTA | "첫 공지 등록하기" (권한 있는 역할만) |

### 비즈니스 룰
- TC-085-006: 공지 없음 → 빈 상태 + 등록 CTA
- fc/staff/readonly: CTA 버튼 숨김 (조회 전용)

### 연결 화면
- 공지 등록: DLG-085-001
- 등록 후: `03-목록-고정공지`
