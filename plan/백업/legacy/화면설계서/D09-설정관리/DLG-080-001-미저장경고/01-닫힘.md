# DLG-080-001 미저장 경고 — 상태: 닫힘 (Closed)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-080-001 |
| 상태 코드 | `closed` |
| 트리거 화면 | SCR-080 |
| variant | warning |
| 우선순위 | P0 |
| 이전 상태 | 초기 |
| 다음 상태 | `02-열림` (isDirty=true + 탭 전환 시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
모달: DLG-080-001 미저장 경고 — 상태: 닫힘 (초기/닫힘 후)

초기 상태:
- isOpen: false
- showUnsavedModal: false
- pendingTab: null

트리거 조건:
- handleTabChange(newTab):
    if (isDirty) {
      setPendingTab(newTab)
      setShowUnsavedModal(true)  // → 열림 상태로 전환
    } else {
      setActiveTab(newTab)
    }

Modal 컴포넌트 (isOpen=false 시 렌더 없음):
<Modal open={showUnsavedModal} onOpenChange={setShowUnsavedModal}>
  ...
</Modal>
```

## 📝 디스크립션

### 사용 시점
- SCR-080 정상 상태 (isDirty=false) — 모달 비표시
- 이벤트 전: `[*] → Closed`

### 비즈니스 룰
- isDirty=false → 탭 전환 시 모달 없이 즉시 이동
- isDirty=true → 탭 전환 시 `02-열림` 전환

### 연결 화면
- 트리거: `02-열림`
