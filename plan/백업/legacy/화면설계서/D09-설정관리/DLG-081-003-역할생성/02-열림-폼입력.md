# DLG-081-003 역할 생성 — 상태: 열림 (폼 입력)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-081-003 |
| 상태 코드 | `open` |
| 트리거 화면 | SCR-081, SCR-087 |
| variant | primary |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-저장중` (저장) / `01-닫힘` (취소) |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-081-003 역할 생성 — 폼 입력

<Modal open={showCreateRoleModal}>
  <ModalContent className="max-w-lg">
    <ModalHeader><ModalTitle>새 역할 추가</ModalTitle></ModalHeader>
    <ModalBody className="space-y-4">
      <!-- 역할 이름 (필수) -->
      <div>
        <label className="text-sm font-medium text-gray-700">역할 이름 <span className="text-red-500">*</span></label>
        <Input
          value={form.name}
          onChange={e => setForm(p => ({...p, name: e.target.value}))}
          placeholder="예: 영업팀장"
          className="mt-1"
        />
      </div>
      <!-- 역할 코드 (필수) -->
      <div>
        <label className="text-sm font-medium text-gray-700">역할 코드 <span className="text-red-500">*</span></label>
        <Input
          value={form.code}
          onChange={e => setForm(p => ({...p, code: e.target.value.toLowerCase().replace(/[^a-z0-9_]/g, '')}))}
          placeholder="예: sales_manager"
          className="mt-1 font-mono"
        />
        {codeError && <p className="text-xs text-red-500 mt-1">{codeError}</p>}
      </div>
      <!-- 설명 -->
      <div>
        <label className="text-sm font-medium text-gray-700">설명</label>
        <Input value={form.description} onChange={...} placeholder="역할 설명 (선택)" className="mt-1" />
      </div>
      <!-- 기준 역할 -->
      <div>
        <label className="text-sm font-medium text-gray-700">기준 역할</label>
        <Select value={form.baseRole} onValueChange={v => setForm(p => ({...p, baseRole: v}))}>
          <option value="">권한 비어있게 시작</option>
          {systemRoles.map(r => <option key={r.code} value={r.code}>{r.name} 권한 복사</option>)}
        </Select>
      </div>
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <Button variant="outline" onClick={handleClose}>취소</Button>
      <Button
        disabled={!form.name || !form.code}
        onClick={handleCreate}
      >저장</Button>
    </ModalFooter>
  </ModalContent>
</Modal>
```

## 📝 디스크립션

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | primary |
| title | 새 역할 추가 |
| fields | 역할 이름(필수), 역할 코드(필수), 설명, 기준 역할 |

### 필드 검증
- 역할 이름: 비어있으면 저장 버튼 비활성
- 역할 코드: 소문자/숫자/언더스코어만, 중복 시 인라인 에러

### 결과 분기 (M3_081003)
| 결과 | 처리 |
|------|------|
| 200 | 역할 추가 + 자동 선택 + 모달 닫기 |
| 409 코드 중복 | 인라인 에러 + 모달 유지 |
| 500 | toast.error + 모달 유지 |

### 연결 화면
- 저장: `03-저장중`
- 취소: `01-닫힘`
