# SCR-086 출석관리(회원) — 상태: 출석 목록 (체크인/체크아웃/미체크인)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-086 |
| 상태 코드 | `has-records` |
| 경로 | `/attendance` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 날짜 변경 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-086 출석관리(회원) — 상태: 출석 목록 (체크인/완료/미체크인 행 구분)

파일: src/app/attendance/page.tsx

통계 카드 (flex gap-4 mb-6):
- 총 출석: {records.length}명
- 체크아웃 완료: {records.filter(r => r.checked_out_at).length}명
- 이용 중: {records.filter(r => !r.checked_out_at).length}명

출석 테이블 (각 행 스타일):

[체크인만 완료 행] (E_F6_086_06, checked_out_at=null):
<tr className="bg-blue-50/30">
  <td><Avatar /> {member.name}</td>
  <td>{format(record.checked_in_at, 'HH:mm')}</td>
  <td><span className="text-xs text-blue-600 font-medium">이용 중</span></td>
  <td>—</td>
  <td>
    <Button size="sm" variant="outline" onClick={() => manualCheckout(record.id)}>
      체크아웃
    </Button>
  </td>
</tr>

[체크인+체크아웃 완료 행] (E_F6_086_07):
<tr>
  <td>...</td>
  <td>{format(record.checked_in_at, 'HH:mm')}</td>
  <td><span className="text-xs text-green-600 font-medium">완료</span></td>
  <td>{format(record.checked_out_at, 'HH:mm')}</td>
  <td>{formatDuration(record.duration_min)}분</td>
</tr>

[미체크인 행] (E_F6_086_08, 예약 있으나 체크인 없음):
<tr className="bg-gray-50/50 opacity-70">
  <td>...</td>
  <td>—</td>
  <td><span className="text-xs text-gray-400">미출석</span></td>
  <td>—</td>
  <td>
    <Button size="sm" variant="ghost" onClick={() => manualCheckin(member.id)}>
      수동 체크인
    </Button>
  </td>
</tr>

수동 체크인/체크아웃:
- supabase.from('attendance').insert/update
- 성공 시 toast.success + 목록 갱신
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- attendance 레코드 존재
- 이벤트: `E_F6_086_05`, `E_F6_086_06`, `E_F6_086_07`, `E_F6_086_08`

### 행 유형별 스타일
| 상태 | 배경 | 배지 색 |
|------|------|---------|
| 이용 중 (체크인만) | bg-blue-50/30 | text-blue-600 "이용 중" |
| 완료 (체크인+아웃) | 기본 | text-green-600 "완료" |
| 미체크인 | bg-gray-50/50 opacity-70 | text-gray-400 "미출석" |

### 비즈니스 룰
- 이용 시간 = checked_out_at - checked_in_at (분 단위)
- 수동 체크인/아웃은 fc, staff 이상 허용
- 실시간 업데이트: supabase realtime subscription

### 연결 화면
- 날짜 변경: `01-로딩` (재조회)
