# SCR-088 다국어설정 — 상태: 미번역 항목 하이라이트

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-088 |
| 상태 코드 | `untranslated` |
| 경로 | `/settings/i18n` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `03-커스텀번역있음` |
| 다음 상태 | `05-전체번역완료` (모두 번역 후) |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-088 다국어설정 — 상태: 미번역 셀 하이라이트 (E_F6_088_06)

미번역 항목 카운트 배지 (테이블 상단):
{untranslatedCount > 0 && (
  <div className="flex items-center gap-2 mb-3 text-sm text-amber-600">
    <AlertTriangle className="w-4 h-4" />
    미번역 항목 {untranslatedCount}개가 있습니다
  </div>
)}

미번역 행 필터 버튼:
<Button
  variant={showOnlyUntranslated ? 'default' : 'outline'}
  size="sm"
  onClick={() => setShowOnlyUntranslated(v => !v)}
>
  미번역만 보기
</Button>

미번역 셀 스타일:
<td className="px-3 py-2 bg-amber-50 border border-amber-200 rounded">
  <span className="text-xs text-amber-500 italic">미번역 — 기본값 사용 중</span>
</td>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 커스텀 번역 키 중 값이 빈 항목 존재
- 이벤트: `E_F6_088_06`

### UI 변화
| 요소 | 표현 |
|------|------|
| 카운트 배지 | "미번역 N개" amber |
| 미번역 셀 | bg-amber-50 border-amber-200 italic |
| 필터 버튼 | "미번역만 보기" 토글 |

### 연결 화면
- 모두 번역 완료: `05-전체번역완료`
