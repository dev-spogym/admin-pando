# SCR-088 다국어설정 — 상태: 기본 번역만 (커스텀 번역 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-088 |
| 상태 코드 | `default-only` |
| 경로 | `/settings/i18n` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-커스텀번역있음` (번역 키 추가 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-088 다국어설정 — 상태: 기본 번역만 + 번역 키 추가 CTA (TC-088-005)

파일: src/app/settings/i18n/page.tsx

언어 탭 (TabNav):
- 한국어 (기본) | English | 日本語

기본 번역 테이블 (읽기 전용):
| 키 | 기본값 | 커스텀 번역 |
|-----|--------|------------|
| welcome | 환영합니다 | — |
| checkin | 체크인 | — |
| checkout | 체크아웃 | — |
...

커스텀 번역 없음 안내 + CTA (E_F6_088_04):
<div className="mt-6 flex items-center justify-between px-4 py-3 bg-blue-50 border border-blue-100 rounded-lg">
  <div>
    <p className="text-sm font-medium text-blue-800">커스텀 번역을 추가해 센터 특성에 맞게 변경할 수 있습니다</p>
    <p className="text-xs text-blue-600 mt-0.5">예: "체크인" → "출근", "회원" → "고객"</p>
  </div>
  <Button size="sm" onClick={openAddTranslation}>
    <Plus className="mr-1.5 h-3.5 w-3.5" />
    번역 키 추가
  </Button>
</div>

TC-088-005: 커스텀 번역 없음 → 기본 번역만 + 추가 CTA
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- i18n_custom = [] (커스텀 번역 없음)
- 이벤트: `E_F6_088_04`

### UI 구성
| 요소 | 내용 |
|------|------|
| 테이블 | 기본 번역 키/값 표시 (읽기) |
| 커스텀 열 | "—" 표시 |
| CTA 배너 | blue-50, "+ 번역 키 추가" |

### 비즈니스 룰
- TC-088-005: 커스텀 번역 없음 → 기본 번역만 + 추가 CTA
- 기본 번역은 수정 불가 (읽기 전용)

### 연결 화면
- 번역 키 추가 → `03-커스텀번역있음`
