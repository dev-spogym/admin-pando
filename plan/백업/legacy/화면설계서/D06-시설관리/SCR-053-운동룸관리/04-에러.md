# SCR-053 운동룸관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-053 |
| 상태 코드 | `error` |
| 경로 | `/facilities/rooms` |
| 역할 | owner, manager, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-053 운동룸 관리 — 에러
조건: E_F6_ERR_01 (API 실패 → error 토스트)

에러 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <AlertCircleIcon className="w-12 h-12 text-red-400 mb-4" />
  <p className="text-lg font-semibold text-gray-700 mb-1">운동룸 정보를 불러오지 못했습니다</p>
  <Button onClick={retry} variant="outline">다시 시도</Button>
</div>
toast.error("운동룸 데이터를 불러오지 못했습니다.")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_ERR_01`: API 실패

### 인터랙션 (User Actions)
1. "다시 시도" → loadRooms() 재호출

### 연결 화면
- 다음: 재시도 성공 → `02-정상`
