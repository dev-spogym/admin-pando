# SCR-058 청소스케줄 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-058 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/cleaning` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-058 청소 스케줄 — 상태: 로딩 (신규 기능)

파일: src/app/(facilities)/cleaning/page.tsx

Skeleton UI:
1. 날짜 헤더 + 진행률 바 Skeleton (h-12 mb-4)
2. 필터 바 Skeleton (h-10 mb-4)
3. DataTable Skeleton:
   - 헤더 h-10 bg-gray-100
   - 행 8개 h-14 animate-pulse

useEffect: supabase
  .from('cleaning_schedules')
  .select('*')
  .eq('branch_id', branchId)
  .gte('scheduled_at', todayStart)
  .lte('scheduled_at', todayEnd)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-058 진입 후 오늘 스케줄 API 대기

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
