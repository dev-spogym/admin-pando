# SCR-051 사물함배정관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `error` |
| 경로 | `/facilities/locker/management` |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함배정관리 — 상태: 에러

파일: src/app/(facilities)/locker/management/page.tsx

조건: fetch 5xx 또는 네트워크 오류

에러 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <AlertCircleIcon className="w-12 h-12 text-red-400 mb-4" />
  <p className="text-lg font-semibold text-gray-700 mb-1">데이터를 불러오지 못했습니다</p>
  <p className="text-sm text-gray-400 mb-6">서버 오류가 발생했습니다.</p>
  <Button onClick={retry} variant="outline">다시 시도</Button>
</div>

toast.error("사물함 데이터를 불러오지 못했습니다.")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_05`: API 5xx 응답

### 인터랙션 (User Actions)
1. "다시 시도" → loadLockers() 재호출

### 연결 화면
- 다음: 재시도 성공 → `02-정상` / 실패 → 에러 유지
