# DLG-P001 전지점 배포 — 상태: 로딩 (지점 목록)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P001 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P001 "전지점 배포" 버튼 클릭 |
| 역할 | owner |
| 우선순위 | P0 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-지점선택` / `04-로드실패` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P001 전지점 배포 — 상태: 로딩

파일: src/components/products/DeployAllBranchesModal.tsx

레이아웃 (Modal w-[480px]):
- 모달 헤더: "전지점 배포"
- 본문: 지점 목록 로딩 중
  <div className="space-y-2 py-4">
    {[1,2,3,4].map(i => (
      <div key={i} className="h-10 bg-gray-100 rounded-lg animate-pulse" />
    ))}
  </div>
- 하단 버튼: "취소" (활성), "배포 확인" (disabled)

진입 즉시 branches 조회:
  const { data: branches } = await supabase.from('branches').select('*')

사용 컴포넌트: Modal, Button
사용 유틸: supabase, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 모달 오픈 후 지점 목록 API 응답 전

### 인터랙션 (User Actions)
- "취소" → 모달 닫기

### 연결 화면
- 다음: `02-지점선택` / `04-로드실패`
