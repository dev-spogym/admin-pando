# DLG-P015 입출고 이력 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P015 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P007 "이력 보기" 버튼 클릭 |
| 역할 | owner, manager, front |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-이력있음` / `03-이력없음` / `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P015 입출고 이력 — 상태: 로딩

파일: src/components/inventory/InventoryLogModal.tsx

레이아웃 (Modal w-[640px]):
- 모달 헤더: "입출고 이력 — {product.name}"
- 본문: Skeleton 행 6개 (h-10 animate-pulse)
- 하단: "닫기"

진입 즉시:
  const { data } = await supabase
    .from('inventory_logs')
    .select('*')
    .eq('product_id', product.id)
    .order('created_at', { ascending: false })
    .limit(50)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 이력 보기 버튼 클릭 후 API 응답 전

### 연결 화면
- 다음: `02-이력있음` / `03-이력없음` / `04-에러`
