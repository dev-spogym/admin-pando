# DLG-P010 카탈로그 설정 — 상태: 설정 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P010 |
| 상태 코드 | `settings-form` |
| 트리거 | 설정 데이터 로드 완료 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 저장 → `03-저장결과` / 취소 → 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P010 카탈로그 설정 — 상태: 설정 폼

파일: src/components/catalog/CatalogSettingsModal.tsx (설정 로드 완료)

폼 필드:
- 카탈로그 제목 Input (브랜드명 등)
- 카탈로그 설명 Textarea
- 로고 이미지 업로드 (파일 Input + 미리보기)
- 테마 색상 ColorPicker or Select (프리셋 5개)
- 가격 표시 여부 Toggle (기본: true)
- 연락처 표시 여부 Toggle

기존 설정값 프리필 후 편집 가능
하단: "취소" + "저장"

사용 컴포넌트: Modal, Button
사용 유틸: supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- catalog_settings 로드 완료

### 인터랙션 (User Actions)
1. 설정 변경 후 "저장" → `03-저장결과`
2. "취소" → 닫기

### 비즈니스 룰
- 기존 설정 프리필
- UPSERT (없으면 INSERT, 있으면 UPDATE)

### 연결 화면
- 저장: `03-저장결과`
