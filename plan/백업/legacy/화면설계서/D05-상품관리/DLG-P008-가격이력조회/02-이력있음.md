# DLG-P008 가격 이력 조회 — 상태: 이력 있음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P008 |
| 상태 코드 | `has-logs` |
| 트리거 | audit_log 조회 완료 (이력 있음) |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 닫기 → 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P008 가격 이력 조회 — 상태: 이력 있음

파일: src/components/discount/PriceHistoryModal.tsx (logs.length > 0)

레이아웃:
- 이력 테이블:
  컬럼: 일시 | 변경 전 가격 | 변경 후 가격 | 변경 사유 | 처리자
  행 렌더링:
    {logs.map(log => (
      <tr key={log.id}>
        <td>{format(new Date(log.created_at), 'yyyy.MM.dd HH:mm')}</td>
        <td className="text-gray-500 line-through">{formatNumber(log.before_price)}원</td>
        <td className="font-semibold">{formatNumber(log.after_price)}원</td>
        <td>{log.reason ?? '-'}</td>
        <td>{log.actor_name}</td>
      </tr>
    ))}
- 페이지네이션 (20건/페이지)
- 하단: "닫기"

사용 유틸: format (date-fns), formatNumber
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- audit_log 조회 완료 + 1건 이상

### 테이블 컬럼

| 컬럼 | 설명 |
|------|------|
| 일시 | yyyy.MM.dd HH:mm 포맷 |
| 변경 전 가격 | line-through 스타일 |
| 변경 후 가격 | font-semibold 강조 |
| 변경 사유 | 없을 경우 '-' |
| 처리자 | 담당 직원명 |

### 인터랙션 (User Actions)
1. 페이지네이션 → 이전/다음
2. "닫기" → 모달 닫기

### 비즈니스 룰
- 최신순 정렬 (created_at DESC)
- 20건씩 페이지네이션

### 연결 화면
- 닫기: SCR-P004 복귀
