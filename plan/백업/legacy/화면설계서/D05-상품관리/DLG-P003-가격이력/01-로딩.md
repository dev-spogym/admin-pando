# DLG-P003 가격 이력 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P003 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P003 "가격 이력" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-이력있음` / `03-이력없음` / `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P003 가격 이력 — 상태: 로딩

파일: src/components/products/PriceHistoryModal.tsx

레이아웃 (Modal w-[560px]):
- 모달 헤더: "가격 변경 이력 — {product.name}"
- 본문: Skeleton 행 5개 (h-10 animate-pulse bg-gray-100 rounded mb-2)
- 하단: "닫기" 버튼

진입 즉시:
  const { data } = await supabase
    .from('audit_log')
    .select('*')
    .eq('table_name', 'products')
    .eq('record_id', product.id)
    .eq('field', 'price')
    .order('created_at', { ascending: false })

사용 컴포넌트: Modal, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 가격 이력 버튼 클릭 후 API 응답 전

### 연결 화면
- 다음: `02-이력있음` / `03-이력없음` / `04-에러`
