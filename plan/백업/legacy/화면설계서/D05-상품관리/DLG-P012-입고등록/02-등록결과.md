# DLG-P012 입고 등록 — 상태: 등록 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P012 |
| 상태 코드 | `register-result` |
| 트리거 | 입고 등록 API 응답 |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 성공 → 닫힘 / 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P012 입고 등록 — 상태: 등록 결과

파일: src/components/inventory/InboundModal.tsx

결과 분기:
1. 성공 (201):
   // inventory_logs INSERT + inventory.current_stock 증가
   toast.success('입고가 등록되었습니다.')
   onClose()
   onRefresh()  // SCR-P007 재고 목록 갱신

2. 실패:
   toast.error('입고 등록에 실패했습니다.')
   setSaving(false)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- inventory_logs INSERT + inventory UPDATE API 응답

### 비즈니스 룰
- 성공: current_stock 증가 + inventory_logs 기록
- 실패: error 토스트 + 모달 유지

### 연결 화면
- 성공: SCR-P007 재고 수량 갱신
