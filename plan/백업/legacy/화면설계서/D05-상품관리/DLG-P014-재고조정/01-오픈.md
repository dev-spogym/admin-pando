# DLG-P014 재고 조정 — 상태: 오픈

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P014 |
| 상태 코드 | `open` |
| 트리거 | SCR-P007 "재고 조정" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | 조정 확인 → `02-조정결과` / 취소 → 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P014 재고 조정 — 상태: 오픈

파일: src/components/inventory/StockAdjustModal.tsx

레이아웃 (Modal w-[400px]):
- 모달 헤더: "재고 조정 — {product.name}"
- 현재 재고 표시: "현재 재고: {currentStock}개" (읽기 전용)
- 폼:
  - 조정 유형 Select: 증가(+) / 감소(-) / 직접 설정
  - 조정 수량 Input (숫자, required)
  - 조정 후 예상 재고: "{예상값}개" (실시간 계산)
  - 조정 사유 Textarea (required, 최소 5자)
- 조정 후 재고 < 0 → 경고 표시 + 확인 버튼 disabled
- 하단: "취소" + "조정 확인"

사용 컴포넌트: Modal, Button, Select
사용 유틸: supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P007 DataTable 재고 조정 버튼 클릭 (특정 상품 행)

### 인터랙션 (User Actions)
1. 조정 유형 선택 → 예상 재고 실시간 계산
2. 수량 입력 → 예상 재고 갱신
3. 사유 입력
4. "조정 확인" → `02-조정결과`

### 비즈니스 룰
- 조정 후 재고 0 이상 강제 (음수 불가)
- 사유 필수 (감사 로그용)

### 연결 화면
- 조정: `02-조정결과`
