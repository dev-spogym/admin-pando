# DLG-P014 재고 조정 — 상태: 조정 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P014 |
| 상태 코드 | `adjust-result` |
| 트리거 | 재고 조정 API 응답 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 성공 → 닫힘 / 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P014 재고 조정 — 상태: 결과 분기

파일: src/components/inventory/StockAdjustModal.tsx

결과 분기:
1. 성공:
   toast.success('재고가 수정되었습니다.')
   onClose(); onRefresh()
   // inventory.current_stock 갱신
   // inventory_logs INSERT (type: 'adjustment', reason: ...)

2. 실패:
   toast.error('재고 수정에 실패했습니다.')
   setSaving(false)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- inventory UPDATE + inventory_logs INSERT API 응답

### 비즈니스 룰
- 성공: current_stock 갱신 + inventory_logs 기록
- 실패: error 토스트 + 모달 유지

### 연결 화면
- 성공: SCR-P007 재고 수량 갱신
