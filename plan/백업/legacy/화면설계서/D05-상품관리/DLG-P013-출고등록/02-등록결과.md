# DLG-P013 출고 등록 — 상태: 등록 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P013 |
| 상태 코드 | `register-result` |
| 트리거 | 출고 등록 API 응답 |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 성공 → 닫힘 / 재고 0 → 경고 후 닫힘 / 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P013 출고 등록 — 상태: 등록 결과

파일: src/components/inventory/OutboundModal.tsx

결과 분기:
1. 성공, 재고 남음:
   toast.success('출고가 등록되었습니다.')
   onClose(); onRefresh()

2. 성공, 재고 0 도달:
   toast.success('출고 완료. 해당 상품 재고가 소진되었습니다.', { icon: '⚠️' })
   onClose(); onRefresh()
   // SCR-P007에서 해당 상품 "소진" 배지 표시

3. 실패 (수량 초과 등):
   toast.error('출고 등록에 실패했습니다.')
   setSaving(false)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- inventory_logs INSERT + inventory UPDATE API 응답

### 비즈니스 룰
- 성공: current_stock 차감 + inventory_logs 기록
- 재고 0 도달: 경고 포함 성공 토스트
- 실패: error 토스트 + 모달 유지

### 연결 화면
- 성공: SCR-P007 재고 갱신
