# DLG-P002 작업 취소 확인 — 상태: 변경 폐기

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P002 |
| 상태 코드 | `discard` |
| 트리거 | "변경 폐기" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 이전 화면 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P002 작업 취소 확인 — 상태: 변경 폐기 (확인 클릭)

파일: src/components/common/UnsavedChangesModal.tsx (confirm 클릭)

구현:
const handleDiscard = () => {
  resetForm()       // 폼 초기화
  onClose()         // 모달 닫기
  router.back()     // 이전 화면으로 이동
  // 또는 onConfirm() 콜백 호출
}

패널 모드에서:
  panelClose()      // 패널 닫기
  setSelectedProduct(null)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- "변경 폐기" 버튼 클릭

### 비즈니스 룰
- 폼 데이터 초기화 (resetForm)
- 이전 화면으로 이동
- 변경 사항 완전 폐기

### 연결 화면
- SCR-P001 또는 이전 화면
