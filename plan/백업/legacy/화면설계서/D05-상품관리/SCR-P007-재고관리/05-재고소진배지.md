# SCR-P007 재고 관리 — 상태: 재고 소진 배지

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P007 |
| 상태 코드 | `zero-stock-badge` |
| 경로 | `/products/inventory` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `02-정상` (재고 0 상품 포함) |
| 다음 상태 | 입고 → DLG-P012 / 조정 → DLG-P014 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P007 재고 관리 — 상태: 재고 소진 (current_stock === 0)

파일: src/app/products/inventory/page.tsx (current_stock === 0)

구현:
소진 행:
  <tr className="bg-red-50">

소진 배지:
  <StatusBadge variant="red">소진</StatusBadge>

조정 버튼 강조:
  <Button className="ring-2 ring-red-400 text-red-600">재고 조정</Button>

상단 긴급 배너:
  <div className="bg-red-50 border border-red-200 rounded-lg p-3 mb-4">
    <p className="text-sm text-red-700 font-semibold">재고 소진 상품 {zeroCount}개 — 즉시 입고 등록이 필요합니다</p>
  </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `current_stock === 0` 인 상품 존재

### 비즈니스 룰
- 빨간 "소진" 배지 + 행 bg-red-50
- 조정 버튼 빨간 ring 강조
- 상단 긴급 배너 표시

### 연결 화면
- DLG-P012 (입고), DLG-P014 (재고 조정)
