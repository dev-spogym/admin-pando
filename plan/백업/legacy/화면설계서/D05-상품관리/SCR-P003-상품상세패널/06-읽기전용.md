# SCR-P003 상품 상세 패널 — 상태: 읽기 전용

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P003 |
| 상태 코드 | `panel-readonly` |
| 경로 | SCR-P001 우측 패널 (trainer/front) |
| 역할 | trainer, front |
| 우선순위 | P1 |
| 이전 상태 | SCR-P001 행 클릭 (trainer/front) |
| 다음 상태 | 닫기 → SCR-P001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P003 상품 상세 패널 — 상태: 읽기 전용 (trainer/front)

파일: src/components/products/ProductDetailPanel.tsx (mode="readonly")

레이아웃:
- 패널 헤더: "상품 상세" + 상태 배지 + X 닫기
- 폼 필드: disabled (시각적으로 조회 전용 표시)
  각 필드 bg-gray-50 pointer-events-none
- 하단 버튼바: "닫기" 버튼만 (저장/삭제/가격이력 버튼 없음)

구현:
const isReadOnly = !hasPermission('canEditProduct')
// isReadOnly=true → 모든 Input disabled, 버튼 숨김

사용 컴포넌트: Button, StatusBadge
사용 유틸: hasPermission, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- trainer, front 역할로 SCR-P001 행 클릭 시

### 인터랙션 (User Actions)
1. "닫기" 버튼 → 패널 닫기

### 비즈니스 룰
- 저장/삭제/가격이력 버튼 미노출
- 모든 폼 필드 disabled
- 패키지 섹션은 조회만 가능 (토글 불가)

### 연결 화면
- 닫기: SCR-P001
