# DLG-P007 할인 정책 추가/수정 — 상태: 저장 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P007 |
| 상태 코드 | `save-result` |
| 트리거 | 저장 API 응답 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-신규모드` 또는 `02-수정모드` |
| 다음 상태 | 성공 → 닫힘 / 중복 → 인라인 에러 / 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P007 할인 정책 — 상태: 저장 결과 분기

파일: src/components/discount/DiscountPolicyModal.tsx

결과 분기:
1. 신규 등록 성공:
   toast.success('할인 정책이 등록되었습니다.')
   onClose(); onRefresh()

2. 수정 성공:
   toast.success('할인 정책이 수정되었습니다.')
   onClose(); onRefresh()

3. 정책명 중복 (409):
   setError('name', { message: '이미 사용 중인 정책명입니다.' })
   // 인라인 에러 표시, 모달 유지

4. 서버 오류 (500):
   toast.error('저장에 실패했습니다.')
   // 모달 유지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 저장 API 응답 수신

### 비즈니스 룰
- 성공: toast + 자동 닫힘
- 중복명: 인라인 에러 (toast 없음)
- 서버 오류: error 토스트 + 모달 유지

### 연결 화면
- 성공: SCR-P004 갱신
