# SCR-P008 시즌 가격 관리 — 상태: 만료 규칙 처리

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P008 |
| 상태 코드 | `expired-style` |
| 경로 | `/products/seasonal-prices` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (만료 규칙 포함) |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P008 시즌 가격 관리 — 상태: 만료 규칙 처리

파일: src/app/products/seasonal-prices/page.tsx (만료 규칙 행)

구현:
만료 규칙 행 (today > end_date):
  <tr className="opacity-50">

만료 배지:
  <StatusBadge variant="gray">만료</StatusBadge>

수정 버튼: disabled
  <Button disabled className="cursor-not-allowed opacity-40">수정</Button>

만료 행 텍스트: text-gray-400 line-through (시즌명)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `today > end_date` 인 규칙이 목록에 포함

### 비즈니스 룰
- 만료 행: 회색 처리 (opacity-50)
- 수정 버튼: disabled
- 삭제 버튼: 활성 (정리 가능)

### 연결 화면
- 없음 (만료 행은 수정 불가)
