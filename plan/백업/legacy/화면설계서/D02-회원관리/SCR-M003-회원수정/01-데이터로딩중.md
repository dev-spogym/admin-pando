# SCR-M003 회원수정 — 상태: 데이터 로딩 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M003 |
| 상태 코드 | `skeleton` |
| 경로 | `/members/edit?id={id}` |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M003_회원수정/F6_상태별.md` |
| 이전 상태 | 진입 (마운트) |
| 다음 상태 | `02-prefill완료` / `03-로드실패` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M003 회원수정 — 상태: 데이터 로딩 중 (스켈레톤)

파일: src/app/members/edit/page.tsx

레이아웃: AppLayout > PageHeader + StepIndicator + FormCard(스켈레톤)

컴포넌트 구조:
- PageHeader: title="회원 수정" (정상 표시)
- StepIndicator: Step1 활성 (스켈레톤)
- FormCard 스켈레톤:
  각 필드 영역을 h-10 bg-gray-200 rounded animate-pulse 으로 대체
  [이름, 연락처, 이메일, 담당FC, 메모] 각각 skeleton 표시
- BottomBar: 버튼 모두 disabled

API: GET /api/members/{id} 호출 중
쿼리 파라미터: searchParams.get('id') 로 id 추출

사용 유틸: useAuthStore, supabase, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점
- `/members/edit?id={id}` 마운트 직후 GET /api/members/{id} 호출 중

### 필수 데이터
| 항목 | 상태 |
|------|------|
| memberId | URL 쿼리에서 추출 |
| member 데이터 | 아직 없음 (로딩 중) |

### 인터랙션
- 모든 입력/버튼 disabled

### 비즈니스 룰
- 로드 실패 시 toast.error + /members 이동
- readonly 역할 진입 불가

### 에지 케이스
- id 파라미터 없음: 즉시 /members 리다이렉트

### 접근성
- 폼 영역 `aria-busy="true"`

### 연결 화면
- 성공: `02-prefill완료`
- 실패: `03-로드실패`

### 다이어그램 참조
- 엣지: `E_INIT_01`
- 상태: `STATE_SKELETON`
