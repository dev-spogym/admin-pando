# SCR-M004 회원상세 — 상태: 로딩 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M004 |
| 상태 코드 | `loading` |
| 경로 | `/members/detail?id={id}` |
| 역할 | primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M004_회원상세/F6_상태별.md` |
| 이전 상태 | 진입 |
| 다음 상태 | `02-ACTIVE` / `03-HOLDING` / `04-EXPIRED` / `05-SUSPENDED` / `06-INACTIVE` / `07-null` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M004 회원상세 — 상태: 로딩 중

파일: src/app/members/detail/page.tsx

레이아웃: AppLayout > 중앙 스피너

컴포넌트 구조:
- isLoading=true 조건부 렌더링:
  <div className="flex flex-col items-center justify-center min-h-[60vh]">
    <Loader2 className="w-10 h-10 animate-spin text-blue-500 mb-3" />
    <p className="text-sm text-gray-400">회원 정보를 불러오는 중...</p>
  </div>

API: GET /api/members/{id} (member 전체 정보 + tabs 기본 데이터)

사용 유틸: supabase, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점
- `/members/detail?id={id}` 마운트 직후
- 탭 전환 시 해당 탭 데이터 로딩 중

### 필수 데이터
| 항목 | 상태 |
|------|------|
| memberId | URL 쿼리 |
| member | 로딩 중 |

### 인터랙션
- 모든 탭/버튼 비활성

### 비즈니스 룰
- member null → `07-null` 분기
- 로드 완료 → member.status 분기

### 에지 케이스
- id 없으면 /members 이동

### 접근성
- `aria-busy="true"`, `aria-label="회원 정보 로딩 중"`

### 연결 화면
- 로드 완료 → status별 화면

### 다이어그램 참조
- 엣지: `E_F6_01`
- 상태: `SPINNER`
