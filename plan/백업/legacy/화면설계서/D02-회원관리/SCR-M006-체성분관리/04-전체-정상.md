# SCR-M006 체성분관리 — 상태: 전체 (2건 이상, 정상)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M006 |
| 상태 코드 | `full-data` |
| 경로 | `/body-composition?memberId={id}` |
| 역할 | primary / owner / manager / fc |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M006_체성분관리/F6_상태별화면.md` |
| 이전 상태 | `01-로딩중` |
| 다음 상태 | `06-차트빈상태` (토글 OFF 시) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M006 체성분관리 — 상태: 전체 (2건+, 라인 차트 포함)

파일: src/app/body-composition/page.tsx

컴포넌트:
- PageHeader: title="체성분 관리", rightSlot=<Button onClick={() => openDlg015()}>측정 추가</Button>
- StatCardGrid (4개):
  <StatCard label="체중" value={latest.weight} unit="kg"
    change={latest.weight - prev.weight} changeLabel="이전 대비" />
  (골격근량, 체지방률, BMI 동일 패턴)
- MetricToggleBar: 체크박스로 차트 메트릭 ON/OFF
  [체중, 골격근량, 체지방률, BMI] 토글
- LineChart (Recharts or Chart.js):
  - x축: 측정일
  - y축: 선택된 메트릭 값
  - 활성 메트릭별 다른 색상 라인
- DataTable: 전체 측정 이력
  columns: [측정일, 체중, 골격근량, 체지방률, BMI, 비고, 액션]
  행 액션: 삭제 → DLG-M016 패턴
- 목표 설정 Button → DLG-M017 (🆕 미구현)

인터랙션:
- 메트릭 토글 OFF 전부 → `06-차트빈상태`
- 측정 추가 → DLG-M015
- 목표 설정 → DLG-M017

데이터:
- bodyCompositions: 전체 이력 배열 (날짜순)
- latest: 최신 측정값
- prev: 직전 측정값

사용 유틸: useAuthStore, supabase, recharts (LineChart), lucide-react
```

## 📝 디스크립션

### 사용 시점
- body_compositions 데이터 2건 이상

### 필수 데이터
| 테이블 | 조건 |
|--------|------|
| body_compositions | member_id = memberId, ORDER BY measured_at DESC |

### 인터랙션
1. 측정 추가 → DLG-M015
2. 동일 날짜 존재 시 → DLG-M016 (덮어쓰기)
3. 메트릭 토글 → 차트 라인 표시/숨김
4. 목표 설정 → DLG-M017

### 비즈니스 룰
- change = 최신값 - 직전값 (양수=증가, 음수=감소)
- 차트 메트릭 모두 OFF → `06-차트빈상태`

### 다이어그램 참조
- 엣지: `E_COUNT_MULTI_01`
- 상태: `UI_FULL`
