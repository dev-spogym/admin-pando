# SCR-C014 출석QR체크인 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C014 |
| 상태 코드 | `empty` |
| 경로 | `/qr-checkin` |
| 우선순위 | P2 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <QrCode className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">오늘 예정된 수업이 없습니다.</p>
  <p className="text-sm text-gray-400">수업이 등록되면 QR 체크인을 사용할 수 있습니다.</p>
  <Button variant="outline" onClick={() => moveToPage('/calendar')}>
    캘린더에서 확인
  </Button>
</div>
```

## 📝 디스크립션
- 오늘 lesson_schedules 없음
- 캘린더 이동 유도
