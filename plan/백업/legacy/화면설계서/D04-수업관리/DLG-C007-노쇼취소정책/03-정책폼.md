# DLG-C007 노쇼/취소정책 — 상태: 정책 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C007 |
| 상태 코드 | `policy-form` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 200) |
| 다음 상태 | 저장 → `04-저장결과` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/LessonPolicyModal.tsx

모달 헤더: "노쇼/취소 정책 설정"
모달 크기: w-[520px]

Zod 스키마:
const policySchema = z.object({
  noshoPenaltyPoint: z.number().min(0, "0 이상 정수를 입력해주세요.").int(),
  cancelHourThreshold: z.number().min(1).max(72, "1~72시간 사이로 입력해주세요.").int(),
  maxNoshoCount: z.number().min(1).max(10, "1~10회 사이로 입력해주세요.").int(),
  autoProcess: z.boolean(),
})

폼 필드 (프리필 포함):
1. 노쇼 패널티 포인트 (Input number, min=0)
2. 취소 기준 시간 (Input number, 1~72, suffix="시간 이내")
3. 최대 노쇼 허용 횟수 (Input number, 1~10, suffix="회")
4. 자동 처리 여부 (Switch)

모달 푸터:
- "취소" Button outline → onClose()
- "저장" Button primary → 유효성 → PUT /api/lesson-policy
```

## 📝 디스크립션

### 표시 데이터
| 필드 | 규칙 |
|------|------|
| 노쇼 패널티 포인트 | 0 이상 정수 |
| 취소 기준 시간 | 1~72시간 |
| 최대 노쇼 횟수 | 1~10회 |
| 자동 처리 | boolean Switch |

### 연결 화면
- 저장: `04-저장결과`
- 닫기: `01-닫힘`
