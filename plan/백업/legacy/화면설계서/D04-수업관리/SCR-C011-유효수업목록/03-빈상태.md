# SCR-C011 유효수업목록 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `empty` |
| 경로 | `/valid-lessons` |
| 우선순위 | P1 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <CalendarX2 className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">예정된 수업이 없습니다.</p>
  <p className="text-sm text-gray-400">수업을 등록하거나 날짜 범위를 변경해보세요.</p>
  <div className="flex gap-3">
    <Button variant="outline" onClick={resetFilter}>필터 초기화</Button>
    {hasPermission(['manager','fc']) && (
      <Button onClick={() => moveToPage('/class-schedule')}>시간표 등록</Button>
    )}
  </div>
</div>
```

## 📝 디스크립션
- 선택 기간에 유효 수업 없음
- 필터 초기화 또는 시간표 등록 유도
