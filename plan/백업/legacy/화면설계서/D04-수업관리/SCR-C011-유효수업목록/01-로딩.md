# SCR-C011 유효수업목록 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `loading` |
| 경로 | `/valid-lessons` |
| 역할 | manager, fc, staff, readonly |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C011 유효수업목록 — 상태: 로딩

파일: src/app/(classes)/valid-lessons/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "유효 수업 목록")
- SearchFilter Skeleton
- DataTable loading=true (Skeleton 10행)

데이터:
- SELECT ls.*, c.name as class_name, u.name as instructor_name
  FROM lesson_schedules ls
  JOIN classes c ON ls.class_id=c.id
  JOIN users u ON ls.instructor_id=u.id
  WHERE ls.branch_id=? AND ls.status NOT IN ('cancelled')
  AND ls.start_at >= NOW()
  ORDER BY ls.start_at ASC
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C011 진입
- 필터 변경 후 재조회

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
