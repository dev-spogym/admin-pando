# DLG-C008 일괄생성확인 — 상태: 생성 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C008 |
| 상태 코드 | `create-result` |
| 부모 화면 | SCR-C003 시간표일괄등록 |
| 역할 | manager, owner |
| 우선순위 | P0 |
| 이전 상태 | `03-생성중` (207 부분성공) |
| 다음 상태 | 확인 → `01-닫힘` + SCR-C003 리셋 |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/BulkCreateConfirmModal.tsx (부분성공 UI)

부분성공 결과 표시 (conflicts.length > 0):
<div className="p-6 space-y-4">
  <div className="flex items-center gap-2 text-yellow-700">
    <AlertTriangle className="w-5 h-5" />
    <p className="font-medium">일부 수업 생성에 실패했습니다.</p>
  </div>
  <p className="text-sm text-gray-600">
    성공: {result.createdCount}개 / 실패: {conflicts.length}개
  </p>
  <div className="max-h-40 overflow-y-auto rounded border divide-y text-sm">
    {conflicts.map((c, i) => (
      <div key={i} className="px-3 py-2 text-gray-700">
        {format(new Date(c.date), 'MM.dd(eee)', { locale: ko })} {c.time} — {c.reason}
      </div>
    ))}
  </div>
  <div className="flex justify-end">
    <Button onClick={() => { onSuccess(); onClose(); }}>확인</Button>
  </div>
</div>
```

## 📝 디스크립션

### API 결과 분기
| 응답 | 동작 |
|------|------|
| 200 전체성공 | success 토스트 + 모달 닫힘 + SCR-C003 리셋 |
| 207 부분성공 | 충돌 목록 표시 → 확인 시 닫힘 |
| 403/500 | warning/error + 모달 유지 |

### 연결 화면
- 확인: `01-닫힘` + SCR-C003 리셋
