# SCR-C004 그룹수업템플릿 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `loading` |
| 경로 | `/class-templates` |
| 역할 | manager, fc, readonly |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 그룹수업 템플릿 관리 — 상태: 로딩

파일: src/app/(classes)/class-templates/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "그룹수업 템플릿 관리")
- 상단 SearchFilter Skeleton (h-10 animate-pulse)
- 카드 그리드 (grid grid-cols-3 gap-4 p-6):
  · 카드 Skeleton 6개 (h-40 bg-gray-100 animate-pulse rounded-lg)

상태: isLoading=true, DataTable loading=true

데이터:
- SELECT * FROM class_templates WHERE branch_id=? ORDER BY created_at DESC
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C004 진입 시
- 템플릿 등록/수정/삭제 후 목록 갱신

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
