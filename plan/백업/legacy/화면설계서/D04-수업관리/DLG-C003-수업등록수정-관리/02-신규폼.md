# DLG-C003 수업등록/수정(관리) — 상태: 신규 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C003 |
| 상태 코드 | `form-create` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, fc |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` (등록 버튼 클릭) |
| 다음 상태 | 저장 → `05-저장성공` / 닫기 → `04-미저장확인` / 유효성실패 → `06-유효성에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 신규 폼을 작성하라.
화면: DLG-C003 수업등록/수정(관리) — 상태: 신규 폼

파일: src/components/classes/LessonManageModal.tsx (mode='create')

모달 구조 (isOpen=true, mode='create'):
- 오버레이: fixed inset-0 bg-black/50 z-50
- 모달 컨테이너: bg-white rounded-xl shadow-xl w-[480px] mx-auto mt-20

모달 헤더:
- 제목: "수업 등록" (text-lg font-semibold)
- X 버튼: onChange 확인 후 닫기

Zod 스키마:
const lessonSchema = z.object({
  title: z.string().min(1, "수업명을 입력해주세요.").max(50),
  instructorId: z.string().min(1, "강사를 선택해주세요."),
  type: z.enum(['PT','GX','pilates','yoga','etc'], { required_error: "수업 분류를 선택해주세요." }),
  startTime: z.string().min(1, "시작 시간을 입력해주세요."),
  endTime: z.string().min(1, "종료 시간을 입력해주세요."),
  capacity: z.number().min(1).max(100, "정원은 1~100명이어야 합니다."),
  memo: z.string().optional(),
})

폼 필드 (space-y-4 p-6):
1. 수업명 * (Input, maxLength=50)
2. 수업 분류 * (Select: PT|GX|필라테스|요가|기타)
3. 담당 강사 * (Select: role=instructor)
4. 시작 시간 * (Input type="time")
5. 종료 시간 * (Input type="time", 시작 이후 검증)
6. 정원 (Input number, 1~100)
7. 메모 (Textarea, rows=3)

모달 푸터:
- "취소" Button outline
- "저장" Button primary
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- mode='create'로 모달 열림 시

### 연결 화면
- 저장: `05-저장성공`
- 닫기(변경있음): `04-미저장확인`
- 닫기(변경없음): `01-닫힘`
- 유효성 실패: `06-유효성에러`
