# SCR-C007 횟수관리 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C007 |
| 상태 코드 | `normal` |
| 경로 | `/lesson-counts` |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | Eye → DLG-C011 / 조정 → DLG-C012 / 이력 → DLG-C013 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C007 횟수관리 — 상태: 정상

파일: src/app/(classes)/lesson-counts/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "횟수 관리")

SearchFilter (bg-white border-b px-6 py-3):
- 회원명/수업명 검색 input
- 잔여횟수 필터 Select: 전체 | 0회 | 1~5회 | 6~10회 | 11회 이상
- 수업 분류 Select

DataTable:
컬럼: 회원명 | 수업명 | 분류 | 총횟수 | 사용횟수 | 잔여횟수 | 만료일 | 액션
- 잔여횟수 강조:
  · 0회: text-red-600 font-semibold + badge "소진"
  · 1~3회: text-amber-600 + badge "부족"
  · 4회 이상: text-gray-700
- 만료일 강조:
  · 7일 이내: text-red-500 "D-N"
  · 지났음: text-gray-400 strikethrough "만료"
- 액션 버튼:
  · Eye → DLG-C011 세션상세
  · Settings → DLG-C012 횟수조정 (manager/owner만)
  · History → DLG-C013 차감이력

페이지네이션: 20행

데이터:
- lesson_counts JOIN users JOIN classes
- remaining_count = total_count - used_count

사용 유틸:
- DataTable, SearchFilter, StatusBadge from '@/components/ui'
- Eye, Settings, History from 'lucide-react'
- differenceInDays, format from 'date-fns'
- hasPermission from '@/lib/auth'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 후 `lesson_counts.length > 0`

### 필수 데이터
| 블록 | 테이블 |
|------|--------|
| 횟수 목록 | `lesson_counts` |
| 회원 정보 | `users` |
| 수업 정보 | `classes` |

### 인터랙션 (User Actions)
1. Eye → DLG-C011 세션상세 (횟수 차감 포함)
2. Settings → DLG-C012 횟수 수동 조정 (manager/owner)
3. History → DLG-C013 차감이력 조회
4. 검색/필터 → 실시간 갱신
5. 컬럼 정렬 → 잔여횟수/만료일 기준

### 비즈니스 룰
- staff 역할: 횟수조정(Settings) 버튼 숨김
- 만료된 횟수권: 목록 표시 유지, 행 opacity-50
- 잔여횟수 0이면서 만료: "소진" 배지

### 에지 케이스
- 검색 결과 없음: `03-빈상태`
- 잔여 0 + 만료: 이중 경고 스타일

### 연결 화면
- Eye: DLG-C011
- Settings: DLG-C012
- History: DLG-C013
- 결과 없음: `03-빈상태`
