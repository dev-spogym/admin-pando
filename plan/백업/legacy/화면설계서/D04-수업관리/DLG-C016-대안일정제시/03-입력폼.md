# DLG-C016 대안일정제시 — 상태: 입력 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C016 |
| 상태 코드 | `form-input` |
| 부모 화면 | SCR-C009 일정요청처리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 200) |
| 다음 상태 | 전송 → `04-전송결과` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/AlternativeScheduleModal.tsx

모달 헤더: "대안 일정 제시"

요청 정보 표시 (읽기전용, rounded-lg bg-gray-50 p-4 mb-4):
<div className="space-y-1 text-sm">
  <p><span className="text-gray-500">회원:</span> {request.member.name}</p>
  <p><span className="text-gray-500">요청 일정:</span> {format(request.requestedDate, 'yyyy.MM.dd')} {request.requestedTime}</p>
  <p><span className="text-gray-500">요청 사유:</span> {request.reason}</p>
</div>

Zod 스키마:
const altSchema = z.object({
  date: z.date({ required_error: "날짜를 입력해주세요." })
    .refine(d => d >= startOfDay(new Date()), "과거 날짜는 선택할 수 없습니다."),
  time: z.string().min(1, "시간을 입력해주세요."),
  instructorId: z.string().min(1, "강사를 선택해주세요."),
  memo: z.string().optional(),
})

대안 일정 입력 필드 (space-y-4):
1. 날짜 * (DatePicker, 과거 비활성)
2. 시간 * (Select, 30분 단위)
3. 강사 * (Select, role=instructor)
4. 메모 (Textarea, rows=2)

모달 푸터:
- "취소" Button outline → onClose()
- "제안 전송" Button primary
```

## 📝 디스크립션

### 유효성 검사
| 필드 | 규칙 |
|------|------|
| 날짜 | 필수, 과거 불가 |
| 시간 | 필수 |
| 강사 | 필수 선택 |

### 연결 화면
- 제안 전송: `04-전송결과`
- 닫기: `01-닫힘`
