# SCR-C003 시간표일괄등록 — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C003 |
| 상태 코드 | `done` |
| 경로 | `/class-schedule` |
| 역할 | manager |
| 우선순위 | P1 |
| 이전 상태 | `04-생성중` |
| 다음 상태 | 폼 초기화 → `01-초기상태` / 캘린더 이동 → SCR-C001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C003 시간표일괄등록 — 상태: 완료

파일: src/app/(classes)/class-schedule/page.tsx (isDone=true 조건)

완료 상태 UI (우측 영역):
<div className="flex flex-col items-center justify-center h-full gap-4">
  <CheckCircle2 className="w-16 h-16 text-green-500" />
  <div className="text-center">
    <p className="text-lg font-semibold text-gray-800">
      {createdCount}개 수업 일정이 생성되었습니다.
    </p>
    {failedCount > 0 && (
      <p className="mt-1 text-sm text-amber-600">
        {failedCount}건은 충돌로 인해 생성되지 않았습니다.
      </p>
    )}
  </div>
  <div className="flex gap-3 mt-2">
    <Button variant="outline" onClick={handleReset}>
      다시 등록
    </Button>
    <Button onClick={() => moveToPage('/calendar')}>
      캘린더에서 확인
    </Button>
  </div>
</div>

완료 시 사이드이펙트:
- toast.success(`${createdCount}개 수업 일정이 생성되었습니다.`)
- 좌측 폼: 초기화 (handleReset)
- isGenerating: false, isDone: true

사용 유틸:
- CheckCircle2 from 'lucide-react'
- toast from 'sonner', moveToPage from '@/lib/utils'
- Button from '@/components/ui'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- API 200 응답 수신 후

### 인터랙션 (User Actions)
1. "다시 등록" → 폼 초기화 → `01-초기상태`
2. "캘린더에서 확인" → SCR-C001 이동

### 비즈니스 룰
- 부분 성공(failedCount > 0): amber 경고 텍스트 표시
- 전체 실패: `06-에러` 상태 처리

### 연결 화면
- 다시 등록: `01-초기상태`
- 캘린더 확인: SCR-C001
