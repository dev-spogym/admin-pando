# DLG-C013 차감이력 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C013 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 성공 → `03-이력표시` / 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionHistoryModal.tsx

모달 크기: w-[520px]
모달 헤더: "{memberName} 차감 이력"

스켈레톤 (animate-pulse, p-6 space-y-3):
- 필터바: h-9 bg-gray-200 rounded
- 행 5개: h-10 bg-gray-200 rounded

데이터 페칭:
const { data, isLoading, error } = useQuery({
  queryKey: ['session-history', sessionId, dateRange],
  queryFn: () => fetch(
    `/api/sessions/${sessionId}/history?start=${dateRange.start}&end=${dateRange.end}`
  ).then(r => r.json()),
  enabled: !!sessionId && isOpen,
})
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-이력표시` 또는 `04-빈상태`
- 로드 실패: `05-에러`
