# SCR-065 급여명세서 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-065 |
| 상태 코드 | `empty` |
| 경로 | `/payroll/statements` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (해당 월 명세서 없음) |
| 다음 상태 | 월 변경 또는 SCR-064로 이동 |

## 🧩 바이브코딩 프롬프트

```
조건: statements.length === 0 && !isLoadingData && !error

빈 상태 UI:
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <DocumentTextIcon className="w-16 h-16 text-gray-300" />
    <p className="mt-4 text-lg font-medium text-gray-500">
      {selectedMonth} 급여 명세서가 없습니다.
    </p>
    <p className="mt-1 text-sm text-gray-400">
      급여 관리에서 급여를 확정하면 명세서가 자동 생성됩니다.
    </p>
    <Button className="mt-6" onClick={() => moveToPage('/payroll')}>
      급여 관리로 이동
    </Button>
  </div>

"전체 이메일 발송" / "PDF 일괄 생성" 버튼: disabled
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `statements.length === 0` — 해당 월 급여 미확정 또는 명세서 미생성

### 인터랙션 (User Actions)
1. "급여 관리로 이동" → `/payroll`
2. MonthPicker → 다른 월 (명세서 있는 월)

### 비즈니스 룰
- 명세서는 급여 확정(DLG-064-002 성공) 시 자동 생성
- 검색어 있을 때 빈 결과: "검색 결과가 없습니다" 별도 메시지

### 연결 화면
- 이전: `01-로딩`
- 다음: `/payroll` / 월 변경 → `01-로딩`
