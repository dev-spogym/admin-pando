# SCR-063 직원근태관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-063 |
| 상태 코드 | `error` |
| 경로 | `/staff/attendance` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (API 실패) |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
에러 UI:
  <div className="rounded-lg border border-red-200 bg-red-50 p-8 text-center">
    <p className="text-sm font-medium text-red-700">근태 데이터를 불러오지 못했습니다.</p>
    <p className="text-xs text-red-500 mt-1">{error.message}</p>
    <Button variant="outline" className="mt-4" onClick={handleRetry}>
      다시 시도
    </Button>
  </div>

toast.error('근태 데이터를 불러오지 못했습니다.') — fetch 실패 시 1회

handleRetry:
  setError(null)
  setIsLoadingData(true)
  fetchAttendanceData()
```

## 📝 디스크립션

### 에러 유형
| 유형 | 처리 |
|------|------|
| fetch 실패 (4xx/5xx) | 에러 배너 + 재시도 버튼 |
| 출근/퇴근 처리 실패 | toast.error + 낙관적 업데이트 롤백 |

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
