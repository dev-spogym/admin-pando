# SCR-064 급여관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-064 |
| 상태 코드 | `error` |
| 경로 | `/payroll` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (API 실패) |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
에러 UI:
  toast.error('급여 데이터를 불러오지 못했습니다.')

  <div className="rounded-lg border border-red-200 bg-red-50 p-8 text-center">
    <p className="text-sm font-medium text-red-700">급여 데이터를 불러오는 중 오류가 발생했습니다.</p>
    <Button variant="outline" className="mt-4" onClick={handleRetry}>
      다시 시도
    </Button>
  </div>

handleRetry: setError(null); setIsLoadingData(true); fetchPayroll()
```

## 📝 디스크립션

### 에러 유형
| 유형 | 처리 |
|------|------|
| 급여 데이터 fetch 실패 | 에러 배너 + 재시도 |
| 권한 없음(403) | toast.error('접근 권한이 없습니다') + 대시보드 이동 |

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
