# SCR-S008 미수금관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S008 |
| 상태 코드 | `loading` |
| 경로 | `/unpaid` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `03-빈상태` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S008_미수금관리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S008 미수금관리 — 상태: 로딩

파일: src/app/unpaid/page.tsx (isLoading=true 분기)

레이아웃:
- AppLayout 풀너비 데스크톱
- PageHeader: "미수금 관리"

스켈레톤:
1. StatCardGrid 4개: animate-pulse bg-gray-200 h-24 rounded-xl
2. TabNav 자리 (전체/미결제/부분납부/연체): animate-pulse h-10 w-full rounded
3. DataTable 5행: animate-pulse bg-gray-100 h-12 rounded mb-1

데이터 로드:
- supabase.from('sales').select('*').eq('branch_id', branchId).gt('unpaid', 0)
  (unpaid > 0 조건으로 미수금 건 조회)

aria-busy="true" aria-label="미수금 데이터 로딩 중"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-S008 진입 시 미수금 목록 로드 중

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
