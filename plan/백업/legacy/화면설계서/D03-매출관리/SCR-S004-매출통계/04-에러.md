# SCR-S004 매출통계 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S004 |
| 상태 코드 | `error` |
| 경로 | `/sales/stats` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S004_매출통계/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S004 매출통계 — 상태: API 에러

파일: src/app/sales/stats/page.tsx (error !== null 분기)

에러 UI (차트 영역):
<div className="flex flex-col items-center justify-center h-64 bg-red-50 rounded-xl border border-red-200">
  <AlertCircle className="h-12 w-12 text-red-400 mb-3" />
  <p className="text-base font-semibold text-gray-700">통계를 불러오지 못했습니다.</p>
  <Button variant="outline" className="mt-4" onClick={fetchStats}>
    <RefreshCw className="h-4 w-4 mr-2" /> 다시 시도
  </Button>
</div>

toast.error('통계 데이터를 불러오지 못했습니다.') — 1회
console.error('[SCR-S004]', error)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase 쿼리 실패 (500/503)
- 집계 쿼리 timeout

### 인터랙션 (User Actions)
1. [다시 시도] → fetchStats() 재호출

### 연결 화면
- 재시도 → `01-로딩` → `02-정상`
