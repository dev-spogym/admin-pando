# SCR-S004 매출통계 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S004 |
| 상태 코드 | `loading` |
| 경로 | `/sales/stats` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `03-빈상태` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S004_매출통계/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S004 매출통계 — 상태: 로딩 (차트/통계 로딩 중)

파일: src/app/sales/stats/page.tsx (isLoading=true 분기)

레이아웃:
- AppLayout 풀너비 데스크톱
- PageHeader: "매출 통계"

스켈레톤:
1. StatCardGrid 4개: animate-pulse bg-gray-200 h-24 rounded-xl
2. 차트 영역 (h-64): animate-pulse bg-gray-200 rounded-xl
3. 기간 선택 바: animate-pulse bg-gray-200 h-10 w-full rounded

상태:
- isLoading=true
- aria-busy="true" aria-label="매출 통계 로딩 중"

데이터 로드:
- supabase.from('sales').select() 집계 쿼리
- 월별/일별 그룹핑 (date_trunc)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-S004 진입 시 통계 집계 쿼리 실행 중

### 인터랙션 (User Actions)
- 로딩 중 인터랙션 비활성

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
