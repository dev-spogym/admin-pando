# SCR-S012 결제취소/부분환불 — 상태: 반려

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S012 |
| 상태 코드 | `rejected` |
| 경로 | `/sales/cancel` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `03-승인대기` (관리자 거절) |
| 다음 상태 | 재요청 → DLG-S015 |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S012_결제취소_부분환불/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S012 결제취소/부분환불 — 상태: 반려 (E_ST_REJECTED_01)

파일: src/app/sales/cancel/page.tsx (refundStatus === 'rejected' 분기)

결제 정보 카드:
- StatusBadge: variant="error" label="반려"
- 빨간 테두리: border-red-300 bg-red-50

반려 사유 표시:
<div className="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
  <div className="flex items-start gap-3">
    <XCircle className="h-5 w-5 text-red-500 flex-shrink-0 mt-0.5" />
    <div>
      <p className="text-sm font-semibold text-red-700">환불 요청이 반려되었습니다.</p>
      <p className="text-xs text-red-600 mt-1">반려 사유: {refund.rejection_reason}</p>
      <p className="text-xs text-gray-400 mt-1">반려일: {refund.rejected_at}</p>
    </div>
  </div>
</div>

하단 버튼:
<div className="flex gap-3 justify-end">
  <Button
    variant="outline"
    onClick={() => setShowPartialRefundModal(true)}
  >
    재요청
  </Button>
</div>

반려 사유 툴팁 (테이블 내):
- 반려 배지 hover → 반려사유 Tooltip 표시

사용 유틸:
- XCircle from 'lucide-react'
- StatusBadge variant="error"
- formatKRW
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 관리자가 환불 요청을 거절한 상태 (E_ST_REJECTED_01)
- 빨간 배지 + 반려사유 표시

### 필수 데이터
| 항목 | 설명 |
|------|------|
| refund | status='거절', rejection_reason 포함 |

### 인터랙션 (User Actions)
1. [재요청] → DLG-S015 환불요청 (새 요청)

### 비즈니스 룰
- 반려 후 재요청 가능 (횟수 제한 없음)
- 반려사유 필수 표시

### 연결 화면
- DLG-S015 환불요청 (재요청)
