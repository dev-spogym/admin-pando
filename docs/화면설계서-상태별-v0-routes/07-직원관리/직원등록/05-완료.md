# SCR-061 직원 등록 — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-061 |
| 상태 코드 | `complete` |
| 경로 | `/staff` (리디렉션) |
| 역할 | 센터장 / 최고관리자 / 슈퍼관리자 |
| 우선순위 | P0 |
| 이전 상태 | `04-저장중` |
| 다음 상태 | SCR-060 직원 목록 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-061 직원 등록 — 상태: 완료 (저장 성공 후 목록 이동)

파일: src/app/staff/new/page.tsx

레이아웃:
- 저장 성공 직후 moveToPage(974) 호출로 SCR-060으로 이동
- /staff 페이지에서 toast.success가 표시되는 상태

컴포넌트 구조:
1. insert 성공 시:
   toast.success("직원이 성공적으로 등록되었습니다.");
   moveToPage(974); // /staff 라우트

2. /staff 페이지 진입 시:
   - fetchStaff() 재호출 → 새 직원 목록 반영
   - toast 메시지 화면 상단에 표시 (sonner 자동 처리)

3. 등록 완료 후 목록에서 신규 직원 하이라이트 (선택적):
   - URL params: ?highlight={newStaffId}
   - 해당 행 bg-primary/10 border-l-4 border-primary 1초 후 페이드아웃

데이터:
- 신규 등록된 staff.id (insert 반환값)
- moveToPage(974) → /staff

인터랙션:
- 자동 리디렉션 (사용자 액션 없음)
- /staff 진입 후 신규 직원 행 하이라이트 (선택적)
- toast 3초 후 자동 사라짐 (sonner 기본)

사용 유틸:
- toast from 'sonner'
- moveToPage from '@/internal'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `supabase.from("staff").insert()` 성공 응답
- `toast.success` + `moveToPage(974)` 실행

### 필수 데이터
- insert 반환된 staff.id (신규 직원 ID)

### 인터랙션 (User Actions)
1. 자동 리디렉션 → SCR-060 직원 목록
2. toast.success 3초 표시 후 자동 소멸
3. SCR-060에서 신규 직원 확인 가능

### 비즈니스 룰
- 등록 완료 후 반드시 SCR-060으로 이동
- 토스트 메시지: "직원이 성공적으로 등록되었습니다."
- SCR-060 진입 시 fetchStaff() 자동 재실행

### 에지 케이스
- moveToPage 실패 시 → 현재 페이지 유지 + toast만 표시
- 브라우저 뒤로가기 → /staff/new 폼 재진입 (defaultValues로 초기화)

### 접근성 (A11y)
- toast `role="status"` (sonner 기본 제공)
- 리디렉션 후 SCR-060 `aria-live` 영역 업데이트

### 연결 화면
- 이전: 04-저장중
- 다음: SCR-060 직원 목록 (02-기본-재직)
