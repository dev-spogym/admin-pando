# SCR-063 직원 근태 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-063 |
| 상태 코드 | `loading` |
| 경로 | `/staff/attendance` |
| 역할 | 센터장 / 최고관리자 / 슈퍼관리자 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 진입 |
| 다음 상태 | `02-기본-당일` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-063 직원 근태 관리 — 상태: 로딩

파일: src/app/staff/attendance/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="직원 근태 관리" subtitle="직원별 출퇴근 기록 및 근태 현황을 관리합니다." />
- <StatCardGrid cols={4}>: 4개 StatCard 스켈레톤 (animate-pulse)
- TabNav 비활성: [📅 일별 뷰] [📋 월별 요약]
- <DataTable> emptyMessage="데이터를 불러오는 중..." isLoading={true}

컴포넌트 구조:
1. isLoading === true 조건
2. StatCardGrid: 총근무일/정상출근/지각/결근 카드 스켈레톤
3. 날짜 선택 input disabled
4. DataTable 로딩 상태

데이터:
- isLoading: boolean = true
- getStaffAttendance(undefined, selectedDate) 실행 중
- supabase staff 목록 + staff_attendance 테이블

인터랙션:
- fetch 완료 → 02-기본-당일

사용 유틸:
- supabase from '@/lib/supabase'
- toast from 'sonner'
- getStaffAttendance from '@/api/endpoints/staffAttendance'
- lucide-react: LogIn, LogOut, Calendar, Users, CheckCircle, AlertCircle
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/staff/attendance` 최초 진입
- `isLoading === true` (getStaffAttendance 실행 중)

### 필수 데이터
- `staff_attendance` 테이블 + `staff` 테이블 병렬 fetch

### 인터랙션 (User Actions)
1. 자동 전환 → fetch 완료 시 02-기본-당일

### 비즈니스 룰
- 센터장/최고관리자/슈퍼관리자만 접근
- 기본 날짜: 오늘 (`fmtLocal(new Date())`)
- `getBranchId()` 기반 지점 필터

### 에지 케이스
- fetch 실패 → `toast.error("근태 데이터를 불러오지 못했습니다.")`

### 접근성 (A11y)
- `aria-busy="true"` 로딩 영역

### 연결 화면
- 이전: 사이드바
- 다음: 02-기본-당일
