# SCR-064 급여 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-064 |
| 상태 코드 | `loading` |
| 경로 | `/payroll` |
| 역할 | 센터장 / 최고관리자 / 슈퍼관리자 (hasPermission 필수) |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 진입 |
| 다음 상태 | `02-기본-월별` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-064 급여 관리 — 상태: 로딩

파일: src/app/payroll/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="급여 관리" subtitle="직원별 월 급여를 관리하고 확정합니다."
    actions={<><Button disabled>급여 확정</Button><Button disabled>엑셀 다운로드</Button></>} />
- 월 선택 Select disabled
- <StatCardGrid cols={4}>: 4개 StatCard 스켈레톤 (animate-pulse)
- <DataTable> emptyMessage="데이터를 불러오는 중..." isLoading={true}

컴포넌트 구조:
1. 권한 체크: hasPermission(user, "payroll:read")
   권한 없으면 → <Forbidden/> 또는 moveToPage(403)
2. isLoadingData === true → 스켈레톤 UI
3. useEffect: fetchPayroll(selectedMonth) 실행

데이터:
- isLoadingData: boolean = true
- selectedMonth: string (현재 월 YYYY-MM)
- supabase.from("staff").select + 급여 계산 로직

인터랙션:
- fetch 완료 → 02-기본-월별

사용 유틸:
- useAuthStore from '@/stores/authStore'
- hasPermission from '@/lib/permissions'
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: Calendar, FileText, Download, Users, CheckCircle2, Clock
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/payroll` 최초 진입
- `isLoadingData === true`

### 필수 데이터
- `hasPermission(user, "payroll:read")` 권한 체크 (센터장/최고관리자/슈퍼관리자)
- `staff` 테이블 + 급여 데이터 fetch

### 인터랙션 (User Actions)
1. 자동 전환 → fetch 완료 시 02-기본-월별
2. 권한 없으면 → 403 페이지

### 비즈니스 룰
- 급여 관리는 센터장/최고관리자/슈퍼관리자만 접근
- `hasPermission` 체크 실패 시 강제 이동
- `getBranchId()`로 지점 필터

### 에지 케이스
- 권한 없는 접근 → 즉시 리디렉션

### 접근성 (A11y)
- `aria-busy="true"` 로딩 영역

### 연결 화면
- 이전: 사이드바
- 다음: 02-기본-월별
