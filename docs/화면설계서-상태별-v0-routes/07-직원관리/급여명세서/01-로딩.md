# SCR-065 급여 명세서 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-065 |
| 상태 코드 | `loading` |
| 경로 | `/payroll/statements` |
| 역할 | 센터장 / 최고관리자 / 슈퍼관리자 (hasPermission 필수) |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 진입 |
| 다음 상태 | `02-기본-개인` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-065 급여 명세서 — 상태: 로딩

파일: src/app/payroll/statements/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="급여 명세서" subtitle="확정된 급여 명세서를 조회하고 PDF로 다운로드합니다."
    actions={<><Button disabled><Download size={16}/>PDF 다운로드</Button><Button disabled><Mail size={16}/>이메일 발송</Button></>} />
- 월 선택 Select disabled
- DataTable 스켈레톤 (animate-pulse rows × 5)

컴포넌트 구조:
1. 권한 체크: hasPermission(user, "payroll:read")
   권한 없으면 → moveToPage(403)
2. isLoadingData === true → 스켈레톤 UI
3. useEffect: fetchStatements(selectedMonth) 실행
   supabase.from("staff").select(...)
   + 급여 명세서 조회 (payroll_statements 가상)

데이터:
- isLoadingData: boolean = true
- selectedMonth: 현재 월 (YYYY-MM)
- useAuthStore: user (권한 체크)

인터랙션:
- fetch 완료 → 02-기본-개인

사용 유틸:
- useAuthStore from '@/stores/authStore'
- hasPermission, normalizeRole from '@/lib/permissions'
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: FileText, Download, Mail, Printer
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/payroll/statements` 최초 진입
- `isLoadingData === true`

### 필수 데이터
- `hasPermission(user, "payroll:read")` 권한 체크
- `staff` + `payroll_statements` (가상) 테이블 fetch

### 인터랙션 (User Actions)
1. 자동 전환 → fetch 완료 시 02-기본-개인

### 비즈니스 룰
- 급여 명세서는 센터장/최고관리자/슈퍼관리자만 접근
- `normalizeRole()` 사용하여 역할 정규화
- 권한 없으면 403 페이지로 이동

### 에지 케이스
- 권한 없는 접근 → 즉시 리디렉션
- fetch 실패 → `toast.error("명세서 데이터를 불러오지 못했습니다.")`

### 접근성 (A11y)
- `aria-busy="true"` 로딩 영역

### 연결 화면
- 이전: 사이드바
- 다음: 02-기본-개인
