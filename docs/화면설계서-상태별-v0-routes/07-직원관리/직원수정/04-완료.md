# SCR-061 직원 수정 — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-061 |
| 상태 코드 | `edit-complete` |
| 경로 | `/staff` (리디렉션) |
| 역할 | 센터장 / 최고관리자 / 슈퍼관리자 |
| 우선순위 | P0 |
| 이전 상태 | `03-저장중` |
| 다음 상태 | SCR-060 직원 목록 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-061 직원 수정 — 상태: 완료 (update 성공 후 목록 이동)

파일: src/app/staff/edit/page.tsx

컴포넌트 구조:
1. update 성공 시:
   toast.success("직원 정보가 수정되었습니다.");
   moveToPage(974); // /staff 라우트

2. /staff 페이지 진입 시:
   - fetchStaff() 재호출 → 수정된 직원 정보 반영
   - toast 메시지 sonner로 상단 표시

3. 수정 완료 후 해당 직원 행 하이라이트 (선택적):
   - URL: /staff?highlight={staffId}
   - 1~2초 후 자동 페이드아웃

데이터:
- moveToPage(974) 실행
- SCR-060 fetchStaff() 자동 재실행

인터랙션:
- 자동 리디렉션 (사용자 액션 없음)
- toast 3초 후 자동 소멸

사용 유틸:
- toast from 'sonner'
- moveToPage from '@/internal'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `supabase.from("staff").update()` 성공 응답
- `toast.success` + `moveToPage(974)` 실행

### 필수 데이터
- 없음 (리디렉션 후 SCR-060에서 처리)

### 인터랙션 (User Actions)
1. 자동 리디렉션 → SCR-060 직원 목록
2. toast.success 3초 표시

### 비즈니스 룰
- 토스트: "직원 정보가 수정되었습니다."
- SCR-060 진입 시 fetchStaff() 자동 재실행

### 에지 케이스
- 뒤로가기 → /staff/edit?id={staffId} 재진입 (01-로딩 상태)

### 접근성 (A11y)
- toast `role="status"` (sonner 기본)

### 연결 화면
- 이전: 03-저장중
- 다음: SCR-060 직원 목록 (02-기본-재직)
