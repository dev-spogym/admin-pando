# SCR-D001 다이어그램 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-D001 |
| 상태 코드 | `loading` |
| 경로 | `/diagrams` |
| 역할 | 센터장 / 본사 관리자 |
| 우선순위 | P1 |
| 이전 상태 | 사이드바 "통합운영 > 다이어그램" 클릭 |
| 다음 상태 | `02-기본` (로드 완료) / `05-에러` (실패) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 컴포넌트를 작성하라.
화면: SCR-D001 다이어그램 — 상태: Mermaid 다이어그램 목록 로딩 중

파일: src/app/diagrams/page.tsx (DiagramBrowser 컴포넌트)

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="통합운영 다이어그램" subtitle="시스템 아키텍처와 업무 흐름 다이어그램을 조회합니다." />
- 좌우 2패널 스켈레톤

스켈레톤 구조:
- 좌측 사이드바 (w-64):
  - 카테고리 제목 skeleton (w-20 h-4 bg-gray-200 animate-pulse)
  - 다이어그램 항목 skeleton 8개 (h-9 bg-gray-100 animate-pulse rounded, space-y-1)
- 우측 메인 (flex-1):
  - 다이어그램 제목 skeleton (w-48 h-6 bg-gray-200 animate-pulse)
  - 다이어그램 뷰어 영역 skeleton (h-[600px] bg-gray-100 animate-pulse rounded-xl)
  - 액션 버튼 skeleton 3개 (w-24 h-8 bg-gray-200 animate-pulse)

데이터 로드 (DiagramBrowser 내부):
- 다이어그램 정의는 파일 시스템 또는 하드코딩 배열로 관리
- Mermaid.js 초기화: import('mermaid').then(m => m.default.initialize({...}))
- 다이어그램 카테고리 목록 로드

사용 유틸:
- lucide-react: GitBranch, Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/diagrams` 진입 직후
- Mermaid.js 동적 임포트 + 다이어그램 목록 로드 중

### 필수 데이터
| 블록 | 소스 | 조건 |
|------|------|------|
| 다이어그램 목록 | 하드코딩 배열 | 카테고리 + 제목 + mermaid 코드 |

### 인터랙션 (User Actions)
1. 로딩 중 — 모든 인터랙션 차단
2. 로드 완료 → `02-기본`
3. Mermaid 로드 실패 → `05-에러`

### 비즈니스 룰
- Mermaid.js는 dynamic import (SSR 비활성)
- 초기 선택 다이어그램: 첫 번째 항목 자동 선택

### 에지 케이스
- Mermaid CDN 로드 실패 → `05-에러`

### 접근성 (A11y)
- `aria-busy="true"`, 스켈레톤 `aria-hidden="true"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본` / `05-에러`
