# SCR-083 IOT 설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-083 |
| 상태 코드 | `loading` |
| 경로 | `/settings/iot` |
| 역할 | 센터장 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 또는 설정 홈에서 IOT 설정 진입 |
| 다음 상태 | `02-기본-기기목록` (로드 완료) / `06-빈상태` (기기 없음) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-083 IOT 설정 — 상태: 기기 목록 로딩 중

파일: src/app/settings/iot/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="IoT 연동 관리" subtitle="출입문, 키오스크, 락커 컨트롤러 등 IoT 장비를 관리합니다." />
- TabNav 스켈레톤 (5개 탭 pill)
- 통계 카드 스켈레톤 3개
- DataTable 스켈레톤 8행

스켈레톤 구조:
- 탭 pill: w-24 h-8 bg-gray-200 animate-pulse rounded-full (5개, flex gap-2)
- 통계 카드: h-20 bg-gray-100 animate-pulse rounded-xl (3개, grid grid-cols-3 gap-4)
- 테이블 헤더: h-10 bg-gray-200 animate-pulse rounded
- 테이블 행: h-12 bg-gray-50 animate-pulse rounded (8개, space-y-2)

데이터 로드:
useEffect(() => {
  const load = async () => {
    setIsLoading(true);
    // iot_devices는 가상 테이블 (실제 DB 없을 수 있음)
    // 로컬 상태 + localStorage 기반으로 관리
    const stored = localStorage.getItem(`iot_devices_${branchId}`);
    const devices = stored ? JSON.parse(stored) : [];
    setDevices(devices);
    setIsLoading(false);
  };
  load();
}, [branchId]);

사용 유틸:
- useAuthStore, getBranchId
- lucide-react: Wifi, Server, Activity
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/settings/iot` 진입 직후
- localStorage에서 기기 목록 로드 중

### 필수 데이터
| 블록 | 소스 | 조건 |
|------|------|------|
| IOT 기기 목록 | `localStorage: iot_devices_{branchId}` | 가상 데이터 |
| 설정 | `settings` | `branchId = ?` (출입 정책 로드) |

### 인터랙션 (User Actions)
1. 로딩 중 — 모든 인터랙션 차단
2. 로드 완료 → 기기 있으면 `02-기본-기기목록`, 없으면 `06-빈상태`

### 비즈니스 룰
- iot_devices는 가상 테이블 (Supabase 미구현)
- localStorage 키: `iot_devices_{branchId}`

### 에지 케이스
- localStorage 파싱 오류 → 빈 배열 처리

### 접근성 (A11y)
- `aria-busy="true"`, 스켈레톤 `aria-hidden="true"`

### 연결 화면
- 이전: 사이드바 / 설정 홈
- 다음: `02-기본-기기목록` / `06-빈상태`
