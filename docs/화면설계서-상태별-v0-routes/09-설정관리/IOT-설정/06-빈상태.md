# SCR-083 IOT 설정 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-083 |
| 상태 코드 | `empty` |
| 경로 | `/settings/iot` |
| 역할 | 센터장 |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (기기 0건) |
| 다음 상태 | `05-기기추가모달` → `02-기본-기기목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-083 IOT 설정 — 상태: 등록된 기기 없음

파일: src/app/settings/iot/page.tsx

빈 상태 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mb-6">
    <Server className="w-10 h-10 text-gray-300" />
  </div>
  <h3 className="text-lg font-semibold text-gray-700 mb-2">
    등록된 IoT 기기가 없습니다
  </h3>
  <p className="text-sm text-gray-500 mb-2 max-w-sm">
    출입문 게이트, 키오스크, 락커 컨트롤러, InBody 장비를 등록하여
    스마트 출입 관리를 시작하세요.
  </p>
  <p className="text-xs text-gray-400 mb-8">
    기기 연동 문의: support@fitgenie.kr
  </p>
  <Button variant="primary" icon={Plus} onClick={() => setShowAddModal(true)}>
    첫 기기 등록하기
  </Button>
</div>

PageHeader actions는 유지:
- [전체 동기화] disabled
- [기기 추가] 활성

출입 규칙 탭은 기기 없어도 설정 가능 (정책 설정 선행 허용)

사용 유틸:
- lucide-react: Server, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- localStorage에 `iot_devices_{branchId}` 키 없거나 빈 배열
- 전체 기기 삭제 후

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. "첫 기기 등록하기" → `05-기기추가모달`
2. 기기 추가 후 → `02-기본-기기목록`

### 비즈니스 룰
- 기기 없어도 출입 규칙 탭은 설정 가능
- 이메일 지원 링크 표시

### 에지 케이스
- 권한 없는 사용자(조회만) → 등록 버튼 미노출, 안내 문구만

### 접근성 (A11y)
- 빈 상태 `role="status"`, `aria-label="IoT 기기 없음"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `05-기기추가모달`
