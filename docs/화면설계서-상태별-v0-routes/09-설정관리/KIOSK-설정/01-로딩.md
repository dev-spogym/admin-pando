# SCR-082 KIOSK 설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `loading` |
| 경로 | `/settings/kiosk` |
| 역할 | 센터장 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 또는 설정 홈에서 KIOSK 설정 진입 |
| 다음 상태 | `02-기본` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 KIOSK 설정 — 상태: 설정 데이터 로딩 중

파일: src/app/settings/kiosk/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="키오스크 설정" subtitle="센터 입구 키오스크의 표시 항목과 기능을 설정합니다." />
- 좌우 2패널 스켈레톤 (좌: 설정 폼, 우: 미리보기)

스켈레톤 구조:
- 좌측 (w-[55%]):
  - TabNav pill 5개 skeleton (w-20 h-8 bg-gray-200 animate-pulse)
  - 섹션 제목 skeleton (w-32 h-4 bg-gray-200)
  - Toggle 행 skeleton 6개 (h-14 bg-gray-50 animate-pulse rounded-lg)
- 우측 (w-[45%]):
  - 키오스크 미리보기 프레임 skeleton (h-96 bg-gray-100 animate-pulse rounded-2xl)

데이터 로드:
useEffect(() => {
  const load = async () => {
    setIsLoading(true);
    const stored = localStorage.getItem(`kiosk_settings_${branchId}`);
    if (stored) {
      setKioskSettings(JSON.parse(stored));
    } else {
      // 기본값 설정
      setKioskSettings(defaultKioskSettings);
    }
    setIsLoading(false);
  };
  load();
}, [branchId]);

defaultKioskSettings:
const defaultKioskSettings = {
  logoUrl: null, bgColor: '#FFFFFF', welcomeMessage: '반갑습니다! 카드를 태그해주세요.',
  idleImageUrl: null, rfidEnabled: true, qrEnabled: false, phoneEnabled: false,
  faceEnabled: false, showPhoto: true, showRemaining: true, showExpiry: true,
  autoSupply: false, showTodayClass: false, beepEnabled: true, ttsEnabled: false,
  ttsVolume: 70, alertUnauthorized: true, blockExpired: false,
  blockMessage: '이용권이 만료되었습니다. 프론트에 문의하세요.',
  staffCheckinEnabled: true, afterCheckinMode: 'manual', autoAssignZones: [],
};

사용 유틸:
- useAuthStore, getBranchId
- lucide-react: Monitor, Settings
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/settings/kiosk` 진입 직후
- localStorage에서 kiosk_settings 로드 중

### 필수 데이터
| 블록 | 소스 | 조건 |
|------|------|------|
| 키오스크 설정 | `localStorage: kiosk_settings_{branchId}` | 가상 데이터 |

### 인터랙션 (User Actions)
1. 로딩 중 — 모든 인터랙션 차단
2. 로드 완료 → `02-기본`

### 비즈니스 룰
- kiosk_settings는 가상 테이블 (localStorage 기반)
- 최초 진입 시 defaultKioskSettings 적용

### 에지 케이스
- localStorage 파싱 오류 → defaultKioskSettings 사용

### 접근성 (A11y)
- `aria-busy="true"`, 스켈레톤 `aria-hidden="true"`

### 연결 화면
- 이전: 사이드바 / 설정 홈
- 다음: `02-기본`
