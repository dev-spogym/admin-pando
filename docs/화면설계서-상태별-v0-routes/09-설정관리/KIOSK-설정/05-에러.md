# SCR-082 KIOSK 설정 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `error` |
| 경로 | `/settings/kiosk` |
| 역할 | 센터장 |
| 우선순위 | P1 |
| 이전 상태 | 연결 테스트 실패 또는 저장 실패 |
| 다음 상태 | `02-기본` (재시도 또는 무시) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 KIOSK 설정 — 상태: 오류 발생 (연결 실패 또는 저장 실패)

파일: src/app/settings/kiosk/page.tsx

에러 시나리오 1 - 연결 테스트 실패:
const handleConnectionTest = async () => {
  setIsTesting(true);
  try {
    await simulatePing(); // 3초 timeout
    setConnectionStatus('connected');
    toast.success('키오스크 연결 확인 완료');
  } catch {
    setConnectionStatus('error');
    toast.error('키오스크 연결에 실패했습니다. 네트워크와 기기 상태를 확인하세요.', {
      action: { label: '재시도', onClick: handleConnectionTest }
    });
  } finally {
    setIsTesting(false);
  }
};

연결 상태 배지 (PageHeader 하단):
{connectionStatus === 'connected' && (
  <div className="flex items-center gap-2 px-4 py-2 bg-green-50 border-b border-green-200">
    <Wifi className="w-4 h-4 text-green-500" />
    <span className="text-sm text-green-700">키오스크 연결됨</span>
  </div>
)}
{connectionStatus === 'error' && (
  <div className="flex items-center gap-2 px-4 py-2 bg-red-50 border-b border-red-200">
    <WifiOff className="w-4 h-4 text-red-500" />
    <span className="text-sm text-red-700">키오스크 연결 실패 — 네트워크 상태를 확인하세요.</span>
    <Button variant="ghost" size="xs" onClick={handleConnectionTest}>재시도</Button>
  </div>
)}

에러 시나리오 2 - 저장 실패:
try {
  localStorage.setItem(`kiosk_settings_${branchId}`, JSON.stringify(kioskSettings));
  toast.success('키오스크 설정이 저장되었습니다.');
  setIsDirty(false);
} catch (e) {
  // localStorage 용량 초과 등
  toast.error('설정 저장에 실패했습니다. 브라우저 저장소 용량을 확인하세요.');
}

에러 시나리오 3 - 검증 오류:
if (!kioskSettings.rfidEnabled && !kioskSettings.qrEnabled && !kioskSettings.phoneEnabled) {
  toast.error('최소 하나의 체크인 방식을 활성화해야 합니다.');
  return;
}

사용 유틸:
- toast from 'sonner'
- lucide-react: WifiOff, Wifi, AlertCircle
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 연결 테스트 실패 시
- 저장 실패 시
- 검증 오류 시 (체크인 방식 전체 OFF)

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. 연결 오류 배너 "재시도" → 연결 테스트 재실행
2. 저장 오류 → toast 4초 자동 소멸
3. 검증 오류 → toast + 해당 섹션 강조

### 비즈니스 룰
- 연결 실패는 설정 저장과 무관 (독립 오류)
- 저장 실패 시 isDirty=true 유지 (재시도 가능)

### 에지 케이스
- localStorage 용량 초과 → 이미지 base64가 주 원인, 이미지 압축 안내

### 접근성 (A11y)
- 오류 배너 `role="alert"`, `aria-live="assertive"`

### 연결 화면
- 이전: `02-기본` (연결 테스트 또는 저장 시도)
- 다음: `02-기본` (재시도 또는 무시)
