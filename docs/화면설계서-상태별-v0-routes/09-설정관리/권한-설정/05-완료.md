# SCR-081 권한 설정 — 상태: 저장 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `save-complete` |
| 경로 | `/settings/permissions` |
| 역할 | 센터장 |
| 우선순위 | P0 |
| 이전 상태 | `04-저장중` (성공) |
| 다음 상태 | `02-역할별탭` (계속 편집) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한 설정 — 상태: 저장 완료 (saveSuccess=true)

파일: src/app/settings/permissions/page.tsx

완료 UI (saveSuccess=true, 3초간):
- PageHeader 하단 인라인 배너:
  <div className="flex items-center gap-2 px-4 py-2 bg-green-50 border-b border-green-200">
    <CheckCircle2 className="w-4 h-4 text-green-500" />
    <span className="text-sm text-green-700">
      '{selectedRole.name}' 권한이 저장되었습니다.
    </span>
  </div>

- 저장 버튼: disabled (isDirty=false)
- 변경된 셀 ring-amber 제거 (changedCells 초기화)
- 충돌 배너 제거 (conflicts 재계산 → 0건)

3초 후 자동 해제:
setTimeout(() => setSaveSuccess(false), 3000);

추가 액션:
- 다른 역할 선택 → 계속 편집 가능
- 탭 전환 또는 페이지 이탈 → 정상 가능 (isDirty=false)

사용 유틸:
- lucide-react: CheckCircle2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `04-저장중` localStorage 저장 성공 직후

### 필수 데이터
- 없음 (저장 완료 상태)

### 인터랙션 (User Actions)
1. 3초 후 배너 자동 소멸
2. 다른 역할 선택 → `02-역할별탭`
3. 계속 편집 → `03-권한토글변경`

### 비즈니스 룰
- isDirty=false → 저장 버튼 disabled
- 저장 완료 배너 3초 자동 해제
- toast.success와 인라인 배너 동시 표시

### 에지 케이스
- 저장 직후 다른 역할 선택 → 배너 즉시 사라짐

### 접근성 (A11y)
- 완료 배너 `role="status"`, `aria-live="polite"`

### 연결 화면
- 이전: `04-저장중`
- 다음: `02-역할별탭`
