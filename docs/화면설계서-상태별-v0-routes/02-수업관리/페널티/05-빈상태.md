# SCR-C008 페널티 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/penalties` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | 없음 (자동 발생 대기) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C008 페널티 — 상태: 빈상태

파일: src/app/(classes)/penalties/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="페널티 관리" />
- TabNav 공통 탭

빈상태 UI:
- 중앙 정렬 py-20 text-center
- ShieldCheck 아이콘 (w-16 h-16 text-green-300 mx-auto mb-4)
- "페널티가 없습니다." (text-lg font-semibold text-gray-500)
- "노쇼/지각 발생 시 자동으로 등록됩니다." (text-sm text-gray-400 mt-1)
- "모든 회원이 성실하게 수업에 참석하고 있습니다. 👍" (text-sm text-green-600 mt-3)

조건:
- penalties.length === 0 && !isLoading

사용 유틸:
- lucide-react: ShieldCheck
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- penalties 조회 결과 0건
- 모든 페널티 면제/만료 처리 후

### 인터랙션 (User Actions)
- 특별한 액션 없음 (페널티는 자동 발생)

### 비즈니스 룰
- 페널티는 수업 완료처리 시 자동 생성 (수동 추가 없음)
- 필터 결과 0건: 빈상태 UI 대신 "검색 결과가 없습니다." 인라인

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: 페널티 자동 발생 시 `02-기본`으로 전환
