# SCR-C008 페널티 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/penalties` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (TabNav "페널티 관리" 클릭) |
| 다음 상태 | `02-기본` / `05-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C008 페널티 — 상태: 로딩

파일: src/app/(classes)/penalties/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="페널티 관리" subtitle="노쇼/지각 페널티를 관리합니다." />
- TabNav 공통 탭 (페널티 관리 active)

스켈레톤:
1. [StatCardGrid 스켈레톤] grid grid-cols-3 gap-4 mb-6
   - h-20 bg-gray-100 animate-pulse rounded-xl × 3개
2. [SearchFilter 스켈레톤] h-10 w-80 bg-gray-200 animate-pulse rounded-lg mb-4
3. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 8: h-14 bg-gray-100 animate-pulse rounded mt-1

데이터:
- useEffect: supabase.from('penalties').select('*, members(name, phone), lessons(name), staff(name)').eq('branch_id', branchId).order('created_at', {ascending: false})

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- TabNav "페널티 관리" 탭 클릭
- 사이드바 > 수업관리 > 페널티 클릭
- 면제 처리 후 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 |
|--------|------|
| `penalties` | `select('*, members(*), lessons(*), staff(*)')` |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본` / `05-빈상태`
2. 로드 실패 → toast.error

### 비즈니스 룰
- 센터장/매니저만 접근

### 접근성 (A11y)
- `role="status"` + `aria-label="페널티 목록 로딩 중"`

### 연결 화면
- 이전: TabNav
- 다음: `02-기본` / `05-빈상태`
