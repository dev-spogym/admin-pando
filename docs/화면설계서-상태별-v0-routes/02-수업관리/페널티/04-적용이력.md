# SCR-C008 페널티 — 상태: 적용이력

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `penalty-history` |
| 경로 | `/app/(classes)/penalties` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `02-기본` ([이력] 아이콘 클릭) |
| 다음 상태 | 닫기 → `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C008 페널티 — 상태: 특정 회원의 페널티 적용 이력 모달

파일: src/app/(classes)/penalties/page.tsx

모달 크기: max-w-lg (512px), max-h-[80vh] overflow-y-auto

모달 구조:
1. [헤더]
   - "{memberName}님 페널티 이력" (font-bold text-lg) + X 버튼

2. [회원 요약] bg-gray-50 rounded-lg p-3 mb-4 grid grid-cols-3
   - 총 페널티: {totalCount}건
   - 누적 점수: {totalScore}점
   - 면제 횟수: {exemptCount}건

3. [점수 경고] (누적 점수 임계값 초과 시)
   - bg-red-50 border border-red-200 rounded-lg p-3 mb-4
   - AlertTriangle(text-red-500) + "누적 점수 {totalScore}점 — 예약 제한 대상입니다."

4. [이력 테이블]
   - 컬럼: 발생일 | 수업명 | 유형 | 점수 | 상태 | 면제사유
   - 유형 배지: 노쇼=red, 지각=amber, 조기퇴장=orange
   - 상태 배지: 적용중=red, 면제=green, 만료=gray
   - 면제사유: 없으면 "-"
   - 최신순 정렬

5. [하단] [닫기] variant=ghost

데이터:
- selectedMember: { id, name } (행에서 선택됨)
- supabase.from('penalties').select('*, lessons(name)').eq('member_id', selectedMember.id).eq('branch_id', branchId).order('created_at', {ascending:false})

사용 유틸:
- supabase from '@/lib/supabase'
- formatDate from '@/lib/format'
- lucide-react: X, AlertTriangle, History
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-기본` 테이블에서 [이력](History) 아이콘 클릭

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 회원 페널티 이력 | `penalties` | `member_id = selectedMember.id` |

### 인터랙션 (User Actions)
1. X / [닫기] / ESC → 모달 닫기

### 비즈니스 룰
- 이력은 READ ONLY
- 누적 점수 임계값: 센터 설정값 (기본 10점)
- 만료 페널티: 만료일 지난 것 자동 status='만료' (배치 처리)

### 에지 케이스
- 이력 0건: "페널티 이력이 없습니다." (정상 케이스)

### 접근성 (A11y)
- `role="dialog"` + `aria-labelledby`
- 누적 점수 경고 `role="alert"`

### 연결 화면
- 이전: `02-기본`
- 다음: 닫기 → `02-기본`
