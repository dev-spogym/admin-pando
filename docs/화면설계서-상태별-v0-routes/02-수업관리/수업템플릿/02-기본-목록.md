# SCR-C004 수업 템플릿 — 상태: 기본 목록

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `default-list` |
| 경로 | `/app/(classes)/class-templates` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-추가모달` / `04-편집모달` / `05-삭제확인` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 수업 템플릿 — 상태: 기본 목록

파일: src/app/(classes)/class-templates/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="수업 템플릿" subtitle="수업 템플릿을 관리합니다."
    actions={[<Button variant="primary" onClick={openAddModal}>+ 템플릿 추가</Button>]} />

컴포넌트 구조:
1. [SearchFilter] mb-4
   - 검색 input: placeholder="템플릿명 검색" → 클라이언트 필터링
   - <Select> 수업 유형 필터 (전체/그룹수업/PT/개인레슨)

2. [DataTable] columns:
   - 템플릿명 (font-medium, 클릭 시 편집모달)
   - 수업 유형 (<StatusBadge>: 그룹수업/PT/개인레슨)
   - 정원 (N명)
   - 수업 시간 (N분)
   - 담당 강사 (없음이면 "-")
   - 색상 (색상 원형 dot w-5 h-5 rounded-full)
   - 활성 여부 (<StatusBadge active/inactive>)
   - 액션 (편집 아이콘 | 삭제 아이콘)

3. 행 클릭 → `04-편집모달`
4. 편집 아이콘 클릭 → `04-편집모달`
5. 삭제 아이콘 클릭 → `05-삭제확인`

데이터:
- interface ClassTemplate { id, branchId, name, type, capacity, duration, instructorId, instructorName, color, isActive, memo }
- supabase.from('class_templates').select('*, staff(name)').eq('branch_id', branchId).order('created_at', {ascending: false})
- 검색: templates.filter(t => t.name.includes(searchQuery))

인터랙션:
- [+ 템플릿 추가] → `03-추가모달`
- 행 클릭 → `04-편집모달`
- 삭제 아이콘 → `05-삭제확인`
- 검색어 입력 → 즉시 필터링

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Pencil, Trash2, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 후 class_templates 데이터 1건 이상

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 템플릿 목록 | `class_templates` | `branch_id = ?` |
| 강사명 | `staff` | JOIN class_templates.instructor_id |

### 인터랙션 (User Actions)
1. [+ 템플릿 추가] → `03-추가모달`
2. 행 클릭 / 편집 아이콘 → `04-편집모달`
3. 삭제 아이콘 → `05-삭제확인`
4. 검색 → 즉시 클라이언트 필터
5. 유형 Select → 클라이언트 필터

### 비즈니스 룰
- 템플릿은 수업 등록 시 사전 입력 자동완성에 사용
- is_active=false 템플릿: 목록 노출 유지, 수업 등록 시 선택 불가

### 에지 케이스
- 검색 결과 없음: "검색 결과가 없습니다." 인라인 메시지

### 접근성 (A11y)
- 테이블 `caption="수업 템플릿 목록"`
- 삭제 버튼 `aria-label="{템플릿명} 삭제"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `03-추가모달` / `04-편집모달` / `05-삭제확인`
