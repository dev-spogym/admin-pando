# SCR-C004 수업 템플릿 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/class-templates` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (사이드바 클릭) |
| 다음 상태 | `02-기본-목록` / `06-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 수업 템플릿 — 상태: 로딩

파일: src/app/(classes)/class-templates/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="수업 템플릿" subtitle="수업 템플릿을 관리합니다."
    actions={[<Button variant="primary" disabled>템플릿 추가</Button>]} />

스켈레톤:
1. [SearchFilter 스켈레톤] h-10 bg-gray-200 animate-pulse rounded-lg w-64 mb-4
2. [테이블 스켈레톤] DataTable 형태
   - 헤더 행: h-10 bg-gray-200 animate-pulse rounded
   - 데이터 행 × 5: h-14 bg-gray-100 animate-pulse rounded mt-1

데이터:
- useState: isLoading = true
- useEffect: supabase.from('class_templates').select('*').eq('branch_id', branchId)

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 사이드바 > 수업관리 > 수업 템플릿 클릭
- 템플릿 추가/수정/삭제 후 목록 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 |
|--------|------|
| `class_templates` | `select('*').eq('branch_id', branchId).order('created_at', {ascending: false})` |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본-목록` (데이터 있음) / `06-빈상태` (0건)
2. 로드 실패 → toast.error('템플릿 목록을 불러오지 못했습니다.')

### 비즈니스 룰
- 센터장/매니저만 접근 가능

### 접근성 (A11y)
- `role="status"` + `aria-label="템플릿 목록 로딩 중"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본-목록` / `06-빈상태`
