# SCR-C004 수업 템플릿 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/class-templates` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | `03-추가모달` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 수업 템플릿 — 상태: 빈상태 (템플릿 0건)

파일: src/app/(classes)/class-templates/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="수업 템플릿" actions={[<Button variant="primary" onClick={openAddModal}>+ 템플릿 추가</Button>]} />

빈상태 UI:
- 중앙 정렬 컨테이너 py-20 text-center
- LayoutTemplate 아이콘 (w-16 h-16 text-gray-300 mx-auto mb-4)
- "등록된 템플릿이 없습니다." (text-lg font-semibold text-gray-500)
- "수업 등록 시 자동 완성되는 템플릿을 추가하세요." (text-sm text-gray-400 mt-1)
- [+ 첫 템플릿 추가하기] 버튼 (variant=primary mt-6 onClick=openAddModal)

조건:
- templates.length === 0 && !isLoading && !error

사용 유틸:
- lucide-react: LayoutTemplate, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- class_templates 테이블 조회 결과 0건
- 모든 템플릿 삭제 후

### 인터랙션 (User Actions)
1. [+ 첫 템플릿 추가하기] → `03-추가모달`
2. PageHeader [+ 템플릿 추가] → `03-추가모달`

### 비즈니스 룰
- 검색 필터 적용 후 0건: 빈상태 UI 대신 "검색 결과가 없습니다." 인라인 메시지

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`
- 추가 버튼 `aria-label="첫 수업 템플릿 추가"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `03-추가모달`
