# SCR-C007 수강권 횟수 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C007 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/lesson-counts` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | `05-추가모달` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C007 수강권 횟수 — 상태: 빈상태

파일: src/app/(classes)/lesson-counts/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="수강권 횟수 관리"
    actions={[<Button variant="primary" onClick={openAddModal}>+ 횟수 추가</Button>]} />
- TabNav 공통 탭

빈상태 UI:
- 중앙 정렬 py-20 text-center
- Hash 아이콘 (w-16 h-16 text-gray-300 mx-auto mb-4)
- "등록된 수강권 횟수가 없습니다." (text-lg font-semibold text-gray-500)
- "회원의 수강권 횟수를 추가하세요." (text-sm text-gray-400 mt-1)
- [+ 횟수 추가하기] (variant=primary mt-6 onClick=openAddModal)

사용 유틸:
- lucide-react: Hash, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- lesson_counts 조회 결과 0건
- 필터 결과 0건 (검색어/수업명/강사 필터)

### 인터랙션 (User Actions)
1. [+ 횟수 추가하기] / PageHeader 버튼 → `05-추가모달`

### 비즈니스 룰
- 검색 결과 0건: 빈상태 UI 대신 "검색 결과가 없습니다." 인라인

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `05-추가모달`
