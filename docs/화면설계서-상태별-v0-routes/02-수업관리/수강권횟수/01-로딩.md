# SCR-C007 수강권 횟수 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C007 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/lesson-counts` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P0 |
| 이전 상태 | 진입 (사이드바 또는 TabNav 클릭) |
| 다음 상태 | `02-기본-목록` / `06-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C007 수강권 횟수 — 상태: 로딩

파일: src/app/(classes)/lesson-counts/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="수강권 횟수 관리" subtitle="회원별 수강권 잔여 횟수를 관리합니다." />
- TabNav (수업관리 공통 탭): [일정표][수업 관리][횟수 관리(active)][페널티 관리][유효 수업 목록]

스켈레톤:
1. [SearchFilter 스켈레톤] flex gap-3 mb-4
   - h-10 w-64 bg-gray-200 animate-pulse rounded-lg
   - h-10 w-32 bg-gray-200 animate-pulse rounded-lg
2. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 8: h-14 bg-gray-100 animate-pulse rounded mt-1

데이터:
- useEffect: supabase.from('lesson_counts').select('*, members(name, phone), lessons(name), staff(name)').eq('branch_id', branchId).order('remaining_count', {ascending: true})

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- TabNav "횟수 관리" 탭 클릭
- 사이드바 > 수업관리 > 수강권 횟수 클릭
- 차감/추가 후 목록 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 |
|--------|------|
| `lesson_counts` | `select('*, members(*), lessons(*), staff(*)')` |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본-목록` / `06-빈상태`
2. 로드 실패 → toast.error('횟수 목록을 불러오지 못했습니다.')

### 비즈니스 룰
- 잔여 횟수 오름차순 정렬 (위험 회원 우선 노출)

### 접근성 (A11y)
- `role="status"` + `aria-label="수강권 횟수 목록 로딩 중"`

### 연결 화면
- 이전: TabNav / 사이드바
- 다음: `02-기본-목록` / `06-빈상태`
