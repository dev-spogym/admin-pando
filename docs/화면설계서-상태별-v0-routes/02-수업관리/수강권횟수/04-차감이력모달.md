# SCR-C007 수강권 횟수 — 상태: 차감이력모달

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C007 |
| 상태 코드 | `deduction-history-modal` |
| 경로 | `/app/(classes)/lesson-counts` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P1 |
| 이전 상태 | `02-기본-목록` ([이력] 아이콘 클릭) |
| 다음 상태 | 닫기 → `02-기본-목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C007 수강권 횟수 — 상태: 차감 이력 모달

파일: src/app/(classes)/lesson-counts/page.tsx

모달 크기: max-w-lg (512px), max-h-[80vh] overflow-y-auto

모달 구조:
1. [헤더]
   - "{memberName}님 — {lessonName} 차감 이력" (font-bold text-lg) + X 버튼

2. [요약 바] bg-gray-50 rounded-lg p-3 mb-4 grid grid-cols-3
   - 총 횟수: {totalCount}회
   - 사용: {usedCount}회
   - 잔여: {remainingCount}회 (잔여 위험 시 text-red-600)

3. [이력 테이블]
   - 컬럼: 일시 | 유형 | 횟수 | 처리자 | 메모
   - 유형 배지:
     · "차감" → bg-red-100 text-red-700 (수업 출석)
     · "추가" → bg-green-100 text-green-700 (수동 추가)
     · "환불" → bg-blue-100 text-blue-700
     · "만료" → bg-gray-100 text-gray-600
   - 최신 이력 우선 (created_at desc)
   - 페이지네이션: 20건씩

4. [하단] flex justify-end
   - [닫기] variant=ghost

데이터:
- supabase.from('lesson_count_histories').select('*, staff(name)').eq('lesson_count_id', selectedLessonCount.id).order('created_at', {ascending:false}).range(0,19)
- interface LessonCountHistory { id, lessonCountId, type, amount, staffId, staffName, memo, createdAt }

인터랙션:
- 페이지네이션 → .range(page*20, (page+1)*20-1)
- X / [닫기] / ESC → 모달 닫기

사용 유틸:
- supabase from '@/lib/supabase'
- formatDate from '@/lib/format'
- lucide-react: X, History, TrendingDown, TrendingUp
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-기본-목록` 테이블에서 [이력](History) 아이콘 클릭

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 차감 이력 | `lesson_count_histories` | `lesson_count_id = ?`, `order by created_at desc` |
| 처리자 | `staff` | JOIN lesson_count_histories.staff_id |

### 인터랙션 (User Actions)
1. 페이지네이션 → 다음/이전 페이지 로드
2. X / [닫기] / ESC → 모달 닫기

### 비즈니스 룰
- 이력은 READ ONLY (수정/삭제 불가)
- 수업 출석에 의한 자동 차감: 처리자 = "시스템"

### 에지 케이스
- 이력 0건: "차감 이력이 없습니다." 텍스트

### 접근성 (A11y)
- `role="dialog"` + `aria-labelledby`
- 테이블 `caption="{회원명} 차감 이력"`

### 연결 화면
- 이전: `02-기본-목록`
- 다음: 닫기 → `02-기본-목록`
