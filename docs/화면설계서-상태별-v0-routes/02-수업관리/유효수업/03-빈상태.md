# SCR-C011 유효 수업 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/valid-lessons` |
| 역할 | 센터장 / 매니저 / 트레이너 / 프론트 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (활성 수업 0건) |
| 다음 상태 | `/lessons` (수업 활성화) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C011 유효 수업 — 상태: 빈상태

파일: src/app/(classes)/valid-lessons/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="유효 수업 목록" />
- TabNav 공통 탭

빈상태 UI:
- 중앙 정렬 py-20 text-center
- BookX 아이콘 (w-16 h-16 text-gray-300 mx-auto mb-4)
- "활성 수업이 없습니다." (text-lg font-semibold text-gray-500)
- "수업 관리에서 수업을 활성화하세요." (text-sm text-gray-400 mt-1)
- [수업 관리로 이동] 버튼 (variant=outline mt-6 onClick={() => moveToPage('/lessons')})

사용 유틸:
- moveToPage from '@/internal'
- lucide-react: BookX
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- lessons WHERE is_active=true 결과 0건
- 모든 수업이 비활성화된 경우

### 인터랙션 (User Actions)
1. [수업 관리로 이동] → moveToPage('/lessons')

### 비즈니스 룰
- 검색/필터 결과 0건: "검색 결과가 없습니다." 인라인

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `/lessons`
