# SCR-C011 유효 수업 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `error` |
| 경로 | `/app/(classes)/valid-lessons` |
| 역할 | 센터장 / 매니저 / 트레이너 / 프론트 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (fetch 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C011 유효 수업 — 상태: 에러

파일: src/app/(classes)/valid-lessons/page.tsx

에러 UI:
- <AppLayout> + <PageHeader title="유효 수업 목록" /> + TabNav 유지
- 본문 중앙:
  · bg-red-50 border border-red-200 rounded-xl p-8 text-center max-w-md mx-auto
  · AlertCircle 아이콘 (w-12 h-12 text-red-400 mx-auto mb-4)
  · "유효 수업 목록을 불러오지 못했습니다." (text-lg font-semibold text-red-700)
  · errorMessage (text-sm text-red-500 mt-1)
  · [다시 시도] 버튼 (variant=outline mt-4 onClick=fetchValidLessons)

에러 처리:
- try/catch → setError(err.message)
- toast.error('유효 수업 목록을 불러오지 못했습니다.')

사용 유틸:
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: AlertCircle, RefreshCw
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase fetch 실패 (네트워크 오류, 권한 오류 등)

### 인터랙션 (User Actions)
1. [다시 시도] → setError(null) + 재fetch → `01-로딩`

### 비즈니스 룰
- 에러 메시지: 사용자 친화적 (DB 코드 미노출)

### 접근성 (A11y)
- `role="alert"` on 에러 카드

### 연결 화면
- 이전: `01-로딩`
- 다음: `01-로딩` (재시도)
