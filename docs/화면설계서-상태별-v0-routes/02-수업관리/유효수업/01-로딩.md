# SCR-C011 유효 수업 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/valid-lessons` |
| 역할 | 센터장 / 매니저 / 트레이너 / 프론트 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (TabNav "유효 수업 목록" 클릭) |
| 다음 상태 | `02-기본` / `03-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C011 유효 수업 — 상태: 로딩

파일: src/app/(classes)/valid-lessons/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="유효 수업 목록" subtitle="현재 진행 중인 활성 수업 목록입니다." />
- TabNav 공통 탭 (유효 수업 목록 active)

스켈레톤:
1. [SearchFilter 스켈레톤] h-10 w-64 bg-gray-200 animate-pulse rounded-lg mb-4
2. [카드 그리드 스켈레톤] grid grid-cols-3 gap-4
   - 각 카드: h-36 bg-gray-100 animate-pulse rounded-xl × 9개

데이터:
- useEffect: supabase.from('lessons').select('*, staff(name), lesson_schedules(count)').eq('branch_id', branchId).eq('is_active', true).order('name')

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- TabNav "유효 수업 목록" 탭 클릭
- 사이드바 > 수업관리 > 유효 수업 클릭

### 필수 데이터
| 테이블 | 조건 |
|--------|------|
| `lessons` | `branch_id = ?`, `is_active = true` |
| `lesson_schedules` | count (예정 일정 수) |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본` / `03-빈상태`
2. 로드 실패 → `04-에러`

### 접근성 (A11y)
- `role="status"` + `aria-label="유효 수업 목록 로딩 중"`

### 연결 화면
- 이전: TabNav
- 다음: `02-기본` / `03-빈상태` / `04-에러`
