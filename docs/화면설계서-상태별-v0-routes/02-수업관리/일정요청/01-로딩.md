# SCR-C009 일정 요청 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C009 |
| 상태 코드 | `loading` |
| 경로 | `/app/schedule-requests` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (캘린더 배너 [처리하기] 클릭 또는 사이드바) |
| 다음 상태 | `02-대기탭` / 빈상태 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C009 일정 요청 — 상태: 로딩

파일: src/app/schedule-requests/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="일정 요청 처리" subtitle="트레이너의 일정 변경 요청을 승인하거나 반려합니다." />

스켈레톤:
1. [탭 스켈레톤] flex gap-2 mb-6
   - h-9 w-24 bg-gray-200 animate-pulse rounded-lg × 2개
2. [StatCardGrid 스켈레톤] grid grid-cols-3 gap-4 mb-6
   - h-20 bg-gray-100 animate-pulse rounded-xl × 3개
3. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 6: h-16 bg-gray-100 animate-pulse rounded mt-1

데이터:
- Promise.all([
    supabase.from('schedule_requests').select('*, staff(name), lessons(name)').eq('branch_id', branchId).eq('status','pending').order('created_at', {ascending:true}),
    supabase.from('schedule_requests').select('*, staff(name), lessons(name)').eq('branch_id', branchId).in('status',['approved','rejected']).order('created_at', {ascending:false}).limit(50)
  ])

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 캘린더 미승인 배너 [처리하기] 버튼 클릭
- 사이드바 > 수업관리 > 일정 요청
- 승인/반려 처리 후 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 목적 |
|--------|-----------|
| `schedule_requests` | 대기/완료 요청 목록 |
| `staff` | 요청자 정보 |
| `lessons` | 수업 정보 |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-대기탭` (pending > 0) / 빈상태 (0건)
2. 로드 실패 → toast.error

### 비즈니스 룰
- 센터장/매니저만 접근

### 접근성 (A11y)
- `role="status"` + `aria-label="일정 요청 목록 로딩 중"`

### 연결 화면
- 이전: 캘린더 배너 / 사이드바
- 다음: `02-대기탭`
