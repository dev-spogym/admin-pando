# SCR-C005 수업 통계 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C005 |
| 상태 코드 | `error` |
| 경로 | `/app/(classes)/class-stats` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (fetch 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C005 수업 통계 — 상태: 데이터 로드 에러

파일: src/app/(classes)/class-stats/page.tsx

에러 UI:
- AppLayout + PageHeader 유지
- 본문 중앙에 에러 카드:
  · bg-red-50 border border-red-200 rounded-xl p-8 text-center
  · AlertCircle 아이콘 (w-12 h-12 text-red-400 mx-auto mb-4)
  · "수업 통계를 불러오지 못했습니다." (text-lg font-semibold text-red-700)
  · errorMessage 표시 (text-sm text-red-500 mt-1)
  · [다시 시도] 버튼 (variant=outline, onClick=fetchStats)
  · [이전 페이지로] 링크 (text-sm text-gray-500 mt-2)

에러 처리:
- try/catch 블록에서 setError(err.message)
- toast.error('수업 통계를 불러오지 못했습니다.')

재시도 로직:
- [다시 시도] 클릭 → setError(null) + setIsLoading(true) + fetchStats()

사용 유틸:
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: AlertCircle, RefreshCw
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase fetch 실패 (네트워크 오류, 권한 오류 등)
- RLS 정책에 의한 접근 거부

### 인터랙션 (User Actions)
1. [다시 시도] → `01-로딩` 재진입
2. [이전 페이지로] → 브라우저 back

### 비즈니스 룰
- 에러 메시지: 사용자 친화적 메시지 (DB 오류 코드 미노출)

### 에지 케이스
- 권한 오류(403): "접근 권한이 없습니다." 별도 메시지

### 접근성 (A11y)
- `role="alert"` on 에러 카드

### 연결 화면
- 이전: `01-로딩`
- 다음: `01-로딩` (재시도)
