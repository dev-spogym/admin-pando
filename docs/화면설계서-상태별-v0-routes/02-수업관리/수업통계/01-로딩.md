# SCR-C005 수업 통계 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C005 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/class-stats` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (사이드바 클릭) |
| 다음 상태 | `02-이번달` (데이터 로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C005 수업 통계 — 상태: 로딩

파일: src/app/(classes)/class-stats/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="수업 통계" subtitle="그룹 수업 현황 및 통계를 확인합니다." />

로딩 스켈레톤:
1. [기간 선택 바 스켈레톤]
   - flex gap-2: h-9 w-32 bg-gray-200 animate-pulse rounded-lg × 2개
2. [StatCardGrid 스켈레톤] grid grid-cols-4 gap-4
   - <StatCard> 형태 스켈레톤 × 4개
   - 각: h-24 bg-gray-100 animate-pulse rounded-xl
3. [차트 영역 스켈레톤]
   - h-72 bg-gray-100 animate-pulse rounded-xl

데이터:
- useState: isLoading = true
- useEffect: Promise.all([
    supabase.from('lesson_schedules').select('*').gte('start_at', monthStart).lte('start_at', monthEnd),
    supabase.from('lesson_bookings').select('*').gte('created_at', monthStart),
    supabase.from('lessons').select('*').eq('branch_id', branchId)
  ])

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 사이드바 > 수업관리 > 수업 통계 클릭
- 기간 탭 전환 시 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 목적 |
|--------|-----------|
| `lesson_schedules` | 수업 횟수 집계 |
| `lesson_bookings` | 예약/출석 인원 집계 |
| `lessons` | 수업별 통계 분류 |

### 인터랙션 (User Actions)
1. 데이터 로드 완료 → `02-이번달` 자동 전환
2. 로드 실패 → `05-에러` 상태 전환

### 비즈니스 룰
- 센터장/매니저만 접근 가능 (트레이너/프론트 접근 시 403)

### 에지 케이스
- 로딩 3초 이상 지속 시 타임아웃 없음 (Supabase 기본 타임아웃 의존)

### 접근성 (A11y)
- `role="status"` + `aria-label="통계 데이터 로딩 중"` on 스켈레톤

### 연결 화면
- 이전: 사이드바 네비게이션
- 다음: `02-이번달` / `05-에러`
