# SCR-C006 강사 현황 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C006 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/instructor-status` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (사이드바 클릭) |
| 다음 상태 | `02-기본-목록` / `05-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C006 강사 현황 — 상태: 로딩

파일: src/app/(classes)/instructor-status/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="강사 현황" subtitle="강사별 수업 현황을 확인합니다." />

스켈레톤:
1. [StatCardGrid 스켈레톤] grid grid-cols-3 gap-4 mb-6
   - h-24 bg-gray-100 animate-pulse rounded-xl × 3개
2. [필터 바 스켈레톤] h-10 w-80 bg-gray-200 animate-pulse rounded-lg mb-4
3. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 5: h-16 bg-gray-100 animate-pulse rounded mt-1

데이터:
- Promise.all([
    supabase.from('staff').select('*, lesson_schedules(count)').eq('branch_id', branchId).eq('role', 'trainer'),
    supabase.from('lesson_schedules').select('instructor_id, count').eq('branch_id', branchId).gte('start_at', monthStart)
  ])

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 사이드바 > 수업관리 > 강사 현황 클릭
- 필터 변경 시 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 목적 |
|--------|-----------|
| `staff` | 강사 목록 및 기본 정보 |
| `lesson_schedules` | 강사별 수업 횟수 집계 |
| `lesson_bookings` | 강사별 총 예약 인원 집계 |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본-목록` / `05-빈상태`
2. 로드 실패 → toast.error

### 비즈니스 룰
- 센터장/매니저만 접근 가능

### 접근성 (A11y)
- `role="status"` + `aria-label="강사 현황 로딩 중"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본-목록` / `05-빈상태`
