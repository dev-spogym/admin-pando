# SCR-C006 강사 현황 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C006 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/instructor-status` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (강사 0명) |
| 다음 상태 | `/staff` (강사 등록) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C006 강사 현황 — 상태: 빈상태 (등록된 강사 없음)

파일: src/app/(classes)/instructor-status/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="강사 현황" />

빈상태 UI:
- 중앙 정렬 py-20 text-center
- UserX 아이콘 (w-16 h-16 text-gray-300 mx-auto mb-4)
- "등록된 강사가 없습니다." (text-lg font-semibold text-gray-500)
- "직원 관리에서 강사를 먼저 등록하세요." (text-sm text-gray-400 mt-1)
- [직원 관리로 이동] 버튼 (variant=outline mt-6 onClick={() => moveToPage('/staff')})

사용 유틸:
- moveToPage from '@/internal'
- lucide-react: UserX
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- staff 테이블에서 role='trainer' AND branch_id=현재지점 결과 0건

### 인터랙션 (User Actions)
1. [직원 관리로 이동] → moveToPage('/staff')

### 비즈니스 룰
- 필터 적용 후 0건: 빈상태 UI 대신 "검색 결과가 없습니다." 인라인

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `/staff` (강사 등록)
