# SCR-C010 운동 프로그램 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C010 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/exercise-programs` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P1 |
| 이전 상태 | 진입 (사이드바 클릭) |
| 다음 상태 | `02-기본` / `06-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C010 운동 프로그램 — 상태: 로딩

파일: src/app/(classes)/exercise-programs/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="운동 프로그램" subtitle="회원별 운동 프로그램을 관리합니다." />

스켈레톤:
1. [SearchFilter 스켈레톤] h-10 w-64 bg-gray-200 animate-pulse rounded-lg mb-4
2. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 6: h-14 bg-gray-100 animate-pulse rounded mt-1

데이터:
- useState: isLoading = true
- useEffect: Promise.all([
    supabase.from('exercise_programs').select('*, staff(name)').eq('branch_id', branchId),
    supabase.from('member_exercise_programs').select('count', {count: 'exact'})
  ])

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 사이드바 > 수업관리 > 운동 프로그램 클릭
- CRUD 후 목록 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 |
|--------|------|
| `exercise_programs` | `select('*, staff(name)').eq('branch_id', branchId)` |
| `member_exercise_programs` | 배정 수 집계 |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본` / `06-빈상태`
2. 로드 실패 → toast.error

### 비즈니스 룰
- 트레이너: 본인이 생성한 프로그램만 조회 (created_by = staffId)

### 접근성 (A11y)
- `role="status"` + `aria-label="운동 프로그램 목록 로딩 중"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본` / `06-빈상태`
