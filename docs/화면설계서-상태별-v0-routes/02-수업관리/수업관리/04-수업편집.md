# SCR-C002 수업 관리 — 상태: 수업편집

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `edit-lesson` |
| 경로 | `/app/(classes)/lessons` |
| 역할 | 센터장 / 매니저 / 트레이너(본인 수업) |
| 우선순위 | P0 |
| 이전 상태 | `02-기본` (행 클릭 / [편집]) |
| 다음 상태 | 저장 → `02-기본` / 취소 → `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업 관리 — 상태: 수업 편집 모달

파일: src/app/(classes)/lessons/page.tsx

모달 크기: max-w-lg (512px)

구조: 추가모달과 동일, 차이점:
1. 헤더: "수업 수정"
2. 모든 필드에 selectedLesson 데이터 사전 입력
3. 하단 버튼: [취소] + [수정 완료]

추가 섹션 (편집 전용):
[예정 일정 수] p-4 bg-gray-50 rounded-lg mb-4
- "연결된 예정 일정: {scheduleCount}회"
- scheduleCount > 0 시: "수업 정보 변경은 기존 일정에 영향을 주지 않습니다." (text-xs text-amber-600)

수업명 중복 체크:
- 기존 이름과 다를 때만 체크

수정 로직:
- supabase.from('lessons').update({ name, type, instructor_id, capacity, duration, color, is_active, memo }).eq('id', selectedLesson.id)
- 성공: toast.success('수업이 수정되었습니다.') + 닫기 + 재fetch

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: X, Info
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-기본` 테이블 행 클릭 또는 [편집] 아이콘 클릭

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 수업 상세 | `lessons` | `id = selectedLesson.id` |
| 예정 일정 수 | `lesson_schedules` | `lesson_id = ?`, `start_at >= now()`, count |
| 강사 목록 | `staff` | `branch_id = ?`, `is_active = true` |

### 인터랙션 (User Actions)
1. 필드 수정 후 [수정 완료] → UPDATE → toast.success
2. [취소] / ESC → 모달 닫기

### 비즈니스 룰
- 트레이너: 본인 담당 수업만 편집 가능
- 수업명 변경: 기존 lesson_schedules의 수업명은 별도 컬럼에 저장 (JOIN 방식이므로 자동 반영)

### 에지 케이스
- 변경 없이 [수정 완료]: UPDATE 스킵 + 닫기

### 접근성 (A11y)
- `role="dialog"` + `aria-labelledby`

### 연결 화면
- 이전: `02-기본`
- 다음: 저장/취소 → `02-기본`
