# SCR-C002 수업 관리 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `empty` |
| 경로 | `/app/(classes)/lessons` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | `03-수업추가모달` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업 관리 — 상태: 빈상태

파일: src/app/(classes)/lessons/page.tsx

레이아웃:
- <AppLayout> + <PageHeader title="수업 관리"
    actions={[<Button variant="primary" onClick={openAddModal}>+ 수업 추가</Button>]} />
- TabNav 공통 탭

빈상태 UI:
- 중앙 정렬 py-20 text-center
- BookOpen 아이콘 (w-16 h-16 text-gray-300 mx-auto mb-4)
- "등록된 수업이 없습니다." (text-lg font-semibold text-gray-500)
- "수업을 추가하여 일정 관리를 시작하세요." (text-sm text-gray-400 mt-1)
- [+ 첫 수업 추가하기] (variant=primary mt-6 onClick=openAddModal)

사용 유틸:
- lucide-react: BookOpen, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- lessons 조회 결과 0건

### 인터랙션 (User Actions)
1. [+ 첫 수업 추가하기] / PageHeader 버튼 → `03-수업추가모달`

### 비즈니스 룰
- 검색 결과 0건: 빈상태 UI 대신 "검색 결과가 없습니다." 인라인
- 트레이너 역할: 추가 버튼 숨김 + "센터장/매니저에게 문의하세요." 안내

### 접근성 (A11y)
- 빈상태 아이콘 `aria-hidden="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `03-수업추가모달`
