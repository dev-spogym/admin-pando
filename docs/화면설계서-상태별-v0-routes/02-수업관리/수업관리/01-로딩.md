# SCR-C002 수업 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `loading` |
| 경로 | `/app/(classes)/lessons` |
| 역할 | 센터장 / 매니저 / 트레이너 |
| 우선순위 | P0 |
| 이전 상태 | 진입 (TabNav "수업 관리" 클릭) |
| 다음 상태 | `02-기본` / `07-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업 관리 — 상태: 로딩

파일: src/app/(classes)/lessons/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="수업 관리" subtitle="수업을 등록하고 관리합니다."
    actions={[<Button variant="primary" disabled>+ 수업 추가</Button>]} />
- TabNav 공통 탭 (수업 관리 탭 active)

스켈레톤:
1. [StatCardGrid 스켈레톤] grid grid-cols-4 gap-4 mb-6
   - h-20 bg-gray-100 animate-pulse rounded-xl × 4개
2. [SearchFilter 스켈레톤] flex gap-3 mb-4
   - h-10 w-64 bg-gray-200 animate-pulse rounded-lg
   - h-10 w-32 bg-gray-200 animate-pulse rounded-lg × 2개
3. [DataTable 스켈레톤]
   - 헤더: h-10 bg-gray-200 animate-pulse rounded mb-1
   - 행 × 8: h-14 bg-gray-100 animate-pulse rounded mt-1

데이터:
- Promise.all([
    supabase.from('lessons').select('*, staff(name), lesson_schedules(count)').eq('branch_id', branchId),
    supabase.from('lesson_schedules').select('id, status').eq('branch_id', branchId).gte('start_at', today)
  ])

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- TabNav "수업 관리" 탭 클릭
- 사이드바 > 수업관리 > 수업 관리 클릭
- CRUD 후 목록 재fetch 중

### 필수 데이터
| 테이블 | 쿼리 목적 |
|--------|-----------|
| `lessons` | 수업 기본 정보 목록 |
| `lesson_schedules` | 수업별 일정 수 집계 |
| `staff` | 강사 정보 |

### 인터랙션 (User Actions)
1. 로드 완료 → `02-기본` / `07-빈상태`
2. 로드 실패 → toast.error

### 접근성 (A11y)
- `role="status"` + `aria-label="수업 목록 로딩 중"`

### 연결 화면
- 이전: TabNav / 사이드바
- 다음: `02-기본` / `07-빈상태`
