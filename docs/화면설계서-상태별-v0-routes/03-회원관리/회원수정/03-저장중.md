# SCR-M003 회원 수정 — 상태: 저장 중 (submitting)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M003 |
| 상태 코드 | `edit-submitting` |
| 경로 | `/members/edit?id={memberId}` |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 이전 상태 | `02-입력중` ([저장] 클릭) |
| 다음 상태 | `04-저장성공` / `05-검증에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M003 회원 수정 — 상태: 저장 중 (API 호출 중)

파일: src/app/members/edit/page.tsx (isSubmitting === true)

레이아웃:
- 폼 전체 영역 overlay (pointer-events-none + opacity-60)
- [저장] 버튼: 스피너 아이콘 + "저장 중..." 텍스트 (disabled)
- [취소] 버튼: disabled

컴포넌트 구조:
1. [폼 오버레이] — 저장 중 전체 폼 비활성화
   - div.absolute.inset-0.bg-white.opacity-40 (폼 컨테이너에 relative 추가)
2. [저장 버튼 상태]
   - <Loader2 className="animate-spin" /> + "저장 중..." 텍스트
   - disabled={true}
3. [취소 버튼] — disabled={true}

데이터:
- isSubmitting: true (react-hook-form formState)
- supabase.from('members').update(dirtyData).eq('id', memberId)
- dirtyData: formState.dirtyFields 기준으로 변경된 필드만 추출하여 PATCH

인터랙션:
- 저장 중 모든 입력 차단
- 성공 → `04-저장성공`
- 실패 → `05-검증에러` (또는 서버 에러 토스트)

사용 유틸:
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- [저장] 버튼 클릭 후 Supabase UPDATE API 호출 중
- `isSubmitting === true` 구간

### 필수 데이터
- 변경된 필드만 PATCH (dirtyFields 기반 최적화)
- `members` 테이블 UPDATE

### 인터랙션 (User Actions)
- 없음 (모든 인터랙션 차단)

### 비즈니스 룰
- 변경된 필드만 업데이트 (전체 재전송 아님)
- updatedAt 자동 갱신

### 에지 케이스
- 네트워크 타임아웃 (10초) → 에러 처리
- RLS 위반 응답 → "권한이 없습니다" 토스트

### 접근성 (A11y)
- 저장 중 `aria-busy="true"` 폼 컨테이너
- 버튼 `aria-label="저장 중, 잠시 기다려주세요"`

### 연결 화면
- 이전: `02-입력중`
- 다음: `04-저장성공` / `05-검증에러`
