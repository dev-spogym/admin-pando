# SCR-M003 회원 수정 — 상태: 저장 성공

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M003 |
| 상태 코드 | `edit-success` |
| 경로 | `/members/detail?id={memberId}` (자동 이동) |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 이전 상태 | `03-저장중` (UPDATE 성공) |
| 다음 상태 | 회원 상세 `02-기본정보` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M003 회원 수정 — 상태: 저장 성공 (post-submit)

파일: src/app/members/edit/page.tsx (onSuccess 핸들러)

동작:
- Supabase UPDATE 성공 응답 수신
- 성공 토스트 표시
- 회원 상세 페이지로 자동 이동

컴포넌트 구조:
1. [성공 토스트] — sonner toast.success
   - 메시지: "회원 정보가 수정되었습니다."
   - duration: 3000ms
   - 위치: top-right
2. [자동 리다이렉트]
   - router.push('/members/detail?id=' + memberId)
   - 0ms 지연 (즉시 이동)

데이터:
- 저장 완료 후 폼 상태: isDirty=false, isSubmitting=false
- 리다이렉트 대상: `/members/detail?id={memberId}`

인터랙션:
- 자동 이동 — 사용자 액션 불필요
- 토스트는 이동 후 상세 페이지에서도 표시

사용 유틸:
- router.push from 'next/navigation'
- toast.success from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase `members` UPDATE 쿼리 성공 응답 (error === null)

### 필수 데이터
- 저장된 memberId (리다이렉트 경로에 사용)

### 인터랙션 (User Actions)
- 없음 (자동 전환)

### 비즈니스 룰
- 저장 성공 즉시 회원 상세 페이지로 이동
- 상세 페이지에서 변경된 데이터가 반영되어야 함 (캐시 무효화 또는 refetch)

### 에지 케이스
- 리다이렉트 중 네트워크 단절 → Next.js router가 처리

### 접근성 (A11y)
- 토스트 `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `03-저장중`
- 다음: 회원 상세 `02-기본정보`
