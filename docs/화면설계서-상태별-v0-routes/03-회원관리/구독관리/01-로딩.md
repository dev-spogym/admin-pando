# SCR-SUB 구독 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-SUB |
| 상태 코드 | `sub-loading` |
| 경로 | `/subscription` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | 사이드바 > 구독 관리 클릭 |
| 다음 상태 | `02-기본-목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-SUB 구독 관리 — 상태: 로딩

파일: src/app/subscription/page.tsx

레이아웃:
- <AppLayout> 데스크톱
- <PageHeader title="구독 관리" subtitle="회원 구독 현황 및 결제 상태를 관리합니다." />
- 통계 카드 + 테이블 스켈레톤

컴포넌트 구조:
1. [StatCardGrid 스켈레톤] — 4개 카드 animate-pulse h-24
2. [필터 바 스켈레톤] — h-9 bg-gray-100
3. [DataTable 스켈레톤] — 8행

데이터:
- isLoading: true
- supabase.from('subscriptions').select('*, members(name, phone)').eq('branchId', branchId) 호출 중

인터랙션:
- 자동 전환

사용 유틸:
- useAuthStore from '@/stores/authStore'
- hasPermission from '@/lib/permissions'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/subscription` 최초 진입
- fc/staff는 접근 불가 → `/forbidden`

### 필수 데이터
- `subscriptions` JOIN `members` fetch 중

### 인터랙션 (User Actions)
- 자동 전환

### 비즈니스 룰
- owner/manager/primary만 접근 가능

### 에지 케이스
- 권한 없는 역할 접근 → `/forbidden`

### 접근성 (A11y)
- `aria-busy="true"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본-목록`
