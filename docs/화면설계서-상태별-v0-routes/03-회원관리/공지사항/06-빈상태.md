# SCR-NOTICE 공지사항 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-NOTICE |
| 상태 코드 | `notice-empty` |
| 경로 | `/notices` |
| 역할 | primary / owner / manager / fc / staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (공지 0건) |
| 다음 상태 | `02-기본-목록` (공지 작성 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-NOTICE 공지사항 — 상태: 빈 상태

파일: src/app/notices/page.tsx (notices.length === 0 && !isLoading)

레이아웃:
- AppLayout + PageHeader (동일)
- 공지 목록 영역 대신 EmptyState

컴포넌트 구조:
1. [EmptyState]
   - 아이콘: BellOff (w-16 h-16 text-gray-300, lucide-react)
   - 제목: "등록된 공지사항이 없습니다"
   - 설명: "공지 작성 버튼을 클릭하여 첫 번째 공지를 등록하세요."
   - [공지 작성] 버튼 (primary, owner/manager만) → openCreateModal()

데이터:
- notices: []

인터랙션:
- [공지 작성] → `03-작성모달`

사용 유틸:
- hasPermission from '@/lib/permissions'
- lucide-react: BellOff
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 공지사항이 한 건도 없을 때
- 검색 결과 0건일 때

### 필수 데이터
- notices 빈 배열

### 인터랙션 (User Actions)
1. [공지 작성] → `03-작성모달` (owner/manager)

### 비즈니스 룰
- fc/staff에게는 [공지 작성] 버튼 비노출

### 에지 케이스
- 검색 결과 0건 → "필터 초기화" 버튼도 표시

### 접근성 (A11y)
- EmptyState `role="status"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `03-작성모달`
