# SCR-M002 회원 등록 — 상태: 저장 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M002 |
| 상태 코드 | `new-submitting` |
| 경로 | `/members/new` |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 이전 상태 | `02-입력중` ([등록] 클릭) |
| 다음 상태 | `05-완료` / `06-검증에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M002 회원 등록 — 상태: 저장 중 (INSERT 진행 중)

파일: src/app/members/new/page.tsx (isSubmitting === true)

레이아웃:
- 폼 전체 비활성화 (pointer-events-none + opacity-60)
- [등록] 버튼: 스피너 + "등록 중..." (disabled)

컴포넌트 구조:
1. [폼 오버레이] — 저장 중 전체 폼 비활성화
2. [등록 버튼]
   - <Loader2 className="animate-spin mr-2" /> + "등록 중..."
   - disabled={true}
3. [취소 버튼] — disabled={true}

데이터:
- isSubmitting: true
- INSERT 페이로드:
  supabase.from('members').insert({
    name, phone, gender, birthDate, email,
    membershipType, membershipStart, membershipExpiry,
    staffId, memberType, referralSource, exercisePurpose,
    specialNote, adConsent,
    status: 'ACTIVE', branchId, registeredAt: now()
  })

인터랙션:
- 모든 입력 차단
- INSERT 성공 → `05-완료`
- INSERT 실패 → `06-검증에러`

사용 유틸:
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: Loader2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- [등록] 버튼 클릭 후 모든 클라이언트 검증 통과
- Supabase INSERT API 호출 중

### 필수 데이터
- 폼 데이터 전체 (members 테이블 INSERT)
- branchId (useAuthStore)
- registeredAt: new Date() (자동)

### 인터랙션 (User Actions)
- 없음 (모든 인터랙션 차단)

### 비즈니스 룰
- INSERT 성공 시 새 memberId 반환
- status 초기값: 'ACTIVE'
- registeredAt: 서버 타임스탬프

### 에지 케이스
- 연락처 중복 (서버 유니크 제약) → `06-검증에러` + "이미 등록된 연락처"
- RLS 위반 → 에러 처리

### 접근성 (A11y)
- `aria-busy="true"` 폼 컨테이너

### 연결 화면
- 이전: `02-입력중`
- 다음: `05-완료` / `06-검증에러`
