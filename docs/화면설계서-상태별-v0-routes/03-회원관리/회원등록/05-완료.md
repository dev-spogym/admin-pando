# SCR-M002 회원 등록 — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M002 |
| 상태 코드 | `new-success` |
| 경로 | `/members/detail?id={newMemberId}` (자동 이동) |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 이전 상태 | `04-저장중` (INSERT 성공) |
| 다음 상태 | 회원 상세 `02-기본정보` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M002 회원 등록 — 상태: 등록 완료

파일: src/app/members/new/page.tsx (onSuccess 핸들러)

동작:
- Supabase INSERT 성공 응답 수신 → newMember.id 획득
- 성공 토스트 표시
- 회원 상세 페이지로 자동 이동

컴포넌트 구조:
1. [성공 토스트] — toast.success
   - 메시지: "{name}님이 등록되었습니다."
   - action: { label: "상세 보기", onClick: () => router.push('/members/detail?id='+newId) }
   - duration: 4000ms
2. [자동 리다이렉트]
   - router.push('/members/detail?id=' + newMember.id)
   - 500ms 지연 (토스트 노출 후 이동)

데이터:
- INSERT 응답: { data: [{ id: newMemberId, name, ... }], error: null }
- 리다이렉트 경로: /members/detail?id={newMemberId}

인터랙션:
- 자동 이동 (500ms 후)
- 토스트 "상세 보기" 클릭 → 즉시 이동

사용 유틸:
- router.push from 'next/navigation'
- toast.success from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase `members` INSERT 성공 응답 (error === null)
- 새 회원 id 반환

### 필수 데이터
- INSERT 응답의 newMemberId

### 인터랙션 (User Actions)
- 자동 전환 (500ms 후 상세 페이지)
- 토스트 액션 클릭 → 즉시 이동

### 비즈니스 룰
- 등록 후 회원 상세 페이지로 이동
- 회원 목록 페이지 캐시 무효화 (revalidate)

### 에지 케이스
- 이동 전 토스트 닫기 → 이미 500ms 지연 중이므로 계속 이동

### 접근성 (A11y)
- 토스트 `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `04-저장중`
- 다음: 회원 상세 `02-기본정보`
