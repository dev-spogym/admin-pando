# SCR-BODY 체성분 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-BODY |
| 상태 코드 | `body-empty` |
| 경로 | `/body-composition` |
| 역할 | primary / owner / manager / fc / staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | `02-기본-목록` (측정 등록 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-BODY 체성분 관리 — 상태: 빈 상태

파일: src/app/body-composition/page.tsx (bodyCompositions.length === 0 && !isLoading)

레이아웃:
- AppLayout + PageHeader (동일)
- DataTable 영역 대신 EmptyState

컴포넌트 구조:
1. [StatCardGrid] — 모두 0
2. [EmptyState 컨테이너]
   - 아이콘: Activity (w-16 h-16 text-gray-300, lucide-react)
   - 제목: "체성분 측정 기록이 없습니다"
   - 설명: "측정 등록 버튼을 클릭하여 첫 번째 체성분 데이터를 등록하세요."
   - [측정 등록] 버튼 (primary) → openMeasureModal()

데이터:
- bodyCompositions: []
- isLoading: false

인터랙션:
- [측정 등록] → `03-측정등록모달`

사용 유틸:
- lucide-react: Activity
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 센터에 체성분 측정 기록이 전혀 없을 때
- 필터 적용 결과 0건일 때

### 필수 데이터
- bodyCompositions 빈 배열 확인

### 인터랙션 (User Actions)
1. [측정 등록] → `03-측정등록모달`

### 비즈니스 룰
- 모든 역할 측정 등록 가능

### 에지 케이스
- 필터 적용 결과 0건 → "필터 초기화" 버튼도 함께 표시

### 접근성 (A11y)
- EmptyState `role="status"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `03-측정등록모달`
