# SCR-BODY 체성분 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-BODY |
| 상태 코드 | `body-loading` |
| 경로 | `/body-composition` |
| 역할 | primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 이전 상태 | 사이드바 > 체성분 관리 클릭 |
| 다음 상태 | `02-기본-목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-BODY 체성분 관리 — 상태: 로딩

파일: src/app/body-composition/page.tsx

레이아웃:
- <AppLayout> 데스크톱
- <PageHeader title="체성분 관리" subtitle="회원별 체성분 측정 데이터를 관리합니다." />
- 통계 카드 + 테이블 스켈레톤

컴포넌트 구조:
1. [StatCardGrid 스켈레톤] — 3개 카드 animate-pulse
2. [검색 필터 스켈레톤] — h-9 w-full bg-gray-100
3. [DataTable 스켈레톤] — 8행 스켈레톤

데이터:
- isLoading: true
- supabase.from('bodyComposition').select('*, members(name, phone)').eq('branchId', branchId) 호출 중

인터랙션:
- 자동 전환 (데이터 수신 시)

사용 유틸:
- useAuthStore from '@/stores/authStore'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/body-composition` 최초 진입

### 필수 데이터
- `bodyComposition` JOIN `members` fetch 중

### 인터랙션 (User Actions)
- 자동 전환

### 비즈니스 룰
- branchId 기준 필터링

### 에지 케이스
- fetch 실패 → 에러 토스트

### 접근성 (A11y)
- `aria-busy="true"`

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본-목록`
