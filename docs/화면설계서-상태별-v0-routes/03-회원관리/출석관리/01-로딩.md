# SCR-ATT 출석 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-ATT |
| 상태 코드 | `attendance-loading` |
| 경로 | `/attendance` |
| 역할 | primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 > 출석 관리 클릭 |
| 다음 상태 | `02-기본-오늘` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-ATT 출석 관리 — 상태: 로딩 (스켈레톤)

파일: src/app/attendance/page.tsx

레이아웃:
- <AppLayout> 데스크톱
- <PageHeader title="출석 관리" subtitle="회원 출석 현황을 관리합니다." />
- 통계 카드 + 날짜 헤더 + 출석 리스트 스켈레톤

컴포넌트 구조:
1. [통계 카드 스켈레톤] — 3개 (오늘 출석 / 전체 대비 / 미출석)
   animate-pulse bg-gray-100 h-24 rounded-lg
2. [날짜 선택 바 스켈레톤] — h-10 w-full bg-gray-100 animate-pulse
3. [출석 리스트 스켈레톤] — 10행 × (아바타 + 이름 + 시간 + 방법)

데이터:
- isLoading: true
- selectedDate: today (기본값)

인터랙션:
- 로딩 중 비활성
- 데이터 수신 → `02-기본-오늘`

사용 유틸:
- useAuthStore from '@/stores/authStore'
- format(today, 'yyyy-MM-dd') from 'date-fns'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/attendance` 최초 진입
- 브라우저 새로고침

### 필수 데이터
- selectedDate: today (기본값)
- `attendance` 테이블 오늘 날짜 기준 조회 시작

### 인터랙션 (User Actions)
- 자동 전환 (데이터 수신 후 `02-기본-오늘`)

### 비즈니스 룰
- 기본 날짜: 오늘 (서버 시각 기준)

### 에지 케이스
- 네트워크 오류 → 에러 토스트 + 빈 상태

### 접근성 (A11y)
- `aria-busy="true"` + `aria-label="출석 관리 로딩 중"`

### 연결 화면
- 이전: 사이드바 메뉴
- 다음: `02-기본-오늘`
