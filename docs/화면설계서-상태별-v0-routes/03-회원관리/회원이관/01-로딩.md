# SCR-M005 회원 이관 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M005 |
| 상태 코드 | `transfer-loading` |
| 경로 | `/members/transfer?id={memberId}` |
| 역할 | primary / owner |
| 우선순위 | P1 |
| 이전 상태 | 회원 상세 [이관] 버튼 클릭 |
| 다음 상태 | `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M005 회원 이관 — 상태: 로딩

파일: src/app/members/transfer/page.tsx

레이아웃:
- <AppLayout> 데스크톱
- <PageHeader title="회원 이관" subtitle="회원을 다른 지점으로 이관합니다." />
- 폼 영역 스켈레톤

컴포넌트 구조:
1. [회원 정보 카드 스켈레톤]
   - 아바타 w-12 h-12 + 이름 w-24 h-5 + 상태 w-16 h-4 animate-pulse
2. [이관 폼 스켈레톤]
   - 레이블 + Select 스켈레톤 3개 (현재 지점 / 대상 지점 / 이관 사유)
3. [버튼 행 스켈레톤]
   - 취소 w-20 h-9 + 이관 w-24 h-9 animate-pulse

데이터:
- memberId: searchParams.get('id')
- isLoading: true (회원 정보 + 브랜치 목록 fetch 중)

인터랙션:
- 로딩 중 비활성
- owner 아닌 역할 → `/forbidden`

사용 유틸:
- useSearchParams from 'next/navigation'
- hasPermission from '@/lib/permissions'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/members/transfer?id={memberId}` 진입 직후
- 회원 정보 + 브랜치 목록 fetch 중

### 필수 데이터
- memberId (URL searchParams)
- `members` 단건 + `branches` 목록 동시 fetch

### 인터랙션 (User Actions)
- 자동 전환 (데이터 로드 완료 시 `02-기본`)

### 비즈니스 룰
- owner/primary만 이관 가능 → fc/manager는 `/forbidden`

### 에지 케이스
- 브랜치 목록 fetch 실패 → 에러 토스트 + 재시도

### 접근성 (A11y)
- `aria-busy="true"` + `aria-label="이관 화면 로딩 중"`

### 연결 화면
- 이전: 회원 상세
- 다음: `02-기본`
