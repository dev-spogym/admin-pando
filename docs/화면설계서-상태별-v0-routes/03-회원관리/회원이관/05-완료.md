# SCR-M005 회원 이관 — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M005 |
| 상태 코드 | `transfer-success` |
| 경로 | `/members` (자동 이동) |
| 역할 | primary / owner |
| 우선순위 | P1 |
| 이전 상태 | `04-이관확인` (이관 실행 성공) |
| 다음 상태 | 회원 목록 `02-회원전체탭` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-M005 회원 이관 — 상태: 완료

파일: src/app/members/transfer/page.tsx (onSuccess 핸들러)

동작:
- members UPDATE + transfer_log INSERT 성공
- 성공 토스트 표시
- 회원 목록으로 자동 이동

컴포넌트 구조:
1. [성공 토스트] — toast.success
   - 메시지: "{member.name}님이 {targetBranch.name}으로 이관되었습니다."
   - duration: 4000ms
2. [자동 리다이렉트]
   - router.push('/members')
   - 이관된 회원은 현재 지점 목록에서 제거됨

데이터:
- 이관 성공 응답 (error === null)
- 이관 로그 id 반환

인터랙션:
- 자동 이동 (즉시)

사용 유틸:
- router.push from 'next/navigation'
- toast.success from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase members UPDATE + transfer_log INSERT 모두 성공

### 필수 데이터
- 성공 응답의 transferLog.id

### 인터랙션 (User Actions)
- 없음 (자동 이동)

### 비즈니스 룰
- 이관 완료 후 현재 지점 목록에서 해당 회원 즉시 제거
- 이관 이력은 member_transfer_logs에 영구 보존

### 에지 케이스
- 리다이렉트 중 에러 → Next.js 라우터가 처리

### 접근성 (A11y)
- 토스트 `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `04-이관확인`
- 다음: 회원 목록 `02-회원전체탭`
