# SCR-073 쿠폰 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-073 |
| 상태 코드 | `loading` |
| 경로 | `/message/coupon` |
| 역할 | 센터장 / 매니저 / FC |
| 우선순위 | P1 |
| 이전 상태 | 사이드바 마케팅 > 쿠폰 관리 |
| 다음 상태 | `02-쿠폰선택` (로딩 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-073 쿠폰 관리 — 상태: 로딩 (fetchCoupons 실행 중)

파일: src/app/(marketing)/message/coupon/page.tsx

로딩 조건:
- useState loading=false 초기값
- fetchCoupons() → setLoading(true) → supabase 조회 → setLoading(false)
- DataTable loading={loading} prop 전달

로딩 UI:
- DataTable 컴포넌트 내부 loading prop으로 처리 (스켈레톤 행)
- StatCardGrid 4개는 즉시 렌더 (coupons 배열이 빈 상태로 계산)
  - 전체 쿠폰: 0 / 활성 쿠폰: 0 / 총 발급 건수: 0 / 사용 완료: 0

fetchCoupons 함수:
- useCallback(async () => { ... }, [branchId])
- setLoading(true)
- supabase.from('coupons').select('*').eq('branchId', branchId).order('id', {ascending:false})
- !error && data → setCoupons(data.map(c => ({...c, value: Number(c.value)})))
- setLoading(false)

StatCardGrid (cols=4, className="mb-xl"):
- StatCard label="전체 쿠폰" value={coupons.length} icon=<Ticket /> variant="default"
- StatCard label="활성 쿠폰" value={enrichedCoupons.filter(c=>c.computedStatus==="active").length} icon=<Ticket /> variant="mint"
- StatCard label="총 발급 건수" value={coupons.reduce((acc,c)=>acc+(c.totalIssued??0),0)} icon=<Send /> variant="default"
- StatCard label="사용 완료" value={coupons.reduce((acc,c)=>acc+(c.totalUsed??0),0)} icon=<Gift /> variant="peach"

getBranchId import:
- import { getBranchId } from '@/lib/getBranchId'

데이터:
- loading: boolean (DataTable prop)
- coupons: any[] (초기 빈 배열)
- branchId: getBranchId() 호출

인터랙션:
- 로딩 중 사용자 액션 없음 (DataTable 스켈레톤)
- 로딩 완료 → coupons 데이터 렌더

사용 유틸:
- supabase from '@/lib/supabase'
- getBranchId from '@/lib/getBranchId'
- StatCard, StatCardGrid from '@/components/common'
- DataTable from '@/components/common/DataTable'
- Ticket, Send, Gift from 'lucide-react'
- useCallback from 'react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/message/coupon` 진입 시 (useEffect → fetchCoupons 실행)

### 필수 데이터
| 항목 | 소스 |
|------|------|
| coupons | `coupons` 테이블 (branchId 필터) |
| loading | fetchCoupons 실행 중 true |

### 인터랙션 (User Actions)
- 없음 (자동 전환)

### 비즈니스 룰
- loading=true 구간: DataTable 스켈레톤 행 표시
- StatCardGrid는 coupons=[] 기준으로 0 렌더 (빈 상태)

### 에지 케이스
- 네트워크 오류 → loading=false, coupons=[] → 빈 상태 테이블

### 접근성 (A11y)
- DataTable loading: `aria-busy="true"`

### 연결 화면
- 이전: 사이드바 마케팅
- 다음: `02-쿠폰선택` (로딩 완료)
