# SCR-074 마일리지 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-074 |
| 상태 코드 | `empty-state` |
| 경로 | `/mileage` |
| 역할 | 센터장 / 매니저 / FC |
| 우선순위 | P2 |
| 이전 상태 | `02-기본-회원별` (members 배열 빈 경우) |
| 다음 상태 | 회원 등록 후 자동 해소 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-074 마일리지 관리 — 상태: 빈 상태 (members 데이터 없음)

파일: src/app/(marketing)/mileage/page.tsx

빈 상태 조건:
- isLoading=false AND members.length === 0

빈 상태 UI (DataTable emptyMessage prop):
- DataTable 내부 emptyMessage 렌더
- 또는 별도 EmptyState 컴포넌트 구성 가능

DataTable emptyMessage 표시:
- columns=statusColumns, data=[] (빈 배열)
- DataTable 내부 로직: data.length===0 → 빈 상태 row 표시
- emptyMessage 기본값 없음 → DataTable 컴포넌트 기본 메시지 사용

StatCardGrid 빈 상태 숫자:
- 전체 발행 마일리지: 0 P
- 전체 사용 마일리지: 0 P
- 잔여 마일리지: 0 P
- 이번 달 적립: 0 P

summary 초기값 (INITIAL_SUMMARY):
- totalIssued: 0
- totalUsed: 0
- currentBalance: 0
- monthlyEarned: 0

마일리지 이력 탭 빈 상태:
- mileageLogs=[], filteredLogs=[]
- DataTable emptyMessage="이력 데이터 없음"

빈 상태 안내 문구 (향후 개선 가능):
- "등록된 회원이 없습니다."
- "회원을 등록하면 마일리지 관리를 시작할 수 있습니다."
- [회원 등록하기] 버튼 → moveToPage(회원등록 페이지)

데이터:
- members: [] (빈 배열)
- mileageLogs: [] (빈 배열)
- summary: INITIAL_SUMMARY (모두 0)

인터랙션:
- DataTable 빈 상태: 별도 액션 없음
- [엑셀 다운로드] → exportToExcel([], ...) → toast.success("0건 엑셀 다운로드 완료")

사용 유틸:
- DataTable from '@/components/common/DataTable'
- StatCardGrid, StatCard from '@/components/common'
- moveToPage from '@/internal' (향후 안내 버튼용)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- fetchMembers 완료 후 `members.length === 0`
- 센터에 등록된 회원 없음

### 필수 데이터
| 항목 | 소스 |
|------|------|
| members | `members` 테이블 결과 빈 배열 |
| summary | INITIAL_SUMMARY (모두 0) |

### 인터랙션 (User Actions)
- DataTable 빈 상태 메시지 확인
- 별도 액션 없음

### 비즈니스 룰
- members.length===0 → mileageLogs 조회 의미 없음 (members 의존 useEffect)
- summary 모두 0 → StatCardGrid 0 P 표시

### 에지 케이스
- branchId 잘못된 경우 → members=[] 동일 처리
- 검색 결과 없음과 구분 필요 (members=[] vs filteredMembers=[])

### 접근성 (A11y)
- 빈 상태 메시지: `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `02-기본-회원별` (자동)
- 다음: 회원 등록 후 `02-기본-회원별` (데이터 자동 표시)
