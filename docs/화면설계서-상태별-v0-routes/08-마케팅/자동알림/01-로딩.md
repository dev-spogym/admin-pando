# SCR-072 자동 알림 설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-072 |
| 상태 코드 | `loading` |
| 경로 | `/message/auto-alarm` |
| 역할 | 센터장 / 매니저 / FC |
| 우선순위 | P1 |
| 이전 상태 | `/message` 상세 설정 버튼 클릭 |
| 다음 상태 | `02-기본-템플릿목록` (로딩 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-072 자동 알림 설정 — 상태: 로딩 (localStorage 데이터 불러오는 중)

파일: src/app/(marketing)/message/auto-alarm/page.tsx

로딩 조건:
- useState loading=true 초기값
- useEffect → loadAlarmSettings() 호출 (localStorage 조회)
- 완료 후 setLoading(false)

로딩 UI (loading === true일 때 전체 페이지 반환):
<AppLayout>
  <div className="flex flex-col gap-xl animate-pulse">
    {/* PageHeader 스켈레톤 */}
    <div className="h-20 bg-surface rounded-xl border border-line" />

    {/* 상단 카드 3개 스켈레톤 */}
    <div className="grid grid-cols-1 md:grid-cols-3 gap-lg">
      {[1,2,3].map(i => (
        <div key={i} className="h-28 bg-surface rounded-xl border border-line" />
      ))}
    </div>

    {/* 규칙 카드 4개 스켈레톤 */}
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-md">
      {[1,2,3,4].map(i => (
        <div key={i} className="h-24 bg-surface rounded-xl border border-line" />
      ))}
    </div>
  </div>
</AppLayout>

loadAlarmSettings 함수:
- key = `settings_{branchId}_auto_alarm` (localStorage)
- JSON.parse(saved)?.rules 가 있으면 반환
- 없으면 null 반환 → INITIAL_RULES 사용

getBranchId 함수:
- typeof window === "undefined" → "1" 반환
- localStorage.getItem("branchId") || "1"

데이터:
- loading: boolean state (초기값 true)
- ALARM_SETTINGS_KEY = "auto_alarm"

인터랙션:
- 로딩 중 사용자 액션 없음
- 로딩 완료 → 02-기본-템플릿목록 상태로 자동 전환

사용 유틸:
- AppLayout from '@/components/layout/AppLayout'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/message/auto-alarm` 진입 시 (useEffect 최초 실행)
- `loadAlarmSettings()` 비동기 실행 완료 전까지

### 필수 데이터
| 항목 | 소스 |
|------|------|
| alarmSettings | localStorage (`settings_{branchId}_auto_alarm`) |
| loading | useState(true) 초기값 |

### 인터랙션 (User Actions)
- 없음 (자동 전환)

### 비즈니스 룰
- localStorage 조회 실패 시 INITIAL_RULES(13종) 기본값 사용
- JSON.parse 오류 시 try-catch로 기본값 폴백

### 에지 케이스
- localStorage 데이터 없음 → INITIAL_RULES 13종 기본값 표시
- JSON.parse 오류 → 기본값 폴백 (catch 블록)

### 접근성 (A11y)
- animate-pulse 스켈레톤: `aria-busy="true"` + `role="status"`

### 연결 화면
- 이전: `/message` [상세 설정] 버튼
- 다음: `02-기본-템플릿목록` (로딩 완료)
