# SCR-097 감사로그 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `audit-empty` |
| 경로 | `/audit-log` |
| 역할 | 슈퍼관리자 / 최고관리자 / 센터장 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` / `03-필터적용` |
| 다음 상태 | `02-기본-목록` (필터 초기화) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 상태: 빈상태 (조회 결과 0건)

파일: src/app/(admin)/audit-log/page.tsx

조건: !isLoading && logs.length === 0

SimpleTable 빈상태 처리:
- SimpleTable의 data=[] 시 자체 빈 메시지 표시
- 또는 조건부 렌더:

{!isLoading && logs.length === 0 && (
  <div className="flex flex-col items-center justify-center py-16 text-center">
    <div className="flex h-14 w-14 items-center justify-center rounded-full bg-surface-secondary mb-md">
      <Search className="w-6 h-6 text-content-tertiary" />
    </div>
    <p className="text-[14px] font-semibold text-content mb-xs">검색 결과가 없습니다</p>
    <p className="text-[12px] text-content-secondary mb-lg">
      필터 조건을 변경하거나 기간을 조정해보세요.
    </p>
    <button
      onClick={clearAllFilters}
      className="px-md py-sm rounded-lg bg-primary/10 text-primary text-[13px] font-medium hover:bg-primary hover:text-white transition-all"
    >
      필터 초기화
    </button>
  </div>
)}

테이블 상단:
- "전체 0건" 표시
- ExportButton: disabled (데이터 없음)

케이스 A — 필터 적용 결과 없음:
- 메시지: "검색 결과가 없습니다"
- CTA: "필터 초기화"

케이스 B — 기간 내 로그 없음 (필터 없이):
- 메시지: "해당 기간에 감사 로그가 없습니다"
- CTA: "기간 확장하기" → fromDate = 90일 전으로 변경

구분 로직:
const hasActiveFilters = !!(actionFilter || targetTypeFilter || branchFilter || userSearch
  || fromDate !== thirtyDaysAgoStr() || toDate !== todayStr());

const emptyMessage = hasActiveFilters
  ? "검색 결과가 없습니다"
  : "해당 기간에 감사 로그가 없습니다";

사용 유틸:
- Search from 'lucide-react'
- formatNumber from '@/lib/format'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- API 응답 `res.data.data.length === 0` 또는 클라이언트 필터 후 0건
- 신규 시스템 초기 (감사 로그 미생성)
- 매우 좁은 날짜 범위 + 특정 액션 필터

### 필수 데이터
- logs: [] (빈 배열)
- totalCount: 0
- hasActiveFilters: 현재 필터 적용 여부

### 인터랙션 (User Actions)
1. "필터 초기화" → clearAllFilters() → 재조회
2. "기간 확장하기" → fromDate 90일 전 설정 → 재조회
3. 날짜/필터 직접 수정 → 재조회

### 비즈니스 룰
- ExportButton disabled (빈 데이터 다운로드 방지)
- 페이지네이션 숨김 (totalPages=0)

### 에지 케이스
- 클라이언트 userSearch 필터로만 0건 → API에서는 데이터 있음 → 안내 메시지 적절히 조정

### 접근성 (A11y)
- 빈 상태 영역: `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `01-로딩` / `03-필터적용`
- 다음: `02-기본-목록` (필터 초기화 후)
