# SCR-094 브랜치목록 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `branch-list-empty` |
| 경로 | `/branches` |
| 역할 | 슈퍼관리자 / 최고관리자 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `04-추가모달` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 브랜치목록 — 상태: 빈 상태 (branches.length === 0)

파일: src/app/(admin)/branches/page.tsx

레이아웃:
- <AppLayout>
- <PageHeader title="지점 관리 (멀티지점)" description="전체 지점의 운영 현황을 통합 관리하고 지점 간 데이터 이동을 처리합니다."
    actions={
      <div className="flex gap-sm">
        <Button variant="outline" size="sm" icon={<ArrowLeftRight size={16} />} disabled>지점 이동 신청</Button>
        <Button variant="primary" size="sm" icon={<Plus size={16} />} onClick={() => setIsAddBranchOpen(true)}>신규 지점 등록</Button>
      </div>
    } />

TabNav (동일하게 렌더):
<TabNav className="mb-lg" tabs={[
  { key: 'list', label: '지점 목록', icon: Building2 },
  { key: 'integrated', label: '통합 현황', icon: BarChart3 },
  { key: 'history', label: '지점 간 이동', icon: ArrowLeftRight },
]} activeTab="list" onTabChange={setActiveTab} />

빈 상태 UI (branches.length === 0):
<div className="flex flex-col items-center justify-center py-[80px] text-center">
  <div className="w-[64px] h-[64px] bg-surface-secondary rounded-full flex items-center justify-center mb-lg">
    <Building2 size={32} className="text-content-secondary" />
  </div>
  <p className="text-base font-semibold text-content mb-xs">등록된 지점이 없습니다</p>
  <p className="text-sm text-content-secondary mb-lg">
    첫 번째 지점을 등록하여 멀티지점 관리를 시작하세요.
  </p>
  <Button
    variant="primary"
    size="sm"
    icon={<Plus size={16} />}
    onClick={() => setIsAddBranchOpen(true)}
  >
    신규 지점 등록
  </Button>
</div>

// StatCardGrid, SearchFilter, DataTable 모두 숨김 (branches.length === 0 조건)

사용 유틸:
- Building2, Plus, BarChart3, ArrowLeftRight from 'lucide-react'
- Button from '@/components/ui/Button'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `01-로딩` 완료 후 branches.length === 0

### 필수 데이터
| 항목 | 초기값 | 설명 |
|------|--------|------|
| branches | [] | 빈 배열 |

### 인터랙션 (User Actions)
1. "신규 지점 등록" 버튼 클릭 → `04-추가모달` 열기
2. "지점 이동 신청" 버튼: disabled (지점 없으므로)

### 비즈니스 룰
- branches.length === 0 → StatCardGrid / SearchFilter / DataTable 미표시
- "신규 지점 등록" 버튼은 항상 활성화 (첫 지점 등록 유도)

### 에지 케이스
- supabase 쿼리 실패 후 빈 배열 → 동일 빈 상태 표시 (에러 배너 별도 없음)

### 접근성 (A11y)
- 빈 상태 아이콘: `aria-hidden="true"`
- CTA 버튼: 명확한 레이블 "신규 지점 등록"

### 연결 화면
- 이전: `01-로딩`
- 다음: `04-추가모달`
