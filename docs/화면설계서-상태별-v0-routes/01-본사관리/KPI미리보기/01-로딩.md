# SCR-095 KPI미리보기 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-095 |
| 상태 코드 | `kpi-preview-loading` |
| 경로 | `/kpi-preview` |
| 역할 | 슈퍼관리자 / 최고관리자 / 지점장 / FC / 트레이너 / 운영 |
| 우선순위 | P1 |
| 이전 상태 | 메뉴 클릭 진입 |
| 다음 상태 | `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-095 KPI미리보기 — 상태: 로딩 (초기 렌더 / 보드 콘텐츠 준비 중)

파일: src/app/(admin)/kpi-preview/page.tsx

레이아웃:
- <AppLayout>
- <PageHeader title="KPI 센터" description="..." actions={...} />
- PageHeader 내부 TabNav + 날짜 뱃지

초기화:
const authUser = useAuthStore(s => s.user);
const isSuperAdmin = authUser?.isSuperAdmin ?? false;
const initialBoard = defaultBoardKey(authUser?.role ?? '', isSuperAdmin);
const [activeBoard, setActiveBoard] = useState<BoardKey>(initialBoard);

// boardContents는 정적 데이터 — Supabase 호출 없음
// 로딩 상태는 초기 렌더 순간(hydration 전) 또는 탭 전환 애니메이션 구간

역할별 초기 탭 결정:
function defaultBoardKey(role: string, isSuperAdmin: boolean): BoardKey {
  if (isSuperAdmin) return 'hq';
  const normalized = normalizeRole(role);
  if (normalized === 'fc') return 'fc';
  if (normalized === 'manager' || normalized === 'owner' || normalized === 'primary') return 'branch';
  if (normalized === 'staff') return 'trainer';
  return 'branch';
}

로딩 UI (초기 렌더):
- Hero 영역: 스켈레톤 (rounded-2xl bg-surface-secondary animate-pulse h-[200px])
- StatCardGrid cols={4}: 4개 StatCard loading={true}
- ActionQueue 영역: 스켈레톤 박스 4개
- RankingPanel / FunnelPanel: 스켈레톤

사용 유틸:
- useAuthStore from '@/stores/authStore'
- normalizeRole from '@/lib/permissions'
- Building2, LayoutDashboard, Users, Activity, BellRing from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/kpi-preview` 첫 진입 시 hydration / 초기 렌더 구간
- boardContents는 정적 데이터이므로 Supabase 호출 없이 즉시 렌더됨 (실질적 로딩 시간 거의 없음)

### 필수 데이터
| 항목 | 초기값 | 설명 |
|------|--------|------|
| activeBoard | defaultBoardKey(role) | 역할별 자동 결정 |
| authUser | useAuthStore | 역할 / isSuperAdmin / branchName |

### 인터랙션 (User Actions)
1. 자동으로 `02-기본` 전환 (정적 데이터 즉시 렌더)

### 비즈니스 룰
- boardContents: 정적 Mock 데이터 (Supabase 미사용)
- superAdmin → 'hq' 탭, fc → 'fc' 탭, manager/owner → 'branch', staff → 'trainer'

### 에지 케이스
- authUser null → defaultBoardKey('', false) → 'branch' 탭

### 접근성 (A11y)
- 스켈레톤: `aria-busy="true"` `aria-label="KPI 데이터 불러오는 중"`

### 연결 화면
- 이전: 사이드바 메뉴
- 다음: `02-기본`
