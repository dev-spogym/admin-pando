# SCR-097 온보딩 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `onboarding-loading` |
| 경로 | `/onboarding` |
| 역할 | 슈퍼관리자 / 최고관리자 / 지점장 |
| 우선순위 | P1 |
| 이전 상태 | 메뉴 클릭 진입 / 새로고침 버튼 클릭 |
| 다음 상태 | `02-Step1-프리온보딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 온보딩 — 상태: 로딩 (isLoading=true)

파일: src/app/(admin)/onboarding/page.tsx

레이아웃:
- <AppLayout>
- <PageHeader title="온보딩 대시보드"
    description="신규회원 정착률을 추적합니다. (프리-온보딩 → 신규 유치 → 신규 안정)"
    actions={
      <Button variant="outline" size="sm"
        icon={<RefreshCw size={14} className="animate-spin" />}
        onClick={fetchData}>새로고침</Button>
    } />

로딩 상태:
- 섹션 헤더 3개 (프리-온보딩 / 신규 유치 / 신규 안정): 스켈레톤
- StatCardGrid cols={5}: StatCard loading={true} ×5
- StatCardGrid cols={3}: StatCard loading={true} ×3
- StatCardGrid cols={5}: StatCard loading={true} ×5
- 온보딩 단계별 진행률 카드: 스켈레톤
- DataTable: loading={true}

초기 상태:
const [loading, setLoading] = useState(true);
const [stats, setStats] = useState<OnboardingStats>({
  newLeadsCount: 0, leadsContactedCount: 0, leadsVisitedCount: 0, leadsConvertedCount: 0,
  newMembersThisMonth: 0,
  day7ActiveCount: 0, day7TotalNew: 0,
  day30ActiveCount: 0, day30TotalNew: 0, day30ChurnCount: 0,
  ptTrialCount: 0, gxFirstCount: 0,
});
const [newMembers, setNewMembers] = useState<NewMemberRow[]>([]);
const branchId = getBranchId();

데이터 패치:
const fetchData = useCallback(async () => {
  setLoading(true);
  try {
    // 다중 병렬 쿼리 실행
    // leads / members / attendance / consultations / lesson_bookings
    ...
  } catch (err) {
    toast.error('온보딩 데이터를 불러오지 못했습니다.');
  } finally {
    setLoading(false);
  }
}, [branchId]);

useEffect(() => { fetchData(); }, [fetchData]);

사용 유틸:
- getBranchId from '@/lib/getBranchId'
- RefreshCw from 'lucide-react'
- Button from '@/components/ui/Button'
- supabase from '@/lib/supabase'
- toast from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/onboarding` 첫 진입
- PageHeader "새로고침" 버튼 클릭

### 필수 데이터
| 항목 | 초기값 | 설명 |
|------|--------|------|
| loading | true | 로딩 플래그 |
| stats | 모든 값 0 | OnboardingStats 초기값 |
| newMembers | [] | 신규 회원 목록 |
| branchId | getBranchId() | 현재 지점 ID |

### 인터랙션 (User Actions)
1. "새로고침": RefreshCw animate-spin 표시, 재호출 가능

### 비즈니스 룰
- 패치 순서: leads → members(이번달/30일/7일) → attendance → consultations → lesson_bookings
- 날짜 기준: today 기준 동적 계산 (monthStart/day7Ago/day30Ago/day60Ago)

### 에지 케이스
- 쿼리 실패 → toast.error + stats 초기값 유지
- branchId 없음 → getBranchId() 반환값으로 처리

### 접근성 (A11y)
- RefreshCw 스피너: `aria-label="데이터 불러오는 중"` `role="status"`
- DataTable loading: 스켈레톤 `aria-busy="true"`

### 연결 화면
- 이전: 사이드바 메뉴
- 다음: `02-Step1-프리온보딩` (성공)
