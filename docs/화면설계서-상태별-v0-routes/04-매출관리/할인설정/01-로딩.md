# SCR-DISC 할인 설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-DISC |
| 상태 코드 | `loading` |
| 경로 | `/discount-settings` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-기본` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-DISC 할인 설정 — 상태: 로딩

파일: src/app/discount-settings/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="할인 설정" subtitle="할인 정책을 관리하고 상품에 적용합니다." />
- <StatCardGrid cols={3}> — 스켈레톤 3개
- <DataTable isLoading={true}> — 스켈레톤 5행

컴포넌트 구조:
1. isLoading=true
2. StatCard 3개 skeleton (활성 정책 수 / 총 할인 적용 건수 / 평균 할인율)
3. DataTable 스켈레톤

데이터:
- interface DiscountPolicy {
    id, name, discountType: 'rate'|'fixed',
    discountRate, discountAmount,
    minAmount, maxDiscount, code,
    isActive, startDate, endDate,
    applicableProducts: string[],
    usageCount, branchId
  }
- 소스: supabase.from('discount_policies').select('*').eq('branchId', getBranchId()).order('createdAt', {ascending:false})

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Tag, Percent, BarChart2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/discount-settings` 페이지 최초 진입

### 필수 데이터
- `isLoading: true`
- `discountPolicies: []`

### 비즈니스 룰
- 할인 정책: `discount_policies` 테이블 (없으면 mock)

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본`
