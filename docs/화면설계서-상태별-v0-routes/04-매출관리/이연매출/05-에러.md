# SCR-S006 이연매출 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S006 |
| 상태 코드 | `error` |
| 경로 | `/deferred-revenue` |
| 역할 | 센터장 / 매니저 / 본사 |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (fetch 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S006 이연매출 — 상태: 데이터 로드 에러

파일: src/app/deferred-revenue/page.tsx

레이아웃:
- <AppLayout> + <PageHeader> 유지
- 메인 에러 UI:
  <div className="flex flex-col items-center justify-center py-24 gap-4 text-content-tertiary">
    <AlertCircle className="w-16 h-16 text-state-error" />
    <h3 className="text-lg font-semibold text-content-primary">데이터를 불러오지 못했습니다</h3>
    <p className="text-sm text-center max-w-sm">
      이연매출 데이터 조회 중 오류가 발생했습니다.<br/>
      deferred_revenue 테이블이 없거나 접근 권한이 없을 수 있습니다.
    </p>
    <Button variant="primary" onClick={handleRetry}>
      <RefreshCw className="w-4 h-4 mr-2" />
      다시 시도
    </Button>
  </div>

컴포넌트 구조:
1. hasError: boolean
2. errorMessage: string — 구체적 오류 메시지
3. fetchDeferredRevenue() catch:
   catch(error) {
     console.error('이연매출 로드 실패:', error)
     toast.error('이연매출 데이터를 불러오지 못했습니다.')
     setHasError(true)
     setErrorMessage(error?.message || '알 수 없는 오류')
   }
4. handleRetry(): setHasError(false) + fetchDeferredRevenue()

데이터:
- hasError: true
- 테이블 미존재 가능성 → 구체적 에러 메시지

인터랙션:
- [다시 시도] → handleRetry()

사용 유틸:
- toast from 'sonner'
- lucide-react: AlertCircle, RefreshCw
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `deferred_revenue` 테이블 없음 또는 네트워크 오류

### 필수 데이터
- `hasError: true`
- `errorMessage: string`

### 인터랙션 (User Actions)
1. [다시 시도] → `01-로딩` 재진입

### 비즈니스 룰
- `deferred_revenue` 테이블이 없는 경우 `sales` 기반 계산 폴백 고려
- 구체적 에러 메시지 표시 (개발 단계에서 디버깅 용이)

### 에지 케이스
- 권한 없음(RLS) → "접근 권한이 없습니다." 메시지

### 접근성 (A11y)
- `role="alert"` + `aria-live="assertive"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `01-로딩` (재시도)
