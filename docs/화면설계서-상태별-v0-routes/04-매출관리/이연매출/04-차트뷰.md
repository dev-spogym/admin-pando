# SCR-S006 이연매출 — 상태: 차트 뷰

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S006 |
| 상태 코드 | `chart-view` |
| 경로 | `/deferred-revenue` |
| 역할 | 센터장 / 매니저 / 본사 |
| 우선순위 | P2 |
| 이전 상태 | `02-기본` / `03-월별뷰` ([차트] 클릭) |
| 다음 상태 | `02-기본` / `03-월별뷰` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S006 이연매출 — 상태: 차트 뷰 (월별 인식/이연 추이)

파일: src/app/deferred-revenue/page.tsx

레이아웃:
- 뷰 전환: [계약별 목록] [월별 인식] [차트(active)]
- 차트 영역 (bg-white rounded-xl border p-6 space-y-6):
  [영역 1] 월별 인식 + 이연 스택 바차트:
  <ResponsiveContainer width="100%" height={280}>
    <BarChart data={monthlySchedule}>
      <XAxis dataKey="month" />
      <YAxis tickFormatter={v=>formatKRW(v)} />
      <Tooltip formatter={v=>formatKRW(v)} />
      <Legend />
      <Bar dataKey="monthlyTotal" name="월 인식액" fill="#3B82F6" stackId="a" />
      <Bar dataKey="remainingDeferred" name="이연 잔액" fill="#E5E7EB" stackId="a" />
    </BarChart>
  </ResponsiveContainer>
  [영역 2] 이연매출 잔액 추이 라인차트:
  <ResponsiveContainer width="100%" height={200}>
    <LineChart data={monthlySchedule}>
      <XAxis dataKey="month" />
      <YAxis tickFormatter={v=>formatKRW(v)} />
      <Tooltip formatter={v=>formatKRW(v)} />
      <Line dataKey="remainingDeferred" name="이연 잔액" stroke="#3B82F6" strokeWidth={2} dot={false} />
      <ReferenceLine x={currentMonth} stroke="#EF4444" strokeDasharray="3 3" label="현재" />
    </LineChart>
  </ResponsiveContainer>

컴포넌트 구조:
1. viewMode = 'chart'
2. monthlySchedule: 03-월별뷰와 동일 데이터 재사용
3. currentMonth: format(today, 'yyyy-MM')
4. ReferenceLine으로 현재 시점 표시

데이터:
- monthlySchedule: 03-월별뷰와 동일
- currentMonth: 현재 월 (ReferenceLine)

인터랙션:
- 차트 바/점 hover → Tooltip (월 + 인식액 + 이연잔액)
- 탭 전환 → 02-기본 / 03-월별뷰

사용 유틸:
- recharts: BarChart, Bar, LineChart, Line, XAxis, YAxis, Tooltip, Legend, ReferenceLine, ResponsiveContainer
- formatKRW from '@/lib/format'
- format from 'date-fns'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- [차트] 탭 클릭

### 필수 데이터
- `monthlySchedule` — 월별 인식/이연 데이터

### 인터랙션 (User Actions)
1. 차트 hover → Tooltip
2. 탭 전환 → `02-기본` / `03-월별뷰`

### 비즈니스 룰
- 스택 바: 인식액(파란) + 이연잔액(회색) = 전체 계약액
- 라인차트: 이연 잔액은 시간이 지날수록 감소
- ReferenceLine: 현재 시점 (빨간 점선)

### 에지 케이스
- 데이터 없음 → "차트 데이터가 없습니다." 빈 상태

### 접근성 (A11y)
- 차트 `role="img"` + `aria-label="이연매출 월별 추이 차트"`

### 연결 화면
- 이전: `02-기본` / `03-월별뷰`
- 다음: `02-기본` / `03-월별뷰`
