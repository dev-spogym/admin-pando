# SCR-S006 이연매출 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S006 |
| 상태 코드 | `loading` |
| 경로 | `/deferred-revenue` |
| 역할 | 센터장 / 매니저 / 본사 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-기본` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S006 이연매출 — 상태: 로딩

파일: src/app/deferred-revenue/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="이연매출 (선수익금)" subtitle="미래에 인식될 매출을 조회합니다." />
- <StatCardGrid cols={3}> — 스켈레톤 3개 (animate-pulse)
- <DataTable isLoading={true}> — 스켈레톤 5행

컴포넌트 구조:
1. isLoading=true 상태
2. StatCard 3개 skeleton: 총 이연매출 / 이번달 인식예정 / 계약 건수
3. DataTable 스켈레톤 (뷰 탭 포함)

데이터:
- interface DeferredItem {
    id, memberId, memberName, productName,
    contractStartDate, contractEndDate,
    totalAmount, recognizedAmount, deferredAmount,
    monthlyRecognition, remainingMonths, branchId
  }
- 소스: supabase.from('deferred_revenue').select('*').eq('branchId', getBranchId())
  (없으면: sales + contracts JOIN, salePrice 기간 안분 계산)
- isLoading: useState(true)

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: TrendingUp, Calendar, FileText
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/deferred-revenue` 페이지 최초 진입

### 필수 데이터
- `isLoading: true`
- `deferredItems: []`

### 비즈니스 룰
- 이연매출 = 선납된 금액 중 아직 인식되지 않은 부분
- 산식: 이연매출 = totalAmount - recognizedAmount
- recognizedAmount = (경과일수 / 전체일수) × totalAmount

### 에지 케이스
- fetch 실패 → `05-에러` 상태

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본` / `05-에러`
