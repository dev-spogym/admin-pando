# SCR-S007 환불 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S007 |
| 상태 코드 | `loading` |
| 경로 | `/refunds` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-대기탭` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S007 환불 관리 — 상태: 로딩

파일: src/app/refunds/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="환불 관리" subtitle="환불 신청을 검토하고 처리합니다." />
- <StatCardGrid cols={3}> — 스켈레톤 3개 (animate-pulse)
- <TabNav> 렌더링 유지 (클릭 비활성)
- <DataTable isLoading={true}> — 스켈레톤 5행

컴포넌트 구조:
1. isLoading=true: 스켈레톤 UI
2. StatCard 3개 skeleton (대기 건수 / 승인 건수 / 환불 총액)
3. TabNav: [대기(0)] [처리완료(0)] 탭 표시 (클릭 비활성)

데이터:
- interface RefundItem { id, memberId, memberName, productName, salePrice, refundAmount, reason, status, requestedAt, processedAt, processedBy }
- 소스: supabase.from('refunds').select('*').eq('branchId', getBranchId()).order('requestedAt', {ascending:false})
- isLoading: useState(true)

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: RotateCcw, CheckCircle, DollarSign
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/refunds` 페이지 최초 진입

### 필수 데이터
- `isLoading: true`
- `refunds: []`

### 인터랙션 (User Actions)
- 없음

### 비즈니스 룰
- 환불 데이터: `refunds` 테이블 (없으면 `sales.status='REFUNDED'` 기반)

### 에지 케이스
- fetch 실패 → toast.error + isLoading=false

### 접근성 (A11y)
- `aria-busy="true"`, `aria-label="환불 데이터 로딩 중"`

### 연결 화면
- 이전: 사이드바 진입
- 다음: `02-대기탭` (완료)
