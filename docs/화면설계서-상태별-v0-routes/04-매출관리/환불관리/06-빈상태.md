# SCR-S007 환불 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S007 |
| 상태 코드 | `empty` |
| 경로 | `/refunds` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (0건) / `02-대기탭` (대기 없음) |
| 다음 상태 | `02-대기탭` (환불 신청 접수 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S007 환불 관리 — 상태: 빈 상태

파일: src/app/refunds/page.tsx

레이아웃:
- 기본 레이아웃 유지 (PageHeader, StatCardGrid(0건), TabNav)
- DataTable 빈 상태 UI:
  <div className="flex flex-col items-center justify-center py-16 gap-3 text-content-tertiary">
    <RotateCcw className="w-12 h-12 text-gray-300" />
    {activeTab==='대기' ? (
      <>
        <p className="text-base font-medium">대기 중인 환불 신청이 없습니다.</p>
        <p className="text-sm">처리완료 탭에서 이전 환불 내역을 확인할 수 있습니다.</p>
      </>
    ) : (
      <>
        <p className="text-base font-medium">처리된 환불 내역이 없습니다.</p>
      </>
    )}
  </div>

컴포넌트 구조:
1. pendingRefunds.length===0 (대기 탭) → 빈 상태 UI
2. completedRefunds.length===0 (처리완료 탭) → 빈 상태 UI
3. 메시지: 탭별 다른 문구

데이터:
- pendingRefunds: [] 또는 completedRefunds: []

인터랙션:
- [처리완료] 탭 클릭 → 03-승인탭 (이전 내역 확인)

사용 유틸:
- lucide-react: RotateCcw
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 대기 탭에서 pendingRefunds.length === 0
- 처리완료 탭에서 completedRefunds.length === 0

### 필수 데이터
- 해당 탭의 데이터 배열이 빈 배열

### 인터랙션 (User Actions)
1. 탭 전환으로 다른 탭 확인
2. 환불 신청은 회원앱(client-pando)에서 진행

### 비즈니스 룰
- 환불 신청: 회원앱 or 매니저가 직접 등록
- 빈 상태는 정상 상태 (신청 없음)

### 에지 케이스
- 검색/필터 결과 0건: "검색 결과가 없습니다." (별도 메시지)

### 접근성 (A11y)
- `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `01-로딩` / `02-대기탭`
- 다음: `02-대기탭` (환불 접수 후)
