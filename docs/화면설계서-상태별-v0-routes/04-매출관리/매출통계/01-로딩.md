# SCR-S004 매출 통계 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S004 |
| 상태 코드 | `loading` |
| 경로 | `/sales/stats` |
| 역할 | 센터장 / 매니저 / 본사 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-기본-차트` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S004 매출 통계 — 상태: 로딩

파일: src/app/sales/stats/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="매출 통계" subtitle="기간별·브랜치별 매출 추이를 분석합니다." />
- <StatCardGrid cols={4}> — 스켈레톤 4개 (animate-pulse)
- 차트 영역 스켈레톤: h-64 bg-gray-100 rounded-xl animate-pulse
- 하단 DataTable 스켈레톤: 행 5개

컴포넌트 구조:
1. isLoading=true: StatCard 4개 skeleton 렌더링
2. 차트 컨테이너: 빈 div with animate-pulse (Recharts 로드 전)
3. DataTable isLoading={true}: 스켈레톤 행 5개

데이터:
- interface StatsSummary { totalRevenue, totalCount, avgRevenue, refundRate }
- interface MonthlyData { month: string, revenue: number, count: number, refund: number }
- 소스: supabase.from('sales').select('saleDate,salePrice,status,branchId').eq('branchId', getBranchId())
  + GROUP BY month (클라이언트 집계)
- isLoading: useState(true)

인터랙션:
- 없음 (로딩 중 비활성)

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: TrendingUp, BarChart2, RefreshCw, AlertCircle
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/sales/stats` 페이지 최초 진입
- 기간 변경 후 데이터 재조회 중

### 필수 데이터
- `isLoading: boolean` — true 상태
- `statsData: MonthlyData[]` — 빈 배열 초기값

### 인터랙션 (User Actions)
- 없음

### 비즈니스 룰
- 차트 라이브러리(Recharts) 동적 import → 로딩 중 skeleton 표시
- 집계 쿼리: `sum(salePrice) group by month` (클라이언트 처리)

### 에지 케이스
- fetch 실패 → `06-에러` 상태로 전환

### 접근성 (A11y)
- `aria-busy="true"`, `aria-label="매출 통계 로딩 중"`

### 연결 화면
- 이전: 사이드바 진입
- 다음: `02-기본-차트` (완료) / `06-에러` (실패)
