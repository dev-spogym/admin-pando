# SCR-S005 매출 통계 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S005 |
| 상태 코드 | `empty` |
| 경로 | `/sales/statistics-management` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (0건) |
| 다음 상태 | `03-수정저장` (항목 추가 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S005 매출 통계 관리 — 상태: 빈 상태 (등록된 통계 항목 없음)

파일: src/app/sales/statistics-management/page.tsx

레이아웃:
- <AppLayout> + <PageHeader> 유지
- DataTable 영역 대신 빈 상태 UI:
  <div className="flex flex-col items-center justify-center py-20 gap-4 text-content-tertiary">
    <BarChart2 className="w-14 h-14 text-gray-300" />
    <h3 className="text-base font-semibold text-content-primary">등록된 통계 항목이 없습니다</h3>
    <p className="text-sm text-center">
      매출 통계에 표시할 항목을 추가해 주세요.
    </p>
    <Button variant="primary" onClick={handleAddNew}>
      + 통계 항목 추가
    </Button>
  </div>

컴포넌트 구조:
1. statItems.length === 0 && !isLoading 조건
2. 빈 상태 UI + [+ 통계 항목 추가] 버튼
3. [추가] 클릭 → showEditModal=true + editItem=null → 03-수정저장

데이터:
- statItems: [] (빈 배열)
- isLoading: false

인터랙션:
- [+ 통계 항목 추가] → 03-수정저장 (신규 추가 모달)

사용 유틸:
- lucide-react: BarChart2
- Button from '@/components/ui/Button'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 처음 센터 설정 시 통계 항목 미등록 상태
- 모든 항목 삭제 후

### 필수 데이터
- `statItems: []`
- `isLoading: false`

### 인터랙션 (User Actions)
1. [+ 통계 항목 추가] → `03-수정저장` 신규 추가 모달

### 비즈니스 룰
- 기본 통계 항목 자동 생성 기능 (향후): 이용권/PT/상품/기타 기본값

### 에지 케이스
- 필터 결과 0건 vs 전체 0건 구분: 필터 있으면 "필터 결과 없음" + 초기화, 전체 0건이면 빈 상태 UI

### 접근성 (A11y)
- `role="status"` + `aria-live="polite"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `02-기본` (항목 추가 후) / `03-수정저장`
