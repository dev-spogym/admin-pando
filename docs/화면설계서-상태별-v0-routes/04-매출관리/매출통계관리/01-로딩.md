# SCR-S005 매출 통계 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S005 |
| 상태 코드 | `loading` |
| 경로 | `/sales/statistics-management` |
| 역할 | 센터장 / 매니저 / 본사 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-기본` (로드 완료) / `05-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S005 매출 통계 관리 — 상태: 로딩

파일: src/app/sales/statistics-management/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="매출 통계 관리" subtitle="매출 통계 항목을 설정하고 관리합니다." />
- 스켈레톤 DataTable (행 5개, animate-pulse)
- 버튼 영역 disabled 처리

컴포넌트 구조:
1. isLoading=true: DataTable 스켈레톤 5행
2. PageHeader actions 버튼 disabled
3. 필터 영역 skeleton (h-10 w-full bg-gray-100 rounded-lg animate-pulse)

데이터:
- interface StatManagementItem { id, name, type, value, period, isActive, updatedAt, updatedBy }
- 소스: supabase.from('sales').select('...') — 집계 데이터 기반 통계 항목
- isLoading: useState(true)

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: BarChart2
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/sales/statistics-management` 페이지 최초 진입

### 필수 데이터
- `isLoading: boolean` — true
- `statItems: StatManagementItem[]` — [] 초기값

### 인터랙션 (User Actions)
- 없음

### 비즈니스 룰
- 통계 항목: 매출 집계 기준 설정 (기간/유형/브랜치별)

### 에지 케이스
- fetch 실패 → toast.error + isLoading=false

### 접근성 (A11y)
- `aria-busy="true"`, skeleton `aria-hidden="true"`

### 연결 화면
- 이전: 사이드바 진입
- 다음: `02-기본` (성공) / `05-빈상태` (0건)
