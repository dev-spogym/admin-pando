# SCR-S008 미수금 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S008 |
| 상태 코드 | `empty` |
| 경로 | `/unpaid` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (0건) / `04-수금처리` (전액 수금 완료 후) |
| 다음 상태 | 해당 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S008 미수금 — 상태: 빈 상태 (미수금 없음)

파일: src/app/unpaid/page.tsx

레이아웃:
- 기본 레이아웃 유지 (PageHeader, StatCardGrid 모두 ₩0/0건)
- DataTable 빈 상태:
  <div className="flex flex-col items-center justify-center py-20 gap-4 text-content-tertiary">
    <div className="w-16 h-16 rounded-full bg-state-success/10 flex items-center justify-center">
      <CheckCircle className="w-10 h-10 text-state-success" />
    </div>
    <h3 className="text-base font-semibold text-content-primary">미수금이 없습니다!</h3>
    <p className="text-sm text-center">
      모든 결제가 정상적으로 완료되었습니다.
    </p>
  </div>

컴포넌트 구조:
1. unpaidItems.length === 0 && !isLoading
2. 성공 아이콘 + 긍정적 메시지 (미수금 없음은 좋은 상태)

데이터:
- unpaidItems: [] (빈 배열)
- totalUnpaid: 0

인터랙션:
- 없음 (정상 완료 상태)

사용 유틸:
- lucide-react: CheckCircle
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `sales.unpaid > 0` 레코드가 없는 경우
- 모든 미수금 수금 완료 후

### 필수 데이터
- `unpaidItems: []`

### 인터랙션 (User Actions)
- 없음 (완료 상태)

### 비즈니스 룰
- 미수금 없음은 긍정적 상태 → CheckCircle (성공 아이콘) 사용
- 다른 빈 상태들과 달리 CTA 버튼 불필요

### 연결 화면
- 이전: `01-로딩` / `04-수금처리` 완료
- 다음: 해당 없음
