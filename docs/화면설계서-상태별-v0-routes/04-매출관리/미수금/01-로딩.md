# SCR-S008 미수금 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S008 |
| 상태 코드 | `loading` |
| 경로 | `/unpaid` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P1 |
| 이전 상태 | 페이지 진입 |
| 다음 상태 | `02-기본` (로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S008 미수금 — 상태: 로딩

파일: src/app/unpaid/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="미수금 관리" subtitle="미납된 금액을 조회하고 수금 처리합니다." />
- <StatCardGrid cols={3}> — 스켈레톤 3개
- <DataTable isLoading={true}> — 스켈레톤 5행

컴포넌트 구조:
1. isLoading=true 상태
2. StatCard 3개 skeleton (총 미수금 / 미수금 건수 / 평균 미수금)
3. DataTable 스켈레톤

데이터:
- interface UnpaidItem { id, memberId, memberName, productName, salePrice, unpaidAmount, saleDate, daysOverdue, branchId }
- 소스: supabase.from('sales').select('id,memberId,memberName,productName,salePrice,unpaid,saleDate,branchId').eq('branchId',getBranchId()).gt('unpaid',0).order('saleDate',{ascending:false})

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: AlertCircle, Users, TrendingDown
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/unpaid` 페이지 최초 진입

### 필수 데이터
- `isLoading: true`
- `unpaidItems: []`

### 비즈니스 룰
- 미수금 = sales.unpaid > 0 인 레코드

### 연결 화면
- 이전: 사이드바
- 다음: `02-기본`
