# SCR-S002 POS — 상태: 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S002 |
| 상태 코드 | `complete` |
| 경로 | `/pos` |
| 역할 | 센터장 / 매니저 / 트레이너 / 프론트 |
| 우선순위 | P0 |
| 이전 상태 | SCR-S003 POS 결제 완료 후 redirect |
| 다음 상태 | `01-기본-상품선택` (초기화) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S002 POS — 상태: 결제 완료 후 POS 초기화

파일: src/app/pos/page.tsx

레이아웃:
- 결제 완료 후 /pos로 redirect 시 쿼리파람 ?success=true 체크
- 성공 배너 (useEffect로 3초 표시 후 사라짐):
  {showSuccessBanner && (
    <div className="fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-state-success text-white px-6 py-3 rounded-full shadow-lg flex items-center gap-2 animate-fade-in">
      <CheckCircle className="w-5 h-5" />
      결제가 완료되었습니다!
    </div>
  )}
- 장바구니: 초기화 (빈 상태, 07-빈카트)
- sessionStorage 초기화: posCart, posBuyer, posDiscount, posTotal 제거
- 상품 그리드: 그대로 유지 (다음 판매 준비)

컴포넌트 구조:
1. useEffect on mount:
   const params = new URLSearchParams(window.location.search)
   if (params.get('success') === 'true') {
     setShowSuccessBanner(true)
     setCart([])
     setBuyer(null)
     setSelectedDiscount(null)
     sessionStorage.removeItem('posCart')
     sessionStorage.removeItem('posBuyer')
     sessionStorage.removeItem('posDiscount')
     sessionStorage.removeItem('posTotal')
     // URL 정리
     window.history.replaceState({}, '', '/pos')
     setTimeout(() => setShowSuccessBanner(false), 3000)
   }
2. showSuccessBanner: boolean

데이터:
- URL 쿼리: ?success=true (결제 완료 신호)
- cart: [] 초기화
- buyer: null 초기화

인터랙션:
- 성공 배너 3초 후 자동 사라짐
- 다음 상품 선택 가능 (빈 장바구니 상태)

사용 유틸:
- lucide-react: CheckCircle
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/pos/payment` 결제 성공 후 `/pos?success=true` redirect

### 필수 데이터
- URL 쿼리 `?success=true` — 결제 완료 신호
- sessionStorage 클리어

### 인터랙션 (User Actions)
1. 성공 배너 3초 표시 후 자동 소멸
2. 장바구니/구매자 초기화 → `01-기본-상품선택` 복귀
3. 즉시 다음 판매 시작 가능

### 비즈니스 룰
- URL에서 `?success=true` 파라미터 제거 (replaceState)
- sessionStorage 완전 초기화
- 상품 목록은 reload 없이 유지

### 에지 케이스
- 직접 `/pos?success=true` 접근 → 동일하게 초기화 처리 (무해)

### 접근성 (A11y)
- 성공 배너 `role="alert"` + `aria-live="assertive"`
- 3초 후 소멸 (자동 dismiss)

### 연결 화면
- 이전: SCR-S003 POS 결제 (`/pos/payment`) 완료
- 다음: `01-기본-상품선택` (다음 판매)
