# SCR-P001 상품 목록 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P001 |
| 상태 코드 | `empty` |
| 경로 | `/products` |
| 역할 | 센터장 / 매니저 / 트레이너 / 스태프 |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (상품 0건) 또는 `03-카테고리필터` / `04-검색` (결과 없음) |
| 다음 상태 | 상품 등록 → `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-P001 상품 목록 — 상태: 빈 상태 (상품 없음 또는 필터 결과 없음)

파일: src/app/products/page.tsx

두 가지 빈 상태 분기:

[A] 전체 상품 0건 (신규 센터):
- 테이블 영역 대신 중앙 빈 상태 블록:
  <div className="flex flex-col items-center justify-center py-20 text-center">
    <Package className="w-16 h-16 text-gray-300 mb-4" />
    <h3 className="text-lg font-semibold text-gray-700 mb-2">등록된 상품이 없습니다</h3>
    <p className="text-sm text-gray-500 mb-6">
      이용권, PT 수강권, GX 수업권 등 다양한 상품을 등록해보세요.
    </p>
    {canEdit && (
      <Button variant="primary" icon={Plus} onClick={() => openPanel('new')}>
        첫 상품 등록하기
      </Button>
    )}
  </div>

[B] 필터/검색 결과 0건:
- 테이블 영역 대신 중앙 빈 상태 블록:
  <div className="flex flex-col items-center justify-center py-20 text-center">
    <Search className="w-16 h-16 text-gray-300 mb-4" />
    <h3 className="text-lg font-semibold text-gray-700 mb-2">검색 결과가 없습니다</h3>
    <p className="text-sm text-gray-500 mb-6">
      다른 검색어나 필터 조건을 사용해보세요.
    </p>
    <Button variant="outline" onClick={resetFilters}>
      필터 초기화
    </Button>
  </div>

분기 조건:
- products.length === 0 → [A]
- products.length > 0 && filteredProducts.length === 0 → [B]

통계 카드:
- [A]: 모든 카드 0 표시
- [B]: 전체 기준 카드값 유지

사용 유틸:
- lucide-react: Package, Search, Plus
- hasFeature from '@/lib/permissions'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- [A] 신규 센터: DB에 해당 branchId 상품이 0건
- [B] 필터/검색 결과 0건

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 상품 목록 | `products` | `branchId = ?` → 0건 |

### 인터랙션 (User Actions)
1. [A] "첫 상품 등록하기" 클릭 → ProductDetailPanel 신규 모드
2. [B] "필터 초기화" 클릭 → 모든 필터 reset → `02-기본`

### 비즈니스 룰
- canEdit=false일 경우 [A]에서 등록 버튼 미노출
- [B]에서 통계 카드는 필터 무관하게 전체 기준 유지

### 에지 케이스
- 비활성 상품만 존재 시 → [B]가 아닌 [A]로 처리 불가, 비활성 포함 목록 표시
- 권한 없는 사용자 [A] → 안내 문구만 표시, 버튼 없음

### 접근성 (A11y)
- 빈 상태 컨테이너 `role="status"`, `aria-label="상품 없음"`

### 연결 화면
- 이전: `01-로딩` / `03-카테고리필터` / `04-검색`
- 다음: `02-기본` (등록 또는 필터 초기화 후)
