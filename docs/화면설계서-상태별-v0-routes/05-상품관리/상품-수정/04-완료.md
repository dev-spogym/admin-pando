# SCR-P003 상품 수정 — 상태: 수정 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P003 |
| 상태 코드 | `save-complete` |
| 경로 | `/products/edit` → `/products` |
| 역할 | 센터장 / 매니저 |
| 우선순위 | P0 |
| 이전 상태 | `03-저장중` (UPDATE 성공) |
| 다음 상태 | `SCR-P001 02-기본` (상품 목록) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-P003 상품 수정 — 상태: 수정 완료 후 목록으로 복귀

파일: src/app/products/edit/page.tsx

완료 플로우:
// UPDATE 성공 후
toast.success(`'${formData.name}' 상품이 수정되었습니다.`);
setIsSaving(false);
setIsDirty(false);
moveToPage('/products');

목록 복귀 시 (/products):
- 수정된 상품이 하이라이트 (bg-primary/5 border-l-2 border-primary, 2초)
- 목록 전체 re-fetch (최신 데이터 반영)
- toast는 /products 화면에서 유지

사용 유틸:
- moveToPage from '@/internal'
- toast from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase UPDATE 성공 응답 수신 직후
- 자동으로 `/products` 라우팅

### 필수 데이터
- 없음 (이동 후 SCR-P001 re-fetch)

### 인터랙션 (User Actions)
- 자동 이동 (사용자 조작 불필요)

### 비즈니스 룰
- isDirty=false 설정 후 이동 (beforeunload 방지)
- 목록에서 수정된 행 2초간 하이라이트

### 에지 케이스
- moveToPage 실패 → window.location.href fallback

### 접근성 (A11y)
- `aria-live="assertive"` 영역에 "상품이 수정되었습니다" 공지

### 연결 화면
- 이전: `03-저장중`
- 다음: `SCR-P001 02-기본`
