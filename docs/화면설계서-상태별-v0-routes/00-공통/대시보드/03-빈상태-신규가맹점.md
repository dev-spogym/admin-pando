# SCR-090 대시보드 — 상태: 빈상태 (신규 가맹점)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-090 |
| 상태 코드 | `dashboard-empty-new-branch` |
| 경로 | `/` |
| 역할 | 전체 역할 |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `02-기본-전체위젯` (회원 등록 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-090 대시보드 — 상태: 빈상태 (신규 가맹점, 데이터 0건)

파일: src/app/(dashboard)/page.tsx

조건: 로드 완료 후 stats.totalMembers === 0

레이아웃:
- <AppLayout> + <PageHeader title="대시보드" />
- StatCardGrid: 모든 카드 값 "0명" / "0원" 표시 (loading=false)

빈상태 처리:
1. StatCard 값은 0으로 표시 (스켈레톤 아님)
2. 운영 현황 섹션:
   - 회원 분포 → "생년월일 데이터가 없습니다" (ageDist.length === 0)
   - 주간 출석 → 모든 바 높이 4px (bg-surface-tertiary)
   - 매출 추이 → 모든 바 높이 4px

3. 빈상태 온보딩 배너 (totalMembers === 0일 때 조건부 렌더):
   <div className="mb-xl rounded-xl border border-primary/20 bg-primary-light p-xl text-center">
     <div className="flex justify-center mb-md">
       <Users size={40} className="text-primary opacity-60" />
     </div>
     <h2 className="text-[18px] font-bold text-content mb-sm">센터 운영을 시작해보세요!</h2>
     <p className="text-[13px] text-content-secondary mb-lg">
       아직 등록된 회원이 없습니다. 첫 번째 회원을 등록하고 운영을 시작하세요.
     </p>
     <div className="flex justify-center gap-sm">
       <Button variant="primary" onClick={() => moveToPage(986)}>회원 등록하기</Button>
       <Button variant="ghost" onClick={() => moveToPage(980)}>설정 가이드</Button>
     </div>
   </div>

4. 하단 위젯:
   - 생일자 → "오늘 생일자가 없습니다"
   - 미수금 → "미수금 내역이 없습니다"
   - 홀딩 → "홀딩 중인 회원이 없습니다"
   - 만료임박 → "만료 임박 회원이 없습니다"

데이터:
- stats.totalMembers === 0 판별 조건
- 나머지 모든 state 기본값(0/빈배열) 유지

사용 유틸:
- moveToPage(986) — 회원 등록
- moveToPage(980) — 설정
- Users from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 가맹점 첫 로그인 후 대시보드 진입
- `fetchDashboard()` 완료 후 `stats.totalMembers === 0`

### 필수 데이터
- stats.totalMembers: 0
- 나머지 모든 통계: 0 또는 빈배열

### 인터랙션 (User Actions)
1. "회원 등록하기" → moveToPage(986) 회원 등록
2. "설정 가이드" → moveToPage(980) 설정
3. 새로고침 버튼 → handleRefresh() 재조회

### 비즈니스 룰
- 빈상태 온보딩 배너는 totalMembers === 0인 경우에만 노출
- StatCard는 0 값 그대로 표시 (스켈레톤 X)
- 차트 바는 최소 높이 4px 표시 (height: '4px')

### 에지 케이스
- 회원 등록 완료 후 대시보드 복귀 → `02-기본-전체위젯`으로 전환
- 회원은 없지만 매출은 있는 경우 → 온보딩 배너 숨김 (totalMembers 기준)

### 접근성 (A11y)
- 온보딩 배너: `role="region"` + `aria-label="시작 가이드"`
- CTA 버튼: 명확한 텍스트 레이블

### 연결 화면
- 이전: `01-로딩`
- 다음: 회원 등록(986) / 설정(980) / 데이터 생성 후 `02-기본`
