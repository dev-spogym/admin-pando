# SCR-052 RFID 밴드/카드 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-052 |
| 상태 코드 | `loading` |
| 경로 | `/rfid` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본-기기목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-052 밴드/카드 관리 — 상태: 로딩

파일: src/app/(facilities)/rfid/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="밴드/카드 관리" subtitle="RFID 밴드/카드를 등록하고 회원과 매핑합니다." />
- StatCardGrid 스켈레톤 (3컬럼 animate-pulse)
- 검색/필터 바 스켈레톤
- DataTable 스켈레톤 (10컬럼 × 5행)

컴포넌트 구조:
1. PageHeader — 타이틀 (actions 비활성)
2. StatCardGrid 스켈레톤 3개
3. 검색 + 필터 스켈레톤 (h-9 bg-gray-200 rounded)
4. DataTable 스켈레톤 — 헤더 + 5행 animate-pulse

데이터:
- isLoading = true
- supabase.from('rfid_cards').select('*').eq('branch_id', branchId) 호출 중

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: CreditCard
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/rfid` 경로 진입 시 데이터 fetch 중

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. 없음

### 비즈니스 룰
- 스켈레톤 레이아웃은 실제 DataTable 컬럼 수와 동일

### 에지 케이스
- 타임아웃 → 에러 상태 안내

### 접근성 (A11y)
- `aria-busy="true"`, `<span className="sr-only">RFID 카드 목록 로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 > 밴드/카드 관리
- 다음: `02-기본-기기목록`
