# SCR-054 골프베이 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-054 |
| 상태 코드 | `loading` |
| 경로 | `/golf-bays` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본-베이현황도` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-054 골프베이 관리 — 상태: 로딩

파일: src/app/(facilities)/golf-bays/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="골프 타석 관리" subtitle="타석 현황 및 예약을 실시간으로 관리합니다." />
- StatCardGrid 스켈레톤 (4컬럼)
- 타석 그리드 스켈레톤 (4열 × N행 카드 블록 animate-pulse)
- 대기열 섹션 스켈레톤

컴포넌트 구조:
1. PageHeader — 제목/설명 표시
2. StatCardGrid 스켈레톤 — 4개 카드 회색 블록
3. 타석 그리드 스켈레톤 — 카드 4개 × 행 animate-pulse
4. 대기열 스켈레톤 — 리스트 3행 회색 줄

데이터:
- isLoading = true
- golf_bays 테이블 fetch 중

인터랙션:
- 없음 (로딩 중 모든 액션 비활성)

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Target 아이콘
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/golf-bays` 경로 최초 진입
- 데이터 fetch 중 (`isLoading === true`)

### 필수 데이터
- 없음 (fetch 중)

### 인터랙션 (User Actions)
1. 없음

### 비즈니스 룰
- 1분 간격 실시간 타이머는 로딩 완료 후 시작
- 스켈레톤 카드 크기는 실제 타석 카드와 동일 (레이아웃 시프트 방지)

### 에지 케이스
- 5초 이상 로딩 → 에러 상태로 전환 고려

### 접근성 (A11y)
- `aria-busy="true"` on main area
- `<span className="sr-only">타석 현황 로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 시설관리 > 골프 타석 클릭
- 다음: `02-기본-베이현황도`
