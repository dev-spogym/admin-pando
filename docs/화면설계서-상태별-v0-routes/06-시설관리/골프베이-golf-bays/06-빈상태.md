# SCR-054 골프베이 관리 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-054 |
| 상태 코드 | `empty` |
| 경로 | `/golf-bays` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 없음) |
| 다음 상태 | `02-기본-베이현황도` (타석 등록 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-054 골프베이 관리 — 상태: 빈상태 (타석 미등록)

파일: src/app/(facilities)/golf-bays/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="골프 타석 관리" subtitle="타석 현황 및 예약을 실시간으로 관리합니다."
    actions={[<Button onClick={openAddModal}>타석 추가</Button>]} />
- StatCardGrid — 모두 0
- 빈 상태 영역:
    Target 아이콘 (w-16 h-16 text-gray-300)
    "등록된 골프 타석이 없습니다." (text-lg text-gray-500)
    "타석을 추가하여 골프베이 관리를 시작하세요." (text-sm text-gray-400 mt-1)
    <Button variant="primary" onClick={openAddModal}>타석 추가하기</Button>

타석 추가 모달:
  - 타석 번호 Input(number)
  - 타석 유형 Select (일반/VIP)
  - 프로젝터 유무 Checkbox
  - [저장] → supabase.from('golf_bays').insert({ number, type, projectorOn: false, status: 'available', branchId })
  - toast.success(`${number}번 타석이 추가되었습니다.`)

데이터:
- golf_bays 테이블 데이터 없음 (length === 0)

인터랙션:
- [타석 추가하기] → 추가 모달 오픈
- 모달 [저장] → insert → toast → 02-기본-베이현황도 전환

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: Target, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `golf_bays` 테이블 데이터 0건
- 신규 지점 또는 골프베이 미운영 센터

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. [타석 추가하기] → 추가 모달 → 저장 → `02-기본-베이현황도`

### 비즈니스 룰
- 센터장/매니저만 타석 추가 가능
- 스태프는 CTA 버튼 숨김

### 에지 케이스
- 골프베이 미운영 센터: "이 지점은 골프베이를 운영하지 않습니다." 안내 (센터 설정에 따라)

### 접근성 (A11y)
- `role="status"` on empty state
- CTA 버튼 `aria-label="골프 타석 추가"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `02-기본-베이현황도`
