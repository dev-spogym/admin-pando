# SCR-050 락커 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-050 |
| 상태 코드 | `loading` |
| 경로 | `/locker` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본-그리드` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-050 락커 관리 — 상태: 로딩

파일: src/app/(facilities)/locker/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="락커 관리" subtitle="센터 내 모든 락커의 배정 현황 및 상태를 관리합니다." />
- StatCardGrid 스켈레톤 (4컬럼 animate-pulse)
- 구역 탭 스켈레톤
- 락커 그리드 스켈레톤 (4×8 셀 animate-pulse)

컴포넌트 구조:
1. PageHeader — 타이틀/설명 (actions 비활성)
2. StatCardGrid 스켈레톤 — 4개 카드 h-20 bg-gray-200 rounded animate-pulse
3. 구역 탭 스켈레톤 — 탭 3개 h-8 bg-gray-200
4. 락커 그리드 스켈레톤:
   grid grid-cols-8 gap-2
   32개 셀 각각 h-16 bg-gray-200 rounded animate-pulse

데이터:
- isLoading = true
- supabase.from('lockers').select('*').eq('branch_id', branchId) 호출 중

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Lock
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/locker` 경로 최초 진입
- `isLoading === true` 상태

### 필수 데이터
- 없음 (fetch 중)

### 인터랙션 (User Actions)
1. 없음 — 모든 버튼 비활성

### 비즈니스 룰
- 그리드 스켈레톤은 실제 셀 크기와 동일하게 유지 (레이아웃 시프트 방지)
- 타임아웃 5초 → 에러 상태 전환 고려

### 에지 케이스
- 네트워크 느린 환경: 로딩 인디케이터 지속 표시

### 접근성 (A11y)
- `aria-busy="true"` on main
- `<span className="sr-only">락커 현황 로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 시설관리 > 락커 관리
- 다음: `02-기본-그리드`
