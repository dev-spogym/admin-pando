# SCR-053 운동룸 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-053 |
| 상태 코드 | `loading` |
| 경로 | `/rooms` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본-룸목록` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-053 운동룸 관리 — 상태: 로딩

파일: src/app/(facilities)/rooms/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="운동룸 관리" subtitle="센터 내 운동룸 현황과 예약 슬롯을 관리합니다." />
- StatCardGrid 스켈레톤 (4컬럼 animate-pulse)
- 뷰 전환 탭 스켈레톤
- 룸 카드 그리드 스켈레톤 (3컬럼 × 2행 카드 animate-pulse)

컴포넌트 구조:
1. PageHeader — 타이틀 (actions 비활성)
2. StatCardGrid 스켈레톤 4개 카드
3. 뷰 전환 + 필터 스켈레톤 (h-9 bg-gray-200)
4. 룸 카드 스켈레톤:
   grid grid-cols-3 gap-4
   6개 카드 각각 h-40 bg-gray-200 rounded-lg animate-pulse

데이터:
- isLoading = true
- supabase.from('rooms').select('*').eq('branch_id', branchId) 호출 중

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: DoorOpen
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/rooms` 경로 진입 시 데이터 fetch 중

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. 없음

### 비즈니스 룰
- 카드 스켈레톤 크기는 실제 RoomCard와 동일

### 에지 케이스
- 타임아웃 → 에러 안내

### 접근성 (A11y)
- `aria-busy="true"`, `<span className="sr-only">운동룸 목록 로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 > 운동룸 관리
- 다음: `02-기본-룸목록`
