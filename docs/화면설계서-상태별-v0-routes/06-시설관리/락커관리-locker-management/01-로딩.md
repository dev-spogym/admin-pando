# SCR-051 락커 관리(사물함 배정) — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `loading` |
| 경로 | `/locker/management` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함 배정 관리 — 상태: 로딩

파일: src/app/(facilities)/locker/management/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="사물함 배정 관리" subtitle="사물함 배정 및 현황을 관리합니다." />
- StatCardGrid 스켈레톤 (5컬럼)
- 탭 스켈레톤 (일일/개인/골프)
- 배정 영역 스켈레톤
- 그리드 스켈레톤

컴포넌트 구조:
1. PageHeader — 타이틀 (actions 비활성)
2. StatCardGrid 스켈레톤 5개 카드 animate-pulse
3. 탭 스켈레톤 3개
4. 배정 폼 영역 스켈레톤 (h-20 bg-gray-200 rounded)
5. 그리드 스켈레톤 (inline flex-wrap gap-1, 30개 사각형 animate-pulse)

데이터:
- isLoading = true
- supabase.from('lockers').select('*').eq('branch_id', branchId) 호출 중

인터랙션:
- 없음

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Lock
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/locker/management` 진입 시 데이터 fetch 중

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. 없음

### 비즈니스 룰
- 스켈레톤은 실제 레이아웃과 동일 구조로 구성

### 에지 케이스
- 타임아웃 → 에러 안내

### 접근성 (A11y)
- `aria-busy="true"`, `<span className="sr-only">사물함 배정 현황 로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 > 사물함 배정
- 다음: `02-기본`
