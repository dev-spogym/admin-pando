# SCR-051 락커 관리(사물함 배정) — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `empty` |
| 경로 | `/locker/management` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 없음) |
| 다음 상태 | `02-기본` (락커 추가 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함 배정 관리 — 상태: 빈상태

파일: src/app/(facilities)/locker/management/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="사물함 배정 관리"
    actions={[<Button onClick={openAddModal}><Plus /> 락커 추가</Button>]} />
- StatCardGrid — 5개 카드 모두 0
- TabNav — 탭 표시 (비활성)
- 빈 상태 영역:
    Lock 아이콘 (w-16 h-16 text-gray-300)
    "등록된 사물함이 없습니다." (text-lg text-gray-500)
    "락커를 추가하여 사물함 배정을 시작하세요." (text-sm text-gray-400)
    <Button variant="primary" onClick={openAddModal}><Plus /> 락커 추가하기</Button>

락커 추가 모달:
  <Modal title="락커 추가">
    락커 번호 Input(number, min=1) *
    구역 Select (A/B/C) *
    유형 Select (일일/개인/골프) *
    [저장] → supabase.from('lockers').insert({
      number, zone, type, status: 'AVAILABLE', branchId
    })
    toast.success(`${number}번 락커가 추가되었습니다.`)
  </Modal>

데이터:
  - lockers.length === 0

인터랙션:
  - [락커 추가하기] → 추가 모달 → 저장 → 02-기본 전환

사용 유틸:
  - useAuthStore, supabase, toast
  - lucide-react: Lock, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `lockers` 테이블 데이터 0건 (신규 지점)
- 로딩 완료 후 `lockers.length === 0`

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
1. [락커 추가하기] → 추가 모달 → 저장 → `02-기본`

### 비즈니스 룰
- 센터장/매니저만 락커 추가 가능
- 스태프는 안내 텍스트만 (버튼 hidden)

### 에지 케이스
- 탭별(일일/개인/골프) 모두 0건이어야 빈상태 표시

### 접근성 (A11y)
- `role="status"` on empty state
- CTA `aria-label="사물함 추가"`

### 연결 화면
- 이전: `01-로딩`
- 다음: `02-기본`
