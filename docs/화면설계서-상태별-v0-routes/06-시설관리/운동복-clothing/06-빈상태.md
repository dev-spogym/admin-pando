# SCR-055 운동복 관리 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-055 |
| 상태 코드 | `empty` |
| 경로 | `/clothing` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (데이터 없음) |
| 다음 상태 | `02-기본-재고목록` (재고 등록 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-055 운동복 관리 — 상태: 빈상태 (재고 미등록)

파일: src/app/(facilities)/clothing/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃
- <PageHeader title="운동복 관리" subtitle="운동복 재고 및 대여/반납 현황을 관리합니다."
    actions={[<Button onClick={openAddModal}>재고 초기화</Button>]} />
- StatCardGrid (4컬럼) — 모두 0 표시
- 빈 상태 영역 (중앙 정렬):
    Shirt 아이콘 (w-16 h-16 text-gray-300)
    "등록된 운동복 재고가 없습니다." (text-lg text-gray-500)
    "재고를 초기화하여 운동복 관리를 시작하세요." (text-sm text-gray-400)
    <Button variant="primary" onClick={openInitModal}>재고 초기화하기</Button>

컴포넌트 구조:
1. PageHeader — [재고 초기화] 버튼
2. StatCardGrid — 4개 카드 모두 값 0
3. EmptyState 블록 (min-h-[400px] flex flex-col items-center justify-center):
   - Shirt 아이콘 (크게)
   - 안내 텍스트
   - [재고 초기화하기] CTA 버튼

재고 초기화 모달:
  - 품목 선택 (상의/하의/수건 복수 선택)
  - 각 품목별 사이즈(XS~XXL)와 초기 수량 입력
  - 일일 한도 설정
  - [저장] → supabase.from('clothing').insert(items)
  - toast.success("운동복 재고가 초기화되었습니다.")

데이터:
- clothing 테이블 데이터 없음 (length === 0)

인터랙션:
- [재고 초기화하기] → 초기화 모달 오픈
- 모달 [저장] → insert → toast → 02-기본-재고목록으로 전환

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- toast from 'sonner'
- lucide-react: Shirt, Plus
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `clothing` 테이블 데이터 0건 (신규 지점 또는 미등록)
- 로딩 완료 후 `clothingItems.length === 0`

### 필수 데이터
- 없음 (빈 상태)

### 인터랙션 (User Actions)
1. [재고 초기화하기] → 초기 재고 등록 모달 오픈
2. 모달 [저장] → insert 후 `02-기본-재고목록` 전환

### 비즈니스 룰
- 센터장/매니저만 재고 초기화 가능
- 스태프는 안내 텍스트만 표시 (버튼 숨김 또는 disabled)

### 에지 케이스
- 필터 적용 결과 0건: 이 빈상태 대신 "검색 결과가 없습니다." 별도 메시지 사용

### 접근성 (A11y)
- `role="status"` on empty state container
- `aria-label="운동복 재고 없음"` on icon

### 연결 화면
- 이전: `01-로딩`
- 다음: `02-기본-재고목록` (재고 등록 완료)
