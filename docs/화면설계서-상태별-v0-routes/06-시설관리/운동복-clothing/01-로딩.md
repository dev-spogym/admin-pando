# SCR-055 운동복 관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-055 |
| 상태 코드 | `loading` |
| 경로 | `/clothing` |
| 역할 | 센터장 / 매니저 / 스태프 |
| 우선순위 | P0 |
| 이전 상태 | 사이드바 메뉴 클릭 |
| 다음 상태 | `02-기본-재고목록` (데이터 로드 완료) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-055 운동복 관리 — 상태: 로딩

파일: src/app/(facilities)/clothing/page.tsx

레이아웃:
- <AppLayout> 데스크톱 레이아웃 (사이드바 + 헤더)
- <PageHeader title="운동복 관리" subtitle="운동복 재고 및 대여/반납 현황을 관리합니다." />
- StatCardGrid 스켈레톤 4컬럼
- 탭 스켈레톤 (재고 현황 / 대여 이력)
- DataTable 스켈레톤 (7컬럼 × 5행)

컴포넌트 구조:
1. PageHeader — 타이틀 + 서브타이틀 표시 (actions 비활성)
2. StatCardGrid 스켈레톤 — 4개 카드 animate-pulse gray 블록
3. TabNav 스켈레톤 — 탭 2개 회색 블록
4. DataTable 스켈레톤 — 헤더 + 5행 회색 줄

데이터:
- isLoading = true
- supabase.from('clothing').select('*').eq('branch_id', branchId) 호출 중

인터랙션:
- 없음 (로딩 중 모든 인터랙션 비활성)

사용 유틸:
- useAuthStore from '@/stores/authStore'
- supabase from '@/lib/supabase'
- lucide-react: Shirt 아이콘
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/clothing` 경로 최초 진입 시
- Supabase `clothing` 테이블 데이터 fetch 중 (`isLoading === true`)

### 필수 데이터
- 없음 (로딩 중)

### 인터랙션 (User Actions)
1. 없음 — 모든 버튼/탭 비활성 상태

### 비즈니스 룰
- 로딩 중 PageHeader actions 버튼 disabled 처리
- 스켈레톤 animation: `animate-pulse bg-gray-200 rounded`

### 에지 케이스
- 3초 이상 로딩 시 → 네트워크 에러 상태로 전환 (타임아웃 처리)

### 접근성 (A11y)
- `aria-busy="true"` on main content area
- 스크린리더용 `<span className="sr-only">로딩 중...</span>`

### 연결 화면
- 이전: 사이드바 시설관리 > 운동복 클릭
- 다음: `02-기본-재고목록` (fetch 완료)
