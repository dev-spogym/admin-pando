# SCR-P008 시즌 가격 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P008 |
| 상태 코드 | `empty` |
| 경로 | `/products/seasonal-prices` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-P016 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P008 시즌 가격 관리 — 상태: 빈 상태

파일: src/app/products/seasonal-prices/page.tsx (seasonalPrices.length === 0)

레이아웃:
- AppLayout + PageHeader (우측: "시즌 추가" 버튼)
- 빈 상태:
  <div className="flex flex-col items-center justify-center py-32">
    <Calendar className="w-16 h-16 text-gray-300 mb-4" />
    <p className="text-lg font-semibold text-gray-500">등록된 시즌 가격 규칙이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">시즌별 특별 가격을 설정해보세요</p>
    <Button onClick={openAddModal} className="mt-6">시즌 추가</Button>
  </div>

아이콘: Calendar (lucide-react)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `seasonal_prices.length === 0`

### 인터랙션 (User Actions)
1. "시즌 추가" → DLG-P016

### 연결 화면
- DLG-P016
