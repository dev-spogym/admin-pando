# SCR-P008 시즌 가격 관리 — 상태: 기간 충돌 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P008 |
| 상태 코드 | `conflict-warning` |
| 경로 | `/products/seasonal-prices` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (기간 겹치는 규칙 존재) |
| 다음 상태 | 충돌 규칙 수정 → DLG-P016 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P008 시즌 가격 관리 — 상태: 기간 충돌 경고

파일: src/app/products/seasonal-prices/page.tsx (기간 충돌 규칙 있음)

구현:
충돌 규칙 행:
  <tr className="bg-yellow-50">

충돌 배지:
  <span className="inline-flex items-center gap-1 text-xs text-yellow-700 bg-yellow-100 px-2 py-0.5 rounded">
    <AlertTriangle className="w-3 h-3" /> 충돌
  </span>

상단 경고 배너:
  <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-3 mb-4">
    <p className="text-sm text-yellow-700 font-semibold">기간이 겹치는 시즌 가격 규칙이 있습니다. 확인 후 수정하세요.</p>
  </div>

충돌 감지 로직:
  규칙 A.start <= 규칙 B.end && 규칙 A.end >= 규칙 B.start 이면 충돌

아이콘: AlertTriangle (lucide-react)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 기간이 겹치는 두 규칙 이상 존재

### 비즈니스 룰
- 충돌 행: 노란 배경 + 충돌 배지
- 상단 경고 배너 표시
- 충돌 규칙 수정 권장

### 연결 화면
- DLG-P016 (충돌 규칙 수정)
