# DLG-P003 상품 삭제 확인 — 상태: 오픈

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P003 |
| 상태 코드 | `open` |
| 트리거 | SCR-P001 행 "삭제" 클릭 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-삭제결과` / 취소 → 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P003 상품 삭제 확인 — 상태: 오픈

파일: src/components/product/ProductDeleteModal.tsx

레이아웃 (Modal w-[420px]):
- 모달 헤더: "상품 삭제" + X 닫기
- 본문:
  <div className="py-4">
    <p className="text-gray-700">
      <span className="font-semibold">{product.name}</span> 상품을 삭제하시겠습니까?
    </p>
    <p className="text-red-500 text-sm mt-2">삭제된 상품은 복구할 수 없습니다.</p>
  </div>
- 하단: "취소" | "삭제" (bg-red-600)

삭제 핸들러:
  const handleDelete = async () => {
    setDeleting(true)
    const { error } = await supabase
      .from('products')
      .delete()
      .eq('id', product.id)
    setDeleting(false)
    if (error) { /* 에러 처리 */ } else { /* 성공 처리 */ }
  }

사용 컴포넌트: Modal, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P001 상품 목록에서 행의 "삭제" 버튼 클릭

### 레이아웃 구성
- 모달 너비: 420px (확인 다이얼로그 표준)
- 상품명 강조 표시 (font-semibold)
- 비가역 작업 경고 문구 (red 색상)
- 삭제 버튼: 위험 액션 표시 (bg-red-600)

### 인터랙션 (User Actions)
1. "삭제" 클릭 → DELETE API → `02-삭제결과`
2. "취소" / X → 모달 닫기

### 비즈니스 룰
- 연결된 판매 이력 또는 할인 정책이 있는 경우 삭제 실패
- owner, manager만 접근 가능

### 연결 화면
- 삭제 성공/실패: `02-삭제결과`
- 취소: SCR-P001 복귀
