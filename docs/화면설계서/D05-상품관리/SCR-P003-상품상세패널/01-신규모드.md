# SCR-P003 상품 상세 패널 — 상태: 신규 모드

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P003 |
| 상태 코드 | `panel-new` |
| 경로 | SCR-P001 우측 패널 (신규) |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-P001 "상품 등록" → 패널 모드 |
| 다음 상태 | 저장 중 → `05-저장중` / 유효성 실패 → `06-유효성에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P003 상품 상세 패널 — 상태: 신규 모드

파일: src/components/products/ProductDetailPanel.tsx (mode="new")

레이아웃 (우측 슬라이드 패널 w-[45%]):
- 패널 헤더: "상품 등록" + X 닫기 버튼 + 배지 없음
- 폼 섹션 (스크롤 가능):
  - 상품명 Input (required)
  - 카테고리 Select
  - 상태 Select (활성/비활성)
  - 판매가 Input
  - 유형 Select
  - 설명 Textarea
- 패키지 섹션: packageOpen=false 기본값 (접힘)
- 하단 고정 버튼바: "취소" + "등록"

진입 시 resetForm() 호출
배지 없음 (신규이므로 상태 배지 미노출)

사용 컴포넌트: Button, Select
사용 유틸: supabase, toast, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P001에서 패널 방식으로 신규 등록 선택 시

### 필수 데이터
- `product_groups`: 카테고리 Select 옵션

### 인터랙션 (User Actions)
1. 폼 입력 후 "등록" → `05-저장중`
2. 취소 → 패널 닫기 (변경 있으면 DLG-P002)

### 비즈니스 룰
- 헤더: "상품 등록"
- 배지 없음 (신규)
- 패키지 섹션 기본 접힘

### 연결 화면
- 저장: SCR-P001 목록 갱신
- 취소: 패널 닫기
