# SCR-P003 상품 상세 패널 — 상태: 복사 모드

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P003 |
| 상태 코드 | `panel-copy` |
| 경로 | SCR-P001 우측 패널 (복사 모드) |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | SCR-P001 "복사" 액션 클릭 |
| 다음 상태 | 저장 → `05-저장중` (INSERT) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P003 상품 상세 패널 — 상태: 복사 모드

파일: src/components/products/ProductDetailPanel.tsx (mode="copy")

레이아웃:
- 패널 헤더: "상품 등록" (복사 모드도 신규 등록 헤더) + 배지 없음 + X 닫기
- 폼: 원본 상품 데이터 프리필 (복사본이므로 id 제외)
  상품명 자동 수정: "{원본명} (복사)" 로 초기 설정
- 안내 배너:
  <div className="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
    <p className="text-sm text-blue-700">기존 상품을 복사하여 새 상품을 등록합니다.</p>
  </div>
- 저장 시 INSERT (UPDATE 아님)

하단 버튼바: "취소" + "등록"

사용 컴포넌트: Button, Select
사용 유틸: supabase, toast, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P001 상품 행 컨텍스트 메뉴 "복사" 클릭

### 필수 데이터
- 원본 상품 데이터 (프리필용)

### 인터랙션 (User Actions)
1. 상품명 등 수정 후 "등록" → 신규 INSERT
2. 취소 → 패널 닫기

### 비즈니스 룰
- 헤더: "상품 등록" (신규와 동일)
- 저장 시 INSERT (원본 id 제외)
- 상품명에 " (복사)" 자동 접미사

### 연결 화면
- 저장 성공: SCR-P001 목록 갱신 (신규 상품 추가)
