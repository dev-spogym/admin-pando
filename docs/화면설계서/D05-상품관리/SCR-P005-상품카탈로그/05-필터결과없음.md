# SCR-P005 상품 카탈로그 — 상태: 필터 결과 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P005 |
| 상태 코드 | `filter-empty` |
| 경로 | `/products/catalog` |
| 역할 | owner, manager, trainer, front |
| 우선순위 | P2 |
| 이전 상태 | `02-정상` 에서 검색/필터 적용 |
| 다음 상태 | 검색 초기화 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P005 상품 카탈로그 — 상태: 필터 결과 없음

파일: src/app/products/catalog/page.tsx (filteredProducts.length === 0 && products.length > 0)

레이아웃:
- 통계 카드, 검색 필터 정상 표시
- 카드 그리드 대신:
  <div className="flex flex-col items-center py-20">
    <Search className="w-12 h-12 text-gray-300 mb-3" />
    <p className="text-gray-500">검색 결과가 없습니다</p>
    <button onClick={clearFilter} className="mt-3 text-blue-500 text-sm underline">
      필터 초기화
    </button>
  </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 검색어/필터 적용 결과 0건 (전체 데이터는 있음)

### 인터랙션 (User Actions)
1. "필터 초기화" → 검색 초기화 → `02-정상`

### 비즈니스 룰
- 빈 상태(`03-빈상태`)와 구분: 데이터는 있으나 필터 결과가 없는 경우

### 연결 화면
- 초기화: `02-정상`
