# SCR-P005 상품 카탈로그 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P005 |
| 상태 코드 | `normal` |
| 경로 | `/products/catalog` |
| 역할 | owner, manager, trainer, front |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 편집 → DLG-P011 / 설정 → DLG-P010 / 미리보기 → DLG-P009 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P005 상품 카탈로그 — 상태: 정상 (카드 그리드)

파일: src/app/products/catalog/page.tsx

레이아웃:
- AppLayout + PageHeader (제목: "상품 카탈로그",
  우측: "미리보기" 버튼, "카탈로그 설정" 버튼)
- 통계 카드 3개:
  공개 상품 수 / 비공개 수 / 총 상품 수
- 카드 그리드 (grid grid-cols-3 gap-4):
  각 카드:
    - 상품 이미지 (h-40 object-cover rounded-t-xl)
    - 상품명 (font-semibold)
    - 가격 (text-blue-600)
    - 공개 여부 토글 배지 (공개/비공개) → hasPermission 조건
    - 편집 버튼 → DLG-P011 → hasPermission 조건
  카드 위 drag handle (catalog_order 드래그 정렬)
- 검색 Input + 필터 Select

사용 컴포넌트: AppLayout, PageHeader, Button, StatusBadge
사용 유틸: supabase, useAuthStore, hasPermission, formatNumber
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `products.length > 0`

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 카탈로그 목록 | `products` JOIN `product_catalog` | `branch_id = ?`, `catalog_order` 정렬 |
| 통계 | `products` | 집계 |

### 인터랙션 (User Actions)
1. "미리보기" → DLG-P009
2. "카탈로그 설정" → DLG-P010
3. 카드 편집 버튼 → DLG-P011
4. 공개 토글 → 즉시 UPDATE
5. 드래그 정렬 → `catalog_order` UPDATE

### 비즈니스 룰
- trainer/front: 편집 버튼, 공개 토글 숨김 (`04-읽기전용`)
- 공개 상품만 회원 앱 카탈로그에 노출

### 연결 화면
- DLG-P009 (미리보기), DLG-P010 (설정), DLG-P011 (편집)
