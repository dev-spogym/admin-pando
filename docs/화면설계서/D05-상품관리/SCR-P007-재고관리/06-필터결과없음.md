# SCR-P007 재고 관리 — 상태: 필터 결과 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P007 |
| 상태 코드 | `filter-empty` |
| 경로 | `/products/inventory` |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `02-정상` 에서 검색/필터 적용 |
| 다음 상태 | 초기화 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P007 재고 관리 — 상태: 필터 결과 없음

파일: src/app/products/inventory/page.tsx (filtered.length === 0 && inventory.length > 0)

구현:
DataTable 대신:
  <div className="flex flex-col items-center py-20">
    <Search className="w-12 h-12 text-gray-300 mb-3" />
    <p className="text-gray-500">검색 결과가 없습니다</p>
    <button onClick={clearFilter} className="mt-3 text-blue-500 text-sm underline">
      필터 초기화
    </button>
  </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 검색/필터 결과 0건 (전체 데이터는 존재)

### 인터랙션 (User Actions)
1. "필터 초기화" → `02-정상`

### 연결 화면
- 초기화: `02-정상`
