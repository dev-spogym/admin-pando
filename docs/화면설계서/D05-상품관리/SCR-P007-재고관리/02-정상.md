# SCR-P007 재고 관리 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P007 |
| 상태 코드 | `normal` |
| 경로 | `/products/inventory` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 입고 → DLG-P012 / 출고 → DLG-P013 / 조정 → DLG-P014 / 이력 → DLG-P015 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P007 재고 관리 — 상태: 정상

파일: src/app/products/inventory/page.tsx

레이아웃:
- AppLayout + PageHeader (제목: "재고 관리",
  우측: "입고 등록" 버튼, "출고 등록" 버튼)
- SearchFilter: 상품명 검색 Input + 재고 상태 Select (전체/정상/부족/소진)
- DataTable:
  컬럼: 상품명 | 카테고리 | 현재 재고 | 최소 임계값 | 재고 상태 | 마지막 입고일 | 액션
  재고 상태 배지:
    재고 > 임계값: StatusBadge "정상" (green)
    0 < 재고 <= 임계값: StatusBadge "부족" (yellow)
    재고 === 0: StatusBadge "소진" (red)
  액션: "재고 조정" 버튼, "이력 보기" 버튼

데이터:
- inventory 테이블 (가상): product_id, current_stock, min_threshold, last_inbound_at
- products JOIN inventory

사용 컴포넌트: AppLayout, PageHeader, DataTable, SearchFilter, StatusBadge, Button
사용 유틸: supabase, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `inventory.length > 0`

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 재고 목록 | `inventory` JOIN `products` | `branch_id = ?` |

### 인터랙션 (User Actions)
1. "입고 등록" → DLG-P012
2. "출고 등록" → DLG-P013
3. "재고 조정" → DLG-P014
4. "이력 보기" → DLG-P015
5. 검색/필터 → 목록 갱신

### 비즈니스 룰
- 재고 상태 배지: 정상(green) / 부족(yellow) / 소진(red)
- 소진 상품 행: 조정 버튼 강조 (ring-red-300)

### 연결 화면
- DLG-P012, DLG-P013, DLG-P014, DLG-P015
- `04-재고부족배지`, `05-재고소진배지`
