# SCR-P006 상품 비교 — 상태: 읽기 전용

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P006 |
| 상태 코드 | `readonly` |
| 경로 | `/products/compare` |
| 역할 | trainer, front |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` (trainer/front) |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P006 상품 비교 — 상태: 읽기 전용 (trainer/front)

파일: src/app/products/compare/page.tsx (hasPermission('canSell')=false)

레이아웃:
- 비교 테이블 정상 표시
- "바로 구매" 버튼 없음 (hasPermission 조건)
- 조회 전용

구현:
const canSell = hasPermission('canSell') // false
// "바로 구매" 버튼 조건부 렌더링 제거
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- trainer, front 역할로 진입

### 인터랙션 (User Actions)
- 비교 조회만 가능

### 비즈니스 룰
- "바로 구매" 버튼 미노출

### 연결 화면
- 없음
