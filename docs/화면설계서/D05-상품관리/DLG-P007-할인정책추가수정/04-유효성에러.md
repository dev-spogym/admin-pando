# DLG-P007 할인 정책 추가/수정 — 상태: 유효성 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P007 |
| 상태 코드 | `validation-error` |
| 트리거 | 저장 클릭 시 클라이언트 유효성 실패 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-신규모드` 또는 `02-수정모드` |
| 다음 상태 | 수정 후 재시도 → `03-저장결과` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P007 할인 정책 — 상태: 유효성 에러

파일: src/components/discount/DiscountPolicyModal.tsx (validation 실패)

구현:
- 정책명 미입력: border-red-400 + "정책명을 입력하세요"
- 할인값 범위 오류: "0~100 사이 값을 입력하세요" (정률)
- 각 필드 하단 <p className="text-xs text-red-500">{error}</p>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 저장 버튼 클릭 시 클라이언트 유효성 검사 실패

### 비즈니스 룰
- API 호출 전 클라이언트 검증
- 에러 필드 빨간 테두리 + 인라인 에러 메시지

### 연결 화면
- 수정 후 재시도: `03-저장결과`
