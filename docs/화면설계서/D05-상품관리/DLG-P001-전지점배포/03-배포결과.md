# DLG-P001 전지점 배포 — 상태: 배포 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P001 |
| 상태 코드 | `deploy-result` |
| 트리거 | 배포 API 응답 수신 |
| 역할 | owner |
| 우선순위 | P0 |
| 이전 상태 | `02-지점선택` 에서 배포 확인 클릭 |
| 다음 상태 | 전체 성공 → 모달 닫힘 / 일부 실패 → 재시도 / 전체 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P001 전지점 배포 — 상태: 배포 결과 분기

파일: src/components/products/DeployAllBranchesModal.tsx (API 응답 후)

결과 분기:
1. 전체 성공:
   toast.success("전지점에 배포되었습니다.")
   onClose()

2. 일부 실패 (partial):
   모달 유지 + 경고 배너:
   <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-3">
     <p className="text-sm text-yellow-700">{failCount}개 지점 배포 실패</p>
     <button onClick={retryFailed} className="mt-2 text-xs text-yellow-800 underline">
       실패 지점 재시도
     </button>
   </div>
   실패 지점만 체크박스 유지

3. 전체 실패:
   toast.error("배포에 실패했습니다.")
   모달 유지 (재시도 가능)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 배포 API 응답 수신 후

### 비즈니스 룰
- 전체 성공: 자동 닫힘 + success 토스트
- 일부 실패: 모달 유지 + 실패 지점 재시도 UI
- 전체 실패: error 토스트 + 모달 유지

### 연결 화면
- 성공: 모달 닫힘, SCR-P001 갱신
- 실패 재시도: 동일 모달에서 재시도
