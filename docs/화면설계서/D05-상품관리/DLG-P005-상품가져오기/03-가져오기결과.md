# DLG-P005 상품 가져오기 — 상태: 가져오기 결과

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P005 |
| 상태 코드 | `import-result` |
| 트리거 | 가져오기 API 응답 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-상품선택` |
| 다음 상태 | 성공 → 닫힘 / 중복 → 덮어쓰기 선택 / 실패 → 모달 유지 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P005 상품 가져오기 — 상태: 결과 분기

파일: src/components/products/ImportProductModal.tsx (API 응답 후)

결과 분기:
1. 전체 성공:
   toast.success("상품을 가져왔습니다.")
   onClose(); onRefresh()

2. 중복 상품 있음:
   경고 배너:
   <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-3">
     <p className="text-sm text-yellow-700">이미 존재하는 상품이 {dupCount}개 포함되어 있습니다</p>
     <div className="flex gap-2 mt-2">
       <button onClick={skipDups} className="text-xs text-gray-600 underline">기존 유지</button>
       <button onClick={overwrite} className="text-xs text-blue-600 underline">덮어쓰기</button>
     </div>
   </div>

3. 전체 실패:
   toast.error("상품 가져오기에 실패했습니다.")
   모달 유지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 가져오기 API 응답 수신

### 비즈니스 룰
- 성공: 자동 닫힘 + 목록 갱신
- 중복: 덮어쓰기/기존유지 선택 UI
- 실패: error 토스트 + 모달 유지

### 연결 화면
- 성공: SCR-P001 갱신
