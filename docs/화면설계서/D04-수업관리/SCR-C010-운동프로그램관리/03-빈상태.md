# SCR-C010 운동프로그램관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C010 |
| 상태 코드 | `empty` |
| 경로 | `/exercise-programs` |
| 우선순위 | P1 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <Dumbbell className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">등록된 운동 프로그램이 없습니다.</p>
  {hasPermission(['manager','fc']) && (
    <Button onClick={() => setIsRegisterOpen(true)}>프로그램 등록</Button>
  )}
</div>
```

## 📝 디스크립션
- 프로그램 없음 또는 검색 결과 없음
