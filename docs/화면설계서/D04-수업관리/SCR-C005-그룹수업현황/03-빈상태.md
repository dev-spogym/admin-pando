# SCR-C005 그룹수업현황 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C005 |
| 상태 코드 | `empty` |
| 경로 | `/class-stats` |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 수업 등록 → SCR-C002 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI (DataTable 대신):
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <BarChart3 className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">등록된 그룹수업이 없습니다.</p>
  <Button variant="outline" onClick={() => moveToPage('/lessons')}>
    수업 관리에서 등록
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- GX/group 타입 수업이 하나도 없는 경우

### 인터랙션
1. "수업 관리에서 등록" → SCR-C002

### 연결 화면
- SCR-C002 수업관리
