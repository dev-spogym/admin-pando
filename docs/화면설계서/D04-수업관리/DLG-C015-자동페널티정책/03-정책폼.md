# DLG-C015 자동페널티정책 — 상태: 정책 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C015 |
| 상태 코드 | `policy-form` |
| 부모 화면 | SCR-C008 페널티관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 200) |
| 다음 상태 | 저장 → `04-저장결과` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/AutoPenaltyPolicyModal.tsx

모달 헤더: "자동 페널티 정책 설정"

Zod 스키마:
const policySchema = z.object({
  triggerCondition: z.enum(['nosho','late_cancel'], { required_error: "자동발동 조건을 선택해주세요." }),
  autoPoint: z.number().int().min(1, "포인트는 1 이상이어야 합니다."),
  thresholdCount: z.number().int().min(1, "임계 횟수는 1 이상이어야 합니다."),
  enabled: z.boolean(),
})

폼 필드 (프리필 포함, space-y-4 p-6):
1. 자동발동 조건 * (RadioGroup: 노쇼 | 늦은취소)
2. 자동 포인트 * (Input number, min=1)
3. 임계 횟수 * (Input number, min=1, suffix="회 초과 시 자동 적용")
4. 활성화 여부 (Switch)

크론 연동 안내:
<div className="rounded-lg bg-blue-50 p-3 text-sm text-blue-700">
  정책 저장 시 자동 발동 크론(A05)이 즉시 업데이트됩니다.
</div>

모달 푸터:
- "취소" Button outline → onClose()
- "저장" Button primary
```

## 📝 디스크립션

### 연결 화면
- 저장: `04-저장결과`
- 닫기: `01-닫힘`
