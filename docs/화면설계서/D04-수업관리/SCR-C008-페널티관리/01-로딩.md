# SCR-C008 페널티관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `loading` |
| 경로 | `/penalties` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C008 페널티관리 — 상태: 로딩

파일: src/app/(classes)/penalties/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "페널티 관리")
- StatCardGrid Skeleton (3개)
- DataTable loading=true

데이터:
- SELECT p.*, m.name as member_name
  FROM penalties p JOIN users m ON p.member_id=m.id
  WHERE p.branch_id=? ORDER BY p.created_at DESC
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C008 진입 시
- 페널티 등록/삭제 후 갱신

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
