# SCR-C008 페널티관리 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `normal` |
| 경로 | `/penalties` |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 등록 → DLG-C014 / 자동정책 → DLG-C015 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C008 페널티관리 — 상태: 정상

파일: src/app/(classes)/penalties/page.tsx

레이아웃:
- AppLayout + PageHeader:
  · 제목 "페널티 관리"
  · 우측: "페널티 등록" Button primary (manager/fc만)
  · 우측: "자동정책 설정" Button outline (manager/owner만)

StatCardGrid (grid-cols-3 gap-4):
- "총 페널티 건수": N건
- "이번달 신규": N건
- "페널티 합계 포인트": N점

DataTable:
컬럼: 회원명 | 페널티 유형 | 포인트 | 사유 | 발생일 | 처리자 | 상태 | 액션
- 페널티 유형 배지: 노쇼(red) / 지각취소(orange) / 기타(gray)
- 상태 배지: 적용중(red) / 면제(green) / 만료(gray)
- 액션: Trash2 → 삭제 confirm (manager/owner만)

SearchFilter:
- 회원명 검색
- 유형 Select: 전체 | 노쇼 | 지각취소 | 기타
- 날짜 범위 DatePicker
- 상태 Select: 전체 | 적용중 | 면제 | 만료

데이터:
- penalties JOIN users (member)

사용 유틸:
- StatCardGrid, DataTable, StatusBadge, SearchFilter from '@/components/ui'
- hasPermission from '@/lib/auth'
- Trash2 from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 후 `penalties.length > 0`

### 필수 데이터
| 블록 | 테이블 |
|------|--------|
| 페널티 목록 | `penalties` |
| 회원 정보 | `users` |

### 인터랙션 (User Actions)
1. "페널티 등록" → DLG-C014
2. "자동정책 설정" → DLG-C015
3. Trash2 → confirm → DELETE
4. 검색/필터 적용

### 비즈니스 룰
- staff: 등록/삭제/자동정책 버튼 숨김
- 자동 생성 페널티: 처리자 "시스템" 표시
- 만료 페널티: opacity-60

### 에지 케이스
- 검색 결과 없음: `03-빈상태`

### 연결 화면
- 등록: DLG-C014
- 자동정책: DLG-C015
- 결과 없음: `03-빈상태`
