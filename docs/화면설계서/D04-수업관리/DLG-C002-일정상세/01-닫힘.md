# DLG-C002 일정상세 — 상태: 닫힘

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C002 |
| 상태 코드 | `closed` |
| 부모 화면 | SCR-C001 수업캘린더 |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | — |
| 다음 상태 | 캘린더 이벤트 클릭 → `02-로딩중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 닫힘 상태를 작성하라.
화면: DLG-C002 일정상세 — 상태: 닫힘

파일: src/components/classes/LessonDetailModal.tsx

모달 트리거 조건:
- SCR-C001 캘린더 이벤트 클릭 → selectedEvent 설정 → isOpen=true

Props:
interface LessonDetailModalProps {
  isOpen: boolean
  lessonId: string | null
  onClose: () => void
  onEdit: (lessonId: string) => void   // DLG-C001 수정 모달 연결
  onSuccess: () => void                // 삭제 후 캘린더 갱신
}

닫힘 상태 (isOpen=false):
- 모달 DOM 미렌더링 (null return)
- selectedEvent: null
- lessonId: null
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 초기 진입 시 기본 상태
- 모달 닫힘 후 복귀 상태

### 트리거 조건
| 액션 | 결과 |
|------|------|
| 캘린더 이벤트 클릭 | `02-로딩중` |

### 연결 화면
- 트리거: `02-로딩중`
