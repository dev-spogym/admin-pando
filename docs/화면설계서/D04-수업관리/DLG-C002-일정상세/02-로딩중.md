# DLG-C002 일정상세 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C002 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C001 수업캘린더 |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` (이벤트 클릭) |
| 다음 상태 | 성공 → `03-정상-상세표시` / 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 로딩 상태를 작성하라.
화면: DLG-C002 일정상세 — 상태: 로딩중

파일: src/components/classes/LessonDetailModal.tsx

모달 구조 (isOpen=true, isLoading=true):
- 오버레이: fixed inset-0 bg-black/50 z-50
- 모달 컨테이너: bg-white rounded-xl shadow-xl w-[400px] mx-auto mt-20

스켈레톤 UI (animate-pulse):
<div className="p-6 space-y-4">
  {/* 헤더 */}
  <div className="h-6 bg-gray-200 rounded w-3/4" />
  {/* 필드 행 4개 */}
  {[...Array(4)].map((_, i) => (
    <div key={i} className="space-y-1">
      <div className="h-3 bg-gray-200 rounded w-1/4" />
      <div className="h-5 bg-gray-200 rounded w-2/3" />
    </div>
  ))}
  {/* 버튼 영역 */}
  <div className="flex gap-2 pt-2">
    <div className="h-9 bg-gray-200 rounded w-20" />
    <div className="h-9 bg-gray-200 rounded w-20" />
  </div>
</div>

데이터 페칭:
const { data, isLoading, error } = useQuery({
  queryKey: ['lesson-detail', lessonId],
  queryFn: () => fetch(`/api/lessons/${lessonId}`).then(r => r.json()),
  enabled: !!lessonId,
})
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 모달 열림 직후 GET /api/lessons/:id 호출 중

### 인터랙션 (User Actions)
- 로딩 중 X 버튼: 모달 닫힘 (요청 취소)

### 연결 화면
- 로드 성공: `03-정상-상세표시`
- 로드 실패: `05-에러`
