# DLG-C005 수업기록상세 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C005 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, fc |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` (Eye 버튼 클릭) |
| 다음 상태 | 성공 → `03-정상-상세표시` / 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 로딩 상태를 작성하라.
화면: DLG-C005 수업기록상세 — 상태: 로딩중

파일: src/components/classes/LessonRecordDetailModal.tsx

스켈레톤 UI (animate-pulse, p-6 space-y-4):
- 헤더: h-6 bg-gray-200 rounded w-3/4
- 상태 배지: h-5 bg-gray-200 rounded w-20
- 필드 5개: h-4 bg-gray-200 rounded w-2/3
- 버튼 영역: flex gap-2, 3개 h-9 w-24 bg-gray-200 rounded

데이터 페칭:
const { data, isLoading, error } = useQuery({
  queryKey: ['lesson-record', recordId],
  queryFn: () => fetch(`/api/lesson-records/${recordId}`).then(r => r.json()),
  enabled: !!recordId,
})
```

## 📝 디스크립션

### 사용 시점
- 모달 열림 직후 GET /api/lesson-records/:id 호출 중

### 연결 화면
- 로드 성공: `03-정상-상세표시`
- 로드 실패: `05-에러`
