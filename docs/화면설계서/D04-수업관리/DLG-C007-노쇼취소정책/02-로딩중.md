# DLG-C007 노쇼/취소정책 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C007 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 성공 → `03-정책폼` / 실패 → toast.error |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/LessonPolicyModal.tsx

모달 열림 시 GET /api/lesson-policy 호출:
const { data: policy, isLoading } = useQuery({
  queryKey: ['lesson-policy'],
  queryFn: () => fetch('/api/lesson-policy').then(r => r.json()),
  enabled: isOpen,
})

스켈레톤 (isLoading=true, animate-pulse, space-y-4 p-6):
- 제목: h-6 bg-gray-200 rounded w-1/2
- 필드 3개: h-10 bg-gray-200 rounded
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-정책폼`
- 로드 실패: toast.error + 모달 닫힘
