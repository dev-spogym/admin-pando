# DLG-C004 일괄변경 — 상태: 닫힘

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C004 |
| 상태 코드 | `closed` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | — |
| 다음 상태 | 일괄변경 버튼 클릭 → 선택수량 확인 → `02-입력폼` 또는 경고 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 닫힘 상태를 작성하라.
화면: DLG-C004 일괄변경 — 상태: 닫힘

파일: src/components/classes/BulkChangeModal.tsx

Props:
interface BulkChangeModalProps {
  isOpen: boolean
  selectedIds: string[]
  onClose: () => void
  onSuccess: () => void
}

트리거 가드:
const handleOpen = () => {
  if (selectedIds.length === 0) {
    toast.warning("수업을 선택해주세요.")
    return
  }
  setIsOpen(true)
}
```

## 📝 디스크립션

### 트리거 조건
| 조건 | 결과 |
|------|------|
| 선택 0개 | toast.warning "수업을 선택해주세요." |
| 선택 1개 이상 | `02-입력폼` |

### 연결 화면
- 트리거: `02-입력폼`
