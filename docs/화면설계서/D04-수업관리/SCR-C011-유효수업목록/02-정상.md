# SCR-C011 유효수업목록 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `normal` |
| 경로 | `/valid-lessons` |
| 역할 | manager, fc, staff, readonly |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 행 클릭 → DLG-C002 또는 DLG-C005 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C011 유효수업목록 — 상태: 정상

파일: src/app/(classes)/valid-lessons/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "유효 수업 목록")
- 요약 배지: "총 N건 예정 수업" (text-sm text-gray-500)

SearchFilter:
- 날짜 범위 DatePicker (기본: 오늘 ~ 1개월 후)
- 강사 Select
- 수업 분류 Select
- 상태 Select: 예정 | 진행중

DataTable:
컬럼: 날짜 | 요일 | 시작시간 | 종료시간 | 수업명 | 강사 | 정원 | 예약 | 잔여 | 상태 | 액션
- 날짜 형식: MM/dd (E)
- 정원/예약/잔여: "N / M / K" 형식
- 잔여 0이면: text-red-500 "마감"
- 상태 배지: scheduled(blue) / in_progress(yellow)
- 액션: Eye → DLG-C002 (캘린더 상세) 또는 DLG-C005 (기록상세)
- 오늘 날짜 행: 배경 bg-blue-50 (하이라이트)

페이지네이션: 30행, 날짜 오름차순

데이터:
- lesson_schedules (status != cancelled, start_at >= NOW())
- 날짜 범위, 강사, 분류 필터

사용 유틸:
- DataTable, SearchFilter, StatusBadge from '@/components/ui'
- format, isToday from 'date-fns'
- Eye from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료, 취소되지 않은 미래 수업 있음

### 필수 데이터
| 블록 | 테이블 |
|------|--------|
| 수업 일정 | `lesson_schedules` |
| 수업 정보 | `classes` |
| 강사 정보 | `users` |
| 예약 수 | `lesson_bookings` COUNT |

### 인터랙션 (User Actions)
1. Eye → 상세 모달
2. 필터 적용 → 재조회
3. 컬럼 정렬

### 비즈니스 룰
- 취소(cancelled) 수업 미노출
- 과거 수업 미노출 (start_at < NOW())
- 잔여 0: "마감" 표시

### 에지 케이스
- 오늘 수업 하이라이트
- 잔여석 0인데 예약 시도 방지 (수업 상세에서 처리)

### 연결 화면
- Eye: DLG-C002 또는 DLG-C005
- 결과 없음: `03-빈상태`
