# DLG-C010 강사상세 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C010 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C006 강사근무현황 |
| 역할 | manager, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-닫힘` (강사 행 클릭) |
| 다음 상태 | 성공 → `03-정상-탭표시` / 실패 → `04-에러` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/InstructorDetailModal.tsx

모달 크기: w-[480px]

스켈레톤 (animate-pulse, p-6 space-y-4):
- 강사 프로필: flex gap-3, avatar w-12 h-12 + name h-5 w-32
- 탭 헤더: h-9 bg-gray-200 rounded
- 콘텐츠: 4개 h-4 bg-gray-200 rounded

데이터 페칭:
const { data, isLoading, error } = useQuery({
  queryKey: ['instructor-work-status', instructorId],
  queryFn: () => fetch(`/api/instructors/${instructorId}/work-status`).then(r => r.json()),
  enabled: !!instructorId && isOpen,
})
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-정상-탭표시`
- 로드 실패 (404/403): toast + 모달 닫힘
- 로드 실패 (500): `04-에러`
