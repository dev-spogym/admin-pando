# SCR-C015 수업녹화관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C015 |
| 상태 코드 | `loading` |
| 경로 | `/class-recordings` |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |
| 비고 | 신규 기능 (🆕) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C015 수업녹화관리 — 상태: 로딩

파일: src/app/(classes)/class-recordings/page.tsx (신규 생성)

레이아웃:
- AppLayout + PageHeader (제목 "수업 녹화 관리")
- StatCardGrid Skeleton (3개)
- 비디오 카드 그리드 Skeleton (grid-cols-3, 6개 카드 h-48 animate-pulse)

데이터:
- class_recordings 테이블 (신규)
- SELECT cr.*, c.name as class_name, u.name as instructor_name
  FROM class_recordings cr
  JOIN lesson_schedules ls ON cr.lesson_schedule_id=ls.id
  JOIN classes c ON ls.class_id=c.id
  JOIN users u ON ls.instructor_id=u.id
  WHERE cr.branch_id=? ORDER BY cr.created_at DESC
```

## 📝 디스크립션

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
