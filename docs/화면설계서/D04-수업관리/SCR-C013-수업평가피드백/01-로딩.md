# SCR-C013 수업평가피드백 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C013 |
| 상태 코드 | `loading` |
| 경로 | `/class-feedback` |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |
| 비고 | 신규 기능 (🆕) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C013 수업평가피드백 — 상태: 로딩

파일: src/app/(classes)/class-feedback/page.tsx (신규 생성)

레이아웃:
- AppLayout + PageHeader (제목 "수업 평가/피드백")
- StatCardGrid Skeleton (4개)
- DataTable loading=true

데이터:
- class_feedback 테이블 (신규 설계)
- SELECT cf.*, m.name as member_name, c.name as class_name,
    u.name as instructor_name
  FROM class_feedback cf
  JOIN users m ON cf.member_id=m.id
  JOIN classes c ON cf.class_id=c.id
  JOIN users u ON cf.instructor_id=u.id
  WHERE cf.branch_id=? ORDER BY cf.created_at DESC
```

## 📝 디스크립션

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
