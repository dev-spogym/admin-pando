# SCR-C003 시간표일괄등록 — 상태: 생성 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C003 |
| 상태 코드 | `generating` |
| 경로 | `/class-schedule` |
| 역할 | manager |
| 우선순위 | P1 |
| 이전 상태 | DLG-C008 확인 클릭 |
| 다음 상태 | 성공 → `05-완료` / 실패 → `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C003 시간표일괄등록 — 상태: 생성 중 (API 호출 중)

파일: src/app/(classes)/class-schedule/page.tsx (isGenerating=true 조건)

생성 중 UI (DLG-C008 닫히고 페이지에 표시):
- 전체 페이지 오버레이 또는 우측 영역:
  <div className="flex flex-col items-center justify-center h-full gap-4">
    <div className="relative w-16 h-16">
      <Loader2 className="w-16 h-16 animate-spin text-blue-500" />
    </div>
    <p className="text-base font-medium text-gray-700">수업 일정을 생성하는 중입니다...</p>
    <p className="text-sm text-gray-400">잠시만 기다려주세요. (예상 시간: {estimatedTime}초)</p>
    <!-- 프로그레스바 -->
    <div className="w-64 bg-gray-200 rounded-full h-2">
      <div
        className="bg-blue-500 h-2 rounded-full transition-all duration-500"
        style={{ width: `${progress}%` }}
      />
    </div>
    <p className="text-xs text-gray-400">{createdCount} / {totalCount}건 완료</p>
  </div>

상태 관리:
- isGenerating: true
- progress: 0 → 100 (API 응답 기반 또는 시뮬레이션)
- 좌측 폼/버튼 모두 disabled

API 호출:
- POST /api/lesson-schedules/bulk
- body: { classId, instructorId, dates[], startTime, duration, capacity }

사용 유틸:
- Loader2 from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- DLG-C008에서 "확인" 버튼 클릭 후 API 호출 시작

### 인터랙션 (User Actions)
- 생성 중에는 모든 인터랙션 차단
- 뒤로가기 경고: "일정 생성 중입니다. 페이지를 벗어나면 생성이 중단될 수 있습니다."

### 비즈니스 룰
- 대량 생성(100건 이상): 배치 처리로 진행, 프로그레스 실시간 업데이트
- 타임아웃 30초: 에러 상태로 전환

### 연결 화면
- 성공: `05-완료`
- 실패: `06-에러`
