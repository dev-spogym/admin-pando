# DLG-C003 수업등록/수정(관리) — 상태: 유효성 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C003 |
| 상태 코드 | `validation-error` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, fc |
| 우선순위 | P0 |
| 이전 상태 | `02-신규폼` 또는 `03-수정폼` (저장 시도 → 유효성 실패) |
| 다음 상태 | 필드 수정 후 재시도 → `05-저장성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 유효성 에러 UI를 작성하라.
화면: DLG-C003 수업등록/수정(관리) — 상태: 유효성 에러

파일: src/components/classes/LessonManageModal.tsx

인라인 에러 표시:
- 각 필드 하단: <p className="mt-1 text-xs text-red-500">{errors.field?.message}</p>
- 에러 필드 border: border-red-500 focus:ring-red-500
- 저장 버튼 클릭 시 첫 에러 필드로 자동 포커스 (setFocus)
```

## 📝 디스크립션

### 에러 목록
| 필드 | 에러 메시지 |
|------|------------|
| 수업명 | "수업명을 입력해주세요." |
| 수업 분류 | "수업 분류를 선택해주세요." |
| 강사 | "강사를 선택해주세요." |
| 시작 시간 | "시작 시간을 입력해주세요." |
| 종료 시간 | "올바른 시간을 입력해주세요." |
| 정원 | "정원은 1~100명이어야 합니다." |

### 인터랙션
1. 에러 필드 수정 → 에러 메시지 즉시 사라짐 (onChange 검증)
2. 재저장 → 유효성 재검사

### 연결 화면
- 수정 후 재저장: `05-저장성공`
