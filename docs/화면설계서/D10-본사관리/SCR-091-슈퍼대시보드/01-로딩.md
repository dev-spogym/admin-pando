# SCR-091 슈퍼대시보드 — 상태 01: 로딩 (스켈레톤)

> 상속: [`00-기본화면.md`](./00-기본화면.md) — 이 문서는 **변경점(델타)**만 기술합니다.

## 메타
| 항목 | 값 |
|---|---|
| 상태 코드 | `super-loading` |
| 상태 ID | STATE_LOADING |
| 다이어그램 노드 | `F6_상태별.md` → STATE_LOADING |
| 이전 상태 | 진입 직후, 새로고침 직후 |
| 다음 상태 | `02-정상` / `03-지점없음` / `05-에러` / `06-오프라인` |

## 상태 진입 조건
- `/super-dashboard` 접근 가드 통과(super/primary)
- `statsQ`/`branchQ`/`auditQ` 중 하나 이상 `isLoading=true`
- 새로고침 직후

## 상태 고유 비주얼 델타

```
┌───────────────────────────────────────────────────────────────┐
│ §A KPI 7개 스켈레톤 (레이아웃 유지)                              │
│ ┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐             │
│ │▓▓▓▓ ││▓▓▓▓ ││▓▓▓▓ ││▓▓▓▓ ││▓▓▓▓ ││▓▓▓▓ ││▓▓▓▓ │             │
│ └─────┘└─────┘└─────┘└─────┘└─────┘└─────┘└─────┘             │
├───────────────────────────────────────────────────────────────┤
│ §B 지점 카드 스켈레톤 (4개 기본, gray ring)                      │
├───────────────────────────────────────────────────────────────┤
│ §C 감사로그 스켈레톤 (5행)                                      │
└───────────────────────────────────────────────────────────────┘
```

| 요소 | 변경 |
|---|---|
| StatCard | `<Skeleton />` (label h-3 w-16, value h-8 w-24) |
| 지점 카드 | 이름/상태/KV 3칸/버튼 2개 스켈레톤, `ring-gray-200` |
| 감사로그 | table 구조 유지, 5행 row 스켈레톤 |
| 새로고침 아이콘 | `animate-spin` |
| 사이드바 | 정상 렌더 |

## 상태 고유 인터랙션
- 모든 §A/§B/§C 클릭 비활성 (`pointer-events-none opacity-70`)
- 새로고침 disabled
- 사이드바 네비 활성
- 5초 초과 → "조회에 시간이 걸리고 있습니다" 보조 텍스트
- 10초 초과 → `05-에러` 전환 (타임아웃)

## 비즈니스 룰 델타
- 기본 스켈레톤 지점 카드 개수: 4개 (이전 세션 `branches.length` 있으면 그 수로)
- CLS 방지: 실제 UI와 동일 그리드
- Stream 렌더(선택): 헤더 → KPI → 지점 → 감사

## 에지 케이스
- `statsQ` 완료 + `branchQ` pending: KPI만 실제, 나머지 스켈레톤
- primary 브랜드 하위 지점 많음: 스켈레톤 4~8개

## 🧩 바이브코딩 프롬프트 (델타)
```
상속: 00-기본화면.md §12 프롬프트 전체 적용
상태: super-loading

Skeleton 컴포넌트:
export const Skeleton = ({className}: {className?:string}) =>
  <div aria-hidden className={cn('animate-pulse rounded bg-gray-200', className)} />

StatCard 로딩:
{isLoading ? (
  <div className="bg-white rounded-xl shadow-sm ring-1 ring-gray-100 p-5">
    <Skeleton className="h-3 w-16 mb-3" />
    <Skeleton className="h-8 w-28 mb-2" />
    <Skeleton className="h-3 w-20" />
  </div>
) : (...)}

지점 카드 로딩:
<article className="bg-white rounded-xl shadow-sm ring-1 ring-gray-200 p-5 flex flex-col gap-3">
  <div className="flex items-start justify-between">
    <Skeleton className="h-5 w-24" />
    <Skeleton className="h-5 w-12 rounded-full" />
  </div>
  <div className="grid grid-cols-3 gap-2">
    {Array.from({length:3}).map((_,i)=>(
      <div key={i}><Skeleton className="h-3 w-8 mb-1"/><Skeleton className="h-4 w-12"/></div>
    ))}
  </div>
  <div className="flex gap-2 pt-2">
    <Skeleton className="h-8 flex-1" />
    <Skeleton className="h-8 flex-1" />
  </div>
</article>

감사로그 로딩:
<table><tbody>
  {Array.from({length:5}).map((_,i)=>(
    <tr key={i}>
      <td className="py-2"><Skeleton className="h-3 w-16"/></td>
      <td className="py-2"><Skeleton className="h-3 w-20"/></td>
      <td className="py-2"><Skeleton className="h-3 w-16"/></td>
      <td className="py-2"><Skeleton className="h-5 w-14 rounded-full"/></td>
      <td className="py-2"><Skeleton className="h-3 w-40"/></td>
    </tr>
  ))}
</tbody></table>

aria-busy="true" on <main>
role="status" sr-only: "슈퍼 대시보드를 불러오는 중입니다"
reduced-motion 시 pulse 제거
```

## TC 후보
| TC ID | 타입 | Given | When | Then |
|---|---|---|---|---|
| TC-091-01-01 | positive | 진입 | pending | KPI 7 + 지점 4 + 감사 5 스켈레톤 |
| TC-091-01-02 | positive | KPI만 완료 | — | KPI 실제, 나머지 스켈레톤 |
| TC-091-01-03 | positive | 5초 경과 | — | "시간이 걸리고 있습니다" 안내 |
| TC-091-01-04 | positive | 새로고침 | — | 전체 스켈레톤 |
| TC-091-01-05 | a11y | SR | — | aria-busy + status 공지 |
| TC-091-01-06 | positive | reduced-motion | — | pulse 제거 |

## 다이어그램 링크
- F6 상태별: STATE_LOADING
- F2 메인: 전 지점 병렬 fetch
