# SCR-091 슈퍼대시보드 — 상태 03: 지점 없음 (빈 상태)

> 상속: [`00-기본화면.md`](./00-기본화면.md) — 이 문서는 **변경점(델타)**만 기술합니다.

## 메타
| 항목 | 값 |
|---|---|
| 상태 코드 | `super-no-branch` |
| 상태 ID | STATE_EMPTY |
| 다이어그램 노드 | `F6_상태별.md` → STATE_EMPTY |
| 이전 상태 | `01-로딩`(branches 빈 배열) |
| 다음 상태 | `02-정상`(지점 생성 후) / `05-에러` |

## 상태 진입 조건
- `branchQ.isSuccess === true` AND `branchQ.data.length === 0`
- 시나리오: 신규 도입 본사, primary의 브랜드에 지점 미등록

## 상태 고유 비주얼 델타

```
┌───────────────────────────────────────────────────────────────┐
│ §A KPI (모두 0)                                                │
│ ┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐┌─────┐             │
│ │  0명││ 0원││ 0명││ 0명││ 0명││ 0명││ 0명│             │
│ └─────┘└─────┘└─────┘└─────┘└─────┘└─────┘└─────┘             │
├───────────────────────────────────────────────────────────────┤
│                       🏢 (64px)                                │
│                 등록된 지점이 없습니다                           │
│    첫 지점을 등록하면 KPI와 운영 현황을 볼 수 있습니다.         │
│                                                               │
│   [+ 지점 등록하기]  [지점 관리로 이동 →]                       │
│                                                               │
│   💡 지점 등록 후 회원·직원·매출이 자동으로 집계됩니다.          │
├───────────────────────────────────────────────────────────────┤
│ §C 감사로그: "아직 기록된 활동이 없습니다"                       │
└───────────────────────────────────────────────────────────────┘
```

### 변경 요소
| 요소 | 변경 |
|---|---|
| §A KPI | 모두 0 표시, delta 배지 숨김 |
| §B 지점 | 섹션 전체 빈 상태 CTA 카드로 교체 |
| §C 감사로그 | "아직 기록된 활동 없음" 또는 시스템 LOGIN 로그만 |
| 새로고침 | 활성 |

## 상태 고유 인터랙션
1. "+ 지점 등록하기" → `/branches/new`
2. "지점 관리로 이동" → `/branches`
3. 새로고침: 지점 등록 후 즉시 반영

## 비즈니스 룰 델타
- KPI 0 표시 (레이아웃 유지)
- 지점 등록 가이드 배너 (`bg-blue-50`)
- 브랜드/회사 정보 선행 필요 시 병행 가이드
- 감사로그 시드: 초기 LOGIN 1건은 노출

## 에지 케이스
- primary만 접근, 브랜드 하위 지점 0 → 이 상태
- 브랜드 자체 없음: 브랜드 등록 CTA 추가 (P1)
- 과거 지점이 모두 CLOSED: "폐점 포함" 토글 제안

## 🧩 바이브코딩 프롬프트 (델타)
```
상속: 00-기본화면.md §12 프롬프트 전체 적용
상태: super-no-branch

조건:
if (branches.length === 0 && !branchQ.isLoading) { /* 이 상태 */ }

§B 섹션 교체:
<section aria-label="지점별 현황">
  <div className="bg-white rounded-xl shadow-sm ring-1 ring-gray-100 p-12
                  flex flex-col items-center text-center gap-5">
    <div className="flex size-16 items-center justify-center rounded-full bg-blue-50">
      <Building2 className="size-8 text-blue-500" aria-hidden="true" />
    </div>
    <div className="space-y-1 max-w-md">
      <h2 className="text-lg font-semibold text-gray-900">등록된 지점이 없습니다</h2>
      <p className="text-sm text-gray-600">첫 지점을 등록하면 KPI와 운영 현황을 볼 수 있습니다.</p>
    </div>
    <div className="flex flex-col sm:flex-row items-center gap-3">
      <button onClick={() => moveToPage('/branches/new')}
        className="h-10 px-5 rounded-lg bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium inline-flex items-center gap-2">
        <Plus className="size-4" /> 지점 등록하기
      </button>
      <Link href="/branches" className="text-sm text-blue-600 hover:underline">지점 관리로 이동 →</Link>
    </div>
    <div className="mt-4 rounded-lg bg-blue-50 border border-blue-200 p-4 text-xs text-blue-800 max-w-xl">
      💡 지점을 등록한 후에는 회원·직원·매출이 자동으로 집계됩니다.
    </div>
  </div>
</section>

§A KPI 처리:
stats = { totalMembers: 0, ... } 그대로 렌더
{delta && delta.value !== 0 && <DeltaBadge {...delta} />}

§C 감사로그:
{auditLogs.length === 0 ? (
  <div className="text-center py-8 text-sm text-gray-400">아직 기록된 활동이 없습니다</div>
) : (<table>...)}

접근성:
- role="status" on 빈 상태 영역
- CTA aria-label 명확
- 아이콘 aria-hidden
```

## TC 후보
| TC ID | 타입 | Given | When | Then |
|---|---|---|---|---|
| TC-091-03-01 | positive | 지점 0개 | 진입 | KPI 0 + 지점 섹션 빈 CTA |
| TC-091-03-02 | positive | "지점 등록하기" 클릭 | — | `/branches/new` |
| TC-091-03-03 | positive | "지점 관리로 이동" | — | `/branches` |
| TC-091-03-04 | positive | 지점 등록 후 새로고침 | — | 02-정상 |
| TC-091-03-05 | positive | 감사로그 0건 | — | "활동 없음" |
| TC-091-03-06 | a11y | SR | — | "등록된 지점이 없습니다" 공지 |

## 다이어그램 링크
- F6 상태별: STATE_EMPTY
- F1 진입: 빈 지점 분기
