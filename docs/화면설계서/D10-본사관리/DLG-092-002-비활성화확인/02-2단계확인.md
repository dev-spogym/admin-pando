# DLG-092-002 비활성화확인 — 상태: 2단계 최종 확인 (지점명 입력)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-092-002 |
| 상태 코드 | `step2-input` |
| 트리거 | 1단계 [계속] 클릭 |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-1단계확인` |
| 다음 상태 | 지점명 일치 + [확인] → `03-성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-092-002 지점 비활성화 — 2단계 최종 확인

E_D92_02_M1_03: 1단계 [계속] → 2단계 렌더링

Modal 레이아웃:
- 헤더: "최종 확인" + [X]
- 설명:
  <p className="text-sm text-gray-600">
    비활성화를 진행하려면 지점명을 정확히 입력하세요.
  </p>
- 힌트 박스 (bg-gray-50 border rounded-lg p-3 mt-3):
  <p className="text-sm text-gray-500">
    입력: <span className="font-semibold text-gray-800">'{branchName}'</span>
  </p>

- Input:
  <Input
    value={confirmInput}
    onChange={(e) => setConfirmInput(e.target.value)}
    placeholder={branchName}
    className={confirmInput && confirmInput !== branchName ? 'border-red-400' : ''}
  />
  {/* 인라인 에러 */}
  {confirmInput && confirmInput !== branchName && (
    <p className="text-xs text-red-500 mt-1">지점명이 일치하지 않습니다.</p>
  )}

- [확인] Button:
  disabled={confirmInput !== branchName}
  variant="destructive"
  onClick={handleDeactivate}

M2 검증 (E_D92_02_M2_01~03):
- 미입력: "지점명을 입력하세요."
- 불일치: "지점명이 일치하지 않습니다."
- 일치: 버튼 활성화

TC-092-DLG-005: 지점명 일치 입력 + 확인 → 비활성화 성공
TC-092-DLG-006: 지점명 불일치 → 인라인 에러 + 확인 버튼 비활성화
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D92_02_M1_03`: 1단계 [계속] 클릭

### TC 연결
- TC-092-DLG-005: 지점명 일치 → 확인 → 비활성화 성공
- TC-092-DLG-006: 지점명 불일치 → 인라인 에러 + 버튼 비활성화

### 인터랙션 (User Actions)
1. 지점명 정확히 입력 → 버튼 활성화
2. [확인] 클릭 → `E_D92_02_M1_05` → PATCH API → `03-성공`
3. [취소] → `E_D92_02_M1_04` → 닫힘

### 연결 화면
- 이전: `01-1단계확인`
- 다음: 성공 → `03-성공` / 409 → `04-차단`
