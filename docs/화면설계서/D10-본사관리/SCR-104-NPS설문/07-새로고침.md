# SCR-104 NPS설문 — 상태: 새로고침 (필터 변경 중)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-104 |
| 상태 코드 | `refreshing` |
| 경로 | `src/app/(admin)/nps/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `02~04` (필터 변경) |
| 다음 상태 | 완료 → `02~04` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-104 NPS설문 — 새로고침 (필터 변경 중)

조건: E_F6_104_08 — 기간 필터 변경으로 API 재호출 중

오버레이 스피너 (기존 데이터 유지):
<div className="relative">
  <div className="opacity-40 pointer-events-none">
    {/* 이전 NPS 카드 + 차트 + 응답 목록 */}
  </div>
  <div className="absolute inset-0 flex items-center justify-center bg-white/60">
    <Loader2 className="w-8 h-8 animate-spin text-blue-500" />
  </div>
</div>

E_F6_104_09: 완료 → 새 NPS 점수에 따라 02/03/04 상태로 분기
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_104_08`: 기간 필터(30일/90일/6개월) 변경

### 연결 화면
- 이전: `02-우수` / `03-보통` / `04-주의`
- 다음: `E_F6_104_09` 완료 → npsScore 기준으로 분기
