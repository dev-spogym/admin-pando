# DLG-092-001 신규지점등록 — 상태: 검증 에러 (필드 인라인 에러)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-092-001 |
| 상태 코드 | `validation-error` |
| 트리거 | [저장] 클릭 시 유효성 실패 |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` (저장 시도) |
| 다음 상태 | 필드 수정 → `03-저장성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-092-001 신규 지점 등록 — 검증 에러 상태

조건: E_D92_01_M1_04 — 유효성 검사 실패

인라인 에러 표시:
각 필드 하단에 에러 메시지:
<p className="text-xs text-red-500 mt-1">{errorMessage}</p>
에러 필드 Input: border-red-400

에러 케이스 (M2 기반):
- 지점명 미입력: "지점명은 필수입니다."
- 지점명 50자 초과: "50자 이내로 입력하세요."
- 지역 미선택: "지역을 선택하세요."
- 주소 미입력: "주소는 필수입니다."
- 전화번호 형식 오류: "올바른 전화번호 형식으로 입력하세요."
- 오픈일 미선택: "오픈일을 선택하세요."
- 정원 0 이하: "1 이상의 값을 입력하세요."

react-hook-form 사용 권장:
const { register, handleSubmit, formState: { errors } } = useForm()

TC-092-DLG-002: 지점명 미입력 → 저장 클릭 → 인라인 에러 "지점명은 필수입니다."
TC-092-DLG-003: 지점코드 자동생성 확인
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D92_01_M1_04`: [저장] 클릭 + 유효성 검사 실패

### TC 연결
- TC-092-DLG-002: 지점명 미입력 → 인라인 에러
- TC-092-DLG-003: 지점코드 자동생성 BR-YYYYMM-NNN 확인

### 인터랙션 (User Actions)
1. 에러 필드 수정 → 에러 메시지 실시간 클리어
2. 모든 에러 수정 후 [저장] 재클릭 → `03-저장성공`

### 연결 화면
- 이전: `01-오픈`
- 다음: 수정 후 성공 → `03-저장성공`
