# DLG-094-001 매출목표설정 — 상태: 오픈 (기존값 프리필 또는 빈 폼)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-094-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-094 [목표 설정] 버튼 클릭 |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-094 정상 |
| 다음 상태 | 입력 → `02-검증에러` 또는 `03-저장성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-094-001 매출 목표 설정 — 오픈

E_D94_01_M1_01: [목표 설정] 클릭 → 다이얼로그 오픈

Modal 레이아웃 (max-w-md):
- 헤더: "매출 목표 설정" + [X]
- 부제목: "월별 매출 목표를 설정합니다"

[폼 필드 4개]
1. 연도 * — Select (현재년도 ~ +2년)
   기본값: 현재 연도
2. 월 * — Select (1월 ~ 12월)
   기본값: 현재 월
3. 목표 매출금액 * — Input type="number"
   placeholder="목표 금액 (만원 단위)"
   suffix: "만원"
4. 메모 — Textarea (placeholder="목표 설정 메모", rows=2, maxLength=200)

프리필 처리 (기존 목표 있을 경우):
const storageKey = `kpi_monthly_target_${branchId}_${year}${String(month).padStart(2,'0')}`
const existing = localStorage.getItem(storageKey)
if (existing) {
  const { amount, memo } = JSON.parse(existing)
  setValue('amount', amount)
  setValue('memo', memo)
}

E_D94_01_M1_02: 기존 목표 있음 → 프리필
E_D94_01_M1_03: 기존 목표 없음 → 빈 폼

[저장] 버튼: 필수 필드 모두 입력 시 활성화
[취소] 버튼 (variant="outline")

TC-094-DLG-001: 목표금액 입력 + 저장 → localStorage 저장 + KPI 카드 갱신
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D94_01_M1_01`: SCR-094 [목표 설정] 클릭

### 필드 검증 규칙 (M2 기반)
| 필드 | 규칙 | 에러 메시지 |
|------|------|------------|
| 연도 | 필수 | "연도를 선택하세요." |
| 월 | 필수 | "월을 선택하세요." |
| 목표금액 | 필수, 양수 정수 | "목표 금액을 입력하세요." / "1 이상의 값을 입력하세요." |
| 메모 | 선택, 200자 | "200자 이내로 입력하세요." |

### 인터랙션 (User Actions)
1. 연도/월 변경 → localStorage 키 변경 → 해당 월 목표 프리필
2. [저장] → `03-저장성공` / 에러 → `02-검증에러`
3. [취소] / ESC → 닫힘

### 연결 화면
- 이전: SCR-094 정상
- 다음: `02-검증에러` / `03-저장성공` / 취소 → SCR-094
