# SCR-096 온보딩대시보드 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-096 |
| 상태 코드 | `error` |
| 경로 | `src/app/(admin)/onboarding/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | 재시도 → 진입 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-096 온보딩대시보드 — 에러

조건: E_096_S_05 — onboarding_progress API 실패

에러 UI:
<div className="flex flex-col items-center justify-center h-80 gap-4">
  <p className="text-gray-500">온보딩 진행 상태를 불러오지 못했습니다.</p>
  <Button variant="outline" onClick={loadOnboarding}>재시도</Button>
</div>

toast.error("온보딩 데이터를 불러오지 못했습니다.")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_096_S_05`: onboarding_progress 테이블 API 실패

### 인터랙션 (User Actions)
1. [재시도] → loadOnboarding() 재호출

### 연결 화면
- 이전: 진입
- 다음: 재시도 → `01-초기상태` / `02-진행중` / `03-필수완료` / `04-전체완료`
