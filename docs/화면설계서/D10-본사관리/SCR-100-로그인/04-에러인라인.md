# SCR-100 로그인 — 상태: 에러 (인라인 에러)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-100 |
| 상태 코드 | `error-inline` |
| 경로 | `src/app/login/page.tsx` |
| 역할 | 비인증 사용자 |
| 우선순위 | P0 |
| 이전 상태 | `03-로딩` |
| 다음 상태 | 재입력 → `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-100 로그인 — 인라인 에러 (이메일/비밀번호 불일치)

조건: E_F6_100_05 — Supabase 인증 실패 (Invalid login credentials)

에러 UI:
- 두 필드 하단에 인라인 에러 메시지:
  <p className="text-xs text-red-500 mt-1">이메일 또는 비밀번호가 올바르지 않습니다.</p>
- 이메일/비밀번호 Input: border-red-400
- [로그인] 버튼: 다시 활성화 (재시도 가능)
- 비밀번호 필드: 자동 클리어

const [loginError, setLoginError] = useState<string | null>(null)

catch (error) {
  if (error.message === 'Invalid login credentials') {
    setLoginError('이메일 또는 비밀번호가 올바르지 않습니다.')
    setPassword('') // 비밀번호 클리어
  }
}

E_F6_100_08: 재입력 시작 → 에러 메시지 클리어 → 02-입력중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_100_05`: Supabase auth.signInWithPassword 인증 실패

### 인터랙션 (User Actions)
1. 필드 재입력 → `E_F6_100_08` → 에러 클리어 → `02-입력중`
2. [로그인] 재클릭 → `03-로딩`

### 비즈니스 룰
- 3회 연속 실패 시 → `05-잠금` 전환
- 비밀번호 필드 자동 클리어 (보안)

### 연결 화면
- 이전: `03-로딩`
- 다음: 재입력 → `02-입력중` / 3회 실패 → `05-잠금`
