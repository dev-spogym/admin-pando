# SCR-100 로그인 — 상태: 서버 에러 (토스트)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-100 |
| 상태 코드 | `error-server` |
| 경로 | `src/app/login/page.tsx` |
| 역할 | 비인증 사용자 |
| 우선순위 | P1 |
| 이전 상태 | `03-로딩` |
| 다음 상태 | 재시도 → `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-100 로그인 — 서버 에러 토스트

조건: E_F6_100_07 — 서버 500 / 네트워크 에러

에러 처리:
} catch (error) {
  if (isNetworkError(error) || isServerError(error)) {
    toast.error("서버 오류가 발생했습니다. 잠시 후 다시 시도하세요.")
  }
  setIsLoading(false)
}

UI 상태:
- 폼은 `02-입력중` 상태로 복귀 (에러 인라인 없음)
- 상단 토스트만 표시
- [로그인] 버튼 다시 활성화

E_F6_100_09: 재시도 → 02-입력중 → 03-로딩
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_100_07`: Supabase 서버 오류 (500) 또는 네트워크 단절

### 인터랙션 (User Actions)
1. 토스트 자동 닫힘 (4초)
2. [로그인] 재클릭 → `E_F6_100_09` → `03-로딩`

### 연결 화면
- 이전: `03-로딩`
- 다음: 재시도 → `02-입력중`
