# SCR-100 로그인 — 상태: 기본 (폼 비어있음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-100 |
| 상태 코드 | `idle` |
| 경로 | `src/app/login/page.tsx` |
| 역할 | 비인증 사용자 (전체) |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | 입력 시작 → `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-100 로그인 — 기본 상태 (폼 비어있음)

파일: src/app/login/page.tsx

레이아웃:
- 전체 화면 중앙 배치 (min-h-screen flex items-center justify-center bg-gray-50)
- 카드 (bg-white rounded-2xl shadow-lg p-8 w-full max-w-md)

[로고 영역]
- FitGenie CRM 로고 (text-2xl font-bold text-blue-600)
- 부제목 "관리자 로그인" (text-sm text-gray-500 mt-1)

[로그인 폼]
- 이메일 Input (type="email", placeholder="이메일을 입력하세요")
- 비밀번호 Input (type="password", placeholder="비밀번호를 입력하세요")
  · 비밀번호 보기/숨기기 토글 아이콘 (우측)
- [로그인] Button (primary, w-full, disabled 시 opacity-50)

초기 상태:
- 두 필드 모두 비어있음
- [로그인] 버튼: disabled=true (입력 전)
- 에러 메시지 없음

E_F6_100_01: /login 접근 → 기본 상태 렌더링
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_100_01`: /login 최초 접근 (비인증 상태)
- 로그아웃 후 리다이렉트

### 인터랙션 (User Actions)
1. 이메일 입력 시작 → `02-입력중`
2. 비밀번호 입력 시작 → `02-입력중`

### 비즈니스 룰
- 이미 인증된 사용자 → /dashboard 리다이렉트
- 이메일/비밀번호 모두 입력 후 [로그인] 버튼 활성화

### 연결 화면
- 이전: 진입 (비인증)
- 다음: `02-입력중`
