# SCR-102 벤치마크비교 — 상태: 새로고침 (필터 변경 중)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-102 |
| 상태 코드 | `refreshing` |
| 경로 | `src/app/(admin)/benchmark/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `02-데이터있음` (필터 변경) |
| 다음 상태 | 완료 → `02-데이터있음` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-102 벤치마크비교 — 새로고침 (필터 변경 중 오버레이)

조건: E_F6_102_08 — 필터 변경으로 데이터 재조회 중

오버레이 스피너:
<div className="relative">
  {/* 기존 데이터 유지 — 블러 처리 */}
  <div className="opacity-40 pointer-events-none">
    {/* 이전 테이블 + 차트 */}
  </div>
  {/* 오버레이 스피너 */}
  <div className="absolute inset-0 flex items-center justify-center bg-white/60">
    <Loader2 className="w-8 h-8 animate-spin text-blue-500" />
  </div>
</div>

E_F6_102_09: 완료 → 새 데이터로 02-데이터있음 렌더링
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_102_08`: 필터 변경으로 API 재호출 중

### 비즈니스 룰
- 기존 데이터 유지 (깜빡임 방지) + 오버레이 스피너

### 연결 화면
- 이전: `02-데이터있음`
- 다음: 완료 → `02-데이터있음`
