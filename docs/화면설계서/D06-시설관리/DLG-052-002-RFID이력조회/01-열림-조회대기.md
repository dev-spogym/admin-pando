# DLG-052-002 RFID이력조회 — 상태: 열림 (조회 대기)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-052-002 |
| 상태 코드 | `open-ready` |
| 트리거 | SCR-052 행 "이력" 버튼 클릭 |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | SCR-052 정상 화면 |
| 다음 상태 | `02-결과있음` 또는 `03-결과없음` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-052-002 RFID 이력 조회 — 열림/조회 대기

<Modal isOpen={isOpen} onClose={onClose} title={`카드 이력 — ${card.cardNumber}`} size="lg">
  <div className="space-y-4">
    {/* 기간 필터 */}
    <div className="flex gap-2 items-end">
      <div className="flex-1">
        <label className="text-xs text-gray-500 mb-1 block">시작일</label>
        <DatePicker value={startDate} onChange={setStartDate} />
      </div>
      <span className="text-gray-400 mb-2">~</span>
      <div className="flex-1">
        <label className="text-xs text-gray-500 mb-1 block">종료일</label>
        <DatePicker value={endDate} onChange={setEndDate} />
      </div>
      <Button variant="primary" size="sm" onClick={handleSearch} loading={isSearching}>조회</Button>
    </div>

    {/* 초기 안내 */}
    <div className="flex items-center justify-center py-8 text-gray-400">
      <p className="text-sm">기간을 선택하고 조회 버튼을 눌러주세요.</p>
    </div>
  </div>

  <div className="flex justify-end mt-4">
    <Button variant="outline" onClick={onClose}>닫기</Button>
  </div>
</Modal>

// 기본값: startDate = 30일 전, endDate = 오늘
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_M1_OPEN_01`: SCR-052 행 "이력" 버튼 클릭

### 인터랙션 (User Actions)
1. 기간 DatePicker 설정 후 "조회" 클릭 → API 호출
2. "닫기" / ESC → 모달 닫힘

### 연결 화면
- 다음: `02-결과있음` / `03-결과없음` / `04-에러`
