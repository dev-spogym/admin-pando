# SCR-055 운동복관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-055 |
| 상태 코드 | `empty` |
| 경로 | `/facilities/clothing` |
| 역할 | owner, manager, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | "운동복 추가" → 등록 모달 |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-055 운동복 관리 — 빈 상태
조건: clothing.length === 0 (E_F6_EMPTY_01)

<div className="flex flex-col items-center justify-center py-24 text-center">
  <ShirtIcon className="w-12 h-12 text-gray-300 mb-4" />
  <p className="text-lg font-semibold text-gray-700 mb-1">등록된 운동복이 없습니다</p>
  <p className="text-sm text-gray-400 mb-6">운동복을 등록하여 대여 관리를 시작하세요.</p>
  <Button onClick={openAddModal} variant="primary">운동복 추가</Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_EMPTY_01`: 0건

### 연결 화면
- 다음: 등록 성공 → `02-정상`
