# DLG-057-002 입출고처리 — 상태: 열림 (출고 모드)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-057-002 |
| 상태 코드 | `open-outbound` |
| 트리거 | SCR-057 행 "출고 -" 버튼 클릭 |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | SCR-057 정상 화면 |
| 다음 상태 | `03-성공` 또는 `04-실패` |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-057-002 출고 처리 (신규 기능)

// 입고 모드와 동일 구조, isOutbound=true 분기
<Modal title={`출고 처리 — ${item.name}`}>
  {/* 현재 재고 */}
  <div className="p-3 bg-gray-50 rounded-lg flex justify-between">
    <span className="text-sm text-gray-600">현재 재고</span>
    <span className="text-lg font-bold">{item.currentStock} {item.unit}</span>
  </div>

  {/* 출고 수량 */}
  // 동일 수량 입력 UI

  {/* 출고 후 재고 미리보기 */}
  <div className={`p-3 rounded-lg flex justify-between ${afterStock < item.safetyStock ? 'bg-red-50' : 'bg-blue-50'}`}>
    <span>출고 후 재고</span>
    <span className={afterStock < item.safetyStock ? 'text-red-700 font-bold' : 'text-blue-700 font-bold'}>
      {afterStock} {item.unit}
      {afterStock < item.safetyStock && ' ⚠ 안전재고 미만'}
    </span>
  </div>
</Modal>

출고 검증 추가:
- E_M2_QTY_EXCEED_01: qty > currentStock → "출고 수량이 현재고를 초과합니다."
```

## 📝 디스크립션

### 검증 규칙 (M2)
| 에러 코드 | 조건 | 메시지 |
|-----------|------|--------|
| E_M2_QTY_EMPTY_01 | 수량 미입력 | "수량을 입력하세요." |
| E_M2_QTY_ZERO_01 | 수량 0 이하 | "1 이상의 수량을 입력하세요." |
| E_M2_QTY_EXCEED_01 | 수량 > 현재고 | "출고 수량이 현재고를 초과합니다." |

### 비즈니스 룰
- 출고 후 안전재고 미만 → 미리보기에서 경고 표시
- OUT(0) 도달 시 "품절" 배지 자동 부여

### 연결 화면
- 다음: `03-성공` / `04-실패`
