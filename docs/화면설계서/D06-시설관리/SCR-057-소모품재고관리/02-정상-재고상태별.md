# SCR-057 소모품재고관리 — 상태: 정상 (재고 상태별)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-057 |
| 상태 코드 | `normal-stock-states` |
| 경로 | `/facilities/consumables` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 행 액션 → DLG-057-001/002/003 |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-057 소모품 재고 관리 — 상태: 정상 (신규 기능)

파일: src/app/(facilities)/consumables/page.tsx

레이아웃:
- AppLayout + PageHeader("소모품 재고 관리", 우측: "소모품 등록" | "발주 생성" 버튼)

1. 통계 카드 (grid grid-cols-5 gap-4 mb-6):
   전체 품목 / 정상 (green) / 주의 (amber) / 부족 (red) / 품절 (red bold)

2. 필터 바:
   - 상태 TabNav: 전체 / 정상 / 주의 / 부족 / 품절
   - 카테고리 Select: 전체 / 청소용품 / 위생용품 / 운동용품 / 기타
   - 검색 Input: 품목명

3. DataTable 컬럼:
   - 품목명 (font-medium)
   - 카테고리 (badge)
   - 현재고 (text-lg font-bold, 상태별 색상)
   - 안전재고 (text-sm text-gray-500)
   - 재고 상태 배지:
     · NORMAL (현재고 >= 안전재고×2): StatusBadge variant="success" "정상"
     · CAUTION (안전재고 <= 현재고 < 안전재고×2): StatusBadge variant="warning" "주의"
     · SHORT (현재고 < 안전재고): StatusBadge variant="error" "부족" + 자동발주 알림 아이콘
     · OUT (현재고 = 0): StatusBadge variant="error" className="font-bold" "품절" + 강조
   - 단위 (text-sm)
   - 최근입고일
   - 액션: "입고" | "출고" | "발주" 버튼

현재고 색상:
- NORMAL: text-green-700
- CAUTION: text-amber-600
- SHORT: text-red-600
- OUT: text-red-700 font-bold

행 배경:
- NORMAL: bg-white
- CAUTION: bg-amber-50/30
- SHORT: bg-red-50/30
- OUT: bg-red-50

데이터: consumables(id, name, category, current_stock, safety_stock, unit, last_received, branch_id)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_OK_01`: API 200 + consumables.length > 0

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 소모품 | `consumables` | `branch_id = ?` |
| 통계 | `consumables` | 상태별 COUNT |

### 인터랙션 (User Actions)
1. "소모품 등록" 버튼 → DLG-057-001 등록 모달
2. "발주 생성" 버튼 → DLG-057-003 발주 모달 (부족 품목 자동 목록)
3. 행 "입고" 버튼 → DLG-057-002 입고 모달
4. 행 "출고" 버튼 → DLG-057-002 출고 모달
5. 행 "발주" 버튼 → DLG-057-003 해당 품목 발주
6. 상태 TabNav → 해당 상태 필터

### 비즈니스 룰
- SHORT 상태: 자동 발주 알림 생성 (관리자 알림)
- OUT 상태: 모든 출고 버튼 비활성화
- 재고 상태 계산: 실시간 (페이지 로드 시)

### 에지 케이스
- 품절 다수: 상단 "품절 품목 N건" 긴급 배너 (bg-red-100 border-red-400)
- 발주 버튼 클릭 후 DLG에서 이미 발주 중인 품목 표시

### 접근성 (A11y)
- 품절 행 `aria-label="품절 - 즉시 발주 필요"`
- 자동발주 아이콘 `aria-label="자동 발주 알림 생성됨"`

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-057-001/002/003 / `03-빈상태`(필터)
