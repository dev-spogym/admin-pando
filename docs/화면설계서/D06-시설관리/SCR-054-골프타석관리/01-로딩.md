# SCR-054 골프타석관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-054 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/golf-bays` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-054 골프 타석 관리 — 상태: 로딩

파일: src/app/(facilities)/golf-bays/page.tsx

Skeleton UI:
1. 통계 카드 5개 (grid grid-cols-5 gap-4 mb-6):
   h-20 rounded-lg bg-gray-200 animate-pulse
2. 타석 그리드 Skeleton (grid grid-cols-4 gap-4):
   - 8개 카드 h-40 rounded-lg bg-gray-200 animate-pulse

useEffect: supabase.from('golf_bays').select('*').eq('branch_id', branchId)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-054 진입 후 API 응답 대기

### 연결 화면
- 다음: `02-정상` / `03-에러`
