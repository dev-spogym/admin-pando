# SCR-051 사물함배정관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/locker/management` |
| 역할 | owner, manager, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함배정관리 — 상태: 로딩

파일: src/app/(facilities)/locker/management/page.tsx

레이아웃:
- AppLayout + PageHeader("사물함 배정 관리")
- 데스크톱 전체 너비

Skeleton UI:
1. 필터 바 Skeleton (h-10 w-full bg-gray-200 animate-pulse rounded mb-4)
2. 사물함 그리드 Skeleton:
   - grid grid-cols-8 gap-3
   - 32개 셀 (h-20 rounded-lg bg-gray-200 animate-pulse)
3. 우측 패널 Skeleton (w-72 h-96 bg-gray-200 animate-pulse rounded)

상태 관리:
- isLoading: true
- useEffect: supabase.from('lockers').select('*, members(name)').eq('branch_id', branchId) 호출
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-051 진입 후 API 응답 대기 중
- 배정 처리 후 목록 갱신 중

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 사물함 목록 | `lockers` | `branch_id = ?` |
| 배정 회원 | `members` | JOIN |

### 인터랙션 (User Actions)
- 로딩 중 모든 버튼 disabled

### 비즈니스 룰
- Skeleton 셀 수는 실제 사물함 수와 동일한 구조 (미리 알 수 없으면 기본 32개)

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러` / `05-오프라인`
