# SCR-087 커스텀역할생성 — 상태: 커스텀 역할 있음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-087 |
| 상태 코드 | `has-custom` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `02-시스템역할만` 또는 `01-로딩` |
| 다음 상태 | 수정/삭제 → DLG |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-087 커스텀역할생성 — 상태: 시스템+커스텀 역할 목록

파일: src/app/settings/permissions/page.tsx

역할 목록 (divide-y divide-gray-100):

[시스템 역할 행] (E_F6_087_06, 수정/삭제 불가):
<div className="flex items-center gap-3 px-4 py-3 bg-gray-50/30">
  <Lock className="w-4 h-4 text-gray-300 shrink-0" />
  <div className="flex-1">
    <p className="text-sm font-medium text-gray-600">{role.name}</p>
  </div>
  <span className="text-xs text-gray-400 bg-gray-100 px-2 py-0.5 rounded-full">시스템</span>
</div>

[커스텀 역할 행] (E_F6_087_07, 수정/삭제 가능):
<div className="flex items-center gap-3 px-4 py-3 hover:bg-gray-50 transition">
  <div className="w-8 h-8 rounded-full bg-primary/10 flex items-center justify-center">
    <UserCog className="w-4 h-4 text-primary" />
  </div>
  <div className="flex-1">
    <p className="text-sm font-medium text-gray-900">{role.name}</p>
    <p className="text-xs text-gray-400">{role.description}</p>
  </div>
  <!-- 직원수 배지 -->
  <span className="text-xs bg-gray-100 text-gray-600 px-2 py-0.5 rounded-full mr-2">
    직원 {role.staff_count}명
  </span>
  <!-- 액션 버튼 -->
  <div className="flex gap-1">
    <Button size="icon" variant="ghost" title="복사" onClick={() => openCopy(role)}>
      <Copy className="w-4 h-4 text-gray-400" />
    </Button>
    <Button size="icon" variant="ghost" title="수정" onClick={() => openEdit(role)}>
      <Pencil className="w-4 h-4 text-gray-400" />
    </Button>
    <Button size="icon" variant="ghost" title="삭제" onClick={() => openDelete(role)}>
      <Trash2 className="w-4 h-4 text-red-400" />
    </Button>
  </div>
</div>

우측 상단: "+ 새 역할 생성" Button (variant=outline)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- custom_roles.length > 0
- 이벤트: `E_F6_087_05`

### 역할 행 구분
| 유형 | 아이콘 | 배지 | 수정/삭제 |
|------|--------|------|---------|
| 시스템 | Lock gray | "시스템" gray | 없음 |
| 커스텀 | UserCog primary | "직원 N명" gray | 수정/삭제/복사 |

### 비즈니스 룰
- 삭제 시 직원 배정 있으면 DLG-087-001 (재배정 모달)
- 삭제 시 직원 없으면 DLG-081-004 (단순 삭제 확인)
- 복사: DLG-081-005

### 연결 화면
- 역할 생성: DLG-081-003
- 역할 삭제 (직원 있음): DLG-087-001
- 역할 삭제 (직원 없음): DLG-081-004
- 역할 복사: DLG-081-005
