# SCR-I005 고정물품락커관리 — 상태: 권한 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I005 |
| 상태 코드 | `forbidden` |
| 경로 | `/(facilities)/locker/fixed` |
| 역할 | (fc — 접근 불가) |
| 우선순위 | P1 |
| 이전 상태 | 인증 체크 권한 없음 |
| 다음 상태 | 대시보드로 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 컴포넌트를 작성하라.
화면: SCR-I005 고정물품락커관리 — 상태: 403 권한 없음

파일: src/app/(facilities)/locker/fixed/page.tsx

권한: manager 이상 (fc 접근 불가)
!hasPermission('locker:fixed:read') → 403 UI

[403 UI]
<div className="flex flex-col items-center justify-center min-h-[60vh] text-center">
  <div className="w-20 h-20 rounded-full bg-red-50 flex items-center justify-center mx-auto mb-6">
    <svg ... className="w-10 h-10 text-red-400" />
  </div>
  <h2 className="text-xl font-bold text-gray-800">접근 권한이 없습니다</h2>
  <p className="mt-2 text-sm text-gray-500">
    고정 물품 락커 관리는 매니저(manager) 이상 권한이 필요합니다.
  </p>
  <button onClick={() => moveToPage('/dashboard')}
    className="mt-6 px-5 py-2 bg-blue-600 text-white text-sm rounded-lg">
    대시보드로 이동
  </button>
</div>
```

## 📝 디스크립션

### 비즈니스 룰
- fc 역할: 사이드바 메뉴 숨김 + 직접 URL 접근 시 403

### 연결 화면
- 다음: `/dashboard`
