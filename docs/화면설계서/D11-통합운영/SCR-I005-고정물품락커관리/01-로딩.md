# SCR-I005 고정물품락커관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I005 |
| 상태 코드 | `loading` |
| 경로 | (미구현 — 예상: `/locker/management` 또는 `/(facilities)/locker/fixed`) |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P0 |
| 이전 상태 | 인증/권한 확인 완료 직후 |
| 다음 상태 | `02-정상` / `03-빈상태` / `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I005 고정물품락커관리 — 상태: 로딩 (스켈레톤)

파일: src/app/(facilities)/locker/fixed/page.tsx (신규 생성)

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "고정 물품 락커 관리"

스켈레톤 구성 (animate-pulse):

[요약 KPI — 4칸]
- 전체/사용중/만료임박/회수대상 카드 각 (h-20 bg-gray-100 rounded-xl)

[필터 바]
- 검색창 스켈레톤 (h-9 w-60 bg-gray-100 rounded)
- 상태 드롭다운 스켈레톤 (h-9 w-32 bg-gray-100 rounded)

[배정 목록 DataTable — 8행]
- 헤더 6컬럼 바 (h-4 bg-gray-200)
- 행 8줄 × 6셀 (h-4 bg-gray-100 rounded)

구현:
- useEffect(() => { loadFixedLockerData() }, [])
- Promise.all([fetchAssignments, fetchExpiringSoon]) 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I005 진입 시
- 배정/반납 후 목록 재조회 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 배정 목록 | `lockers` (type='fixed') JOIN `members` | `branch_id = ?` |
| 만료 임박 | `lockers` | `expires_at <= NOW() + 7days` |
| 회수 대상 | `lockers` | `expires_at < NOW()` |

### 연결 화면
- 이전: 사이드바 시설 메뉴
- 다음: `02-정상` / `03-빈상태` / `06-에러`
