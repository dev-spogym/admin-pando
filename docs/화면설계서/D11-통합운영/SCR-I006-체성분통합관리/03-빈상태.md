# SCR-I006 체성분통합관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I006 |
| 상태 코드 | `empty` |
| 경로 | `/body-composition` |
| 역할 | superAdmin, primary, owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (데이터 0건) |
| 다음 상태 | 수기 등록 → DLG-I003 / 파일 업로드 → `07-파싱중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I006 체성분통합관리 — 상태: 빈 상태

파일: src/app/body-composition/page.tsx (조건부 렌더링)

조건: measurements.length === 0 (로딩 완료 후)

[빈 상태 UI]
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center mb-5">
    <svg ... className="w-10 h-10 text-gray-300" /> {/* 체성분/그래프 아이콘 */}
  </div>
  <p className="text-lg font-semibold text-gray-600">측정 데이터가 없습니다</p>
  <p className="mt-2 text-sm text-gray-400 max-w-sm">
    InBody 장비 연동 또는 수기 등록으로 체성분 데이터를 추가하세요.
  </p>
  {hasPermission('bodyComposition:write') && (
    <div className="flex gap-3 mt-6">
      <button onClick={openManualModal}
        className="px-5 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700">
        수기 등록
      </button>
      <label className="px-5 py-2.5 bg-white border border-gray-300 text-gray-700 text-sm font-medium rounded-lg cursor-pointer hover:bg-gray-50">
        파일 업로드
        <input type="file" accept=".csv,.xlsx" className="hidden" onChange={handleFileChange} />
      </label>
    </div>
  )}
</div>

필터 결과 0건 분기:
- "해당 기간에 측정 데이터가 없습니다" + 필터 초기화 버튼
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 지점 최초 접근 (측정 데이터 미등록)
- 필터 결과 0건

### 인터랙션
1. "수기 등록" → DLG-I003
2. "파일 업로드" → 파일 선택 → `07-파싱중`
3. `bodyComposition:write` 없으면 버튼 숨김

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-I003 / `07-파싱중` / 등록 완료 후 `02-정상`
