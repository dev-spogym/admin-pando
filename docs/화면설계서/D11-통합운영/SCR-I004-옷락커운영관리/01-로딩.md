# SCR-I004 옷락커운영관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I004 |
| 상태 코드 | `loading` |
| 경로 | `/(facilities)/clothing` 또는 `/(facilities)/locker` |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P0 |
| 이전 상태 | 인증/권한 확인 완료 직후 |
| 다음 상태 | `02-정상` / `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I004 옷락커운영관리 — 상태: 로딩 (스켈레톤)

파일: src/app/(facilities)/locker/page.tsx

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "옷 락커 운영 관리" + 우측 버튼 영역 스켈레톤

스켈레톤 구성 (animate-pulse):

[요약 KPI 영역 — 3칸]
- 전체/사용중/빈 락커 카드 각 (h-20 w-full bg-gray-100 rounded-xl)

[탭 바 스켈레톤]
- 3개 탭 바 (h-10 w-24 bg-gray-100 rounded)

[락커 그리드 영역 — grid grid-cols-8 gap-2]
- 64개 셀 (h-16 w-full bg-gray-100 rounded-lg)

구현:
- useEffect(() => { loadLockerData() }, [])
- Promise.all([fetchLockers, fetchLockerStatus, fetchIotStatus]) 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I004 진입 시
- 배정 완료 후 목록 재조회 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 락커 목록 | `lockers` | `branch_id = ?`, `type = 'clothing'` |
| 배정 정보 | `clothing` | `locker_id IN (...)` |
| IoT 상태 | `iot_devices` | `type = 'locker_controller'` |

### 연결 화면
- 이전: 사이드바 시설 메뉴
- 다음: `02-정상` / `04-IoT장애` / `05-에러`
