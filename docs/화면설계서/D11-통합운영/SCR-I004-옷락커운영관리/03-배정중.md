# SCR-I004 옷락커운영관리 — 상태: 배정 처리 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I004 |
| 상태 코드 | `assigning` |
| 경로 | `/(facilities)/locker` |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | DLG-I002 저장 클릭 후 API 호출 중 |
| 다음 상태 | 배정 완료 → `02-정상` 갱신 / 실패 → `02-정상` 유지 + 에러 토스트 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I004 옷락커운영관리 — 상태: 배정 처리 중

파일: src/app/(facilities)/locker/page.tsx

배정 중 상태:
const [assigningLockerId, setAssigningLockerId] = useState<string | null>(null)

배정 처리 중인 락커 셀 스타일:
- assigningLockerId === locker.id 인 셀:
  bg-blue-100 border-blue-400 pointer-events-none relative
  + 중앙 스피너 오버레이:
    <div className="absolute inset-0 flex items-center justify-center bg-blue-50/70 rounded-lg">
      <svg className="animate-spin w-5 h-5 text-blue-600" />
    </div>

나머지 락커 셀: pointer-events-none opacity-60 (배정 중 전체 잠금)

배정 완료 후:
1. setAssigningLockerId(null)
2. loadLockerData() // 그리드 갱신
3. toast.success('락커 {번호} 배정 완료')

배정 실패 후:
1. setAssigningLockerId(null)
2. 락커 상태 롤백 (UI 원상복구)
3. toast.error('배정에 실패했습니다')
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- DLG-I002에서 "배정 확인" 클릭 → POST /locker/assign API 호출 중

### 인터랙션
- 배정 중 전체 그리드 클릭 차단
- 배정 완료: 자동으로 `02-정상` 복귀 + 해당 셀 사용중으로 전환
- 배정 실패: `02-정상` 유지 + 에러 토스트

### 비즈니스 룰
- Optimistic UI 미적용 (API 완료 후 UI 갱신)
- 배정 중 다른 락커 클릭 방지 (전체 잠금)
- 타임아웃 10초: 초과 시 실패 처리

### 연결 화면
- 이전: DLG-I002 (배정 확인 클릭)
- 다음: 완료 → `02-정상` / 실패 → `02-정상` + 에러
