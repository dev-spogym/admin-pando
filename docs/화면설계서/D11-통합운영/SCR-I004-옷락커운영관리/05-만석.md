# SCR-I004 옷락커운영관리 — 상태: 만석 (모든 락커 사용 중)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I004 |
| 상태 코드 | `full` |
| 경로 | `/(facilities)/locker` |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (모든 락커 사용중) |
| 다음 상태 | 반납 처리 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I004 옷락커운영관리 — 상태: 만석

파일: src/app/(facilities)/locker/page.tsx

조건: lockers.every(l => l.status === 'occupied' || l.status === 'maintenance')
      && lockers.filter(l => l.status === 'available').length === 0

[만석 배너 — PageHeader 아래]
<div className="flex items-center gap-3 bg-red-50 border border-red-200 rounded-lg px-4 py-3 mb-5">
  <svg ... className="w-5 h-5 text-red-500 flex-shrink-0" />
  <div>
    <p className="text-sm font-semibold text-red-700">모든 락커가 사용 중입니다</p>
    <p className="text-xs text-red-500 mt-0.5">
      새 배정을 하려면 기존 락커를 반납 처리해 주세요.
    </p>
  </div>
</div>

[KPI]
- 빈 락커: <span className="text-red-600 font-bold">0개</span>
- 대기 중 회원: {waitingCount}명 (있을 경우)

[락커 그리드]
- 모든 셀: 사용중 스타일 (bg-blue-50 border-blue-200)
- 빈 락커 없으므로 배정 클릭 불가 (셀 전체 pointer-events-none)
- 사용중 셀 클릭: 반납/연장 모달 표시

[만료 임박 셀 강조]
- D-3 이내: bg-amber-100 border-amber-400 → 반납 권장

[안내 메시지]
<div className="mt-4 text-center text-sm text-gray-500">
  만료 임박 배정을 먼저 반납 처리하면 빈 락커가 생깁니다.
</div>

사용중 셀 클릭 시 모달:
- 배정 정보: 회원명, 배정일, 만료일
- 버튼: "반납 처리" (POST /locker/return) | "만료일 연장"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `lockers.filter(l => l.status === 'available').length === 0`
- 마지막 빈 락커 배정 후 자동 전환

### 필수 데이터
- 락커 전체 목록 + 배정 현황
- 만료 임박 락커 목록 (D-7 이내)

### 인터랙션 (User Actions)
1. 사용중 셀 클릭 → 반납/연장 모달
2. 반납 처리 → `01-로딩` 재실행 → `02-정상`

### 비즈니스 룰
- 만석 시 수동 배정 불가 (DLG-I002 열기 차단)
- 점검중 락커는 만석 계산에서 제외 (별도 관리)
- 대기 회원 목록 연동 (향후 기능)

### 에지 케이스
- 점검중 + 전체 사용중: 만석 배너 + "일부 점검중" 안내
- 만료 즉시 반납: KPI 실시간 갱신

### 연결 화면
- 이전: `02-정상` (마지막 배정 후)
- 다음: 반납 처리 → `02-정상`
