# SCR-I003 IoT연동관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I003 |
| 상태 코드 | `loading` |
| 경로 | `/settings/iot` |
| 역할 | superAdmin, primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 인증/권한 확인 완료 직후 |
| 다음 상태 | `02-정상` / `03-빈상태` / `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I003 IoT연동관리 — 상태: 로딩 (스켈레톤)

파일: src/app/settings/iot/page.tsx

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "IoT 연동 관리" + 우측 "장비 추가" 버튼 스켈레톤

스켈레톤 구성 (animate-pulse):

[요약 배지 영역]
- 3개 배지 스켈레톤 (h-8 w-24 bg-gray-100 rounded-full)

[장비 목록 카드 그리드 — grid grid-cols-3 gap-4]
- 6개 카드 (bg-gray-100 rounded-xl h-36):
  · 상단 아이콘 영역 (h-8 w-8 bg-gray-200 rounded-full)
  · 장비명 바 (h-4 w-28 bg-gray-200 mt-3)
  · 상태 바 (h-3 w-16 bg-gray-100 mt-2)
  · 마지막 통신 바 (h-3 w-20 bg-gray-100 mt-1)

구현:
- useEffect(() => { loadIotDevices() }, [])
- useState: loading = true 초기값
- Promise.all([fetchDevices, fetchDeviceStatus]) 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I003 진입 시
- 장비 추가/삭제 후 목록 재조회 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 장비 목록 | `iot_devices` | `branch_id = ?` |
| 실시간 상태 | IoT 상태 API | 각 장비 ping |

### 연결 화면
- 이전: 사이드바 설정 메뉴 / SCR-I001 IoT 배지 클릭
- 다음: `02-정상` / `03-빈상태` / `05-에러`
