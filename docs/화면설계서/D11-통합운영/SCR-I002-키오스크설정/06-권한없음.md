# SCR-I002 키오스크설정 — 상태: 권한 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I002 |
| 상태 코드 | `forbidden` |
| 경로 | `/settings/kiosk` |
| 역할 | (manager, fc, staff — 접근 불가) |
| 우선순위 | P1 |
| 이전 상태 | 인증 체크 결과 권한 없음 |
| 다음 상태 | 대시보드로 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 컴포넌트를 작성하라.
화면: SCR-I002 키오스크설정 — 상태: 403 권한 없음

파일: src/app/settings/kiosk/page.tsx

접근 권한: superAdmin, primary, owner만 허용
조건: !hasPermission('kiosk:read') → 403 렌더

[403 UI]
<div className="flex flex-col items-center justify-center min-h-[60vh] text-center">
  <div className="w-20 h-20 rounded-full bg-red-50 flex items-center justify-center mx-auto mb-6">
    <svg ... className="w-10 h-10 text-red-400" />
  </div>
  <h2 className="text-xl font-bold text-gray-800">접근 권한이 없습니다</h2>
  <p className="mt-2 text-sm text-gray-500">
    키오스크 설정은 원장(owner) 이상 권한이 필요합니다.
  </p>
  <button
    onClick={() => moveToPage('/dashboard')}
    className="mt-6 px-5 py-2 bg-blue-600 text-white text-sm rounded-lg"
  >
    대시보드로 이동
  </button>
</div>

권한 체크 순서:
1. !session → redirect('/login')
2. !hasPermission('kiosk:read') → 403 UI 렌더
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- manager, fc, staff 역할로 `/settings/kiosk` 직접 접근
- URL 직접 입력 시

### 비즈니스 룰
- 사이드바에서 manager 이하는 키오스크 설정 메뉴 숨김 처리 (nav 레벨 권한 체크)
- 직접 URL 접근 시에만 403 UI 노출

### 연결 화면
- 이전: 직접 URL 입력
- 다음: `/dashboard`
