# SCR-M006 체성분관리 — 상태: 에러

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M006 |
| 상태 코드 | `error` |
| 경로 | `/body-composition?memberId={id}` |
| 역할 | primary / owner / manager / fc |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M006_체성분관리/F6_상태별화면.md` |
| 이전 상태 | `01-로딩중` |
| 다음 상태 | `01-로딩중` (재시도) |

## 🧩 바이브코딩 프롬프트
```
화면: SCR-M006 체성분관리 — 상태: 에러

<div role="alert" className="rounded-lg border border-red-200 bg-red-50 p-4 m-4">
  <p className="text-sm text-red-700">데이터를 불러오지 못했습니다.</p>
  <button onClick={refetch} className="mt-2 text-xs text-red-600 underline">
    다시 시도
  </button>
</div>
```

## 📝 디스크립션

### 사용 시점
- GET /api/body-compositions API 오류

### 다이어그램 참조
- 엣지: `E_STATE_ERR_01`
- 상태: `UI_ERR`
