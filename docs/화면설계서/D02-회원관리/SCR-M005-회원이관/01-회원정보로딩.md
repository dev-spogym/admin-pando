# SCR-M005 회원이관 — 상태: 회원 정보 로딩 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M005 |
| 상태 코드 | `member-loading` |
| 경로 | `/members/transfer?id={id}` |
| 역할 | primary / owner |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M005_회원이관/F6_상태별화면.md` |
| 이전 상태 | 진입 |
| 다음 상태 | `02-체크리스트로딩` / `03-null` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M005 회원이관 — 상태: 회원 정보 로딩 중

파일: src/app/members/transfer/page.tsx

레이아웃: AppLayout > PageHeader("회원 이관") + 중앙 로딩

컴포넌트:
<div className="flex flex-col items-center justify-center min-h-[60vh]">
  <Loader2 className="w-8 h-8 animate-spin text-blue-500 mb-2" />
  <p className="text-sm text-gray-400">회원 정보를 불러오는 중...</p>
</div>

권한: primary/owner만 접근 (hasPermission 체크)

사용 유틸: useAuthStore, hasPermission, supabase, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점
- `/members/transfer?id={id}` 마운트 직후

### 비즈니스 룰
- primary/owner만 접근 가능 (manager 이하 권한 없음 화면)

### 다이어그램 참조
- 엣지: `E_STATE_MEMBER_LOADING_01`
