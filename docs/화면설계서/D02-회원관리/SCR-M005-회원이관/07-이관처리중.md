# SCR-M005 회원이관 — 상태: 이관 처리 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M005 |
| 상태 코드 | `submitting` |
| 경로 | `/members/transfer?id={id}` |
| 역할 | primary / owner |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M005_회원이관/F6_상태별화면.md` |
| 이전 상태 | DLG-M023 확인 후 |
| 다음 상태 | SCR-M001 (성공) / `06-이관가능` (실패) |

## 🧩 바이브코딩 프롬프트
```
화면: SCR-M005 회원이관 — 상태: 이관 처리 중 (submitting=true)

컴포넌트:
- 이관 설정 폼: disabled
- "이관 처리 중..." 버튼:
  <Button disabled>
    <Loader2 className="w-4 h-4 animate-spin mr-2" />
    이관 처리 중...
  </Button>

API: PUT /api/members/{id} + POST /api/member-transfer-logs
성공: toast.success + /members 이동
실패: toast.error + 폼 복귀
```

## 📝 디스크립션

### 사용 시점
- DLG-M023에서 "이관 확인" 클릭 후

### 다이어그램 참조
- 엣지: `E_STATE_SUBMITTING_01`
- 상태: `UI_SUBMITTING`
