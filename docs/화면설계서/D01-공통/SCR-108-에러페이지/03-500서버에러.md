# SCR-108 에러 페이지 — 상태: 500 서버 오류

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-108 |
| 상태 코드 | `error-500` |
| 경로 | `/error` 또는 Next.js `error.tsx` |
| 역할 | all |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-108_에러페이지/F6_상태별.md` |
| 이전 상태 | 서버 오류 발생 |
| 다음 상태 | SCR-101 대시보드 (재시도 성공) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-108 에러 페이지 — 상태: 500 서버 오류

파일: src/app/error.tsx (Next.js Error Boundary)

레이아웃:
- AppLayout 포함 또는 단독
- min-h-screen flex items-center justify-center

컴포넌트 구조:
1. 에러 코드 (text-8xl font-black text-gray-200): "500"
2. ServerCrash 아이콘 (w-16 h-16 text-red-400 mx-auto)
3. 제목: "서버 오류가 발생했습니다" (text-2xl font-bold text-gray-700 mt-4)
4. 설명: "일시적인 오류입니다. 잠시 후 다시 시도해주세요." (text-sm text-gray-500)
5. 버튼 그룹:
   - "다시 시도" Button (primary) → reset() (Next.js error.tsx reset prop)
   - "대시보드로 이동" Button (ghost) → moveToPage('/')
6. 문의 링크 (text-xs): "문제가 계속되면 관리자에게 문의하세요."

데이터:
- error: Error (Next.js error.tsx props)
- reset: () => void (Next.js error.tsx props)
- 에러 ID: crypto.randomUUID() (문의 시 참조용, 선택적)

인터랙션:
- "다시 시도" → reset() → 이전 화면 재시도
- "대시보드로 이동" → /

사용 유틸:
- moveToPage, lucide-react (ServerCrash, RefreshCw, Home)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Next.js Error Boundary 트리거 (런타임 에러)
- API 500/502/504 응답 후 페이지 레벨 에러

### 필수 데이터
- error (Error 객체, 로깅용)
- reset (재시도 함수)

### 인터랙션 (User Actions)
1. "다시 시도" → reset() 실행 → 이전 컴포넌트 재렌더
2. "대시보드로 이동" → SCR-101

### 비즈니스 룰
- 에러 상세 메시지 사용자에게 노출 금지 (보안)
- 에러 로깅: Supabase 또는 Sentry로 자동 전송
- 에러 ID 표시로 문의 시 추적 가능

### 에지 케이스
- 재시도 반복 실패 → 동일 에러 페이지 유지
- 대시보드 이동 후에도 에러 → 루트 에러 핸들러 동작

### 접근성 (A11y)
- `<h1>` "서버 오류가 발생했습니다"
- 재시도 버튼 `aria-label="페이지 다시 시도"`

### 연결 화면
- 재시도 성공: 이전 화면
- 대시보드: SCR-101

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-108_에러페이지/F6_상태별.md` → STATE_SERVER 노드
