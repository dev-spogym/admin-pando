# DLG-S011 세금계산서발행 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S011 |
| 상태 코드 | `open` |
| 경로 | DLG-S010 위 중첩 모달 |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | DLG-S010 [발행] 클릭 |
| 다음 상태 | [발행 확인] → `02-처리중` / [취소] → 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S011_세금계산서발행/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S011 세금계산서발행 — 상태: 열림 (신규 기획)

파일: src/app/tax-invoices/page.tsx 내 TaxInvoiceIssueModal

모달 크기: 400px × 360px (DLG-S010 위 중첩)

<Modal isOpen={showIssueModal} onClose={() => setShowIssueModal(false)} title="세금계산서 발행">
  <div className="p-6 space-y-4">
    {/* 발행 요약 */}
    <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-4 space-y-2">
      <div className="flex items-start gap-2">
        <AlertTriangle className="h-4 w-4 text-yellow-600 mt-0.5 shrink-0" />
        <p className="text-sm text-yellow-800">
          발행 후에는 취소가 어렵습니다. 내용을 확인 후 진행하세요.
        </p>
      </div>
    </div>

    <div className="space-y-2">
      <div className="flex justify-between text-sm">
        <span className="text-gray-500">거래처명</span>
        <span className="font-medium">{taxInvoice.company_name}</span>
      </div>
      <div className="flex justify-between text-sm">
        <span className="text-gray-500">공급가액</span>
        <span className="font-medium">{formatKRW(taxInvoice.supply_amount)}</span>
      </div>
      <div className="flex justify-between text-sm">
        <span className="text-gray-500">세액</span>
        <span className="font-medium">{formatKRW(taxInvoice.tax_amount)}</span>
      </div>
      <div className="flex justify-between text-sm font-bold border-t pt-2">
        <span>합계</span>
        <span className="text-blue-700">{formatKRW(taxInvoice.total_amount)}</span>
      </div>
    </div>

    {/* 발행일 */}
    <div>
      <label className="block text-sm font-medium text-gray-700 mb-1">발행일</label>
      <input
        type="date"
        value={issueDate}
        onChange={(e) => setIssueDate(e.target.value)}
        className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </div>
  </div>

  <div className="p-4 border-t flex gap-3">
    <Button variant="outline" className="flex-1" onClick={() => setShowIssueModal(false)}>
      취소
    </Button>
    <Button variant="primary" className="flex-1" onClick={handleIssue}>
      발행 확인
    </Button>
  </div>
</Modal>

const [issueDate, setIssueDate] = useState(new Date().toISOString().split('T')[0]);
사용 아이콘: AlertTriangle from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_OPEN_01: DLG-S010 [발행] 버튼 클릭

### 필수 데이터
| 항목 | 설명 |
|------|------|
| taxInvoice | 세금계산서 상세 |
| issueDate | 발행일 (기본값: 오늘) |

### 인터랙션 (User Actions)
1. 발행일 변경
2. [발행 확인] → `02-처리중`
3. [취소] / X → 닫힘

### 비즈니스 룰
- 발행 전 경고 문구 표시 (취소 어려움)
- 발행일 기본값: 오늘

### 연결 화면
- `02-처리중`
- 닫힘 (취소)
