# SCR-S009 할부결제관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S009 |
| 상태 코드 | `loading` |
| 경로 | `/installments` |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `03-빈상태` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S009_할부결제관리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S009 할부결제관리 — 상태: 로딩 (신규 기획 초안)

파일: src/app/installments/page.tsx (isLoading=true 분기)
※ 미구현 화면 — 다이어그램 기반 작성

레이아웃:
- AppLayout 풀너비 데스크톱
- PageHeader: "할부 결제 관리" + [할부 등록] 버튼 (disabled)

스켈레톤:
1. StatCardGrid 3개: animate-pulse bg-gray-200 h-24 rounded-xl
2. DataTable 5행: animate-pulse bg-gray-100 h-12 rounded mb-1

데이터 로드:
// 가상 테이블: installments
- supabase.from('installments').select('*').eq('branch_id', branchId)

aria-busy="true" aria-label="할부 결제 데이터 로딩 중"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-S009 진입 시 할부 목록 로드 중
- 신규 기획 초안 (미구현 → 다이어그램 기반 작성)

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
