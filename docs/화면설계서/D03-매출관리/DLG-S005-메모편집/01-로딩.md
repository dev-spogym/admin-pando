# DLG-S005 메모편집 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S005 |
| 상태 코드 | `loading` |
| 경로 | SCR-S008 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | SCR-S008 Pencil 버튼 클릭 |
| 다음 상태 | `02-열림-메모있음` / `03-열림-빈메모` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S005_메모편집/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S005 메모편집 — 상태: 로딩 (기존 메모 조회 중)

파일: src/app/unpaid/page.tsx 내 MemoEditModal

모달 크기: 400px × 360px

로딩 UI:
<Modal isOpen={isOpen} onClose={onClose} title="메모 편집">
  <div className="p-6">
    <div className="animate-pulse bg-gray-200 h-32 rounded-lg w-full mb-4" />
    <div className="flex gap-3">
      <div className="animate-pulse bg-gray-200 h-9 flex-1 rounded" />
      <div className="animate-pulse bg-gray-200 h-9 flex-1 rounded" />
    </div>
  </div>
</Modal>

데이터 로드:
const fetchMemo = async (unpaidId: number) => {
  setIsMemoLoading(true);
  const { data } = await supabase
    .from('sales')  // unpaid의 memo 필드 사용
    .select('memo')
    .eq('id', unpaidId)
    .single();
  setMemoText(data?.memo ?? '');
  setIsMemoLoading(false);
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_OPEN_01: SCR-S008 Pencil 버튼 클릭 → 기존 메모 조회 중

### 연결 화면
- 메모 있음 → `02-열림-메모있음`
- 메모 없음 → `03-열림-빈메모`
