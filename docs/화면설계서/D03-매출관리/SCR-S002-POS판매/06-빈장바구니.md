# SCR-S002 POS판매 — 상태: 빈 장바구니

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S002 |
| 상태 코드 | `cart-empty` |
| 경로 | `/pos` |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | 진입 시 초기 / 전체 삭제 후 |
| 다음 상태 | 상품 클릭 → `05-장바구니-채워짐` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S002_POS판매/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S002 POS판매 — 상태: 빈 장바구니

파일: src/app/pos/page.tsx (cartItems.length === 0 분기)

우측 장바구니 패널 빈 상태:
<div className="flex flex-col h-full">
  <div className="flex items-center gap-2 p-4 border-b">
    <ShoppingCart className="h-5 w-5 text-gray-400" />
    <span className="font-semibold text-gray-600">장바구니</span>
  </div>
  
  {/* 빈 상태 메시지 */}
  <div className="flex flex-col items-center justify-center flex-1 text-center px-6">
    <ShoppingCart className="h-12 w-12 text-gray-200 mb-3" />
    <p className="text-sm font-medium text-gray-400">장바구니가 비어 있습니다.</p>
    <p className="text-xs text-gray-300 mt-1 opacity-50">
      왼쪽에서 상품을 선택해주세요.
    </p>
  </div>
  
  {/* 하단 결제 버튼: disabled */}
  <div className="p-4 border-t">
    <div className="flex justify-between mb-2">
      <span className="text-sm text-gray-400">총 금액</span>
      <span className="font-bold text-gray-300">0원</span>
    </div>
    <Button className="w-full" variant="primary" disabled>
      결제하기
    </Button>
  </div>
</div>

조건: cartItems.length === 0
결제하기 버튼: disabled + opacity-50 + cursor-not-allowed
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 최초 POS 진입 시 (cartItems=[])
- 전체 삭제 버튼 클릭 후
- 결제 완료 후 장바구니 초기화

### 인터랙션 (User Actions)
- 상품 카드 클릭 → 장바구니에 아이템 추가 → `05-장바구니-채워짐`

### 비즈니스 룰
- 결제하기 버튼 disabled (클릭 불가)
- 총 금액 0원 표시

### 연결 화면
- 상품 클릭 → `05-장바구니-채워짐`
