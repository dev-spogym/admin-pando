# DLG-S013 환불처리 — 상태: 처리중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S013 |
| 상태 코드 | `processing` |
| 경로 | SCR-S007 위 모달 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` [환불 확인] 클릭 |
| 다음 상태 | `03-성공` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S013_환불처리/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S013 환불처리 — 상태: 처리중 (E_CONFIRM_01)

파일: src/app/refunds/page.tsx 내 RefundProcessModal

처리중 UI:
<div className="p-4 border-t flex gap-3">
  <Button variant="outline" className="flex-1" disabled>취소</Button>
  <Button variant="danger" className="flex-1" disabled>
    <Loader2 className="h-4 w-4 animate-spin mr-2" />
    처리중...
  </Button>
</div>

처리 로직:
const handleConfirm = async () => {
  setIsProcessing(true);
  const { error } = await supabase
    .from('refunds')
    .update({
      status: 'APPROVED',
      refund_amount: refundAmount,
      refund_type: refundType,
      reason: refundReason,
      processed_at: new Date().toISOString(),
      processed_by: userId,
    })
    .eq('id', refund.id);

  if (error) {
    toast.error('환불 처리에 실패했습니다.');
    setIsProcessing(false);
  } else {
    onClose();
    onRefresh();
    toast.success('환불이 처리되었습니다.');
  }
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_CONFIRM_01: [환불 확인] 클릭 → refunds UPDATE 호출 중

### 비즈니스 룰
- status → APPROVED
- refund_amount, refund_type, reason, processed_at, processed_by 기록
- 모든 버튼 비활성화

### 연결 화면
- `03-성공` (UPDATE 성공)
- `04-에러` (UPDATE 실패)
