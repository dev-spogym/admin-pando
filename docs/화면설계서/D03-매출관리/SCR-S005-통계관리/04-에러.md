# SCR-S005 통계관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S005 |
| 상태 코드 | `error` |
| 경로 | `/sales/statistics-management` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S005_통계관리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-S005 통계관리 — 상태: 에러

파일: src/app/sales/statistics-management/page.tsx (error !== null 분기)

에러 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <AlertCircle className="h-12 w-12 text-red-400 mb-3" />
  <p className="text-base font-semibold text-gray-700">통계를 불러오지 못했습니다.</p>
  <Button variant="outline" className="mt-4" onClick={fetchStats}>
    <RefreshCw className="h-4 w-4 mr-2" /> 다시 시도
  </Button>
</div>

toast.error('통계 데이터를 불러오지 못했습니다.')
console.error('[SCR-S005]', error)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- API 오류 / 네트워크 실패

### 인터랙션
1. [다시 시도] → fetchStats()

### 연결 화면
- 재시도 → `01-로딩`
