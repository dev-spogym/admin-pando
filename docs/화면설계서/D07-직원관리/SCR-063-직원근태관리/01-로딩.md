# SCR-063 직원근태관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-063 |
| 상태 코드 | `loading` |
| 경로 | `/staff/attendance` |
| 역할 | primary / owner / manager |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 성공 → `02-일별뷰`, 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-063 직원 근태 관리 — 상태: 로딩

파일: src/app/staff/attendance/page.tsx

조건: isLoadingData=true (페이지 진입 직후)

레이아웃:
- AppLayout + PageHeader: title="직원 근태 관리"
- 데스크톱 전용 (min-w-[1024px])

상단:
- TabNav: [일별 | 월별] — disabled (로딩 중)
- 날짜 선택기 (DatePicker): 오늘 날짜 기본값, disabled

통계 카드 영역 (4개) — 스켈레톤:
  <div className="grid grid-cols-4 gap-4 mb-6">
    {[...Array(4)].map((_, i) => (
      <div key={i} className="h-20 rounded-xl bg-gray-100 animate-pulse" />
    ))}
  </div>

DataTable 스켈레톤 (6행):
  <div className="space-y-2">
    {[...Array(6)].map((_, i) => (
      <div key={i} className="h-12 rounded-lg bg-gray-100 animate-pulse" />
    ))}
  </div>

fetch:
  Promise.all([
    supabase.from('staff').select('id, name, department, position').eq('staff_status', 'ACTIVE'),
    supabase.from('staff_attendance').select('*').eq('date', today),
  ])

상태 변수:
- isLoadingData: true
- selectedDate: today
- viewMode: 'daily' | 'monthly'

사용 컴포넌트:
- AppLayout, PageHeader, TabNav
- useAuthStore, supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/staff/attendance` 진입 즉시
- 날짜 변경 후 재fetch 중에도 동일 로딩 UI

### 인터랙션 (User Actions)
- 없음 (로딩 중 모든 액션 비활성)

### 비즈니스 룰
- 진입 권한: `hasPermission('attendance:view')` — primary/owner/manager
- 기본 날짜: 오늘 (서버 기준 KST)

### 연결 화면
- 다음: `02-일별뷰` / `05-에러`
