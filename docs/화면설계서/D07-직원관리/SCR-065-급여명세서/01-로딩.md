# SCR-065 급여명세서 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-065 |
| 상태 코드 | `loading` |
| 경로 | `/payroll/statements` |
| 역할 | primary / owner / manager (hasPermission('payroll:view') 필수) |
| 우선순위 | P0 |
| 이전 상태 | SCR-064 "급여 명세서" 버튼 클릭 또는 직접 진입 |
| 다음 상태 | 성공+있음 → `02-정상`, 없음 → `03-빈상태`, 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-065 급여 명세서 — 상태: 로딩

파일: src/app/payroll/statements/page.tsx

권한 체크:
  if (!hasPermission('payroll:view')) return <PermissionDenied />

조건: isLoadingData=true

레이아웃:
- AppLayout + PageHeader: title="급여 명세서"
- 데스크톱 전용

상단:
- MonthPicker: 이번 달 기본, disabled
- SearchFilter: 직원 이름 검색, disabled
- "전체 발송" 버튼: disabled
- "PDF 일괄 생성" 버튼: disabled

DataTable 스켈레톤 (5행)
통계 카드 3개 스켈레톤

fetch:
  supabase.from('payroll_statements')
    .select('*, staff(name, department, position, email)')
    .eq('year_month', selectedMonth)
    .order('created_at', { ascending: false })

상태 변수:
- isLoadingData: true
- selectedMonth: format(new Date(), 'yyyy-MM')
- statements: []

사용 컴포넌트:
- AppLayout, PageHeader, Button
- useAuthStore, hasPermission, supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/payroll/statements` 진입 직후
- MonthPicker / 검색 변경 후 재fetch 중

### 권한
- hasPermission('payroll:view'): primary / owner / manager
- FC / STAFF: 접근 불가 → PermissionDenied

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `05-에러`
