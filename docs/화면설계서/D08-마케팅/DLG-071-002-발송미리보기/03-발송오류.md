# DLG-071-002 발송미리보기 — 상태: 발송 오류

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-071-002 |
| 상태 코드 | `send-error` |
| 트리거 | POST /messages → 402/500/timeout |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 모달 유지 (재시도) |

## 🧩 바이브코딩 프롬프트

```
발송 오류 처리 (MessagePreviewModal.tsx):

402 포인트 부족:
- toast.error("포인트가 부족합니다. 충전 후 다시 시도하세요.")
- 모달 유지
- 버튼 loading 해제

500 서버 오류:
- toast.error("발송에 실패했습니다. 잠시 후 다시 시도하세요.")
- 모달 유지

타임아웃:
- toast.warning("요청 시간이 초과되었습니다.")
- 모달 유지

catch (err: any) {
  if (err.status === 402) {
    toast.error("포인트가 부족합니다.")
  } else {
    toast.error("발송에 실패했습니다.")
  }
  setIsSending(false)
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 402: 포인트 부족
- 500: 서버 오류
- timeout: 네트워크 지연

### 인터랙션 (User Actions)
1. "발송하기" 재클릭 → 재시도
2. "취소" → 모달 닫힘

### 연결 화면
- 다음: 재시도 → `02-발송성공` 또는 반복 오류
