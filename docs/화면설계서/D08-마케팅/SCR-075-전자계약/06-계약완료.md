# SCR-075 전자계약 — 상태: 계약 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-075 |
| 상태 코드 | `completed` |
| 경로 | `/contracts/new` |
| 역할 | owner, manager, fc |
| 우선순위 | P0 |
| 이전 상태 | `05-Step5-서명` (제출 성공) |
| 다음 상태 | 계약 목록 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-075 전자계약 — 상태: 계약 완료

파일: src/app/(marketing)/contracts/new/page.tsx

계약 완료 UI:
- AppLayout + PageHeader("전자 계약 작성")
- 본문 중앙 flex flex-col items-center py-16:

  완료 애니메이션:
  <div className="w-20 h-20 rounded-full bg-green-100 flex items-center justify-center mb-6">
    <CheckCircle2 className="w-12 h-12 text-green-500" />
  </div>

  <h2 className="text-2xl font-bold text-gray-800 mb-2">계약이 완료되었습니다!</h2>
  <p className="text-gray-500 mb-6">
    {member.name}님의 {product.name} 계약이 성공적으로 체결되었습니다.
  </p>

  계약 요약 카드 (bg-white border rounded-xl p-5 w-full max-w-md mb-6):
  - 계약번호: CNT-{id 앞 8자리}
  - 회원: {member.name}
  - 상품: {product.name}
  - 계약기간: {startDate} ~ {endDate}
  - 금액: {amount:,}원

  버튼 그룹 (flex gap-3):
  - "계약 목록 보기" Button variant=outline onClick={() => moveToPage('/contracts')}
  - "새 계약 작성" Button variant=primary onClick={resetAndNew}

구현:
- resetAndNew: 모든 state 초기화 후 Step 1으로
- moveToPage('/contracts'): 계약 목록 페이지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Step 5 서명 후 API 201 응답 (TC-075-F6-01)

### 인터랙션 (User Actions)
1. "계약 목록 보기" → `/contracts`
2. "새 계약 작성" → 폼 초기화 후 Step 1

### 비즈니스 룰
- 계약 완료 시 회원의 이용권 자동 활성화
- 계약 PDF 생성 (향후 기능)

### 연결 화면
- 이전: `05-Step5-서명`
- 다음: `/contracts` 목록 또는 새 계약 작성
