# SCR-079 AB테스트 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-079 |
| 상태 코드 | `loading` |
| 경로 | `/ab-tests` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-079 A/B 테스트 — 상태: 로딩

파일: src/app/(marketing)/ab-tests/page.tsx

레이아웃:
- AppLayout + PageHeader("A/B 테스트")
- newFeature 배너 (bg-purple-50 border-dashed border-purple-300)

Skeleton:
- 툴바: h-10 animate-pulse rounded mb-4
- 테스트 카드 그리드 (grid grid-cols-2 gap-4):
  · 4개 카드: h-48 animate-pulse rounded-xl

구현:
- supabase.from('ab_tests') (가상 테이블)
- loadAbTests() 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/ab-tests` 진입 시

### 연결 화면
- 다음: `02-정상` / `03-빈상태`
