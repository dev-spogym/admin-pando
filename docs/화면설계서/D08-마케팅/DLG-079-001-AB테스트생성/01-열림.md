# DLG-079-001 AB테스트생성 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-079-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-079 "A/B 테스트 생성" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-079 |
| 다음 상태 | `02-저장성공` 또는 `03-저장오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-079-001 A/B 테스트 생성 모달 (newFeature)

파일: src/components/marketing/AbTestModal.tsx

모달 구조:
- title: "A/B 테스트 생성"
- size: lg (max-w-lg)
- newFeature 배지 (상단)

폼 필드:
1. 테스트명 * (Input)
2. 테스트 유형 (Select): 메시지 / 쿠폰 / 랜딩페이지
3. 안 A 내용 * (Textarea, rows=3, placeholder="A 버전 내용")
4. 안 B 내용 * (Textarea, rows=3, placeholder="B 버전 내용")
5. 트래픽 분배 (Slider):
   <div className="flex items-center gap-3">
     <span className="text-sm font-medium">A {trafficA}%</span>
     <input type="range" min={10} max={90} step={10} value={trafficA}
       onChange={e => setTrafficA(Number(e.target.value))}
       className="flex-1" />
     <span className="text-sm font-medium">B {100 - trafficA}%</span>
   </div>
6. 테스트 기간 (DatePicker × 2: 시작일 / 종료일)
7. 목표 지표 (Select): 클릭률 / 전환율 / 매출

하단 버튼:
- "취소" variant=outline
- "생성" variant=primary onClick={handleSave}

구현:
- POST /ab-tests (supabase.from('campaigns').insert with type='ab_test')
- 성공: toast.success("A/B 테스트가 생성되었습니다.")
- onClose() + refreshAbTests()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-079 "A/B 테스트 생성" 버튼 (E_DLG079001_OPEN)

### 필수 데이터
- 테스트명, 안 A/B 내용, 테스트 기간

### 인터랙션 (User Actions)
1. 테스트명 입력
2. 유형 선택 → 안 A/B 내용 작성
3. 트래픽 슬라이더 조정 (기본 50:50)
4. 기간 설정
5. 목표 지표 선택
6. "생성" 클릭

### 비즈니스 룰
- 안 A, 안 B 모두 필수
- 트래픽 합계 = 100%
- 종료일 > 시작일

### 연결 화면
- 다음: `02-저장성공` 또는 `03-저장오류`
