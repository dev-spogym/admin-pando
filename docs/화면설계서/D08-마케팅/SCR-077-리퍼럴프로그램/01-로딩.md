# SCR-077 리퍼럴프로그램 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-077 |
| 상태 코드 | `loading` |
| 경로 | `/referral` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-대시보드` 또는 `03-초기설정유도` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-077 리퍼럴프로그램 — 상태: 로딩

파일: src/app/(marketing)/referral/page.tsx

레이아웃:
- AppLayout + PageHeader("리퍼럴 프로그램")
- 상단 newFeature 배너: "이 기능은 개발 예정입니다." (bg-purple-50 border-dashed border-purple-300)

Skeleton:
- 통계 카드 3개: h-24 bg-gray-100 animate-pulse rounded-xl (grid grid-cols-3 gap-4 mb-6)
- 초대코드 테이블: h-10 + 5행 h-12 animate-pulse

구현:
- supabase.from('referrals') (가상 테이블)
- loadReferralData() 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/referral` 진입 시

### 연결 화면
- 다음: `02-대시보드` / `03-초기설정유도`
