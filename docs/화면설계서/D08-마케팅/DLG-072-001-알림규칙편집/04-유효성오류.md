# DLG-072-001 알림규칙편집 — 상태: 유효성 오류

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-072-001 |
| 상태 코드 | `validation-error` |
| 트리거 | 저장 클릭 시 채널/메시지 누락 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 수정 후 재시도 |

## 🧩 바이브코딩 프롬프트

```
유효성 오류 UI (AlarmRuleModal.tsx):

채널 미선택 오류 (TC-072-M2-001-01):
- Select: border-red-500
- "채널을 선택해주세요" (text-xs text-red-500 mt-1)

메시지 빈값 오류:
- Textarea: border-red-500
- "메시지 내용을 입력해주세요" (text-xs text-red-500 mt-1)

글자수 초과:
- 카운터: text-red-500 (초과 시)
- "글자수를 초과했습니다 ({현재}/{최대})" 표시

Zod 스키마:
const schema = z.object({
  channel: z.string().min(1, '채널을 선택해주세요'),
  message: z.string()
    .min(1, '메시지 내용을 입력해주세요')
    .max(채널별최대, '글자수를 초과했습니다'),
  sendTiming: z.string().min(1),
  isActive: z.boolean(),
})
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 채널 미선택 (TC-072-M2-001-01)
- 메시지 빈값
- 글자수 초과

### 연결 화면
- 다음: 수정 후 `03-저장성공`
