# DLG-071-001 수신자검색 — 상태: 선택 완료

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-071-001 |
| 상태 코드 | `confirmed` |
| 트리거 | "선택 완료" 버튼 (1명 이상 선택) |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 모달 닫힘 → SCR-071 수신자 목록 업데이트 |

## 🧩 바이브코딩 프롬프트

```
선택 완료 처리 (RecipientSearchModal.tsx):

handleConfirm:
1. onSelect(selectedMembers) 콜백 호출
   - selectedMembers: Member[] (선택된 회원 전체 데이터)
2. onClose() 모달 닫힘

SCR-071에서 수신자 반영:
- 수신자 수 칩 업데이트: "{N}명 선택됨"
- 예상 비용 재계산: N × 채널별 단가 (TC-071-008)
  · SMS: N × 15원
  · 카카오: N × 8원
  · 앱푸시: 무료

비용 표시:
<div className="text-sm text-gray-500 mt-1">
  예상 비용: <span className="font-semibold text-blue-600">{cost:,}원</span>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 1명 이상 선택 후 "선택 완료" 클릭

### 비즈니스 룰
- SCR-071 수신자 상태 업데이트
- 예상 비용 재계산 (TC-071-008)
- 취소 클릭 시 기존 수신자 유지

### 연결 화면
- 다음: SCR-071 폼 (수신자 업데이트됨)
