# DLG-073-001 쿠폰생성수정 — 상태: 유효성 오류

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-073-001 |
| 상태 코드 | `validation-error` |
| 트리거 | 저장 클릭 시 필수 필드 누락 또는 기간 오류 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` 또는 `02-열림` |
| 다음 상태 | 수정 후 재시도 |

## 🧩 바이브코딩 프롬프트

```
유효성 오류 UI (CouponFormModal.tsx):

쿠폰명 빈값 (TC-073-M2-001-01):
- Input border-red-500
- "쿠폰명을 입력해주세요" (text-xs text-red-500 mt-1)

유효기간 오류 (TC-073-M2-001-02):
- 종료일 < 시작일 시:
  "유효 종료일은 시작일 이후여야 합니다." (text-xs text-red-500 mt-1)

409 코드 중복:
- toast.error("이미 사용 중인 쿠폰 코드입니다.")
- 코드 영역 border-red-500 + 재생성 버튼 강조

Zod 스키마:
const schema = z.object({
  name: z.string().min(1, '쿠폰명을 입력해주세요'),
  type: z.enum(['discount', 'free']),
  discountType: z.enum(['amount', 'rate']).optional(),
  discountValue: z.number().positive().optional(),
  validFrom: z.string().min(1),
  validUntil: z.string().min(1),
}).refine(d => new Date(d.validUntil) >= new Date(d.validFrom), {
  message: '유효 종료일은 시작일 이후여야 합니다.',
  path: ['validUntil'],
})
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 쿠폰명 빈값 (TC-073-M2-001-01)
- 종료일 < 시작일 (TC-073-M2-001-02)
- 409 코드 중복

### 연결 화면
- 다음: 수정 후 `03-저장성공`
