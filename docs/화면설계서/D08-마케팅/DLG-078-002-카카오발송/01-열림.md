# DLG-078-002 카카오발송 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-078-002 |
| 상태 코드 | `open` |
| 트리거 | SCR-078 "카카오 발송" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-078 |
| 다음 상태 | `02-발송성공` 또는 `03-발송오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-078-002 카카오 알림톡 발송 모달 (newFeature)

파일: src/components/marketing/KakaoModal.tsx

모달 구조:
- title: "카카오 알림톡 발송"
- size: md (max-w-md)
- newFeature 배지 (상단)

폼 필드:
1. 수신자 (Input, readonly) + "수신자 선택" Button
2. 템플릿 선택 (Select): 사전 승인된 알림톡 템플릿 목록
3. 템플릿 미리보기:
   <div className="p-3 bg-yellow-50 border border-yellow-200 rounded-lg text-sm whitespace-pre-wrap">
     {templatePreview}
   </div>
4. 변수 입력 (동적): 선택한 템플릿의 #{변수명} 별 Input 자동 생성
5. 예약 발송 (Toggle + DateTimePicker, optional)

하단 버튼:
- "취소" variant=outline
- "발송" variant=primary onClick={handleSend}

구현:
- POST /messages/kakao (supabase.from('message_logs').insert)
- 성공: toast.success("카카오 알림톡이 발송되었습니다.")
- onClose() + refreshMessages()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-078 "카카오 발송" 버튼 (E_DLG078002_OPEN)

### 필수 데이터
- 수신자 1명 이상
- 승인된 템플릿 선택
- 템플릿 변수 모두 입력

### 인터랙션 (User Actions)
1. 수신자 선택
2. 템플릿 선택 → 미리보기 + 변수 필드 자동 생성
3. 변수 값 입력
4. 예약 발송 토글 (선택)
5. "발송" 클릭

### 비즈니스 룰
- 카카오 채널 사전 연동 필요
- 템플릿은 카카오 사전 심사 통과 후 사용 가능
- 수신자 휴대폰 번호 필수

### 연결 화면
- 다음: `02-발송성공` 또는 `03-발송오류`
