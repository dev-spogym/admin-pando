# DLG-077-002 초대코드생성 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-077-002 |
| 상태 코드 | `open` |
| 트리거 | SCR-077 "초대코드 생성" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-077 대시보드 |
| 다음 상태 | `02-생성성공` 또는 `03-생성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-077-002 리퍼럴 초대코드 생성 모달 (newFeature)

파일: src/components/marketing/ReferralCodeModal.tsx

모달 구조:
- title: "초대코드 생성"
- size: md (max-w-md)
- newFeature 배지 (상단)

생성된 코드 미리보기:
<div className="flex items-center gap-2 p-3 bg-gray-50 rounded-lg mb-4">
  <code className="flex-1 font-mono text-lg font-bold text-gray-800 tracking-widest">
    {code}
  </code>
  <Button variant="ghost" size="sm" onClick={handleCopy}>
    <Copy className="w-4 h-4" />
  </Button>
</div>
- code: generateReferralCode() → "REF-XXXXXX" 패턴 (6자리 대문자+숫자)

폼 필드:
1. 코드 유형 (Select): 개인용 / 공개용
2. 사용 횟수 제한 (Input, number, 0=무제한)
3. 유효 만료일 (DatePicker, optional)
4. 메모 (Input, optional)

하단 버튼:
- "취소" variant=outline
- "생성" variant=primary onClick={handleCreate}

구현:
- generateReferralCode: `REF-${Math.random().toString(36).slice(2,8).toUpperCase()}`
- POST /referral/codes (supabase.from('referrals').insert)
- 성공: toast.success("초대코드가 생성되었습니다.")
- onClose() + refreshReferral()
- Copy 버튼: navigator.clipboard.writeText(code) + toast.success("복사됨")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-077 대시보드 "초대코드 생성" 버튼 (E_DLG077002_OPEN)
- 리퍼럴 프로그램이 활성화된 상태에서만 접근 가능

### 필수 데이터
- 없음 (코드는 클라이언트에서 자동 생성)

### 인터랙션 (User Actions)
1. 코드 자동 생성 후 미리보기 표시
2. Copy 버튼 클릭 → 클립보드 복사
3. 유형/제한 설정 후 "생성" 클릭

### 비즈니스 룰
- 코드 유형: 개인용(특정 회원 1회용) / 공개용(다수 사용 가능)
- 만료일 미설정 시 무기한

### 연결 화면
- 다음: `02-생성성공` 또는 `03-생성오류`
