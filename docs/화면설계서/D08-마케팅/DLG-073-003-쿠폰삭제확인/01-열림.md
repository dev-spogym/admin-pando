# DLG-073-003 쿠폰삭제확인 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-073-003 |
| 상태 코드 | `open` |
| 트리거 | SCR-073 Trash2 아이콘 (totalIssued=0) |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-073 정상 |
| 다음 상태 | `02-삭제성공` 또는 `03-삭제오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-073-003 쿠폰 삭제 확인 모달

파일: src/components/marketing/CouponDeleteModal.tsx

모달 구조:
- title: "쿠폰 삭제"
- variant: danger
- size: sm (max-w-sm)

본문:
<div className="text-center py-4">
  <AlertTriangle className="w-12 h-12 text-red-500 mx-auto mb-3" />
  <p className="text-base font-semibold text-gray-800">"{coupon.name}" 쿠폰을 삭제하시겠습니까?</p>
  <p className="text-sm text-gray-500 mt-1">삭제된 쿠폰은 복구할 수 없습니다.</p>
  <div className="mt-3 p-2 bg-amber-50 rounded-lg">
    <p className="text-xs text-amber-700">쿠폰 코드: {coupon.code}</p>
  </div>
</div>

하단 버튼:
- "취소" variant=outline onClick={onClose}
- "삭제" variant=danger onClick={handleDelete} loading={isDeleting}

구현:
- PATCH /coupons/:id { isActive: false } (soft delete) (TC-073-006)
- 성공: toast.success("삭제되었습니다.") + onClose() + refreshCoupons()
- 404: toast.error("이미 삭제된 쿠폰입니다.") + onClose()
- 500: toast.error("삭제에 실패했습니다.")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-073 Trash2 아이콘 (totalIssued=0인 쿠폰만)

### 인터랙션 (User Actions)
1. "삭제" → soft delete (TC-073-006)
2. "취소"/X → 모달 닫힘

### 비즈니스 룰
- totalIssued > 0 쿠폰은 삭제 불가 (SCR-073에서 Trash2 비활성)
- isActive=false soft delete

### 연결 화면
- 다음: `02-삭제성공` 또는 `03-삭제오류`
