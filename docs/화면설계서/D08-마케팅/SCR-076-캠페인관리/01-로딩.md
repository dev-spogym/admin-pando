# SCR-076 캠페인관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-076 |
| 상태 코드 | `loading` |
| 경로 | `/campaigns` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-076 캠페인관리 — 상태: 로딩 (Skeleton UI)
참고: 미구현 화면 — 다이어그램 기반 설계

파일: src/app/(marketing)/campaigns/page.tsx

레이아웃:
- AppLayout + PageHeader("캠페인 관리")
- max-w-7xl mx-auto px-6 py-6
- 상단 newFeature 배너: "이 기능은 개발 예정입니다." (bg-purple-50 border border-purple-200 rounded-lg p-3 mb-4)

Skeleton:
- 툴바: h-10 bg-gray-100 animate-pulse rounded mb-4
- 카드 그리드 (grid grid-cols-3 gap-4):
  · 6개 카드: h-48 bg-gray-100 animate-pulse rounded-xl

구현:
- useEffect(() => { loadCampaigns() }, [])
- supabase.from('campaigns') (가상 테이블)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/campaigns` 진입 시

### 비즈니스 룰
- 미구현 기능 (newFeature 스타일: dashed purple border)

### 연결 화면
- 다음: `02-정상` / `03-빈상태`
