---
title: FitGenie CRM RBAC 권한 위계
type: flowchart
lastUpdated: 2026-04-20
---

# R8 — 권한 위계

> primary → owner → manager → fc → staff → readonly 위계 구조.
> 설정관리 SCR-081 역할 정의 기준.

```mermaid
flowchart TD
    SUPER["\n슈퍼관리자\n본사 운영 총괄\n전 지점 접근"]

    PRIMARY["primary\n최고관리자\n지점 내 최상위 권한\n수정 불가(동작)"]

    OWNER["owner\n센터장\n지점 경영/매출 관리\n권한 수정 가능"]

    MANAGER["manager\n매니저\n운영 전반 관리\n권한 수정 가능"]

    FC["fc\/피트니스코치\n상담/결제/수업\n권한 수정 가능"]

    STAFF["staff\n스태프\n기본 조회/출석 확인\n권한 수정 가능"]

    READONLY["readonly\n조회전용\n읽기 전용 전체\n권한 수정 가능"]

    SUPER -->|"지점 위임"|PRIMARY
    PRIMARY -->|"경영 위임"|OWNER
    OWNER -->|"운영 위임"|MANAGER
    MANAGER -->|"업무 위임"|FC
    FC -->|"기본 권한"|STAFF
    STAFF -->|"열람 전용"|READONLY

    subgraph SUPER_ONLY[" 전용"]
        P_SUPER1["전 지점 통합 대시보드\n/super-dashboard"]
        P_SUPER2["지점 리포트\n/branch-report"]
    end

    subgraph PRIMARY_ONLY["primary 추가 접근"]
        P_P1["지점 관리 /branches"]
        P_P2["온보딩 /onboarding"]
        P_P3["구독 관리 /subscription"]
    end

    subgraph OWNER_ONLY["owner 추가 접근"]
        P_O1["매출 현황 /sales"]
        P_O2["환불 관리 /"]
        P_O3["직원 관리 /staff"]
        P_O4["급여 관리 /payroll"]
        P_O5["상품 관리 /"]
        P_O6["센터 설정 /settings"]
    end

    subgraph MANAGER_ONLY["manager 추가 접근"]
        P_M1["POS 결제 /pos"]
        P_M2["시설 관리 /locker 등"]
        P_M3["마케팅 / 등"]
        P_M4["수업 관리 /lessons 등"]
    end

    subgraph FC_ONLY["fc 추가 접근"]
        P_F1["회원 등록/수정"]
        P_F2["횟수/유효수업 관리"]
        P_F3["전자계약 "]
        P_F4["출석 관리 /"]
    end

    subgraph STAFF_ONLY["staff 기본 접근"]
        P_ST1["대시보드 /"]
        P_ST2["회원 목록 조회(○)"]
        P_ST3["공지사항 /notices"]
        P_ST4["락커/운동복 시설(●)"]
        P_ST5["KPI 센터 /kpi-preview"]
    end

    subgraph READONLY_AREA["readonly 조회 전용"]
        P_R1["전체 접근 가능 화면\n읽기(○)만 허용"]
        P_R2["CRUD 액션 비활성"]
    end

    SUPER --> SUPER_ONLY
    PRIMARY --> PRIMARY_ONLY
    OWNER --> OWNER_ONLY
    MANAGER --> MANAGER_ONLY
    FC --> FC_ONLY
    STAFF --> STAFF_ONLY
    READONLY --> READONLY_AREA

    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class SUPER system
    class PRIMARY,OWNER screen
    class MANAGER,FC warning
    class STAFF,READONLY rbacBlocked
```

---

## 권한 상속 원칙

| 원칙 | 설명 |
|------|------|
| 상위 포함 원칙 | 상위 역할은 하위 역할의 모든 권한 포함 |
| 시스템 역할 잠금 | `primary`, ``은 권한 수정 불가 (``) |
| 최소 권한 원칙 | 기본값은 최소 권한. 필요 시 SCR-081에서 확장 |
| 커스텀 역할 | owner 이하는 SCR-081에서 커스텀 역할 생성 가능 |
| 지점 격리 | 각 역할은 자신이 속한 지점 데이터만 접근 ( 제외) |

## 역할별 핵심 차별화 권한

| 역할 | 독점 권한 | 제한 내용 |
|------|---------|---------|
| | 전 지점 통합 조회, 지점 리포트 | 지점 내 CRUD 직접 수행 없음 |
| primary | 지점 관리, 온보딩, 구독 관리 | 본사 전체 통합 불가 |
| owner | 매출/환불/직원/급여/상품/설정 전체 | 타 지점 접근 불가 |
| manager | POS, 시설, 마케팅, 수업 관리 | 매출/직원/급여 조회만 일부 |
| fc | 회원 CRUD, 수업, 전자계약 | 매출/상품/직원/설정 불가 |
| staff | 대시보드, 시설일부, 출석, 공지 | 대부분 조회만 또는 차단 |
| readonly | 허용 화면 전체 조회 | CRUD 액션 전부 비활성 |
