---
title: 직원 근태 관리 메인 인터랙션 플로우
type: flowchart
scope: SCR-063
dependencies: []
actors: [primary, owner, manager]
lastUpdated: 2026-04-20
---

## 1. 목적

SCR-063 일별/월별 뷰 전환 및 출퇴근 기록 흐름. 성공/검증실패/시스템에러 3갈래 분기 강제.

## 3. 다이어그램

```mermaid
flowchart TD
  SCR_063([SCR-063 직원 근태 관리])
  VIEW_TAB{뷰 선택}

  SCR_063 -->|진입|VIEW_TAB
  VIEW_TAB -->|일별 뷰 선택|DAILY[일별 뷰\n날짜별 전 직원 출퇴근]
  VIEW_TAB -->|월별 요약 선택|MONTHLY[월별 요약\n월간 직원별 집계]

  DAILY -->|날짜 변경|DATE_LOAD[해당 날짜 근태 조회]
  DATE_LOAD -->|성공|DAILY
  DATE_LOAD -->|실패|ERR_DATE[에러 토스트]
  DATE_LOAD -->|시스템 에러|ERR_SYS[서버 오류 토스트]

  DAILY -->|출근 버튼 클릭 출근 전|CLOCKIN_API[\ staff_attendance]
  CLOCKIN_API -->|성공|TOAST_CLOCKIN_OK((toast — 출근 처리 완료))
  CLOCKIN_API -->|실패|TOAST_CLOCKIN_ERR((toast — 출근 처리 실패))
  CLOCKIN_API -->|시스템 에러|TOAST_SYS((toast — 서버 오류))
  TOAST_CLOCKIN_OK --> DAILY

  DAILY -->|퇴근 버튼 클릭 출근 후|CLOCKOUT_API[\ staff_attendance]
  CLOCKOUT_API -->|성공|TOAST_CLOCKOUT_OK((toast — 퇴근 처리 완료))
  CLOCKOUT_API -->|실패|TOAST_CLOCKOUT_ERR((toast — 퇴근 처리 실패))
  CLOCKOUT_API -->|시스템 에러|TOAST_SYS2((toast — 서버 오류))
  TOAST_CLOCKOUT_OK --> DAILY

  MONTHLY -->|월 변경|MONTH_LOAD[해당 월 근태 조회]
  MONTH_LOAD -->|성공|MONTHLY
  MONTH_LOAD -->|실패|ERR_MONTH[에러 토스트]

  classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
  classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
  classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
  classDef system fill:#EDE7F6,stroke:#5E35B2
  classDef info fill:#E0F7FA,stroke:#00838F

  class SCR_063 screen
  class DAILY,MONTHLY info
  class CLOCKIN_API,CLOCKOUT_API,DATE_LOAD,MONTH_LOAD,VIEW_TAB system
  class TOAST_CLOCKIN_OK,TOAST_CLOCKOUT_OK success
  class TOAST_CLOCKIN_ERR,TOAST_CLOCKOUT_ERR,TOAST_SYS,TOAST_SYS2,ERR_DATE,ERR_SYS,ERR_MONTH error
```
