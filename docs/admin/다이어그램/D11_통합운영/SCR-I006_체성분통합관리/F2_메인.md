---
title: 체성분 통합 관리 메인 인터랙션 플로우
type: flowchart
scope: SCR-I006
dependencies: [DLG-I003]
actors: [superAdmin, primary, owner, manager, fc]
lastUpdated: 2026-04-20
---

# F2 메인 인터랙션 플로우 — SCR-I006 체성분 통합 관리

## 목적
3개 수집 경로(수기/파일업로드/실시간API)와 탭별 흐름, 오류 검수 정상 경로를 정의한다.

## 다이어그램

```mermaid
flowchart TD
    SCR_I006[SCR-I006 체성분 통합 관리] --> TAB[탭 선택]

    TAB --> TAB_RESULT[측정 결과\n회원별 최신+추이]
    TAB --> TAB_UPLOAD[업로드 센터\n파일 업로드+파싱]
    TAB --> TAB_REALTIME[실시간 연동\ API 수신 로그]
    TAB --> TAB_ERROR[오류/검수\n중복/매칭실패/보정필요]

    TAB_RESULT -->|": 수기 등록 버튼"|DLG_I003[DLG-I003 체성분 수기 등록]
    TAB_UPLOAD -->|": 파일 드래그/업로드"|UPLOAD_PROC[파일 파싱 처리]
    TAB_REALTIME -->|": InBody 수신 이벤트"|EXT_INBODY[EXT_INBODY\ API 자동 수신]
    TAB_ERROR -->|": 검수 승인"|APPROVE[데이터 확정]
    TAB_ERROR -->|": 검수 거부"|REJECT[데이터 폐기]

    DLG_I003 -->|": 저장"|API_MANUAL[POST]
    UPLOAD_PROC -->|": 파싱 성공"|PARSE_OK[파싱 결과 미리보기]
    UPLOAD_PROC -->|": 중복 감지"|TAB_ERROR
    EXT_INBODY -->|": 수신 성공"|AUTO_SAVE[자동 저장\n입력소스=InBody API]
    EXT_INBODY -->|": 중복 날짜"|TAB_ERROR
    PARSE_OK -->|": 업로드 확정"|API_UPLOAD[POST]

    API_MANUAL -->|": 200"|T_OK[등록 완료 토스트]
    API_UPLOAD -->|": 200"|T_OK2[업로드 완료 토스트]
    APPROVE --> T_OK3[검수 승인 토스트]
    AUTO_SAVE --> T_INFO[실시간 수신 알림]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef external fill:#ECEFF1,stroke:#455A64,stroke-dasharray:3 3
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef info fill:#E0F7FA,stroke:#00838F
    class SCR_I006 screen
    class DLG_I003 modal
    class EXT_INBODY external
    class API_MANUAL,API_UPLOAD,UPLOAD_PROC,APPROVE,REJECT,AUTO_SAVE system
    class T_OK,T_OK2,T_OK3 success
    class T_INFO info
    class TAB_ERROR error
```

## TC 후보
| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-I006-F2-01 | positive | fc | 수기 등록 버튼 클릭 | DLG-I003 열림 |
| TC-I006-F2-02 | positive | fc | CSV 파일 업로드 | 파싱 결과 미리보기 표시 |
| TC-I006-F2-03 | positive | fc | InBody API 자동 수신 | 실시간 연동 탭에 수신 로그 추가 |
| TC-I006-F2-04 | negative | fc | 중복 날짜 수신 | 오류/검수 탭으로 이동 |
| TC-I006-F2-05 | positive | fc | 검수 승인 | 데이터 확정, 승인 토스트 |
