---
title: 데이터 불일치 / 상태 충돌 에러 플로우
type: flowchart
scope: SYSTEM_DATA
lastUpdated: 2026-04-20
---

# E13 — 데이터 불일치

## 1. 개요

| 항목 | 내용 |
|------|------|
| 에러코드 | E409101 / E422100 |
| HTTP | 409 / 422 |
| 발생 모듈 | 회원 관리, 수업, 이용권 |
| 영향 화면 | 회원 상태 변경, 이용권 처리, 수업 처리 화면 |

## 2. 발생 조건

- 클라이언트가 보유한 상태와 서버 실제 상태 불일치
- 타 사용자가 동시에 동일 리소스 수정
- 화면 로드 후 오래된 캐시 상태로 액션
- 잘못된 상태 전이 경로 시도

## 3. 다이어그램

```mermaid
flowchart TD
    ACTION([상태 변경 요청]) --> SERVER{서버 상태 검증}

    SERVER -->|상태 일치|PROCESS[정상 처리]
    SERVER -->|상태 불일치|MISMATCH_TYPE{불일치 유형}

    MISMATCH_TYPE -->|상태 전이 오류|E409101[E409101<br/>현재 상태에서 해당 작업 불가]
    MISMATCH_TYPE -->|비즈니스 규칙|E422100[E422100<br/>홀딩 제한 초과 등]
    MISMATCH_TYPE -->|오래된 데이터|STALE_ERR[클라이언트 캐시<br/>서버 상태 불일치]

    E409101 --> TOAST_409[에러 토스트<br/>현재 상태 안내]
    E422100 --> MODAL_422[안내 모달<br/>비즈니스 규칙 설명]
    STALE_ERR --> AUTO_REFRESH[화면 자동 새로고침<br/>최신 상태 로드]

    TOAST_409 --> RELOAD[현재 상태 재조회<br/>UI 갱신]
    MODAL_422 -->|확인|RELOAD
    AUTO_REFRESH --> ACTION_RETRY{갱신 후 재시도?}

    ACTION_RETRY -->|재시도|ACTION
    ACTION_RETRY -->|취소|CANCEL[액션 취소]

    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class E409101,E422100,STALE_ERR,TOAST_409 error
    class PROCESS success
    class SERVER,MISMATCH_TYPE,ACTION_RETRY system
    class MODAL_422 modal
    class RELOAD,AUTO_REFRESH warning
```

## 4. 복구/재시도 전략

| 상황 | 전략 |
|------|------|
| 상태 전이 오류 | 현재 상태 재조회, UI 갱신 후 올바른 액션 유도 |
| 오래된 캐시 | 자동 새로고침, 최신 상태로 재시도 |
| 비즈니스 규칙 위반 | 규칙 안내 모달, 올바른 처리 방법 안내 |

## 5. 사용자 노출 메시지

| 에러코드 | 메시지 |
|----------|--------|
| E409101 | 현재 상태에서는 해당 작업을 수행할 수 없습니다 |
| E422100 | 기간정지 가능 횟수를 초과했습니다 |
| 오래된 데이터 | 데이터가 변경되었습니다. 화면을 새로고침합니다. |
