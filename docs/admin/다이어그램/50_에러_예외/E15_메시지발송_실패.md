---
title: 메시지 발송 실패 에러 플로우
type: flowchart
scope: MESSAGING
lastUpdated: 2026-04-20
---

# E15 — 메시지 발송 실패

## 1. 개요

| 항목 | 내용 |
|------|------|
| 에러코드 | E503002 (외부 채널 오류) |
| HTTP | 503 Service Unavailable |
| 발생 모듈 | 마케팅 (SMS/카카오/앱 푸시) |
| 영향 화면 | SCR-071 메시지 발송, SCR-072 자동 알림 설정 |

## 2. 발생 조건

- SMS 발송사 API 오류
- 카카오 알림톡 채널 장애
- 앱 푸시 서버(FCM/APNs) 응답 실패
- 수신자 번호 오류/수신 거부

## 3. 다이어그램

```mermaid
flowchart TD
    SEND([메시지 발송 요청]) --> CHANNEL{발송 채널 선택}

    CHANNEL -->|SMS|SMS_SEND[EXT: SMS 발송사]
    CHANNEL -->|카카오|KAKAO_SEND[EXT: 카카오 알림톡]
    CHANNEL -->|앱 푸시|PUSH_SEND[EXT: FCM/APNs]

    SMS_SEND -->|성공|LOG_OK[발송 이력 저장<br/>status=SENT]
    SMS_SEND -->|실패|SMS_ERR{실패 유형}

    KAKAO_SEND -->|성공|LOG_OK
    KAKAO_SEND -->|실패|KAKAO_FALLBACK[SMS로 대체 발송]

    PUSH_SEND -->|성공|LOG_OK
    PUSH_SEND -->|실패|PUSH_ERR[푸시 실패 로그<br/>토큰 무효화 여부 확인]

    SMS_ERR -->|번호 오류|NUM_ERR[수신자 번호 오류<br/>로그 기록]
    SMS_ERR -->|수신 거부|OPTOUT_LOG[수신 거부 처리<br/>회원]
    SMS_ERR -->|게이트웨이 오류|RETRY{재시도?}

    RETRY --> SMS_SEND
    RETRY -->|초과|LOG_FAIL[발송 실패 로그<br/>status=FAILED]

    KAKAO_FALLBACK -->|성공|LOG_OK
    KAKAO_FALLBACK -->|실패|LOG_FAIL

    NUM_ERR & OPTOUT_LOG & PUSH_ERR & LOG_FAIL --> ADMIN_NOTIFY{실패율<br/>임계치 초과?}
    ADMIN_NOTIFY -->|초과|OPS_ALERT[운영팀 알림<br/>채널 장애 경보]
    ADMIN_NOTIFY -->|정상|END([처리 완료])
    OPS_ALERT --> END
    LOG_OK --> END

    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef external fill:#ECEFF1,stroke:#455A64,stroke-dasharray:3 3
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class SMS_ERR,NUM_ERR,LOG_FAIL,PUSH_ERR error
    class LOG_OK success
    class CHANNEL,RETRY,ADMIN_NOTIFY system
    class SMS_SEND,KAKAO_SEND,PUSH_SEND external
    class OPTOUT_LOG,KAKAO_FALLBACK,OPS_ALERT warning
```

## 4. 복구/재시도 전략

| 상황 | 전략 |
|------|------|
| 게이트웨이 오류 | 최대 3회 자동 재시도 |
| 카카오 장애 | SMS 자동 대체 발송 |
| 수신 거부 | 처리, 이후 발송 차단 |
| 번호 오류 | 실패 로그, 관리자 번호 수정 유도 |
| 실패율 임계치 초과 | 운영팀 알림, 채널 점검 |

## 5. 사용자 노출 메시지

| 대상 | 메시지 |
|------|--------|
| 관리자 화면 | 발송 실패 건수 배지 표시 (SCR-071) |
| 운영팀 알림 | "[FitGenie ALERT] 메시지 발송 실패율 {N}% 초과. 채널 점검 필요." |
