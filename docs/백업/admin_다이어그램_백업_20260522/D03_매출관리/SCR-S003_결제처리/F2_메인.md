---
title: 결제 처리 메인 인터랙션 플로우
type: flowchart
scope: SCR-S003
dependencies: [DLG-S003, DLG-S004, DLG-S016]
actors: [primary, owner, manager, fc]
lastUpdated: 2026-05-12
---

## 1. 목적
결제 처리를 `할인 적용 → 수납 방식 선택 → 현장 영수증 첨부 등록 또는 결제링크 발송 → 결제완료 + 회원/이용권 등록 완료` 기준으로 표현한다.

## 2. 전제조건
- SCR-S003 진입 완료, 장바구니 데이터 복원됨

## 3. 다이어그램

```mermaid
flowchart TD
    S003[SCR-S003 결제 처리\n장바구니 + 회원 + 결제요약]

    S003 --> DISCOUNT[할인 입력 / 확인]
    DISCOUNT --> COLLECTION{수납 방식 선택}

    COLLECTION -->|": 현장 전액 등록"|FULL[현장 전액 등록]
    COLLECTION -->|": 계약금 등록"|DEPOSIT[계약금 입력 + 잔액 계산]
    COLLECTION -->|": 잔액 등록"|BALANCE[기존 미수 잔액 등록]
    COLLECTION -->|": 결제링크 발송"|LINK_READY[고정 금액 링크 발송 준비]

    FULL --> FIELD_INPUT[현장 결제 등록 정보 입력]
    DEPOSIT --> FIELD_INPUT
    BALANCE --> FIELD_INPUT

    FIELD_INPUT --> METHOD[결제수단\n카드 또는 현금]
    FIELD_INPUT --> PAY_AMOUNT[결제가격 입력]
    FIELD_INPUT --> POINT[포인트 사용 여부/사용액]
    FIELD_INPUT --> PAID_AT[결제일시 입력]
    FIELD_INPUT --> RECEIPT[영수증 파일 첨부]
    FIELD_INPUT --> MEMO[FC 메모 입력]

    POINT -->|": 사용함"|POINT_MEMBER{회원 선택 + 보유 포인트 확인}
    POINT_MEMBER -->|": 회원 미선택"|ERR_NO_MEMBER[포인트 사용은 회원 선택이 필요합니다.]
    POINT_MEMBER -->|": 포인트 부족"|ERR_POINT[포인트가 부족합니다.]
    POINT_MEMBER -->|": 충분"|AMOUNT_CHECK
    POINT -->|": 사용 안 함"|AMOUNT_CHECK{금액 일치 검증}
    PAY_AMOUNT --> AMOUNT_CHECK

    RECEIPT -->|": 미첨부"|ERR_RECEIPT[영수증 파일을 첨부해주세요.]
    RECEIPT -->|": 첨부 완료"|AMOUNT_CHECK
    AMOUNT_CHECK -->|": 결제가격 + 포인트 = 장바구니 합계"|READY[결제 등록 준비 완료]
    AMOUNT_CHECK -->|": 불일치"|ERR_AMOUNT[N원 부족/초과 error 표시]

    READY -->|": 결제 등록 클릭"|DLG_S003[DLG-S003 결제 확인 모달]
    LINK_READY -->|": 결제링크 발송 클릭"|DLG_S016[DLG-S016 결제링크 발송]

    DLG_S003 -->|": 취소"|S003
    DLG_S003 -->|": 결제 완료 등록 클릭"|DUP_CHECK{중복 결제 확인}

    DUP_CHECK -->|": 중복 발견"|DLG_S004[DLG-S004 중복 결제 경고]
    DUP_CHECK -->|": 중복 없음"|REGISTER[결제 등록 실행\npaymentStatus=APPROVED]

    DLG_S004 -->|": 취소"|S003
    DLG_S004 -->|": 계속 진행"|REGISTER

    REGISTER -->|": 200 OK"|COMPLETE[결제완료 화면\n회원/이용권 등록 완료]
    REGISTER -->|": 저장 실패"|TOAST_ERR((오류 토스트\n결제 등록에 실패했습니다.))
    REGISTER -->|": 포인트 차감 실패"|TOAST_POINT_ERR((오류 토스트\n포인트 차감에 실패했습니다.))

    DLG_S016 -->|": 취소"|S003
    DLG_S016 -->|": 발송 성공"|LINK_SENT[결제링크 발송 완료\nlinkStatus=SENT]
    DLG_S016 -->|": 발송 실패"|TOAST_LINK_ERR((오류 토스트\n결제링크 발송에 실패했습니다.))

    COMPLETE -->|": 영수증 파일 보기"|RECEIPT_VIEW[영수증 파일 열기]
    COMPLETE -->|": 문자 발송"|SMS[문자 영수증 발송]
    COMPLETE -->|": 계속 판매하기"|RESET[전체 초기화]
    COMPLETE -->|": 매출 현황으로"|SCR_S001[SCR-S001 매출 현황]
    LINK_SENT -->|": 미수금/매출 확인"|SCR_S001

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class S003,SCR_S001 screen
    class DLG_S003,DLG_S004,DLG_S016 modal
    class ERR_NO_MEMBER,ERR_POINT,ERR_AMOUNT,ERR_RECEIPT,TOAST_ERR,TOAST_POINT_ERR,TOAST_LINK_ERR error
    class COMPLETE,RECEIPT_VIEW,SMS,LINK_SENT success
    class DUP_CHECK,REGISTER,READY,DISCOUNT,COLLECTION,FIELD_INPUT,METHOD,PAY_AMOUNT,POINT,PAID_AT,RECEIPT,MEMO,AMOUNT_CHECK,POINT_MEMBER,FULL,DEPOSIT,BALANCE,LINK_READY system
    class RESET warning
```

## 4. 엣지 설명

| 출발 | 도착 | 설명 |
|------|------|------|
| COLLECTION | LINK_READY | 링크 발송은 별도 비대면 수납 경로 |
| FIELD_INPUT | RECEIPT | 현장 결제 등록은 영수증 첨부 필수 |
| POINT_MEMBER | ERR_NO_MEMBER | 포인트 사용 시 회원 선택 필수 |
| POINT_MEMBER | ERR_POINT | 포인트 잔액 부족 |
| AMOUNT_CHECK | READY | 결제가격 + 포인트 사용액이 장바구니 합계와 일치 |
| AMOUNT_CHECK | ERR_AMOUNT | 결제 총액 불일치 |
| DUP_CHECK | DLG-S004 | 중복 결제 경고 모달 |
| REGISTER | COMPLETE | 결제완료 + 회원/이용권 등록 완료 |
| DLG-S016 | LINK_SENT | 고정 금액 링크 발송 성공 |
