---
title: 통합 출석 관리 메인 인터랙션 플로우
type: flowchart
scope: SCR-I001
dependencies: [DLG-I001, DLG-I002]
actors: [superAdmin, primary, owner, manager, fc, staff]
lastUpdated: 2026-04-20
---

# F2 메인 인터랙션 플로우 — SCR-I001 통합 출석 관리

## 목적
통합 출석 관리 화면의 정상 시나리오(Happy Path)를 정의한다. IoT 채널별 출석 이벤트 수신, 옷 락커 배정, 수동 출석 등록 흐름을 포함한다.

## 전제조건
- 로그인 세션 유효, 출석 관리 권한 보유
- 실시간 스트림 WebSocket 연결 상태

## 다이어그램

```mermaid
flowchart TD
    SCR_I001[SCR-I001 통합 출석 관리\n실시간 스트림 표시] -->|": 페이지 진입"|LOAD[출석 로그 로드<br/> 카드 집계]

    LOAD -->|": 로드 완료"|STREAM[실시간 이벤트 스트림 수신]
    LOAD --> VERIFY[실시간 출석 확인 카드\n최대 10개]

    STREAM -->|": IoT 밴드 체크인"|EVT_IOT[EXT_IOT_BAND<br/> 밴드 출입 이벤트]
    STREAM -->|": 키오스크 QR 체크인"|EVT_KIOSK[EXT_KIOSK\n키오스크 QR 이벤트]
    STREAM -->|": 얼굴인식 체크인"|EVT_FACE[EXT_FACE\n얼굴인식 이벤트]
    STREAM -->|": 앱 QR 체크인"|EVT_APP[EXT_APP_QR\n회원앱 QR 이벤트]
    STREAM -->|": 수동 출석 클릭"|DLG_I001[DLG-I001 수동 출석 등록]

    EVT_IOT --> PROC[출석 처리 엔진\n회원권 유효성 검증]
    EVT_KIOSK --> PROC
    EVT_FACE --> PROC
    EVT_APP --> PROC

    PROC -->|": 성공 + 수동배정 모드"|LOCKER_MANUAL[옷 락커 미배정 상태\n→ 미배정 목록에 추가]
    PROC -->|": 성공 + 자동배정 모드"|LOCKER_AUTO[락커 자동 배정\nEXT_LOCKER_CTRL]
    PROC -->|": 실패"|FAIL_LOG[실패 로그 추가\n실패 사유 기록]
    PROC -->|": 성공/주의 이벤트"|VERIFY

    VERIFY -->|": 신규 이벤트"|CARD_ALERT[카드 강조\n알림음 + 토스트]
    VERIFY -->|": 같은 회원 2시간 내"|NO_DUP[중복 카드 미생성]
    VERIFY -->|": 사진 없음"|PHOTO_GUIDE[기본 아바타\n사진 촬영 안내]
    VERIFY -->|": 카드 클릭"|MEMBER_DETAIL[SCR-M004 회원 상세 기본정보]

    LOCKER_MANUAL -->|": 미배정 회원 행 클릭"|DLG_I002[DLG-I002 옷 락커 배정]
    LOCKER_AUTO -->|": 자동 배정 완료"|KPI_UPDATE[KPI 카드 갱신]
    DLG_I002 -->|": 배정 완료"|KPI_UPDATE
    DLG_I001 -->|": 수동 출석 등록 완료"|KPI_UPDATE

    KPI_UPDATE --> STREAM

    FAIL_LOG -->|": 실패 로그 클릭"|FAIL_DETAIL[실패 사유 상세\n인증실패/회원권만료/시간제한]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef external fill:#ECEFF1,stroke:#455A64,stroke-dasharray:3 3
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    class SCR_I001 screen
    class DLG_I001,DLG_I002 modal
    class EVT_IOT,EVT_KIOSK,EVT_FACE,EVT_APP external
    class PROC,STREAM,LOAD,KPI_UPDATE,VERIFY,CARD_ALERT,NO_DUP system
    class FAIL_LOG,FAIL_DETAIL,PHOTO_GUIDE error
    class MEMBER_DETAIL screen
    class LOCKER_AUTO,LOCKER_MANUAL success
```

## 엣지 설명

| 출발 | 도착 | 설명 |
|------|------|------|
| STREAM | EVT_IOT | IoT 밴드 출입 이벤트 수신 (X14 시퀀스 참조) |
| STREAM | EVT_KIOSK | 키오스크 QR 이벤트 수신 (X23 시퀀스 참조) |
| STREAM | EVT_APP | 회원앱 QR 이벤트 수신 |
| PROC | LOCKER_MANUAL | 성공 + 수동배정 모드 → 미배정 목록 추가 |
| PROC | LOCKER_AUTO | 성공 + 자동배정 모드 → 락커 컨트롤러 자동 배정 |
| PROC | FAIL_LOG | 인증 실패/회원권 만료/시간 제한 |
| PROC | VERIFY | 성공 또는 주의 이벤트 → FC 동일인 확인 카드 반영 |
| VERIFY | NO_DUP | 동일 회원 2시간 이내 재출석은 카드 미생성 |

## TC 후보

| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-I001-F2-01 | positive | manager, 유효 회원권 | IoT 밴드 체크인 | 출석 성공, 미배정 목록 추가 |
| TC-I001-F2-02 | positive | manager, 자동배정 설정 | 키오스크 QR 체크인 | 출석 성공, 락커 자동 배정 |
| TC-I001-F2-03 | negative | manager | 만료 회원 체크인 시도 | 실패 로그 추가, 실패 사유 표시 |
| TC-I001-F2-04 | positive | staff | 수동 출석 등록 버튼 클릭 | DLG-I001 모달 열림 |
| TC-I001-F2-05 | positive | fc | 신규 출석 이벤트 수신 | 출석 확인 카드 추가 + 알림음/토스트 |
| TC-I001-F2-06 | warning | fc | 사진 없는 회원 출석 | 기본 아바타 + 사진 촬영 안내 |
| TC-I001-F2-07 | positive | fc | 동일 회원 2시간 내 재출석 | 중복 카드 미생성 |
