---
title: 체성분 통합 관리 필터/검색 플로우
type: flowchart
scope: SCR-I006
dependencies: []
actors: [superAdmin, primary, owner, manager, fc]
lastUpdated: 2026-04-20
---

# F4 필터/검색 플로우 — SCR-I006 체성분 통합 관리

## 다이어그램

```mermaid
flowchart TD
    SCR_I006[SCR-I006 체성분 통합 관리] --> FILTER[필터 영역]

    FILTER --> F_MEMBER[회원 검색]
    FILTER --> F_DATE[측정일 범위]
    FILTER --> F_SOURCE[입력 소스 필터\n수기/파일/실시간API]
    FILTER --> F_STATUS[상태 필터\n정상/검수대기/오류]

    F_MEMBER --> APPLY{필터 적용}
    F_DATE --> APPLY
    F_SOURCE --> APPLY
    F_STATUS --> APPLY

    APPLY -->|": 결과 있음"|TABLE[측정 결과 목록 갱신]
    APPLY -->|": 없음"|EMPTY[검색 결과 없음]
    TABLE --> SORT[정렬\n최근 측정일/회원명]
    TABLE --> RESET[필터 초기화]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    class SCR_I006 screen
    class APPLY,TABLE system
    class EMPTY rbacBlocked
```

## TC 후보
| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-I006-F4-01 | positive | fc | 입력 소스 = InBody API 필터 | API 수신 데이터만 표시 | | TC-I006-F4-02 | positive | fc | 상태 = 검수대기 필터 | 검수 대기 데이터만 표시 | | TC-I006-F4-03 | positive | fc | 회원명 검색 | 해당 회원 체성분만 표시 |
