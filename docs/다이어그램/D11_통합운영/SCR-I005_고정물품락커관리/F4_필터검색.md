---
title: 고정 물품 락커 관리 필터/검색 플로우
type: flowchart
scope: SCR-I005
dependencies: []
actors: [superAdmin, primary, owner, manager, staff]
lastUpdated: 2026-04-20
---

# F4 필터/검색 플로우 — SCR-I005 고정 물품 락커 관리

## 다이어그램

```mermaid
flowchart TD
    SCR_I005[SCR-I005 고정 물품 락커 관리] --> TABS[상단 탭]

    TABS --> TAB_ALL[전체]
    TABS --> TAB_USED[사용중]
    TABS --> TAB_EXPIRE[만료 예정]
    TABS --> TAB_RECLAIM[회수 대기]
    TABS --> TAB_CHECK[점검중]

    TAB_ALL --> FILTER[추가 필터]
    FILTER --> F_MEMBER[회원명 검색]
    FILTER --> F_PRODUCT[상품명 필터]
    FILTER --> F_DATE[만료일 범위]
    FILTER --> F_STATUS[상태 필터]

    F_MEMBER --> APPLY{필터 적용}
    F_PRODUCT --> APPLY
    F_DATE --> APPLY
    F_STATUS --> APPLY

    APPLY -->|": 결과 있음"|TABLE[목록 갱신]
    APPLY -->|": 없음"|EMPTY[빈 상태]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    class SCR_I005 screen
    class APPLY,TABLE,TABS system
    class TAB_EXPIRE,TAB_RECLAIM warning
    class EMPTY rbacBlocked
```

## TC 후보
| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-I005-F4-01 | positive | manager | 만료 예정 탭 | D-7 이내 목록 | | TC-I005-F4-02 | positive | manager | 회원명 검색 | 해당 회원 락커만 표시 | | TC-I005-F4-03 | positive | manager | 회수 대기 탭 | 회수 대상 목록 |
