---
title: 고정 물품 락커 관리 상태별 화면 플로우
type: flowchart
scope: SCR-I005
dependencies: []
actors: [superAdmin, primary, owner, manager, staff]
lastUpdated: 2026-04-20
---

# F6 상태별 화면 플로우 — SCR-I005 고정 물품 락커 관리

## 다이어그램

```mermaid
flowchart TD
    ENTER([SCR-I005 진입]) --> AUTH{인증/권한}
    AUTH -->|": 미인증"|LOGIN[로그인]
    AUTH -->|": 권한없음"|FORBIDDEN[403]
    AUTH -->|": 정상"|LOADING{로딩}

    LOADING --> STATE_LOADING[스켈레톤]
    LOADING -->|": 성공 + 데이터"|STATE_NORMAL[배정 목록 정상]
    LOADING -->|": 성공 + 빈"|STATE_EMPTY[빈 상태\n배정된 락커가 없습니다]
    LOADING -->|": 실패"|STATE_ERROR[에러 + 재시도]

    STATE_NORMAL -->|": 만료 임박 있음"|BADGE_EXPIRE[만료 임박 배지\-7 이내 N건]
    STATE_NORMAL -->|": 회수 대상 있음"|BADGE_RECLAIM[회수 대상 배지]
    STATE_ERROR -->|": 재시도"|LOADING

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    class STATE_NORMAL,STATE_LOADING screen
    class STATE_ERROR,FORBIDDEN error
    class BADGE_EXPIRE,BADGE_RECLAIM warning
    class STATE_EMPTY rbacBlocked
    class AUTH,LOADING system
```

## TC 후보
| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-I005-F6-01 | positive | manager | 만료 임박 배정 있음 | 만료 임박 배지 표시 |
| TC-I005-F6-02 | positive | manager | 배정 없음 | 빈 상태 메시지 |
| TC-I005-F6-03 | negative | fc | 접근 | 403 |
