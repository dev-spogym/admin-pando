---
title: 통합 출석 관리 상태별 화면 플로우
type: flowchart
scope: SCR-I001
dependencies: []
actors: [superAdmin, primary, owner, manager, fc, staff]
lastUpdated: 2026-04-20
---

# F6 상태별 화면 플로우 — SCR-I001 통합 출석 관리

## 목적
로딩/빈/에러/권한없음/오프라인/IoT장애 등 UI 상태별 분기를 정의한다.

## 다이어그램

```mermaid
flowchart TD
    ENTER([SCR-I001 진입]) --> AUTH_CHECK{인증 상태}

    AUTH_CHECK -->|": 미인증"|STATE_LOGIN[로그인 리다이렉트]
    AUTH_CHECK -->|": 권한 없음"|STATE_FORBIDDEN[403 권한없음 페이지\n대시보드로 이동 버튼]
    AUTH_CHECK -->|": 인증 정상"|LOADING{데이터 로딩}

    LOADING -->|": 로딩 중"|STATE_LOADING[스켈레톤 로딩\카드 + 테이블 animate-pulse]
    LOADING -->|": 로드 성공 + 데이터 있음"|STATE_NORMAL[정상 화면\n실시간 스트림 표시]
    LOADING -->|": 로드 성공 + 데이터 없음"|STATE_EMPTY[빈 상태\n오늘 출석 기록이 없습니다]
    LOADING -->|": API 오류"|STATE_ERROR[에러 상태\n토스트 + 재시도 버튼]
    LOADING -->|": 네트워크 오프라인"|STATE_OFFLINE[오프라인 배너\n연결 대기 중...]

    STATE_NORMAL -->|": IoT 장비 오류"|IOT_BADGE[IoT 장애 배지 표시\-I003 링크]
    STATE_NORMAL -->|": WebSocket 끊김"|WS_RECONNECT[WebSocket 재연결 시도\n재연결 중 배너]

    STATE_ERROR -->|": 재시도 클릭"|LOADING
    STATE_OFFLINE -->|": 네트워크 복구"|LOADING
    WS_RECONNECT -->|": 재연결 성공"|STATE_NORMAL
    WS_RECONNECT -->|": 재연결 실패 3회"|STATE_ERROR

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    class STATE_NORMAL,STATE_LOADING screen
    class STATE_ERROR,STATE_FORBIDDEN error
    class STATE_OFFLINE,IOT_BADGE,WS_RECONNECT warning
    class STATE_EMPTY rbacBlocked
    class AUTH_CHECK,LOADING system
    class STATE_LOGIN success
```

## TC 후보

| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-I001-F6-01 | positive | manager | 페이지 진입 중 | 스켈레톤 로딩 표시 | | TC-I001-F6-02 | positive | manager, 오늘 출석 없음 | 페이지 로드 완료 | 빈 상태 메시지 표시 | | TC-I001-F6-03 | negative | manager | API 서버 오류 | 에러 토스트 + 재시도 버튼 | | TC-I001-F6-04 | negative | staff | 네트워크 오프라인 | 오프라인 배너 표시 | | TC-I001-F6-05 | negative | manager | WebSocket 연결 끊김 | 재연결 시도 배너 표시 | | TC-I001-F6-06 | negative | manager | IoT 장비 오류 상태 | 장애 배지 표시 + SCR-I003 링크 |
