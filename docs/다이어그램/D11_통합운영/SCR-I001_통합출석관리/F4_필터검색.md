---
title: 통합 출석 관리 필터/검색/정렬 플로우
type: flowchart
scope: SCR-I001
dependencies: []
actors: [superAdmin, primary, owner, manager, fc, staff]
lastUpdated: 2026-04-20
---

# F4 필터/검색/정렬 플로우 — SCR-I001 통합 출석 관리

## 목적
필터 조합, 검색, 정렬, 페이지네이션 동작을 정의한다.

## 다이어그램

```mermaid
flowchart TD
    SCR_I001[SCR-I001 통합 출석 관리] --> FILTER[필터 영역]

    FILTER --> F_TYPE[회원/직원/게스트 필터]
    FILTER --> F_RESULT[성공/실패/중복/퇴실 필터]
    FILTER --> F_BRANCH[지점 필터]
    FILTER --> F_CHANNEL[채널 필터\n앱QR/키오스크QR/얼굴인식/수동]
    FILTER --> F_LOCKER[락커상태 필터\n미배정/배정완료/대상아님]
    FILTER --> F_SEARCH[이름/회원번호 검색]
    FILTER --> F_DATE[날짜 범위 필터]

    F_TYPE --> APPLY{필터 적용}
    F_RESULT --> APPLY
    F_BRANCH --> APPLY
    F_CHANNEL --> APPLY
    F_LOCKER --> APPLY
    F_SEARCH --> APPLY
    F_DATE --> APPLY

    APPLY -->|": 결과 있음"|TABLE[출석 로그 테이블 갱신]
    APPLY -->|": 결과 없음"|EMPTY[빈 상태\n검색 결과가 없습니다]
    APPLY -->|": API 오류"|ERR[에러 토스트]

    TABLE --> SORT[컬럼 정렬\n시간/이름/채널/결과]
    TABLE --> PAGE[페이지네이션\n이전/다음/페이지 번호]
    TABLE --> RESET[필터 초기화]

    RESET --> SCR_I001

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    class SCR_I001 screen
    class APPLY,TABLE system
    class ERR error
    class EMPTY warning
    class SORT,PAGE success
```

## TC 후보

| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-I001-F4-01 | positive | manager | 회원 타입 + 실패 결과 필터 조합 | 해당 조건 출석 로그만 표시 |
| TC-I001-F4-02 | positive | manager | 이름 검색 | 검색어 포함 회원 출석만 표시 |
| TC-I001-F4-03 | positive | manager | 채널 = 키오스크QR | 키오스크 체크인만 필터 |
| TC-I001-F4-04 | positive | manager | 락커상태 = 미배정 | 미배정 회원만 표시 |
| TC-I001-F4-05 | positive | manager | 필터 초기화 클릭 | 전체 출석 로그 복원 |
