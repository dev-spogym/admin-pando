---
title: 키오스크 설정 상태별 화면 플로우
type: flowchart
scope: SCR-I002
dependencies: []
actors: [superAdmin, primary, owner]
lastUpdated: 2026-04-20
---

# F6 상태별 화면 플로우 — SCR-I002 키오스크 설정

## 다이어그램

```mermaid
flowchart TD
    ENTER([SCR-I002 진입]) --> AUTH{인증 상태}
    AUTH -->|": 미인증"|LOGIN[로그인 리다이렉트]
    AUTH -->|": 권한 없음"|FORBIDDEN[403 페이지]
    AUTH -->|": 정상"|LOADING{설정 로딩}
    LOADING --> STATE_LOADING[스켈레톤 로딩]
    LOADING -->|": 성공"|STATE_NORMAL[정상 설정 폼]
    LOADING -->|": 실패"|STATE_ERROR[에러 + 재시도]
    STATE_NORMAL -->|": 키오스크 미연결"|STATE_WARN[연결 없음 배지\n연결 테스트 안내]
    STATE_NORMAL -->|": 저장 중"|STATE_SAVING[버튼 로딩 + disabled]
    STATE_SAVING -->|": 완료"|STATE_NORMAL
    STATE_ERROR -->|": 재시도"|LOADING

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef system fill:#EDE7F6,stroke:#5E35B2
    class STATE_NORMAL,STATE_LOADING,STATE_SAVING screen
    class STATE_ERROR,FORBIDDEN error
    class STATE_WARN warning
    class AUTH,LOADING system
```

## TC 후보
| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-I002-F6-01 | positive | owner | 페이지 진입 | 스켈레톤 로딩 후 설정 폼 표시 |
| TC-I002-F6-02 | negative | manager | 접근 | 403 페이지 |
| TC-I002-F6-03 | negative | owner | 키오스크 미연결 상태 | 연결 없음 배지 표시 |
