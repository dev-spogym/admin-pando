---
title: 키오스크 설정 탭 전환 플로우
type: flowchart
scope: SCR-I002
dependencies: []
actors: [superAdmin, primary, owner]
lastUpdated: 2026-04-20
---

# F4 탭 전환 플로우 — SCR-I002 키오스크 설정

## 목적
설정 화면의 5개 탭 전환 및 미저장 경고 처리를 정의한다.

## 다이어그램

```mermaid
flowchart TD
    SCR_I002[SCR-I002 키오스크 설정] --> CURRENT_TAB[현재 탭]

    CURRENT_TAB -->|": 탭 클릭"|DIRTY{?}
    DIRTY -->|": 변경 없음"|SWITCH[탭 전환]
    DIRTY -->|": 변경 있음"|DLG_WARN[DLG-COM-003 이탈 경고]

    DLG_WARN -->|": 저장 후 이동"|SAVE_SWITCH[저장 후 탭 전환]
    DLG_WARN -->|": 저장 안 함"|SWITCH
    DLG_WARN -->|": 취소"|CURRENT_TAB

    SWITCH --> TAB_BASIC[기본 설정 탭]
    SWITCH --> TAB_SCREEN[화면 설정 탭]
    SWITCH --> TAB_TTS[음성/TTS 탭]
    SWITCH --> TAB_RULE[출석 규칙 탭]
    SWITCH --> TAB_LOCKER[락커 후처리 탭]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    class SCR_I002 screen
    class DLG_WARN modal
    class DIRTY,SWITCH system
    class TAB_BASIC,TAB_SCREEN,TAB_TTS,TAB_RULE,TAB_LOCKER warning
```

## TC 후보
| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-I002-F4-01 | positive | owner, 변경 없음 | 탭 클릭 | 즉시 탭 전환 | | TC-I002-F4-02 | positive | owner, 변경 있음 | 탭 클릭 | DLG-COM-003 이탈 경고 표시 | | TC-I002-F4-03 | positive | owner | DLG-COM-003 > 저장 후 이동 | 저장 후 탭 전환 |
