---
diagramId: DF02
title: 결제 데이터 흐름
type: erDiagram+flowchart
scope: Payment, Sale, SaleItem, Refund
actors: [staff, manager, member, PG, system]
tcMappings: [TC-DF02-01, TC-DF02-02, TC-DF02-03, TC-DF02-04]
lastUpdated: 2026-04-20
---

## 1. 엔티티 개요

결제(`Payment`)는 판매(`Sale`) 단위로 묶이며, 상품 라인아이템(`SaleItem`)과 환불(`Refund`)이 연결된다. 이용권 자동 개시의 트리거.

## 2. ER 다이어그램

```mermaid
erDiagram
  Sale ||--|{ SaleItem : 포함
  Sale ||--o{ Payment : 결제
  Payment ||--o{ Refund : 환불
  Sale }o--|| Member : 구매자
  Sale }o--|| Staff : 판매자
  SaleItem }o--|| Product : 상품
  SaleItem ||--o| Membership : 이용권생성
  Payment }o--|| PaymentMethod : 수단
```

## 3. 쓰기 경로 (POS 결제)

```mermaid
flowchart LR
  POS[SCR-S002 POS] -->|E_DF02_01| API1[POST /api/sales]
  API1 -->|E_DF02_02| Txn[(TRANSACTION BEGIN)]
  Txn -->|E_DF02_03| DB1[(INSERT sales)]
  DB1 -->|E_DF02_04| DB2[(INSERT sale_items)]
  DB2 -->|E_DF02_05| PG{PG 승인}
  PG -->|승인| DB3[(INSERT payments status=APPROVED)]
  PG -->|거부| Rollback[(ROLLBACK)] --> ErrResp[/4xx/]
  DB3 -->|E_DF02_06| DB4[(INSERT memberships status=ACTIVE)]
  DB4 -->|E_DF02_07| Commit[(COMMIT)]
  Commit -->|E_DF02_08| Evt[이벤트: sale.completed]
  Evt --> Audit[(audit_logs)]
  Evt --> Notif[영수증 알림]
  Commit --> Resp[201 OK] --> UI[영수증 화면]

  classDef api fill:#EDE7F6,stroke:#5E35B2
  classDef db fill:#E8F5E9,stroke:#2E7D32
  classDef external fill:#ECEFF1,stroke:#455A64,stroke-dasharray:3 3
  classDef error fill:#FFEBEE,stroke:#C62828
  class API1 api
  class DB1,DB2,DB3,DB4,Audit db
  class PG external
  class ErrResp,Rollback error
```

## 4. 읽기 경로 (매출 현황)

```mermaid
flowchart LR
  UI[SCR-S001 매출현황] -->|GET /api/sales?range| Cache{캐시}
  Cache -->|miss| DB[(SELECT sales JOIN payments JOIN members)]
  DB --> Agg[집계: 합계/수단별/담당자별] --> Cache
  Cache -->|hit| UI
```

## 5. 주요 필드

| 필드 | 테이블 | 타입 | 비고 |
|------|--------|------|------|
| id | sales | uuid | PK |
| member_id | sales | uuid | FK |
| total_amount | sales | bigint | 원 단위 |
| status | sales | enum | DRAFT/COMPLETED/CANCELED |
| payment_id | payments | uuid | PK |
| method | payments | enum | CARD/CASH/TRANSFER/POINT |
| approval_code | payments | text | PG 응답 |
| installment | payments | int | 할부 개월 🆕 |
| refund_amount | refunds | bigint | 부분 환불 |

## 6. 인덱스/제약

- `INDEX(sales.member_id, sales.created_at DESC)`
- `INDEX(sales.status, sales.created_at)` — 미수금/환불 조회
- `FK(sale_items.product_id)` ON DELETE RESTRICT
- 트랜잭션 무결성: 판매-결제-이용권 한 묶음

## 7. TC 후보

| TC ID | 타입 | 설명 |
|-------|:----:|------|
| TC-DF02-01 | positive | POS 결제 성공 → 이용권 ACTIVE 자동 생성 |
| TC-DF02-02-NEG | negative | PG 거부 → 전체 트랜잭션 롤백 |
| TC-DF02-03 | boundary | 할부 12개월 결제 🆕 |
| TC-DF02-04-EXC | exception | PG 타임아웃 → 정산 확인 큐 |
| TC-DF02-05 | positive | 부분 환불 → refund_amount 누적, sale.status 유지 |
