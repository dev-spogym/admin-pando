---
diagramId: DF01
title: 회원 데이터 흐름
type: erDiagram+flowchart
scope: Member, MembershipCard, Consultation
actors: [manager, fc, staff, system]
tcMappings: [TC-DF01-01, TC-DF01-02, TC-DF01-03]
lastUpdated: 2026-04-20
---

## 1. 엔티티 개요

회원 도메인의 핵심 엔티티는 `Member`이며, 이용권(`Membership`), 출석(`Attendance`), 결제(`Payment`), 체성분(`BodyComposition`), 상담(`Consultation`)과 연결된다.

## 2. ER 다이어그램

```mermaid
erDiagram
  Member ||--o{ Membership : 보유
  Member ||--o{ Payment : 결제
  Member ||--o{ Attendance : 출석
  Member ||--o{ BodyComposition : 측정
  Member ||--o{ Consultation : 상담
  Member ||--o{ Note : 메모
  Member ||--o| MembershipCard : 대표이용권
  Member }o--|| Branch : 소속
  Member }o--o| Member : 가족
  Membership }o--|| Product : 기반상품
```

## 3. 쓰기 경로 (등록)

```mermaid
flowchart LR
  Form[SCR-M002 회원등록 폼] -->|E_DF01_01| API1[POST /api/members]
  API1 -->|E_DF01_02| Valid{검증}
  Valid -->|중복| Reject[/409/]
  Valid -->|통과| DB1[(members INSERT)]
  DB1 -->|E_DF01_03| Evt[이벤트: member.created]
  Evt -->|E_DF01_04| CacheInv[캐시 무효화]
  Evt -->|E_DF01_05| Audit[(audit_logs INSERT)]
  DB1 -->|E_DF01_06| Resp[201 + memberId] --> UI[SCR-M004 상세이동]

  classDef form fill:#E3F2FD,stroke:#1976D2
  classDef api fill:#EDE7F6,stroke:#5E35B2
  classDef db fill:#E8F5E9,stroke:#2E7D32
  classDef cache fill:#FFF8E1,stroke:#F9A825
  classDef ui fill:#E0F7FA,stroke:#00838F
  class Form,UI form
  class API1 api
  class DB1,Audit db
  class CacheInv cache
```

## 4. 읽기 경로 (목록/상세)

```mermaid
flowchart LR
  UI1[SCR-M001 목록] -->|GET /api/members?q| Cache{캐시 hit?}
  Cache -->|hit| Resp1[응답 200]
  Cache -->|miss| DB2[(SELECT members WHERE branch_id)]
  DB2 --> Fill[캐시 갱신] --> Resp1
  Resp1 --> UI1

  UI2[SCR-M004 상세] -->|GET /api/members/:id| DB3[(JOIN memberships, payments)]
  DB3 --> Resp2[응답 200 + 15탭 데이터]
  Resp2 --> UI2
```

## 5. 주요 필드

| 필드 | 타입 | 제약 | 비고 |
|------|------|------|------|
| id | uuid | PK | |
| branch_id | uuid | FK | 필수 |
| name | text | not null | |
| phone | text | unique(branch_id) | 중복 검증 대상 |
| gender | enum | M/F/O | |
| birth | date | | |
| status | enum | 8종 | S1 MemberStatus |
| membership_expiry | date | | 자동 계산 |
| last_visit | timestamp | | 휴면 판정 기준 |
| deleted_at | timestamp | | 소프트 삭제 |

## 6. 인덱스/제약

- `UNIQUE(branch_id, phone) WHERE deleted_at IS NULL`
- `INDEX(status, membership_expiry)` — 만료 임박 조회
- `INDEX(last_visit)` — 휴면 판정
- `FK(branch_id) ON DELETE RESTRICT`

## 7. TC 후보

| TC ID | 타입 | 설명 |
|-------|:----:|------|
| TC-DF01-01 | positive | 신규 회원 등록 → DB INSERT + 이벤트 발행 |
| TC-DF01-02-NEG | negative | 동일 지점 내 동일 전화번호 등록 시 409 |
| TC-DF01-03 | boundary | 소프트 삭제된 회원의 전화번호로 재등록 허용 |
| TC-DF01-04 | positive | 회원 조회 시 캐시 hit 응답시간 < 50ms |
| TC-DF01-05-EXC | exception | branch_id 없는 요청 400 |
