---
title: 입출고 처리 모달 생명주기
type: flowchart
scope: DLG-057-002
dependencies: []
actors: [primary, owner, manager, front]
lastUpdated: 2026-04-20
---

# M1 모달 생명주기 — DLG-057-002 입출고 처리 🆕

## 다이어그램

```mermaid
flowchart TD
    TRIGGER_IN([SCR-057 입고 +]) --> OPEN_IN[모달 열림\n입고 수량 입력]
    TRIGGER_OUT([SCR-057 출고 -]) --> OPEN_OUT[모달 열림\n출고 수량 입력]

    OPEN_IN -->|": 확인"|InAPI{입고 API}
    InAPI -->|": 성공"|IN_SUCCESS((success\n입고 처리되었습니다.))
    InAPI -->|": 실패"|IN_FAIL((error\n입고에 실패했습니다.))

    OPEN_OUT -->|": 확인"|OutAPI{출고 API}
    OutAPI -->|": 성공"|OUT_SUCCESS((success\n출고 처리되었습니다.))
    OutAPI -->|": 실패"|OUT_FAIL((error\n출고에 실패했습니다.))

    OPEN_IN & OPEN_OUT -->|": 취소/ESC"|CLOSED([모달 닫힘])
    IN_SUCCESS & OUT_SUCCESS --> CLOSED

    classDef newFeature fill:#F3E5F5,stroke:#9C27B0,stroke-dasharray:5 5
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef system fill:#EDE7F6,stroke:#5E35B2
    class OPEN_IN,OPEN_OUT newFeature
    class IN_SUCCESS,OUT_SUCCESS success
    class IN_FAIL,OUT_FAIL error
    class InAPI,OutAPI system
```

## TC 후보

| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-057-002 | positive | 입고 모달 | 수량 입력 후 확인 | 현재고 증가 | | TC-057-003 | positive | 출고 모달 | 수량 입력 후 확인 | 현재고 감소 |
