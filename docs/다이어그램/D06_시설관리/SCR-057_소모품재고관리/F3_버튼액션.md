---
title: 소모품 재고 관리 버튼 액션 플로우
type: flowchart
scope: SCR-057
dependencies: []
actors: [primary, owner, manager, front]
lastUpdated: 2026-04-20
---

# F3 버튼 액션 플로우 — SCR-057 소모품 재고 관리 🆕

## 다이어그램

```mermaid
flowchart LR
    SCR_057[SCR-057 소모품 재고 관리]

    subgraph HeaderBtns["PageHeader 버튼"]
        BTN_REG[+ 소모품 등록]
        BTN_ORDER[발주]
    end

    subgraph TableBtns["테이블 행 액션"]
        BTN_IN[입고 +]
        BTN_OUT[출고 -]
    end

    subgraph TabBtns["탭 전환"]
        TAB_STOCK[재고 현황]
        TAB_HISTORY[입출고 이력]
        TAB_ORDER[발주 관리]
    end

    SCR_057 --> HeaderBtns
    SCR_057 --> TableBtns
    SCR_057 --> TabBtns

    BTN_REG --> RegModal[소모품 등록 모달\n품목명/카테고리/안전재고/단가]
    BTN_ORDER --> OrderModal[발주 모달\n부족 품목 일괄 발주]
    BTN_IN --> InModal[입고 수량 입력 모달]
    BTN_OUT --> OutModal[출고 수량 입력 모달]
    TAB_STOCK --> StockView[재고 현황 테이블]
    TAB_HISTORY --> HistView[입출고 이력 테이블]
    TAB_ORDER --> OrderView[발주 관리 테이블]

    classDef newFeature fill:#F3E5F5,stroke:#9C27B0,stroke-dasharray:5 5
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef screen fill:#E3F2FD,stroke:#1565C0,stroke-dasharray:5 5
    class SCR_057 newFeature
    class RegModal,OrderModal,InModal,OutModal modal
    class StockView,HistView,OrderView screen
```

## TC 후보

| TC ID | 타입 | Given | When | Then | |-------|------|-------|------|------| | TC-057-002 | positive | 재고 행 | 입고 + 클릭 | 입고 수량 입력 모달 열림 | | TC-057-003 | positive | 재고 행 | 출고 - 클릭 | 출고 수량 입력 모달 열림 |
