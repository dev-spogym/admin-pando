---
diagramId: SITEMAP_N6
title: FitGenie CRM 에러 및 복귀 경로
type: flowchart
lastUpdated: 2026-04-20
---

# N6 — 에러 복귀 경로

> 404/500/403 에러 발생 시 사용자 복귀 경로. 공통.md 에러 페이지(SCR-108) 기준.

```mermaid
flowchart TD
    USER["사용자 요청"]

    USER -->|"라우트 접근"| CHECK_AUTH{"인증 확인"}
    CHECK_AUTH -->|"미인증"| REDIRECT_LOGIN["/login으로 리다이렉트\nSCR-100"]
    CHECK_AUTH -->|"인증됨"| CHECK_RBAC{"권한(RBAC) 확인"}

    CHECK_RBAC -->|"권한 없음"| ERR_403["/forbidden\nERR-001 403 접근불가"]
    CHECK_RBAC -->|"권한 있음"| CHECK_ROUTE{"라우트 존재 확인"}

    CHECK_ROUTE -->|"라우트 없음 (App Router)"| ERR_404["not-found 처리\nERR-002 404"]
    CHECK_ROUTE -->|"라우트 존재"| CHECK_SERVER{"서버/API 응답"}

    CHECK_SERVER -->|"서버 오류 5xx"| ERR_500["error.tsx 처리\nERR-003 500"]
    CHECK_SERVER -->|"정상"| SCR_TARGET["목표 화면 렌더링"]

    ERR_403 --> RETURN_403{"403 복귀 옵션"}
    RETURN_403 -->|"홈으로"| HOME["/\n지점 대시보드"]
    RETURN_403 -->|"뒤로가기"| PREV_403["이전 페이지"]
    RETURN_403 -->|"로그아웃"| LOGOUT["/login\n로그아웃"]

    ERR_404 --> RETURN_404{"404 복귀 옵션"}
    RETURN_404 -->|"홈으로"| HOME
    RETURN_404 -->|"뒤로가기"| PREV_404["이전 페이지"]
    RETURN_404 -->|"검색"| SEARCH["글로벌 검색"]

    ERR_500 --> RETURN_500{"500 복귀 옵션"}
    RETURN_500 -->|"새로고침"| RETRY["현재 페이지 재시도"]
    RETURN_500 -->|"홈으로"| HOME
    RETURN_500 -->|"지원 문의"| SUPPORT["고객지원 채널"]

    REDIRECT_LOGIN -->|"로그인 성공"| HOME

    subgraph SESSION_ERR["세션 만료 처리"]
        SE_DETECT["세션 만료 감지\n(401 응답)"]
        SE_TOAST["토스트: 세션이 만료되었습니다"]
        SE_REDIRECT["/login?redirect={currentPath}"]
        SE_LOGIN["재로그인"]
        SE_RETURN["원래 페이지 복귀"]
        SE_DETECT --> SE_TOAST --> SE_REDIRECT --> SE_LOGIN --> SE_RETURN
    end

    CHECK_SERVER -->|"401 세션만료"| SE_DETECT

    subgraph NETWORK_ERR["네트워크 오류 처리"]
        NE_DETECT["네트워크 끊김 감지"]
        NE_TOAST["토스트: 네트워크 연결을 확인하세요"]
        NE_RETRY["자동 재시도 (3회)"]
        NE_FAIL["재시도 실패 → 오프라인 UI"]
        NE_DETECT --> NE_TOAST --> NE_RETRY --> NE_FAIL
    end

    CHECK_SERVER -->|"네트워크 오류"| NE_DETECT

    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2

    class ERR_403,ERR_404,ERR_500 error
    class HOME,SCR_TARGET screen
    class SE_TOAST,NE_TOAST warning
    class SE_RETURN,RETRY success
    class CHECK_AUTH,CHECK_RBAC,CHECK_ROUTE,CHECK_SERVER system
```

---

## 에러 유형별 복귀 경로 요약

| 에러 | 코드 | 발생 원인 | 복귀 경로 1 | 복귀 경로 2 | 복귀 경로 3 |
|------|------|---------|-----------|-----------|-----------|
| 403 접근불가 | ERR-001 | 권한 미보유 | `/` (홈) | 뒤로가기 | `/login` (로그아웃) |
| 404 페이지없음 | ERR-002 | 잘못된 URL | `/` (홈) | 뒤로가기 | 글로벌 검색 |
| 500 서버오류 | ERR-003 | 서버/API 오류 | 새로고침 | `/` (홈) | 고객지원 |
| 401 세션만료 | - | 토큰 만료 | `/login?redirect=` | - | - |
| 네트워크 오류 | - | 연결 끊김 | 자동 재시도 | 오프라인 UI | - |

## Next.js App Router 에러 파일 매핑

| 파일 | 처리 에러 | 라우트 |
|------|---------|--------|
| `app/(error)/not-found/page.tsx` | 404 Not Found | `/not-found` |
| `app/(error)/forbidden/page.tsx` | 403 Forbidden | `/forbidden` |
| `app/error.tsx` (글로벌) | 500 Server Error | 모든 라우트 |
| `app/not-found.tsx` (글로벌) | 404 (App Router) | 모든 라우트 |

## 비인증 리다이렉트 규칙

```
미인증 접근 → /login?redirect={원래경로}
로그인 성공 → redirect 파라미터 경로로 복귀
redirect 없음 → / (지점 대시보드)
```
