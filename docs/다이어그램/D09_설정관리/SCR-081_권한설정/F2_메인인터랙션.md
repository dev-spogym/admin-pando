---
title: SCR-081 권한 설정 — 메인 인터랙션 플로우
type: flowchart
scope: SCR-081
dependencies: [DLG-081-001, DLG-081-002, DLG-081-003]
actors: [primary, owner]
lastUpdated: 2026-04-20
---

## 목적
권한 설정 화면의 정상 시나리오: 역할 선택 → 매트릭스 조작 → 충돌 검증 → 저장

## 다이어그램

```mermaid
flowchart TD
    Entry([SCR-081 진입]) --> RoleList[역할 목록 표시\n6개 시스템 + 커스텀]
    RoleList -->|": 역할 클릭"|SelectRole[역할 선택\nbg-primary-light 하이라이트]
    SelectRole --> LoadMatrix[권한 매트릭스 로드\n9그룹 × 22메뉴 × 5권한]

    LoadMatrix -->|": primary 선택"|PrimaryMatrix[최고관리자 매트릭스\n모든 권한 true 고정\ncursor-default]
    LoadMatrix -->|": 기타 역할"|EditMatrix[편집 가능 매트릭스]

    EditMatrix -->|": 체크박스 클릭"|TogglePerm[권한 토글\nring-2 ring-amber 표시]
    TogglePerm -->|": 모두 허용"|AllAllow[전체 메뉴 전체 권한 true]
    TogglePerm -->|": 모두 거부"|AllDeny[전체 메뉴 전체 권한 false]

    EditMatrix -->|": 초기화 버튼"|DLG_001[DLG-081-001 초기화 확인]
    DLG_001 -->|": 확인"|ResetPerm[복원\]
    DLG_001 -->|": 취소"|EditMatrix

    EditMatrix -->|": 변경사항 저장"|ConflictCheck{충돌 검증 3규칙}
    ConflictCheck -->|": 충돌 없음"|SavePerm[저장\n저장 완료 표시]
    ConflictCheck -->|": 충돌 있음"|DLG_002[DLG-081-002 충돌 경고]
    DLG_002 -->|": 무시하고 저장"|SavePerm
    DLG_002 -->|": 취소"|EditMatrix

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef modal fill:#FFF3E0,stroke:#F57C00,color:#E65100
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    class RoleList,EditMatrix screen
    class DLG_001,DLG_002 modal
    class LoadMatrix,SelectRole,ConflictCheck,TogglePerm system
    class SavePerm success
    class PrimaryMatrix rbacBlocked
```

## TC 후보
- TC-081-001: 매니저 역할 클릭 → 매트릭스 표시
- TC-081-002: 최고관리자 선택 → 체크박스 변경 불가 (cursor-default)
- TC-081-003: 센터장 → 회원 목록 삭제 권한 클릭 → 토글 + ring-amber
- TC-081-004: 모두 허용 → 22메뉴 × 5권한 전체 true
- TC-081-006: 충돌 감지 → DLG-081-002 표시
- TC-081-007: 충돌 경고 → "무시하고 저장" → 저장
- TC-081-008: 초기화 → 확인 → 복원
