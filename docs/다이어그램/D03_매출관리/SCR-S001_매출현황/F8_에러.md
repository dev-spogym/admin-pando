---
title: 매출 현황 에러/예외/복구 플로우
type: flowchart
scope: SCR-S001
dependencies: []
actors: [primary, owner, manager, trainer, fc]
lastUpdated: 2026-04-20
---

## 1. 목적
SCR-S001에서 발생 가능한 모든 에러/예외 상황과 복구 경로를 표현한다.

## 2. 전제조건
- SCR-S001 진입 시도 또는 운영 중

## 3. 다이어그램

```mermaid
flowchart TD
    S001[SCR-S001 매출 현황] --> API_CALL[호출]

    API_CALL -->|": 200 OK"|SUCCESS[정상 데이터 표시]
    API_CALL -->|": 401 Unauthorized"|ERR_401[세션 만료\n로그인 페이지 리다이렉트]
    API_CALL -->|": 403 Forbidden"|ERR_403[권한없음 토스트\n이전 화면 유지]
    API_CALL -->|": 500 Server Error"|ERR_500[서버 오류 토스트\n매출 데이터를 불러오지 못했습니다.\nconsole]
    API_CALL -->|": Timeout"|ERR_TIMEOUT[타임아웃 토스트\n재시도 안내]
    API_CALL -->|": Network Error"|ERR_NETWORK[네트워크 오류\n오프라인 안내]

    ERR_401 --> LOGIN[로그인 화면]
    ERR_500 -->|": 재시도 클릭"|API_CALL
    ERR_TIMEOUT -->|": 재시도 클릭"|API_CALL
    ERR_NETWORK -->|": 연결 복구 후"|API_CALL

    S001 --> EXCEL_CALL[엑셀 다운로드 실행]
    EXCEL_CALL -->|": 다운로드 실패"|ERR_EXCEL[엑셀 다운로드에 실패했습니다.]

    S001 --> ROW_CLICK[행 클릭 → 조회]
    ROW_CLICK -->|": 404 Not Found"|ERR_ROW[해당 매출 데이터 없음\n경고 토스트]
    ROW_CLICK -->|": 500 Error"|ERR_ROW_SYS[서버 오류 토스트]

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class S001,LOGIN screen
    class ERR_401,ERR_403,ERR_500,ERR_TIMEOUT,ERR_NETWORK,ERR_EXCEL,ERR_ROW,ERR_ROW_SYS error
    class SUCCESS success
    class API_CALL,EXCEL_CALL,ROW_CLICK system
```

## 4. 엣지 설명

| 출발 | 도착 | 설명 |
|------|------|------|
| API_CALL | ERR_401 | 401 → 세션 만료 처리 |
| API_CALL | ERR_403 | 403 → 권한없음 토스트 |
| API_CALL | ERR_500 | 500 → 서버 오류 토스트 |
| API_CALL | ERR_TIMEOUT | 타임아웃 → 재시도 안내 |
| API_CALL | ERR_NETWORK | 네트워크 오류 → 오프라인 안내 |
| ERR_500 | API_CALL | 재시도 |
| ERR_TIMEOUT | API_CALL | 재시도 |
| EXCEL_CALL | ERR_EXCEL | 엑셀 다운로드 실패 |
| ROW_CLICK | ERR_ROW | 행 데이터 없음 |
