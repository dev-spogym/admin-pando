---
title: 미수금 관리 진입 플로우
type: flowchart
scope: SCR-S008
dependencies: []
actors: [primary, owner, manager, trainer, fc]
lastUpdated: 2026-04-20
---

## 1. 목적
SCR-S008 미수금 관리 진입 경로와 권한 분기를 표현한다.

## 2. 전제조건
- 로그인 완료

## 3. 다이어그램

```mermaid
flowchart TD
    SB([사이드바 > 매출 > 미수금 관리]) --> AUTH
    URL([직접 URL /unpaid]) --> AUTH
    NOTIF([알림 > 미수금 알림]) --> AUTH

    AUTH{권한 확인}
    AUTH -->|": primary"|FULL[전체 접근\n상태변경/메모 가능]
    AUTH -->|": trainer"|TRAINER[조회만\n상태변경 불가]
    AUTH -->|": readonly"|BLOCKED[접근 차단]

    FULL --> LOAD[미수금 데이터 로드]
    TRAINER --> LOAD

    LOAD --> S008[SCR-S008 미수금 관리\n4통계카드 + 탭 + 테이블]
    LOAD -->|": unpaid 오류"|FALLBACK[sales unpaid 대체]
    LOAD --> ERR((미수금 데이터를 불러오지 못했습니다.))
    FALLBACK --> S008

    classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef rbacBlocked fill:#F5F5F5,stroke:#9E9E9E,color:#616161
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class S008 screen
    class ERR error
    class AUTH,LOAD system
    class BLOCKED rbacBlocked
    class FALLBACK warning
```

## 4. 엣지 설명

| 출발 | 도착 | 설명 | |---------|------|------|------| | | AUTH | TRAINER | 트레이너 조회만 | | | LOAD | FALLBACK | unpaid 테이블 오류 → sales fallback |
