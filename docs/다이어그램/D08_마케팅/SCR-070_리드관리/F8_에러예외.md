---
title: 리드 관리 에러/예외/복구 플로우
type: flowchart
scope: SCR-070
dependencies: []
actors: [primary, owner, manager, fc]
lastUpdated: 2026-04-20
---

## 1. 목적

SCR-070에서 발생 가능한 에러/예외 케이스와 복구 경로를 네거티브 TC 원천으로 제공한다.

## 2. 전제조건

- SCR-070 접근 가능 역할 로그인

## 3. 다이어그램

```mermaid
flowchart TD
  SCR_070[SCR-070 리드 관리] --> ACT[액션 수행]

  ACT -->|목록 로드|API_LOAD[GET /]
  ACT -->|리드 등록|API_CREATE[POST /]
  ACT -->|리드 수정|API_UPDATE[PATCH]
  ACT -->|리드 삭제|API_DELETE[DELETE]

  API_LOAD -->|200|SUCCESS[정상 렌더]
  API_LOAD -->|401 미인증|ERR_AUTH[로그인 페이지 리다이렉트]
  API_LOAD -->|403 권한없음|ERR_PERM[권한없음 toast + 이전 화면]
  API_LOAD -->|500 서버오류|ERR_SERVER[서버오류 toast\n재시도 버튼]
  API_LOAD -->|Timeout|ERR_TIMEOUT[타임아웃 toast\n재시도 안내]
  API_LOAD -->|네트워크 단절|ERR_NET[오프라인 banner\n자동 재연결]

  API_CREATE -->|이름 필드 빈값|ERR_VALID[toast 이름 입력]
  API_CREATE -->|500|ERR_SERVER
  API_UPDATE -->|500|ERR_SERVER
  API_DELETE -->|500|ERR_SERVER

  ERR_SERVER -->|재시도|ACT
  ERR_TIMEOUT -->|재시도|ACT
  ERR_NET -->|재연결|API_LOAD

  classDef screen fill:#E3F2FD,stroke:#1976D2,color:#0D47A1
  classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
  classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
  classDef system fill:#EDE7F6,stroke:#5E35B2

  class SCR_070,SUCCESS screen
  class ERR_AUTH,ERR_PERM,ERR_SERVER,ERR_TIMEOUT,ERR_NET,ERR_VALID error
  class API_LOAD,API_CREATE,API_UPDATE,API_DELETE,ACT system
```

## 4. 엣지 설명

| 에러 코드 | 처리 |
|----------|------|
| 401 미인증 | 로그인 리다이렉트 |
| 403 권한없음 | toast + 이전 화면 |
| 500 서버오류 | 에러 toast + 재시도 |
| Timeout | 타임아웃 toast + 재시도 |
| 네트워크 단절 | 오프라인 banner |
| 필드 검증 실패 | toast |
