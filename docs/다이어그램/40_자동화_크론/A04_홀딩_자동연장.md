---
diagramId: A04_홀딩_자동연장
title: 홀딩 기간만큼 이용권 만료일 자동 연장
type: flowchart
scope: SYSTEM_CRON
tcMappings: []
lastUpdated: 2026-04-20
---

# A04 — 홀딩 자동 연장 계산

## 1. 개요

| 항목 | 내용 |
|------|------|
| 트리거 | 크론 — 매일 00:20 |
| 대상 엔티티 | Membership, MembershipHolding |
| 조건 | 홀딩 종료일이 도래한 이용권 (holdingEndAt <= NOW()) |
| 결과 | 홀딩 기간만큼 expiresAt 연장, status = ACTIVE 복원 |
| 관련 화면 | SCR-M004-02 이용권 탭, DLG-M003 홀딩 등록/해제 |

## 2. 발생 조건

- `Membership.status = HOLDING`
- `MembershipHolding.endAt <= NOW()`
- 홀딩 일수 = `endAt - startAt`
- 새 만료일 = 기존 만료일 + 홀딩 일수

## 3. 다이어그램

```mermaid
flowchart TD
    CRON([크론 트리거<br/>매일 00:20]) -->|E_A04_CRON_QUERY_01| QUERY[홀딩 종료 도래 이용권 조회<br/>status=HOLDING AND holdingEndAt<=NOW]

    QUERY -->|E_A04_QUERY_EMPTY_01 없음| EMPTY[대상 없음<br/>정상 종료]
    QUERY -->|E_A04_QUERY_FOUND_01 있음| LOOP[대상 이용권 순회]

    LOOP -->|E_A04_LOOP_CALC_01| CALC[홀딩 일수 계산<br/>days = endAt - startAt]
    CALC -->|E_A04_CALC_NEW_DATE_01| NEW_DATE[새 만료일 계산<br/>newExpiry = expiresAt + days]

    NEW_DATE -->|E_A04_TXN_START_01| TXN[트랜잭션 시작]
    TXN -->|E_A04_UPDATE_EXP_01| UPDATE_EXPIRY[Membership.expiresAt 업데이트<br/>= newExpiry]
    UPDATE_EXPIRY -->|E_A04_UPDATE_STATUS_01| UPDATE_STATUS[Membership.status<br/>HOLDING → ACTIVE]
    UPDATE_STATUS -->|E_A04_UPDATE_HOLDING_01| CLOSE_HOLDING[MembershipHolding.status<br/>= COMPLETED]

    CLOSE_HOLDING -->|E_A04_COMMIT_01| COMMIT{트랜잭션 커밋}
    COMMIT -->|E_A04_COMMIT_OK_01 성공| LOG_HISTORY[연장 이력 저장<br/>days / newExpiry 기록]
    COMMIT -->|E_A04_COMMIT_FAIL_01 실패| ROLLBACK[롤백<br/>오류 로그]

    LOG_HISTORY -->|E_A04_NOTIFY_01| NOTIFY[홀딩 해제 알림 발송<br/>새 만료일 안내]
    NOTIFY -->|E_A04_NOTIFY_OK_01| LOG_OK[알림 이력 저장]
    NOTIFY -->|E_A04_NOTIFY_FAIL_01| LOG_NOTIFY_FAIL[알림 실패 로그]

    LOG_OK & LOG_NOTIFY_FAIL & ROLLBACK -->|E_A04_NEXT_01| NEXT{다음 이용권?}
    NEXT -->|E_A04_NEXT_YES_01| LOOP
    NEXT -->|E_A04_NEXT_NO_01 완료| REPORT[실행 리포트]
    REPORT -->|E_A04_END_01| END([크론 완료])

    classDef cron fill:#E0F2F1,stroke:#00695C
    classDef system fill:#EDE7F6,stroke:#5E35B2
    classDef success fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef error fill:#FFEBEE,stroke:#C62828,color:#B71C1C
    classDef warning fill:#FFF8E1,stroke:#F9A825,color:#F57F17

    class CRON,END cron
    class QUERY,LOOP,CALC,COMMIT,NEXT system
    class LOG_HISTORY,LOG_OK,REPORT success
    class ROLLBACK,LOG_NOTIFY_FAIL error
    class EMPTY warning
```

## 4. 복구/재시도 전략

| 상황 | 전략 |
|------|------|
| 트랜잭션 실패 | 롤백, 오류 로그, 다음 크론 재처리 |
| 홀딩 일수 0 이하 | 연장 없이 ACTIVE 복원만 수행 |
| 중복 실행 방지 | COMPLETED 상태 확인으로 중복 방지 |

## 5. 사용자 노출 메시지

| 채널 | 메시지 |
|------|--------|
| SMS | "[FitGenie] 기간정지가 해제되었습니다. 새 만료일: {newExpiry}. 다시 이용하세요!" |
| 앱 알림 | "홀딩 해제 완료 — 이용권이 {days}일 연장되었습니다." |

## 6. TC 후보

| TC ID | 타입 | Given | When | Then |
|-------|------|-------|------|------|
| TC-A04-01 | positive | HOLDING 이용권, holdingEndAt=어제 | 크론 00:20 | status=ACTIVE, expiresAt 연장 |
| TC-A04-02 | positive | 30일 홀딩 | 크론 실행 | expiresAt = 기존+30일 |
| TC-A04-03 | negative | holdingEndAt=내일 | 크론 실행 | 아직 처리 안 됨 |
| TC-A04-04 | negative | 트랜잭션 실패 | 크론 실행 | 롤백, 다음 날 재처리 |
| TC-A04-05 | edge | 홀딩 일수 0 | 크론 실행 | 연장 없이 ACTIVE 복원 |
