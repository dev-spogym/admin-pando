# DLG-S007 할부상세 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S007 |
| 상태 코드 | `loading` |
| 경로 | SCR-S009 위 모달 |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | SCR-S009 할부 행 클릭 |
| 다음 상태 | `02-열림` / `03-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S007_할부상세/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S007 할부상세 — 상태: 로딩 (신규 기획)

파일: src/app/installments/page.tsx 내 InstallmentDetailModal

모달 크기: 480px × 600px

로딩 UI:
<Modal isOpen={isOpen} onClose={onClose} title="할부 상세">
  <div className="p-6 space-y-3">
    {Array.from({ length: 5 }).map((_, i) => (
      <div key={i} className="flex justify-between">
        <div className="animate-pulse bg-gray-200 h-4 w-24 rounded" />
        <div className="animate-pulse bg-gray-200 h-4 w-28 rounded" />
      </div>
    ))}
    <div className="mt-4 animate-pulse bg-gray-200 h-40 rounded-xl" />
  </div>
</Modal>

데이터 로드:
const fetchInstallmentDetail = async (id: number) => {
  setIsLoading(true);
  const { data, error } = await supabase
    .from('installments')
    .select('*')
    .eq('id', id)
    .single();
  if (error) {
    toast.error('데이터를 불러올 수 없습니다.');
    onClose();
  } else {
    setInstallment(data);
  }
  setIsLoading(false);
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_OPEN_01: SCR-S009 할부 행 클릭 → installments/:id 조회 중

### 연결 화면
- 다음: `02-열림` / `03-에러`
