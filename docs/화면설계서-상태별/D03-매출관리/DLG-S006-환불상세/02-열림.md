# DLG-S006 환불상세 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S006 |
| 상태 코드 | `open` |
| 경로 | SCR-S007 위 모달 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | [닫기] → 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S006_환불상세/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S006 환불상세 — 상태: 열림 (신규 기획)

파일: src/app/refunds/page.tsx 내 RefundDetailModal

모달 크기: 480px × 560px

<Modal isOpen={isOpen} onClose={onClose} title="환불 상세">
  <div className="divide-y">
    <div className="p-6 space-y-3">
      <h4 className="text-xs font-semibold text-gray-400 uppercase mb-3">환불 정보</h4>
      <InfoRow label="환불일" value={refund.refundDate} />
      <InfoRow label="회원명" value={refund.memberName} />
      <InfoRow label="상품명" value={refund.productName} />
      <InfoRow label="환불금액" value={formatKRW(refund.amount)} valueClassName="font-bold text-red-600" />
      <InfoRow label="결제수단" value={refund.method} />
      <InfoRow label="환불사유" value={refund.reason} />
      <InfoRow label="담당자" value={refund.staffName} />
    </div>

    <div className="p-6">
      <div className="flex items-center justify-between">
        <span className="text-sm text-gray-500">처리 상태</span>
        <StatusBadge
          label={refund.status}
          variant={
            refund.status === '완료' ? 'success' :
            refund.status === '거절' ? 'error' : 'warning'
          }
        />
      </div>
      {refund.status === '거절' && refund.rejection_reason && (
        <div className="mt-3 p-3 bg-red-50 rounded-lg">
          <p className="text-xs text-red-600">반려사유: {refund.rejection_reason}</p>
        </div>
      )}
    </div>
  </div>

  <div className="p-4 border-t">
    <Button variant="outline" className="w-full" onClick={onClose}>
      닫기
    </Button>
  </div>
</Modal>

사용 유틸:
- formatKRW from '@/lib/format'
- StatusBadge, Modal, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_LOAD_OK_01: refund 상세 데이터 로드 완료

### 필수 데이터
| 항목 | 테이블 | 설명 |
|------|--------|------|
| 환불 상세 | `refunds` | id=refundId 단건 |

### 인터랙션 (User Actions)
1. [닫기] / X / ESC → 닫힘

### 비즈니스 룰
- 거절 상태: 반려사유 표시
- 읽기 전용 (수정 불가)

### 연결 화면
- 닫힘 → SCR-S007 유지
