# DLG-S008 납입처리 — 상태: 처리중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S008 |
| 상태 코드 | `processing` |
| 경로 | DLG-S007 위 중첩 모달 |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` [납입 확인] 클릭 |
| 다음 상태 | `03-성공` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S008_납입처리/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S008 납입처리 — 상태: 처리중 (E_CONFIRM_01)

파일: src/app/installments/page.tsx 내 PaymentModal

처리중 UI (버튼 비활성화 + 스피너):
<Modal isOpen={showPayModal} onClose={() => {}} title="납입 처리">
  {/* 동일 내용, 버튼만 변경 */}
  <div className="p-4 border-t flex gap-3">
    <Button variant="outline" className="flex-1" disabled>
      취소
    </Button>
    <Button variant="primary" className="flex-1" disabled>
      <Loader2 className="h-4 w-4 animate-spin mr-2" />
      처리중...
    </Button>
  </div>
</Modal>

처리 로직:
const handleConfirmPayment = async () => {
  setIsPayProcessing(true);
  const { error } = await supabase
    .from('installment_payments')
    .insert({
      installment_id: installment.id,
      paid_date: payDate,
      amount: installment.monthly_amount,
      payment_method: payMethod,
      branch_id: branchId,
    });

  if (error) {
    toast.error('납입 처리에 실패했습니다.');
    setIsPayProcessing(false);
  } else {
    setShowPayModal(false);
    await fetchInstallmentDetail(installment.id);  // DLG-S007 갱신
    toast.success('납입이 처리되었습니다.');
    setIsPayProcessing(false);
  }
};

사용 아이콘: Loader2 from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_CONFIRM_01: [납입 확인] 클릭 → supabase INSERT 호출 중

### 인터랙션 (User Actions)
- 모든 버튼 비활성화 (취소 불가)
- X 버튼도 비활성화 (onClose 빈 함수)

### 비즈니스 룰
- installment_payments 테이블에 납입 기록 INSERT
- 성공 시 installments.paid_months +1, remaining_amount 감소 (트리거 또는 RPC)
- 완납 시 installments.status → COMPLETED

### 연결 화면
- `03-성공` (INSERT 성공)
- `04-에러` (INSERT 실패)
