# DLG-S002 구매자검색 — 상태: 검색 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S002 |
| 상태 코드 | `searching` |
| 경로 | SCR-S002 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` (검색어 입력) |
| 다음 상태 | `03-결과있음` / `04-결과없음` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S002_구매자검색/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S002 구매자검색 — 상태: 검색 중 (isSearching=true)

파일: src/app/pos/page.tsx 내 BuyerSearchModal

검색 중 스켈레톤:
{isSearching && (
  <div className="space-y-1">
    {Array.from({ length: 3 }).map((_, i) => (
      <div key={i} className="animate-pulse flex items-center gap-3 p-3 border-b">
        <div className="bg-gray-200 rounded-full w-9 h-9 flex-shrink-0" />
        <div className="flex-1 space-y-1.5">
          <div className="bg-gray-200 h-3 w-20 rounded" />
          <div className="bg-gray-200 h-3 w-28 rounded" />
        </div>
      </div>
    ))}
  </div>
)}

검색 입력 인디케이터:
- 입력창 우측 Loader2 animate-spin (검색 중 표시)
{isSearching && (
  <Loader2 className="absolute right-3 top-1/2 -translate-y-1/2 h-4 w-4 text-blue-400 animate-spin" />
)}

조건: searchQuery.length > 0 AND isSearching=true
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 검색어 입력 후 debounce 300ms 경과 → API 호출 중

### 인터랙션
- 추가 입력 가능 (기존 요청 취소 후 새 요청)

### 연결 화면
- 검색 완료 → `03-결과있음` 또는 `04-결과없음`
