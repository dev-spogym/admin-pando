# DLG-S002 구매자검색 — 상태: 검색 결과 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S002 |
| 상태 코드 | `no-result` |
| 경로 | SCR-S002 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `02-검색중` |
| 다음 상태 | 검색어 변경 → `02-검색중` / 비회원 → 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S002_구매자검색/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S002 구매자검색 — 상태: 결과 없음

파일: src/app/pos/page.tsx 내 BuyerSearchModal (searchResults.length === 0 && searchQuery.length > 0)

결과 없음 UI:
<div className="flex flex-col items-center py-10 text-center">
  <UserX className="h-10 w-10 text-gray-300 mb-2" />
  <p className="text-sm font-semibold text-gray-500">검색 결과가 없습니다.</p>
  <p className="text-xs text-gray-400 mt-1">
    "{searchQuery}" 회원을 찾을 수 없습니다.
  </p>
  <button
    className="mt-4 text-sm text-blue-500 hover:underline"
    onClick={() => { onSelectBuyer(null); onClose(); }}
  >
    비회원으로 진행
  </button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- members 검색 결과 0건

### 인터랙션
1. 검색어 변경 → 재검색
2. [비회원으로 진행] → buyer=null, 닫힘

### 연결 화면
- 검색어 변경 → `02-검색중`
- 비회원 → 닫힘 (SCR-S002)
