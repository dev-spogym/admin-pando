# DLG-S015 환불요청 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S015 |
| 상태 코드 | `error` |
| 경로 | SCR-S012 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `02-처리중` INSERT 실패 |
| 다음 상태 | `01-열림` (재시도) / 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S015_환불요청/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S015 환불요청 — 상태: 에러 (E_REQUEST_ERR_01)

파일: src/app/payment-cancel/page.tsx 내 RefundRequestModal

에러 처리:
if (error) {
  toast.error('환불 요청에 실패했습니다.');
  setIsProcessing(false);
  // 모달 유지 → 재시도 또는 취소 가능
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_REQUEST_ERR_01: refunds INSERT 실패

### 비즈니스 룰
- 모달 닫히지 않음 (재시도 가능)
- toast.error 표시
- [환불 요청] 버튼 다시 활성화
- 입력값 유지

### 연결 화면
- `01-열림` (재시도)
- 닫힘 ([취소] 클릭)
