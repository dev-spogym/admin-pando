# DLG-S015 환불요청 — 상태: 처리중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S015 |
| 상태 코드 | `processing` |
| 경로 | SCR-S012 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` [환불 요청] 클릭 |
| 다음 상태 | `03-성공` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S015_환불요청/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S015 환불요청 — 상태: 처리중 (E_REQUEST_01)

파일: src/app/payment-cancel/page.tsx 내 RefundRequestModal

처리중 UI:
<div className="p-4 border-t flex gap-3">
  <Button variant="outline" className="flex-1" disabled>취소</Button>
  <Button variant="primary" className="flex-1" disabled>
    <Loader2 className="h-4 w-4 animate-spin mr-2" />
    요청중...
  </Button>
</div>

요청 로직:
const handleRequest = async () => {
  setIsProcessing(true);
  const { error } = await supabase.from('refunds').insert({
    sale_id: sale.id,
    member_id: sale.member_id,
    member_name: sale.member_name,
    product_name: sale.product_name,
    original_amount: sale.amount,
    refund_type: reqType,
    refund_amount: reqAmount,
    reason: reqReason,
    status: 'PENDING',
    requested_by: userId,
    branch_id: branchId,
  });

  if (error) {
    toast.error('환불 요청에 실패했습니다.');
    setIsProcessing(false);
  } else {
    onClose();
    onRefresh();
    toast.success('환불 요청이 접수되었습니다.');
  }
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_REQUEST_01: [환불 요청] 클릭 → refunds INSERT 호출 중

### 비즈니스 룰
- status: PENDING (승인 대기)
- requested_by: 요청자 userId 기록
- 모든 버튼 비활성화

### 연결 화면
- `03-성공` (INSERT 성공)
- `04-에러` (INSERT 실패)
