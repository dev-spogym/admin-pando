# SCR-S012 결제취소/부분환불 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S012 |
| 상태 코드 | `loading` |
| 경로 | `/sales/cancel` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | DLG-S001에서 [결제 취소] 클릭 |
| 다음 상태 | `02-취소가능` / `03-승인대기` / `04-반려` / `05-환불완료` / `06-부분환불완료` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S012_결제취소_부분환불/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S012 결제취소/부분환불 — 상태: 로딩

파일: src/app/sales/cancel/page.tsx (isLoading=true 분기)

레이아웃:
- AppLayout 풀너비 데스크톱
- PageHeader: "결제 취소 / 부분환불" + 뒤로가기 버튼

스켈레톤:
1. 결제 정보 카드 (h-40): animate-pulse bg-gray-200 rounded-xl
2. 환불 건 목록 헤더 + 3행: animate-pulse bg-gray-100 h-12 rounded mb-1
3. 하단 액션 버튼 영역: animate-pulse bg-gray-200 h-10 rounded w-48

데이터 로드:
- URL 파라미터 saleId로 해당 매출 건 조회
supabase.from('sales').select('*').eq('id', saleId).single()

aria-busy="true" aria-label="결제 취소 데이터 로딩 중"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- DLG-S001 매출상세에서 [결제 취소] 버튼 클릭 후 진입
- saleId 파라미터로 해당 매출 건 조회 중

### 연결 화면
- 다음: saleId에 해당하는 환불 상태별 화면
