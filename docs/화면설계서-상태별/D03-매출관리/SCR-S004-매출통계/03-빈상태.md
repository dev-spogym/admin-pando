# SCR-S004 매출통계 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S004 |
| 상태 코드 | `empty` |
| 경로 | `/sales/stats` |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 기간 변경 → `01-로딩` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S004_매출통계/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S004 매출통계 — 상태: 빈상태 (선택 기간 매출 0건)

파일: src/app/sales/stats/page.tsx (statsData.length === 0 분기)

빈 상태 UI (차트 영역):
<div className="flex flex-col items-center justify-center h-64 bg-gray-50 rounded-xl border">
  <BarChart2 className="h-14 w-14 text-gray-300 mb-3" />
  <p className="text-base font-semibold text-gray-500">통계 데이터가 없습니다.</p>
  <p className="text-sm text-gray-400 mt-1">다른 기간을 선택해주세요.</p>
</div>

StatCardGrid: 0 표시 (회색)
기간 선택: 유지 (변경 가능)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 선택 기간에 매출 데이터 0건

### 인터랙션 (User Actions)
1. 기간 변경 → 재조회

### 연결 화면
- 기간 변경 → `01-로딩` → `02-정상`
