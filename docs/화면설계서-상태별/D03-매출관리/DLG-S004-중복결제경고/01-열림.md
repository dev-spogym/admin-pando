# DLG-S004 중복결제경고 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S004 |
| 상태 코드 | `open` |
| 경로 | DLG-S003 위 중첩 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | DLG-S003 중복 감지 |
| 다음 상태 | [진행] → DLG-S003 결제 처리 / [취소] → DLG-S003 유지 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S004_중복결제경고/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S004 중복결제경고 — 상태: 열림 (DLG-S003 위 중첩)

파일: src/app/pos/payment/page.tsx 내 DuplicatePaymentWarning

모달 크기: 320px × 240px (DLG-S003 위에 중첩)

<Modal
  isOpen={showDupWarning}
  onClose={() => setShowDupWarning(false)}
  title=""
  className="z-[60]"  // DLG-S003(z-50)보다 높게
>
  <div className="p-6 text-center">
    <div className="w-14 h-14 rounded-full bg-orange-100 flex items-center justify-center mx-auto mb-4">
      <AlertTriangle className="h-8 w-8 text-orange-500" />
    </div>
    <h3 className="font-bold text-gray-800 mb-2">중복 결제 감지</h3>
    <p className="text-sm text-gray-600 mb-5">
      최근 5분 내 동일한 금액의 결제가 있습니다.
      계속 진행하시겠습니까?
    </p>
    <div className="flex gap-3">
      <Button
        variant="outline"
        className="flex-1"
        onClick={() => setShowDupWarning(false)}
      >
        취소
      </Button>
      <Button
        variant="warning"
        className="flex-1"
        onClick={() => {
          setShowDupWarning(false);
          processPayment(); // 강제 진행
        }}
      >
        그래도 진행
      </Button>
    </div>
  </div>
</Modal>

중첩 오버레이 처리:
- DLG-S003: z-50
- DLG-S004: z-60 (더 높은 z-index)
- DLG-S004 오버레이: bg-black/20 (반투명)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_DUP_TRIGGER_01: DLG-S003에서 중복결제 감지 시 자동 표시

### 인터랙션 (User Actions)
1. [그래도 진행] → DLG-S004 닫힘 → processPayment() 강제 실행
2. [취소] → DLG-S004 닫힘 → DLG-S003 유지

### 비즈니스 룰
- 중복 판정: buyer_id + sale_price 동일 + 5분 이내
- 비회원(buyer=null): 중복 감지 미적용

### 연결 화면
- [진행] → DLG-S003 `02-처리중` → SCR-S003 `04-완료`
- [취소] → DLG-S003 `01-열림` 유지
