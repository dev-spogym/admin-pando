# DLG-S010 세금계산서상세 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S010 |
| 상태 코드 | `open` |
| 경로 | SCR-S010 위 모달 |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` 완료 |
| 다음 상태 | [발행] → DLG-S011 / [닫기] → 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S010_세금계산서상세/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S010 세금계산서상세 — 상태: 열림 (신규 기획)

파일: src/app/tax-invoices/page.tsx 내 TaxInvoiceDetailModal

모달 크기: 480px × 560px

<Modal isOpen={isOpen} onClose={onClose} title="세금계산서 상세">
  <div className="p-6 border-b space-y-3">
    <InfoRow label="발행 번호" value={inv.invoice_number} />
    <InfoRow label="거래처명" value={inv.company_name} />
    <InfoRow label="사업자번호" value={inv.business_number} />
    <InfoRow label="공급가액" value={formatKRW(inv.supply_amount)} />
    <InfoRow label="세액" value={formatKRW(inv.tax_amount)} />
    <InfoRow label="합계" value={formatKRW(inv.total_amount)} valueClassName="font-bold text-lg" />
    <InfoRow label="발행일" value={inv.issued_at ?? '-'} />
    <InfoRow label="상태"
      value={<StatusBadge label={INV_STATUS_KO[inv.status]} variant={INV_STATUS_VARIANT[inv.status]} />} />
  </div>

  {/* 품목 */}
  <div className="p-6">
    <h4 className="text-sm font-semibold mb-3">품목</h4>
    <div className="space-y-1 text-sm">
      {inv.items?.map((item, i) => (
        <div key={i} className="flex justify-between p-2 bg-gray-50 rounded">
          <span>{item.name}</span>
          <span>{formatKRW(item.amount)}</span>
        </div>
      ))}
    </div>
  </div>

  <div className="p-4 border-t flex gap-3">
    <Button variant="outline" className="flex-1" onClick={onClose}>닫기</Button>
    {inv.status === 'PENDING' && (
      <Button
        variant="primary"
        className="flex-1"
        onClick={() => setShowIssueModal(true)}
      >
        발행
      </Button>
    )}
  </div>
</Modal>

const INV_STATUS_KO = { PENDING: '대기', ISSUED: '발행완료', CANCELLED: '취소' };
const INV_STATUS_VARIANT = { PENDING: 'warning', ISSUED: 'success', CANCELLED: 'error' };

타입:
interface TaxInvoice {
  id: number;
  invoice_number: string;
  company_name: string;
  business_number: string;
  supply_amount: number;
  tax_amount: number;
  total_amount: number;
  issued_at: string | null;
  status: 'PENDING' | 'ISSUED' | 'CANCELLED';
  items: { name: string; amount: number }[];
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_LOAD_OK_01: tax_invoices 상세 로드 완료

### 필수 데이터
| 항목 | 설명 |
|------|------|
| taxInvoice | 세금계산서 상세 (번호, 거래처, 금액, 품목, 상태) |

### 인터랙션 (User Actions)
1. [발행] → DLG-S011 (status=PENDING일 때만 표시)
2. [닫기] / X → 닫힘

### 비즈니스 룰
- ISSUED / CANCELLED 상태: [발행] 버튼 숨김
- 세액 = 공급가액 × 10%
- 합계 = 공급가액 + 세액

### 연결 화면
- DLG-S011 세금계산서발행 (중첩)
- 닫힘
