# DLG-S012 목표매출설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S012 |
| 상태 코드 | `loading` |
| 경로 | SCR-S004 또는 SCR-S005 위 모달 |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | [목표 설정] 버튼 클릭 |
| 다음 상태 | `02-열림` / `03-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S012_목표매출설정/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S012 목표매출설정 — 상태: 로딩 (기존 목표 조회 중)

파일: src/app/sales-stats/page.tsx 내 SalesTargetModal

모달 크기: 400px × 400px

로딩 UI:
<Modal isOpen={isOpen} onClose={onClose} title="목표 매출 설정">
  <div className="p-6 space-y-4">
    <div className="animate-pulse bg-gray-200 h-5 w-32 rounded" />
    <div className="animate-pulse bg-gray-200 h-10 w-full rounded-lg" />
    <div className="animate-pulse bg-gray-200 h-5 w-32 rounded" />
    <div className="animate-pulse bg-gray-200 h-10 w-full rounded-lg" />
    <div className="flex gap-3 mt-4">
      <div className="animate-pulse bg-gray-200 h-9 flex-1 rounded" />
      <div className="animate-pulse bg-gray-200 h-9 flex-1 rounded" />
    </div>
  </div>
</Modal>

데이터 로드:
const fetchSalesTarget = async () => {
  setIsLoading(true);
  const { data } = await supabase
    .from('sales_targets')
    .select('*')
    .eq('branch_id', branchId)
    .eq('year', targetYear)
    .eq('month', targetMonth)
    .single();
  setTarget(data ?? null);
  setIsLoading(false);
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_OPEN_01: [목표 설정] 버튼 클릭 → 기존 목표 조회 중

### 연결 화면
- 기존 목표 있음 → `02-열림` (값 채워진 상태)
- 기존 목표 없음 → `02-열림` (빈 상태)
- 조회 실패 → `03-에러`
