# SCR-S007 환불관리 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S007 |
| 상태 코드 | `normal` |
| 경로 | `/refunds` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 행 클릭 → DLG-S006 / 필터 변경 → `01-로딩` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S007_환불관리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S007 환불관리 — 상태: 정상

파일: src/app/refunds/page.tsx

레이아웃:
- AppLayout 풀너비 데스크톱
- PageHeader: "환불 관리" + [엑셀 다운로드] 버튼

StatCardGrid (3카드):
- 총 환불금액: formatKRW(totalRefund) + RefreshCw 아이콘
- 환불 건수: formatNumber(count) + "건" + Hash 아이콘
- 평균 환불금액: formatKRW(avgRefund) + BarChart2 아이콘

SearchFilter:
- 날짜 범위 (기본: 이번달)
- 상태 Select: 전체 / 완료 / 처리중 / 거절
- 검색어 (회원명)

DataTable:
- 컬럼: No / 환불일 / 회원명 / 상품명 / 환불금액 / 결제수단 / 환불사유 / 담당자 / 상태
- StatusBadge: 완료(success) / 처리중(warning) / 거절(error)
- 행 클릭 → DLG-S006 환불상세 모달 열기

const statusVariant = (status: string) => {
  if (status === '완료') return 'success';
  if (status === '거절') return 'error';
  if (status === '처리중') return 'warning';
  return 'default';
};

데이터:
supabase.from('refunds')
  .select('*')
  .eq('branch_id', branchId)
  .gte('refund_date', startDate)
  .lte('refund_date', endDate)
  .order('refund_date', { ascending: false })

사용 유틸:
- formatKRW from '@/lib/format'
- exportToExcel from '@/lib/exportExcel'
- DataTable, StatCard, StatCardGrid, StatusBadge, SearchFilter
- AppLayout, PageHeader
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- isLoading=false AND refunds.length > 0

### 필수 데이터
| 항목 | 테이블 | 조건 |
|------|--------|------|
| 환불 목록 | `refunds` | branch_id, 날짜 범위 |
| 요약 통계 | `refunds` | SUM(amount), COUNT |

### 인터랙션 (User Actions)
1. 행 클릭 → DLG-S006 환불상세
2. 날짜/상태 필터 변경 → 재조회
3. [엑셀 다운로드] → exportToExcel()

### 비즈니스 룰
- 환불 완료: 수정/삭제 불가
- 처리중: 승인/거절 가능 (manager 이상)
- 거절: 재요청 가능 (DLG-S015)

### 에지 케이스
- 필터 결과 0건 → `03-빈상태` 메시지 (StatCard 유지)

### 접근성 (A11y)
- 행 `aria-label="환불 상세 보기: {회원명} {금액}"`

### 연결 화면
- DLG-S006 환불상세
- DLG-S013 환불처리 (처리중 건 승인)
- DLG-S015 환불요청 (재요청)
