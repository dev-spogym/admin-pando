# SCR-S009 할부결제관리 — 상태: 빈상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S009 |
| 상태 코드 | `empty` |
| 경로 | `/installments` |
| 역할 | owner, manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | [할부 등록] → DLG-S009 |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S009_할부결제관리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-S009 할부결제관리 — 빈상태

파일: src/app/installments/page.tsx (installments.length === 0 분기)

빈 상태 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <CreditCard className="h-14 w-14 text-gray-300 mb-3" />
  <p className="text-base font-semibold text-gray-500">할부 결제 내역이 없습니다.</p>
  <p className="text-sm text-gray-400 mt-1">할부 등록 버튼으로 새 할부를 등록하세요.</p>
  <Button
    variant="primary"
    className="mt-6"
    onClick={() => setShowInstallmentModal(true)}
  >
    <Plus className="h-4 w-4 mr-2" /> 할부 등록
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 등록된 할부 건 없음

### 인터랙션
1. [할부 등록] → DLG-S009 할부등록 모달

### 연결 화면
- DLG-S009 할부등록
