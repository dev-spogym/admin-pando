# SCR-S003 결제처리 — 상태: 장바구니 없음 오류

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S003 |
| 상태 코드 | `no-cart` |
| 경로 | `/pos/payment` |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | 직접 URL 접근 또는 장바구니 소실 |
| 다음 상태 | POS 판매로 이동 |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S003_결제처리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S003 결제처리 — 상태: 장바구니 없음 (E_STATE_NO_CART_01)

파일: src/app/pos/payment/page.tsx (cartItems.length === 0 진입 시)

장바구니 없음 UI:
{cartItems.length === 0 && (
  <div className="flex flex-col items-center justify-center min-h-[60vh] text-center">
    <ShoppingCart className="h-16 w-16 text-gray-300 mb-4" />
    <h2 className="text-xl font-bold text-gray-600 mb-2">결제할 상품이 없습니다.</h2>
    <p className="text-sm text-gray-400 mb-6">
      POS 판매 화면에서 상품을 먼저 선택해주세요.
    </p>
    <Button variant="primary" onClick={() => moveToPage('/pos')}>
      POS 판매로 이동
    </Button>
  </div>
)}

진입 시 체크:
useEffect(() => {
  if (cartItems.length === 0) {
    // URL 직접 접근 감지
    setPaymentStatus('no-cart');
  }
}, []);

사용 유틸:
- ShoppingCart from 'lucide-react'
- moveToPage from '@/internal'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/pos/payment` 직접 URL 접근 (cartItems 없음)
- 브라우저 새로고침으로 state 소실

### 인터랙션 (User Actions)
1. [POS 판매로 이동] → moveToPage('/pos')

### 비즈니스 룰
- 장바구니 없이 결제 처리 불가
- 자동 리다이렉트 3초 후 (선택적)

### 연결 화면
- SCR-S002 POS판매
