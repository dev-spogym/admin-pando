# SCR-S003 결제처리 — 상태: 처리중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-S003 |
| 상태 코드 | `processing` |
| 경로 | `/pos/payment` |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P0 |
| 이전 상태 | DLG-S003 결제 확인 클릭 |
| 다음 상태 | `04-완료` / `07-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/SCR-S003_결제처리/F6_상태별.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-S003 결제처리 — 상태: 처리중 (결제 API 호출 중)

파일: src/app/pos/payment/page.tsx (paymentStatus === 'processing' 분기)

처리중 오버레이 또는 버튼 상태:
// 방법 1: 버튼 내 스피너
<Button
  className="w-full"
  variant="primary"
  disabled={true}
>
  <Loader2 className="h-4 w-4 mr-2 animate-spin" />
  처리 중...
</Button>

// 방법 2: 전체 화면 오버레이 (선호)
{paymentStatus === 'processing' && (
  <div className="fixed inset-0 bg-black/30 flex items-center justify-center z-50"
       aria-live="polite" aria-label="결제 처리 중">
    <div className="bg-white rounded-2xl p-8 flex flex-col items-center gap-4 shadow-xl">
      <Loader2 className="h-12 w-12 text-blue-600 animate-spin" />
      <p className="text-lg font-semibold text-gray-700">결제를 처리하고 있습니다.</p>
      <p className="text-sm text-gray-400">잠시만 기다려주세요...</p>
    </div>
  </div>
)}

처리 중 페이지 인터랙션 차단:
- 결제수단 선택: pointer-events-none
- [결제 확인] 버튼: disabled

결제 API 호출:
const processPayment = async () => {
  setPaymentStatus('processing');
  try {
    const { data, error } = await supabase.from('sales').insert({
      branch_id: branchId,
      buyer_id: buyer?.id,
      buyer_name: buyer?.name ?? '비회원',
      payment_method: paymentMethod,
      sale_price: totalAmount,
      card: cardAmount,
      cash: cashAmount,
      mileage: mileageAmount,
      purchase_date: new Date().toISOString(),
      status: '완료',
    });
    if (error) throw error;
    setPaymentStatus('complete');
    setSaleResult(data[0]);
  } catch (err) {
    console.error('[SCR-S003] 결제 처리 실패:', err);
    setPaymentStatus('error');
    toast.error('결제 처리 중 오류가 발생했습니다.');
  }
};

사용 유틸:
- Loader2 from 'lucide-react' (animate-spin)
- supabase from '@/lib/supabase'
- toast from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- DLG-S003에서 [결제 확인] 클릭 후 PG/API 요청 중
- E_STATE_LOADING_01: paymentStatus='processing'

### 필수 데이터
| 항목 | 설명 |
|------|------|
| cartItems | 결제할 상품 목록 |
| paymentMethod | 선택된 결제수단 |
| totalAmount | 결제 금액 |

### 인터랙션 (User Actions)
- 처리 중 모든 인터랙션 차단 (뒤로가기 포함)
- ESC 키: 무시 (처리 중 취소 불가)

### 비즈니스 룰
- 처리 timeout: 30초 → 에러 상태 전환
- 중복 요청 방지: processPayment 1회만 호출 (useRef flag)
- sales 테이블 INSERT + 마일리지 차감 (트랜잭션)

### 에지 케이스
- 네트워크 끊김: 30초 후 `07-결제에러`

### 접근성 (A11y)
- 오버레이 `aria-live="polite"` + `aria-busy="true"`
- 스피너 `aria-label="결제 처리 중"`

### 연결 화면
- 성공 → `04-완료`
- 실패 → `07-결제에러`
