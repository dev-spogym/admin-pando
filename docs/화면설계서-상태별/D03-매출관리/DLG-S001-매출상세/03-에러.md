# DLG-S001 매출상세 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S001 |
| 상태 코드 | `error` |
| 경로 | SCR-S001 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (API 오류) |
| 다음 상태 | 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S001_매출상세/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-S001 매출상세 — 상태: 에러 (E_LOAD_ERR_01)

파일: src/app/sales/page.tsx 내 SaleDetailModal

에러 처리:
- 모달은 열리지 않음 (isOpen=false 유지)
- toast.error('데이터를 불러올 수 없습니다.') 발사
- console.error('[DLG-S001]', error)

if (error) {
  toast.error('데이터를 불러올 수 없습니다.');
  console.error('[DLG-S001] sale detail fetch error:', error);
  // 모달 열지 않고 return
  return;
}

별도 에러 모달 없음 — 토스트로 대체
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_LOAD_ERR_01: sales/:id API 오류
- saleId 없음 또는 RLS 차단

### 필수 데이터
| 항목 | 설명 |
|------|------|
| error 객체 | 로깅용 |

### 인터랙션 (User Actions)
- 토스트 자동 닫힘 (3초)
- 모달 미열림 → SCR-S001 유지

### 비즈니스 룰
- 에러 시 모달 열지 않음 (빈 모달 방지)
- 토스트 1회만 발사

### 연결 화면
- SCR-S001 유지 (에러 후 화면 변화 없음)
