# DLG-S009 할부등록 — 상태: 처리중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S009 |
| 상태 코드 | `processing` |
| 경로 | SCR-S009 위 모달 |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` [등록] 클릭 |
| 다음 상태 | `03-성공` / `04-에러` |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S009_할부등록/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S009 할부등록 — 상태: 처리중 (E_SUBMIT_01)

파일: src/app/installments/page.tsx 내 InstallmentRegisterModal

처리중 UI:
<div className="p-4 border-t flex gap-3">
  <Button variant="outline" className="flex-1" disabled>닫기</Button>
  <Button variant="primary" className="flex-1" disabled>
    <Loader2 className="h-4 w-4 animate-spin mr-2" />
    등록중...
  </Button>
</div>

등록 로직:
const handleRegister = async () => {
  setIsProcessing(true);
  const monthly_amount = Math.ceil(form.total_amount / form.total_months);

  // 스케줄 생성
  const schedule = Array.from({ length: form.total_months }, (_, i) => {
    const d = new Date(form.start_date);
    d.setMonth(d.getMonth() + i);
    return { month: i + 1, due_date: d.toISOString().split('T')[0], amount: monthly_amount, paid: false };
  });

  const { error } = await supabase.from('installments').insert({
    member_id: selectedMember.id,
    member_name: selectedMember.name,
    product_name: form.product_name,
    total_amount: form.total_amount,
    monthly_amount,
    total_months: form.total_months,
    paid_months: 0,
    remaining_amount: form.total_amount,
    start_date: form.start_date,
    next_due_date: form.start_date,
    status: 'ACTIVE',
    schedule,
    branch_id: branchId,
  });

  if (error) {
    toast.error('할부 등록에 실패했습니다.');
    setIsProcessing(false);
  } else {
    onClose();
    await fetchInstallments();
    toast.success('할부가 등록되었습니다.');
  }
};
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_SUBMIT_01: [등록] 클릭 → installments INSERT 호출 중

### 비즈니스 룰
- monthly_amount = Math.ceil(total_amount / total_months)
- schedule 배열: 회차별 due_date, amount, paid(false) 자동 생성
- 모든 버튼 비활성화

### 연결 화면
- `03-성공` (INSERT 성공)
- `04-에러` (INSERT 실패)
