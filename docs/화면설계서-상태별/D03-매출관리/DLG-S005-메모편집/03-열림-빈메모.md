# DLG-S005 메모편집 — 상태: 열림 (빈 메모)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-S005 |
| 상태 코드 | `open-empty` |
| 경로 | SCR-S008 위 모달 |
| 역할 | owner, manager, trainer, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (메모 없음) |
| 다음 상태 | [저장] → `04-저장성공` / [취소] → 닫힘 |
| 다이어그램 | docs/다이어그램/D03_매출관리/DLG-S005_메모편집/M1_모달생명주기.md |

## 🧩 바이브코딩 프롬프트

```
화면: DLG-S005 메모편집 — 상태: 열림 (빈 메모, 신규 작성)

파일: src/app/unpaid/page.tsx 내 MemoEditModal (memoText='' 분기)

빈 메모 UI (02-열림-메모있음과 동일 구조):
- Textarea: value='' placeholder 표시
- placeholder: "미수금 관련 메모를 입력하세요..."
- 글자 수: 0/500

저장 비활성 조건 (선택적):
- memoText.trim().length === 0 → [저장] disabled
  (빈 메모 저장은 허용하지 않을 경우)

또는:
- 빈 메모 저장 허용 → 기존 메모 삭제 효과
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- E_LOAD_EMPTY_01: 해당 미수금 건에 메모 없음 (memo=null)

### 인터랙션
1. 메모 입력 → memoText 업데이트
2. [저장] → 신규 메모 저장

### 비즈니스 룰
- 빈 Textarea로 저장 → memo='' 저장 (허용)

### 연결 화면
- 저장 → 닫힘 → SCR-S008 갱신
