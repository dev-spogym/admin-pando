# SCR-052 밴드카드관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-052 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/rfid` |
| 역할 | owner, manager, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-052 밴드/카드 관리 — 상태: 로딩

파일: src/app/(facilities)/rfid/page.tsx

레이아웃:
- AppLayout + PageHeader("밴드/카드 관리")
- 데스크톱 전체 너비

Skeleton UI:
1. 통계 카드 3개 (grid grid-cols-3 gap-4 mb-6):
   - h-24 rounded-lg bg-gray-200 animate-pulse
2. 필터/검색 바 Skeleton (h-10 w-full bg-gray-200 animate-pulse rounded mb-4)
3. DataTable Skeleton:
   - 헤더 행 (h-10 bg-gray-100)
   - 데이터 행 10개 (h-12 bg-gray-50 animate-pulse border-b)

상태 관리:
- isLoading: true
- useEffect: supabase.from('rfid_cards').select('*, members(name)').eq('branch_id', branchId)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-052 진입 후 API 응답 대기
- 필터 변경 후 재조회 중

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| RFID 카드 | `rfid_cards` | `branch_id = ?` |
| 회원 정보 | `members` | JOIN |

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
