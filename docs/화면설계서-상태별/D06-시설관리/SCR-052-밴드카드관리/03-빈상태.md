# SCR-052 밴드카드관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-052 |
| 상태 코드 | `empty` |
| 경로 | `/facilities/rfid` |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | "카드 등록" → DLG-052-001 |

## 🧩 바이브코딩 프롬프트

```
화면: SCR-052 밴드/카드 관리 — 상태: 빈 상태
조건: rfid_cards.length === 0

빈 상태 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <CreditCardIcon className="w-12 h-12 text-gray-300 mb-4" />
  <p className="text-lg font-semibold text-gray-700 mb-1">등록된 카드가 없습니다</p>
  <p className="text-sm text-gray-400 mb-6">RFID 카드를 등록하여 출입 관리를 시작하세요.</p>
  <Button onClick={openRegisterModal} variant="primary">카드 등록</Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_EMPTY_01`: API 200 + 0건

### 인터랙션 (User Actions)
1. "카드 등록" → DLG-052-001 등록 모달

### 연결 화면
- 다음: 등록 성공 → `02-정상`
