# DLG-052-003 RFID삭제확인 — 상태: 실패

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-052-003 |
| 상태 코드 | `error` |
| 트리거 | RFID 삭제 API 실패 |
| 우선순위 | P0 |
| 이전 상태 | `02-삭제가능` |
| 다음 상태 | 모달 유지 (재시도 가능) |

## 🧩 바이브코딩 프롬프트

```
실패 처리:
1. toast.error("RFID 카드 삭제에 실패했습니다.")
2. 모달 유지
3. isSubmitting = false → 버튼 재활성화
```

## 📝 디스크립션

### 비즈니스 룰
- API 실패 시 모달을 닫지 않음
- 카드 번호 입력값 보존하여 재시도 가능

### 연결 화면
- 유지: `02-삭제가능` (재시도)
