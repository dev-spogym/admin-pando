# DLG-052-003 RFID삭제확인 — 상태: 삭제 가능 (입력 일치)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-052-003 |
| 상태 코드 | `confirm-matched` |
| 트리거 | 입력값 === "삭제" |
| 우선순위 | P1 |
| 이전 상태 | `01-열림-입력대기` |
| 다음 상태 | "삭제" 클릭 → `03-성공` 또는 `04-실패` |

## 🧩 바이브코딩 프롬프트

```
입력 일치 상태 (E_M1_MATCH_01):
- 삭제 버튼: disabled 해제 (variant="danger" 활성)
- input: border-green-300 (선택적)

// 버튼 상태:
<Button
  variant="danger"
  onClick={handleDelete}
  disabled={confirmText !== '삭제'} // false → 활성
  loading={isSubmitting}
>
  삭제
</Button>
```

## 📝 디스크립션

### 인터랙션 (User Actions)
1. "삭제" 버튼 클릭 → 삭제 API 호출
2. "취소" → 모달 닫힘

### 연결 화면
- 다음: `03-성공` / `04-실패`
