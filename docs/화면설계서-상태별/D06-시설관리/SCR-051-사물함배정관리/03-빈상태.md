# SCR-051 사물함배정관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `empty` |
| 경로 | `/facilities/locker/management` |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | SCR-050에서 락커 추가 후 재진입 |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함배정관리 — 상태: 빈 상태

파일: src/app/(facilities)/locker/management/page.tsx

조건: lockers.length === 0

빈 상태 UI:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-16 h-16 mb-4 rounded-full bg-gray-100 flex items-center justify-center">
    <GridIcon className="w-8 h-8 text-gray-400" />
  </div>
  <p className="text-lg font-semibold text-gray-700 mb-1">배정 가능한 사물함이 없습니다</p>
  <p className="text-sm text-gray-400 mb-6">
    락커 관리에서 사물함을 먼저 등록해주세요.
  </p>
  <Button onClick={() => moveToPage('/facilities/locker')} variant="outline">
    락커 관리로 이동
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_04`: API 200 + 0건
- 필터 검색 결과 0건

### 인터랙션 (User Actions)
1. "락커 관리로 이동" → `/facilities/locker` (SCR-050)
2. 필터 결과 없음 → "필터 초기화" 버튼

### 연결 화면
- 다음: SCR-050 락커 추가 후 재진입
