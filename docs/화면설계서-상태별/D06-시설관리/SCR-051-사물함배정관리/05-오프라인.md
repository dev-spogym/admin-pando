# SCR-051 사물함배정관리 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-051 |
| 상태 코드 | `offline` |
| 경로 | `/facilities/locker/management` |
| 역할 | owner, manager, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 네트워크 복구 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-051 사물함배정관리 — 상태: 오프라인

파일: src/app/(facilities)/locker/management/page.tsx

오프라인 배너:
<div className="mb-4 p-3 bg-blue-50 border border-blue-200 rounded flex items-center gap-2">
  <WifiOffIcon className="w-4 h-4 text-blue-500" />
  <p className="text-sm text-blue-700">네트워크 연결이 없습니다. 연결되면 자동으로 새로고침됩니다.</p>
</div>

window 'online'/'offline' 이벤트 구독으로 자동 복구
캐시 데이터 있으면 grayscale opacity-60으로 표시
모든 액션 버튼 disabled
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_06`: 네트워크 없음

### 인터랙션 (User Actions)
- 네트워크 복구 시 자동 재조회

### 연결 화면
- 다음: 온라인 복구 → `01-로딩` → `02-정상`
