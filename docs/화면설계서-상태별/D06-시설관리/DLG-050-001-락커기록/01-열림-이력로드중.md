# DLG-050-001 락커기록 — 상태: 열림 (이력 로드중)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-050-001 |
| 상태 코드 | `open-loading` |
| 트리거 | SCR-050 셀 호버 → "기록" 버튼 클릭 |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | SCR-050 정상 화면 |
| 다음 상태 | `02-이력있음` 또는 `03-이력없음` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 Modal 컴포넌트를 작성하라.
모달: DLG-050-001 락커 기록 — 상태: 열림/로딩

Modal 구조 (Modal 공통 컴포넌트 사용):
<Modal isOpen={isOpen} onClose={onClose} title="락커 기록" size="lg">
  <div className="space-y-4">
    {/* 기간 필터 */}
    <div className="flex gap-2 items-center">
      <DatePicker label="시작일" ... />
      <span>~</span>
      <DatePicker label="종료일" ... />
      <Button variant="outline" size="sm">조회</Button>
    </div>

    {/* 로딩 상태 */}
    <div className="space-y-2">
      {[...Array(5)].map((_, i) => (
        <div key={i} className="h-10 bg-gray-200 animate-pulse rounded" />
      ))}
    </div>
  </div>
</Modal>

트리거: E_M1_OPEN_01 (SCR-050 호버 > 기록 버튼)
초기 로드: supabase.from('locker_logs').select('*').eq('locker_id', lockerId).order('created_at', {ascending: false}).limit(30)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_M1_OPEN_01`: SCR-050 락커 셀 호버 → 기록 버튼 클릭
- 모달 열림 + 이력 API 호출 중

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 락커 이력 | `locker_logs` | `locker_id = ?`, 최근 30건 |

### 인터랙션 (User Actions)
- 로딩 중 X 버튼 / ESC 클릭 → 모달 닫힘 (`E_M1_CLOSE_01`)

### 비즈니스 룰
- 조회 전용 모달 (입력 필드 없음, 기간 필터만 존재)

### 연결 화면
- 다음: `02-이력있음` / `03-이력없음` / `04-에러`
