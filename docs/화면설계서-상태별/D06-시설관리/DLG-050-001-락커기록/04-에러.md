# DLG-050-001 락커기록 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-050-001 |
| 상태 코드 | `error` |
| 트리거 | API 실패 |
| 역할 | owner, manager, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-열림-이력로드중` |
| 다음 상태 | 닫기 → SCR-050 유지 |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-050-001 락커 기록 — 에러 (E_M1_LOAD_ERR_01)

<Modal isOpen={isOpen} onClose={onClose} title="락커 기록" size="lg">
  <div className="flex flex-col items-center justify-center py-12 text-center">
    <AlertCircleIcon className="w-10 h-10 text-red-400 mb-3" />
    <p className="text-sm text-red-600 font-medium">이력을 불러오지 못했습니다.</p>
    <Button variant="outline" size="sm" onClick={retry} className="mt-3">다시 시도</Button>
  </div>
  <div className="flex justify-end mt-2">
    <Button variant="outline" onClick={onClose}>닫기</Button>
  </div>
</Modal>

toast.error("이력을 불러오지 못했습니다.")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_M1_LOAD_ERR_01`: 이력 API 실패

### 인터랙션 (User Actions)
1. "다시 시도" → 이력 API 재호출
2. "닫기" → 모달 닫힘

### 연결 화면
- 다음: 재시도 성공 → `02-이력있음` / 닫기 → SCR-050
