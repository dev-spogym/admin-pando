# DLG-050-001 락커기록 — 상태: 이력 없음

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-050-001 |
| 상태 코드 | `history-empty` |
| 트리거 | API 200 + 이력 0건 |
| 역할 | owner, manager, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-열림-이력로드중` |
| 다음 상태 | 닫기 → SCR-050 유지 |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-050-001 락커 기록 — 이력 없음

<Modal isOpen={isOpen} onClose={onClose} title={`락커 ${lockerNumber} 기록`} size="lg">
  <div className="flex flex-col items-center justify-center py-12 text-center">
    <ClockIcon className="w-10 h-10 text-gray-300 mb-3" />
    <p className="text-sm text-gray-500">기록이 없습니다</p>
    <p className="text-xs text-gray-400 mt-1">선택한 기간에 락커 이력이 없습니다.</p>
  </div>
  <div className="flex justify-end">
    <Button variant="outline" onClick={onClose}>닫기</Button>
  </div>
</Modal>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 조회 기간 내 이력 0건
- 신규 락커 (배정 이력 없음)

### 인터랙션 (User Actions)
1. 기간 필터 변경 → 재조회
2. "닫기" → 모달 닫힘

### 연결 화면
- 다음: 닫기 → SCR-050 유지
