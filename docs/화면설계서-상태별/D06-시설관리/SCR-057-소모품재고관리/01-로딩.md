# SCR-057 소모품재고관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-057 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/consumables` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-057 소모품 재고 관리 — 상태: 로딩 (신규 기능)

파일: src/app/(facilities)/consumables/page.tsx

Skeleton UI:
1. 통계 카드 5개 (grid grid-cols-5 gap-4 mb-6):
   h-20 rounded-lg bg-gray-200 animate-pulse
2. 필터 바 Skeleton (h-10 mb-4)
3. DataTable Skeleton:
   - 헤더 h-10 bg-gray-100
   - 행 10개 h-12 animate-pulse

useEffect: supabase.from('consumables').select('*').eq('branch_id', branchId)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-057 진입 후 API 응답 대기

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
