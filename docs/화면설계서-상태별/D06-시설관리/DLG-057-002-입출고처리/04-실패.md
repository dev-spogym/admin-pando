# DLG-057-002 입출고처리 — 상태: 실패

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-057-002 |
| 상태 코드 | `error` |
| 트리거 | 입고/출고 API 실패 |
| 우선순위 | P0 |
| 이전 상태 | `01-열림-입고모드` 또는 `02-열림-출고모드` |
| 다음 상태 | 모달 유지 (재시도 가능) |

## 🧩 바이브코딩 프롬프트

```
실패 처리:
- 입고 실패: toast.error("입고 처리에 실패했습니다.")
- 출고 실패: toast.error("출고 처리에 실패했습니다.")
공통:
  모달 유지 (수량 입력값 보존)
  isSubmitting = false → 버튼 재활성화
```

## 📝 디스크립션

### 비즈니스 룰
- API 실패 시 모달을 닫지 않고 수량/메모 입력값 보존
- 입고/출고 모드 구분 없이 동일 실패 처리 패턴 적용

### 연결 화면
- 유지: `01-열림-입고모드` 또는 `02-열림-출고모드` (재시도)
