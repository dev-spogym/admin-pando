# SCR-050 락커관리 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-050 |
| 상태 코드 | `offline` |
| 경로 | `/facilities/locker` |
| 역할 | owner, manager, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 네트워크 복구 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-050 락커관리 — 상태: 오프라인

파일: src/app/(facilities)/locker/page.tsx

조건: window.navigator.onLine === false 또는 네트워크 fetch 실패 (network error)

오프라인 감지:
useEffect(() => {
  const handleOffline = () => setIsOffline(true)
  const handleOnline = () => { setIsOffline(false); loadLockers() }
  window.addEventListener('offline', handleOffline)
  window.addEventListener('online', handleOnline)
  return () => { window.removeEventListener('offline', handleOffline); window.removeEventListener('online', handleOnline) }
}, [])

오프라인 배너 (상단 고정):
<div className="mb-4 p-3 bg-blue-50 border border-blue-200 rounded flex items-center gap-2">
  <WifiOffIcon className="w-4 h-4 text-blue-500" />
  <p className="text-sm text-blue-700">네트워크 연결이 없습니다. 연결되면 자동으로 새로고침됩니다.</p>
</div>

그리드 영역: 이전에 로드된 데이터가 있으면 그대로 표시 (grayscale opacity-60)
데이터 없으면: 빈 그리드 + "오프라인 상태" 안내
모든 버튼 disabled 처리
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_07`: 네트워크 없음 상태에서 페이지 로드
- 페이지 이용 중 네트워크 단절

### 필수 데이터
- 이전 캐시 데이터 (있으면 표시, 없으면 빈 화면)

### 인터랙션 (User Actions)
1. 네트워크 복구 → 자동으로 loadLockers() 재호출 (`window.online` 이벤트)
2. 모든 버튼 비활성화 (클릭 시 "오프라인 상태입니다" toast)

### 비즈니스 룰
- 온라인 복구 감지는 `window.addEventListener('online')` 사용
- 캐시된 데이터 표시 시 "오프라인 데이터" 배지 표시

### 에지 케이스
- 오프라인 중 필터 변경 시도 → toast.warning("오프라인 상태에서는 필터를 변경할 수 없습니다.")

### 접근성 (A11y)
- 오프라인 배너 `role="status"` + `aria-live="polite"`
- 비활성 버튼 `aria-disabled="true"`

### 연결 화면
- 이전: `01-로딩`
- 다음: 네트워크 복구 → `01-로딩` → `02-정상`
