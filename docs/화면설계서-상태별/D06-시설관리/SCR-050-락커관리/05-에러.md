# SCR-050 락커관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-050 |
| 상태 코드 | `error` |
| 경로 | `/facilities/locker` |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-050 락커관리 — 상태: 에러 (API 4xx/5xx)

파일: src/app/(facilities)/locker/page.tsx

조건: fetch 실패 (4xx/5xx 응답)

에러 UI:
- AppLayout + PageHeader 유지
- 그리드 영역 대신:
<div className="flex flex-col items-center justify-center py-24 text-center">
  <div className="w-16 h-16 mb-4 rounded-full bg-red-50 flex items-center justify-center">
    <AlertCircleIcon className="w-8 h-8 text-red-400" />
  </div>
  <p className="text-lg font-semibold text-gray-700 mb-1">데이터를 불러오지 못했습니다</p>
  <p className="text-sm text-gray-400 mb-6">서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요.</p>
  <Button onClick={retry} variant="outline">다시 시도</Button>
</div>

toast.error("락커 데이터를 불러오지 못했습니다.") 동시 발사

retry 함수: loadLockers() 재호출 → 01-로딩 상태로 전환
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_06`: API 4xx/5xx 응답
- Supabase 서버 오류
- RLS 정책 위반

### 필수 데이터
- error 객체 (로깅용, 사용자에게는 제네릭 메시지 표시)

### 인터랙션 (User Actions)
1. "다시 시도" 버튼 → loadLockers() 재호출
2. 사이드바 다른 메뉴 이동 가능

### 비즈니스 룰
- 재시도 횟수 제한 없음 (단, rate limit 방지 위해 1초 debounce)
- JWT 만료(401) → useAuthStore 자동 갱신 후 재시도, 실패 시 /login 리다이렉트

### 에지 케이스
- 403 Forbidden → "접근 권한이 없습니다" 메시지로 변경 + 사이드바로 돌아가기 버튼

### 접근성 (A11y)
- 에러 컨테이너 `role="alert"`
- 재시도 버튼 `aria-label="락커 데이터 다시 불러오기"`

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 성공 → `02-정상` / 계속 실패 → 에러 유지
