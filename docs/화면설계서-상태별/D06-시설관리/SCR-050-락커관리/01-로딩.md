# SCR-050 락커관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-050 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/locker` |
| 역할 | owner, manager, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 (사이드바 클릭) |
| 다음 상태 | `02-정상-데이터있음` 또는 `03-빈상태` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-050 락커관리 — 상태: 로딩 (Skeleton)

파일: src/app/(facilities)/locker/page.tsx

레이아웃:
- AppLayout + PageHeader("락커 관리")
- 데스크톱 기준 전체 너비

로딩 UI 구성:
1. 상단 통계 카드 영역 (grid grid-cols-4 gap-4 mb-6):
   - 4개의 Skeleton 카드 (h-24 rounded-lg bg-gray-200 animate-pulse)

2. 필터/검색 바 Skeleton (h-10 w-full bg-gray-200 animate-pulse rounded mb-4)

3. 락커 그리드 Skeleton:
   - grid grid-cols-10 gap-2 (10열 기준)
   - 40개 셀 (h-16 rounded bg-gray-200 animate-pulse)

상태 관리:
- isLoading: true → Skeleton 표시
- useEffect로 lockers 테이블 fetch 시작

데이터:
- supabase.from('lockers').select('*').eq('branch_id', branchId)
- 통계: 전체/사용중/고장/만료임박 count
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-050 최초 진입 시 권한 확인 통과 후 API 응답 대기 중
- 필터 변경 후 재조회 중

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 락커 목록 | `lockers` | `branch_id = ?` |
| 통계 | `lockers` | 상태별 count |

### 인터랙션 (User Actions)
- 로딩 중 인터랙션 비활성 (버튼 disabled)

### 비즈니스 룰
- Skeleton은 실제 그리드 구조와 동일한 열 수(10열)로 표시
- 로딩 타임아웃 10초 초과 시 에러 상태 전환

### 에지 케이스
- 빠른 네트워크: Skeleton이 100ms 미만으로 표시될 수 있음 (최소 300ms 보장)

### 접근성 (A11y)
- `aria-busy="true"` on 그리드 컨테이너
- `aria-label="락커 데이터 로딩 중"`

### 연결 화면
- 다음: `02-정상-데이터있음` / `03-빈상태` / `05-에러` / `06-오프라인`
