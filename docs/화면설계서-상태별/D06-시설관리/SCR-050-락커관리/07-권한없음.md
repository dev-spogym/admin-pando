# SCR-050 락커관리 — 상태: 권한 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-050 |
| 상태 코드 | `forbidden` |
| 경로 | `/facilities/locker` |
| 역할 | FC, 코치 (접근 불가) |
| 우선순위 | P1 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 사이드바로 돌아가기 |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-050 락커관리 — 상태: 권한 없음 (403)

파일: src/app/(facilities)/locker/page.tsx

조건: 로그인 사용자 역할이 FC 또는 코치 (hasPermission('locker:read') === false)

권한 체크 로직:
const { user } = useAuthStore()
if (!hasPermission('locker:read')) return <ForbiddenPage />

ForbiddenPage 컴포넌트:
<div className="flex flex-col items-center justify-center h-[60vh] text-center">
  <div className="w-16 h-16 mb-4 rounded-full bg-gray-100 flex items-center justify-center">
    <ShieldOffIcon className="w-8 h-8 text-gray-400" />
  </div>
  <p className="text-lg font-semibold text-gray-700 mb-1">접근 권한이 없습니다</p>
  <p className="text-sm text-gray-400 mb-6">락커 관리는 원장, 매니저, 직원만 접근할 수 있습니다.</p>
  <Button onClick={() => moveToPage('/dashboard')} variant="outline">
    대시보드로 돌아가기
  </Button>
</div>

미들웨어에서도 권한 체크:
- middleware.ts: role FC/코치 → /facilities 경로 접근 시 /dashboard 리다이렉트
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_01`: FC 또는 코치 역할로 `/facilities/locker` 직접 접근 시
- URL 직접 입력 또는 비정상 접근

### 필수 데이터
- useAuthStore().user.role

### 인터랙션 (User Actions)
1. "대시보드로 돌아가기" 버튼 → `/dashboard` 이동
2. 브라우저 뒤로가기

### 비즈니스 룰
- 미들웨어 레벨에서 1차 차단 (리다이렉트)
- 컴포넌트 레벨에서 2차 차단 (403 UI)
- 허용 역할: owner, manager, staff

### 에지 케이스
- 역할 변경 후 세션 만료 전 접근 → 미들웨어에서 차단

### 접근성 (A11y)
- `role="alert"` + `aria-label="접근 권한 없음"`

### 연결 화면
- 이전: 사이드바 클릭 시도
- 다음: 대시보드 또는 이전 페이지
