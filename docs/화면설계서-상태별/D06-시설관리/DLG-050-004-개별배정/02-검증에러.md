# DLG-050-004 개별배정 — 상태: 검증 에러

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-050-004 |
| 상태 코드 | `validation-error` |
| 트리거 | "저장" 클릭 + 검증 실패 |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-열림-입력` |
| 다음 상태 | 수정 후 → `03-성공` |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-050-004 개별 배정 — 검증 에러

에러 필드 인라인 표시:
- 회원 미선택: MemberSearchInput border-red-300 + "회원을 선택하세요."
- 만료일 미입력: DatePicker border-red-300 + "만료일을 입력하세요."
- 과거 날짜: DatePicker border-red-300 + "만료일은 오늘 이후여야 합니다."

에러 텍스트 공통:
<p className="text-xs text-red-500 mt-1 flex items-center gap-1">
  <AlertCircleIcon className="w-3 h-3" />
  {errorMessage}
</p>

"저장" 버튼: 에러 있을 시 disabled 또는 클릭 시 재검증
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- M2 검증 실패 시

### 인터랙션 (User Actions)
1. 에러 필드 수정 → 에러 메시지 사라짐
2. 모든 필드 유효 → "저장" 재클릭

### 연결 화면
- 다음: 수정 후 `03-성공` / `04-실패`
