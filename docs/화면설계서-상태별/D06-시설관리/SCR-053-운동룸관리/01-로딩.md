# SCR-053 운동룸관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-053 |
| 상태 코드 | `loading` |
| 경로 | `/facilities/rooms` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-053 운동룸 관리 — 상태: 로딩

파일: src/app/(facilities)/rooms/page.tsx

Skeleton UI:
1. 통계 카드 4개 (grid grid-cols-4 gap-4 mb-6):
   h-24 rounded-lg bg-gray-200 animate-pulse
2. 필터 바 Skeleton (h-10 w-full bg-gray-200 animate-pulse rounded mb-4)
3. 룸 카드 그리드 Skeleton (grid grid-cols-3 gap-4):
   - 6개 카드 h-48 rounded-lg bg-gray-200 animate-pulse

useEffect: supabase.from('rooms').select('*').eq('branch_id', branchId)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-053 진입 후 API 응답 대기

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 운동룸 | `rooms` | `branch_id = ?` |

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
