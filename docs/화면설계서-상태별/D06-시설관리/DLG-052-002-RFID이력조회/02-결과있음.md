# DLG-052-002 RFID이력조회 — 상태: 결과 있음

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-052-002 |
| 상태 코드 | `result-loaded` |
| 트리거 | 이력 API 200 + 데이터 존재 |
| 우선순위 | P1 |
| 이전 상태 | `01-열림-조회대기` |
| 다음 상태 | 닫기 → SCR-052 유지 |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-052-002 RFID 이력 조회 — 결과 있음 (E_M1_OK_01)

이력 테이블:
<div className="overflow-auto max-h-80 mt-4">
  <table className="w-full text-sm">
    <thead className="bg-gray-50 sticky top-0">
      <tr>
        <th className="px-3 py-2 text-left text-xs text-gray-500">등록일시</th>
        <th className="px-3 py-2 text-left text-xs text-gray-500">카드번호</th>
        <th className="px-3 py-2 text-left text-xs text-gray-500">회원명</th>
        <th className="px-3 py-2 text-left text-xs text-gray-500">상태</th>
        <th className="px-3 py-2 text-left text-xs text-gray-500">처리자</th>
      </tr>
    </thead>
    <tbody>
      {logs.map(log => (
        <tr key={log.id} className="border-b hover:bg-gray-50">
          <td className="px-3 py-2 text-gray-500 text-xs">{format(log.created_at, 'yyyy.MM.dd HH:mm')}</td>
          <td className="px-3 py-2 font-mono text-xs">{log.card_number}</td>
          <td className="px-3 py-2">{log.member_name || '-'}</td>
          <td className="px-3 py-2">
            <StatusBadge status={log.status} />
          </td>
          <td className="px-3 py-2 text-gray-500 text-xs">{log.staff_name}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
<p className="text-xs text-gray-400 mt-2">총 {logs.length}건</p>
```

## 📝 디스크립션

### 인터랙션 (User Actions)
1. 기간 재설정 → "조회" 재클릭
2. "닫기" → 모달 닫힘

### 연결 화면
- 다음: 닫기 → SCR-052 유지
