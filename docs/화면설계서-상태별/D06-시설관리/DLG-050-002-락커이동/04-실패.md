# DLG-050-002 락커이동 — 상태: 실패

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-050-002 |
| 상태 코드 | `api-error` |
| 트리거 | 이동 API 실패 |
| 역할 | owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-열림-대상선택` |
| 다음 상태 | 재시도 → `01-열림` 유지 |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-050-002 락커 이동 — API 실패 (E_M3_ERR_01)

실패 처리:
1. toast.error("이동에 실패했습니다.")
2. 모달 유지 (닫지 않음)
3. "확인" 버튼 loading 상태 해제
4. 모달 내 에러 배너 표시:
   <div className="p-3 bg-red-50 border border-red-200 rounded text-sm text-red-600">
     이동에 실패했습니다. 다시 시도해주세요.
   </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_M3_ERR_01`: 이동 API 5xx 실패

### 인터랙션 (User Actions)
1. 에러 확인 후 "확인" 재클릭 → 재시도
2. "취소" → 모달 닫힘

### 연결 화면
- 다음: 재시도 성공 → `03-성공` / 취소 → SCR-050
