# SCR-056 장비점검일정 — 상태: 정상 (장비 상태별)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-056 |
| 상태 코드 | `normal-equipment-states` |
| 경로 | `/facilities/equipment` |
| 역할 | owner, manager, front |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 행 액션 → DLG-056-001/002/003 |

## 🧩 바이브코딩 프롬프트

```
Next.js App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-056 장비 점검 일정 — 상태: 정상 (신규 기능)

파일: src/app/(facilities)/equipment/page.tsx

레이아웃:
- AppLayout + PageHeader("장비 점검 일정", 우측: "장비 등록" | "점검 등록" 버튼)

1. 통계 카드 (grid grid-cols-5 gap-4 mb-6):
   전체 / 정상 (green) / 점검필요 (amber) / 수리중 (red) / 폐기예정 (gray)

2. 필터 바:
   - 상태 TabNav: 전체 / 정상 / 점검필요 / 수리중 / 폐기예정
   - 카테고리 Select: 전체 / 헬스 / 유산소 / GX / 기타
   - 검색 Input: 장비명 / 위치

3. DataTable 컬럼:
   - 장비명 (font-medium)
   - 카테고리 (badge)
   - 위치 (text-sm text-gray-600)
   - 상태 배지:
     · NORMAL: StatusBadge variant="success" "정상"
     · NEED_INSPECTION: StatusBadge variant="warning" "점검필요" + 다음점검일 강조 (text-amber-600 font-semibold)
     · REPAIR: StatusBadge variant="error" "수리중" + "사용불가" 표시
     · DISCARD: StatusBadge variant="default" "폐기예정"
   - 마지막점검일 (text-sm)
   - 다음점검일 (점검필요 시 text-amber-600 font-semibold)
   - 점검주기 (N일)
   - 액션: "점검 등록" | "수리 등록" | "수정" 버튼

상태별 행 배경:
- NORMAL: bg-white
- NEED_INSPECTION: bg-amber-50
- REPAIR: bg-red-50
- DISCARD: bg-gray-50

데이터: equipment(id, name, category, location, status, last_inspected, next_inspection, inspection_cycle, branch_id)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_OK_01`: API 200 + equipment.length > 0

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 장비 | `equipment` | `branch_id = ?` |
| 통계 | `equipment` | 상태별 COUNT |

### 인터랙션 (User Actions)
1. "장비 등록" 버튼 → DLG-056-001 장비 등록 모달
2. "점검 등록" 버튼 → DLG-056-002 점검 등록 모달
3. 행 "수리 등록" → DLG-056-003 수리 등록 모달
4. 행 "수정" → DLG-056-001 수정 모달 (기존 데이터)
5. 상태 TabNav → 해당 상태 필터

### 비즈니스 룰
- 다음점검일 <= today: NEED_INSPECTION 상태 자동 표시
- REPAIR 상태: 모든 사용 불가 처리 (빨간 행 배경)
- 점검주기 도래 전 7일: 다음점검일 amber 강조

### 에지 케이스
- 수리중 장비 다수: 상단 "수리중 장비 N건" 경고 배너
- 폐기예정: 관리자 승인 후 실제 삭제

### 접근성 (A11y)
- 상태 배지 `aria-label="장비 상태: {상태명}"`
- 점검필요 행 `aria-label="점검 필요 항목"`

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-056-001/002/003 / `03-빈상태`(필터)
