# SCR-094 KPI대시보드 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 로딩 (24개 KPI 스켈레톤)

파일: src/app/(admin)/kpi/page.tsx

레이아웃:
- AppLayout + PageHeader title="KPI 대시보드"

로딩 스켈레톤:
- KPI 카드 24개 (4열 grid):
  · animate-pulse bg-gray-200 rounded-xl h-28 w-full
- 코호트 차트 영역 skeleton (h-64)
- 퍼널 차트 영역 skeleton (h-48)

const [isLoading, setIsLoading] = useState(true)
E_094_S_01: 24개 KPI 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_01`: KPI 24개 동시 로드 중

### 비즈니스 룰
- primary, owner, manager 접근 가능
- 권한 체크 후 로딩 시작

### 연결 화면
- 이전: 진입
- 다음: `02-정상` / `03-일부실패` / `04-빈상태` / `05-에러` / `06-권한없음`
