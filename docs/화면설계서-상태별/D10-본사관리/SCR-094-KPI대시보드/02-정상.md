# SCR-094 KPI대시보드 — 상태: 정상 (24개 KPI 표시)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 목표 설정 → DLG-094-001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 정상 (24개 KPI + 코호트 + 퍼널)

파일: src/app/(admin)/kpi/page.tsx

레이아웃:
- AppLayout + PageHeader title="KPI 대시보드"
- 우측 상단: [목표 설정] Button → DLG-094-001 모달

[KPI 카드 24개 — 4열 StatCardGrid]
카테고리별 섹션 분리:
1. 회원 지표 (6개): 총 회원, 신규, 탈퇴, 재등록, 출석률, PT 예약률
2. 매출 지표 (6개): 총매출, PT매출, 회원권매출, 달성률, 전월비, 객단가
3. 운영 지표 (6개): 수업 진행율, 강사 가동률, 빈 타임슬롯, 시설 이용률, 고객 만족도, 민원수
4. 성장 지표 (6개): MoM 성장률, 누적 매출, 연간 목표달성률, 순증가 회원, NPS, 리텐션율

각 StatCard:
- title, value, trend (up/down/flat), trendValue, unit
- 목표 달성률 있으면 미니 프로그레스바 표시

[코호트 분석 차트]
- 월별 재등록율 히트맵 (recharts)

[퍼널 차트]
- 상담 → 체험 → 등록 → 재등록 단계별 전환율

데이터:
const kpiData = await supabase.from('kpi_snapshots')
  .select('*')
  .order('snapshot_date', { ascending: false })
  .limit(1)
  .single()

목표: localStorage.getItem(`kpi_monthly_target_${branchId}_${yearMonth}`)

사용 유틸: formatNumber, StatCard, StatCardGrid, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_02`: 24개 KPI 전체 로드 성공

### 필수 데이터
| 블록 | 테이블/소스 | 비고 |
|------|------------|------|
| KPI 24개 | `kpi_snapshots` | 최신 1건 |
| 목표 | `localStorage` | kpi_monthly_target_{id}_{ym} |
| 코호트 | `kpi_snapshots` | 6개월치 |

### 인터랙션 (User Actions)
1. [목표 설정] → DLG-094-001 모달
2. KPI 카드 클릭 → 해당 상세 드릴다운 (구현 시)

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-094-001 / SCR-095(프리뷰)
