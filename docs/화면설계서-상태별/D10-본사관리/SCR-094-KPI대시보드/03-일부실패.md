# SCR-094 KPI대시보드 — 상태: 일부 KPI 실패

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `partial-error` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 해당 카드 재시도 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 일부 KPI 에러

조건: E_094_S_03 — 24개 중 일부 KPI 로드 실패, 나머지 정상

구현:
- KPI별 독립 로드 (Promise.allSettled 사용)
- 실패한 KPI 카드:
  <div className="flex flex-col items-center justify-center h-28 bg-red-50 rounded-xl border border-red-200">
    <p className="text-xs text-red-500">불러오기 실패</p>
    <button onClick={() => retryKpi(kpiKey)} className="text-xs text-blue-500 mt-1 underline">
      재시도
    </button>
  </div>

- 성공한 KPI 카드: 정상 StatCard 렌더링

const results = await Promise.allSettled(kpiPromises)
results.forEach((result, i) => {
  if (result.status === 'rejected') {
    setKpiErrors(prev => ({ ...prev, [kpiKeys[i]]: true }))
  }
})
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_03`: 24개 KPI 중 일부만 실패

### 인터랙션 (User Actions)
1. 실패 카드 [재시도] → 해당 KPI만 재로드

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 성공 → `02-정상` (전체 성공 시)
