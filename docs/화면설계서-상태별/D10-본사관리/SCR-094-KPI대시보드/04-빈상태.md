# SCR-094 KPI대시보드 — 상태: 빈 상태 (0값 KPI)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `empty-zero` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 데이터 축적 후 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 신규 지점 (0값 KPI)

조건: E_094_S_06 — kpi_snapshots 데이터 없음 또는 모든 값이 0

레이아웃:
- 24개 KPI 카드 모두 0값으로 표시
  · value: "0" 또는 "0원", trend 배지 없음
- 안내 배너:
  <div className="bg-blue-50 border border-blue-200 rounded-lg px-4 py-3 text-sm text-blue-700 mb-4">
    데이터가 쌓이면 KPI가 표시됩니다. 회원 등록 및 수업 운영을 시작하세요.
  </div>

TC-094-F6-001: 신규 지점 → 진입 → 0값 KPI 표시
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_06`: 신규 지점 또는 kpi_snapshots 데이터 없음

### TC 연결
- TC-094-F6-001: 신규 지점 → 진입 → 0값 KPI 표시

### 연결 화면
- 이전: `01-로딩`
- 다음: 운영 시작 후 데이터 축적 → `02-정상`
