# SCR-094 KPI대시보드 — 상태: 전체 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `error` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 전체 API 실패

조건: E_094_S_04 — 24개 KPI 전체 로드 실패

에러 처리:
toast.error("KPI 데이터를 불러오지 못했습니다.")

에러 UI:
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <p className="text-gray-500">KPI 데이터를 불러오지 못했습니다.</p>
  <Button variant="outline" onClick={loadKpi}>재시도</Button>
</div>

E_094_S_08: 재시도 → loadKpi() → 01-로딩
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_04`: kpi_snapshots API 전체 실패

### TC 연결
- TC-094-011: API 에러 → KPI 로드 → toast.error "KPI 데이터를 불러오지 못했습니다."

### 인터랙션 (User Actions)
1. [재시도] → `E_094_S_08` → `01-로딩`

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
