# SCR-094 KPI대시보드 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-094 |
| 상태 코드 | `blocked` |
| 경로 | `src/app/(admin)/kpi/page.tsx` |
| 역할 | fc, staff, readonly (차단) |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 대시보드 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-094 KPI대시보드 — 상태: 권한없음

조건: E_094_S_05 — hasPermission(user, 'kpi:read') === false
접근 가능: primary, owner, manager
차단 대상: fc, staff, readonly

권한없음 UI:
if (!hasPermission(user, 'kpi:read')) {
  return (
    <AppLayout>
      <div className="flex flex-col items-center justify-center min-h-[70vh] gap-4">
        <div className="text-5xl">🔒</div>
        <h2 className="text-xl font-semibold text-gray-700">접근 권한이 없습니다</h2>
        <p className="text-sm text-gray-500">KPI 대시보드는 매니저 이상만 접근 가능합니다.</p>
        <Button onClick={() => moveToPage('/dashboard')} variant="outline">
          대시보드로 이동
        </Button>
      </div>
    </AppLayout>
  )
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_094_S_05`: fc / staff / readonly 역할로 URL 직접 접근

### 비즈니스 룰
- primary, owner, manager만 KPI 대시보드 접근 가능

### 연결 화면
- 이전: 진입 시도
- 다음: 대시보드 이동 → SCR-090
