# SCR-092 지점관리 — 상태: 폐점 지점 포함

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-092 |
| 상태 코드 | `with-closed` |
| 경로 | `src/app/(admin)/branches/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` 또는 `01-로딩` |
| 다음 상태 | 필터로 `02-정상`으로 전환 가능 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-092 지점관리 — 상태: 폐점 지점 포함

조건: E_092_S_04 — branches에 status='closed' 지점 존재

레이아웃:
- AppLayout + PageHeader title="지점 관리"

[Amber 배너 — DataTable 상단]
<div className="bg-amber-50 border border-amber-200 rounded-lg px-4 py-3 flex items-center gap-2 mb-4">
  <span className="text-amber-600 text-sm font-medium">
    ⚠️ 폐점 지점이 포함되어 있습니다 — 조회 전용
  </span>
  <button
    onClick={() => setStatusFilter('active')}
    className="ml-auto text-xs text-amber-700 underline"
  >
    활성 지점만 보기
  </button>
</div>

[DataTable]
- 폐점(closed) 지점 행: bg-gray-50 opacity-60
- 상태 배지: red "폐점"
- [비활성화] 버튼 숨김 (closed 지점은 액션 불가)
- 조회 전용 표시 (수정 버튼도 비활성)

TC-092-014: 폐점 지점 존재 → 목록 조회 → amber 배너 표시
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_092_S_04`: branches 중 status='closed' 항목 1개 이상

### TC 연결
- TC-092-014: 폐점 지점 존재 → 목록 조회 → amber 배너 표시

### 인터랙션 (User Actions)
1. "활성 지점만 보기" → statusFilter='active' 로 전환 → `02-정상`
2. 폐점 지점 행 클릭 → SCR-093 (읽기 전용)

### 비즈니스 룰
- 폐점 지점은 수정/비활성화 불가
- 폐점 지점 데이터는 리포트에서는 여전히 조회 가능

### 연결 화면
- 이전: `01-로딩` 또는 `02-정상`
- 다음: 필터 변경 → `02-정상`
