# SCR-092 지점관리 — 상태: 빈 목록 (지점 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-092 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/branches/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 지점 등록 → DLG-092-001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-092 지점관리 — 상태: 지점 없음

조건: E_092_S_03 — branches 조회 결과 0건

레이아웃:
- AppLayout + PageHeader title="지점 관리"
- [신규 지점 등록] 버튼 유지 (클릭 가능)

[빈 상태 영역]
<div className="flex flex-col items-center justify-center h-80 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-4 mt-6">
  <p className="text-gray-500 font-medium">등록된 지점이 없습니다.</p>
  <p className="text-sm text-gray-400">신규 지점을 등록하여 관리를 시작하세요.</p>
  <Button onClick={() => setIsRegisterOpen(true)} size="sm">
    지점 등록하기
  </Button>
</div>

TC-092-F6-001: 지점 0개 → 진입 → 빈 상태 표시
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_092_S_03`: 로드 성공 + 지점 0개

### TC 연결
- TC-092-F6-001: 지점 0개 → 진입 → 빈 상태 표시

### 인터랙션 (User Actions)
1. [지점 등록하기] → DLG-092-001 모달

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-092-001 → 등록 성공 → `02-정상`
