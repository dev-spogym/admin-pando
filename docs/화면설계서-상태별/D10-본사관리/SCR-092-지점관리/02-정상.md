# SCR-092 지점관리 — 상태: 정상 (지점 목록 + 통계카드)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-092 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/branches/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 신규등록 → DLG-092-001 / 비활성화 → DLG-092-002 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-092 지점관리 — 상태: 정상

파일: src/app/(admin)/branches/page.tsx

권한:
const { user } = useAuthStore()
if (!hasPermission(user, 'branches:read')) return <BlockedState />

레이아웃:
- AppLayout + PageHeader title="지점 관리"
- 우측 상단: [신규 지점 등록] Button (primary, onClick → DLG-092-001 모달 열기)

[StatCardGrid — 3개 카드]
- 전체 지점 수
- 활성 지점 수
- 비활성 지점 수

[DataTable — 지점 목록]
columns:
- 지점명 (text-left font-medium)
- 지역 (text-sm text-gray-600)
- 회원수 (text-right)
- 상태 배지 (active: green, inactive: gray, closed: red)
- 오픈일 (yyyy.MM.dd)
- 액션: [수정] [비활성화] 버튼

기능:
- 검색 (지점명 검색 input)
- 정렬 (컬럼 헤더 클릭)
- 페이지네이션 (10건씩)

데이터:
const { data: branches } = await supabase
  .from('branches')
  .select('id, name, region, member_count, status, opened_at')
  .order('opened_at', { ascending: false })

사용 유틸: useAuthStore, supabase, DataTable, StatCard, formatNumber, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_092_S_02`: 로드 성공 + 지점 목록 정상

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| 통계 카드 | `branches` | status별 count |
| 지점 목록 | `branches` | 전체 (페이지네이션) |

### 인터랙션 (User Actions)
1. [신규 지점 등록] → DLG-092-001 모달
2. [비활성화] → DLG-092-002 모달
3. [수정] → 지점 수정 폼 (인라인 또는 별도 화면)
4. 행 클릭 → SCR-093 지점성과리포트 (해당 지점)

### 비즈니스 룰
- primary: 모든 액션 가능
- owner: 읽기 + 자기 지점만 수정 가능
- closed 상태 지점: [비활성화] 버튼 숨김

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-092-001 / DLG-092-002 / SCR-093
