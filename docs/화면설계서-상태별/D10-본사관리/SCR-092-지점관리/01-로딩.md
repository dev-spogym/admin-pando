# SCR-092 지점관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-092 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/branches/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-092 지점관리 — 상태: 로딩

파일: src/app/(admin)/branches/page.tsx

레이아웃:
- AppLayout + PageHeader title="지점 관리"
- 우측 상단 [신규 지점 등록] 버튼 (비활성 처리 또는 표시 유지)

로딩 UI:
- StatCardGrid 상단 skeleton (카드 3개: 전체/활성/비활성 지점수)
- DataTable skeleton:
  · 헤더: 지점명 / 지역 / 회원수 / 상태 / 오픈일 / 액션
  · 행 5개 animate-pulse bg-gray-100 h-10 rounded

const [isLoading, setIsLoading] = useState(true)
E_092_S_01: 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_092_S_01`: 지점 목록 API 호출 중

### 비즈니스 룰
- primary, owner만 접근 가능 (로딩 전 권한 확인)
- manager 이하 → `07-권한없음` 즉시 렌더링

### 연결 화면
- 이전: 진입
- 다음: `02-정상` / `03-빈목록` / `04-폐점포함` / `06-에러` / `07-권한없음`
