# SCR-092 지점관리 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-092 |
| 상태 코드 | `blocked` |
| 경로 | `src/app/(admin)/branches/page.tsx` |
| 역할 | manager, fc, staff, readonly (차단) |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 대시보드 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-092 지점관리 — 상태: 권한없음

조건: E_092_S_06 — hasPermission(user, 'branches:read') === false

권한없음 UI:
if (!hasPermission(user, 'branches:read')) {
  return (
    <AppLayout>
      <div className="flex flex-col items-center justify-center min-h-[70vh] gap-4">
        <div className="text-5xl">🔒</div>
        <h2 className="text-xl font-semibold text-gray-700">접근 권한이 없습니다</h2>
        <p className="text-sm text-gray-500">지점 관리는 최고관리자 또는 대표만 접근 가능합니다.</p>
        <Button onClick={() => moveToPage('/dashboard')} variant="outline">
          대시보드로 이동
        </Button>
      </div>
    </AppLayout>
  )
}

사용 유틸: hasPermission, moveToPage, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_092_S_06`: manager 이하 역할로 URL 직접 접근

### 비즈니스 룰
- primary, owner만 지점 관리 접근 가능
- 서버사이드 미들웨어에서도 리다이렉트 처리 권장

### 연결 화면
- 이전: 진입 시도
- 다음: 대시보드 이동
