# SCR-101 커스텀대시보드빌더 — 상태: 저장 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `saved` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` 또는 `07-미저장` ([저장] 클릭 후) |
| 다음 상태 | 추가 변경 → `07-미저장` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 저장 완료

조건: E_F6_101_07 — [저장] 클릭 후 localStorage 저장 성공

저장 후 UI:
- [저장] 버튼: "저장됨 ✓" 텍스트 + green (2초 후 원복)
- 마지막 저장 시각 표시 (PageHeader 우측):
  "마지막 저장: HH:mm:ss" (text-xs text-gray-400)
- hasUnsaved = false → [저장] 버튼 일반 상태 복귀

저장 로직:
const saveLayout = () => {
  localStorage.setItem(`dashboard-layout-${userId}`, JSON.stringify(layout))
  setLastSaved(new Date())
  setHasUnsaved(false)
  toast.success("대시보드 레이아웃이 저장되었습니다.")
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_07`: [저장] 클릭 → localStorage 저장 성공

### 인터랙션 (User Actions)
1. 추가 위젯 변경 → `07-미저장`

### 연결 화면
- 이전: `02-정상` / `07-미저장`
- 다음: 변경 시 `07-미저장`
