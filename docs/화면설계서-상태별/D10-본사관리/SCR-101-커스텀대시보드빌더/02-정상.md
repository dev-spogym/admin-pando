# SCR-101 커스텀대시보드빌더 — 상태: 정상 (위젯 데이터 표시)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `populated` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 드래그 → `05-드래그중` / 저장 → `06-저장완료` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 정상 (모든 위젯 데이터 표시)

파일: src/app/(admin)/dashboard-builder/page.tsx

E_F6_101_02: 전체 위젯 데이터 로드 성공

레이아웃:
- AppLayout + PageHeader title="커스텀 대시보드 빌더"
- 우측 툴바: [위젯 추가] [저장] [초기화] 버튼

[드래그앤드롭 그리드]
라이브러리: react-grid-layout 또는 @dnd-kit/core
- 그리드: 12열 기반 반응형
- 위젯 카드: bg-white border rounded-xl shadow-sm p-4
  · 드래그 핸들 (상단 점 6개 아이콘)
  · 위젯 제목 + 데이터
  · 우측 상단: [제거] X 버튼

[위젯 패널 — 우측 사이드 드로어 or 버튼]
추가 가능 위젯 목록:
- 매출 카드, 회원 카드, 출석 차트, KPI 미니, 공지 리스트 등

[미저장 변경 감지]
E_F6_101_08: 레이아웃 변경 감지 → [저장] 버튼 강조 (ring-2 ring-blue-500)

레이아웃 저장:
localStorage.setItem('dashboard-layout-{userId}', JSON.stringify(layout))
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_02`: 저장된 레이아웃 복원 + 모든 위젯 데이터 로드 성공

### 인터랙션 (User Actions)
1. 위젯 드래그 → `05-드래그중`
2. [저장] 클릭 → `06-저장완료`
3. [위젯 추가] → 위젯 패널 열기
4. [X] 클릭 → 위젯 제거 → `07-미저장`

### 연결 화면
- 이전: `01-로딩`
- 다음: `05-드래그중` / `06-저장완료` / `07-미저장`
