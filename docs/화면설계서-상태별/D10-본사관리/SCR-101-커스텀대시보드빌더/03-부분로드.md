# SCR-101 커스텀대시보드빌더 — 상태: 부분 로드 (일부 위젯 실패)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-101 |
| 상태 코드 | `partial` |
| 경로 | `src/app/(admin)/dashboard-builder/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 위젯 재시도 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-101 커스텀대시보드빌더 — 부분 로드 (일부 위젯 실패)

조건: E_F6_101_03 — 일부 위젯 API 실패, 나머지 정상

실패한 위젯 카드:
<div className="flex flex-col items-center justify-center h-full bg-red-50 rounded-xl border border-red-200 gap-2">
  <p className="text-xs text-red-500">위젯을 불러오지 못했습니다</p>
  <button onClick={() => retryWidget(widgetId)} className="text-xs text-blue-500 underline">
    재시도
  </button>
</div>

구현:
const results = await Promise.allSettled(widgetPromises)
results.forEach((result, i) => {
  if (result.status === 'rejected') {
    setWidgetErrors(prev => new Set([...prev, widgetIds[i]]))
  }
})

E_F6_101_09: 실패 위젯 재시도 → 해당 위젯만 재로드
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_101_03`: Promise.allSettled 중 일부 rejected

### 인터랙션 (User Actions)
1. 실패 위젯 [재시도] → 해당 위젯만 재로드

### 연결 화면
- 이전: `01-로딩`
- 다음: 모든 위젯 성공 → `02-정상`
