# SCR-097 감사로그 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/audit-log/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 상태: 로딩

파일: src/app/(admin)/audit-log/page.tsx

권한: primary, owner만 접근 (로딩 전 체크)

레이아웃:
- AppLayout + PageHeader title="감사 로그"

로딩 스켈레톤:
- 필터 바 skeleton (날짜 범위 + 액션 타입 + 사용자 드롭다운)
- DataTable skeleton:
  · 헤더: 시각 / 사용자 / 액션 / 대상 / IP
  · 행 10개 animate-pulse bg-gray-100 h-10 rounded

const [isLoading, setIsLoading] = useState(true)
E_097_S_01: 로그 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_097_S_01`: audit_log 테이블 API 호출 중

### 비즈니스 룰
- primary, owner만 접근 가능
- manager 이하 → `05-권한없음` 즉시 렌더링

### 연결 화면
- 이전: 진입
- 다음: `02-정상` / `03-필터결과없음` / `04-에러` / `05-권한없음`
