# SCR-097 감사로그 — 상태: 정상 (로그 목록 + 필터)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-097 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/audit-log/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 필터 적용 → 데이터 갱신 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-097 감사로그 — 상태: 정상

파일: src/app/(admin)/audit-log/page.tsx

레이아웃:
- AppLayout + PageHeader title="감사 로그"

[필터 바]
- 날짜 범위 DateRangePicker (기본: 최근 7일)
- 액션 타입 Select: 전체 / 로그인 / 데이터수정 / 삭제 / 권한변경
- 사용자 Input (이름/이메일 검색)
- [초기화] 버튼

[DataTable — 감사 로그]
columns:
- 시각 (yyyy.MM.dd HH:mm:ss, text-sm text-gray-500)
- 사용자 (이름 + 역할 배지)
- 액션 (액션 타입 배지: 로그인=blue, 수정=yellow, 삭제=red, 권한변경=purple)
- 대상 (테이블명 + 레코드 ID)
- IP 주소 (text-xs text-gray-400)
- [상세] 버튼 → 로그 상세 모달 (미구현 시 생략)

정렬: created_at desc (기본, 변경 불가)
페이지네이션: 20건씩

데이터:
supabase.from('audit_log')
  .select('id, created_at, actor_name, actor_role, action_type, target_table, target_id, ip_address')
  .order('created_at', { ascending: false })
  .range(page * 20, (page + 1) * 20 - 1)

필터 적용 시 .eq('action_type', filter) 등 추가

사용 유틸: supabase, DataTable, useAuthStore, formatNumber
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_097_S_02`: 로드 성공 + 로그 존재

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| 로그 목록 | `audit_log` | created_at desc, 페이지네이션 |

### 인터랙션 (User Actions)
1. 필터 변경 → API 재호출
2. [초기화] → 필터 리셋
3. 페이지 이동 → 페이지네이션

### 비즈니스 룰
- 로그는 수정/삭제 불가 (읽기 전용)
- IP 주소 마스킹 옵션 (*.*.*.* 처리) 고려

### TC 연결
- TC-097-F6-001: 필터 적용 → 결과 없음 → 빈 상태 메시지

### 연결 화면
- 이전: `01-로딩`
- 다음: 필터 결과 없음 → `03-필터결과없음`
