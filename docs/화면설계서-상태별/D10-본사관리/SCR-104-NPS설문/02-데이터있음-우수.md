# SCR-104 NPS설문 — 상태: 데이터 있음 (NPS >= 50, 우수)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-104 |
| 상태 코드 | `high-score` |
| 경로 | `src/app/(admin)/nps/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 필터 변경 → `06-새로고침` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-104 NPS설문 — 우수 (NPS >= 50)

파일: src/app/(admin)/nps/page.tsx

조건: E_F6_104_05 — npsScore >= 50

레이아웃:
- AppLayout + PageHeader title="NPS 설문"

[NPS 점수 카드 — 메인]
<div className="bg-green-50 border-2 border-green-300 rounded-2xl p-6 flex items-center gap-6">
  <div className="text-center">
    <p className="text-xs text-green-600 font-medium uppercase tracking-wide">NPS 점수</p>
    <p className="text-6xl font-bold text-green-700 mt-1">{npsScore}</p>
    <span className="inline-block bg-green-100 text-green-700 text-sm font-semibold px-3 py-1 rounded-full mt-2">
      우수
    </span>
  </div>
  <div className="flex-1 space-y-2">
    <div className="flex justify-between text-sm">
      <span className="text-gray-600">추천자 (9~10점)</span>
      <span className="font-semibold text-green-600">{promoters}명 ({promoterPct}%)</span>
    </div>
    <div className="flex justify-between text-sm">
      <span className="text-gray-600">중립자 (7~8점)</span>
      <span className="font-semibold text-gray-500">{passives}명 ({passivePct}%)</span>
    </div>
    <div className="flex justify-between text-sm">
      <span className="text-gray-600">비추천자 (0~6점)</span>
      <span className="font-semibold text-red-500">{detractors}명 ({detractorPct}%)</span>
    </div>
  </div>
</div>

[분포 막대 차트] — 0~10점 각 구간별 응답 수

[응답 목록 DataTable]
- 응답자 / 점수 / 한마디 / 응답일
- 점수별 배지: 9~10=green, 7~8=gray, 0~6=red

[필터]
- 기간 (최근 30일 / 90일 / 6개월)
- 최소 응답 수: {totalResponses}건

데이터:
supabase.from('nps_surveys')
  .select('member_id, score, comment, created_at')
  .order('created_at', { ascending: false })
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_104_05`: npsScore >= 50

### NPS 계산 공식
- NPS = (추천자% - 비추천자%)
- 추천자: 9~10점, 비추천자: 0~6점

### 인터랙션 (User Actions)
1. 필터 변경 → `E_F6_104_08` → `06-새로고침`
2. 응답 행 클릭 → 상세 (코멘트 전체 보기)

### 연결 화면
- 이전: `01-로딩`
- 다음: 필터 변경 → `06-새로고침`
