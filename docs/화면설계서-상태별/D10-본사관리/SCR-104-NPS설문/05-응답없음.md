# SCR-104 NPS설문 — 상태: 응답 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-104 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/nps/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 설문 발송 후 → `02~04` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-104 NPS설문 — 응답 없음

조건: E_F6_104_03 — nps_surveys 0건

빈 상태 UI:
<div className="flex flex-col items-center justify-center min-h-[60vh] gap-5">
  <div className="text-6xl">📋</div>
  <h2 className="text-xl font-semibold text-gray-700">설문 응답이 없습니다</h2>
  <p className="text-sm text-gray-500 text-center max-w-sm">
    아직 NPS 설문 응답이 없습니다.<br />
    회원들에게 설문을 발송하여 만족도를 측정해 보세요.
  </p>
  <Button onClick={openSurveyModal} size="sm">설문 발송하기</Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_104_03`: nps_surveys 응답 0건

### 인터랙션 (User Actions)
1. [설문 발송하기] → 설문 발송 모달 (미구현 시 toast.info "설문 발송 기능 준비중")

### 연결 화면
- 이전: `01-로딩`
- 다음: 응답 수신 후 → `02~04` 상태
