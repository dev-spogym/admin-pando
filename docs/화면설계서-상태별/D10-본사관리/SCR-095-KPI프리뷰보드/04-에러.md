# SCR-095 KPI프리뷰보드 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-095 |
| 상태 코드 | `error` |
| 경로 | `src/app/(admin)/kpi-preview/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-095 KPI프리뷰보드 — 에러

조건: E_095_S_04 — API 실패

에러 UI:
<div className="flex flex-col items-center justify-center h-80 gap-4">
  <p className="text-gray-500">KPI 데이터를 불러오지 못했습니다.</p>
  <Button variant="outline" onClick={loadKpiPreview}>재시도</Button>
</div>

toast.error("KPI 데이터를 불러오지 못했습니다.")
E_095_S_06: 재시도 → loadKpiPreview() → 01-로딩
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_095_S_04`: kpi_snapshots API 실패

### TC 연결
- TC-095-F6-001: API 실패 → 진입 → 에러 상태 + 재시도

### 인터랙션 (User Actions)
1. [재시도] → `E_095_S_06` → `01-로딩`

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
