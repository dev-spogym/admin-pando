# SCR-095 KPI프리뷰보드 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-095 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/kpi-preview/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-095 KPI프리뷰보드 — 상태: 로딩

파일: src/app/(admin)/kpi-preview/page.tsx

레이아웃:
- AppLayout + PageHeader title="KPI 프리뷰"
- TabNav skeleton (5개 탭 버튼 skeleton)

로딩 스켈레톤:
- 탭별 KPI 카드 4개 skeleton (2x2 grid, animate-pulse bg-gray-200 rounded-xl h-24)
- 퍼널 차트 skeleton (h-48 bg-gray-200)
- 인사이트 텍스트 skeleton (3줄 bg-gray-100 rounded h-4)

E_095_S_01: 탭별 데이터 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_095_S_01`: 5개 탭 데이터 로드 중

### 비즈니스 룰
- 전 역할(readonly 제외) 접근 가능 — 읽기 전용 뷰

### 연결 화면
- 이전: 진입
- 다음: `02-정상` / `03-빈상태` / `04-에러`
