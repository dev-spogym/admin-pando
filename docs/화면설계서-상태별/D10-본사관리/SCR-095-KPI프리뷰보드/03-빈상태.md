# SCR-095 KPI프리뷰보드 — 상태: 빈 상태 (0값 KPI)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-095 |
| 상태 코드 | `empty-zero` |
| 경로 | `src/app/(admin)/kpi-preview/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 데이터 축적 후 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-095 KPI프리뷰보드 — 빈 상태 (0값 KPI)

조건: E_095_S_03 — kpi_snapshots 없음 또는 모두 0

레이아웃:
- AppLayout + PageHeader title="KPI 프리뷰"
- TabNav 유지 (탭 전환 가능)

각 탭 콘텐츠:
- KPI 카드 4개 → 모두 value="0" 표시
- 안내 배너:
  <div className="bg-blue-50 border border-blue-200 rounded-lg px-4 py-3 text-sm text-blue-700 mt-4">
    데이터가 쌓이면 KPI가 표시됩니다.
  </div>

- 차트 영역: 빈 상태 placeholder
  <div className="flex items-center justify-center h-48 bg-gray-50 rounded-xl border border-dashed border-gray-300">
    <p className="text-sm text-gray-400">표시할 데이터가 없습니다</p>
  </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_095_S_03`: kpi_snapshots 데이터 없음 또는 전체 0

### 연결 화면
- 이전: `01-로딩`
- 다음: 운영 후 데이터 축적 → `02-정상`
