# SCR-096 온보딩대시보드 — 상태: 초기 (0% 미시작)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-096 |
| 상태 코드 | `init` |
| 경로 | `src/app/(admin)/onboarding/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | 단계 시작 → `02-진행중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-096 온보딩대시보드 — 초기 상태 (0% 미시작)

파일: src/app/(admin)/onboarding/page.tsx

조건: E_096_S_01 — onboarding_progress 전체 미완료 (0%)

레이아웃:
- AppLayout + PageHeader title="온보딩 대시보드" subtitle="FitGenie CRM 시작하기"

[진행률 바 — 전체 상단]
<div className="bg-gray-100 rounded-full h-3 w-full">
  <div className="bg-blue-500 h-3 rounded-full transition-all" style={{ width: '0%' }} />
</div>
<p className="text-sm text-gray-500 mt-1">0% 완료</p>

[온보딩 단계 목록]
필수 4단계 + 선택 단계:
1. 📝 지점 기본 정보 입력 (필수) — 미완료
2. 👤 첫 번째 직원 등록 (필수) — 미완료
3. 🏋️ 수업/프로그램 설정 (필수) — 미완료
4. 💳 이용권 상품 생성 (필수) — 미완료
5. 📊 KPI 목표 설정 (선택) — 미완료

각 단계 카드 (bg-white border rounded-xl p-4 flex items-center gap-4):
- 아이콘 (회색 원형 배지 — 미완료)
- 단계명 + 설명
- [시작하기] 버튼 (첫 단계만 활성, 나머지 비활성)

데이터:
supabase.from('onboarding_progress')
  .select('step_id, completed, completed_at')
  .eq('branch_id', branchId)

TC-096-F6-002: 신규 지점 → 진입 → 전체 미완료 초기 상태
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_096_S_01`: onboarding_progress 전체 completed=false

### TC 연결
- TC-096-F6-002: 신규 지점 → 진입 → 전체 미완료 초기 상태

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| 진행 현황 | `onboarding_progress` | branch_id 필터 |

### 인터랙션 (User Actions)
1. [시작하기] (1단계) → 해당 설정 화면으로 이동
2. 2~4단계 버튼 비활성 (순차 진행)

### 비즈니스 룰
- 이전 단계 완료 후 다음 단계 활성화
- primary, owner만 접근 가능

### 연결 화면
- 이전: 진입
- 다음: 단계별 설정 화면 → `02-진행중`
