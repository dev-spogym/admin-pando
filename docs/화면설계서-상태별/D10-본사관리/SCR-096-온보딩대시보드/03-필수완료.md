# SCR-096 온보딩대시보드 — 상태: 필수 단계 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-096 |
| 상태 코드 | `required-done` |
| 경로 | `src/app/(admin)/onboarding/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-진행중` |
| 다음 상태 | 선택 단계 완료 → `04-전체완료` 또는 대시보드 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-096 온보딩대시보드 — 필수 4단계 완료

조건: E_096_S_03 — 필수 4단계 모두 completed=true

[완료 배너]
<div className="bg-green-50 border border-green-200 rounded-xl px-6 py-5 flex items-center gap-4 mb-6">
  <div className="text-4xl">🎉</div>
  <div>
    <p className="font-semibold text-green-800">필수 설정이 완료되었습니다!</p>
    <p className="text-sm text-green-700 mt-0.5">대시보드에서 운영을 시작하세요.</p>
  </div>
  <Button onClick={() => moveToPage('/dashboard')} className="ml-auto" size="sm">
    대시보드로 이동
  </Button>
</div>

[진행률 바] — 100% (필수 기준)

[선택 단계 안내]
<div className="bg-blue-50 border border-blue-100 rounded-xl p-4">
  <p className="text-sm font-medium text-blue-800 mb-3">선택 단계로 더 강력하게 활용하세요</p>
  {optionalSteps.map(step => (
    <div key={step.id} className="flex items-center justify-between py-2">
      <span className="text-sm text-gray-700">{step.title}</span>
      {step.completed
        ? <span className="text-xs text-green-600">완료</span>
        : <Button size="xs" variant="outline" onClick={() => startStep(step.id)}>시작</Button>
      }
    </div>
  ))}
</div>

toast.success E_096_S_06: "온보딩 완료!"
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_096_S_03`: 필수 4단계 모두 완료 직후

### TC 연결
- TC-096-F6-001: 필수 4단계 완료 → 확인 → 완료 toast + 대시보드 이동

### 인터랙션 (User Actions)
1. [대시보드로 이동] → SCR-090
2. 선택 단계 [시작] → 해당 설정 화면

### 연결 화면
- 이전: `02-진행중`
- 다음: SCR-090 또는 선택 단계 완료 → `04-전체완료`
