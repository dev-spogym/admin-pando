# SCR-096 온보딩대시보드 — 상태: 전체 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-096 |
| 상태 코드 | `all-done` |
| 경로 | `src/app/(admin)/onboarding/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `03-필수완료` |
| 다음 상태 | 대시보드 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-096 온보딩대시보드 — 전체 완료 (필수+선택 모두)

조건: E_096_S_04 — 모든 단계 completed=true

[완료 화면]
<div className="flex flex-col items-center justify-center min-h-[60vh] gap-6">
  <div className="text-6xl">🏆</div>
  <h2 className="text-2xl font-bold text-gray-800">온보딩 완료!</h2>
  <p className="text-gray-500 text-center max-w-sm">
    모든 설정을 완료했습니다. FitGenie CRM을 최대한 활용할 준비가 되었습니다.
  </p>
  <div className="flex gap-3">
    <Button onClick={() => moveToPage('/dashboard')}>대시보드 시작하기</Button>
    <Button variant="outline" onClick={() => moveToPage('/kpi')}>KPI 확인하기</Button>
  </div>
</div>

[진행률 바] — 100%
[전체 단계 목록] — 모두 초록 체크

toast.success("온보딩을 모두 완료했습니다! 🎉")
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_096_S_04`: 필수 + 선택 모든 단계 완료

### 인터랙션 (User Actions)
1. [대시보드 시작하기] → SCR-090
2. [KPI 확인하기] → SCR-094

### 연결 화면
- 이전: `03-필수완료`
- 다음: SCR-090 또는 SCR-094
