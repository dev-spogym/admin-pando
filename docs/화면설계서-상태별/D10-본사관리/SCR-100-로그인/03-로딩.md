# SCR-100 로그인 — 상태: 로딩 (로그인 요청 중)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-100 |
| 상태 코드 | `loading` |
| 경로 | `src/app/login/page.tsx` |
| 역할 | 비인증 사용자 |
| 우선순위 | P0 |
| 이전 상태 | `02-입력중` |
| 다음 상태 | 성공 → 리다이렉트 / 실패 → `04-에러인라인` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-100 로그인 — 로딩 상태

조건: E_F6_100_03 — [로그인] 클릭 후 Supabase auth 요청 중

로딩 UI:
- [로그인] 버튼: disabled=true + 스피너 표시
  <Button disabled className="w-full">
    <Loader2 className="w-4 h-4 animate-spin mr-2" />
    로그인 중...
  </Button>
- 두 Input: disabled=true (입력 불가)
- 폼 전체 opacity-70

API 호출:
const { error } = await supabase.auth.signInWithPassword({ email, password })

성공 (E_F6_100_04):
→ useAuthStore().setUser(user)
→ router.push('/dashboard')

실패 (E_F6_100_05): → 04-에러인라인
잠금 (E_F6_100_06): → 05-잠금
서버에러 (E_F6_100_07): → 06-서버에러토스트
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_100_03`: [로그인] 버튼 클릭 후 API 응답 대기

### 인터랙션 (User Actions)
- 없음 (입력 비활성 상태)

### 연결 화면
- 이전: `02-입력중`
- 다음: 성공 → /dashboard 리다이렉트 / 실패 → `04-에러인라인` / 잠금 → `05-잠금`
