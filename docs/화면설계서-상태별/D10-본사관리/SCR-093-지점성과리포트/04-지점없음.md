# SCR-093 지점성과리포트 — 상태: 지점 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-093 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/branch-report/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 지점 등록 → SCR-092 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-093 지점성과리포트 — 지점 없음

조건: E_093_S_04 — branches.length === 0

빈 상태 UI:
<div className="flex flex-col items-center justify-center h-80 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-4 mt-6">
  <p className="text-gray-500 font-medium">비교할 지점이 없습니다.</p>
  <p className="text-sm text-gray-400">지점을 등록하면 성과를 비교할 수 있습니다.</p>
  <Button onClick={() => moveToPage('/branches')} variant="outline" size="sm">
    지점 관리로 이동
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_093_S_04`: active 지점 0개

### 인터랙션 (User Actions)
1. [지점 관리로 이동] → SCR-092

### 연결 화면
- 이전: `01-로딩`
- 다음: SCR-092
