# SCR-093 지점성과리포트 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-093 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/branch-report/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-093 지점성과리포트 — 상태: 로딩

파일: src/app/(admin)/branch-report/page.tsx

레이아웃:
- AppLayout + PageHeader title="지점 성과 리포트"

로딩 스켈레톤:
- 필터 바 skeleton (기간 선택 영역)
- DataTable skeleton (지점 행 5개):
  · 컬럼: 순위 / 지점명 / 매출 / 회원수 / PT건수 / 달성률
  · animate-pulse bg-gray-100 h-10 rounded per row
- 차트 영역 skeleton (h-64 bg-gray-200 animate-pulse rounded-xl)

const [isLoading, setIsLoading] = useState(true)
E_093_S_01: 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_093_S_01`: 지점 성과 데이터 API 호출 중

### 비즈니스 룰
- primary, owner만 접근 가능

### 연결 화면
- 이전: 진입 (SCR-091, SCR-092에서 이동)
- 다음: `02-정상(복수지점)` / `03-단일지점` / `04-지점없음` / `05-에러`
