# SCR-093 지점성과리포트 — 상태: 단일 지점 (메달 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-093 |
| 상태 코드 | `single-branch` |
| 경로 | `src/app/(admin)/branch-report/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 지점 추가 후 → `02-정상-복수지점` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-093 지점성과리포트 — 단일 지점 (메달 없이 데이터만)

조건: E_093_S_03 — branches.length === 1

레이아웃:
- AppLayout + PageHeader title="지점 성과 리포트"

[안내 배너]
<div className="bg-blue-50 border border-blue-200 rounded-lg px-4 py-3 text-sm text-blue-700 mb-4">
  지점이 1개일 때는 순위 비교가 제공되지 않습니다.
</div>

[단일 지점 성과 카드]
- 지점명, 이번달 매출, 회원수, PT건수, 달성률
- 메달 아이콘 없음
- 순위 컬럼 없음

[차트]
- 단일 지점 월별 추이 바 차트 (최근 6개월)

조건 분기:
if (branches.length === 1) {
  // 단일 지점 뷰 렌더링
  // 순위 표시 없음, 메달 없음
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_093_S_03`: active 지점이 정확히 1개

### TC 연결
- TC-093-F6-001: 지점 1개 → 리포트 조회 → 메달 없이 데이터만 표시

### 비즈니스 룰
- 메달(🥇🥈🥉)은 지점 2개 이상일 때만 표시
- 단일 지점이어도 달성률/매출은 정상 표시

### 연결 화면
- 이전: `01-로딩`
- 다음: 지점 추가 후 `02-정상-복수지점`
