# SCR-093 지점성과리포트 — 상태: 정상 (지점 2개 이상)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-093 |
| 상태 코드 | `normal-multi` |
| 경로 | `src/app/(admin)/branch-report/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 필터 변경 → 데이터 갱신 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-093 지점성과리포트 — 정상 (지점 2개 이상, 메달 표시)

파일: src/app/(admin)/branch-report/page.tsx

조건: E_093_S_02 — branches.length >= 2

레이아웃:
- AppLayout + PageHeader title="지점 성과 리포트"
- 기간 필터 (이번달 / 지난달 / 분기 선택 드롭다운)

[DataTable — 지점 성과 순위표]
columns:
- 순위: 1위 🥇 2위 🥈 3위 🥉 그 외 숫자
- 지점명
- 이번달 매출 (formatNumber + "원")
- 회원수
- PT 건수
- 달성률 (프로그레스바 + %)

정렬: 이번달 매출 desc (기본)

[바 차트]
- 지점별 매출 비교 바 차트 (라이브러리: recharts 또는 nivo)
- X축: 지점명, Y축: 매출(만원)

[메달 요약 카드]
- 1위 지점 강조 카드 (border-2 border-yellow-400 bg-yellow-50)

데이터:
supabase.from('branches')
  .select('id, name, monthly_revenue, member_count, pt_count, target_revenue')
  .eq('status','active')
  .order('monthly_revenue', { ascending: false })

달성률: (monthly_revenue / target_revenue) * 100

사용 유틸: formatNumber, DataTable, StatCard
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_093_S_02`: 로드 성공 + 지점 2개 이상

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| 순위표 | `branches` | monthly_revenue desc 정렬 |
| 목표 | localStorage 또는 `kpi_snapshots` | 달성률 계산 |

### 인터랙션 (User Actions)
1. 기간 필터 변경 → 데이터 재조회
2. 지점명 클릭 → 해당 지점 상세 (SCR-092)
3. 차트 hover → 툴팁 (매출 상세)

### 비즈니스 룰
- 메달(🥇🥈🥉)은 지점 2개 이상일 때만 표시
- 달성률 100% 이상: green 배지, 50~99%: yellow, <50%: red

### TC 연결
- (정상 케이스 — TC별도 없음)

### 연결 화면
- 이전: `01-로딩`
- 다음: 필터 변경 → 데이터 갱신 (동일 상태 유지)
