# SCR-091 슈퍼대시보드 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary (isSuperAdmin=true만 접근) |
| 우선순위 | P1 |
| 이전 상태 | 진입 + isSuperAdmin 확인 후 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 로딩

권한 체크 먼저:
const { user } = useAuthStore()
const isSuperAdmin = user?.role === 'primary' && user?.is_super_admin === true

if (!isSuperAdmin) → 권한없음 상태(04-권한없음.md) 렌더링

로딩 스켈레톤 (isSuperAdmin=true이고 데이터 로드 중):
- AppLayout + PageHeader title="슈퍼 대시보드"
- KPI 카드 7개 skeleton (animate-pulse bg-gray-200 rounded-xl h-28)
- 지점 카드 그리드 skeleton (3열, 각 h-40)
- 감사로그 테이블 skeleton (5행)

상태 관리:
const [isLoading, setIsLoading] = useState(true)

E_091_S_03: 로딩 중 상태
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_02`: isSuperAdmin=true 확인 후 데이터 로드 시작
- `E_091_S_03`: 로딩 중

### 비즈니스 룰
- isSuperAdmin=false 진입 시 이 상태로 오지 않음 → `04-권한없음`으로 즉시 분기
- 로딩 중에도 PageHeader는 표시

### 연결 화면
- 이전: 진입 (isSuperAdmin 체크 통과 후)
- 다음: `02-정상` / `05-에러` / `06-오프라인`
