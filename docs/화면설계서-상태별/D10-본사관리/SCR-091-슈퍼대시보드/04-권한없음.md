# SCR-091 슈퍼대시보드 — 상태: 권한없음 (isSuperAdmin=false)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `blocked` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | owner, manager, fc, staff, readonly (차단) |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 |
| 다음 상태 | 대시보드 이동 버튼 → SCR-090 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 권한없음

조건: E_091_S_01 — isSuperAdmin=false

권한 체크:
const { user } = useAuthStore()
const isSuperAdmin = user?.is_super_admin === true

if (!isSuperAdmin) {
  return (
    <AppLayout>
      <div className="flex flex-col items-center justify-center min-h-[70vh] gap-4">
        <div className="text-5xl">🔒</div>
        <h2 className="text-xl font-semibold text-gray-700">접근 권한이 없습니다</h2>
        <p className="text-sm text-gray-500 text-center max-w-sm">
          슈퍼 대시보드는 최고 관리자만 접근할 수 있습니다.
        </p>
        <Button onClick={() => moveToPage('/dashboard')} variant="outline">
          대시보드로 이동
        </Button>
      </div>
    </AppLayout>
  )
}

E_091_S_08: [대시보드 이동] 클릭 → SCR-090
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_01`: 진입 시 isSuperAdmin=false 확인

### TC 연결
- TC-091-002: isSuperAdmin=false → 진입 → 권한없음 안내

### 인터랙션 (User Actions)
1. [대시보드로 이동] → SCR-090

### 연결 화면
- 이전: 진입 시도 (URL 직접 접근 또는 메뉴 클릭)
- 다음: `E_091_S_08` → SCR-090
