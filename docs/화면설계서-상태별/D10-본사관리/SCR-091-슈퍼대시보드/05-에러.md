# SCR-091 슈퍼대시보드 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `error` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary (isSuperAdmin=true) |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 에러 (API 실패)

조건: E_091_S_06 — API 전체 실패

에러 UI:
- AppLayout + PageHeader 유지
- 본문 중앙:
  <div className="flex flex-col items-center justify-center h-96 gap-4">
    <p className="text-gray-600">데이터를 불러오지 못했습니다.</p>
    <Button variant="outline" onClick={loadSuperDashboard}>재시도</Button>
  </div>

toast.error("슈퍼 대시보드 데이터를 불러오지 못했습니다.")

E_091_S_10: 재시도 클릭 → loadSuperDashboard() → 01-로딩
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_06`: API 실패 (4xx / 5xx)

### TC 연결
- TC-091-F6-001: API 실패 → 데이터 로드 → 에러 상태 + 재시도

### 인터랙션 (User Actions)
1. [재시도] → `E_091_S_10` → `01-로딩`

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
