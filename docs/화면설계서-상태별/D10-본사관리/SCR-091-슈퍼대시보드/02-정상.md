# SCR-091 슈퍼대시보드 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary (isSuperAdmin=true) |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 각 위젯 액션 → 상세 화면 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 정상 (KPI 7개 + 지점 카드 + 감사로그)

권한: isSuperAdmin=true 필수
const { user } = useAuthStore()
if (!user?.is_super_admin) return <BlockedState />

레이아웃:
- AppLayout + PageHeader title="슈퍼 대시보드" subtitle="전체 지점 통합 현황"

[KPI 카드 7개 — StatCardGrid]
1. 전체 회원 수
2. 이번달 전체 매출
3. 전체 지점 수
4. 신규 가입 (오늘)
5. PT 예약률 (%)
6. 평균 재등록률 (%)
7. 활성 지점 수

[지점 카드 그리드 — 3열]
- 각 지점: 지점명, 회원수, 이번달 매출, 달성률 배지
- 클릭 → SCR-093 지점성과리포트 (해당 지점)
- 달성률 >= 100% → green 배지, 50~99% → yellow, <50% → red

[감사로그 섹션]
- DataTable: 최근 10건 (actor, action, target, created_at)
- 더보기 → SCR-097

데이터:
- supabase.from('branches').select('*').eq('status','active')
- supabase.from('audit_log').select('*').order('created_at',{ascending:false}).limit(10)
- kpi_snapshots: 전체 합산

사용 유틸:
- useAuthStore, supabase, formatNumber, moveToPage, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_04`: isSuperAdmin=true + 데이터 로드 성공

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| KPI | `kpi_snapshots` | 전체 집계 최신 1건 |
| 지점 카드 | `branches` | status='active' |
| 감사로그 | `audit_log` | 최신 10건 |

### TC 연결
- TC-091-001: isSuperAdmin=true → 진입 → 정상 상태

### 연결 화면
- 이전: `01-로딩`
- 다음: SCR-092 / SCR-093 / SCR-097
