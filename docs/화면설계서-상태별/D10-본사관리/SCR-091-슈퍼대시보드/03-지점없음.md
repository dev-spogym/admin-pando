# SCR-091 슈퍼대시보드 — 상태: 지점 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `empty-branch` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary (isSuperAdmin=true) |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 지점 등록 후 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 지점 없음

조건: E_091_S_05 — branches 테이블에 active 지점 0개

레이아웃:
- AppLayout + PageHeader title="슈퍼 대시보드"
- KPI 카드 7개: 모두 0값 표시 (데이터 없음)

[지점 카드 영역 빈 상태]
<div className="flex flex-col items-center justify-center h-64 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-3">
  <p className="text-gray-500 text-base font-medium">등록된 지점이 없습니다.</p>
  <p className="text-sm text-gray-400">지점을 등록하면 통합 현황을 확인할 수 있습니다.</p>
  <Button onClick={() => moveToPage('/branches/new')} size="sm">
    첫 지점 등록하기
  </Button>
</div>

[감사로그]
- 빈 테이블: "로그가 없습니다."
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_05`: 로드 성공이지만 active 지점 0개

### 인터랙션 (User Actions)
1. [첫 지점 등록하기] → SCR-092 또는 `/branches/new`

### 연결 화면
- 이전: `01-로딩`
- 다음: 지점 등록 후 `02-정상`
