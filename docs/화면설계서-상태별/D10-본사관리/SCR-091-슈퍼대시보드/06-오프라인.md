# SCR-091 슈퍼대시보드 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-091 |
| 상태 코드 | `offline` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary (isSuperAdmin=true) |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 네트워크 복구 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-091 슈퍼대시보드 — 상태: 오프라인

조건: E_091_S_07 — 네트워크 없음

오프라인 배너 (상단):
<div className="bg-amber-50 border-b border-amber-200 px-6 py-2 text-sm text-amber-700">
  네트워크 연결을 확인해 주세요.
</div>

window online 이벤트 감지 → E_091_S_11 복구 → loadSuperDashboard()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_091_S_07`: 네트워크 없음

### 인터랙션 (User Actions)
1. 네트워크 복구 → `E_091_S_11` → 자동 `01-로딩`

### 연결 화면
- 이전: `01-로딩`
- 다음: 복구 시 `01-로딩`
