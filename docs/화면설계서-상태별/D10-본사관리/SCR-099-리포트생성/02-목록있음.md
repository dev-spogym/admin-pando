# SCR-099 리포트생성 — 상태: 목록 있음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-099 |
| 상태 코드 | `normal` |
| 경로 | `src/app/(admin)/reports/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 리포트 상태별 액션 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-099 리포트생성 — 목록 있음

파일: src/app/(admin)/reports/page.tsx

레이아웃:
- AppLayout + PageHeader title="리포트 생성"
- 우측: [리포트 생성] Button → 생성 폼 모달 또는 별도 페이지

[리포트 목록 — DataTable]
columns:
- 리포트명
- 유형 (월간/주간/커스텀 배지)
- 상태:
  · pending: amber 배지 "생성 중" + 진행률 스피너
  · completed: green 배지 "완료" + [PDF 다운로드] 버튼
  · failed: red 배지 "실패" + [재생성] 버튼
- 생성일 (yyyy.MM.dd HH:mm)
- 기간 (yyyy.MM ~ yyyy.MM)
- 액션

[리포트 상태별 행 스타일]
pending: bg-amber-50
completed: bg-white
failed: bg-red-50

데이터:
supabase.from('reports')
  .select('id, title, type, status, created_at, period_start, period_end, file_url')
  .order('created_at', { ascending: false })

[pending 행] 자동 폴링 (5초마다 상태 갱신):
useEffect(() => {
  if (reports.some(r => r.status === 'pending')) {
    const interval = setInterval(loadReports, 5000)
    return () => clearInterval(interval)
  }
}, [reports])

TC-099-006: 완료 상태 리포트 → PDF 다운로드 버튼
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_099_S_02`: 리포트 목록 1건 이상

### 필수 데이터
| 블록 | 테이블 | 비고 |
|------|--------|------|
| 리포트 목록 | `reports` (가상) | created_at desc |

### 리포트 상태 분기 (E_099_S_07~09)
| 상태 | 배지 | 액션 |
|------|------|------|
| pending | amber "생성 중" | 진행률 표시 |
| completed | green "완료" | [PDF 다운로드] |
| failed | red "실패" | [재생성] |

### TC 연결
- TC-099-006: 완료 상태 리포트 → 목록 진입 → PDF 다운로드 버튼 표시

### 인터랙션 (User Actions)
1. [PDF 다운로드] → file_url 다운로드
2. [재생성] → POST /reports/{id}/retry
3. [리포트 생성] → 생성 폼

### 연결 화면
- 이전: `01-로딩`
- 다음: 동일 화면 (폴링으로 상태 갱신)
