# SCR-099 리포트생성 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-099 |
| 상태 코드 | `loading` |
| 경로 | `src/app/(admin)/reports/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-목록있음` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-099 리포트생성 — 로딩

파일: src/app/(admin)/reports/page.tsx

레이아웃:
- AppLayout + PageHeader title="리포트 생성"
- 우측 [리포트 생성] 버튼 (skeleton 또는 표시)

로딩 스켈레톤:
- 리포트 목록 skeleton (5행):
  · 각 행: h-16 bg-gray-100 rounded-lg animate-pulse

E_099_S_01: 리포트 목록 로딩 중
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_099_S_01`: 리포트 목록 API 호출 중

### 비즈니스 룰
- primary, owner, manager 접근 가능
- fc, staff → 권한없음 처리

### 연결 화면
- 이전: 진입
- 다음: `02-목록있음` / `03-목록없음` / `04-에러`
