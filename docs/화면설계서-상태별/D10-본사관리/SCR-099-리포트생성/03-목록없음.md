# SCR-099 리포트생성 — 상태: 목록 없음 (빈 상태)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-099 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/reports/page.tsx` |
| 역할 | primary, owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 리포트 생성 → `02-목록있음` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-099 리포트생성 — 빈 상태

조건: E_099_S_03 — 생성된 리포트 0건

빈 상태 UI:
<div className="flex flex-col items-center justify-center h-80 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-4 mt-6">
  <div className="text-4xl">📊</div>
  <p className="text-gray-500 font-medium">생성된 리포트가 없습니다.</p>
  <p className="text-sm text-gray-400">첫 리포트를 생성하여 성과를 분석해 보세요.</p>
  <Button onClick={() => setIsCreateOpen(true)} size="sm">
    리포트 생성하기
  </Button>
</div>

TC-099-F6-001: 리포트 없음 → 진입 → 빈 상태 메시지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_099_S_03`: reports 테이블 0건

### TC 연결
- TC-099-F6-001: 리포트 없음 → 진입 → 빈 상태 메시지 표시

### 인터랙션 (User Actions)
1. [리포트 생성하기] → 리포트 생성 폼

### 연결 화면
- 이전: `01-로딩`
- 다음: 생성 후 → `02-목록있음`
