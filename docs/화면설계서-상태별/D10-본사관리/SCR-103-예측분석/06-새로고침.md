# SCR-103 예측분석 — 상태: 새로고침 (필터 변경 중)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-103 |
| 상태 코드 | `refreshing` |
| 경로 | `src/app/(admin)/prediction/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `02-정상` 또는 `03-정상` (필터 변경) |
| 다음 상태 | 완료 → `02` 또는 `03` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-103 예측분석 — 새로고침

조건: E_F6_103_07 — 필터(기간/임계값) 변경으로 데이터 재조회 중

오버레이 스피너 (기존 데이터 유지):
<div className="relative">
  <div className="opacity-40 pointer-events-none">
    {/* 이전 차트 + 목록 */}
  </div>
  <div className="absolute inset-0 flex items-center justify-center bg-white/60">
    <Loader2 className="w-8 h-8 animate-spin text-blue-500" />
  </div>
</div>

E_F6_103_08: 완료 → 새 데이터로 화면 갱신
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_103_07`: 필터 변경으로 API 재호출

### 연결 화면
- 이전: `02-정상-고위험있음` / `03-정상-고위험없음`
- 다음: `E_F6_103_08` 완료 → 결과에 따라 `02` 또는 `03`
