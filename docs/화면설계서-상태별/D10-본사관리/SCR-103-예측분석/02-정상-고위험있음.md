# SCR-103 예측분석 — 상태: 정상 (고위험 회원 있음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-103 |
| 상태 코드 | `high-risk` |
| 경로 | `src/app/(admin)/prediction/page.tsx` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 필터 변경 → `06-새로고침` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-103 예측분석 — 고위험 회원 있음

파일: src/app/(admin)/prediction/page.tsx

조건: E_F6_103_05 — highRiskMembers.length > 0

레이아웃:
- AppLayout + PageHeader title="예측 분석"

[고위험 배너 — 최상단]
<div className="bg-red-50 border border-red-300 rounded-xl px-5 py-4 flex items-center gap-3 mb-4">
  <span className="text-2xl">⚠️</span>
  <div>
    <p className="font-semibold text-red-800">즉시 조치가 필요한 회원 {highRiskCount}명</p>
    <p className="text-sm text-red-600">이탈 위험도가 높습니다. 빠른 연락을 권장합니다.</p>
  </div>
  <Button size="sm" className="ml-auto bg-red-600 hover:bg-red-700 text-white">
    연락하기
  </Button>
</div>

[예측 차트]
- 회원 이탈 확률 분포 바 차트 (위험도별 구간)
- X축: 이탈 확률 (0~100%), Y축: 회원 수

[위험 회원 목록]
- DataTable: 이름 / 마지막 방문 / 잔여 이용권 / 위험도 점수 / 액션
- 위험도 색상: 80%+ = red, 50~79% = orange, <50% = yellow
- [연락] 버튼 → 해당 회원 연락 화면

[필터]
- 기간 선택 (7일 / 30일 / 90일)
- 위험도 임계값 슬라이더 (기본 50%)

데이터:
supabase.from('members')
  .select('id, name, last_visit, risk_score')
  .gte('risk_score', 0.5)
  .order('risk_score', { ascending: false })
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_F6_103_05`: risk_score >= 0.5 회원 존재

### 인터랙션 (User Actions)
1. [연락하기] → 해당 회원 상세 (연락 기능)
2. 필터 변경 → `E_F6_103_07` → `06-새로고침`
3. 위험도 슬라이더 → 목록 필터링

### 연결 화면
- 이전: `01-로딩`
- 다음: 필터 변경 → `06-새로고침`
