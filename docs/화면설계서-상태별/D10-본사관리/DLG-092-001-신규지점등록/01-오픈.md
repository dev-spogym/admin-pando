# DLG-092-001 신규지점등록 — 상태: 오픈 (초기화)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-092-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-092 [신규 지점 등록] 버튼 |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | SCR-092 정상 |
| 다음 상태 | `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-092-001 신규 지점 등록 — 오픈 상태 (빈 폼 초기화)

E_D92_01_M1_01: [신규 지점 등록] 클릭 → 다이얼로그 오픈

Modal 레이아웃 (max-w-2xl):
- 헤더: "신규 지점 등록" + [X] 닫기 버튼
- 본문: 9개 필드 폼 (2열 grid)
- 푸터: [취소] [저장] 버튼

9개 필드 (초기값 모두 빈 값):
1. 지점명 * — Input (placeholder="예: 강남점")
2. 지점코드 — Input disabled (자동생성: BR-YYYYMM-NNN, bg-gray-50)
3. 지역 * — Select (서울/경기/부산/대구/인천/광주/대전/울산/기타)
4. 주소 * — Input (placeholder="도로명 주소")
5. 전화번호 — Input (placeholder="000-0000-0000")
6. 오픈일 * — DatePicker
7. 센터장 — Input (직원 검색, placeholder="센터장 이름 검색")
8. 정원 * — Input type="number" (placeholder="최대 회원 수")
9. 상태 — Select (기본값: active)

지점코드 자동생성 로직:
const branchCode = `BR-${format(new Date(), 'yyyyMM')}-${String(nextSeq).padStart(3, '0')}`

[저장] 버튼: disabled (모든 필수 필드 미입력 상태)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D92_01_M1_01`: SCR-092 [신규 지점 등록] 클릭

### 필드 검증 규칙 (M2 기반)
| 필드 | 규칙 | 에러 메시지 |
|------|------|------------|
| 지점명 | 필수, 최대 50자 | "지점명은 필수입니다." |
| 지역 | 필수 | "지역을 선택하세요." |
| 주소 | 필수 | "주소는 필수입니다." |
| 전화번호 | 선택, 형식 체크 | "올바른 전화번호 형식으로 입력하세요." |
| 오픈일 | 필수 | "오픈일을 선택하세요." |
| 정원 | 필수, 양수 | "1 이상의 값을 입력하세요." |

### 인터랙션 (User Actions)
1. 필드 입력 → `02-입력중`
2. [취소] / ESC / 외부 클릭 → `E_D92_01_M1_07~08` → 다이얼로그 닫힘

### 연결 화면
- 이전: SCR-092 정상
- 다음: `02-입력중` / 취소 → SCR-092
