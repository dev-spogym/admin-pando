# SCR-090 본사대시보드 — 상태: 빈 상태 (회원 0명 / 매출 0원)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-090 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/super-dashboard/page.tsx` |
| 역할 | primary, owner, manager, fc, staff, readonly |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 회원 등록 후 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-090 본사대시보드 — 상태: 빈 상태 (데이터 없음)

조건: API 로드 성공이지만 모든 집계값이 0

레이아웃:
- AppLayout + PageHeader title="본사 대시보드"

[StatCardGrid — 4개 카드 모두 0 표시]
- 총 회원 수: 0
- 오늘 신규: 0
- 이번달 매출: 0원
- 이번달 PT건수: 0건
- trend 배지 미표시 (변화량 없음)

[빈 위젯 영역]
- 차트 자리에 빈 상태 컴포넌트:
  <div className="flex flex-col items-center justify-center h-64 bg-gray-50 rounded-xl border border-dashed border-gray-300">
    <p className="text-gray-400 text-sm">표시할 데이터가 없습니다</p>
  </div>

[DataTable 빈 상태]
- "등록된 지점이 없습니다." 메시지
- [지점 등록하기] 버튼 → /branches/new (또는 SCR-092)

[공지 위젯]
- 빈 상태: "등록된 공지가 없습니다."

조건 분기:
if (kpiData.totalMembers === 0 && kpiData.monthlyRevenue === 0) {
  // 빈 상태 렌더링
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_090_ST_03`: API 로드 성공이지만 회원 0명 / 매출 0원
- 신규 지점 개설 직후, 아직 회원 등록 전

### 필수 데이터
- kpi_snapshots: 모든 집계값 0
- branches: 없거나 active 지점 없음

### 인터랙션 (User Actions)
1. [지점 등록하기] → SCR-092
2. StatCard 클릭 → 해당 관리 화면 (빈 상태에서도 이동 가능)

### 비즈니스 룰
- 0값 카드도 정상 렌더링 (숨김 처리 안 함)
- 신규 설치 온보딩 CTA 배너 표시 권장

### TC 연결
- TC-090-009: 회원 0명 → 페이지 진입 → 빈 상태 위젯 표시

### 연결 화면
- 이전: `01-로딩`
- 다음: 회원/지점 등록 후 `02-정상`
