# DLG-094-001 매출목표설정 — 상태: 검증 에러

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-094-001 |
| 상태 코드 | `validation-error` |
| 트리거 | [저장] 클릭 시 유효성 실패 |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 필드 수정 후 → `03-저장성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-094-001 매출 목표 설정 — 검증 에러

조건: E_D94_01_M1_06 — 유효성 검사 실패

인라인 에러 표시:
각 필드 하단 <p className="text-xs text-red-500 mt-1">{error}</p>

에러 케이스 (M2 기반):
- 연도 미선택: "연도를 선택하세요."
- 월 미선택: "월을 선택하세요."
- 목표금액 미입력: "목표 금액을 입력하세요."
- 목표금액 0 이하: "1 이상의 값을 입력하세요."
- 목표금액 숫자 아님: "숫자만 입력 가능합니다."
- 메모 200자 초과: "200자 이내로 입력하세요."

react-hook-form:
const { register, handleSubmit, formState: { errors }, watch } = useForm({
  defaultValues: { year: currentYear, month: currentMonth, amount: '', memo: '' }
})

TC-094-DLG-002: 목표금액 미입력 → 저장 → "목표 금액을 입력하세요."
TC-094-DLG-003: 목표금액 0 → 저장 → "1 이상의 값을 입력하세요."
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D94_01_M1_06`: 유효성 검사 실패

### TC 연결
- TC-094-DLG-002: 목표금액 미입력 → 인라인 에러
- TC-094-DLG-003: 목표금액 0 → 인라인 에러

### 연결 화면
- 이전: `01-오픈`
- 다음: 수정 후 → `03-저장성공`
