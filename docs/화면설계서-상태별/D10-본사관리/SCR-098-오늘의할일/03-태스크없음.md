# SCR-098 오늘의할일 — 상태: 태스크 없음 (빈 상태)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-098 |
| 상태 코드 | `empty` |
| 경로 | `src/app/(admin)/tasks/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 태스크 추가 → DLG-098-001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-098 오늘의할일 — 빈 상태

조건: E_098_S_03 — today_tasks 0건

레이아웃:
- AppLayout + PageHeader title="오늘의 할일"
- [+ 태스크 추가] 버튼 유지

[StatCardGrid — 4개 카드 모두 0]

[빈 상태 영역]
<div className="flex flex-col items-center justify-center h-64 bg-gray-50 rounded-xl border border-dashed border-gray-300 gap-4 mt-4">
  <div className="text-4xl">✅</div>
  <p className="text-gray-500 font-medium">오늘 할일이 없습니다.</p>
  <p className="text-sm text-gray-400">새 태스크를 추가하여 업무를 관리하세요.</p>
  <Button onClick={() => setIsAddOpen(true)} size="sm">
    태스크 추가하기
  </Button>
</div>

TC-098-F6-001: 태스크 없음 → 진입 → 빈 상태 메시지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_098_S_03`: today_tasks 0건

### TC 연결
- TC-098-F6-001: 태스크 없음 → 진입 → 빈 상태 메시지

### 인터랙션 (User Actions)
1. [태스크 추가하기] → DLG-098-001

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-098-001 → 등록 성공 → `02-태스크있음`
