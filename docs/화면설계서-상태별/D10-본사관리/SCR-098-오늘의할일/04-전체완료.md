# SCR-098 오늘의할일 — 상태: 전체 완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-098 |
| 상태 코드 | `all-done` |
| 경로 | `src/app/(admin)/tasks/page.tsx` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `02-태스크있음` (마지막 태스크 완료 후) |
| 다음 상태 | 새 태스크 추가 → `02-태스크있음` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-098 오늘의할일 — 전체 완료 상태

조건: E_098_S_04 — 모든 태스크 status='completed'

레이아웃:
- AppLayout + PageHeader title="오늘의 할일"
- [+ 태스크 추가] 버튼 유지

[StatCardGrid — 4개 카드]
- 전체: N, 대기: 0, 진행중: 0, 완료: N (전부 완료)

[완료 축하 배너]
<div className="flex flex-col items-center justify-center h-64 bg-green-50 rounded-xl border border-green-200 gap-4 mt-4">
  <div className="text-5xl">🎉</div>
  <p className="text-green-800 font-semibold text-lg">오늘 할일을 모두 완료했습니다!</p>
  <p className="text-sm text-green-600">수고하셨습니다. 내일 할일을 미리 추가해 보세요.</p>
</div>

[완료된 태스크 목록 — 축소 표시]
- 전체 목록을 collapsed 상태로 표시 (기본 접힘)
- [완료 목록 보기] 토글 버튼 → 펼치기/접기

toast.success("오늘 할일을 모두 완료했습니다!") — DLG-098-002 M3에서 발사
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_098_S_04`: tasks.every(t => t.status === 'completed')

### 인터랙션 (User Actions)
1. [+ 태스크 추가] → DLG-098-001
2. [완료 목록 보기] → 토글 펼치기

### 연결 화면
- 이전: `02-태스크있음` (마지막 완료 처리)
- 다음: 새 태스크 추가 → `02-태스크있음`
