# DLG-098-001 태스크추가 — 상태: 오픈 (빈 폼)

## 메타

| 항목 | 값 |
|------|----|
| 다이얼로그 ID | DLG-098-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-098 [+ 태스크 추가] 버튼 클릭 |
| 역할 | primary, owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-098 정상 |
| 다음 상태 | 입력 → `02-검증에러` 또는 `03-저장성공` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
다이얼로그: DLG-098-001 태스크 추가 — 오픈 (빈 폼)

E_D98_01_M1_01: [+ 태스크 추가] 클릭 → 다이얼로그 오픈

Modal 레이아웃 (max-w-lg):
- 헤더: "태스크 추가" + [X]

[폼 필드 6개]
1. 제목 * — Input (placeholder="태스크 제목을 입력하세요", maxLength=100)
   우측 글자수: {titleLength}/100
2. 카테고리 * — Select
   옵션: 회원관리 / 매출관리 / 시설관리 / 인사관리 / 마케팅 / 기타
3. 우선순위 * — RadioGroup (horizontal)
   [🔴 높음] [🟡 보통] [🟢 낮음]
   기본값: medium
4. 마감일 — DatePicker (오늘 이후 날짜만 선택 가능)
5. 담당자 — Combobox (직원 검색, placeholder="담당자 검색")
6. 메모 — Textarea (rows=3, maxLength=500, placeholder="상세 내용을 입력하세요")

[저장] 버튼: 제목 + 카테고리 + 우선순위 입력 시 활성화
[취소] 버튼 (variant="outline")

초기값:
- 우선순위: 'medium' (기본 선택)
- 나머지 빈 값

E_D98_01_M1_07~08: [취소] / ESC → 닫힘
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `E_D98_01_M1_01`: SCR-098 [+ 태스크 추가] 클릭

### 필드 검증 규칙 (M2)
| 필드 | 규칙 | 에러 메시지 |
|------|------|------------|
| 제목 | 필수, 100자 이하 | "제목은 필수입니다." / "100자 이내" |
| 카테고리 | 필수 | "카테고리를 선택하세요." |
| 우선순위 | 필수 | "우선순위를 선택하세요." |
| 마감일 | 선택, 오늘 이후 | "오늘 이후 날짜를 선택하세요." |
| 메모 | 선택, 500자 | "500자 이내로 입력하세요." |

### 인터랙션 (User Actions)
1. 필드 입력 → 실시간 유효성 체크
2. [저장] → `03-저장성공` / 에러 → `02-검증에러`
3. [취소] / ESC → 닫힘

### 연결 화면
- 이전: SCR-098 정상
- 다음: `02-검증에러` / `03-저장성공` / 취소 → SCR-098
