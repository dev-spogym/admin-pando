# SCR-M002 회원등록 — 상태: 등록 완료

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M002 |
| 상태 코드 | `done` |
| 경로 | `/members/new` → `/members/detail?id={id}` |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M002_회원등록/F6_상태별.md` |
| 이전 상태 | `07-저장중` |
| 다음 상태 | SCR-M004 (회원 상세) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M002 회원등록 — 상태: 등록 완료 (성공 후 리다이렉트)

파일: src/app/members/new/page.tsx

완료 처리:
- API 200 응답 후 즉시 실행:
  1. toast.success("회원이 등록되었습니다.")
  2. router.push(`/members/detail?id=${newMember.id}`)
  3. 이 화면 자체는 표시되지 않음 (리다이렉트)

선택적 완료 화면 (리다이렉트 전 짧게 표시):
<div className="flex flex-col items-center justify-center min-h-[60vh]">
  <CheckCircle className="w-16 h-16 text-green-500 mb-4 animate-in zoom-in" />
  <p className="text-xl font-bold text-gray-700">회원 등록 완료!</p>
  <p className="mt-2 text-sm text-gray-400">회원 상세 페이지로 이동합니다...</p>
</div>

인터랙션: 자동 리다이렉트 (setTimeout 500ms)

사용 유틸: router, toast, lucide-react (CheckCircle)
```

## 📝 디스크립션

### 사용 시점
- POST /api/members 200 응답 직후

### 필수 데이터
| 항목 | 상태 |
|------|------|
| newMember.id | 신규 생성된 회원 ID |

### 인터랙션
1. toast.success 표시
2. 자동 리다이렉트 → `/members/detail?id={id}`

### 비즈니스 룰
- 등록 완료 후 회원 상세 페이지에서 이용권 등록 유도
- 폼 데이터는 완료 후 초기화

### 에지 케이스
- 리다이렉트 실패 시: toast.success + 수동 링크 제공

### 접근성
- `aria-live="assertive"` 완료 메시지

### 연결 화면
- SCR-M004 (회원 상세)

### 다이어그램 참조
- 엣지: `E_SAVE_OK_01` → 완료 → 상세 이동
- 상태: `STATE_DONE`
