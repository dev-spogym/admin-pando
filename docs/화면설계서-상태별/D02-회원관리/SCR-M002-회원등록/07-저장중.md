# SCR-M002 회원등록 — 상태: 저장 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M002 |
| 상태 코드 | `saving` |
| 경로 | `/members/new` |
| 역할 | primary / owner / manager / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M002_회원등록/F6_상태별.md` |
| 이전 상태 | `06-Step2` |
| 다음 상태 | `08-등록완료` / `06-Step2` (실패) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M002 회원등록 — 상태: 저장 중 (isSubmitting=true)

파일: src/app/members/new/page.tsx

컴포넌트 구조:
- FormCard: 모든 필드 disabled (pointer-events-none opacity-60)
- BottomBar:
  - 이전 Button: disabled
  - 저장 Button: disabled + Loader2 스피너
    <Button disabled className="gap-2">
      <Loader2 className="w-4 h-4 animate-spin" />
      저장 중...
    </Button>

API 호출:
- POST /api/members → members 테이블 INSERT
- 성공 시: router.push(`/members/detail?id=${newMember.id}`) → `08-등록완료`
- 실패 시: toast.error("회원 등록에 실패했습니다.") → Step2 복귀

인터랙션:
- 저장 중 모든 인터랙션 차단
- 완료/실패 후 자동 상태 전환

사용 유틸: supabase, toast, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점
- Step2 "저장" 버튼 클릭 후 isSubmitting=true 상태

### 필수 데이터
| 항목 | 상태 |
|------|------|
| isSubmitting | true |
| formData | Step1 + Step2 전체 |

### 인터랙션
- 모든 입력/버튼 비활성 (중복 제출 방지)
- API 완료 시 자동 전환

### 비즈니스 룰
- 저장 버튼 더블클릭 방지 (isSubmitting 체크)
- 실패 시 폼 데이터 보존 (재시도 가능)

### 에지 케이스
- 저장 중 브라우저 뒤로 가기: `beforeunload` 경고
- 타임아웃 (30초): 실패 처리

### 접근성
- 폼 영역 `aria-busy="true"`

### 연결 화면
- 성공: `08-등록완료` → SCR-M004 (회원 상세)
- 실패: `06-Step2`

### 다이어그램 참조
- 엣지: `E_SAVE_START_01` → `E_SAVE_OK_01` / `E_SAVE_FAIL_01`
- 상태: `STATE_SAVING`
