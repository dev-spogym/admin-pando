# SCR-M006 체성분관리 — 상태: 1건 (현재값만)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M006 |
| 상태 코드 | `single-record` |
| 경로 | `/body-composition?memberId={id}` |
| 역할 | primary / owner / manager / fc |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M006_체성분관리/F6_상태별화면.md` |
| 이전 상태 | `01-로딩중` |
| 다음 상태 | `04-전체` (2번째 측정 추가 후) |

## 🧩 바이브코딩 프롬프트
```
화면: SCR-M006 체성분관리 — 상태: 1건 (차트 미표시)

컴포넌트:
- StatCardGrid: 현재값 표시 (change 없음 — 비교 대상 없음)
  <StatCard label="체중" value={latest.weight} unit="kg" change={null} />
- 차트 영역:
  <div className="flex items-center justify-center h-48 bg-gray-50 rounded-lg">
    <p className="text-xs text-gray-400">차트 표시를 위해 최소 2개 이상의 측정 데이터가 필요합니다.</p>
  </div>
- DataTable: 1행 표시
- "측정 추가" 버튼

인터랙션: "측정 추가" → DLG-M015
```

## 📝 디스크립션

### 사용 시점
- body_compositions 데이터 정확히 1건

### 비즈니스 룰
- change 값 없음 (비교 불가)
- 차트 최소 2건 필요 안내

### 다이어그램 참조
- 엣지: `E_COUNT_ONE_01`
- 상태: `UI_ONE`
