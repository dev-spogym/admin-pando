# SCR-M006 체성분관리 — 상태: 데이터 없음 (0건)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M006 |
| 상태 코드 | `empty-zero` |
| 경로 | `/body-composition?memberId={id}` |
| 역할 | primary / owner / manager / fc |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M006_체성분관리/F6_상태별화면.md` |
| 이전 상태 | `01-로딩중` |
| 다음 상태 | `04-전체` (측정 추가 후) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M006 체성분관리 — 상태: 0건 (측정 데이터 없음)

파일: src/app/body-composition/page.tsx

컴포넌트:
- StatCardGrid: 4개 카드 모두 값 "-" 표시
  <StatCard label="체중" value="-" unit="kg" />
  <StatCard label="골격근량" value="-" unit="kg" />
  <StatCard label="체지방률" value="-" unit="%" />
  <StatCard label="BMI" value="-" />
- 차트 영역: 빈 상태 메시지
  <div className="flex items-center justify-center h-48 bg-gray-50 rounded-lg">
    <p className="text-sm text-gray-400">측정 데이터가 없습니다.</p>
  </div>
- DataTable: 빈 상태
- FAB or Button: "측정 추가" → DLG-M015

인터랙션:
- "측정 추가" 클릭 → DLG-M015

사용 유틸: useAuthStore, supabase, lucide-react (PlusCircle)
```

## 📝 디스크립션

### 사용 시점
- body_compositions 데이터 0건

### 필수 데이터
- bodyCompositions: [] (빈 배열)

### 인터랙션
- "측정 추가" → DLG-M015

### 비즈니스 룰
- 통계 카드 하이픈(-) 표시
- 차트 미표시

### 다이어그램 참조
- 엣지: `E_COUNT_ZERO_01`
- 상태: `UI_ZERO`
