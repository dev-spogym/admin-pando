# DLG-M003 홀딩등록 — 상태: 성공 / 실패

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-M003 |
| 상태 코드 | `done` |
| 역할 | primary / owner / manager |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/DLG/DLG-M003_홀딩등록/M1_생명주기.md` |

## 🧩 바이브코딩 프롬프트
```
DLG-M003 홀딩등록 — 결과 처리

성공: toast.success("홀딩 처리가 완료되었습니다") → 모달 닫기 + refetch
실패: toast.error("홀딩 처리에 실패했습니다") → 모달 유지 (재시도)

API: POST /api/members/:id/holding
```

## 📝 디스크립션
### 사용 시점
API 응답 완료.

### 비즈니스 룰
- 성공: 닫힘 + 상위 화면 갱신
- 실패: 모달 유지, toast.error

### 다이어그램 참조
- 엣지: `E_SUCCESS_01` / `E_FAIL_01`
