# SCR-M005 회원이관 — 상태: 차단 (이관 불가)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-M005 |
| 상태 코드 | `blocked` |
| 경로 | `/members/transfer?id={id}` |
| 역할 | primary / owner |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D02_회원관리/SCR-M005_회원이관/F6_상태별화면.md` |
| 이전 상태 | `02-체크리스트로딩` |
| 다음 상태 | (없음 — 이관 불가) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 'use client' 컴포넌트 작성.
화면: SCR-M005 회원이관 — 상태: 차단 (blockers 존재)

파일: src/app/members/transfer/page.tsx

컴포넌트:
- 빨간 배너:
  <div className="rounded-lg border border-red-200 bg-red-50 p-4">
    <p className="text-sm font-medium text-red-700">이관을 진행할 수 없습니다.</p>
    <ul className="mt-2 space-y-1">
      {blockers.map(b => (
        <li key={b.id} className="flex items-center gap-2 text-xs text-red-600">
          <XCircle className="w-4 h-4" /> {b.message}
        </li>
      ))}
    </ul>
  </div>
- 이관 설정 폼: 완전 disabled
- "이관 실행" 버튼: disabled

사용 유틸: supabase, lucide-react (XCircle)
```

## 📝 디스크립션

### 사용 시점
- 체크리스트 결과 blockers.length > 0

### 비즈니스 룰
- 미납 결제, 진행 중 홀딩 등 블로커 항목 표시
- 이관 폼 전체 비활성

### 다이어그램 참조
- 엣지: `E_STATE_CHECK_BLOCK_01`
- 상태: `UI_BLOCK`
