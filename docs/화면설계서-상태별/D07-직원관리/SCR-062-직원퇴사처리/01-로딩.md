# SCR-062 직원퇴사처리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-062 |
| 상태 코드 | `loading` |
| 경로 | `/staff/resignation` |
| 역할 | primary / owner / manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-060 "퇴사 처리" 버튼 클릭 또는 직접 진입 |
| 다음 상태 | 로드 성공 → `02-Step1`, 실패 → `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-062 직원 퇴사 처리 — 상태: 로딩

파일: src/app/staff/resignation/page.tsx

조건: isLoadingStaff=true (페이지 진입 직후 재직 중 직원 목록 fetch 중)

레이아웃:
- AppLayout + PageHeader: title="직원 퇴사 처리"
- 위자드 진행 표시 (Step 1~4):
  [1.직원 선택] — [2.회원 이관] — [3.스케줄 확인] — [4.최종 확인]
  Step 1 active (현재), 나머지 gray

로딩 UI:
- Select 컴포넌트 (직원 선택) disabled + skeleton 표시
- "다음" 버튼 disabled
- 중앙 스피너 (animate-spin w-6 h-6 text-blue-500)

fetch:
  const { data: activeStaff } = await supabase
    .from('staff')
    .select('id, name, department, position, role')
    .in('staff_status', ['ACTIVE', 'ON_LEAVE'])
    .order('name')

상태 변수:
- isLoadingStaff: true
- activeStaffList: []
- selectedStaffId: null

사용 컴포넌트:
- AppLayout, PageHeader, Select, Button
- useAuthStore, supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/staff/resignation` 진입 즉시
- ACTIVE / ON_LEAVE 직원 목록 fetch 중

### 인터랙션 (User Actions)
- 없음 (로딩 중 모든 액션 비활성)

### 비즈니스 룰
- RESIGNED / TRANSFERRED / LOCKED 직원은 Select 목록에서 제외
- 진입 권한: `hasPermission('staff:resign')` — primary/owner/manager

### 에지 케이스
- 재직 중 직원 없음 → `02-Step1` 빈 상태 (빈 Select)
- fetch 실패 → `06-에러` 전환

### 접근성 (A11y)
- 스피너 `role="status"` + `aria-label="퇴사 처리 대상 직원 불러오는 중"`
- 위자드 스텝 `aria-current="step"` (Step 1)

### 연결 화면
- 다음: `02-Step1` / `06-에러`
