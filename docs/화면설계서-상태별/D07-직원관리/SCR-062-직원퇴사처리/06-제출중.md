# SCR-062 직원퇴사처리 — 상태: 제출 중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-062 |
| 상태 코드 | `submitting` |
| 경로 | `/staff/resignation` |
| 역할 | primary / owner / manager |
| 우선순위 | P0 |
| 이전 상태 | `05-Step4` "퇴사 처리 확정" 클릭 |
| 다음 상태 | 성공 → `07-완료`, 실패 → `05-Step4` 복귀 |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-062 직원 퇴사 처리 — 상태: 제출 중 (isSubmitting=true)

파일: src/app/staff/resignation/page.tsx

조건: isSubmitting === true

UI 변화:
1. "퇴사 처리 확정" 버튼:
   <Button variant="danger" disabled className="opacity-70">
     <Spinner className="mr-2 h-4 w-4 animate-spin" />
     처리 중...
   </Button>

2. "이전" 버튼: disabled

3. 페이지 전체 오버레이 (처리 중 이탈 방지):
   <div className="fixed inset-0 bg-black/20 z-40 flex items-center justify-center">
     <div className="bg-white rounded-xl p-8 text-center shadow-xl">
       <Spinner className="mx-auto h-8 w-8 animate-spin text-blue-500" />
       <p className="mt-4 font-medium text-gray-700">퇴사 처리 중입니다...</p>
       <p className="mt-1 text-sm text-gray-500">잠시만 기다려주세요.</p>
     </div>
   </div>

API 호출 순서:
  1. PATCH staff → staff_status: 'RESIGNED', resign_date, resign_reason
  2. PATCH members → assigned_fc_id (회원 이관, FC인 경우)
  3. DELETE/UPDATE staff_attendance (스케줄 처리)
  4. 모두 성공 → toast.success + moveToPage('/staff')
  5. 부분 실패 → rollback 또는 오류 보고

상태 변수:
- isSubmitting: true
- 모든 버튼/입력 disabled
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Step 4 "퇴사 처리 확정" 클릭 직후
- Supabase 복수 API 호출 진행 중

### 인터랙션 (User Actions)
- 없음 (모든 액션 비활성, 이탈 불가)
- 브라우저 탭 닫기: OS 수준 경고 (beforeunload 이벤트)

### 비즈니스 룰
- 퇴사 처리는 복수 테이블 업데이트 — 순차 처리
- 실패 시 이미 변경된 데이터 롤백 불가 → 에러 시 지원팀 안내
- 처리 시간 예상: 회원 이관 건수에 따라 1~5초

### 에지 케이스
- API 타임아웃 15초: toast.error + isSubmitting=false + Step 4 복귀
- 부분 성공(직원 RESIGNED, 회원 이관 실패): toast.warning + 수동 이관 안내

### 접근성 (A11y)
- 오버레이 `role="dialog"` + `aria-modal="true"` + `aria-label="퇴사 처리 중"`
- `beforeunload` 이벤트로 이탈 방지

### 연결 화면
- 이전: `05-Step4`
- 다음: `07-완료` / 실패 시 `05-Step4`
