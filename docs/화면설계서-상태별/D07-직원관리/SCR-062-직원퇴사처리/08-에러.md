# SCR-062 직원퇴사처리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-062 |
| 상태 코드 | `error` |
| 경로 | `/staff/resignation` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (데이터 로드 실패) 또는 `06-제출중` (API 실패) |
| 다음 상태 | 목록 복귀 또는 Step 4 재시도 |

## 🧩 바이브코딩 프롬프트

```
에러 처리:

로드 에러 (activeStaffList fetch 실패):
  toast.error('직원 목록을 불러오지 못했습니다.')
  moveToPage('/staff')

제출 에러 (API 실패):
  setIsSubmitting(false)
  toast.error('퇴사 처리에 실패했습니다. 다시 시도해주세요.')
  // Step 4 유지, 재시도 가능

제출 에러 UI (Step 4 상단):
  <div className="rounded-md bg-red-50 border border-red-200 p-4 mb-4" role="alert">
    <p className="text-sm font-medium text-red-700">퇴사 처리 중 오류가 발생했습니다.</p>
    <p className="text-xs text-red-500 mt-1">
      일부 처리가 완료되었을 수 있습니다. 직원 목록을 확인 후 재시도하세요.
    </p>
    <Button variant="outline" size="sm" className="mt-2" onClick={() => moveToPage('/staff')}>
      직원 목록 확인
    </Button>
  </div>
```

## 📝 디스크립션

### 에러 유형
| 유형 | 발생 시점 | 처리 |
|------|-----------|------|
| 로드 에러 | Step 1 진입 시 fetch 실패 | toast + 목록 복귀 |
| 제출 에러 | Step 4 확정 후 API 실패 | toast + Step 4 유지 |
| 권한 에러(403) | 제출 시 권한 없음 | toast.error('권한 없음') + 목록 복귀 |

### 연결 화면
- 이전: `01-로딩` / `06-제출중`
- 다음: 목록 복귀 / `05-Step4` 재시도
