# SCR-064 급여관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-064 |
| 상태 코드 | `empty` |
| 경로 | `/payroll` |
| 역할 | primary / owner / manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (해당 월 급여 데이터 없음) |
| 다음 상태 | 월 변경 → `01-로딩` 재시작 |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-064 급여 관리 — 상태: 빈 상태 (해당 월 급여 데이터 없음)

파일: src/app/payroll/page.tsx

조건: payrollList.length === 0 && !isLoadingData && !error

빈 상태 UI:
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <svg ...> (CurrencyWon 아이콘, w-16 h-16 text-gray-300) </svg>
    <p className="mt-4 text-lg font-medium text-gray-500">
      {selectedMonth} 급여 데이터가 없습니다.
    </p>
    <p className="mt-1 text-sm text-gray-400">
      다른 월을 선택하거나 급여 데이터를 입력하세요.
    </p>
    <Button className="mt-6" onClick={() => setCreatePayrollModalOpen(true)}>
      이달 급여 데이터 생성
    </Button>
  </div>

"이달 급여 데이터 생성":
  - 활성 직원 기준으로 기본 급여(staff.salary) 복사하여 payroll 레코드 일괄 생성
  - 생성 후 `02-정상` 전환

"급여 확정" 버튼: disabled (데이터 없음)
통계 카드: 0원 / 0명 / 0명 표시

사용 컴포넌트:
- AppLayout, PageHeader, Button
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- fetch 완료 후 `payrollList.length === 0`
- 해당 월에 급여 레코드 미생성 상태

### 인터랙션 (User Actions)
1. MonthPicker → 다른 월 선택 (데이터 있는 월)
2. "이달 급여 데이터 생성" → 활성 직원 기준 일괄 생성

### 비즈니스 룰
- 급여 데이터 생성: `hasPermission('payroll:create')` — primary/owner만
- manager: 빈 상태 메시지만 확인, 생성 버튼 hidden

### 연결 화면
- 이전: `01-로딩`
- 다음: 생성 완료 → `02-정상-미확정있음`
