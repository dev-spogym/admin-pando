# SCR-064 급여관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-064 |
| 상태 코드 | `loading` |
| 경로 | `/payroll` |
| 역할 | primary / owner / manager (hasPermission('payroll:view') 필수) |
| 우선순위 | P0 |
| 이전 상태 | 진입 시도 (권한 확인 통과) |
| 다음 상태 | 성공+데이터있음 → `02-정상`, 없음 → `03-빈상태`, 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-064 급여 관리 — 상태: 로딩

파일: src/app/payroll/page.tsx

권한 체크 (최우선):
  const { user, role } = useAuthStore()
  if (!hasPermission('payroll:view')) {
    return <PermissionDenied />  // 권한없음 UI
  }
  // 허용 역할: primary(본사) / owner(센터장) / manager(매니저)

조건: isLoadingData=true

레이아웃:
- AppLayout + PageHeader: title="급여 관리"
- 데스크톱 전용

상단:
- MonthPicker: 이번 달 기본값, disabled
- "급여 확정" 버튼: disabled
- "급여 명세서" 링크 버튼: disabled

통계 카드 (3개) — 스켈레톤:
  총 급여액 / 확정 인원 / 미확정 인원

DataTable 스켈레톤 (5행, animate-pulse)

fetch:
  Promise.all([
    supabase.from('payroll').select('*, staff(name, department, position)')
      .eq('year_month', selectedMonth)
      .order('staff_id'),
  ])

상태 변수:
- isLoadingData: true
- selectedMonth: format(new Date(), 'yyyy-MM')
- payrollList: []

사용 컴포넌트:
- AppLayout, PageHeader, Button
- useAuthStore, hasPermission, supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/payroll` 진입 직후 (권한 통과 후)
- MonthPicker 변경 후 재fetch 중

### 권한 매트릭스
| 역할 | payroll:view |
|------|-------------|
| primary (본사) | O |
| owner (센터장) | O |
| manager (매니저) | O |
| fc | X |
| staff | X |

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `05-에러`
