# SCR-060 직원목록 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-060 |
| 상태 코드 | `loading` |
| 경로 | `/staff` |
| 역할 | primary / owner / manager |
| 우선순위 | P0 |
| 이전 상태 | 권한 확인 통과 직후 |
| 다음 상태 | 데이터 있음 → `02-정상`, 없음 → `03-빈상태`, 오류 → `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 14 App Router + TypeScript + Tailwind CSS 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-060 직원목록 — 상태: 로딩 (isLoadingData=true)

파일: src/app/staff/page.tsx

레이아웃:
- AppLayout 내부, PageHeader 포함 (title="직원 관리")
- 데스크톱 전용 (min-w-[1024px])

로딩 표시:
- 페이지 중앙 스피너 (animate-spin w-8 h-8 text-blue-500)
- DataTable 영역에 스켈레톤 rows 5개 표시
  · 각 row: h-10 bg-gray-100 rounded animate-pulse
- SearchFilter, TabNav 비활성(opacity-50 pointer-events-none)
- 액션 버튼(메시지/상태변경/퇴사) disabled 상태

상태 변수:
- isLoadingData: true
- staffList: []
- selectedRows: Set<string>()

데이터 fetch:
- useEffect(() => { fetchStaffList() }, [])
- fetchStaffList: supabase.from('staff').select('*').order('created_at', { ascending: false })
- isLoadingData → false 후 결과 분기

사용 컴포넌트:
- AppLayout, PageHeader, DataTable(skeleton), SearchFilter, TabNav, Button
- useAuthStore: role 확인 (primary/owner/manager만 진입 허용)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-060 진입 직후 권한 확인 통과 → staff 테이블 데이터 fetch 시작
- `isLoadingData === true` 구간 전체

### 필수 데이터
| 항목 | 값 |
|------|----|
| isLoadingData | true |
| staffList | [] (초기) |
| selectedRows | Set() 비어있음 |

### 인터랙션 (User Actions)
- 없음 (로딩 중 모든 액션 비활성)
- ESC키, 뒤로가기는 가능 (라우터 수준)

### 비즈니스 룰
- 권한 없는 역할(FC/스태프)은 이 상태 도달 불가 → `05-권한없음`으로 분기
- 로딩 타임아웃 10초 초과 시 자동으로 `04-에러` 상태 전환

### 에지 케이스
- 네트워크 매우 느림: 스켈레톤 장시간 노출 → 타임아웃 처리
- 브라우저 탭 전환 후 복귀: 재fetch 없이 기존 상태 유지

### 접근성 (A11y)
- 스피너에 `role="status"` + `aria-label="직원 목록 불러오는 중"`
- 스켈레톤 영역 `aria-hidden="true"`

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러` / `06-오프라인`
