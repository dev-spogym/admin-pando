# DLG-P008 가격 이력 조회 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P008 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P004 "가격 이력" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-이력있음` / `03-이력없음` / `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P008 가격 이력 조회 — 상태: 로딩

파일: src/components/discount/PriceHistoryModal.tsx

레이아웃 (Modal w-[600px]):
- 모달 헤더: "가격 이력 — {product.name}" + X 닫기
- 본문: Skeleton 행 5개 (h-10 animate-pulse bg-gray-100 rounded mb-2)
- 하단: "닫기"

진입 즉시:
  const { data, error } = await supabase
    .from('audit_log')
    .select('*')
    .eq('target_type', 'product')
    .eq('target_id', product.id)
    .eq('action', 'price_change')
    .order('created_at', { ascending: false })
    .limit(50)

사용 컴포넌트: Modal, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P004 할인설정 화면에서 "가격 이력" 버튼 클릭
- API 응답 전 로딩 상태 표시

### 레이아웃 구성
- 모달 너비: 600px
- 헤더: "가격 이력 — {상품명}" + X 닫기
- 본문: 스켈레톤 5행 (animate-pulse)
- 하단: "닫기" 버튼

### 연결 화면
- 이력 있음: `02-이력있음`
- 이력 없음: `03-이력없음`
- API 실패: `04-에러`
