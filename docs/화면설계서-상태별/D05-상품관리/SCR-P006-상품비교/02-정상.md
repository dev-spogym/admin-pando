# SCR-P006 상품 비교 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P006 |
| 상태 코드 | `normal` |
| 경로 | `/products/compare` |
| 역할 | owner, manager, trainer, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 읽기전용 → `03-읽기전용` / 판매중지 포함 → `04-판매중지포함` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P006 상품 비교 — 상태: 정상 (비교 테이블)

파일: src/app/products/compare/page.tsx

레이아웃:
- AppLayout + PageHeader (제목: "상품 비교")
- 비교 테이블 (가로 스크롤 가능):
  - 좌측 고정 열: 항목명 (상품명 / 카격 / 유형 / 상태 / 혜택 / 등록일)
  - 우측 상품 열 (선택 상품 수만큼):
    - 상품명
    - 가격 (최저가 셀: bg-blue-50 font-bold text-blue-700 강조)
    - 유형, 상태 Badge
    - 혜택 설명
    - 재고 Badge
    - "바로 구매" 버튼 (hasPermission 조건)
- 하단: "상품 추가" 버튼 (최대 4개까지 비교 가능)

최저가 하이라이트:
  const minPrice = Math.min(...products.map(p => p.price))
  셀 className: price === minPrice ? 'bg-blue-50 font-bold' : ''

사용 컴포넌트: AppLayout, PageHeader, StatusBadge, Button
사용 유틸: supabase, formatNumber, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + 선택 상품 모두 정상 (판매중지 없음)

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 상품 상세 | `products` | `id IN (선택 ids)` |
| 재고 | `inventory` | `product_id IN (선택 ids)` |

### 인터랙션 (User Actions)
1. "바로 구매" → 판매/계약 화면 이동
2. "상품 추가" → 상품 선택 모달
3. 상품 열 X → 해당 상품 비교에서 제거

### 비즈니스 룰
- 최저가 셀 파란 배경 강조
- 최대 4개 상품 비교

### 연결 화면
- 판매중지 포함: `04-판매중지포함`
- trainer/front: `03-읽기전용`
