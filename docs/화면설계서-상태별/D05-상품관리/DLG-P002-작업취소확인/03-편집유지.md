# DLG-P002 작업 취소 확인 — 상태: 편집 유지

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P002 |
| 상태 코드 | `stay` |
| 트리거 | "계속 편집" 클릭 또는 ESC |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 편집 화면 복귀 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P002 작업 취소 확인 — 상태: 편집 유지

파일: src/components/common/UnsavedChangesModal.tsx (cancel 클릭)

구현:
const handleStay = () => {
  onClose()   // 모달만 닫기
  // 폼 상태 유지, 화면 이동 없음
}

// ESC 키 핸들러도 동일하게 handleStay 호출
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- "계속 편집" 클릭 또는 ESC 키

### 비즈니스 룰
- 모달만 닫힘
- 폼 데이터 및 편집 화면 그대로 유지
- isDirty 상태 보존

### 연결 화면
- SCR-P002 또는 SCR-P003 편집 화면 복귀
