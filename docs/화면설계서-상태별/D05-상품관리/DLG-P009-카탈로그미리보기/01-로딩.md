# DLG-P009 카탈로그 미리보기 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P009 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P005 "미리보기" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-시뮬표시` / `03-공개상품없음` / `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P009 카탈로그 미리보기 — 상태: 로딩

파일: src/components/catalog/CatalogPreviewModal.tsx

레이아웃 (Modal w-[375px] h-[667px] — 모바일 시뮬):
- 모달 헤더: "카탈로그 미리보기" + X 닫기
- 본문: 375×667 내부 Skeleton 카드 4개 animate-pulse

진입 즉시:
  const { data } = await supabase
    .from('products')
    .select('*, product_catalog(*)')
    .eq('branch_id', branchId)
    .eq('product_catalog.is_public', true)
    .order('product_catalog.catalog_order')

사용 컴포넌트: Modal, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 미리보기 버튼 클릭 후 API 응답 전

### 연결 화면
- 다음: `02-시뮬표시` / `03-공개상품없음` / `04-에러`
