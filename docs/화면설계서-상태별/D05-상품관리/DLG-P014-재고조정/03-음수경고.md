# DLG-P014 재고 조정 — 상태: 음수 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P014 |
| 상태 코드 | `negative-warning` |
| 트리거 | 조정 후 예상 재고 < 0 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` (수량 입력 중) |
| 다음 상태 | 수량 수정 → `01-오픈` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P014 재고 조정 — 상태: 음수 경고

파일: src/components/inventory/StockAdjustModal.tsx (예상 재고 < 0)

구현:
const expectedStock = calculateExpected(currentStock, type, qty)
const isNegative = expectedStock < 0

{isNegative && (
  <p className="text-xs text-red-500 mt-1">
    조정 후 재고가 0 미만이 됩니다 (예상: {expectedStock}개)
  </p>
)}

조정 확인 버튼: disabled={isNegative}
예상 재고 표시: isNegative ? text-red-500 : text-gray-700
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 감소/직접 설정으로 인해 조정 후 재고가 0 미만 예상

### 비즈니스 룰
- 음수 재고 불허
- 실시간 인라인 경고
- 확인 버튼 즉시 disabled

### 연결 화면
- 수량 수정: `01-오픈`
