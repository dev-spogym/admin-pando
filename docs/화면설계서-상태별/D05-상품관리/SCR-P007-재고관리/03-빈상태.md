# SCR-P007 재고 관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P007 |
| 상태 코드 | `empty` |
| 경로 | `/products/inventory` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | SCR-P001으로 이동 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P007 재고 관리 — 상태: 빈 상태 (등록 상품 없음)

파일: src/app/products/inventory/page.tsx (inventory.length === 0 분기)

레이아웃:
- AppLayout + PageHeader
- 빈 상태:
  <div className="flex flex-col items-center justify-center py-32">
    <Package className="w-16 h-16 text-gray-300 mb-4" />
    <p className="text-lg font-semibold text-gray-500">등록된 상품이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">상품 관리에서 먼저 상품을 등록하세요</p>
    <Button onClick={() => router.push('/products')} className="mt-6">
      상품 관리로 이동
    </Button>
  </div>

아이콘: Package (lucide-react)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + 상품/재고 데이터 없음

### 인터랙션 (User Actions)
1. "상품 관리로 이동" → SCR-P001

### 연결 화면
- SCR-P001
