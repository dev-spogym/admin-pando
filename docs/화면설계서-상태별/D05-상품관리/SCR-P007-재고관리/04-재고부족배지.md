# SCR-P007 재고 관리 — 상태: 재고 부족 배지

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P007 |
| 상태 코드 | `low-stock-badge` |
| 경로 | `/products/inventory` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (임계값 이하 상품 포함) |
| 다음 상태 | 재고 조정 → DLG-P014 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P007 재고 관리 — 상태: 재고 부족 배지 (임계값 이하)

파일: src/app/products/inventory/page.tsx (0 < current_stock <= min_threshold)

구현:
재고 상태 배지 로직:
  if (current_stock === 0) → "소진" red
  else if (current_stock <= min_threshold) → "부족" yellow
  else → "정상" green

부족 행 강조:
  <tr className="bg-yellow-50">

상단 요약 배너 (부족 상품 있을 때):
  <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-3 mb-4">
    <p className="text-sm text-yellow-700">재고 부족 상품 {lowCount}개 — 입고 등록을 권장합니다</p>
  </div>

사용 컴포넌트: DataTable, StatusBadge
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-정상` 상태에서 `0 < current_stock <= min_threshold` 인 상품 존재

### 비즈니스 룰
- 노란 "부족" 배지 + 행 배경 bg-yellow-50
- 상단 요약 배너로 입고 권장

### 연결 화면
- DLG-P012 (입고 등록), DLG-P014 (재고 조정)
