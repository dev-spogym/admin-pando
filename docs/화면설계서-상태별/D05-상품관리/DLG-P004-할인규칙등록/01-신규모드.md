# DLG-P004 할인 규칙 등록 — 상태: 신규 모드

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P004 |
| 상태 코드 | `new` |
| 트리거 | SCR-P004 "할인 규칙 추가" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `03-저장결과` / 취소 → 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P004 할인 규칙 등록 — 상태: 신규 모드

파일: src/components/discount/DiscountRuleCreateModal.tsx

레이아웃 (Modal w-[520px]):
- 모달 헤더: "할인 규칙 추가" + X 닫기
- 본문 폼:
  - 규칙명 (input, required)
  - 할인 유형 (Select: 정률/정액)
  - 할인 값 (input type="number", required, suffix="%" 또는 "원")
  - 적용 조건 (Select: 항상/특정기간/회원등급)
  - 적용 기간 (DateRangePicker, 조건이 "특정기간"일 때만 표시)
  - 적용 상품 (MultiSelect, optional)
- 하단: "취소" | "등록"

등록 핸들러:
  const { error } = await supabase
    .from('discount_policies')
    .insert({
      branch_id: branchId,
      name: form.name,
      type: form.discountType,
      value: Number(form.value),
      condition: form.condition,
      start_date: form.start_date || null,
      end_date: form.end_date || null,
      product_ids: form.product_ids,
    })

사용 컴포넌트: Modal, Button, Input, Select, DateRangePicker, MultiSelect
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P004 할인설정 화면에서 "할인 규칙 추가" 클릭

### 필드 정의

| 필드 | 타입 | 필수 | 조건 |
|------|------|------|------|
| 규칙명 | text | Y | |
| 할인 유형 | Select | Y | 정률/정액 |
| 할인 값 | number | Y | 유형에 따라 suffix 변경 |
| 적용 조건 | Select | Y | 항상/특정기간/회원등급 |
| 적용 기간 | DateRangePicker | 조건부 | 특정기간 선택 시 표시 |
| 적용 상품 | MultiSelect | N | |

### 인터랙션 (User Actions)
1. 할인 유형 변경 → suffix 동적 변경 (% / 원)
2. 적용 조건 "특정기간" 선택 → DateRangePicker 표시
3. "등록" 클릭 → INSERT → `03-저장결과`
4. "취소" / X → 닫기

### 연결 화면
- 등록 결과: `03-저장결과`
- 취소: SCR-P004 복귀
