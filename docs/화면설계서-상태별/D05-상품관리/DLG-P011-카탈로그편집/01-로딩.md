# DLG-P011 카탈로그 편집 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P011 |
| 상태 코드 | `loading` |
| 트리거 | SCR-P005 카드 "편집" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 모달 오픈 |
| 다음 상태 | `02-편집폼` / `03-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P011 카탈로그 편집 — 상태: 로딩

파일: src/components/catalog/CatalogEditModal.tsx

레이아웃 (Modal w-[560px]):
- 모달 헤더: "카탈로그 편집"
- 본문: 폼 Skeleton (이미지 Skeleton + 텍스트 필드 4개)
- 하단: "취소" + "저장" (disabled)

진입 즉시:
  const { data } = await supabase
    .from('product_catalog')
    .select('*')
    .eq('product_id', product.id)
    .single()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 카탈로그 카드 편집 버튼 클릭 후 API 응답 전

### 연결 화면
- 다음: `02-편집폼` / `03-에러`
