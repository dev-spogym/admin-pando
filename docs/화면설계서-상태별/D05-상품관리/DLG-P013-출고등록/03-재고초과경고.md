# DLG-P013 출고 등록 — 상태: 재고 초과 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P013 |
| 상태 코드 | `stock-overflow` |
| 트리거 | 출고 수량 > 현재 재고 |
| 역할 | owner, manager, front |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` (수량 입력 중) |
| 다음 상태 | 수량 수정 → `01-오픈` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P013 출고 등록 — 상태: 재고 초과 경고

파일: src/components/inventory/OutboundModal.tsx (qty > currentStock)

구현 (실시간):
const isOverStock = qty > currentStock

{isOverStock && (
  <p className="text-xs text-red-500 mt-1">
    출고 수량이 현재 재고({currentStock}개)를 초과합니다
  </p>
)}

수량 Input: isOverStock ? border-red-400 : border-gray-300
등록 버튼: disabled={isOverStock}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 출고 수량 Input 값이 현재 재고를 초과할 때 (실시간)

### 비즈니스 룰
- 인라인 경고 표시 (토스트 아님)
- 등록 버튼 즉시 disabled
- 수량 수정 시 경고 해제

### 연결 화면
- 수량 수정: `01-오픈` 상태 복귀
