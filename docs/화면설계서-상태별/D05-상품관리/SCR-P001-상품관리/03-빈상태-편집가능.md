# SCR-P001 상품 관리 — 상태: 빈 상태 (편집 가능)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P001 |
| 상태 코드 | `empty-editable` |
| 경로 | `/products` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 상품 등록 클릭 → SCR-P002 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P001 상품 관리 — 상태: 빈 상태 (canEditProduct=true)

파일: src/app/products/page.tsx (products.length === 0 && canEditProduct 분기)

레이아웃:
- AppLayout + PageHeader (제목: "상품 관리", 우측: "상품 등록" 버튼 표시)
- 통계 카드 4개: 모두 0 표시
- DataTable 대신 빈 상태 컴포넌트:
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <Package className="w-16 h-16 text-gray-300 mb-4" />
    <p className="text-lg font-semibold text-gray-500">등록된 상품이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">첫 번째 상품을 등록해보세요</p>
    <Button onClick={() => router.push('/products/new')} className="mt-6">
      첫 상품 등록하기
    </Button>
  </div>

사용 컴포넌트: AppLayout, PageHeader, Button
사용 유틸: useAuthStore, hasPermission, moveToPage
아이콘: Package (lucide-react)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `products.length === 0` + `hasPermission('canEditProduct') = true`
- 신규 지점 또는 상품 전체 삭제 후

### 필수 데이터
- `products.length === 0` 확인

### 인터랙션 (User Actions)
1. "첫 상품 등록하기" 버튼 → `/products/new` 이동

### 비즈니스 룰
- 통계 카드 4개는 0으로 표시
- 검색 필터 숨김 (데이터 없으므로 불필요)

### 에지 케이스
- 필터 적용 중 결과 0건과 구분: 필터 없는 전체 0건만 이 상태

### 연결 화면
- 이전: `01-로딩`
- 다음: 상품 등록 → SCR-P002
