# SCR-P001 상품 관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P001 |
| 상태 코드 | `error` |
| 경로 | `/products` |
| 역할 | owner, manager, trainer, front |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P001 상품 관리 — 상태: 에러 (API 실패)

파일: src/app/products/page.tsx (error 분기)

구현:
- DataTable 영역 대신:
  <div className="flex flex-col items-center justify-center py-24">
    <p className="text-red-500 font-semibold">상품 목록을 불러오지 못했습니다</p>
    <button onClick={reload} className="mt-4 px-4 py-2 bg-blue-600 text-white rounded-lg text-sm">
      다시 시도
    </button>
  </div>
- toast.error("상품 목록을 불러오지 못했습니다.") 1회 발사
- 통계 카드 4개: -- 표시 (데이터 없음)

사용 컴포넌트: AppLayout, PageHeader
사용 유틸: supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase 쿼리 실패 (네트워크 오류, 500, RLS 위반 등)
- 10초 타임아웃 초과

### 필수 데이터
- 실패한 error 객체 (로깅용)

### 인터랙션 (User Actions)
1. "다시 시도" 버튼 → `reload()` → `01-로딩` 재진입

### 비즈니스 룰
- toast.error는 중복 방지: 1회만 발사
- 재시도 3회 연속 실패 시 "잠시 후 다시 시도해주세요" 문구로 교체

### 에지 케이스
- 부분 실패 (통계만 실패, 목록 성공): 통계 카드만 -- 처리 후 목록 정상 표시

### 연결 화면
- 재시도: `01-로딩`
