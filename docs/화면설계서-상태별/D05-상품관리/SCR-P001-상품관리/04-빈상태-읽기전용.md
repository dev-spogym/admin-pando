# SCR-P001 상품 관리 — 상태: 빈 상태 (읽기 전용)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P001 |
| 상태 코드 | `empty-readonly` |
| 경로 | `/products` |
| 역할 | trainer, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 없음 (조회 전용) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P001 상품 관리 — 상태: 빈 상태 (canEditProduct=false)

파일: src/app/products/page.tsx (products.length === 0 && !canEditProduct 분기)

레이아웃:
- AppLayout + PageHeader (제목: "상품 관리", 우측 버튼 없음)
- 통계 카드 4개: 모두 0 표시
- 빈 상태 컴포넌트 (등록 링크 없음):
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <Package className="w-16 h-16 text-gray-300 mb-4" />
    <p className="text-lg font-semibold text-gray-500">등록된 상품이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">관리자에게 문의하세요</p>
  </div>

사용 컴포넌트: AppLayout, PageHeader
사용 유틸: useAuthStore, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `products.length === 0` + `hasPermission('canEditProduct') = false`
- trainer / front 역할로 진입 시

### 필수 데이터
- `products.length === 0` 확인

### 인터랙션 (User Actions)
- 없음 (등록 버튼, 등록 링크 모두 숨김)

### 비즈니스 룰
- 상단 PageHeader 우측 액션 버튼 전체 숨김
- "관리자에게 문의하세요" 안내 문구 표시

### 에지 케이스
- RBAC 변경으로 권한이 부여되면 `03-빈상태-편집가능`으로 전환

### 연결 화면
- 이전: `01-로딩`
