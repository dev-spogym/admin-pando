# SCR-P004 할인 설정 — 상태: 읽기 전용

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P004 |
| 상태 코드 | `readonly` |
| 경로 | `/discount-settings` |
| 역할 | trainer, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (trainer/front 역할) |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P004 할인 설정 — 상태: 읽기 전용 (trainer/front)

파일: src/app/discount-settings/page.tsx (hasPermission('canEditDiscount')=false)

레이아웃:
- AppLayout + PageHeader (제목: "할인 설정", 우측 버튼 없음)
- DataTable: 액션 컬럼 없음 (수정/삭제 버튼 제거)
- 변경 이력: 조회만 가능

구현:
const canEdit = hasPermission('canEditDiscount') // false
// 조건부로 버튼 및 액션 컬럼 숨김

사용 컴포넌트: AppLayout, PageHeader, DataTable, StatusBadge
사용 유틸: hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- trainer, front 역할로 진입

### 인터랙션 (User Actions)
- 조회만 가능 (추가/수정/삭제 불가)

### 비즈니스 룰
- "할인 정책 추가" 버튼 숨김
- DataTable 액션 컬럼 숨김

### 연결 화면
- 없음 (조회 전용)
