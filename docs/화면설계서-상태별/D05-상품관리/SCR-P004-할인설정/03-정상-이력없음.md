# SCR-P004 할인 설정 — 상태: 정상 (이력 없음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P004 |
| 상태 코드 | `normal-no-history` |
| 경로 | `/discount-settings` |
| 역할 | owner, manager, trainer, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 추가 → DLG-P007 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P004 할인 설정 — 상태: 정상 (정책 있음, 이력 없음)

파일: src/app/discount-settings/page.tsx (historyList.length === 0 분기)

레이아웃:
- DataTable 정상 표시 (정책 목록 있음)
- 변경 이력 섹션: 숨김 처리 (historyList.length === 0)
  또는 "변경 이력이 없습니다." 안내 텍스트

구현:
{historyList.length > 0 ? <HistoryTimeline ... /> : null}

사용 컴포넌트: AppLayout, PageHeader, DataTable, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + 정책 있음 + 이력 없음 (초기 등록 직후 등)

### 비즈니스 룰
- 이력 섹션 숨김 (빈 섹션 미노출)

### 연결 화면
- DLG-P007 (정책 추가)
