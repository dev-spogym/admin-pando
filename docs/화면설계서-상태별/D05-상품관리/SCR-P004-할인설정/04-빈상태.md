# SCR-P004 할인 설정 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P004 |
| 상태 코드 | `empty` |
| 경로 | `/discount-settings` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 추가 → DLG-P007 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P004 할인 설정 — 상태: 빈 상태 (등록된 할인 정책 없음)

파일: src/app/discount-settings/page.tsx (policies.length === 0 분기)

레이아웃:
- AppLayout + PageHeader (제목: "할인 설정", 우측: "할인 정책 추가" 버튼)
- DataTable 대신 빈 상태:
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <p className="text-lg font-semibold text-gray-500">등록된 할인 정책이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">할인 정책을 추가하면 상품에 적용할 수 있습니다</p>
  </div>

사용 컴포넌트: AppLayout, PageHeader, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `discount_policies.length === 0`

### 인터랙션 (User Actions)
1. "할인 정책 추가" → DLG-P007

### 비즈니스 룰
- 안내 문구: "등록된 할인 정책이 없습니다."

### 연결 화면
- DLG-P007 (신규 등록)
