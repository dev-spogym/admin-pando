# DLG-P003 가격 이력 — 상태: 이력 있음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P003 |
| 상태 코드 | `has-history` |
| 트리거 | audit_log 조회 완료 (이력 있음) |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 닫기 → 모달 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P003 가격 이력 — 상태: 이력 있음

파일: src/components/products/PriceHistoryModal.tsx (history.length > 0)

레이아웃:
- 모달 헤더: "가격 변경 이력 — {product.name}"
- 이력 테이블:
  <table className="w-full text-sm">
    <thead>
      <tr className="border-b text-gray-500">
        <th>변경일</th><th>변경 전</th><th>변경 후</th><th>변경자</th>
      </tr>
    </thead>
    <tbody>
      {history.map(h => (
        <tr key={h.id} className="border-b hover:bg-gray-50">
          <td>{format(h.created_at, 'yyyy.MM.dd HH:mm')}</td>
          <td className="text-red-500 line-through">{formatNumber(h.old_value)}원</td>
          <td className="text-blue-600 font-semibold">{formatNumber(h.new_value)}원</td>
          <td>{h.changed_by_name}</td>
        </tr>
      ))}
    </tbody>
  </table>
- 페이지네이션 (10건/페이지)
- 하단: "닫기" 버튼

사용 유틸: formatNumber, format (date-fns)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- audit_log 조회 완료 + 이력 1건 이상

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 가격 이력 | `audit_log` | `table='products'`, `field='price'`, `record_id=product.id` |

### 인터랙션 (User Actions)
1. 페이지네이션 → 이전/다음 페이지
2. "닫기" → 모달 닫기

### 비즈니스 룰
- 변경 전 가격: 빨간 취소선
- 변경 후 가격: 파란 굵은 글씨
- 최신순 정렬

### 연결 화면
- 닫기: SCR-P003 복귀
