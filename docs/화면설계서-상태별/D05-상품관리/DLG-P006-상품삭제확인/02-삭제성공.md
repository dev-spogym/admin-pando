# DLG-P006 상품 삭제 확인 — 상태: 삭제 성공

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P006 |
| 상태 코드 | `success` |
| 트리거 | 삭제 API 200 응답 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 모달 자동 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P006 상품 삭제 확인 — 상태: 성공

파일: src/components/products/DeleteProductModal.tsx (성공 분기)

구현:
const handleDelete = async () => {
  const { error } = await supabase
    .from('products')
    .delete()
    .eq('id', product.id)

  if (!error) {
    toast.success('상품이 삭제되었습니다.')
    onClose()
    onRefresh()   // 목록 갱신
  }
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 삭제 API 성공

### 비즈니스 룰
- toast.success 후 자동 닫힘
- SCR-P001 목록에서 해당 행 제거

### 연결 화면
- SCR-P001 목록 갱신
