# SCR-P005 상품 카탈로그 — 상태: 읽기 전용

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P005 |
| 상태 코드 | `readonly` |
| 경로 | `/products/catalog` |
| 역할 | trainer, front |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (trainer/front) |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P005 상품 카탈로그 — 상태: 읽기 전용 (trainer/front)

파일: src/app/products/catalog/page.tsx (hasPermission('canEditCatalog')=false)

레이아웃:
- PageHeader 우측 버튼 없음 (미리보기, 설정 버튼 숨김)
- 카드 그리드 정상 표시
- 각 카드: 편집 버튼 없음, 공개 토글 없음
- 드래그 정렬 비활성 (pointer-events-none)

사용 유틸: hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- trainer, front 역할로 진입

### 인터랙션 (User Actions)
- 조회만 가능

### 비즈니스 룰
- 편집 버튼, 공개 토글, 드래그 정렬 모두 비활성

### 연결 화면
- 없음
