# DLG-P004 비활성화 안내 — 상태: 비활성화 성공

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-P004 |
| 상태 코드 | `success` |
| 트리거 | 비활성화 API 200 응답 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-오픈` |
| 다음 상태 | 모달 자동 닫힘 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 모달 컴포넌트를 작성하라.
화면: DLG-P004 비활성화 안내 — 상태: 성공

파일: src/components/products/DeactivateProductModal.tsx (성공 분기)

구현:
const handleDeactivate = async () => {
  const { error } = await supabase
    .from('products')
    .update({ status: 'inactive' })
    .eq('id', product.id)

  if (!error) {
    toast.success('상품이 비활성화되었습니다.')
    onClose()
    onRefresh()
  }
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 비활성화 API 성공 (200)

### 비즈니스 룰
- toast.success 발사 후 모달 자동 닫힘
- SCR-P001 목록 및 SCR-P003 패널 상태 갱신

### 연결 화면
- SCR-P003 → 비활성 배지로 전환
