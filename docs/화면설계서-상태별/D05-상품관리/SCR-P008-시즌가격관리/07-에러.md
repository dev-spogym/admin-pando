# SCR-P008 시즌 가격 관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P008 |
| 상태 코드 | `error` |
| 경로 | `/products/seasonal-prices` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P008 시즌 가격 관리 — 상태: 에러

파일: src/app/products/seasonal-prices/page.tsx (error 분기)

구현:
- toast.error("데이터를 불러오지 못했습니다.")
- 목록 대신:
  <div className="flex flex-col items-center py-24">
    <p className="text-red-500 font-semibold">데이터를 불러오지 못했습니다</p>
    <button onClick={reload} className="mt-4 px-4 py-2 bg-blue-600 text-white rounded-lg text-sm">
      다시 시도
    </button>
  </div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- API 실패

### 인터랙션 (User Actions)
1. "다시 시도" → `01-로딩`

### 연결 화면
- 재시도: `01-로딩`
