# SCR-P008 시즌 가격 관리 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P008 |
| 상태 코드 | `normal` |
| 경로 | `/products/seasonal-prices` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 추가 → DLG-P016 / 수정 → DLG-P016 / 활성 → `04-활성강조` / 만료 → `05-만료처리` / 충돌 → `06-충돌경고` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P008 시즌 가격 관리 — 상태: 정상

파일: src/app/products/seasonal-prices/page.tsx

레이아웃:
- AppLayout + PageHeader (제목: "시즌 가격 관리", 우측: "시즌 추가" 버튼)
- DataTable (시즌 가격 규칙 목록):
  컬럼: 시즌명 | 적용 기간 | 적용 단가 | 적용 상품 수 | 상태 | 액션
  상태 배지:
    현재 적용 중: StatusBadge "활성" (green)
    미래 예정: StatusBadge "예정" (blue)
    만료: StatusBadge "만료" (gray)
  액션: "수정" 버튼 (만료 규칙은 disabled), "삭제" 버튼

데이터:
- seasonal_prices 테이블 (가상): id, name, start_date, end_date, price, product_ids, status
  status 자동 계산:
    today < start_date → 예정
    start_date <= today <= end_date → 활성
    today > end_date → 만료

사용 컴포넌트: AppLayout, PageHeader, DataTable, StatusBadge, Button
사용 유틸: supabase, useAuthStore, formatNumber
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `seasonal_prices.length > 0`

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 시즌 규칙 | `seasonal_prices` | `branch_id = ?` |

### 인터랙션 (User Actions)
1. "시즌 추가" → DLG-P016 (신규)
2. "수정" → DLG-P016 (수정)
3. "삭제" → 삭제 확인 모달 (미구현 별도)

### 비즈니스 룰
- 만료 규칙: 수정 버튼 disabled
- 활성 규칙: 초록 배경 강조 (`04-활성강조`)
- 기간 충돌 규칙: 노란 경고 배지 (`06-충돌경고`)

### 연결 화면
- DLG-P016
- `04-활성강조`, `05-만료처리`, `06-충돌경고`
