# SCR-P003 상품 상세 패널 — 상태: 수정 (활성 상품)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P003 |
| 상태 코드 | `panel-edit-active` |
| 경로 | SCR-P001 우측 패널 (활성 상품 클릭) |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-P001 활성 상품 행 클릭 |
| 다음 상태 | 저장 → `05-저장중` / 비활성화 → DLG-P004 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P003 상품 상세 패널 — 상태: 수정 모드 (활성 상품)

파일: src/components/products/ProductDetailPanel.tsx (mode="edit", product.status="active")

레이아웃:
- 패널 헤더: "상품 수정" + 활성 배지 (bg-mint-100 text-mint-700 "활성") + X 닫기
- 폼: 기존 상품 데이터 프리필
- 가격 이력 버튼: hasPermission('canViewPriceHistory') 시 표시
- 비활성화 버튼 → DLG-P004 트리거
- 패키지 섹션: packageOpen 토글 버튼 (상품 유형이 패키지일 때)
  packageOpen=true 시: 구성 상품 목록 표시

하단 버튼바: "취소" + "가격 이력" + "삭제" + "저장"
(hasPermission에 따라 버튼 표시/숨김)

사용 컴포넌트: Button, Select, StatusBadge
사용 유틸: supabase, toast, useAuthStore, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-P001에서 status='active' 상품 행 클릭

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 상품 상세 | `products` | `id = selectedProduct.id` |
| 패키지 구성 | `products` (JOIN) | 패키지 유형일 때 |

### 인터랙션 (User Actions)
1. 폼 수정 → "저장" → `05-저장중`
2. "가격 이력" → DLG-P003
3. "비활성화" → DLG-P004
4. "삭제" → DLG-P006
5. 패키지 토글 → packageOpen 상태 전환

### 비즈니스 룰
- 헤더: "상품 수정" + mint 활성 배지
- 가격 이력/삭제: owner, manager만 노출

### 연결 화면
- 저장: SCR-P001 갱신
- 가격이력: DLG-P003
- 비활성화: DLG-P004
- 삭제: DLG-P006
