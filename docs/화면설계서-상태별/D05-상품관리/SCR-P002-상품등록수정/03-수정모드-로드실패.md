# SCR-P002 상품 등록/수정 — 상태: 수정 모드 로드 실패

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-P002 |
| 상태 코드 | `edit-load-error` |
| 경로 | `/products/edit?id={productId}` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-수정모드` 로드 중 실패 |
| 다음 상태 | 뒤로가기 → SCR-P001 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 CRM 관리자 화면을 작성하라.
화면: SCR-P002 상품 등록/수정 — 상태: 수정 모드 데이터 로드 실패

파일: src/app/products/edit/page.tsx (editId 조회 실패 분기)

구현:
- 상품 정보 조회 실패 시:
  toast.error("상품 정보를 불러오지 못했습니다.")
  폼 대신 에러 안내:
  <div className="flex flex-col items-center py-24">
    <p className="text-red-500">상품 정보를 불러오지 못했습니다</p>
    <Button onClick={() => router.back()} className="mt-4">목록으로 돌아가기</Button>
  </div>

사용 컴포넌트: AppLayout, PageHeader, Button
사용 유틸: supabase, toast, useAuthStore
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `editId`에 해당하는 상품 없음 (404)
- API 500 / 네트워크 오류

### 필수 데이터
- error 객체 (로깅용)

### 인터랙션 (User Actions)
1. "목록으로 돌아가기" → SCR-P001

### 비즈니스 룰
- toast.error 1회 발사
- 빈 폼 노출 금지 (데이터 없이 저장 방지)

### 연결 화면
- 목록 복귀: SCR-P001
