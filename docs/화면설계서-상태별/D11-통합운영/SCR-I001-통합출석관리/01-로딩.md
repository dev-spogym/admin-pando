# SCR-I001 통합출석관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I001 |
| 상태 코드 | `loading` |
| 경로 | `/attendance` |
| 역할 | superAdmin, primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 인증 완료 직후 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I001 통합출석관리 — 상태: 로딩 (스켈레톤)

파일: src/app/attendance/page.tsx

레이아웃:
- AppLayout (데스크톱, 사이드바 포함)
- PageHeader: 제목 "통합 출석 관리" + 브레드크럼
- 본문 space-y-6 p-6

스켈레톤 구성 (animate-pulse):

[KPI 카드 영역 — StatCardGrid 4칸]
- 4개 카드 각 (bg-gray-100 rounded-xl h-24)
- 내부: 짧은 라벨 바 (h-3 w-20 bg-gray-200) + 숫자 바 (h-8 w-16 bg-gray-300 mt-2)

[필터 바]
- 날짜 선택 스켈레톤 (h-9 w-40 bg-gray-100 rounded)
- 채널 드롭다운 스켈레톤 (h-9 w-32 bg-gray-100 rounded)
- 검색창 스켈레톤 (h-9 w-60 bg-gray-100 rounded)

[출석 로그 테이블]
- 헤더 행: 6개 컬럼 바 (h-4 bg-gray-200 rounded)
- 데이터 행 8줄: 각 행 6개 셀 스켈레톤 (h-4 bg-gray-100 rounded)
- 행 사이 border-b border-gray-50

구현:
- useEffect(() => { loadData() }, []) 패턴
- useState: loading = true 초기값
- 로딩 완료 시 loading = false → 정상 컴포넌트로 전환
- 타임아웃 없음 (Supabase 응답 대기)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I001 최초 진입 시 인증 확인 직후
- 필터 변경 후 재조회 시 (부분 로딩)
- 수동 새로고침 클릭 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| KPI | `attendance` | `branch_id = ?`, 오늘 날짜 집계 |
| 로그 | `attendance` JOIN `members` | `branch_id = ?`, 필터 조건 |
| IoT 상태 | `iot_devices` | `branch_id = ?`, `type = 'entrance'` |

### 인터랙션 (User Actions)
- 로딩 중 모든 버튼 비활성화 (disabled)
- 뒤로가기 가능 (router.back())

### 비즈니스 룰
- 최대 로딩 대기 30초 → 초과 시 에러 상태로 전환
- KPI·테이블 병렬 조회 (Promise.all)

### 에지 케이스
- 느린 네트워크: 스켈레톤 애니메이션 지속
- 인증 만료 중 로딩: useAuthStore 감지 → 로그인으로 리다이렉트

### 연결 화면
- 이전: 로그인 완료 / 사이드바 메뉴 클릭
- 다음: `02-정상` / `03-빈상태` / `05-에러`
