# SCR-I007 회원건강연동요약 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I007 |
| 상태 코드 | `loading` |
| 경로 | (미구현 — 예상: `/members/[id]/health` 또는 `/health-summary`) |
| 역할 | superAdmin, primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 회원 상세 탭 전환 또는 직접 진입 |
| 다음 상태 | `02-정상` / `03-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I007 회원건강연동요약 — 상태: 로딩 (스켈레톤)

파일: src/app/members/[id]/health/page.tsx (신규 생성)

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "[회원명] 건강·연동 요약" (스켈레톤)

스켈레톤 구성 (animate-pulse) — 5개 카드:

[카드 1 — 체성분 요약]
- 제목 바 (h-4 w-24 bg-gray-200)
- 측정값 3개 바 (h-6 w-16 bg-gray-100, 가로 배열)
- 측정일 바 (h-3 w-28 bg-gray-100)

[카드 2 — 출석 현황]
- 제목 바
- 월별 출석 그래프 스켈레톤 (h-20 w-full bg-gray-100 rounded)

[카드 3 — 옷 락커]
- 제목 바
- 락커 번호 + 만료일 바

[카드 4 — HC (헬스커넥트) 연동]
- 제목 바
- 연동 상태 배지 스켈레톤 (h-8 w-20 bg-gray-100 rounded-full)

[카드 5 — 이용권]
- 제목 바
- 이용권 상태 바

구현:
- 라우트 파라미터: params.id (회원 ID)
- Promise.all([fetchBodyComp, fetchAttendance, fetchLocker, fetchHC, fetchContracts]) 5개 병렬 실행
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 회원 상세 페이지 "건강·연동" 탭 클릭
- 직접 URL 접근: `/members/[id]/health`

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 체성분 | `bodyComposition` | `member_id=?`, 최신 1건 |
| 출석 | `attendance` | `member_id=?`, 최근 3개월 |
| 락커 | `clothing` JOIN `lockers` | `member_id=?`, `status='active'` |
| HC 연동 | `health_data` (가상) | `member_id=?` |
| 이용권 | `contracts` | `member_id=?`, `status='서명완료'` |

### 연결 화면
- 이전: 회원 상세 탭바
- 다음: `02-정상` / `03-에러`
