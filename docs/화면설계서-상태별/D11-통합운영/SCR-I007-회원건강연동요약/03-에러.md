# SCR-I007 회원건강연동요약 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I007 |
| 상태 코드 | `error` |
| 경로 | `/members/[id]/health` |
| 역할 | superAdmin, primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (API 실패) |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I007 회원건강연동요약 — 상태: 에러

파일: src/app/members/[id]/health/page.tsx

에러 처리 정책:
- 개별 카드 실패: 해당 카드만 에러 표시 (전체 페이지 에러 지양)
- 전체 실패 (member 정보 조회 포함): 페이지 에러 UI

[개별 카드 에러 UI]
<div className="bg-red-50 border border-red-100 rounded-xl p-5 flex flex-col items-center text-center">
  <p className="text-sm text-red-500 font-medium">{cardName} 정보를 불러오지 못했습니다</p>
  <button onClick={() => retryCard(cardName)} className="mt-2 text-xs text-red-400 underline">
    다시 시도
  </button>
</div>

[전체 에러 UI]
<div className="flex flex-col items-center justify-center py-20 text-center">
  <div className="w-16 h-16 rounded-full bg-red-50 flex items-center justify-center mb-4">
    <svg ... className="w-8 h-8 text-red-400" />
  </div>
  <p className="text-base font-semibold text-gray-700">건강 요약 정보를 불러오지 못했습니다</p>
  <p className="mt-1 text-sm text-gray-400">{errorMessage}</p>
  <button
    onClick={() => { setError(null); setLoading(true); loadAllCards() }}
    className="mt-4 px-4 py-2 bg-blue-600 text-white text-sm rounded-lg"
  >
    다시 시도
  </button>
</div>

개별 카드 재시도:
const retryCard = async (cardName: string) => {
  setCardError(prev => ({ ...prev, [cardName]: false }))
  // 해당 카드만 재조회
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 특정 카드 API 실패 (개별 에러)
- 회원 기본 정보 조회 실패 (전체 에러)

### 인터랙션
1. 개별 카드 "다시 시도" → 해당 카드만 재조회
2. 전체 에러 "다시 시도" → loadAllCards() 재실행

### 비즈니스 룰
- HC API 타임아웃: → `04-HC미연결` 상태로 처리 (에러 아님)
- 회원 정보 조회 실패: 전체 페이지 에러
- 나머지: 카드별 개별 에러 + 재시도

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
