# SCR-I007 회원건강연동요약 — 상태: 락커 미배정

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I007 |
| 상태 코드 | `locker-unassigned` |
| 경로 | `/members/[id]/health` |
| 역할 | superAdmin, primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (락커 미배정 회원) |
| 다음 상태 | 락커 배정 → DLG-I002 → `02-정상` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I007 회원건강연동요약 — 상태: 옷 락커 미배정 카드

파일: src/app/members/[id]/health/page.tsx (락커 카드 조건부)

조건: !lockerAssignment || lockerAssignment.status !== 'active'

[락커 카드 — 미배정 상태]
<div className="bg-white rounded-xl shadow-sm p-5">
  <div className="flex items-center justify-between mb-3">
    <h3 className="font-semibold text-gray-800">옷 락커</h3>
    <span className="px-2 py-1 bg-gray-100 text-gray-500 text-xs rounded-full font-medium">
      미배정
    </span>
  </div>

  <div className="flex flex-col items-center py-4 text-center">
    <svg ... className="w-10 h-10 text-gray-300 mb-3" /> {/* 자물쇠 아이콘 */}
    <p className="text-sm text-gray-500 font-medium">배정된 락커가 없습니다</p>
    <p className="mt-1 text-xs text-gray-400">
      회원에게 옷 락커를 배정하면 여기에 표시됩니다.
    </p>
  </div>

  {hasPermission('locker:write') && (
    <button
      onClick={() => openLockerAssignModal(memberId)}
      className="w-full mt-2 px-4 py-2 bg-gray-50 border border-gray-200 text-gray-700 text-sm rounded-lg hover:bg-gray-100"
    >
      락커 배정
    </button>
  )}
</div>

openLockerAssignModal:
- DLG-I002 열기 (회원 pre-fill, SCR-I007에서 트리거)
- 배정 완료 후: onSuccess → 카드 재조회

배정 완료 후 카드 전환:
- 락커 번호, 만료일, 배정일 표시 (정상 카드로 전환)
- toast.success('락커 {번호} 배정 완료')
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `clothing.status !== 'active'` 또는 배정 레코드 없음
- 반납 완료 후 재방문

### 필수 데이터
- `clothing` JOIN `lockers` WHERE `member_id=?`, `status='active'`: 없음

### 인터랙션 (User Actions)
1. "락커 배정" → DLG-I002 (member_id pre-fill)
2. 배정 완료: 카드 즉시 갱신
3. `locker:write` 없으면 버튼 숨김

### 비즈니스 룰
- 미배정은 info 상태 (에러 아님)
- DLG-I002 완료 후 onSuccess 콜백으로 카드 재조회
- 락커 만석 시: "락커 배정" 버튼 disabled + "현재 빈 락커가 없습니다" 안내

### 에지 케이스
- 락커 만석: 버튼 disabled + 안내 텍스트
- 이전 배정 만료 상태: "이전 배정: {lockerNumber} (만료)" 표시

### 연결 화면
- 이전: `02-정상` (미배정 회원)
- 다음: DLG-I002 → 배정 완료 후 `02-정상` 카드로 전환
