# SCR-I004 옷락커운영관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I004 |
| 상태 코드 | `error` |
| 경로 | `/(facilities)/locker` |
| 역할 | superAdmin, primary, owner, manager, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` (API 실패) |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I004 옷락커운영관리 — 상태: 에러

파일: src/app/(facilities)/locker/page.tsx

에러 UI:
<div className="flex flex-col items-center justify-center py-20 text-center">
  <div className="w-16 h-16 rounded-full bg-red-50 flex items-center justify-center mb-4">
    <svg ... className="w-8 h-8 text-red-400" />
  </div>
  <p className="text-base font-semibold text-gray-700">락커 데이터를 불러오지 못했습니다</p>
  <p className="mt-1 text-sm text-gray-400">{errorMessage}</p>
  <button
    onClick={() => { setError(null); setLoading(true); loadLockerData() }}
    className="mt-4 px-4 py-2 bg-blue-600 text-white text-sm rounded-lg hover:bg-blue-700"
  >
    다시 시도
  </button>
</div>

toast.error('락커 데이터 로드 실패', { id: 'locker-error' })
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `lockers` 또는 `clothing` 조회 API 실패
- Supabase RLS 오류

### 인터랙션
1. "다시 시도" → loadLockerData() 재실행
2. 뒤로가기 가능

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
