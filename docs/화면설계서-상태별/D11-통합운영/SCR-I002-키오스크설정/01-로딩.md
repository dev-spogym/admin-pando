# SCR-I002 키오스크설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-I002 |
| 상태 코드 | `loading` |
| 경로 | `/settings/kiosk` |
| 역할 | superAdmin, primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 인증/권한 확인 완료 직후 |
| 다음 상태 | `02-정상` 또는 `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-I002 키오스크설정 — 상태: 로딩 (스켈레톤)

파일: src/app/settings/kiosk/page.tsx

레이아웃:
- AppLayout (데스크톱)
- PageHeader: "키오스크 설정" (스켈레톤 유지)

스켈레톤 구성 (animate-pulse):

[설정 폼 영역]
- 섹션 제목 바 (h-5 w-32 bg-gray-200 rounded mb-4)
- 설정 항목 3줄:
  · 라벨 (h-4 w-24 bg-gray-100) + 입력 (h-10 w-full bg-gray-100 rounded) — 각 행 mb-4
- 토글 스위치 스켈레톤 (h-6 w-11 bg-gray-200 rounded-full)

[키오스크 연결 상태 영역]
- 상태 배지 스켈레톤 (h-8 w-28 bg-gray-100 rounded-full)

[저장 버튼 영역]
- 버튼 스켈레톤 (h-10 w-24 bg-gray-200 rounded-lg)

구현:
- useEffect(() => { loadKioskSettings() }, [])
- useState: loading = true 초기값
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-I002 진입 시 권한 확인 완료 후
- 설정 저장 후 재조회 시

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 설정값 | `kiosk_sessions` (가상) | `branch_id = ?` |
| 연결 상태 | 키오스크 ping API | 실시간 |

### 인터랙션
- 로딩 중 폼 입력/저장 버튼 비활성화

### 연결 화면
- 이전: 사이드바 설정 메뉴
- 다음: `02-정상` / `04-미연결` / `05-에러`
