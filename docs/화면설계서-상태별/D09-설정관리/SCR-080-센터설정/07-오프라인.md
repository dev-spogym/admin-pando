# SCR-080 센터설정 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-080 |
| 상태 코드 | `offline` |
| 경로 | `/settings` |
| 역할 | primary, owner |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `01-로딩` (연결 복구 시) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-080 센터설정 — 상태: 오프라인 (네트워크 연결 없음)

파일: src/app/settings/page.tsx

오프라인 감지:
- useEffect(() => {
    const handleOffline = () => setIsOffline(true)
    const handleOnline = () => { setIsOffline(false); fetchSettings() }
    window.addEventListener('offline', handleOffline)
    window.addEventListener('online', handleOnline)
    return () => { ... removeEventListener }
  }, [])

오프라인 UI:
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <div className="w-16 h-16 rounded-full bg-amber-100 flex items-center justify-center">
    <WifiOff className="w-8 h-8 text-amber-500" />
  </div>
  <div className="text-center">
    <p className="text-lg font-semibold text-gray-900">네트워크 연결을 확인해 주세요</p>
    <p className="mt-1 text-sm text-gray-500">인터넷 연결이 복구되면 자동으로 다시 불러옵니다.</p>
  </div>
  <div className="flex items-center gap-2 text-sm text-amber-600">
    <div className="w-2 h-2 rounded-full bg-amber-400 animate-pulse" />
    연결 대기 중...
  </div>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- navigator.onLine = false 또는 fetch 타임아웃
- 이벤트: `E_F6_080_07` (네트워크 오프라인)
- TC-080-NEG-004: 오프라인 → 오프라인 안내

### UI 구성
| 요소 | 내용 |
|------|------|
| 아이콘 | WifiOff, amber-500, bg-amber-100 원형 |
| 주 메시지 | "네트워크 연결을 확인해 주세요" |
| 부 메시지 | "인터넷 연결이 복구되면 자동으로 다시 불러옵니다." |
| 상태 표시 | amber pulse dot + "연결 대기 중..." |

### 비즈니스 룰
- online 이벤트 감지 시 자동으로 fetchSettings() 재호출
- 수동 재시도 버튼 없음 (자동 복구)

### 에지 케이스
- 연결 복구 후 자동 리페치 성공 → 정상 상태
- 연결 복구 후 서버 오류 → 에러 상태

### 연결 화면
- 자동: `01-로딩` (online 이벤트)
