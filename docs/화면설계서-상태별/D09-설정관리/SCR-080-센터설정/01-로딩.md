# SCR-080 센터설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-080 |
| 상태 코드 | `loading` |
| 경로 | `/settings` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `05-에러` / `06-권한없음` / `07-오프라인` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-080 센터설정 — 상태: 로딩 (Skeleton UI)

파일: src/app/settings/page.tsx

레이아웃:
- AppLayout 적용 (데스크톱, 사이드바 포함)
- PageHeader: 제목 "센터 설정", 저장 버튼 영역 포함

Skeleton 구성 (animate-pulse):
- 탭 영역: 4개 탭 버튼 Skeleton (h-9 w-24 bg-gray-200 rounded-md)
- 탭 하단 border-b

폼 섹션 Skeleton (space-y-6 mt-6):
- 섹션 헤더: h-5 w-32 bg-gray-200 rounded
- 필드 그룹 3개 반복:
  · 라벨: h-4 w-20 bg-gray-200 rounded
  · 인풋: h-10 w-full bg-gray-200 rounded-md
- 이미지 업로드 영역: h-32 w-32 bg-gray-200 rounded-lg

저장 버튼 Skeleton:
- h-10 w-24 bg-gray-200 rounded-md ml-auto

구현:
- useEffect(() => { fetchSettings() }, [])
- fetchSettings: supabase.from('settings').select('*').single()
- 로딩 중 return <SkeletonLayout />
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-080 최초 진입 시 settings 테이블 API 호출 중
- 이벤트: `E_F6_080_01`

### UI 구성
| 영역 | Skeleton 표현 |
|------|--------------|
| 탭 바 | 4개 pill h-9 w-24 |
| 폼 라벨 | h-4 w-20 |
| 폼 인풋 | h-10 w-full |
| 이미지 업로드 | h-32 w-32 rounded-lg |
| 저장 버튼 | h-10 w-24 |

### 비즈니스 룰
- Skeleton은 실제 레이아웃 구조와 동일한 높이/너비 유지
- animate-pulse 클래스로 shimmer 효과
- 로딩 시간 초과(10초) 시 에러 상태로 전환

### 에지 케이스
- 네트워크 오프라인 → Skeleton 유지 후 오프라인 상태로 전환
- 권한 없음 응답 → 권한없음 상태로 즉시 전환

### 연결 화면
- 다음: `02-정상` (API 성공)
- 다음: `05-에러` (API 실패)
- 다음: `06-권한없음` (403)
- 다음: `07-오프라인` (네트워크 오류)
