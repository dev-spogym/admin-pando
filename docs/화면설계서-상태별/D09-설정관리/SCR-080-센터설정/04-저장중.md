# SCR-080 센터설정 — 상태: 저장중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-080 |
| 상태 코드 | `saving` |
| 경로 | `/settings` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `03-변경됨` |
| 다음 상태 | `02-정상` (성공) / `03-변경됨` (실패) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-080 센터설정 — 상태: 저장중 (animate-pulse, 인터랙션 잠금)

파일: src/app/settings/page.tsx

저장중 상태 UI:
- 저장 버튼:
  <Button disabled className="animate-pulse">
    <Loader2 className="mr-2 h-4 w-4 animate-spin" />
    저장 중...
  </Button>

- 폼 필드 전체 비활성화:
  · 모든 Input, Textarea, Select에 disabled={isSaving} 적용
  · opacity-60 포인터 이벤트 없음

- amber 배지 유지 (저장 완료 전까지)

저장 결과 분기:
성공 시 (E_F6_080_11):
  - setSavedData({...form})
  - setIsDirty(false)
  - amber 배지 → accent(green) 배지 "저장 완료" 3초 표시 후 제거
  - toast.success('설정이 저장되었습니다')

실패 시 (E_F6_080_12):
  - toast.error('저장에 실패했습니다. 다시 시도해 주세요.')
  - isDirty 유지
  - 저장 버튼 재활성화
  - 폼 인터랙션 복구

성공 후 3초 자동 복귀 (E_F6_080_13):
  - setTimeout(() => setShowSavedBadge(false), 3000)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 저장 버튼 클릭 (isDirty=true)
- 이벤트: `E_F6_080_10`

### UI 구성
| 요소 | 저장중 표현 |
|------|------------|
| 저장 버튼 | animate-pulse + spinner + "저장 중..." 텍스트 |
| 폼 전체 | disabled + opacity-60 |
| amber 배지 | 유지 |
| 탭 전환 | 차단 (isSaving=true 동안) |

### 비즈니스 룰
- PATCH /settings 호출 중 모든 인터랙션 차단
- 성공 → accent 배지 3초 후 자동 제거
- 실패 → 에러 토스트 + isDirty 유지

### 에지 케이스
- 저장 중 네트워크 끊김 → 타임아웃 후 실패 처리
- 연속 클릭 방지: isSaving=true 동안 버튼 disabled

### 연결 화면
- 성공: `02-정상` (3초 후)
- 실패: `03-변경됨` (에러 토스트)
