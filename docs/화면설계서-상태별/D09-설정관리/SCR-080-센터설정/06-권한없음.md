# SCR-080 센터설정 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-080 |
| 상태 코드 | `no-auth` |
| 경로 | `/settings` |
| 역할 | 권한 없는 사용자 (fc, staff, readonly) |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 없음 (접근 불가) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-080 센터설정 — 상태: 권한없음 (접근 불가 메시지)

파일: src/app/settings/page.tsx

권한 체크 로직:
- useAuthStore().user.role
- hasPermission(role, 'settings:read') 함수로 체크
- primary, owner만 접근 허용

권한없음 UI:
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <div className="w-16 h-16 rounded-full bg-gray-100 flex items-center justify-center">
    <Lock className="w-8 h-8 text-gray-400" />
  </div>
  <div className="text-center">
    <p className="text-lg font-semibold text-gray-700">접근 권한이 없습니다</p>
    <p className="mt-1 text-sm text-gray-400">센터 설정은 최고관리자 또는 원장만 접근할 수 있습니다.</p>
  </div>
  <Button variant="outline" onClick={() => moveToPage('/dashboard')}>
    대시보드로 돌아가기
  </Button>
</div>

진입 시 권한 체크:
- useEffect(() => {
    if (!hasPermission(user.role, 'settings:read')) {
      setNoAuth(true)
    }
  }, [user.role])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 403 응답 또는 클라이언트 사이드 권한 체크 실패
- 이벤트: `E_F6_080_06` (권한 없음)

### UI 구성
| 요소 | 내용 |
|------|------|
| 아이콘 | Lock, gray-400, bg-gray-100 원형 |
| 주 메시지 | "접근 권한이 없습니다" |
| 부 메시지 | "센터 설정은 최고관리자 또는 원장만 접근할 수 있습니다." |
| CTA | "대시보드로 돌아가기" |

### 비즈니스 룰
- RBAC: primary, owner만 settings:read 권한 보유
- 권한 없는 역할이 직접 URL 접근 시에도 동일 처리
- 사이드바 메뉴에서도 설정 메뉴 숨김 처리

### 에지 케이스
- 세션 만료 후 재접속 → 로그인 페이지로 리다이렉트

### 연결 화면
- CTA: `/dashboard`
