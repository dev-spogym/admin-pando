# SCR-080 센터설정 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-080 |
| 상태 코드 | `error` |
| 경로 | `/settings` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-080 센터설정 — 상태: 에러 (API 실패, 재시도 버튼)

파일: src/app/settings/page.tsx

에러 상태 UI (fetchError가 true일 때 렌더):
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <div className="w-16 h-16 rounded-full bg-red-100 flex items-center justify-center">
    <AlertCircle className="w-8 h-8 text-red-500" />
  </div>
  <div className="text-center">
    <p className="text-lg font-semibold text-gray-900">설정을 불러오지 못했습니다</p>
    <p className="mt-1 text-sm text-gray-500">잠시 후 다시 시도해 주세요.</p>
  </div>
  <Button
    variant="outline"
    onClick={handleRetry}
    className="mt-2"
  >
    <RefreshCw className="mr-2 h-4 w-4" />
    다시 시도
  </Button>
</div>

handleRetry:
- setFetchError(false)
- setIsLoading(true)
- fetchSettings() 재호출
- 이벤트: E_F6_080_14

에러 감지:
- const { data, error } = await supabase.from('settings')...
- if (error) setFetchError(true)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- settings API 호출 실패 (네트워크 오류, 500 등)
- 이벤트: `E_F6_080_05` (API 에러)

### UI 구성
| 요소 | 내용 |
|------|------|
| 아이콘 | AlertCircle, red-500, w-16 h-16 원형 bg-red-100 |
| 주 메시지 | "설정을 불러오지 못했습니다" |
| 부 메시지 | "잠시 후 다시 시도해 주세요." |
| CTA 버튼 | "다시 시도" (variant=outline, RefreshCw 아이콘) |

### 비즈니스 룰
- TC-080-NEG-003: API 에러 → 에러 상태 + 재시도 버튼
- 재시도 버튼 클릭 → 로딩 상태로 복귀 후 API 재호출
- 에러 상태에서 탭 전환 불가 (폼 없음)

### 에지 케이스
- 재시도 중 다시 실패 → 동일 에러 화면 유지
- 재시도 성공 → 정상 상태로 전환

### 연결 화면
- 재시도: `01-로딩`
