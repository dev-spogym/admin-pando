# DLG-080-001 미저장 경고 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-080-001 |
| 상태 코드 | `open` |
| 트리거 화면 | SCR-080 |
| variant | warning |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-저장후이동` / `04-폐기후이동` / `01-닫힘` (취소) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 Modal 컴포넌트를 작성하라.
모달: DLG-080-001 미저장 경고 — 상태: 열림 (warning variant)

파일: src/app/settings/page.tsx (모달 포함)

<Modal open={showUnsavedModal} onOpenChange={(v) => !v && handleModalClose()}>
  <ModalContent className="max-w-md">
    <ModalHeader>
      <div className="flex items-center gap-2">
        <AlertTriangle className="w-5 h-5 text-amber-500" />
        <ModalTitle>저장되지 않은 변경사항</ModalTitle>
      </div>
    </ModalHeader>
    <ModalBody>
      <p className="text-sm text-gray-600">
        변경사항이 저장되지 않았습니다. 저장 후 이동하시겠습니까?
      </p>
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <!-- 취소: 모달 닫기, isDirty 유지 -->
      <Button variant="ghost" onClick={handleCancelModal}>
        취소
      </Button>
      <!-- 저장 안 함: 변경 폐기 후 탭 이동 -->
      <Button variant="outline" onClick={handleDiscardAndMove}>
        저장 안 함
      </Button>
      <!-- 저장 후 이동: PATCH 후 탭 이동 -->
      <Button variant="default" onClick={handleSaveAndMove}>
        저장 후 이동
      </Button>
    </ModalFooter>
  </ModalContent>
</Modal>

핸들러:
handleCancelModal: () => setShowUnsavedModal(false)  // isDirty 유지
handleDiscardAndMove: () => {
  setForm(savedData)     // 변경 폐기
  setIsDirty(false)
  setShowUnsavedModal(false)
  setActiveTab(pendingTab)  // 탭 이동
}
handleSaveAndMove: async () => → 03-저장후이동 상태
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- isDirty=true 상태에서 탭 전환 시도
- 이벤트: `Closed → Open`

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | warning |
| title | 저장되지 않은 변경사항 |
| description | 변경사항이 저장되지 않았습니다. 저장 후 이동하시겠습니까? |
| 버튼1 | 취소 (ghost) |
| 버튼2 | 저장 안 함 (outline) |
| 버튼3 | 저장 후 이동 (default/primary) |

### 비즈니스 룰
- 취소: 모달 닫기, 현재 탭 유지, isDirty 유지
- 저장 안 함: 변경 폐기 → 탭 이동
- 저장 후 이동: PATCH /settings → 탭 이동

### 연결 화면
- 저장 후 이동: `03-저장후이동`
- 저장 안 함 / 취소: `01-닫힘`
