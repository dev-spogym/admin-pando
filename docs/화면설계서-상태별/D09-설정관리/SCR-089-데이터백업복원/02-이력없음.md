# SCR-089 데이터백업복원 — 상태: 백업 이력 없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-089 |
| 상태 코드 | `no-history` |
| 경로 | `/settings/backup` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-이력있음` (첫 백업 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-089 데이터백업복원 — 상태: 백업 이력 없음 + 지금 백업 CTA (TC-089-006)

파일: src/app/settings/backup/page.tsx

자동 백업 카드 (상단 항상 표시):
<div className="p-5 rounded-xl border bg-white flex items-center justify-between">
  <div>
    <p className="font-semibold text-gray-900">자동 백업</p>
    <p className="text-sm text-gray-500 mt-0.5">
      {settings.auto_backup_enabled
        ? `매 ${settings.backup_interval} · ${settings.backup_time}`
        : '자동 백업 비활성화됨'}
    </p>
  </div>
  <div className="flex items-center gap-3">
    <Button variant="outline" size="sm" onClick={openBackupSettings}>설정</Button>
    <Button size="sm" onClick={runManualBackup}>지금 백업</Button>
  </div>
</div>

백업 이력 없음 (E_F6_089_04, TC-089-006):
<div className="flex flex-col items-center justify-center py-16 gap-4">
  <div className="w-16 h-16 rounded-full bg-gray-100 flex items-center justify-center">
    <Archive className="w-8 h-8 text-gray-300" />
  </div>
  <div className="text-center">
    <p className="text-base font-semibold text-gray-600">백업 이력이 없습니다</p>
    <p className="text-sm text-gray-400 mt-1">지금 백업을 실행하면 여기에 이력이 표시됩니다.</p>
  </div>
  <Button onClick={runManualBackup}>
    <Download className="mr-2 h-4 w-4" />
    지금 백업 실행
  </Button>
</div>

자동 백업 OFF 경고 (E_F6_089_09):
{!settings.auto_backup_enabled && (
  <div className="flex items-center gap-2 px-4 py-3 bg-amber-50 border border-amber-200 rounded-lg mb-4 text-sm text-amber-700">
    <AlertTriangle className="w-4 h-4 shrink-0" />
    자동 백업이 비활성화되어 있습니다. 데이터 보호를 위해 활성화를 권장합니다.
  </div>
)}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- backup_history = []
- 이벤트: `E_F6_089_04`
- TC-089-006: 백업 이력 없음 → 빈 상태 + 지금 백업 CTA

### UI 구성
| 요소 | 내용 |
|------|------|
| 자동 백업 카드 | 항상 표시 (설정/지금 백업 버튼) |
| 빈 메시지 | Archive 아이콘 + "지금 백업 실행" CTA |
| OFF 경고 배너 | amber-50, 자동 백업 비활성 시 |

### 비즈니스 룰
- TC-089-006: 이력 없음 → 빈 상태 + CTA
- 자동 백업 OFF → amber 경고 배너 (이벤트: `E_F6_089_09`)
- 지금 백업 → 즉시 실행 후 이력 생성

### 연결 화면
- 백업 설정: DLG-089-002
- 백업 실행 후: `03-이력있음`
