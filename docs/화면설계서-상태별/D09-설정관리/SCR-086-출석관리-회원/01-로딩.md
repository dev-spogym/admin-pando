# SCR-086 출석관리(회원) — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-086 |
| 상태 코드 | `loading` |
| 경로 | `/attendance` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-빈상태` / `03-출석목록` / `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-086 출석관리(회원) — 상태: 로딩

파일: src/app/attendance/page.tsx

Skeleton (animate-pulse):
- PageHeader: "출석 관리"
- 날짜 필터: h-9 w-40 bg-gray-200 rounded-md
- 통계 카드 3개 (flex gap-4): h-20 bg-gray-200 rounded-xl
- 출석 테이블:
  · 헤더: h-10 bg-gray-200 rounded
  · 행 8개: h-14 bg-gray-200 rounded

데이터 페치:
- selectedDate: new Date() (오늘)
- supabase.from('attendance')
    .select('*, members(name, phone, avatar_url)')
    .eq('branch_id', branchId)
    .gte('checked_in_at', startOfDay(selectedDate).toISOString())
    .lte('checked_in_at', endOfDay(selectedDate).toISOString())
    .order('checked_in_at', { ascending: false })
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 이벤트: `E_F6_086_01`
- 기본 날짜: 오늘

### 연결 화면
- records=[] → `02-빈상태`
- 데이터 있음 → `03-출석목록`
- 실패 → `05-에러`
