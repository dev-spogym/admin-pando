# SCR-086 출석관리(회원) — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-086 |
| 상태 코드 | `empty` |
| 경로 | `/attendance` |
| 역할 | primary, owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-출석목록` (날짜 변경 또는 체크인 발생) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-086 출석관리(회원) — 상태: 빈 상태 (TC-086-007)

파일: src/app/attendance/page.tsx

빈 상태 UI (records.length === 0):
- 날짜 필터: 표시 유지 (날짜 변경 가능)
- 통계 카드: 모두 0 표시

<div className="flex flex-col items-center justify-center py-20 gap-3">
  <div className="w-16 h-16 rounded-full bg-gray-100 flex items-center justify-center">
    <UserX className="w-8 h-8 text-gray-300" />
  </div>
  <p className="text-base font-semibold text-gray-600">
    {format(selectedDate, 'M월 d일')} 출석 기록이 없습니다
  </p>
  <p className="text-sm text-gray-400">해당 날짜에 체크인한 회원이 없습니다.</p>
</div>

날짜 필터 컴포넌트:
<div className="flex items-center gap-2 mb-6">
  <Button variant="ghost" size="icon" onClick={prevDay}><ChevronLeft /></Button>
  <DatePicker value={selectedDate} onChange={handleDateChange} />
  <Button variant="ghost" size="icon" onClick={nextDay}><ChevronRight /></Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 선택 날짜 attendance 레코드 = []
- 이벤트: `E_F6_086_04`
- TC-086-007: 해당 날짜 출석 없음 → 빈 상태

### UI 구성
| 요소 | 내용 |
|------|------|
| 날짜 필터 | 유지 (이전/다음 이동) |
| 통계 카드 | 0 표시 |
| 빈 메시지 | "{날짜} 출석 기록이 없습니다" |

### 비즈니스 룰
- 날짜 변경 시 해당 날짜 재조회
- 오늘 날짜에서 미래 날짜 이동 제한 (today = max)

### 연결 화면
- 날짜 변경: `03-출석목록` 또는 `02-빈상태`
