# SCR-084 구독결제관리 — 상태: 사용량 경고 (80~99%)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-084 |
| 상태 코드 | `usage-warn` |
| 경로 | `/subscription` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `02-정상-사용량정상` |
| 다음 상태 | `04-사용량초과` (100% 도달) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-084 구독결제관리 — 상태: 사용량 경고 (80~99%, amber 게이지)

파일: src/app/subscription/page.tsx

사용량 80~99% 게이지 (E_F6_084_05):
- 게이지 색상: bg-amber-500
- 해당 행 우측에 경고 뱃지:
  <span className="text-xs text-amber-600 font-medium">⚠ {pct}%</span>

업그레이드 권고 배너 (사용률 80% 이상 항목 존재 시):
<div className="flex items-center justify-between px-4 py-3 bg-amber-50 border border-amber-200 rounded-lg">
  <div className="flex items-center gap-2">
    <AlertTriangle className="w-4 h-4 text-amber-500" />
    <p className="text-sm text-amber-800">
      사용량이 한도에 근접했습니다. 플랜 업그레이드를 권장합니다.
    </p>
  </div>
  <Button size="sm" variant="outline" className="border-amber-300 text-amber-700" onClick={openPlanChange}>
    플랜 업그레이드
  </Button>
</div>

게이지 색상 분기:
const getGaugeColor = (pct: number) => {
  if (pct >= 100) return 'bg-red-500'
  if (pct >= 80) return 'bg-amber-500'
  return 'bg-blue-500'
}
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 하나 이상의 항목 사용률 80~99%
- 이벤트: `E_F6_084_05`

### UI 변화
| 요소 | 경고 표현 |
|------|----------|
| 게이지 | bg-amber-500 |
| 퍼센트 배지 | ⚠ {pct}% amber 텍스트 |
| 업그레이드 배너 | amber-50 bg, 플랜 업그레이드 버튼 |

### 비즈니스 룰
- 80% 이상 항목이 하나라도 있으면 배너 표시
- 플랜 업그레이드 버튼 → DLG-084-001

### 연결 화면
- 플랜 변경: DLG-084-001
- 100% 도달: `04-사용량초과`
