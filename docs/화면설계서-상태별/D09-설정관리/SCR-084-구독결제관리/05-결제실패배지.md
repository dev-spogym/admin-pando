# SCR-084 구독결제관리 — 상태: 결제 실패 이력 배지

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-084 |
| 상태 코드 | `pay-failed` |
| 경로 | `/subscription` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상-사용량정상` |
| 다음 상태 | `02-정상-사용량정상` (결제 성공 후) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-084 구독결제관리 — 상태: 결제 실패 이력 배지 (E_F6_084_07)

파일: src/app/subscription/page.tsx

결제 이력 테이블에서 실패 행:
<tr className="bg-red-50/50">
  <td>{format(payment.created_at, 'yyyy.MM.dd')}</td>
  <td>{payment.amount.toLocaleString()}원</td>
  <td>
    <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs bg-red-100 text-red-700 font-medium">
      <XCircle className="w-3 h-3" />
      결제 실패
    </span>
  </td>
  <td>
    <Button size="sm" variant="ghost" className="text-primary" onClick={() => retryPayment(payment.id)}>
      재결제
    </Button>
  </td>
</tr>

플랜 카드 하단 경고 (실패 이력 존재 시):
<div className="flex items-center gap-2 mt-3 px-3 py-2 bg-red-50 rounded-lg text-xs text-red-600">
  <AlertCircle className="w-3.5 h-3.5" />
  최근 결제 실패가 있습니다. 카드 정보를 확인해 주세요.
  <Button variant="link" size="sm" className="text-xs text-red-700 p-0 h-auto ml-auto">카드 변경</Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- payment_history에 status='failed' 레코드 존재
- 이벤트: `E_F6_084_07`

### UI 변화
| 요소 | 실패 표현 |
|------|----------|
| 실패 행 | bg-red-50/50 배경 |
| 상태 배지 | bg-red-100 text-red-700 "결제 실패" |
| 재결제 버튼 | ghost, text-primary |
| 플랜 카드 경고 | red-50 인라인, 카드 변경 링크 |

### 비즈니스 룰
- 결제 실패 후 3일 내 재결제 안 하면 구독 일시 중단
- 재결제 성공 시 배지 자동 제거

### 연결 화면
- 카드 변경 → 결제 수단 변경 페이지
