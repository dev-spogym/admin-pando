# SCR-084 구독결제관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-084 |
| 상태 코드 | `loading` |
| 경로 | `/subscription` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-084 구독결제관리 — 상태: 로딩

파일: src/app/subscription/page.tsx

Skeleton (animate-pulse, space-y-6):
플랜 카드: h-40 w-full bg-gray-200 rounded-xl
사용량 섹션:
  - 헤더: h-5 w-24 bg-gray-200 rounded
  - 게이지 4개: h-6 w-full bg-gray-200 rounded-full
결제 이력 테이블:
  - 헤더: h-8 bg-gray-200 rounded
  - 행 5개: h-12 bg-gray-200 rounded

데이터 페치:
Promise.all([
  supabase.from('subscriptions').select('*').eq('branch_id', branchId).single(),
  supabase.from('subscription_usage').select('*').eq('branch_id', branchId).single(),
  supabase.from('payment_history').select('*').eq('branch_id', branchId).order('created_at', {ascending: false}).limit(20)
])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-084 진입 시 구독/사용량/결제이력 병렬 로딩
- 이벤트: `E_F6_084_01`

### 연결 화면
- 성공: `02-정상`
- 실패: `06-에러`
