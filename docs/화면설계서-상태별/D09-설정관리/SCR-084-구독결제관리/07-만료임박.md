# SCR-084 구독결제관리 — 상태: 구독 만료 임박 (D-7 이내)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-084 |
| 상태 코드 | `expire-warn` |
| 경로 | `/subscription` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `02-정상-사용량정상` |
| 다음 상태 | `08-만료됨` (만료 시) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-084 구독결제관리 — 상태: 만료 임박 배너 (D-7 이내 amber)

파일: src/app/subscription/page.tsx

만료 임박 배너 (E_F6_084_08, D-7 이내):
const daysLeft = differenceInDays(new Date(plan.expires_at), new Date())

{daysLeft <= 7 && (
  <div className="flex items-center gap-3 px-4 py-3 bg-amber-50 border border-amber-200 rounded-lg">
    <Clock className="w-5 h-5 text-amber-500 shrink-0" />
    <div>
      <p className="text-sm font-medium text-amber-800">
        구독이 <strong>D-{daysLeft}</strong>에 만료됩니다
      </p>
      <p className="text-xs text-amber-600 mt-0.5">
        {format(new Date(plan.expires_at), 'yyyy년 M월 d일')} 만료 예정
      </p>
    </div>
    <Button size="sm" variant="outline" className="ml-auto border-amber-300 text-amber-700" onClick={openPlanChange}>
      플랜 갱신
    </Button>
  </div>
)}

플랜 카드 만료일 표시:
- 만료 7일 이내: text-amber-600 font-semibold + Clock 아이콘
- 정상: text-gray-500
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 구독 만료일까지 7일 이내
- 이벤트: `E_F6_084_08`

### UI 변화
| 요소 | 임박 표현 |
|------|----------|
| 배너 | amber-50 bg, Clock 아이콘, D-N 표시 |
| 만료일 | text-amber-600 + Clock 아이콘 |
| 갱신 버튼 | border-amber-300 |

### 비즈니스 룰
- D-7 이내 amber 배너
- D-0 (당일) → `08-만료됨`

### 연결 화면
- 갱신: DLG-084-001
- 만료: `08-만료됨`
