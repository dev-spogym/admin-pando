# SCR-083 IoT출입관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-083 |
| 상태 코드 | `loading` |
| 경로 | `/settings/iot` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-083 IoT출입관리 — 상태: 로딩

파일: src/app/settings/iot/page.tsx

레이아웃:
- AppLayout + PageHeader "IoT 출입 관리"
- 2컬럼 (좌: 장비 목록, 우: 출입 이력)

Skeleton (animate-pulse):
좌측 장비 패널:
  - 헤더: h-5 w-24 bg-gray-200 rounded
  - 장비 카드 3개: h-20 w-full bg-gray-200 rounded-xl

우측 이력 패널:
  - 헤더 + 날짜 필터: h-9 w-full bg-gray-200 rounded
  - 이력 행 6개: h-12 w-full bg-gray-200 rounded

데이터 페치:
Promise.all([
  supabase.from('iot_devices').select('*').eq('branch_id', branchId),
  supabase.from('attendance').select('*').eq('branch_id', branchId).order('checked_in_at', { ascending: false }).limit(50)
])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-083 진입 시 IoT 장비 + 출입 이력 병렬 로딩
- 이벤트: `E_F6_083_01`

### 연결 화면
- 성공: `02-정상`
- 실패: `06-에러`
