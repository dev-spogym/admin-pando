# SCR-083 IoT/출입 관리 — 상태 01: 로딩

> 상속: [`00-기본화면.md`](./00-기본화면.md) — **델타**만 기술.

## 메타
| 항목 | 값 |
|---|---|
| 상태 코드 | `iot-loading` |
| 이전 | 진입 / BranchSwitcher |
| 다음 | `02-정상-장비온라인` / `03-장비오프라인` / `05-IoT연결끊김` / `06-에러` / `07-장비없음` |

## 진입 조건
- `useQueries([devices, policies, env, thresholds, accessLogs])` pending

## 비주얼 델타
| 요소 | 변경 |
|---|---|
| 장비 카드 | 3개 `h-20 bg-gray-200 animate-pulse rounded-xl` |
| 정책 토글 | 4행 `h-14 bg-gray-200` |
| 환경 3종 | `h-16 bg-gray-200` |
| 이력 테이블 | 헤더 `h-10` + 행 10개 `h-12 bg-gray-200` |
| 모든 버튼 | disabled |

## 비즈니스 룰
- 병렬 쿼리 5개
- 10초 타임아웃 → `06-에러`
- WebSocket 연결 실패 → polling fallback 활성

## 에지 케이스
- 빠른 응답(<150ms) 스켈레톤 플래시 방지
- device 쿼리만 200, logs 5xx → 장비 패널 렌더 + 이력 영역만 에러

## 🧩 바이브코딩 프롬프트 (델타)

```
상속: 00-기본화면.md §14

<div className="grid lg:grid-cols-5 gap-6" role="status" aria-busy="true" aria-label="IoT 로딩 중">
  <section className="lg:col-span-2 space-y-4">
    {Array.from({length:3}).map((_,i) => (
      <div key={i} className="h-20 bg-gray-200 rounded-xl animate-pulse" />
    ))}
    {Array.from({length:4}).map((_,i) => (
      <div key={i} className="h-14 bg-gray-200 rounded-xl animate-pulse" />
    ))}
  </section>
  <section className="lg:col-span-3 space-y-2">
    <div className="h-10 bg-gray-200 rounded animate-pulse" />
    {Array.from({length:10}).map((_,i) => (
      <div key={i} className="h-12 bg-gray-200 rounded animate-pulse" />
    ))}
  </section>
</div>

useQueries:
const [devicesQ, policiesQ, envQ, thresholdsQ, logsQ] = useQueries({
  queries: [
    { queryKey:['devices',branchId], queryFn: () => fetchDevices(branchId) },
    { queryKey:['policies',branchId], queryFn: () => fetchPolicies(branchId) },
    { queryKey:['env',branchId], queryFn: () => fetchEnv(branchId), refetchInterval:10_000 },
    { queryKey:['envThresholds',branchId], queryFn: () => fetchThresholds(branchId) },
    { queryKey:['accessLogs',branchId,date], queryFn: () => fetchAccessLogs(branchId,date) },
  ]
});
```

## TC 후보
| TC | 타입 | Given | When | Then |
|---|---|---|---|---|
| TC-083-01-01 | positive | 진입 | render | 2컬럼 스켈레톤 |
| TC-083-01-02 | negative | 10초 | timeout | 06-에러 |
| TC-083-01-03 | edge | 빠른 응답 | — | 스켈레톤 플래시 없음 |

## 다이어그램
- F1 진입 / F6 STATE_LOADING
