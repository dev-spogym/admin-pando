# SCR-083 IoT출입관리 — 상태: 환경 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-083 |
| 상태 코드 | `env-warning` |
| 경로 | `/settings/iot` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상-장비온라인` |
| 다음 상태 | `02-정상-장비온라인` (임계값 정상화) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-083 IoT출입관리 — 상태: 환경 임계값 초과 경고 배너 (TC-083-008)

파일: src/app/settings/iot/page.tsx

환경 경고 배너 (E_F6_083_08, 페이지 상단):
<div className="flex items-start gap-3 px-4 py-3 bg-amber-50 border border-amber-200 rounded-lg mb-6">
  <Thermometer className="w-5 h-5 text-amber-500 shrink-0 mt-0.5" />
  <div className="flex-1">
    <p className="text-sm font-medium text-amber-800">환경 임계값 초과 감지</p>
    <div className="mt-1 flex gap-4 text-xs text-amber-700">
      {envAlerts.temperature && (
        <span>온도: {currentTemp}°C (기준: {thresholdTemp}°C 초과)</span>
      )}
      {envAlerts.humidity && (
        <span>습도: {currentHumidity}% (기준: {thresholdHumidity}% 초과)</span>
      )}
    </div>
  </div>
  <Button variant="ghost" size="sm" onClick={dismissAlert}>닫기</Button>
</div>

환경 데이터 모니터링:
- IoT 장비에서 온도/습도 센서 데이터 수신 (30초 폴링)
- threshold: settings.env_temp_max, settings.env_humidity_max
- 초과 시 setEnvAlerts({ temperature: true, humidity: true })

TC-083-008: 환경 온도 임계값 초과 → 경고 배너
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 온도 또는 습도가 설정 임계값 초과
- 이벤트: `E_F6_083_08`

### UI 구성
| 요소 | 표현 |
|------|------|
| 배너 | amber-50 bg, Thermometer 아이콘 |
| 온도 초과 | 현재값 vs 기준값 표시 |
| 습도 초과 | 현재값 vs 기준값 표시 |
| 닫기 | ghost 버튼 (일시 해제) |

### 비즈니스 룰
- TC-083-008: 환경 온도 임계값 초과 → 경고 배너
- 임계값 정상화 시 자동 배너 제거 (실시간 감지)
- 닫기는 일시 해제 (30분 후 재표시)

### 연결 화면
- 정상화: `02-정상-장비온라인`
