# DLG-084-002 구독 해지 확인 — 상태: 열림 ("해지" 타이핑)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-084-002 |
| 상태 코드 | `open-typing` |
| 트리거 화면 | SCR-084 |
| variant | danger |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-해지완료` ("해지" 일치 + 클릭) / `01-닫힘` (취소) |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-084-002 구독 해지 확인 — "해지" 타이핑

<Modal open={showCancelModal}>
  <ModalContent className="max-w-md">
    <ModalHeader>
      <div className="flex items-center gap-2">
        <XCircle className="w-5 h-5 text-red-500" />
        <ModalTitle>구독 해지</ModalTitle>
      </div>
    </ModalHeader>
    <ModalBody className="space-y-4">
      <div className="px-4 py-3 bg-red-50 border border-red-100 rounded-lg">
        <p className="text-sm text-red-700 font-medium">해지 시 주의사항</p>
        <ul className="mt-1 text-xs text-red-600 space-y-1 list-disc list-inside">
          <li>현재 결제 기간 종료 후 서비스가 중단됩니다</li>
          <li>데이터는 30일간 보관 후 삭제됩니다</li>
          <li>해지 후 재구독 시 데이터 복구 가능합니다</li>
        </ul>
      </div>
      <p className="text-sm text-gray-600">
        해지를 확인하려면 <strong>"해지"</strong>를 입력해 주세요.
      </p>
      <Input
        value={confirmText}
        onChange={e => setConfirmText(e.target.value)}
        placeholder='"해지" 입력'
      />
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <Button variant="outline" onClick={() => setShowCancelModal(false)}>취소</Button>
      <Button
        variant="destructive"
        disabled={confirmText !== '해지'}
        onClick={handleCancel}
      >해지 확인</Button>
    </ModalFooter>
  </ModalContent>
</Modal>

handleCancel: async () => {
  const res = await fetch('/api/subscription/cancel', { method: 'POST' })
  if (!res.ok) { toast.error('해지 처리 실패'); return }
  const { endDate } = await res.json()
  onSubscriptionCancelled(endDate)
  setShowCancelModal(false)
  toast.warning(`구독이 해지되었습니다. ${format(endDate, 'yyyy.MM.dd')}까지 이용 가능합니다.`)
}
```

## 📝 디스크립션

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | danger |
| confirmationText | "해지" |

### 결과 분기 (M3_084002)
| 결과 | 처리 |
|------|------|
| 200 (M3_084002_02) | 종료일 표시 + toast.warning |
| 500 (M3_084002_03) | toast.error + 모달 유지 |
