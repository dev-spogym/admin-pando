# SCR-088 다국어설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-088 |
| 상태 코드 | `loading` |
| 경로 | `/settings/i18n` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-기본번역만` / `03-커스텀번역있음` / `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-088 다국어설정 — 상태: 로딩

파일: src/app/settings/i18n/page.tsx

Skeleton (animate-pulse, space-y-4):
- 언어 선택 탭: 3개 언어 탭 h-9 w-16 bg-gray-200 rounded
- 번역 키 테이블:
  · 헤더: h-10 bg-gray-200 rounded
  · 행 8개: h-12 bg-gray-200 rounded

데이터 페치:
Promise.all([
  fetch('/api/i18n/default'),   // 기본 번역 키/값
  supabase.from('i18n_custom').select('*').eq('branch_id', branchId)
])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 이벤트: `E_F6_088_01`

### 연결 화면
- 커스텀 번역 없음: `02-기본번역만`
- 있음: `03-커스텀번역있음`
- 실패: `06-에러`
