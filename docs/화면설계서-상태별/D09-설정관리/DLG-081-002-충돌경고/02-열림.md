# DLG-081-002 충돌 경고 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-081-002 |
| 상태 코드 | `open` |
| 트리거 화면 | SCR-081 |
| variant | warning |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-무시저장` / `01-닫힘` (수정/취소) |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-081-002 충돌 경고 — 열림

<Modal open={showConflictModal}>
  <ModalContent className="max-w-md">
    <ModalHeader>
      <div className="flex items-center gap-2">
        <AlertTriangle className="w-5 h-5 text-amber-500" />
        <ModalTitle>권한 충돌 감지</ModalTitle>
      </div>
    </ModalHeader>
    <ModalBody className="space-y-3">
      <p className="text-sm text-gray-600">
        삭제 권한이 활성화되었으나 수정 권한이 비활성화되어 있습니다. 이 설정은 논리적으로 충돌합니다.
      </p>
      <!-- 충돌 항목 목록 -->
      <div className="space-y-1">
        {conflicts.map(c => (
          <div key={`${c.role}-${c.feature}`} className="flex items-center gap-2 text-xs text-amber-700 bg-amber-50 px-3 py-2 rounded">
            <AlertTriangle className="w-3.5 h-3.5 shrink-0" />
            {c.role} · {c.feature}: 삭제 ON / 쓰기 OFF
          </div>
        ))}
      </div>
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <Button variant="outline" onClick={handleFixConflict}>돌아가서 수정</Button>
      <Button variant="default" onClick={handleForceSave}>무시하고 저장</Button>
    </ModalFooter>
  </ModalContent>
</Modal>

handleFixConflict: () => {
  // M3_081002_02: 모달 닫기 + 충돌 항목 하이라이트
  setShowConflictModal(false)
  setConflictCells(new Set(conflicts.map(c => `${c.role}-${c.feature}-delete`)))
}
handleForceSave: async () => → 03-무시저장
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 저장 시 충돌 감지 (삭제 ON + 쓰기 OFF)
- 이벤트: `Closed → Open`

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | warning |
| title | 권한 충돌 감지 |
| 확인 | "무시하고 저장" |
| 취소 | "돌아가서 수정" |

### 결과 분기
| 선택 | 처리 |
|------|------|
| 무시하고 저장 (M3_081002_01) | localStorage 저장 → `03-무시저장` |
| 돌아가서 수정 (M3_081002_02) | 모달 닫기 + 충돌 셀 하이라이트 |
| 취소 (M3_081002_03) | 모달 닫기 + 변경사항 유지 |
