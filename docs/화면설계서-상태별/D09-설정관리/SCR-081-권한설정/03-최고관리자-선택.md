# SCR-081 권한설정 — 상태: 최고관리자 선택 (수정 불가)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `primary-selected` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상-역할선택` |
| 다음 상태 | `02-정상-역할선택` (다른 역할 선택) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한설정 — 상태: 최고관리자(primary) 선택 시 수정 불가 UI

파일: src/app/settings/permissions/page.tsx

최고관리자 선택 상태 (selectedRole === 'primary'):
- 매트릭스 상단 안내 배너:
  <div className="flex items-center gap-2 px-4 py-3 bg-gray-50 rounded-lg border border-gray-200 text-sm text-gray-500 mb-4">
    <ShieldCheck className="w-4 h-4 text-gray-400" />
    최고관리자는 모든 권한을 보유하며 변경할 수 없습니다.
  </div>

- 모든 체크박스:
  · checked={true}
  · disabled={true}
  · className="cursor-default opacity-75"
  · 클릭 이벤트 차단

- 저장/초기화 버튼: disabled={true}, opacity-50

- TC-081-002: cursor-default, 변경 불가 확인

역할 패널에서 primary 항목:
- 우측에 ShieldCheck 아이콘 (text-primary)
- "최고관리자" 라벨 옆 "(고정)" 부제
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 좌측 역할 목록에서 "최고관리자(primary)" 선택
- 이벤트: `E_F6_081_04`

### UI 특이사항
| 요소 | 처리 |
|------|------|
| 모든 체크박스 | checked=true, disabled, cursor-default |
| 저장 버튼 | disabled, opacity-50 |
| 초기화 버튼 | disabled, opacity-50 |
| 안내 배너 | gray-50 배경, ShieldCheck 아이콘 |

### 비즈니스 룰
- TC-081-002: primary 선택 → cursor-default, 변경 불가
- 최고관리자는 시스템 보호 역할로 모든 권한 항상 on
- 권한 변경 시도 시 클릭 이벤트 완전 차단 (onChange 없음)

### 에지 케이스
- 키보드 탭으로 체크박스 포커스 가능하나 Space 키 이벤트 차단

### 연결 화면
- 다른 역할 클릭: `02-정상-역할선택` 또는 `04-편집`
