# SCR-081 권한설정 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `error` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한설정 — 상태: 에러 (API 실패, 재시도)

파일: src/app/settings/permissions/page.tsx

에러 UI (fetchError=true):
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <div className="w-16 h-16 rounded-full bg-red-100 flex items-center justify-center">
    <AlertCircle className="w-8 h-8 text-red-500" />
  </div>
  <div className="text-center">
    <p className="text-lg font-semibold text-gray-900">권한 데이터를 불러오지 못했습니다</p>
    <p className="mt-1 text-sm text-gray-500">역할 목록 또는 권한 정보를 가져오는 중 오류가 발생했습니다.</p>
  </div>
  <Button variant="outline" onClick={handleRetry}>
    <RefreshCw className="mr-2 h-4 w-4" />
    다시 시도
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 역할 목록 또는 권한 데이터 API 실패
- 이벤트: `E_F6_081_03`

### 비즈니스 룰
- Promise.all 중 하나라도 실패 시 에러 상태
- 재시도 → 두 API 모두 재호출

### 연결 화면
- 재시도: `01-로딩`
