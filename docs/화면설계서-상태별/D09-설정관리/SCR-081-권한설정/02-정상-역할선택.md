# SCR-081 권한설정 — 상태: 정상 (역할 선택)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `normal` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-최고관리자-선택` / `04-편집` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한설정 — 상태: 정상 (역할×기능 권한 매트릭스)

파일: src/app/settings/permissions/page.tsx

레이아웃 (flex gap-6):
좌측 역할 패널 (w-48 shrink-0):
  - 제목: "역할" (text-sm font-semibold text-gray-500 mb-2)
  - 역할 목록 (space-y-1):
    · 시스템 역할: primary, owner, manager, fc, staff, readonly
    · 커스텀 역할: custom_roles 테이블에서 로드
    · 선택된 항목: bg-primary/10 text-primary font-medium rounded-md px-3 py-2
    · 미선택: hover:bg-gray-100 rounded-md px-3 py-2 text-sm text-gray-700
  - 하단: "+ 역할 추가" Button (variant=ghost, onClick: DLG-081-003 열기)

우측 권한 매트릭스:
  - 상단 버튼 행 (justify-end gap-2):
    · "초기화" Button (variant=outline, isDirty=true 시 활성)
    · "저장" Button (variant=primary, isDirty=true 시 활성)
  
  - 매트릭스 테이블:
    행: 기능 (회원관리, 예약, 수업, 결제, 직원, 보고서, 설정, 공지)
    열: 읽기, 쓰기, 삭제
    셀: Checkbox (checked=permission[role][feature][action])
  
  - 최고관리자 선택 시: 모든 셀 checked=true, cursor-default

역할 선택 핸들러:
- handleRoleSelect(role):
    if (isDirty) → confirm(미저장 경고) 또는 DLG
    setSelectedRole(role)
    → primary 이면 PrimaryState, 아니면 EditState

데이터 구조:
type PermMatrix = Record<string, Record<string, { read: boolean; write: boolean; delete: boolean }>>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- API 성공 후 첫 번째 역할 자동 선택된 상태
- 이벤트: `E_F6_081_02` (로딩 성공)

### 매트릭스 구조
| 기능 | 읽기 | 쓰기 | 삭제 |
|------|------|------|------|
| 회원관리 | ✓/✗ | ✓/✗ | ✓/✗ |
| 예약 | ✓/✗ | ✓/✗ | ✓/✗ |
| 수업 | ✓/✗ | ✓/✗ | ✓/✗ |
| 결제 | ✓/✗ | ✓/✗ | ✓/✗ |
| 직원 | ✓/✗ | ✓/✗ | ✓/✗ |
| 보고서 | ✓/✗ | ✓/✗ | ✓/✗ |
| 설정 | ✓/✗ | ✓/✗ | ✓/✗ |
| 공지 | ✓/✗ | ✓/✗ | ✓/✗ |

### 비즈니스 룰
- TC-081-002: primary 선택 → cursor-default, 변경 불가
- 저장/초기화 버튼은 isDirty=true 시에만 활성
- 역할 전환 시 isDirty=true면 미저장 경고

### 연결 화면
- primary 선택: `03-최고관리자-선택`
- 기타 역할 선택: `04-편집`
- 역할 추가: DLG-081-003
