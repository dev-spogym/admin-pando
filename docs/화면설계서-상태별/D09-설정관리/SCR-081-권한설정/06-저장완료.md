# SCR-081 권한설정 — 상태: 저장완료

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `saved` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `05-저장중` |
| 다음 상태 | `02-정상-역할선택` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한설정 — 상태: 저장완료 (하이라이트 해제, isDirty=false)

파일: src/app/settings/permissions/page.tsx

저장 성공 후 처리 (E_F6_081_08):
- setSavedMatrix(deepClone(permMatrix))
- setChangedCells(new Set()) → 모든 amber 하이라이트 해제
- setIsDirty(false)
- toast.success('권한이 저장되었습니다.')

저장 완료 상태 UI:
- 매트릭스: 모든 amber ring 제거 (changedCells.size === 0)
- 저장 버튼: disabled + opacity-50 (isDirty=false)
- 초기화 버튼: disabled + opacity-50
- 매트릭스 인터랙션: 복구 (pointer-events-auto)

인라인 완료 표시 (선택적):
- 매트릭스 헤더 우측에 0.5초 동안:
  <span className="text-xs text-green-600 flex items-center gap-1">
    <CheckCircle2 className="w-3.5 h-3.5" /> 저장 완료
  </span>
  setTimeout(() => setShowSavedHint(false), 1500)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- PATCH /permissions 성공
- 이벤트: `E_F6_081_08` (저장 완료, isDirty=false, 하이라이트 해제)

### UI 변화
| 요소 | 저장완료 표현 |
|------|-------------|
| amber 하이라이트 | 전체 제거 |
| 저장/초기화 버튼 | disabled + opacity-50 |
| 토스트 | success "권한이 저장되었습니다." |
| 인라인 힌트 | CheckCircle2 green 1.5초 표시 |

### 비즈니스 룰
- savedMatrix와 permMatrix 동기화 완료
- 이후 체크박스 변경 시 다시 변경됨 상태로 전환 가능

### 연결 화면
- 자연스럽게: `02-정상-역할선택` (동일 UI)
