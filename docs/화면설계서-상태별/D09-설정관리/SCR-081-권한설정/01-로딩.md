# SCR-081 권한설정 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-081 |
| 상태 코드 | `loading` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` / `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-081 권한설정 — 상태: 로딩 (역할 목록 + 권한 매트릭스 Skeleton)

파일: src/app/settings/permissions/page.tsx

레이아웃:
- AppLayout + PageHeader "권한 설정"

Skeleton 구성 (animate-pulse):
좌측 역할 패널 (w-48):
  - 역할 헤더: h-5 w-20 bg-gray-200 rounded
  - 역할 항목 5개: h-9 w-full bg-gray-200 rounded-md (간격 gap-1)

우측 권한 매트릭스:
  - 헤더 행: h-8 w-full bg-gray-200 rounded (기능명 열)
  - 데이터 행 8개 반복:
    · 좌측 기능명: h-5 w-28 bg-gray-200 rounded
    · 체크박스 셀 5개: h-5 w-5 bg-gray-200 rounded (각 열)

우측 상단 버튼 영역:
  - h-9 w-20 bg-gray-200 rounded-md (초기화 버튼 자리)
  - h-9 w-20 bg-gray-200 rounded-md (저장 버튼 자리)

데이터 로딩:
- Promise.all([
    supabase.from('user_permissions').select('role, feature, can_read, can_write, can_delete'),
    supabase.from('custom_roles').select('*').eq('branch_id', branchId)
  ])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-081 최초 진입, 역할 목록 + 권한 데이터 API 호출 중
- 이벤트: `E_F6_081_01`

### UI 구성
| 영역 | Skeleton |
|------|---------|
| 좌측 역할 목록 | 5개 역할 행 h-9 |
| 매트릭스 헤더 | 전체 너비 h-8 |
| 매트릭스 행 | 8개 행 × 5열 체크박스 |
| 버튼 영역 | 2개 버튼 h-9 |

### 비즈니스 룰
- 역할 목록과 권한 데이터를 병렬로 fetch
- 로딩 중 모든 인터랙션 비활성화

### 연결 화면
- 성공: `02-정상`
- 실패: `05-에러`
