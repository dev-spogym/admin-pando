# DLG-089-002 백업 설정 — 상태: 열림 (폼 입력)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-089-002 |
| 상태 코드 | `open` |
| 트리거 화면 | SCR-089 |
| variant | primary |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-저장완료` / `01-닫힘` (취소) |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-089-002 백업 설정

<Modal open={showBackupSettingsModal}>
  <ModalContent className="max-w-lg">
    <ModalHeader><ModalTitle>백업 설정</ModalTitle></ModalHeader>
    <ModalBody className="space-y-5">
      <!-- 자동 백업 활성 (M2_089002_01~03) -->
      <div className="flex items-center justify-between">
        <div>
          <p className="text-sm font-medium">자동 백업</p>
          <p className="text-xs text-gray-400 mt-0.5">정기적으로 데이터를 자동 백업합니다</p>
        </div>
        <Toggle
          checked={form.autoBackup}
          onChange={v => setForm(p => ({...p, autoBackup: v}))}
        />
      </div>

      <!-- 자동 백업 ON일 때만 활성 (M2_089002_03~06) -->
      <div className={cn("space-y-4", !form.autoBackup && "opacity-40 pointer-events-none")}>
        <div>
          <label className="text-sm font-medium">백업 주기</label>
          <Select value={form.interval} onValueChange={v => setForm(p => ({...p, interval: v}))} className="mt-1">
            <option value="daily">매일</option>
            <option value="weekly">매주</option>
            <option value="monthly">매월</option>
          </Select>
        </div>
        <div>
          <label className="text-sm font-medium">백업 시간</label>
          <Input
            type="time"
            value={form.backupTime}
            onChange={e => setForm(p => ({...p, backupTime: e.target.value}))}
            className="mt-1"
          />
          {errors.time && <p className="text-xs text-red-500 mt-1">{errors.time}</p>}
        </div>
        <div>
          <label className="text-sm font-medium">보관 기간</label>
          <Select value={form.retentionDays} onValueChange={...} className="mt-1">
            <option value="7">7일</option>
            <option value="30">30일</option>
            <option value="90">90일</option>
            <option value="365">1년</option>
          </Select>
        </div>
        <div>
          <label className="text-sm font-medium">백업 범위</label>
          <div className="mt-1 space-y-2">
            {['전체', '회원 데이터', '설정', '결제 이력'].map(scope => (
              <label key={scope} className="flex items-center gap-2 text-sm">
                <input type="checkbox" checked={form.scopes.includes(scope)} onChange={...} />
                {scope}
              </label>
            ))}
          </div>
        </div>
        <div>
          <label className="text-sm font-medium">알림 수신자</label>
          <Select value={form.notifyRole} onValueChange={...} className="mt-1">
            <option value="owner">원장</option>
            <option value="primary">최고관리자</option>
          </Select>
        </div>
      </div>
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <Button variant="outline" onClick={() => setShowBackupSettingsModal(false)}>취소</Button>
      <Button onClick={handleSaveSettings}>저장</Button>
    </ModalFooter>
  </ModalContent>
</Modal>
```

## 📝 디스크립션

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | primary |
| title | 백업 설정 |
| fields | 자동백업(Toggle), 주기(Select), 시간(Input time), 보관기간(Select), 범위(Checkbox), 알림수신자(Select) |

### 필드 검증 (M2)
| 조건 | 처리 |
|------|------|
| 자동백업 OFF (M2_089002_02) | 주기/시간/보관기간 비활성 |
| 자동백업 ON (M2_089002_03) | 하위 필드 활성 |
| 시간 형식 오류 (M2_089002_06) | 인라인 에러 |

### 결과 분기 (M3_089002)
| 결과 | 처리 |
|------|------|
| 200 (M3_089002_02) | 자동백업 카드 업데이트 + toast.success |
| 400 (M3_089002_03) | 인라인 에러 |
| 500 (M3_089002_04) | toast.error |
