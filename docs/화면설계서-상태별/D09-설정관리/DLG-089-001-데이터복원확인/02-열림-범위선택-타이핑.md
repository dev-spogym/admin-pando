# DLG-089-001 데이터 복원 확인 — 상태: 열림 (범위 선택 + "복원" 타이핑)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-089-001 |
| 상태 코드 | `open` |
| 트리거 화면 | SCR-089 |
| variant | danger |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | `03-복원중` (복원 실행) / `01-닫힘` (취소) |

## 🧩 바이브코딩 프롬프트

```
모달: DLG-089-001 데이터 복원 확인 (단계: 범위선택 → 타이핑 확인)

<Modal open={showRestoreModal}>
  <ModalContent className="max-w-md">
    <ModalHeader>
      <div className="flex items-center gap-2">
        <RotateCcw className="w-5 h-5 text-red-500" />
        <ModalTitle>데이터 복원</ModalTitle>
      </div>
    </ModalHeader>
    <ModalBody className="space-y-4">
      <div className="px-4 py-3 bg-red-50 border border-red-100 rounded-lg">
        <p className="text-sm font-semibold text-red-800">⚠ 복원 주의사항</p>
        <ul className="mt-1 text-xs text-red-600 list-disc list-inside space-y-1">
          <li>{format(targetBackup.created_at, 'yyyy.MM.dd HH:mm')} 백업으로 복원합니다</li>
          <li>현재 데이터가 완전히 덮어씌워집니다</li>
          <li>이 작업은 되돌릴 수 없습니다</li>
        </ul>
      </div>
      <!-- 복원 범위 Select -->
      <div>
        <label className="text-sm font-medium">복원 범위</label>
        <Select value={restoreScope} onValueChange={setRestoreScope} className="mt-1">
          <option value="all">전체 데이터</option>
          <option value="members">회원 데이터만</option>
          <option value="settings">설정만</option>
        </Select>
      </div>
      <!-- "복원" 타이핑 확인 -->
      <div>
        <label className="text-sm font-medium">
          복원을 확인하려면 <strong>"복원"</strong>을 입력해 주세요.
        </label>
        <Input
          value={confirmText}
          onChange={e => setConfirmText(e.target.value)}
          placeholder='"복원" 입력'
          className="mt-1"
        />
      </div>
    </ModalBody>
    <ModalFooter className="flex justify-end gap-2">
      <Button variant="outline" onClick={() => setShowRestoreModal(false)}>취소</Button>
      <Button
        variant="destructive"
        disabled={confirmText !== '복원'}
        onClick={handleRestore}
      >복원 실행</Button>
    </ModalFooter>
  </ModalContent>
</Modal>
```

## 📝 디스크립션

### 모달 속성
| 항목 | 값 |
|------|-----|
| variant | danger |
| confirmationText | "복원" |
| fields | 복원 범위 Select |

### 결과 분기 (M3_089001)
| 결과 | 처리 |
|------|------|
| API 호출 → 진행률 표시 (M3_089001_02) | → `03-복원중` |
| 400 잘못된 백업 (M3_089001_04) | toast.error + 모달 유지 |
| 500 (M3_089001_05) | toast.error + 롤백 안내 |
