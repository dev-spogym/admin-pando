# SCR-087 커스텀역할생성 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-087 |
| 상태 코드 | `loading` |
| 경로 | `/settings/permissions` (커스텀 역할 탭) |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-시스템역할만` / `03-커스텀역할있음` / `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-087 커스텀역할생성 — 상태: 로딩

파일: src/app/settings/permissions/page.tsx (커스텀 역할 탭)

Skeleton (animate-pulse):
- 탭 헤더: 권한 설정 | 커스텀 역할
- 역할 행 5개: h-16 w-full bg-gray-200 rounded-lg
- "+ 새 역할 생성" 버튼 자리: h-9 w-32 bg-gray-200 rounded-md

데이터 페치:
Promise.all([
  supabase.from('custom_roles').select('*, staff_count:staff(count)').eq('branch_id', branchId),
  supabase.from('user_permissions').select('role').eq('branch_id', branchId)
])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 커스텀 역할 탭 진입
- 이벤트: `E_F6_087_01`

### 연결 화면
- 커스텀 역할 없음: `02-시스템역할만`
- 커스텀 역할 있음: `03-커스텀역할있음`
- 실패: `04-에러`
