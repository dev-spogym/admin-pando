# SCR-087 커스텀역할생성 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-087 |
| 상태 코드 | `error` |
| 경로 | `/settings/permissions` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
에러 UI (E_F6_087_03):
<div className="flex flex-col items-center justify-center h-80 gap-4">
  <AlertCircle className="w-10 h-10 text-red-400" />
  <p className="text-base font-semibold text-gray-700">역할 데이터를 불러오지 못했습니다</p>
  <Button variant="outline" onClick={handleRetry}>
    <RefreshCw className="mr-2 h-4 w-4" /> 다시 시도
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점
- custom_roles API 실패, 이벤트: `E_F6_087_03`

### 연결 화면
- 재시도: `01-로딩`
