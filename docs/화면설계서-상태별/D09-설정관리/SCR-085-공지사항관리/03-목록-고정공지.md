# SCR-085 공지사항관리 — 상태: 목록 (고정/일반 공지)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-085 |
| 상태 코드 | `list-with-pinned` |
| 경로 | `/notices` |
| 역할 | primary, owner, manager, fc, staff, readonly |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `04-조회전용` (권한 없는 역할) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-085 공지사항관리 — 상태: 공지 목록 (고정 + 일반)

파일: src/app/notices/page.tsx

레이아웃:
- PageHeader: "공지사항"
- 우측 버튼: "+ 공지 등록" (hasPermission('notices:write') 시만)

공지 목록 (divide-y divide-gray-100):
고정 공지 (is_pinned=true, E_F6_085_06):
  <div className="flex items-start gap-3 px-4 py-4 bg-amber-50/50 hover:bg-amber-50 transition">
    <Pin className="w-4 h-4 text-amber-500 mt-0.5 shrink-0" />
    <div className="flex-1 min-w-0">
      <p className="font-medium text-gray-900 truncate">{notice.title}</p>
      <p className="text-xs text-gray-400 mt-0.5">{format(notice.created_at, 'yyyy.MM.dd')} · {notice.author}</p>
    </div>
    {hasPermission(role, 'notices:write') && (
      <div className="flex gap-1 shrink-0">
        <Button size="icon" variant="ghost" onClick={() => openEdit(notice)}><Pencil className="w-4 h-4" /></Button>
        <Button size="icon" variant="ghost" onClick={() => openDelete(notice)}><Trash2 className="w-4 h-4 text-red-400" /></Button>
      </div>
    )}
  </div>

일반 공지 (is_pinned=false, E_F6_085_07):
  <div className="flex items-start gap-3 px-4 py-4 hover:bg-gray-50 transition">
    <FileText className="w-4 h-4 text-gray-300 mt-0.5 shrink-0" />
    ... (동일 구조, bg 없음)
  </div>

섹션 구분:
- 고정 공지 그룹: "📌 고정 공지" 라벨 (text-xs text-amber-600)
- 일반 공지 그룹: "일반 공지" 라벨 (text-xs text-gray-400)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- notices API 성공 + 공지 존재
- 이벤트: `E_F6_085_05`, `E_F6_085_06`, `E_F6_085_07`

### 공지 행 구성
| 요소 | 고정 공지 | 일반 공지 |
|------|-----------|-----------|
| 배경 | bg-amber-50/50 | 기본 |
| 아이콘 | Pin amber-500 | FileText gray-300 |
| 수정/삭제 버튼 | 권한 있는 역할만 | 동일 |

### 비즈니스 룰
- is_pinned=true 항목 먼저 표시 (상단 고정)
- 같은 그룹 내: created_at desc 정렬
- 수정/삭제 버튼: primary, owner, manager만 표시

### 연결 화면
- fc/staff/readonly: `04-조회전용`
- 수정: DLG-085-001
- 삭제: DLG-085-002
