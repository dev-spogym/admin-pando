# SCR-085 공지사항관리 — 상태: 조회 전용 (fc/staff/readonly)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-085 |
| 상태 코드 | `read-only` |
| 경로 | `/notices` |
| 역할 | fc, staff, readonly |
| 우선순위 | P1 |
| 이전 상태 | `03-목록-고정공지` |
| 다음 상태 | 없음 (조회만) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-085 공지사항관리 — 상태: 조회 전용 (E_F6_085_08)

파일: src/app/notices/page.tsx

조회 전용 모드 (role === 'fc' || 'staff' || 'readonly'):
- PageHeader: "+ 공지 등록" 버튼 숨김
- 공지 행: 수정/삭제 버튼 완전 제거
- 공지 클릭 시: 상세 내용 아코디언 펼치기 (수정 없이 읽기만)

아코디언 상세:
<div className="px-4 py-3 bg-gray-50 text-sm text-gray-700 border-t border-gray-100">
  {notice.content}
  {notice.attachment_url && (
    <a href={notice.attachment_url} className="mt-2 flex items-center gap-1 text-xs text-primary" target="_blank">
      <Paperclip className="w-3.5 h-3.5" /> 첨부파일 다운로드
    </a>
  )}
</div>

조회 전용 안내 배너 (readonly 역할만):
<div className="text-xs text-gray-400 text-right mb-2">조회 전용 모드</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- role = fc, staff, readonly 진입
- 이벤트: `E_F6_085_08`

### UI 차이
| 역할 | 등록 버튼 | 수정/삭제 버튼 | 아코디언 |
|------|-----------|---------------|---------|
| primary/owner/manager | 표시 | 표시 | 읽기 |
| fc/staff/readonly | 숨김 | 숨김 | 읽기만 |

### 비즈니스 룰
- hasPermission(role, 'notices:write') = false → 모든 편집 UI 제거
- 첨부파일 다운로드는 모든 역할 허용

### 연결 화면
- 없음 (조회 전용)
