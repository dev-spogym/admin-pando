# SCR-085 공지사항관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-085 |
| 상태 코드 | `loading` |
| 경로 | `/notices` |
| 역할 | primary, owner, manager, fc, staff, readonly |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-빈상태` / `03-목록-고정공지` / `05-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-085 공지사항관리 — 상태: 로딩

파일: src/app/notices/page.tsx

Skeleton (animate-pulse):
- PageHeader: "공지사항"
- 우측 버튼 영역: h-9 w-24 bg-gray-200 rounded-md (+ 공지 등록)
- 공지 행 6개: h-16 w-full bg-gray-200 rounded-lg

데이터 페치:
supabase.from('notices')
  .select('*')
  .eq('branch_id', branchId)
  .order('is_pinned', { ascending: false })
  .order('created_at', { ascending: false })
```

## 📝 디스크립션

### 사용 시점
- 이벤트: `E_F6_085_01`

### 연결 화면
- notices=[] → `02-빈상태`
- 공지 있음 → `03-목록-고정공지`
- 실패 → `05-에러`
