# SCR-082 키오스크설정 — 상태: D11 연결 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `d11-disconnect-warn` |
| 경로 | `/settings/kiosk` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` |
| 다음 상태 | `02-정상` (연결 복구) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 키오스크설정 — 상태: D11 연결 끊김 경고 배너

파일: src/app/settings/kiosk/page.tsx

D11 연결 끊김 배너 (E_F6_082_09):
- 폼 최상단에 배너 삽입:
<div className="flex items-center gap-3 px-4 py-3 bg-amber-50 border border-amber-200 rounded-lg mb-6">
  <AlertTriangle className="w-5 h-5 text-amber-500 shrink-0" />
  <div>
    <p className="text-sm font-medium text-amber-800">D11 IoT 연결이 끊겼습니다</p>
    <p className="text-xs text-amber-600 mt-0.5">설정 저장은 가능하지만 실시간 제어 기능이 비활성화됩니다.</p>
  </div>
  <Button variant="ghost" size="sm" className="ml-auto text-amber-700" onClick={retryD11}>
    재연결
  </Button>
</div>

D11 연결 상태 감지:
- useEffect로 WebSocket 또는 polling (30초 간격)
- iot_connection_status: 'connected' | 'disconnected' | 'error'
- disconnected → 배너 표시

IoT 연동 섹션 내 상태 배지:
- 연결됨: <span className="bg-green-100 text-green-700 text-xs px-2 py-0.5 rounded-full">연결됨</span>
- 끊김: <span className="bg-red-100 text-red-700 text-xs px-2 py-0.5 rounded-full">연결 끊김</span>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- D11 IoT 시스템 연결 상태가 disconnected로 변경
- 이벤트: `E_F6_082_09`

### UI 구성
| 요소 | 표현 |
|------|------|
| 경고 배너 | amber-50 bg, AlertTriangle 아이콘 |
| 배너 메시지 | "D11 IoT 연결이 끊겼습니다" |
| 부연 메시지 | "설정 저장은 가능하나 실시간 제어 비활성" |
| 재연결 버튼 | ghost variant, 우측 정렬 |
| IoT 배지 | 빨강 "연결 끊김" |

### 비즈니스 룰
- 설정 저장(PATCH) 자체는 여전히 가능
- D11 관련 실시간 제어 기능만 비활성화
- 재연결 성공 시 배너 자동 제거

### 연결 화면
- 연결 복구: `02-정상` (배너 제거)
