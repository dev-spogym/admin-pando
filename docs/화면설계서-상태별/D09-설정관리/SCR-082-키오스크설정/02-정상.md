# SCR-082 키오스크설정 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `normal` |
| 경로 | `/settings/kiosk` |
| 역할 | primary, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `03-변경됨` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 키오스크설정 — 상태: 정상 (5개 섹션 표시)

파일: src/app/settings/kiosk/page.tsx

레이아웃:
- AppLayout + PageHeader "키오스크 설정"
- 우측 상단: 저장 버튼 (isDirty=true 시 활성)

5개 섹션 (space-y-8, divide-y divide-gray-100):

[섹션1] 기본 설정:
- 키오스크 사용 여부 (Toggle)
- 키오스크 이름 (Input)
- 화면 대기 시간 (Select: 30초/1분/3분/5분)

[섹션2] 체크인 설정:
- 체크인 방식 (RadioGroup: QR코드/회원번호/지문)
- 체크인 성공 메시지 (Input, placeholder: "환영합니다, {name}님!")
- 자동 체크아웃 (Toggle + 시간 Input)

[섹션3] 디스플레이 설정:
- 대기 화면 배경 (Select: 기본/센터 로고/동영상)
- 시계 표시 (Toggle)
- 날씨 표시 (Toggle)

[섹션4] 보안 설정:
- 관리자 PIN (Input type=password, 4-6자리)
- PIN 변경 주기 (Select: 30일/90일/없음)

[섹션5] 연동 설정:
- D11 IoT 연동 (Toggle + 연결 상태 배지)
- 프린터 연동 (Toggle)

DB: supabase.from('kiosk_settings').select('*').eq('branch_id', branchId)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- kiosk_settings API 성공
- 이벤트: `E_F6_082_02`

### 필수 데이터
| 섹션 | 테이블 | 컬럼 |
|------|--------|------|
| 기본 | `kiosk_settings` | `enabled`, `name`, `idle_timeout` |
| 체크인 | `kiosk_settings` | `checkin_method`, `success_msg`, `auto_checkout` |
| 디스플레이 | `kiosk_settings` | `bg_type`, `show_clock`, `show_weather` |
| 보안 | `kiosk_settings` | `admin_pin`, `pin_rotation` |
| 연동 | `kiosk_settings` | `iot_enabled`, `printer_enabled` |

### 비즈니스 룰
- isDirty=false → 저장 버튼 disabled + opacity-50
- D11 IoT 연결 끊김 → `04-D11연결경고` 배너 표시

### 연결 화면
- 필드 수정: `03-변경됨`
- D11 끊김: `04-D11연결경고`
