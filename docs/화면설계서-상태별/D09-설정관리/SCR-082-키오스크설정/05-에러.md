# SCR-082 키오스크설정 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `error` |
| 경로 | `/settings/kiosk` |
| 역할 | primary, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 키오스크설정 — 상태: 에러 (로드 실패, 재시도)

파일: src/app/settings/kiosk/page.tsx

에러 UI (fetchError=true):
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <AlertCircle className="w-12 h-12 text-red-400" />
  <p className="text-base font-semibold text-gray-800">키오스크 설정을 불러오지 못했습니다</p>
  <p className="text-sm text-gray-500">TC-082-008: 설정 로드 실패</p>
  <Button variant="outline" onClick={handleRetry}>
    <RefreshCw className="mr-2 h-4 w-4" /> 다시 시도
  </Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- kiosk_settings API 실패 (이벤트: `E_F6_082_03`)
- TC-082-008: 설정 로드 실패 → 에러 상태 + 재시도

### 연결 화면
- 재시도: `01-로딩`
