# SCR-082 키오스크 설정 — 상태 01: 로딩

> 상속: [`00-기본화면.md`](./00-기본화면.md) — **델타**만 기술.

## 메타
| 항목 | 값 |
|---|---|
| 상태 코드 | `kiosk-loading` |
| 이전 | 진입 / BranchSwitcher |
| 다음 | `02-정상`(200) / `05-에러`(5xx) / `06-권한없음`(403) |

## 진입 조건
- `useQueries([kioskConfig, d11Status, printerStatus])` pending

## 비주얼 델타
| 요소 | 변경 |
|---|---|
| 6개 섹션 | 각 `<Skeleton>` 카드 (헤더 `h-5 w-36` + 토글 행 3~4개 `h-14`) |
| 프리뷰 패널 | `aspect-[9/16] bg-gray-200 animate-pulse` |
| D11 배지 | 스켈레톤 `h-5 w-16` |
| 저장 버튼 | disabled |

## 비즈니스 룰
- 병렬 쿼리 3개: config + d11 + printer
- 10초 타임아웃 → `05-에러`
- skeletonMinDelay 150ms

## 에지 케이스
- WebSocket 연결 실패 시 d11Status='disconnected'로 폴백
- partial success: config만 200 → 섹션 렌더, d11 배지만 '확인 중'

## 🧩 바이브코딩 프롬프트 (델타)

```
상속: 00-기본화면.md §14

<div className="grid lg:grid-cols-3 gap-6" role="status" aria-busy="true" aria-label="키오스크 설정 로딩 중">
  <div className="lg:col-span-2 space-y-6">
    {Array.from({length:6}).map((_,i) => (
      <div key={i} className="bg-white rounded-xl ring-1 ring-gray-100 p-6 space-y-4">
        <div className="h-5 w-36 bg-gray-200 rounded animate-pulse" />
        <div className="space-y-3">
          {Array.from({length:3}).map((_,j) => (
            <div key={j} className="flex justify-between items-center h-14">
              <div className="h-4 w-48 bg-gray-200 rounded animate-pulse" />
              <div className="h-6 w-11 bg-gray-200 rounded-full animate-pulse" />
            </div>
          ))}
        </div>
      </div>
    ))}
  </div>
  <aside className="lg:col-span-1">
    <div className="sticky top-6 aspect-[9/16] bg-gray-200 rounded-xl animate-pulse" />
  </aside>
</div>

useQueries:
const [configQ, d11Q, printerQ] = useQueries({
  queries: [
    { queryKey:['kioskConfig', branchId], queryFn: () => fetchKioskConfig(branchId) },
    { queryKey:['d11Status', branchId], queryFn: () => fetchD11Status(branchId), refetchInterval: 30_000 },
    { queryKey:['printerStatus', branchId], queryFn: () => fetchPrinterStatus(branchId) },
  ]
});
```

## TC 후보
| TC | 타입 | Given | When | Then |
|---|---|---|---|---|
| TC-082-01-01 | positive | 진입 | render | 6섹션 스켈레톤 + 프리뷰 |
| TC-082-01-02 | negative | 10초 | timeout | 05-에러 |
| TC-082-01-03 | positive | partial | config만 200 | 섹션만 렌더 |
| TC-082-01-04 | edge | 빠른 응답 | <150ms | 플래시 없음 |

## 다이어그램
- F1 진입, F6 상태별 STATE_LOADING
