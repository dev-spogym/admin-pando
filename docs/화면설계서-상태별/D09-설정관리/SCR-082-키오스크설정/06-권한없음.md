# SCR-082 키오스크설정 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-082 |
| 상태 코드 | `no-auth` |
| 경로 | `/settings/kiosk` |
| 역할 | fc, staff, readonly (접근 불가) |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 없음 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-082 키오스크설정 — 상태: 권한없음

파일: src/app/settings/kiosk/page.tsx

권한없음 UI (이벤트: E_F6_082_04):
<div className="flex flex-col items-center justify-center h-96 gap-4">
  <Lock className="w-12 h-12 text-gray-300" />
  <p className="text-base font-semibold text-gray-700">접근 권한이 없습니다</p>
  <p className="text-sm text-gray-400">키오스크 설정은 최고관리자 또는 원장만 접근 가능합니다.</p>
  <Button variant="outline" onClick={() => moveToPage('/settings')}>설정 홈으로</Button>
</div>
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 403 응답 또는 hasPermission(role, 'kiosk:read') = false
- 이벤트: `E_F6_082_04`

### 비즈니스 룰
- primary, owner만 접근 허용
- 사이드바 키오스크 설정 메뉴 숨김

### 연결 화면
- CTA: `/settings`
