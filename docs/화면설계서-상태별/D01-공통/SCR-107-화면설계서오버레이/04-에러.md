# SCR-107 화면설계서 오버레이 — 상태: 에러

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-107 |
| 상태 코드 | `spec-overlay-error` |
| 경로 | 모든 인증 경로 (오버레이) |
| 역할 | superAdmin / primary / owner / manager |
| 우선순위 | P2 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-107_화면설계서오버레이/F6_상태별.md` |
| 이전 상태 | `01-로딩` (로드 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-107 화면설계서 오버레이 — 상태: 에러

파일: src/components/common/SpecOverlay.tsx (isError=true 분기)

레이아웃:
- 동일 오버레이 패널

컴포넌트 구조:
1. 오버레이 헤더 (닫기 버튼)
2. 에러 블록:
   - AlertTriangle 아이콘 (w-12 h-12 text-red-400 mx-auto)
   - "설계서를 불러오지 못했습니다" (text-sm text-gray-500 mt-3)
   - 재시도 Button (outline) + 닫기 Button

데이터:
- isError: boolean = true
- retry: () => void

인터랙션:
- 재시도 → `01-로딩`
- 닫기/ESC → 오버레이 닫힘

사용 유틸:
- lucide-react (AlertTriangle, RefreshCw, X)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 설계서 파일 API/Storage 접근 실패

### 필수 데이터
- retry 함수

### 인터랙션 (User Actions)
1. 재시도 → 재로딩
2. 닫기 → 오버레이 닫힘

### 비즈니스 룰
- 에러는 오버레이 내 처리 (전체 페이지 에러 아님)

### 접근성 (A11y)
- `role="alert"`

### 연결 화면
- 재시도: `01-로딩`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-107_화면설계서오버레이/F6_상태별.md` → STATE_ERROR 노드
