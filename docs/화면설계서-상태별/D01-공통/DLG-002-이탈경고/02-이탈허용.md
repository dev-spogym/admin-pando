# DLG-002 이탈 경고 — 상태: 이탈 허용 (나가기)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-002 |
| 상태 코드 | `leave-warning-allowed` |
| 경로 | → destination |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/DLG/DLG-002_이탈경고/M1_생명주기.md` |
| 이전 상태 | `01-열림` (나가기 버튼) |
| 다음 상태 | 이탈 목적지 화면 |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 컴포넌트를 작성하라.
화면: DLG-002 이탈 경고 — 상태: 이탈 허용

파일: src/components/common/LeaveWarningModal.tsx (handleLeave 핸들러)

처리 순서:
1. isDirty = false (변경사항 버리기)
2. 라우터 가드 해제 (이벤트 리스너 제거)
3. router.push(destination)
4. 모달 닫힘 (isOpen = false)

데이터:
- destination: string (저장된 이탈 목적지)
- 변경사항 폐기 (formData 초기화 없음, 화면 이동으로 자동 소멸)

인터랙션:
- 즉시 처리 (로딩 UI 없음)
- router.push(destination) → 목적지 화면 렌더

사용 유틸:
- router
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `01-열림`에서 "나가기" 버튼 클릭

### 필수 데이터
- destination URL

### 인터랙션 (User Actions)
- 없음 (자동 처리)

### 비즈니스 룰
- 변경사항 서버 저장 없이 폐기
- 라우터 가드(beforePopState) 해제 필수 (무한 루프 방지)
- 이탈 후 목적지 화면은 새로운 상태로 진입

### 에지 케이스
- 목적지 URL 없음 → router.back() 폴백

### 접근성 (A11y)
- 모달 닫힘 → 목적지 화면 첫 포커스 요소로 이동

### 연결 화면
- 목적지: 이탈 시도한 화면 (사이드바 메뉴 등)

### 다이어그램 참조
- M1: `docs/다이어그램/D01_공통/DLG/DLG-002_이탈경고/M1_생명주기.md` → DISCARD, NAV 노드
