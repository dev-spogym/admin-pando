# DLG-001 로그아웃 확인 — 상태: 로그아웃 처리 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-001 |
| 상태 코드 | `logout-confirm-processing` |
| 경로 | 트리거 화면 |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/DLG/DLG-001_로그아웃확인/M1_생명주기.md` |
| 이전 상태 | `01-열림` (로그아웃 확인) |
| 다음 상태 | SCR-100 로그인 |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-001 로그아웃 확인 — 상태: 처리 중

파일: src/components/common/LogoutConfirmModal.tsx (isLoggingOut=true 분기)

레이아웃:
- 동일 모달 카드
- 버튼 영역 로딩 상태

컴포넌트 구조:
1. 동일 아이콘 + 제목
2. 본문 텍스트 유지
3. 버튼 그룹:
   - "취소" Button (disabled)
   - "로그아웃" Button (disabled, Loader2 스피너 + "로그아웃 중...")
4. X 버튼 (disabled)

처리 순서 (onConfirm):
1. isLoggingOut = true
2. supabase.auth.signOut() (fire-and-forget)
3. useAuthStore.reset()
4. localStorage.clear(), sessionStorage.clear()
5. router.replace('/login?logout=true')

인터랙션:
- 모든 버튼 disabled
- 처리 완료 → SCR-100 이동

사용 유틸:
- useAuthStore, supabase, router, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `01-열림`에서 "로그아웃" 버튼 클릭

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
- 모든 상호작용 차단

### 비즈니스 룰
- SCR-109 로그아웃 화면과 동일 처리 로직
- 완료 후 `/login?logout=true` 이동

### 에지 케이스
- API 타임아웃 → 강제 클리어 후 이동

### 접근성 (A11y)
- `aria-busy="true"` on 모달
- `aria-live="polite"` "로그아웃 처리 중"

### 연결 화면
- SCR-100 로그인 (완료 토스트)

### 다이어그램 참조
- M1: `docs/다이어그램/D01_공통/DLG/DLG-001_로그아웃확인/M1_생명주기.md` → LOGOUT, SCR_100 노드
