# DLG-001 로그아웃 확인 — 상태: 열림

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-001 |
| 상태 코드 | `logout-confirm-open` |
| 경로 | 트리거 화면 (사이드바 로그아웃 버튼) |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/DLG/DLG-001_로그아웃확인/M1_생명주기.md` |
| 이전 상태 | 로그아웃 버튼 클릭 |
| 다음 상태 | `02-로그아웃처리중` (확인) / 닫힘 (취소) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-001 로그아웃 확인 — 상태: 열림

파일: src/components/common/LogoutConfirmModal.tsx

레이아웃:
- 전체 화면 오버레이 (fixed inset-0 bg-black/40 z-[100] flex items-center justify-center)
- 모달 카드 (max-w-sm w-full bg-white rounded-2xl shadow-xl p-6)

컴포넌트 구조:
1. 모달 헤더:
   - LogOut 아이콘 (w-10 h-10 text-gray-400 mx-auto)
   - 제목: "로그아웃" (text-lg font-bold text-center mt-3)
2. 본문:
   - "로그아웃 하시겠습니까?" (text-sm text-gray-500 text-center mt-2)
3. 버튼 그룹 (mt-6 grid grid-cols-2 gap-3):
   - "취소" Button (ghost) → 닫기
   - "로그아웃" Button (destructive, bg-red-500) → `02-로그아웃처리중`
4. X 닫기 버튼 (우측 상단 absolute)

데이터:
- isOpen: boolean = true
- onConfirm: () => void
- onCancel: () => void

인터랙션:
- "로그아웃" 버튼 → `02-로그아웃처리중`
- "취소" / X / ESC / 배경 클릭 → 모달 닫힘 + 현재 화면 유지

사용 유틸:
- lucide-react (LogOut, X)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 사이드바 하단 "로그아웃" 버튼 클릭
- 프로필 메뉴 "로그아웃" 클릭

### 필수 데이터
- 없음 (정적 확인 모달)

### 인터랙션 (User Actions)
1. "로그아웃" 버튼 → `02-로그아웃처리중`
2. "취소" / X / ESC / 배경 클릭 → 모달 닫힘

### 비즈니스 룰
- 배경 스크롤 잠금
- ESC 키 → 닫힘 허용 (세션만료 모달과 다름)
- 배경 클릭 → 닫힘 허용

### 에지 케이스
- 폼 편집 중 로그아웃 시도 → DLG-001 먼저 표시 (이탈 경고 없음, 로그아웃 우선)

### 접근성 (A11y)
- `role="dialog"` `aria-modal="true"` `aria-labelledby="logout-title"`
- 모달 열림 시 "로그아웃" 버튼에 포커스
- 닫힘 시 트리거 버튼으로 포커스 복원

### 연결 화면
- 확인: `02-로그아웃처리중` → SCR-100
- 취소: 현재 화면 유지

### 다이어그램 참조
- M1: `docs/다이어그램/D01_공통/DLG/DLG-001_로그아웃확인/M1_생명주기.md` → OPEN, DISPLAY 노드
