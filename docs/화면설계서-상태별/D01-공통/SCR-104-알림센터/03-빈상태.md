# SCR-104 알림 센터 — 상태: 빈 상태

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-104 |
| 상태 코드 | `notification-empty` |
| 경로 | `/notifications` |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-104_알림센터/F6_상태별.md` |
| 이전 상태 | `01-로딩` (알림 없음) |
| 다음 상태 | `02-정상` (알림 수신 시) / `05-실시간수신` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-104 알림 센터 — 상태: 빈 상태

파일: src/app/(dashboard)/notifications/page.tsx (notifications.length === 0 분기)

레이아웃:
- AppLayout (사이드바 + 헤더)
- PageHeader: "알림 센터"
- 빈 상태 UI

컴포넌트 구조:
1. PageHeader (정상)
2. TabNav (전체/미읽음/읽음, 전체 탭 활성)
3. 빈 상태 블록 (flex-1 flex items-center justify-center):
   - BellOff 아이콘 (w-16 h-16 text-gray-300 mx-auto)
   - "새 알림이 없습니다" (text-lg font-medium text-gray-400 mt-4)
   - "새로운 알림이 오면 여기에 표시됩니다." (text-sm text-gray-400 mt-1)

데이터:
- notifications: [] (전체 빈 배열)
- Supabase realtime 구독 유지 (새 알림 대기)

인터랙션:
- 실시간 알림 수신 → `02-정상` 자동 전환
- 탭 전환 → 동일 빈 상태 (탭별 별도 메시지 옵션)

사용 유틸:
- useAuthStore, supabase, lucide-react (BellOff)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 알림 목록 API 응답 0건
- 미읽음 탭에서 모두 읽음 처리 후

### 필수 데이터
- notifications: [] (빈 배열)

### 인터랙션 (User Actions)
1. 탭 전환 → 각 탭 빈 상태 표시
2. 실시간 알림 수신 → 자동으로 `02-정상` 전환

### 비즈니스 룰
- 빈 상태에서도 realtime 구독 유지
- 탭별 빈 메시지 차별화:
  - 전체: "새 알림이 없습니다"
  - 미읽음: "읽지 않은 알림이 없습니다"
  - 읽음: "읽은 알림이 없습니다"

### 에지 케이스
- "모두 읽음" 처리 후 미읽음 탭 → 빈 상태

### 접근성 (A11y)
- `role="status"` `aria-label="알림 없음"`

### 연결 화면
- 실시간 알림 수신 시: `02-정상`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-104_알림센터/F6_상태별.md` → STATE_EMPTY 노드
