# SCR-104 알림 센터 — 상태: 에러

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-104 |
| 상태 코드 | `notification-error` |
| 경로 | `/notifications` |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-104_알림센터/F6_상태별.md` |
| 이전 상태 | `01-로딩` (API 오류) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-104 알림 센터 — 상태: 에러

파일: src/app/(dashboard)/notifications/page.tsx (isError 분기)

레이아웃:
- AppLayout (사이드바 + 헤더)
- PageHeader: "알림 센터"
- 에러 UI

컴포넌트 구조:
1. PageHeader (정상)
2. 에러 블록 (메인 영역 중앙):
   - AlertTriangle 아이콘 (w-12 h-12 text-red-400 mx-auto)
   - "알림을 불러오지 못했습니다" (text-base text-gray-600 mt-4)
   - 재시도 Button (primary, mt-4) → notifications 재로딩

데이터:
- isError: boolean = true
- retry: () => void

인터랙션:
- 재시도 버튼 → `01-로딩` 재진입

사용 유틸:
- supabase, lucide-react (AlertTriangle, RefreshCw)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- notifications API 500/502 응답
- 네트워크 오류

### 필수 데이터
- retry 함수

### 인터랙션 (User Actions)
1. 재시도 버튼 → 재로딩
2. 사이드바 이동 → 다른 화면으로 이동 가능

### 비즈니스 룰
- 에러 토스트 병행 표시

### 에지 케이스
- 재시도 반복 실패 → 에러 상태 유지

### 접근성 (A11y)
- `role="alert"` `aria-live="assertive"`

### 연결 화면
- 재시도: `01-로딩`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-104_알림센터/F6_상태별.md` → STATE_ERROR 노드
