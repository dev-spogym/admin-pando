# DLG-004 저장 확인 — 상태: 저장 실패

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-004 |
| 상태 코드 | `save-confirm-error` |
| 경로 | 트리거 화면 |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/DLG/DLG-004_저장확인/M1_생명주기.md` |
| 이전 상태 | `02-저장중` (500 실패) |
| 다음 상태 | `01-열림` (재시도 가능) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 컴포넌트를 작성하라.
화면: DLG-004 저장 확인 — 상태: 저장 실패

파일: src/components/common/SaveConfirmModal.tsx (onError 핸들러, 500 에러)

처리 순서:
1. 모달 닫힘 (isOpen = false)
2. toast.error("저장에 실패했습니다. 잠시 후 다시 시도해주세요.")
3. 편집 화면 유지 (isDirty 유지)
4. 폼 데이터 변경 없음

데이터:
- errorCode: '500' | 'network'
- 에러 메시지:
  - 500: "저장에 실패했습니다. 잠시 후 다시 시도해주세요."
  - network: "네트워크 오류로 저장에 실패했습니다."

인터랙션:
- 자동 처리 (모달 닫힘 + 에러 토스트)
- 편집 화면 복귀 (isDirty 유지)
- 재시도: 저장 버튼 재클릭

사용 유틸:
- toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 저장 API → 500/502/504 실패 응답
- 네트워크 타임아웃

### 필수 데이터
- errorCode

### 인터랙션 (User Actions)
- 에러 토스트 확인 후 재시도

### 비즈니스 룰
- dirty 상태 유지 (저장 실패이므로 이탈 경고 계속 활성)
- 폼 데이터 변경 없음 (사용자 입력 보존)
- 재시도: 저장 버튼 → `01-열림` → `02-저장중`

### 에지 케이스
- 연속 저장 실패 → 에러 토스트 중첩 표시 방지 (dedup)

### 접근성 (A11y)
- 에러 토스트 `role="alert"` `aria-live="assertive"`

### 연결 화면
- 재시도: `01-열림`
- 현재 편집 화면 유지

### 다이어그램 참조
- M1: `docs/다이어그램/D01_공통/DLG/DLG-004_저장확인/M1_생명주기.md` → ERROR 노드
- M3: `docs/다이어그램/D01_공통/DLG/DLG-004_저장확인/M3_결과분기.md` → RESULT_ERR 노드
