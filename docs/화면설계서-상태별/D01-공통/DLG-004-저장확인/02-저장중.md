# DLG-004 저장 확인 — 상태: 저장 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | DLG-004 |
| 상태 코드 | `save-confirm-saving` |
| 경로 | 트리거 화면 |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/DLG/DLG-004_저장확인/M1_생명주기.md` |
| 이전 상태 | `01-열림` (저장 버튼) |
| 다음 상태 | `03-저장성공` / `04-저장실패` / `05-충돌` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 'use client' 컴포넌트를 작성하라.
화면: DLG-004 저장 확인 — 상태: 저장 중

파일: src/components/common/SaveConfirmModal.tsx (isSaving=true 분기)

레이아웃:
- 동일 모달 카드

컴포넌트 구조:
1. Loader2 스피너 (w-10 h-10 animate-spin text-blue-500 mx-auto)
2. "저장 중..." 텍스트 (text-sm text-gray-500 text-center mt-3)
3. 버튼 그룹: 모두 disabled
4. X 버튼: disabled
5. ESC/배경 클릭: 차단

데이터:
- isSaving: boolean = true
- PATCH/POST /api/{resource}/{id} 호출 중

인터랙션:
- 모든 UI 차단
- 성공 → `03-저장성공`
- 실패 → `04-저장실패`
- 409 충돌 → `05-충돌`

사용 유틸:
- lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `01-열림`에서 저장 버튼 클릭 후 API 호출 중

### 필수 데이터
- 저장 payload (부모 폼에서 전달)

### 인터랙션 (User Actions)
- 모든 상호작용 차단

### 비즈니스 룰
- 중복 저장 방지
- 타임아웃 10초 → `04-저장실패`

### 에지 케이스
- 저장 중 세션 만료 → DLG-000 세션만료 모달 우선 (저장 실패)

### 접근성 (A11y)
- `aria-busy="true"` `aria-live="polite"` "저장 처리 중"

### 연결 화면
- 성공: `03-저장성공`
- 실패: `04-저장실패`
- 충돌: `05-충돌`

### 다이어그램 참조
- M1: `docs/다이어그램/D01_공통/DLG/DLG-004_저장확인/M1_생명주기.md` → SAVE_API 노드
