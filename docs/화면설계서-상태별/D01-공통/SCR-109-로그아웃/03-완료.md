# SCR-109 로그아웃 — 상태: 완료

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-109 |
| 상태 코드 | `logout-done` |
| 경로 | `/login` (리다이렉트 완료) |
| 역할 | all (로그아웃 후) |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` |
| 이전 상태 | `02-클리어중` |
| 다음 상태 | SCR-100 로그인 |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind 기반 컴포넌트를 작성하라.
화면: SCR-109 로그아웃 — 상태: 완료

파일: src/app/(auth)/logout/page.tsx (step='done' 분기)

레이아웃:
- 별도 UI 없음 (즉시 리다이렉트)
- 리다이렉트 전 토스트 표시

인터랙션:
1. moveToPage('/login') 실행
2. 로그인 화면 도착 후 toast.success("로그아웃 되었습니다.") 표시
   - 방법: URL params ?logout=true → 로그인 페이지에서 감지 후 토스트

구현:
- router.replace('/login?logout=true')
- SCR-100 page.tsx에서 searchParams.get('logout') === 'true' → toast.success

데이터:
- step: 'done'
- 세션: 완전 제거 완료

사용 유틸:
- moveToPage, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-클리어중` 완료 후 자동 진행

### 필수 데이터
- 없음

### 인터랙션 (User Actions)
- 없음 (자동 리다이렉트)

### 비즈니스 룰
- 리다이렉트: `/login?logout=true`
- 로그인 페이지에서 `?logout=true` 파라미터 감지 → 성공 토스트
- 로그아웃 완료 후 뒤로가기 → 세션 없으므로 로그인 화면으로 재리다이렉트

### 에지 케이스
- 리다이렉트 실패 → window.location.href = '/login' 폴백

### 접근성 (A11y)
- 리다이렉트 전 `aria-live="assertive"` "로그아웃 완료, 로그인 페이지로 이동합니다"

### 연결 화면
- SCR-100 로그인 (로그아웃 완료 토스트 표시)

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` → STATE_DONE, SCR_100 노드
