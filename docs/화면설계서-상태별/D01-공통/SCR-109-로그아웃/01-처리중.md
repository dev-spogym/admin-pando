# SCR-109 로그아웃 — 상태: 처리 중

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-109 |
| 상태 코드 | `logout-processing` |
| 경로 | `/logout` (처리 전용) |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P0 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` |
| 이전 상태 | DLG-001 로그아웃 확인 (확인 버튼) |
| 다음 상태 | `02-클리어중` |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-109 로그아웃 — 상태: 처리 중

파일: src/app/(auth)/logout/page.tsx 또는 로그아웃 핸들러 함수

레이아웃:
- AppLayout 제외
- 전체 화면 로딩 (fixed inset-0 bg-white flex items-center justify-center)

컴포넌트 구조:
1. Loader2 스피너 (w-12 h-12 animate-spin text-blue-500)
2. "로그아웃 중..." 텍스트 (text-sm text-gray-500 mt-4)

데이터:
- POST /auth/logout (supabase.auth.signOut())
- 성공 여부와 관계없이 `02-클리어중` 진행

인터랙션:
- API 완료 (성공/실패 무관) → `02-클리어중`
- 사용자 입력 차단

사용 유틸:
- useAuthStore, supabase, lucide-react (Loader2)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- DLG-001 로그아웃 확인 모달에서 "확인" 클릭
- supabase.auth.signOut() API 호출 중

### 필수 데이터
- 없음 (현재 세션 토큰으로 자동 처리)

### 인터랙션 (User Actions)
- 모든 상호작용 차단
- API 완료 시 자동 다음 단계 진행

### 비즈니스 룰
- API 실패해도 클리어 단계로 진행 (강제 로그아웃)
- 감사 로그 LOGOUT 기록 (서버 처리)

### 에지 케이스
- API 타임아웃 5초 → 강제 `02-클리어중` 진행

### 접근성 (A11y)
- `role="status"` `aria-label="로그아웃 처리 중"` `aria-live="polite"`

### 연결 화면
- 다음: `02-클리어중`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-109_로그아웃/F6_상태별.md` → STATE_PROCESSING 노드
