# SCR-103 글로벌 검색 — 상태: 결과 없음

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-103 |
| 상태 코드 | `search-empty` |
| 경로 | 모든 인증 경로 (오버레이) |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P1 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-103_글로벌검색/F6_상태별.md` |
| 이전 상태 | `03-검색중` (결과 없음) |
| 다음 상태 | `03-검색중` (검색어 변경) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-103 글로벌 검색 — 상태: 결과 없음

파일: src/components/common/GlobalSearchOverlay.tsx (results 전체 빈 배열 분기)

레이아웃:
- 동일 오버레이 패널
- 빈 상태 일러스트 + 안내 문구

컴포넌트 구조:
1. 검색 입력 (query 유지)
2. 구분선
3. 빈 상태 블록:
   - SearchX 아이콘 (w-12 h-12 text-gray-300 mx-auto)
   - "'{query}'에 대한 검색 결과가 없습니다." (text-sm text-gray-500 text-center)
   - 검색 팁: "· 다른 키워드로 검색해보세요\n· 전화번호 또는 이메일로도 검색 가능합니다"

데이터:
- results: { members: [], classes: [], staff: [] }
- query: string

인터랙션:
- 검색어 수정 → `03-검색중`
- ESC → 닫기

사용 유틸:
- lucide-react (SearchX)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 검색 API 응답 결과 0건

### 필수 데이터
- query (현재 검색어, 빈 상태 메시지에 표시)

### 인터랙션 (User Actions)
1. 검색어 수정 → `03-검색중`
2. 검색어 초기화(X 버튼) → `02-초기-힌트`
3. ESC → 닫기

### 비즈니스 룰
- 검색 팁 제공으로 사용자 재시도 유도
- 결과 없음은 에러가 아님 (정상 응답 0건)

### 에지 케이스
- 오타 교정 제안 기능 없음 (단순 안내)
- 권한 제한으로 실제 존재하지만 결과 없음 → 일반 "없음" 메시지 (권한 정보 노출 금지)

### 접근성 (A11y)
- `role="status"` `aria-label="검색 결과 없음"`
- 빈 상태 메시지 `aria-live="polite"`

### 연결 화면
- 재검색: `03-검색중`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-103_글로벌검색/F6_상태별.md` → STATE_EMPTY 노드
