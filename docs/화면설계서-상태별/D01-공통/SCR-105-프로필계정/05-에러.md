# SCR-105 프로필/계정 — 상태: 에러 (데이터 로드 실패)

## 메타
| 항목 | 값 |
|------|----|
| 화면 ID | SCR-105 |
| 상태 코드 | `profile-error` |
| 경로 | `/profile` |
| 역할 | superAdmin / primary / owner / manager / fc / staff |
| 우선순위 | P2 |
| 다이어그램 | `docs/다이어그램/D01_공통/SCR-105_프로필_계정/F6_상태별.md` |
| 이전 상태 | `01-로딩` (로드 실패) |
| 다음 상태 | `01-로딩` (재시도) |

## 🧩 바이브코딩 프롬프트
```
Next.js 15 App Router + TypeScript + Tailwind + Supabase 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-105 프로필/계정 — 상태: 에러

파일: src/app/(dashboard)/profile/page.tsx (isError 분기)

레이아웃:
- AppLayout (사이드바 + 헤더)
- 에러 UI (메인 콘텐츠 영역)

컴포넌트 구조:
1. PageHeader: "프로필/계정"
2. 에러 블록 (max-w-2xl mx-auto, text-center, mt-16):
   - UserX 아이콘 (w-16 h-16 text-red-300 mx-auto)
   - "프로필 정보를 불러오지 못했습니다" (text-lg text-gray-600 mt-4)
   - 재시도 Button (primary, mt-6)

데이터:
- isError: boolean = true
- retry: () => void

인터랙션:
- 재시도 버튼 → `01-로딩` 재진입

사용 유틸:
- lucide-react (UserX, RefreshCw)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/users/{id}` API 500/502 응답
- Supabase 연결 실패

### 필수 데이터
- retry 함수

### 인터랙션 (User Actions)
1. 재시도 버튼 → 재로딩

### 비즈니스 룰
- 에러 토스트 병행 표시

### 에지 케이스
- 권한 없는 타인 프로필 접근 → 403 에러 페이지(SCR-108) 표시

### 접근성 (A11y)
- `role="alert"` `aria-live="assertive"`

### 연결 화면
- 재시도: `01-로딩`

### 다이어그램 참조
- F6: `docs/다이어그램/D01_공통/SCR-105_프로필_계정/F6_상태별.md` → STATE_ERROR 노드
