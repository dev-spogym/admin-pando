# SCR-074 마일리지관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-074 |
| 상태 코드 | `loading` |
| 경로 | `/mileage` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-074 마일리지관리 — 상태: 로딩 (Skeleton UI)

파일: src/app/(marketing)/mileage/page.tsx

레이아웃:
- AppLayout + PageHeader("마일리지 관리")
- max-w-7xl mx-auto px-6 py-6

Skeleton 구성:
1. 통계 카드 영역 (grid grid-cols-3 gap-4 mb-6):
   - 3개 카드: h-28 bg-gray-100 animate-pulse rounded-xl
2. 탭 네비게이션:
   - h-10 w-80 bg-gray-100 animate-pulse rounded mb-4
3. 탭 컨텐츠:
   - 헤더 행: h-10 bg-gray-200 animate-pulse rounded mb-2
   - 데이터 행 6개: h-12 bg-gray-100 animate-pulse rounded mb-1

구현:
- useEffect(() => { loadMileageData() }, [])
- Promise.all([fetchStats(), fetchRules(), fetchMembers(), fetchHistory()]) 병렬
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/mileage` 진입 시

### 연결 화면
- 다음: `02-정상` / `03-에러`
