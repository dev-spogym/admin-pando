# DLG-074-001 적립규칙편집 — 상태: 유효성 오류

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-074-001 |
| 상태 코드 | `validation-error` |
| 트리거 | 음수/0 값 입력 후 저장 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 수정 후 재시도 |

## 🧩 바이브코딩 프롬프트

```
유효성 오류 UI (MileageRuleModal.tsx):

음수 오류 (TC-074-M2-001-01):
- Input border-red-500
- "0보다 큰 값을 입력해주세요" (text-xs text-red-500 mt-1)

Zod 스키마:
const schema = z.object({
  rate: z.number().positive('0보다 큰 값을 입력해주세요'),
  monthlyLimit: z.number().min(0),
  isActive: z.boolean(),
})
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 음수 입력 (TC-074-M2-001-01)
- 0 입력 (적립률)

### 연결 화면
- 다음: 수정 후 `02-저장성공`
