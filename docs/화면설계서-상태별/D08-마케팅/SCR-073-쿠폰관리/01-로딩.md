# SCR-073 쿠폰관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-073 |
| 상태 코드 | `loading` |
| 경로 | `/message/coupon` |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-073 쿠폰관리 — 상태: 로딩 (Skeleton UI)

파일: src/app/(marketing)/message/coupon/page.tsx

레이아웃:
- AppLayout + PageHeader("쿠폰 관리")
- max-w-7xl mx-auto px-6 py-6

Skeleton 구성:
- 상단 툴바: h-10 w-full bg-gray-100 animate-pulse rounded mb-4
- DataTable:
  · 헤더 행: h-10 bg-gray-200 animate-pulse rounded mb-2
  · 데이터 행 8개: h-14 bg-gray-100 animate-pulse rounded mb-1

구현:
- useEffect(() => { loadCoupons() }, [])
- loading = true 초기값
- supabase.from('coupons') 쿼리
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/message/coupon` 진입 시
- 권한 확인(owner/manager) 통과 후

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
