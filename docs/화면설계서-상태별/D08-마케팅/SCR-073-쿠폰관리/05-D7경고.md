# SCR-073 쿠폰관리 — 상태: D-7 만료 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-073 |
| 상태 코드 | `d7-warning` |
| 경로 | `/message/coupon` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `02-정상` (서브 상태) |
| 다음 상태 | DLG-073-001 (수정) 또는 DLG-073-002 (발급) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-073 쿠폰관리 — 상태: D-7 만료 경고 (02-정상의 서브 상태)

파일: src/app/(marketing)/message/coupon/page.tsx

이 상태는 02-정상 상태와 동일하되, D-7 이내 만료 쿠폰이 있을 때 추가 UI:

1. 상단 경고 배너 (bg-amber-50 border border-amber-200 rounded-lg p-3 mb-4):
   <div className="flex items-center gap-2">
     <AlertTriangle className="w-4 h-4 text-amber-600" />
     <span className="text-sm text-amber-700">
       만료 임박 쿠폰이 {count}개 있습니다. 확인하세요.
     </span>
   </div>

2. DataTable 해당 행:
   - 유효기간 셀: amber D-N 배지 + 날짜
     <span className="text-xs bg-amber-100 text-amber-700 px-2 py-0.5 rounded mr-1">
       D-{daysLeft}
     </span>
   - 행 전체: bg-amber-50 하이라이트

구현:
- 유효기간 D-7 이내 쿠폰 필터링:
  const soonExpiring = coupons.filter(c => {
    const days = differenceInDays(new Date(c.validUntil), new Date())
    return days >= 0 && days <= 7 && c.status === 'active'
  })
- soonExpiring.length > 0 → 배너 표시

사용 유틸:
- differenceInDays from date-fns
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-정상` 상태에서 D-7 이내 만료 쿠폰 존재 (TC-073-F6-04)

### 비즈니스 룰
- D-day 계산: `differenceInDays(validUntil, today) <= 7`
- 만료된 쿠폰(D-0 초과)은 만료 배지로 별도 처리

### 에지 케이스
- D-0(당일 만료): "D-0" 배지 + 더 진한 amber

### 연결 화면
- 이전: `02-정상` (서브 상태)
- 다음: DLG-073-001 수정 / DLG-073-002 발급
