# DLG-070-001 리드등록수정 — 상태: 열림 (등록 모드)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-070-001 |
| 상태 코드 | `open-add` |
| 트리거 | SCR-070 "리드 등록" 버튼 |
| 역할 | owner, manager, fc |
| 우선순위 | P0 |
| 이전 상태 | SCR-070 정상/빈상태 |
| 다음 상태 | `03-저장성공` 또는 `04-유효성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-070-001 리드 등록/수정 모달 — 상태: 등록 모드 (빈 폼)

파일: src/components/marketing/LeadFormModal.tsx

모달 구조 (Modal 컴포넌트 기반):
- title: "리드 등록"
- size: lg (max-w-lg)

폼 필드:
1. 이름 * (Input, placeholder="이름을 입력하세요")
2. 연락처 (Input, placeholder="010-0000-0000")
3. 유입경로 * (Select):
   옵션: SNS / 지인소개 / 검색 / 전단지 / 기타
4. 상태 (Select, 기본값: '신규'):
   옵션: 신규 / 상담중 / 견적발송 / 등록완료 / 이탈
5. 담당 FC (Select): FC 목록 동적 로드
6. 문의일 (DatePicker, 기본값: 오늘)
7. 후속 일정 (DatePicker, optional)
8. 메모 (Textarea, rows=3, optional)

필드 레이아웃: grid grid-cols-2 gap-4 (이름/연락처, 유입경로/상태, 문의일/후속일정), 메모 col-span-2

하단 버튼:
- "취소" Button variant=outline onClick={onClose}
- "등록" Button variant=primary onClick={handleSave} loading={isSaving}

구현:
- react-hook-form + zod validation
- mode: 'add' → POST /leads (supabase.from('leads').insert)
- 성공: toast.success("리드가 등록되었습니다.") + onClose() + refreshLeads()

사용 컴포넌트:
- Modal, Button, Select, Textarea
- useAuthStore, supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-070 "리드 등록" 버튼 클릭 (E_DLG070001_OPEN_ADD)

### 필수 데이터
| 블록 | 설명 |
|------|------|
| FC 목록 | `users` where role='fc', branchId=? |

### 인터랙션 (User Actions)
1. 필드 입력 → 실시간 검증
2. "등록" → 필수 검증 → POST /leads
3. "취소"/X → 변경사항 폐기, 모달 닫힘

### 비즈니스 룰
- 이름 필수 (TC-070-003)
- 유입경로 필수
- 연락처: 숫자/하이픈만 허용 (선택)
- 코드 자동 생성: LEAD-{branchId}-{timestamp}

### 에지 케이스
- API 500: toast.error + 모달 유지 (TC-070-M3-01)

### 연결 화면
- 이전: SCR-070
- 다음: `03-저장성공` 또는 `04-유효성오류`
