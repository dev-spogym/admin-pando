# DLG-070-001 리드등록수정 — 상태: 열림 (수정 모드)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-070-001 |
| 상태 코드 | `open-edit` |
| 트리거 | SCR-070 수정 아이콘 클릭 |
| 역할 | owner, manager, fc |
| 우선순위 | P0 |
| 이전 상태 | SCR-070 정상 |
| 다음 상태 | `03-저장성공` 또는 `04-유효성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-070-001 리드 등록/수정 모달 — 상태: 수정 모드 (기존값 주입)

파일: src/components/marketing/LeadFormModal.tsx

01-열림-등록모드와 동일한 구조에서 다음만 변경:

1. title: "리드 수정"
2. 기존값 주입:
   - useEffect(() => { if (lead) form.reset(lead) }, [lead])
3. 하단 버튼:
   - "취소" variant=outline
   - "수정" variant=primary onClick={handleSave}
4. API: PATCH /leads/:id (supabase.from('leads').update)
5. 성공: toast.success("리드가 수정되었습니다.") (TC-070-004)

Props:
- mode: 'edit'
- lead: Lead (기존 데이터)
- onSuccess: () => void (목록 갱신)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-070 수정 아이콘 클릭 (E_DLG070001_OPEN_EDIT)

### 인터랙션 (User Actions)
1. 기존값 확인 후 수정
2. "수정" → 변경된 필드만 PATCH
3. "취소" → 변경 폐기

### 비즈니스 룰
- 수정 전/후 diff 확인 (변경 없으면 버튼 비활성 옵션)
- 이름/유입경로 필수 유지

### 연결 화면
- 이전: SCR-070
- 다음: `03-저장성공` 또는 `04-유효성오류`
