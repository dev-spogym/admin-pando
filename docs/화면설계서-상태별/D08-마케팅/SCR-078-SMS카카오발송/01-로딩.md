# SCR-078 SMS카카오발송 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-078 |
| 상태 코드 | `loading` |
| 경로 | `/messages/send` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-078 SMS/카카오 발송 — 상태: 로딩

파일: src/app/(marketing)/messages/send/page.tsx

레이아웃:
- AppLayout + PageHeader("SMS/카카오 발송")
- newFeature 배너 (bg-purple-50 border-dashed border-purple-300)

Skeleton:
- 툴바: h-10 animate-pulse rounded mb-4
- 발송 내역 테이블: 헤더 + 8행 animate-pulse

구현:
- supabase.from('message_logs').select('*').in('channel', ['SMS', 'KAKAO'])
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/messages/send` 진입 시

### 연결 화면
- 다음: `02-정상` / `03-빈상태`
