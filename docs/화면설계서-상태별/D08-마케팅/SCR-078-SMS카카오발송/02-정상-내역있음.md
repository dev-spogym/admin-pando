# SCR-078 SMS카카오발송 — 상태: 정상 (발송 내역 있음)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-078 |
| 상태 코드 | `normal-with-data` |
| 경로 | `/messages/send` |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-078-001(SMS발송) / DLG-078-002(카카오발송) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-078 SMS/카카오 발송 — 상태: 발송 내역 있음

파일: src/app/(marketing)/messages/send/page.tsx

레이아웃:
- AppLayout + PageHeader("SMS/카카오 발송",
    우측: "SMS 발송" Button + "카카오 발송" Button variant=primary)
- newFeature 배너

채널 탭 (TabNav):
- 전체 | SMS | 카카오

DataTable 컬럼:
- 채널 | 수신자 | 내용(truncate 40자) | 발송일시 | 상태(StatusBadge) | 처리자

StatusBadge:
- 성공(success): bg-green-100 text-green-700 (TC-078-F6-01 기준)
- 실패(failed): bg-red-100 text-red-700 "실패"
- 예약(scheduled): bg-blue-100 text-blue-700 "예약"
- 취소(cancelled): bg-gray-100 text-gray-500

실패 행 하이라이트: bg-red-50

데이터:
- supabase.from('message_logs').select('*').in('channel', ['SMS', 'KAKAO']).order('created_at', { ascending: false })

사용 컴포넌트:
- AppLayout, PageHeader, DataTable, TabNav, StatusBadge, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `message_logs.length > 0`

### 인터랙션 (User Actions)
1. "SMS 발송" → DLG-078-001
2. "카카오 발송" → DLG-078-002
3. 채널 탭 전환 → 필터

### 비즈니스 룰
- 실패 배지: TC-078-F6-01
- 실패 행 bg-red-50 하이라이트

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-078-001 / DLG-078-002
