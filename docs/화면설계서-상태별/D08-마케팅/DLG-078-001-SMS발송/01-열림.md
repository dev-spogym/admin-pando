# DLG-078-001 SMS발송 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-078-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-078 "SMS 발송" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-078 |
| 다음 상태 | `02-발송성공` 또는 `03-발송오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-078-001 SMS 발송 모달 (newFeature)

파일: src/components/marketing/SmsModal.tsx

모달 구조:
- title: "SMS 발송"
- size: md (max-w-md)
- newFeature 배지 (상단)

폼 필드:
1. 수신자 (Input, readonly) + "수신자 선택" Button
   - 선택된 수신자: chips 형태로 표시
2. 발신번호 (Select): 등록된 발신번호 목록
3. 메시지 내용 (Textarea, rows=5, maxLength=90)
   - 하단 글자수 카운터: {length}/90
   - 90자 초과 시 LMS 자동 전환 안내
4. 예약 발송 (Toggle + DateTimePicker, optional)

하단 버튼:
- "취소" variant=outline
- "발송" variant=primary onClick={handleSend}

구현:
- POST /messages/sms (supabase.from('message_logs').insert)
- 성공: toast.success("SMS가 발송되었습니다.")
- onClose() + refreshMessages()
- 예약 시: scheduled_at 포함
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-078 "SMS 발송" 버튼 (E_DLG078001_OPEN)

### 필수 데이터
- 수신자 1명 이상 선택
- 메시지 내용 1자 이상

### 인터랙션 (User Actions)
1. 수신자 선택 (회원 검색 팝업)
2. 메시지 작성 (90자 이내 SMS / 초과 시 LMS)
3. 예약 발송 토글 → 날짜/시간 선택
4. "발송" 클릭

### 비즈니스 룰
- 수신자 필수
- 내용 1~90자: SMS / 91자~: LMS (자동 전환)
- 발신번호 사전 등록 필요

### 연결 화면
- 다음: `02-발송성공` 또는 `03-발송오류`
