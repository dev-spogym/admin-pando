# DLG-071-002 발송미리보기 — 상태: 발송 성공

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-071-002 |
| 상태 코드 | `send-success` |
| 트리거 | POST /messages → 200 |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | `01-열림` |
| 다음 상태 | 모달 닫힘 → SCR-071 이력 탭 갱신 |

## 🧩 바이브코딩 프롬프트

```
발송 성공 처리 (MessagePreviewModal.tsx):

즉시 발송 성공 (TC-071-001):
- toast.success("메시지가 발송되었습니다.")
- onClose()
- refreshHistory() → SCR-071 이력 탭 갱신
- SCR-071 폼 초기화 (선택 사항)

예약 발송 성공 (TC-071-009):
- toast.success("메시지 예약이 완료되었습니다.")
- onClose()
- refreshHistory()

이력 탭 갱신:
- 새 항목 최상단에 추가 (낙관적 업데이트 또는 refetch)
- 상태: 즉시 발송 → '발송완료', 예약 → '예약'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- POST /messages → 200 SENT (TC-071-001)
- POST /messages → 200 SCHEDULED (TC-071-009)

### 연결 화면
- 다음: SCR-071 이력 탭 갱신됨
