# DLG-071-001 수신자검색 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-071-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-071 "대상 검색" 버튼 |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | SCR-071 정상 |
| 다음 상태 | `02-선택완료` 또는 `03-0명오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-071-001 수신자 검색 모달

파일: src/components/marketing/RecipientSearchModal.tsx

모달 구조:
- title: "수신자 선택"
- size: xl (max-w-2xl)

상단 검색/액션 바 (flex gap-3 mb-4):
- SearchInput: 이름/전화번호 실시간 필터 (onChange → 디바운스 300ms)
- "전체 선택" Button variant=ghost size=sm
- "전체 해제" Button variant=ghost size=sm

선택 현황 칩:
- "{N}명 선택됨" (bg-blue-50 text-blue-700 rounded-full px-3 py-1 text-sm)

회원 목록 (max-h-80 overflow-y-auto):
각 행 (flex items-center gap-3 p-3 hover:bg-gray-50 cursor-pointer):
- Checkbox (w-5 h-5, checked={selectedIds.includes(member.id)})
- 아바타 (w-8 h-8 rounded-full bg-gray-200 text-xs flex items-center justify-center)
- 이름 (text-sm font-medium) + 연락처 (text-xs text-gray-500)
- onClick: toggle 선택

검색 결과 없음:
- "검색 결과가 없습니다" 중앙 표시

하단 버튼:
- "취소" variant=outline onClick={onClose}
- "선택 완료 ({N}명)" variant=primary disabled={selectedIds.length === 0} onClick={handleConfirm}

구현:
- selectedIds: string[] state
- handleConfirm: onSelect(selectedIds) 호출 → 모달 닫힘
- members: supabase.from('members').select('id, name, phone').eq('branchId', branchId)

사용 컴포넌트:
- Modal, Button
- supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-071 "대상 검색" 버튼 클릭 (TC-071-008)

### 필수 데이터
| 블록 | 테이블 | 조건 |
|------|--------|------|
| 회원 목록 | `members` | branchId = ?, 검색 필터 |

### 인터랙션 (User Actions)
1. 검색 → 실시간 필터 (TC-071-M1-001-01)
2. 체크박스 → 개별 선택/해제
3. "전체 선택" → 모든 회원 선택
4. "선택 완료" → 수신자 목록 업데이트 (TC-071-M1-001-02)
5. "취소"/X → 기존 수신자 유지

### 비즈니스 룰
- 0명 선택 시 "선택 완료" 버튼 비활성 (TC-071-M2-001-01)
- 전체 선택 후 일부 해제 가능

### 연결 화면
- 다음: `02-선택완료` 또는 `03-0명오류`
