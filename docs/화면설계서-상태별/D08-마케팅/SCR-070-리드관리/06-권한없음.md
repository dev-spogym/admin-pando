# SCR-070 리드관리 — 상태: 권한없음

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-070 |
| 상태 코드 | `blocked` |
| 경로 | `/leads` |
| 역할 | staff, readonly (차단) |
| 우선순위 | P1 |
| 이전 상태 | 진입 (권한 확인) |
| 다음 상태 | 없음 (접근 차단) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-070 리드관리 — 상태: 권한없음

파일: src/app/(marketing)/leads/page.tsx

권한 없음 UI:
- AppLayout + PageHeader("리드 관리")
- 본문 중앙: flex flex-col items-center py-24
  · 아이콘: Lock (lucide, w-16 h-16 text-gray-300 mb-4)
  · 제목: "접근 권한이 없습니다" (text-xl font-bold text-gray-600)
  · 설명: "리드 관리는 매니저 이상 역할에서만 접근할 수 있습니다." (text-sm text-gray-400 mt-2)
  · Button "대시보드로 이동" onClick={() => moveToPage('/dashboard')} variant=outline mt-6

구현:
- 페이지 최상단: const { user } = useAuthStore()
- hasPermission(user.role, 'leads') 검사
- false이면 권한없음 UI 반환 (redirect 아닌 인라인 표시)

사용 컴포넌트:
- AppLayout, PageHeader, Button
- useAuthStore, hasPermission, moveToPage
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로그인 사용자 role이 staff 또는 readonly인 경우
- `hasPermission(role, 'leads') === false`

### 인터랙션 (User Actions)
1. "대시보드로 이동" → `/dashboard`

### 비즈니스 룰
- 서버사이드 미들웨어 + 클라이언트 이중 검사
- 권한 없는 역할: staff, readonly

### 연결 화면
- 다음: `/dashboard` (이탈)
