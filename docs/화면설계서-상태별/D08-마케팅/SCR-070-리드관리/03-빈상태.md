# SCR-070 리드관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-070 |
| 상태 코드 | `empty` |
| 경로 | `/leads` |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-070-001 (리드 등록) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-070 리드관리 — 상태: 빈 상태 (리드 0건)

파일: src/app/(marketing)/leads/page.tsx

레이아웃:
- AppLayout + PageHeader("리드 관리", 우측: "리드 등록" Button)
- max-w-7xl mx-auto px-6 py-6

통계 카드 영역:
- 4개 카드 모두 0 표시 (숫자 text-2xl font-bold text-gray-300)
- 전환율: "0.0%"

Empty State 영역 (DataTable 위치):
- 중앙 정렬 flex flex-col items-center py-20
- 아이콘: Users (lucide, w-16 h-16 text-gray-300 mb-4)
- 제목: "아직 등록된 리드가 없습니다" (text-lg font-semibold text-gray-500)
- 설명: "첫 번째 리드를 등록하고 영업 파이프라인을 시작해보세요." (text-sm text-gray-400 mt-1)
- CTA: Button variant=primary "리드 등록하기" → DLG-070-001 open

구현:
- leads.length === 0 조건 분기
- 통계 카드는 0 값으로 렌더

사용 컴포넌트:
- AppLayout, PageHeader, Button
- useAuthStore, supabase
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 후 `leads.length === 0`
- 필터 조건 없는 전체 조회 기준

### 필수 데이터
- `leads` count = 0 확인

### 인터랙션 (User Actions)
1. "리드 등록하기" 버튼 → DLG-070-001 열기 (등록 모드)
2. PageHeader "리드 등록" 버튼 → 동일

### 비즈니스 룰
- 통계 카드는 숨기지 않고 0 표시 (TC-070-012)
- 필터 바는 비활성화하지 않음 (향후 데이터 대비)

### 에지 케이스
- 필터 적용 후 0건: 이 상태 아님, 테이블 내 inline empty 메시지

### 연결 화면
- 이전: `01-로딩`
- 다음: DLG-070-001 → 등록 성공 시 `02-정상`
