# SCR-070 리드관리 — 상태: 에러 (서버 오류)

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-070 |
| 상태 코드 | `error-server` |
| 경로 | `/leads` |
| 역할 | owner, manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 새로고침 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-070 리드관리 — 상태: 에러 (500 서버 오류)

파일: src/app/(marketing)/leads/page.tsx

에러 상태 UI:
- AppLayout + PageHeader("리드 관리")
- 본문 중앙: flex flex-col items-center py-20
  · 아이콘: AlertCircle (lucide, w-16 h-16 text-red-400 mb-4)
  · 제목: "데이터를 불러오지 못했습니다" (text-lg font-semibold text-gray-700)
  · 설명: "서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요." (text-sm text-gray-400 mt-1)
  · Button "새로고침" onClick={() => window.location.reload()} variant=outline mt-4

구현:
- try-catch 에서 error state = true
- error.status === 500 || !error.status 조건
- toast.error("데이터를 불러오지 못했습니다.") 1회 발사

사용 컴포넌트:
- AppLayout, PageHeader, Button
- toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- Supabase API 500 응답
- 쿼리 실패 (네트워크 외 서버 측 오류)

### 필수 데이터
- error 객체 (로깅용)

### 인터랙션 (User Actions)
1. "새로고침" 버튼 → `window.location.reload()`

### 비즈니스 룰
- toast는 최초 1회만 (중복 방지)
- 3회 연속 실패 시 고객센터 CTA 추가 노출

### 에지 케이스
- 401/403: 로그인 페이지 리다이렉트 (useAuthStore 처리)
- 부분 실패: 해당 섹션만 에러, 나머지 정상 표시

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 → `01-로딩`
