# SCR-070 리드관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-070 |
| 상태 코드 | `loading` |
| 경로 | `/leads` |
| 역할 | owner, manager, fc |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-070 리드관리 — 상태: 로딩 (Skeleton UI)

파일: src/app/(marketing)/leads/page.tsx

레이아웃:
- AppLayout + PageHeader("리드 관리")
- 최대 너비 max-w-7xl mx-auto px-6 py-6

Skeleton 구성:
1. 통계 카드 영역 (grid grid-cols-4 gap-4 mb-6):
   - 4개 카드 각각: h-24 rounded-xl bg-gray-100 animate-pulse
2. 필터/검색 바 영역:
   - h-10 w-full rounded-lg bg-gray-100 animate-pulse mb-4
3. DataTable 영역:
   - 헤더 행: h-10 bg-gray-200 animate-pulse rounded mb-2
   - 데이터 행 8개: h-12 bg-gray-100 animate-pulse rounded mb-1

구현:
- useEffect(() => { loadLeads() }, [])
- loading state = true 초기값
- Promise.all([fetchStats(), fetchLeads()]) 병렬 실행

사용 컴포넌트:
- AppLayout, PageHeader
- useAuthStore().user
- supabase (createClient)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/leads` 경로 최초 진입 시
- 권한 확인(owner/manager/fc) 통과 후 즉시 표시
- 필터 변경·페이지 이동 시 재표시

### 필수 데이터
| 블록 | 설명 |
|------|------|
| 통계 카드 | `leads` 테이블 집계 쿼리 |
| 리드 목록 | `leads` 테이블 페이지네이션 쿼리 |

### 인터랙션 (User Actions)
- 없음 (로딩 중 입력 불가)

### 비즈니스 룰
- 로딩 중 버튼 비활성화
- 500ms 이상 지속 시 Skeleton 표시 (UX 기준)

### 에지 케이스
- 2초 초과 시 타임아웃 에러 → `04-에러` 전환

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러` / `05-권한없음` / `06-오프라인`
