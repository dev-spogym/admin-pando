# SCR-070 리드관리 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-070 |
| 상태 코드 | `offline` |
| 경로 | `/leads` |
| 역할 | owner, manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-070 리드관리 — 상태: 오프라인 (네트워크 오류)

파일: src/app/(marketing)/leads/page.tsx

오프라인 상태 UI:
- AppLayout + PageHeader("리드 관리")
- 상단 배너 (bg-amber-50 border border-amber-200 rounded-lg p-3 flex items-center gap-2 mb-6):
  · WifiOff 아이콘 (lucide, w-4 h-4 text-amber-600)
  · "네트워크 연결을 확인해주세요." (text-sm text-amber-700)
- 본문 중앙: flex flex-col items-center py-16
  · 아이콘: WifiOff (w-16 h-16 text-gray-300 mb-4)
  · 제목: "인터넷 연결이 끊겼습니다" (text-lg font-semibold text-gray-600)
  · Button "다시 시도" onClick={handleRetry} variant=outline mt-4

구현:
- catch (err) { if (!navigator.onLine) setIsOffline(true) }
- handleRetry: setIsOffline(false) → loadLeads() 재호출
- window.addEventListener('online', handleRetry) 자동 복구

사용 컴포넌트:
- AppLayout, PageHeader, Button
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `navigator.onLine === false`
- fetch 네트워크 에러 (ERR_NETWORK)

### 인터랙션 (User Actions)
1. "다시 시도" → `loadLeads()` 재호출
2. 네트워크 복구 감지 → 자동 재시도

### 비즈니스 룰
- 온라인 복구 이벤트(`window.online`) 수신 시 자동 재로드

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 성공 → `02-정상` 또는 `03-빈상태`
