# SCR-071 메시지발송 — 상태: 오프라인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-071 |
| 상태 코드 | `offline` |
| 경로 | `/message` |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-071 메시지발송 — 상태: 오프라인

파일: src/app/(marketing)/message/page.tsx

오프라인 UI:
- AppLayout + PageHeader("메시지 발송")
- 상단 amber 배너:
  <div className="bg-amber-50 border border-amber-200 rounded-lg p-3 flex items-center gap-2 mb-6">
    <WifiOff className="w-4 h-4 text-amber-600" />
    <span className="text-sm text-amber-700">네트워크 연결을 확인해주세요.</span>
  </div>
- 본문 중앙:
  · WifiOff 아이콘 (w-14 h-14 text-gray-300 mb-4)
  · "인터넷 연결이 끊겼습니다" (text-lg font-semibold text-gray-600)
  · Button "다시 시도" onClick={handleRetry} variant=outline mt-4

구현:
- !navigator.onLine 감지
- window.addEventListener('online', handleRetry) 자동 복구
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 네트워크 오류 (ERR_NETWORK)
- `!navigator.onLine`

### 인터랙션 (User Actions)
1. "다시 시도" → 재로드
2. 네트워크 복구 → 자동 재시도

### 연결 화면
- 다음: 재시도 성공 → `02-정상` 또는 `03-빈이력`
