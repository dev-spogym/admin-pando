# SCR-071 메시지발송 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-071 |
| 상태 코드 | `loading` |
| 경로 | `/message` |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈이력` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-071 메시지발송 — 상태: 로딩 (Skeleton UI)

파일: src/app/(marketing)/message/page.tsx

레이아웃:
- AppLayout + PageHeader("메시지 발송")
- max-w-7xl mx-auto px-6 py-6
- 좌우 2열 레이아웃: 발송 폼(좌 40%) + 이력 테이블(우 60%)

Skeleton 구성:
[좌측 발송 폼]:
- 채널 선택: h-10 bg-gray-100 animate-pulse rounded mb-3
- 수신자 영역: h-10 bg-gray-100 animate-pulse rounded mb-3
- 메시지 내용: h-32 bg-gray-100 animate-pulse rounded mb-3
- 예약 설정: h-10 bg-gray-100 animate-pulse rounded mb-3
- 발송 버튼: h-10 w-full bg-gray-200 animate-pulse rounded

[우측 이력 테이블]:
- 헤더: h-10 bg-gray-200 animate-pulse rounded mb-2
- 행 6개: h-12 bg-gray-100 animate-pulse rounded mb-1

구현:
- useEffect(() => { loadHistory() }, [])
- loading = true 초기값
- supabase.from('message_logs') 쿼리
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `/message` 경로 진입 시
- 권한 확인 통과 후 즉시 표시

### 필수 데이터
| 블록 | 설명 |
|------|------|
| 발송 이력 | `message_logs` 테이블 조회 |

### 연결 화면
- 다음: `02-정상` / `03-빈이력` / `04-에러` / `05-권한없음` / `06-오프라인`
