# SCR-071 메시지발송 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-071 |
| 상태 코드 | `error` |
| 경로 | `/message` |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P2 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 새로고침 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-071 메시지발송 — 상태: 에러 (이력 로드 실패)

파일: src/app/(marketing)/message/page.tsx

에러 UI:
- AppLayout + PageHeader("메시지 발송")
- 우측 이력 영역에 인라인 에러:
  <div className="flex flex-col items-center py-12">
    <AlertCircle className="w-12 h-12 text-red-400 mb-3" />
    <p className="text-sm text-red-600 font-medium">이력을 불러오지 못했습니다</p>
    <button onClick={loadHistory} className="mt-2 text-xs text-blue-500 underline">
      다시 시도
    </button>
  </div>
- 좌측 발송 폼은 정상 렌더 유지

구현:
- historyError state 분리 (전체 에러 아닌 부분 에러)
- toast.error("발송 이력을 불러오지 못했습니다.") 1회
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `message_logs` 쿼리 500 응답
- 발송 폼은 영향 없음

### 인터랙션 (User Actions)
1. "다시 시도" → `loadHistory()` 재호출
2. 발송 폼은 계속 사용 가능

### 연결 화면
- 이전: `01-로딩`
- 다음: 재시도 성공 → `02-정상` 또는 `03-빈이력`
