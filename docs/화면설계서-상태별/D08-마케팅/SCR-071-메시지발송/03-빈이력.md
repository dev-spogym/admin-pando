# SCR-071 메시지발송 — 상태: 빈 이력

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-071 |
| 상태 코드 | `empty-history` |
| 경로 | `/message` |
| 역할 | owner, manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | DLG-071-001 → DLG-071-002 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 데스크톱 페이지를 작성하라.
화면: SCR-071 메시지발송 — 상태: 빈 이력 (발송 이력 없음)

파일: src/app/(marketing)/message/page.tsx

레이아웃:
- AppLayout + PageHeader("메시지 발송")
- grid grid-cols-5 gap-6

[좌측 발송 폼]: 02-정상 상태와 동일하게 렌더 (폼은 항상 표시)

[우측 빈 이력]:
- bg-white rounded-xl border p-6
- "발송 이력" 제목
- 이력 없음 Empty State (py-16 flex flex-col items-center):
  · MessageSquare 아이콘 (lucide, w-14 h-14 text-gray-300 mb-3)
  · "아직 발송 이력이 없습니다" (text-base font-medium text-gray-500)
  · "첫 메시지를 발송해보세요." (text-sm text-gray-400 mt-1)

구현:
- message_logs.length === 0 조건 분기
- 좌측 폼은 동일 렌더 유지
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 + `message_logs.length === 0`

### 인터랙션 (User Actions)
- 좌측 폼으로 메시지 발송 → 이력 생성 → `02-정상` 전환

### 연결 화면
- 이전: `01-로딩`
- 다음: 발송 성공 → `02-정상`
