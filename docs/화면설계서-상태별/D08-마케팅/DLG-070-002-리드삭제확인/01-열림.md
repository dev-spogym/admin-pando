# DLG-070-002 리드삭제확인 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-070-002 |
| 상태 코드 | `open` |
| 트리거 | SCR-070 삭제 아이콘 클릭 |
| 역할 | owner |
| 우선순위 | P1 |
| 이전 상태 | SCR-070 정상 |
| 다음 상태 | `02-삭제성공` 또는 `03-삭제오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-070-002 리드 삭제 확인 모달

파일: src/components/marketing/LeadDeleteModal.tsx

모달 구조:
- title: "리드 삭제"
- variant: danger (헤더 bg-red-50, 타이틀 text-red-700)
- size: sm (max-w-sm)

본문:
<div className="text-center py-4">
  <AlertTriangle className="w-12 h-12 text-red-500 mx-auto mb-3" />
  <p className="text-base font-semibold text-gray-800">"{lead.name}" 리드를 삭제하시겠습니까?</p>
  <p className="text-sm text-gray-500 mt-1">삭제된 리드는 복구할 수 없습니다.</p>
</div>

하단 버튼:
- "취소" Button variant=outline onClick={onClose}
- "삭제" Button variant=danger onClick={handleDelete} loading={isDeleting}

구현:
- handleDelete: DELETE /leads/:id (supabase.from('leads').delete().eq('id', lead.id))
- 성공: toast.success("삭제되었습니다.") + onClose() + refreshLeads()
- 실패: toast.error("삭제에 실패했습니다.")

사용 컴포넌트:
- Modal, Button
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-070 삭제 아이콘 클릭 (E_DLG070002_OPEN)
- owner 역할만 삭제 가능

### 인터랙션 (User Actions)
1. "삭제" → DELETE API (TC-070-005)
2. "취소"/X → 모달 닫힘, 데이터 유지 (TC-070-M_002-01)

### 비즈니스 룰
- 소프트 삭제 또는 하드 삭제 (정책에 따라)
- 계약 연결된 리드는 삭제 불가 (서버에서 409 반환)

### 에지 케이스
- 404: "이미 삭제된 리드입니다." toast.error

### 연결 화면
- 다음: `02-삭제성공` 또는 `03-삭제오류`
