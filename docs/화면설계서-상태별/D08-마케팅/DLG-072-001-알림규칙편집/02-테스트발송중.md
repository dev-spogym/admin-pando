# DLG-072-001 알림규칙편집 — 상태: 테스트 발송 중

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-072-001 |
| 상태 코드 | `test-sending` |
| 트리거 | "테스트 발송" 버튼 클릭 |
| 역할 | owner, manager |
| 우선순위 | P2 |
| 이전 상태 | `01-열림-편집모드` |
| 다음 상태 | 테스트 완료 후 `01-열림` 복귀 |

## 🧩 바이브코딩 프롬프트

```
테스트 발송 처리 (AlarmRuleModal.tsx):

handleTestSend:
1. 수신 번호 검증 (빈값이면 toast.warning("테스트 수신 번호를 입력해주세요."))
2. POST /messages/test { channel, message, recipient: testPhone }
3. 성공: toast.success("테스트 발송이 완료되었습니다.")
4. 실패: toast.error("테스트 발송에 실패했습니다.")

테스트 발송 버튼 loading:
<Button
  variant=outline
  size=sm
  onClick={handleTestSend}
  disabled={isTestSending}
>
  {isTestSending ? <Loader2 className="w-3 h-3 animate-spin" /> : "테스트 발송"}
</Button>

메인 폼은 계속 편집 가능 (테스트와 독립적)
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 테스트 발송 버튼 클릭

### 비즈니스 룰
- 테스트 발송은 모달 유지 상태에서 실행
- 실제 발송과 별도 API 엔드포인트

### 연결 화면
- 다음: 완료 후 `01-열림` 복귀
