# DLG-077-001 리퍼럴설정 — 상태: 열림

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-077-001 |
| 상태 코드 | `open` |
| 트리거 | SCR-077 "설정" 버튼 또는 "리퍼럴 프로그램 시작하기" |
| 역할 | owner, manager |
| 우선순위 | P1 |
| 이전 상태 | SCR-077 |
| 다음 상태 | `02-저장성공` 또는 `03-저장오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-077-001 리퍼럴 설정 모달 (newFeature)

파일: src/components/marketing/ReferralSettingModal.tsx

모달 구조:
- title: "리퍼럴 프로그램 설정"
- size: lg (max-w-lg)
- newFeature 배지 (상단)

폼 필드:
1. 프로그램 활성화 (Toggle switch)
2. 초대자 보상 (Input, number) + "P" suffix
3. 피초대자 보상 (Input, number) + "P" suffix
4. 보상 지급 시점 (Select):
   옵션: 가입 즉시 / 첫 결제 완료 / 첫 수업 완료
5. 유효기간 (Input, number) + "일" suffix
6. 최대 초대 수 (Input, number, 0=무제한)
7. 안내 메시지 (Textarea, rows=3)

하단 버튼:
- "취소" variant=outline
- "저장" variant=primary onClick={handleSave}

구현:
- PATCH /referral/settings
- 성공: toast.success("리퍼럴 설정이 저장되었습니다.")
- onClose() + refreshReferral()
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-077 설정 버튼 (TC-DLG-077-001-M1-01)
- SCR-077 초기 설정 유도 화면 CTA

### 비즈니스 룰
- 보상 포인트 > 0
- 유효기간 > 0

### 연결 화면
- 다음: `02-저장성공` 또는 `03-저장오류`
