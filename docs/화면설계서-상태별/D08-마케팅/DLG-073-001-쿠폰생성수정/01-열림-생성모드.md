# DLG-073-001 쿠폰생성수정 — 상태: 열림 (생성 모드)

## 메타

| 항목 | 값 |
|------|----|
| 모달 ID | DLG-073-001 |
| 상태 코드 | `open-create` |
| 트리거 | SCR-073 "쿠폰 생성" 버튼 |
| 역할 | owner, manager |
| 우선순위 | P0 |
| 이전 상태 | SCR-073 |
| 다음 상태 | `03-저장성공` 또는 `04-유효성오류` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 CRM 모달 컴포넌트를 작성하라.
화면: DLG-073-001 쿠폰 생성/수정 모달 — 생성 모드

파일: src/components/marketing/CouponFormModal.tsx

모달 구조:
- title: "쿠폰 생성"
- size: lg (max-w-lg)

쿠폰 코드 자동생성 (상단):
<div className="flex items-center gap-2 mb-4 p-3 bg-gray-50 rounded-lg">
  <code className="flex-1 font-mono text-sm font-bold text-gray-800">{couponCode}</code>
  <Button variant=ghost size=sm onClick={generateCode}>
    <RefreshCw className="w-4 h-4" /> 재생성
  </Button>
</div>
- 초기값: generateCouponCode() → "COUP-XXXX-XXXX" 패턴 (TC-073-002)
- RefreshCw 클릭: 새 코드 생성 (TC-073-003)

폼 필드 (grid grid-cols-2 gap-4):
1. 쿠폰명 * (Input)
2. 쿠폰 유형 * (Select): 할인쿠폰 / 무료이용권
3. 할인 방식 (Select, 유형=할인쿠폰 시): 금액할인 / 비율할인
4. 할인 금액/비율 * (Input, number)
5. 유효 시작일 * (DatePicker)
6. 유효 종료일 * (DatePicker)
7. 발급 한도 (Input, number, 0=무제한)
8. 설명 (Textarea, col-span-2, optional)

하단 버튼:
- "취소" variant=outline
- "생성" variant=primary onClick={handleSave}

구현:
- generateCouponCode: `COUP-${randomHex(4).toUpperCase()}-${randomHex(4).toUpperCase()}`
- POST /coupons (supabase.from('coupons').insert)
- 201: toast.success("쿠폰이 생성되었습니다.") (TC-073-001)
- 409 중복: toast.error("이미 사용 중인 코드입니다.")

사용 컴포넌트:
- Modal, Button, Select, Textarea
- supabase, toast
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-073 "쿠폰 생성" 버튼 (E_DLG073001_OPEN_NEW)

### 인터랙션 (User Actions)
1. 코드 재생성 (TC-073-003)
2. 유형 변경 → 할인 필드 조건부 표시
3. 유효기간 선택
4. "생성" → 검증 후 API

### 비즈니스 룰
- 쿠폰명 필수 (TC-073-M2-001-01)
- 종료일 ≥ 시작일 (TC-073-M2-001-02)
- 409 코드 중복: 재생성 안내

### 연결 화면
- 다음: `03-저장성공` 또는 `04-유효성오류`
