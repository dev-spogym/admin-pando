# DLG-C012 횟수조정 — 상태: 입력 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C012 |
| 상태 코드 | `form-input` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 저장 → `03-저장결과` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionAdjustModal.tsx

모달 구조: w-[400px]
모달 헤더: "횟수 조정"

현재 잔여 표시:
<div className="rounded-lg bg-gray-50 p-3 text-center mb-4">
  <p className="text-sm text-gray-500">현재 잔여 횟수</p>
  <p className="text-2xl font-bold">{currentRemaining}회</p>
</div>

Zod 스키마:
const adjustSchema = z.object({
  amount: z.number().int().refine(v => v !== 0, "조정값을 입력해주세요."),
  reason: z.string().min(1, "조정 사유를 입력해주세요.").max(200),
}).refine(
  data => currentRemaining + data.amount >= 0,
  { message: "잔여 횟수가 0 미만이 될 수 없습니다.", path: ['amount'] }
)

폼 필드:
1. 조정값 (Input number, +/- prefix 버튼):
   <div className="flex gap-2">
     <Button type="button" variant="outline" onClick={() => setSign(-1)}>-</Button>
     <Input type="number" min={1} {...register('absAmount')} />
     <Button type="button" variant="outline" onClick={() => setSign(1)}>+</Button>
   </div>
   <p className="text-sm text-gray-500 mt-1">
     조정 후 잔여: {currentRemaining + watchedAmount}회
   </p>

2. 조정 사유 * (Textarea, rows=3, maxLength=200)

모달 푸터:
- "취소" Button outline → onClose()
- "저장" Button primary
```

## 📝 디스크립션

### 유효성 검사
| 필드 | 규칙 |
|------|------|
| 조정값 | 0이 아닌 정수 |
| 사유 | 필수, 최대 200자 |
| 조정 후 잔여 | 0 이상 |

### 연결 화면
- 저장: `03-저장결과`
- 닫기: `01-닫힘`
