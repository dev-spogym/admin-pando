# SCR-C008 페널티관리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C008 |
| 상태 코드 | `empty` |
| 경로 | `/penalties` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <ShieldOff className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">페널티 내역이 없습니다.</p>
  <p className="text-sm text-gray-400">회원의 노쇼/지각취소 시 자동으로 기록됩니다.</p>
  {hasPermission(['manager','fc']) && (
    <Button onClick={() => setIsRegisterOpen(true)}>페널티 등록</Button>
  )}
</div>
```

## 📝 디스크립션
- 페널티 레코드 없음 또는 필터 결과 없음
- 연결: DLG-C014 페널티 등록
