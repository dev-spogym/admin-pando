# SCR-C009 일정요청처리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C009 |
| 상태 코드 | `loading` |
| 경로 | `/schedule-requests` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C009 일정요청처리 — 상태: 로딩

파일: src/app/schedule-requests/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "일정 요청 처리")
- 미처리 건수 배지 Skeleton
- DataTable loading=true

데이터:
- SELECT sr.*, m.name as member_name
  FROM schedule_requests sr JOIN users m ON sr.member_id=m.id
  WHERE sr.branch_id=? ORDER BY sr.created_at DESC
```

## 📝 디스크립션

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
