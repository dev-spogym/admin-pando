# SCR-C009 일정요청처리 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C009 |
| 상태 코드 | `empty` |
| 경로 | `/schedule-requests` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |

## 🧩 바이브코딩 프롬프트

```
빈 상태 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <CalendarCheck className="w-12 h-12 text-gray-300" />
  <p className="text-base font-medium text-gray-500">처리할 일정 요청이 없습니다.</p>
  <p className="text-sm text-gray-400">회원이 일정 변경 요청 시 여기에 표시됩니다.</p>
</div>
```

## 📝 디스크립션
- 요청 없음 또는 선택 탭 필터 결과 없음
- "대기중" 탭에서 빈 상태: "모든 요청이 처리되었습니다." 긍정 메시지
