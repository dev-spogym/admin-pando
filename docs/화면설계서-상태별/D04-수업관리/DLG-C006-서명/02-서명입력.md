# DLG-C006 서명 — 상태: 서명 입력

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C006 |
| 상태 코드 | `sign-input` |
| 부모 화면 | DLG-C005 수업기록상세 |
| 역할 | manager, fc |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` (DLG-C005 완료처리 버튼) |
| 다음 상태 | 확인 → `03-저장중` / X/ESC → `01-닫힘` (DLG-C005 유지) |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 서명 입력 UI를 작성하라.
화면: DLG-C006 서명 — 상태: 서명 입력

파일: src/components/classes/SignatureModal.tsx

모달 구조 (isOpen=true):
- 오버레이: fixed inset-0 bg-black/50 z-60 (DLG-C005 위)
- 모달 컨테이너: bg-white rounded-xl shadow-xl w-[480px] mx-auto mt-20

모달 헤더:
- 제목: "수업 완료 서명" (text-lg font-semibold)
- X 버튼: onClose() → DLG-C005 유지

서명 캔버스:
import SignaturePad from 'react-signature-pad-wrapper'

<div className="p-6 space-y-4">
  <p className="text-sm text-gray-500">아래 영역에 서명해주세요.</p>
  <div className="border-2 border-dashed border-gray-300 rounded-lg overflow-hidden">
    <SignaturePad
      ref={sigPadRef}
      options={{ minWidth: 1, maxWidth: 3, penColor: '#1a1a1a' }}
      width={432}
      height={200}
    />
  </div>
  <div className="flex justify-between">
    <Button variant="ghost" size="sm" onClick={() => sigPadRef.current?.clear()}>
      지우기
    </Button>
    <p className="text-xs text-gray-400">최소 3획 이상 서명해주세요.</p>
  </div>
</div>

모달 푸터:
- "취소" Button outline → onClose()
- "확인" Button primary → 유효성 검사 → `03-저장중`
```

## 📝 디스크립션

### 유효성 검사
| 조건 | 에러 |
|------|------|
| 캔버스 빈 상태 | "서명을 입력해주세요." |
| 3획 미만 | "서명이 너무 짧습니다. 다시 서명해주세요." |

### 인터랙션
1. 서명 입력 → canvas에 그리기
2. 지우기 → 캔버스 초기화
3. 확인 → 유효성 검사 → `03-저장중`
4. X/취소 → DLG-C006 닫힘, DLG-C005 유지

### 연결 화면
- 확인(유효): `03-저장중`
- X/취소: `01-닫힘` (DLG-C005 유지)
