# SCR-C006 강사근무현황 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C006 |
| 상태 코드 | `loading` |
| 경로 | `/instructor-status` |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C006 강사 근무현황 — 상태: 로딩

파일: src/app/(classes)/instructor-status/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "강사 근무현황")
- 월 선택기 Skeleton (h-10 w-40 animate-pulse)
- StatCardGrid Skeleton (4개 카드)
- DataTable loading=true

데이터:
- SELECT u.*, COUNT(ls.id) as lesson_count,
    SUM(EXTRACT(EPOCH FROM (ls.end_at - ls.start_at))/3600) as total_hours
  FROM users u
  LEFT JOIN lesson_schedules ls ON u.id=ls.instructor_id
    AND ls.start_at >= ? AND ls.start_at < ?
  WHERE u.role='instructor' AND u.branch_id=?
  GROUP BY u.id
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C006 진입 시
- 월 변경 후 재조회

### 연결 화면
- 다음: `02-정상` / `03-빈상태` / `04-에러`
