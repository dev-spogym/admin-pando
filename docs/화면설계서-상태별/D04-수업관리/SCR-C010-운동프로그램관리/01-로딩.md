# SCR-C010 운동프로그램관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C010 |
| 상태 코드 | `loading` |
| 경로 | `/exercise-programs` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C010 운동프로그램관리 — 상태: 로딩

파일: src/app/(classes)/exercise-programs/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "운동 프로그램 관리")
- TabNav Skeleton: "프로그램 목록 | 회원별 프로그램" 탭
- DataTable loading=true

데이터 (병렬):
- exercise_programs: SELECT * FROM exercise_programs WHERE branch_id=?
- member_programs: SELECT mep.*, m.name as member_name, ep.name as program_name
    FROM member_exercise_programs mep
    JOIN users m ON mep.member_id=m.id
    JOIN exercise_programs ep ON mep.program_id=ep.id
    WHERE mep.branch_id=?
```

## 📝 디스크립션

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
