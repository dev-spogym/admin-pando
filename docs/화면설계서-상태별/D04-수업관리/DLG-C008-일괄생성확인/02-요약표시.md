# DLG-C008 일괄생성확인 — 상태: 요약 표시

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C008 |
| 상태 코드 | `summary` |
| 부모 화면 | SCR-C003 시간표일괄등록 |
| 역할 | manager, owner |
| 우선순위 | P0 |
| 이전 상태 | `01-닫힘` (SCR-C003 생성 버튼) |
| 다음 상태 | 확인 → `03-생성중` / 취소 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/BulkCreateConfirmModal.tsx

모달 구조:
- 크기: w-[480px]
- 헤더: "{summary.totalCount}개 수업 일괄 생성"

요약 표시 (p-6 space-y-3):
<dl className="space-y-2 text-sm">
  <div className="flex justify-between">
    <dt className="text-gray-500">기간</dt>
    <dd>{format(summary.startDate,'yyyy.MM.dd')} ~ {format(summary.endDate,'yyyy.MM.dd')}</dd>
  </div>
  <div className="flex justify-between">
    <dt className="text-gray-500">요일</dt>
    <dd>{summary.weekdays.join(', ')}</dd>
  </div>
  <div className="flex justify-between">
    <dt className="text-gray-500">시간</dt>
    <dd>{summary.startTime} ~ {summary.endTime}</dd>
  </div>
  <div className="flex justify-between">
    <dt className="text-gray-500">강사</dt>
    <dd>{summary.instructorName}</dd>
  </div>
  <div className="flex justify-between font-medium">
    <dt>총 수업 수</dt>
    <dd className="text-blue-600">{summary.totalCount}개</dd>
  </div>
</dl>

100개 초과 경고 (summary.totalCount > 100):
<div className="rounded-lg border border-yellow-200 bg-yellow-50 p-3 text-sm text-yellow-800">
  100개 이상의 수업을 생성합니다. 계속하시겠습니까?
</div>

모달 푸터:
- "취소" Button outline → onClose()
- "확인" Button primary → `03-생성중`
```

## 📝 디스크립션

### 유효성 체크
| 조건 | 동작 |
|------|------|
| totalCount = 0 | toast.warning "생성할 수업이 없습니다." + 닫힘 |
| totalCount > 100 | 추가 경고 배너 표시 |

### 연결 화면
- 확인: `03-생성중`
- 취소: `01-닫힘` (SCR-C003 유지)
