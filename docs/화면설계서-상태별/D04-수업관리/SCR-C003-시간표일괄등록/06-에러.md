# SCR-C003 시간표일괄등록 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C003 |
| 상태 코드 | `error` |
| 경로 | `/class-schedule` |
| 역할 | manager |
| 우선순위 | P1 |
| 이전 상태 | `04-생성중` |
| 다음 상태 | 재시도 → `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C003 시간표일괄등록 — 상태: 에러

파일: src/app/(classes)/class-schedule/page.tsx (isError=true 조건)

에러 UI (우측 영역):
<div className="flex flex-col items-center justify-center h-full gap-4">
  <XCircle className="w-12 h-12 text-red-400" />
  <div className="text-center">
    <p className="text-base font-semibold text-gray-700">
      시간표 생성에 실패했습니다.
    </p>
    <p className="mt-1 text-sm text-gray-400">
      {errorMessage || "잠시 후 다시 시도해주세요."}
    </p>
  </div>
  <Button variant="outline" onClick={() => { setIsError(false); setHasPreview(true); }}>
    다시 시도
  </Button>
</div>

에러 발생 시:
- toast.error("시간표 생성에 실패했습니다.")
- 좌측 폼 입력값 유지 (데이터 손실 방지)
- isGenerating: false, isError: true

사용 유틸:
- XCircle from 'lucide-react'
- toast from 'sonner'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- bulkCreateClasses API 500 응답
- 타임아웃 30초 초과
- 403 권한 오류

### 인터랙션 (User Actions)
1. "다시 시도" → 미리보기 상태로 복귀 → 재생성 가능
2. 폼 수정 후 재시도 가능

### 비즈니스 룰
- 입력 폼 값은 에러 후에도 유지
- 403: 권한 없음 메시지 + `/` 이동 권유

### 연결 화면
- 다시 시도: `03-미리보기활성`
- 폼 수정: `02-입력중`
