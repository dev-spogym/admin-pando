# SCR-C003 시간표일괄등록 — 상태: 미리보기 활성

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C003 |
| 상태 코드 | `preview-active` |
| 경로 | `/class-schedule` |
| 역할 | manager |
| 우선순위 | P0 |
| 이전 상태 | `02-입력중` |
| 다음 상태 | 일괄생성 클릭 → DLG-C008 / 수정 → `02-입력중` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C003 시간표일괄등록 — 상태: 미리보기 활성

파일: src/app/(classes)/class-schedule/page.tsx

우측 미리보기 영역 (hasPreview=true):
- 상단 헤더:
  · "미리보기" 제목 (font-semibold)
  · 건수 배지: "총 N건" (bg-blue-100 text-blue-700 rounded-full px-3 py-1 text-sm)

- 미리보기 테이블:
  컬럼: 날짜 | 요일 | 시작시간 | 종료시간 | 수업명 | 강사
  - 최대 10행 표시 + "외 N건 더보기" 링크
  - 날짜 형식: yyyy.MM.dd (E)
  - 충돌 예상 항목: 배경 bg-amber-50, 앞에 AlertTriangle 아이콘

- 하단 액션 버튼:
  · "일괄 생성" Button primary → DLG-C008 열기
  · "다시 설정" Button outline → hasPreview=false, 좌측 폼 유지

좌측 폼: 값 유지 (수정 가능)

미리보기 데이터 생성:
- generatePreviewDates(startDate, endDate, selectedDays, startTime, duration)
- 기간 내 선택 요일에 해당하는 날짜 배열 생성
- 각 날짜에 수업명/강사/시간 조합

사용 유틸:
- eachDayOfInterval, getDay, format from 'date-fns'
- AlertTriangle from 'lucide-react'
- Button from '@/components/ui'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-입력중`에서 미리보기 버튼 클릭 + 유효성 통과

### 필수 데이터
- 폼 입력값 기반 클라이언트 사이드 계산 (API 호출 없음)

### 인터랙션 (User Actions)
1. "일괄 생성" 클릭 → DLG-C008 (생성 요약 확인 모달)
2. "다시 설정" 클릭 → 미리보기 숨김, 좌측 폼 수정 가능
3. 미리보기 테이블 스크롤 → 전체 목록 확인

### 비즈니스 룰
- 충돌 예상 항목: 기존 수업과 동일 강사·시간 겹침 → amber 하이라이트
- 충돌 확인은 클라이언트 측 사전 체크 (실제 확정은 API에서)
- 100건 초과: "외 N건 더보기" 링크로 전체 보기 (모달 또는 스크롤)

### 에지 케이스
- 모든 날짜 충돌 예상: 경고 배너 "모든 일정이 충돌할 수 있습니다. 계속 진행하시겠습니까?"
- 생성 건수 0: "미리보기할 수업이 없습니다." → 다시 설정 유도

### 연결 화면
- 일괄 생성: DLG-C008
- 다시 설정: `02-입력중`
