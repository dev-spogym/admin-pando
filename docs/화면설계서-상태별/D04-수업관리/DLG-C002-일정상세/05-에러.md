# DLG-C002 일정상세 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C002 |
| 상태 코드 | `error` |
| 부모 화면 | SCR-C001 수업캘린더 |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 500) |
| 다음 상태 | 재시도 → `02-로딩중` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 모달 에러 상태를 작성하라.
화면: DLG-C002 일정상세 — 상태: 에러

파일: src/components/classes/LessonDetailModal.tsx

에러 상태 UI (isError=true):
<div className="p-6 text-center space-y-4">
  <div className="w-12 h-12 rounded-full bg-red-100 flex items-center justify-center mx-auto">
    <AlertCircle className="w-6 h-6 text-red-500" />
  </div>
  <div>
    <p className="font-medium text-gray-800">데이터를 불러올 수 없습니다.</p>
    <p className="text-sm text-gray-500 mt-1">잠시 후 다시 시도해주세요.</p>
  </div>
  <div className="flex justify-center gap-3">
    <Button variant="outline" onClick={onClose}>닫기</Button>
    <Button onClick={() => refetch()}>재시도</Button>
  </div>
</div>

에러 분기:
- 500: 에러 UI + toast.error "데이터 로드에 실패했습니다."
- 404: toast.warning "수업을 찾을 수 없습니다." + 모달 닫힘
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- GET /api/lessons/:id 500 응답

### 에러 유형
| 상태코드 | 동작 |
|---------|------|
| 404 | toast.warning + 모달 자동 닫힘 |
| 500 | 에러 UI + 재시도 버튼 |

### 인터랙션 (User Actions)
1. "재시도" → `02-로딩중`
2. "닫기" → `01-닫힘`

### 연결 화면
- 재시도: `02-로딩중`
- 닫기: `01-닫힘`
