# DLG-C013 차감이력 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C013 |
| 상태 코드 | `empty` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `02-로딩중` (데이터 없음) |
| 다음 상태 | 필터 변경 → `02-로딩중` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionHistoryModal.tsx

빈 상태 UI (data.length === 0):
<div className="py-12 text-center">
  <ClipboardList className="w-10 h-10 text-gray-300 mx-auto mb-3" />
  <p className="text-gray-500 text-sm">차감 이력이 없습니다.</p>
  {isFiltered && (
    <Button variant="ghost" size="sm" className="mt-2" onClick={resetFilter}>
      필터 초기화
    </Button>
  )}
</div>
```

## 📝 디스크립션

### 사용 시점
- 이력 데이터 0건 (신규 세션 또는 필터 결과 없음)

### 연결 화면
- 필터 초기화/변경: `02-로딩중`
- 닫기: `01-닫힘`
