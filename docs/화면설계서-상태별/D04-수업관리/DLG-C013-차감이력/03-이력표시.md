# DLG-C013 차감이력 — 상태: 이력 표시

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C013 |
| 상태 코드 | `history-rendered` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `02-로딩중` (데이터 있음) |
| 다음 상태 | 필터 변경 → `02-로딩중` / 엑셀 → 다운로드 / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionHistoryModal.tsx

기간 필터 (DateRangePicker):
<div className="flex gap-2 items-center mb-4">
  <Input type="date" value={startDate} onChange={...} />
  <span className="text-gray-400">~</span>
  <Input type="date" value={endDate} onChange={...} />
  <Button size="sm" onClick={applyFilter}>적용</Button>
</div>

유효성:
- 종료일 < 시작일: "종료일은 시작일 이후여야 합니다."
- 범위 > 1년: "최대 1년 범위로 조회할 수 있습니다."

이력 테이블:
<DataTable
  columns={[
    { key: 'date', label: '날짜' },
    { key: 'type', label: '유형' },      // 차감/조정/복구
    { key: 'amount', label: '횟수' },    // -1, +3 등
    { key: 'handler', label: '처리자' },
  ]}
  data={history}
/>

엑셀 다운로드:
<Button variant="outline" size="sm" onClick={handleExcelExport}>
  <Download className="w-4 h-4 mr-1" /> 엑셀
</Button>

handleExcelExport:
- GET /api/sessions/:id/history/export
- 성공: toast.success "N건 엑셀 다운로드"
- 실패: toast.error "다운로드 실패"
```

## 📝 디스크립션

### 필터 유효성
| 조건 | 에러 |
|------|------|
| 종료일 < 시작일 | "종료일은 시작일 이후여야 합니다." |
| 범위 > 1년 | "최대 1년 범위로 조회할 수 있습니다." |

### 연결 화면
- 필터 적용: `02-로딩중`
- 데이터 없음: `04-빈상태`
- 닫기: `01-닫힘`
