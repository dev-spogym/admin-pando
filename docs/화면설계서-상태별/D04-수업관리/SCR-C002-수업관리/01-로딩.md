# SCR-C002 수업관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `loading` |
| 경로 | `/lessons` |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-수업목록-정상` 또는 `04-빈상태` 또는 `06-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업관리 — 상태: 로딩

파일: src/app/(classes)/lessons/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "수업 관리")
- TabNav 스켈레톤: "수업 목록 | 수업 기록" 탭 (비활성)

로딩 UI:
- 상단 SearchFilter 영역: Skeleton 바 h-10 w-full animate-pulse
- DataTable loading=true:
  · 헤더 행: 컬럼명 표시 (실제 텍스트 유지)
  · 데이터 행 5개: 각 셀 Skeleton (h-4 bg-gray-200 animate-pulse rounded)
  · 페이지네이션 영역: Skeleton

상태 관리:
- isLoading: true (초기)
- activeTab: 'lessons' (기본)
- DataTable에 loading prop 전달

데이터:
- useEffect → lessons fetch + lesson_records fetch 병렬 실행
- Promise.all([fetchLessons(), fetchRecords()])

사용 유틸:
- DataTable, TabNav, SearchFilter from '@/components/ui'
- AppLayout, PageHeader from '@/components/layout'
- useAuthStore, createClient
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C002 최초 진입
- 탭 전환 시 해당 탭 데이터 재로드
- 검색/필터 적용 후 재조회

### 필수 데이터
| 쿼리 | 테이블 | 조건 |
|------|--------|------|
| 수업 목록 | `classes` | branch_id, 필터 조건 |
| 수업 기록 | `lesson_schedules` JOIN `classes` | branch_id, 날짜 필터 |

### 인터랙션 (User Actions)
- 로딩 중 탭/버튼 비활성화
- 뒤로가기 허용

### 비즈니스 룰
- 두 탭 데이터 병렬 fetch
- DataTable 자체 loading 스켈레톤 사용

### 연결 화면
- 다음: `02-수업목록-정상` / `04-수업목록-빈상태` / `06-에러`
