# SCR-C002 수업관리 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `error` |
| 경로 | `/lessons` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 재시도 → `01-로딩` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업관리 — 상태: API 에러

파일: src/app/(classes)/lessons/page.tsx (error 상태)

에러 UI (DataTable 영역 대신):
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <AlertCircle className="w-12 h-12 text-red-400" />
  <div className="text-center">
    <p className="text-base font-semibold text-gray-700">수업 데이터를 불러오지 못했습니다.</p>
    <p className="mt-1 text-sm text-gray-400">잠시 후 다시 시도해주세요.</p>
  </div>
  <Button variant="outline" onClick={handleRetry}>다시 시도</Button>
</div>

에러 발생 시:
- toast.error("수업 데이터를 불러오지 못했습니다.")
- TabNav/SearchFilter는 유지 (재시도 가능)

사용 유틸:
- AlertCircle from 'lucide-react'
- toast from 'sonner', Button from '@/components/ui'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `classes` 또는 `lesson_schedules` fetch 실패 (500, network error)
- RLS 정책 위반 (403)

### 인터랙션 (User Actions)
1. 다시 시도 → `fetchData()` 재실행
2. 탭 전환 → 해당 탭 데이터 별도 재시도

### 비즈니스 룰
- toast 1회만 발사
- 403: `/login` 자동 리다이렉트

### 연결 화면
- 재시도: `01-로딩` → 정상 또는 유지
