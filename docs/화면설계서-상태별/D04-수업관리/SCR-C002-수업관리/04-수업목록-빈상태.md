# SCR-C002 수업관리 — 상태: 수업 목록 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C002 |
| 상태 코드 | `lessons-empty` |
| 경로 | `/lessons` |
| 역할 | manager, fc, staff |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 수업 등록 버튼 → DLG-C003 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C002 수업관리 — 상태: 수업 목록 빈 상태

파일: src/app/(classes)/lessons/page.tsx (lessons.length === 0 조건)

빈 상태 UI (DataTable 영역 대신):
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <BookOpen className="w-12 h-12 text-gray-300" />
  <div className="text-center">
    <p className="text-base font-medium text-gray-500">등록된 수업이 없습니다.</p>
    <p className="mt-1 text-sm text-gray-400">수업을 등록하여 관리를 시작하세요.</p>
  </div>
  {hasPermission(['manager', 'fc']) && (
    <Button onClick={() => setIsRegisterOpen(true)}>
      수업 등록
    </Button>
  )}
</div>

조건:
- classes.length === 0 (필터 없는 초기 상태)
- 검색 결과 없음일 때는 "검색 결과가 없습니다." 메시지 변형

SearchFilter는 유지 (빈 상태에서도 검색 가능)
TabNav는 유지

사용 유틸:
- BookOpen from 'lucide-react'
- Button, hasPermission from '@/components/ui' / '@/lib/auth'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 지점: 수업이 하나도 없는 상태
- 모든 수업 삭제 후
- 검색/필터 결과 없음

### 필수 데이터
- `classes.length === 0` (쿼리 결과)

### 인터랙션 (User Actions)
1. 수업 등록 버튼 → DLG-C003 (빈 폼)
2. 검색 입력 → 결과 없음 유지 또는 있으면 목록 표시

### 비즈니스 룰
- staff 역할: 수업 등록 버튼 숨김 (안내 메시지만 표시)
- 검색 필터로 인한 빈 상태와 실제 빈 상태 메시지 구분

### 에지 케이스
- 검색어 입력 후 빈 상태: "'{검색어}'에 해당하는 수업이 없습니다." 메시지

### 연결 화면
- 수업 등록: DLG-C003
- 등록 완료: `02-수업목록-정상`
