# SCR-C004 그룹수업템플릿 — 상태: 빈 상태

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `empty` |
| 경로 | `/class-templates` |
| 역할 | manager, fc, readonly |
| 우선순위 | P1 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 템플릿 등록 → DLG-C009 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 그룹수업 템플릿 — 상태: 빈 상태

파일: src/app/(classes)/class-templates/page.tsx (templates.length===0)

빈 상태 UI:
<div className="flex flex-col items-center justify-center py-24 gap-4">
  <LayoutTemplate className="w-12 h-12 text-gray-300" />
  <div className="text-center">
    <p className="text-base font-medium text-gray-500">등록된 템플릿이 없습니다.</p>
    <p className="mt-1 text-sm text-gray-400">자주 사용하는 수업 형태를 템플릿으로 저장하세요.</p>
  </div>
  {hasPermission(['manager','fc']) && (
    <Button onClick={() => setIsRegisterOpen(true)}>템플릿 등록</Button>
  )}
</div>

사용 유틸:
- LayoutTemplate from 'lucide-react'
- Button, hasPermission
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 신규 지점: 템플릿 없음
- 전체 삭제 후
- 검색 결과 없음

### 인터랙션
1. "템플릿 등록" → DLG-C009
2. 검색어 삭제 → `02-정상` 또는 유지

### 연결 화면
- 등록: DLG-C009 → 성공 시 `02-정상`
