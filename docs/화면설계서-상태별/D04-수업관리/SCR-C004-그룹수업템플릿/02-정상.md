# SCR-C004 그룹수업템플릿 — 상태: 정상

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C004 |
| 상태 코드 | `normal` |
| 경로 | `/class-templates` |
| 역할 | manager, fc, readonly |
| 우선순위 | P0 |
| 이전 상태 | `01-로딩` |
| 다음 상태 | 등록 → DLG-C009 / 수정 → DLG-C009 |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C004 그룹수업 템플릿 관리 — 상태: 정상

파일: src/app/(classes)/class-templates/page.tsx

레이아웃:
- AppLayout + PageHeader:
  · 제목 "그룹수업 템플릿 관리"
  · 우측: "템플릿 등록" Button primary (manager/fc만)

SearchFilter:
- 템플릿명 검색 input
- 수업 분류 Select

템플릿 카드 그리드 (grid grid-cols-3 gap-4 p-6):
- 각 카드 (bg-white rounded-lg border p-4 hover:shadow-md transition):
  · 상단: 템플릿명 (font-semibold text-gray-800)
  · 분류 배지 (StatusBadge: PT/GX/기타)
  · 정원: "정원 N명"
  · 수업 시간: "60분"
  · 담당 강사: 강사명 또는 "미지정"
  · 하단 액션: Edit2 버튼 → DLG-C009 / Trash2 → 삭제 confirm
  · "적용" 버튼 → 해당 템플릿으로 DLG-C001 열기 (빠른 수업 등록)

DataTable 대신 카드 그리드 사용

데이터:
- SELECT * FROM class_templates WHERE branch_id=? ORDER BY created_at DESC
- 검색: name ILIKE ?, type=?

사용 유틸:
- useAuthStore, createClient, hasPermission
- StatusBadge, Button, Input, Select from '@/components/ui'
- Edit2, Trash2 from 'lucide-react'
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- 로딩 완료 후 `class_templates.length > 0`

### 필수 데이터
| 블록 | 테이블 |
|------|--------|
| 템플릿 목록 | `class_templates` |

### 인터랙션 (User Actions)
1. "템플릿 등록" → DLG-C009 (빈 폼)
2. Edit2 → DLG-C009 (프리필)
3. Trash2 → confirm → DELETE
4. "적용" → DLG-C001 (템플릿 값 주입)
5. 검색/필터 → 실시간 목록 갱신

### 비즈니스 룰
- readonly 역할: 등록/수정/삭제/적용 버튼 숨김
- 삭제 시: "이 템플릿을 삭제하시겠습니까?" confirm
- 템플릿 적용: DLG-C001의 수업명/분류/정원/시간 자동 입력

### 에지 케이스
- 검색 결과 없음 → `03-빈상태`
- 템플릿 9개 초과: 3열 그리드 스크롤

### 연결 화면
- 등록/수정: DLG-C009
- 적용: DLG-C001
- 결과 없음: `03-빈상태`
