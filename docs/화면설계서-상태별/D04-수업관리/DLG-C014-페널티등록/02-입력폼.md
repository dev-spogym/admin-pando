# DLG-C014 페널티등록 — 상태: 입력 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C014 |
| 상태 코드 | `form-input` |
| 부모 화면 | SCR-C008 페널티관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 저장 → `03-저장결과` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/PenaltyRegisterModal.tsx

모달 헤더: "페널티 등록"
모달 크기: w-[480px]

Zod 스키마:
const penaltySchema = z.object({
  memberId: z.string().min(1, "회원을 선택해주세요."),
  type: z.enum(['nosho','late_cancel','violation'], { required_error: "페널티 유형을 선택해주세요." }),
  point: z.number().int().min(1, "포인트는 1 이상이어야 합니다."),
  reason: z.string().min(1, "사유를 입력해주세요.").max(200),
})

폼 필드 (space-y-4 p-6):
1. 회원 * (MemberSearchSelect — 검색 가능 Select)
2. 페널티 유형 * (Select: 노쇼|늦은취소|규정위반)
3. 포인트 * (Input number, min=1)
4. 사유 * (Textarea, rows=3, maxLength=200)

모달 푸터:
- "취소" Button outline → onClose()
- "등록" Button primary
```

## 📝 디스크립션

### 유효성 검사
| 필드 | 규칙 |
|------|------|
| 회원 | 필수 선택 |
| 유형 | 필수 선택 |
| 포인트 | 1 이상 정수 |
| 사유 | 필수, 최대 200자 |

### 연결 화면
- 저장: `03-저장결과`
- 닫기: `01-닫힘`
