# DLG-C014 페널티등록 — 상태: 유효성 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C014 |
| 상태 코드 | `validation-error` |
| 부모 화면 | SCR-C008 페널티관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-입력폼` (저장 시도 → 유효성 실패) |
| 다음 상태 | 필드 수정 후 재시도 → `03-저장결과` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/PenaltyRegisterModal.tsx

인라인 에러 표시:
- 각 필드 하단: <p className="mt-1 text-xs text-red-500">{errors.field?.message}</p>
- 에러 필드 border: border-red-500
- 저장 클릭 시 첫 에러 필드로 setFocus
```

## 📝 디스크립션

### 에러 목록
| 필드 | 에러 메시지 |
|------|------------|
| 회원 미선택 | "회원을 선택해주세요." |
| 유형 미선택 | "페널티 유형을 선택해주세요." |
| 포인트 0 이하 | "포인트는 1 이상이어야 합니다." |
| 사유 빈값 | "사유를 입력해주세요." |

### 연결 화면
- 수정 후 재저장: `03-저장결과`
