# DLG-C005 수업기록상세 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C005 |
| 상태 코드 | `error` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 500) |
| 다음 상태 | 재시도 → `02-로딩중` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 에러 상태 UI를 작성하라.
화면: DLG-C005 수업기록상세 — 상태: 에러

파일: src/components/classes/LessonRecordDetailModal.tsx

에러 UI (p-6 text-center space-y-4):
<div className="w-12 h-12 rounded-full bg-red-100 flex items-center justify-center mx-auto">
  <AlertCircle className="w-6 h-6 text-red-500" />
</div>
<p className="font-medium text-gray-800">데이터를 불러올 수 없습니다.</p>
<p className="text-sm text-gray-500">잠시 후 다시 시도해주세요.</p>
<div className="flex justify-center gap-3">
  <Button variant="outline" onClick={onClose}>닫기</Button>
  <Button onClick={() => refetch()}>재시도</Button>
</div>
```

## 📝 디스크립션

### 에러 유형
| 상태코드 | 동작 |
|---------|------|
| 500 | 에러 UI + 재시도 버튼 |
| 404 | toast.warning + 모달 닫힘 |

### 연결 화면
- 재시도: `02-로딩중`
- 닫기: `01-닫힘`
