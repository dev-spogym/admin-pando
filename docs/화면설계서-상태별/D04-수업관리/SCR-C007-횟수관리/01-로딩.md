# SCR-C007 횟수관리 — 상태: 로딩

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C007 |
| 상태 코드 | `loading` |
| 경로 | `/lesson-counts` |
| 역할 | manager, fc, staff |
| 우선순위 | P0 |
| 이전 상태 | 진입 |
| 다음 상태 | `02-정상` 또는 `03-빈상태` 또는 `04-에러` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 'use client' 컴포넌트를 작성하라.
화면: SCR-C007 횟수관리 — 상태: 로딩

파일: src/app/(classes)/lesson-counts/page.tsx

레이아웃:
- AppLayout + PageHeader (제목 "횟수 관리")
- SearchFilter Skeleton
- DataTable loading=true (Skeleton 행 10개)

데이터 (병렬 fetch):
- lesson_counts: SELECT lc.*, m.name as member_name, c.name as class_name
    FROM lesson_counts lc
    JOIN users m ON lc.member_id=m.id
    JOIN classes c ON lc.class_id=c.id
    WHERE lc.branch_id=?
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- SCR-C007 진입 시
- 횟수 조정/차감 후 목록 갱신

### 연결 화면
- `02-정상` / `03-빈상태` / `04-에러`
