# DLG-C011 세션상세 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C011 |
| 상태 코드 | `error` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P2 |
| 이전 상태 | `02-로딩중` (API 500) |
| 다음 상태 | 재시도 → `02-로딩중` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionDetailModal.tsx

에러 분기:
- 500: 에러 UI
- 404: toast.warning("세션을 찾을 수 없습니다.") + 자동 닫힘

에러 UI (p-6 text-center space-y-4):
<AlertCircle className="w-6 h-6 text-red-500 mx-auto" />
<p className="font-medium">데이터를 불러올 수 없습니다.</p>
<div className="flex justify-center gap-3">
  <Button variant="outline" onClick={onClose}>닫기</Button>
  <Button onClick={() => refetch()}>재시도</Button>
</div>
```

## 📝 디스크립션

### 연결 화면
- 재시도: `02-로딩중`
- 닫기: `01-닫힘`
