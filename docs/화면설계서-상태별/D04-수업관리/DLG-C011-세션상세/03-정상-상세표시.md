# DLG-C011 세션상세 — 상태: 정상 상세표시

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C011 |
| 상태 코드 | `detail-rendered` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `02-로딩중` (API 200) |
| 다음 상태 | 차감 버튼 → `04-차감확인` / 닫기 → `01-닫힘` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionDetailModal.tsx

모달 헤더:
- "{session.member.name} 세션 상세"
- X 버튼: onClose()

상세 정보 (p-6 space-y-4):
<div className="rounded-lg bg-blue-50 p-4 flex justify-between items-center">
  <div>
    <p className="text-sm text-gray-500">잔여 횟수</p>
    <p className="text-2xl font-bold text-blue-600">{session.remaining_count}회</p>
  </div>
  <div className="text-right text-sm text-gray-500">
    <p>총 {session.total_count}회</p>
    <p>사용 {session.used_count}회</p>
  </div>
</div>

세션 정보:
- 상품명 / 강사 / 시작일 ~ 종료일 / 상태 Badge

최근 차감 이력 (최대 5건):
<table className="w-full text-sm">
  <thead><tr><th>날짜</th><th>유형</th><th>횟수</th><th>처리자</th></tr></thead>
  <tbody>{session.recentHistory.map(...)}</tbody>
</table>

버튼 (session.status === 'ACTIVE' && session.remaining_count > 0):
<Button onClick={() => setShowDeductConfirm(true)}>횟수 차감</Button>

비활성 조건:
- EXPIRED/SUSPENDED: 버튼 숨김
- remaining_count === 0: 버튼 disabled
```

## 📝 디스크립션

### 차감 버튼 활성 조건
| 조건 | 버튼 상태 |
|------|----------|
| ACTIVE + 잔여 있음 | 활성 |
| EXPIRED / SUSPENDED | 숨김 |
| 잔여 0 | disabled |

### 연결 화면
- 차감 버튼: `04-차감확인`
- 닫기: `01-닫힘`
