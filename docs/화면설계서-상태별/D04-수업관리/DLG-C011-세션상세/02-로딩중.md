# DLG-C011 세션상세 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C011 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 성공 → `03-정상-상세표시` / 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionDetailModal.tsx

모달 크기: w-[480px]

스켈레톤 (animate-pulse, p-6 space-y-4):
- 회원명: h-6 bg-gray-200 rounded w-1/2
- 상품명: h-4 bg-gray-200 rounded w-2/3
- 잔여횟수 배지: h-8 bg-gray-200 rounded w-24
- 이력 행 3개: h-10 bg-gray-200 rounded

데이터 페칭:
const { data, isLoading, error } = useQuery({
  queryKey: ['session-detail', sessionId],
  queryFn: () => fetch(`/api/sessions/${sessionId}`).then(r => r.json()),
  enabled: !!sessionId && isOpen,
})
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-정상-상세표시`
- 로드 실패: `05-에러`
