# DLG-C011 세션상세 — 상태: 닫힘

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C011 |
| 상태 코드 | `closed` |
| 부모 화면 | SCR-C007 횟수관리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | — |
| 다음 상태 | 세션 상세 버튼 → `02-로딩중` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/SessionDetailModal.tsx

Props:
interface SessionDetailModalProps {
  isOpen: boolean
  sessionId: string | null
  onClose: () => void
  onSuccess: () => void  // 차감 후 목록 갱신
}

닫힘 상태 (isOpen=false): null return
```

## 📝 디스크립션

### 연결 화면
- 트리거: `02-로딩중`
