# DLG-C009 템플릿등록/수정 — 상태: 신규 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C009 |
| 상태 코드 | `form-create` |
| 부모 화면 | SCR-C004 그룹수업템플릿 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 저장 → `05-저장결과` / 닫기 → `04-미저장확인` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/ClassTemplateModal.tsx (mode='create')

모달 헤더: "그룹수업 템플릿 등록"
모달 크기: w-[480px]

Zod 스키마:
const templateSchema = z.object({
  name: z.string().min(1, "템플릿명을 입력해주세요.").max(50),
  category: z.enum(['GX','pilates','yoga','etc'], { required_error: "분류를 선택해주세요." }),
  duration: z.number().min(10, "수업 시간은 10분 이상이어야 합니다."),
  capacity: z.number().min(1).max(100, "정원은 1~100명이어야 합니다."),
  description: z.string().optional(),
})

폼 필드:
1. 템플릿명 * (Input, maxLength=50)
2. 분류 * (Select: GX|필라테스|요가|기타)
3. 수업 시간 * (Select: 30|60|90|120분)
4. 정원 (Input number, 1~100)
5. 설명 (Textarea, rows=3)

모달 푸터:
- "취소" Button outline
- "저장" Button primary
```

## 📝 디스크립션

### 연결 화면
- 저장: `05-저장결과`
- 닫기(변경있음): `04-미저장확인`
- 닫기(변경없음): `01-닫힘`
