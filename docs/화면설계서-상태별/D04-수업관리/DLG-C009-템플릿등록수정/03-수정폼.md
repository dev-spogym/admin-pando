# DLG-C009 템플릿등록/수정 — 상태: 수정 폼

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C009 |
| 상태 코드 | `form-edit` |
| 부모 화면 | SCR-C004 그룹수업템플릿 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` (수정 버튼 클릭) |
| 다음 상태 | 저장 → `05-저장결과` / 닫기 → `04-미저장확인` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/ClassTemplateModal.tsx (mode='edit')

모달 헤더: "그룹수업 템플릿 수정"

프리필 처리:
useEffect(() => {
  if (template && mode === 'edit') {
    reset({
      name: template.name,
      category: template.category,
      duration: template.duration,
      capacity: template.capacity,
      description: template.description || '',
    })
  }
}, [template, mode])

모달 푸터:
- "취소" Button outline
- "수정 저장" Button primary (isDirty=false 시 disabled)
```

## 📝 디스크립션

### 연결 화면
- 저장: `05-저장결과`
- 닫기(변경있음): `04-미저장확인`
- 닫기(변경없음): `01-닫힘`
