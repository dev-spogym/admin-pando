# DLG-C009 템플릿등록/수정 — 상태: 닫힘

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C009 |
| 상태 코드 | `closed` |
| 부모 화면 | SCR-C004 그룹수업템플릿 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | — |
| 다음 상태 | 등록 버튼 → `02-신규폼` / 수정 버튼 → `03-수정폼` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/ClassTemplateModal.tsx

Props:
interface ClassTemplateModalProps {
  isOpen: boolean
  mode: 'create' | 'edit'
  template?: ClassTemplate | null
  onClose: () => void
  onSuccess: () => void
}

닫힘 상태 (isOpen=false): null return
```

## 📝 디스크립션

### 연결 화면
- 트리거: `02-신규폼` 또는 `03-수정폼`
