# DLG-C009 템플릿등록/수정 — 상태: 미저장 확인

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C009 |
| 상태 코드 | `unsaved-confirm` |
| 부모 화면 | SCR-C004 그룹수업템플릿 |
| 역할 | manager, owner |
| 우선순위 | P2 |
| 이전 상태 | `02-신규폼` 또는 `03-수정폼` (변경 후 닫기 시도) |
| 다음 상태 | 확인 → `01-닫힘` / 취소 → 폼 복귀 |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/ClassTemplateModal.tsx (내부 상태)

트리거: formState.isDirty === true && 닫기 시도

미저장 확인 다이얼로그:
<div className="absolute inset-0 bg-black/30 flex items-center justify-center rounded-xl">
  <div className="bg-white rounded-lg p-6 w-80 shadow-xl">
    <h3 className="font-semibold text-gray-800">변경사항이 있습니다</h3>
    <p className="mt-2 text-sm text-gray-500">저장하지 않고 닫으시겠습니까?</p>
    <div className="mt-4 flex justify-end gap-3">
      <Button variant="outline" onClick={() => setShowUnsaved(false)}>계속 편집</Button>
      <Button variant="destructive" onClick={() => { reset(); setShowUnsaved(false); onClose(); }}>닫기</Button>
    </div>
  </div>
</div>
```

## 📝 디스크립션

### 연결 화면
- 계속 편집: `02-신규폼` 또는 `03-수정폼`
- 닫기: `01-닫힘`
