# DLG-C016 대안일정제시 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C016 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C009 일정요청처리 |
| 역할 | manager, fc |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 성공 → `03-입력폼` / 실패 → `05-에러` |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/AlternativeScheduleModal.tsx

모달 크기: w-[480px]

스켈레톤 (animate-pulse, p-6 space-y-4):
- 요청 정보 영역: h-20 bg-gray-200 rounded
- 입력 필드 3개: h-10 bg-gray-200 rounded

데이터 페칭:
const { data: request, isLoading, error } = useQuery({
  queryKey: ['schedule-request', requestId],
  queryFn: () => fetch(`/api/schedule-requests/${requestId}`).then(r => r.json()),
  enabled: !!requestId && isOpen,
})
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-입력폼`
- 로드 실패: `05-에러`
