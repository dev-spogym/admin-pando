# DLG-C015 자동페널티정책 — 상태: 로딩중

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C015 |
| 상태 코드 | `loading` |
| 부모 화면 | SCR-C008 페널티관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `01-닫힘` |
| 다음 상태 | 성공 → `03-정책폼` / 실패 → toast.error + 닫힘 |

## 🧩 바이브코딩 프롬프트

```
파일: src/components/classes/AutoPenaltyPolicyModal.tsx

모달 크기: w-[520px]

스켈레톤 (animate-pulse, p-6 space-y-4):
- 헤더: h-6 bg-gray-200 rounded w-1/2
- 필드 3개: h-10 bg-gray-200 rounded
- 토글: h-8 bg-gray-200 rounded w-20

데이터 페칭:
const { data: policy, isLoading } = useQuery({
  queryKey: ['penalty-policy'],
  queryFn: () => fetch('/api/penalty-policy').then(r => r.json()),
  enabled: isOpen,
})
```

## 📝 디스크립션

### 연결 화면
- 로드 성공: `03-정책폼`
- 로드 실패: toast.error + 모달 닫힘
