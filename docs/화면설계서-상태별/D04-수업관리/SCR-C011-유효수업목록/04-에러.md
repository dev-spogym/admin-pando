# SCR-C011 유효수업목록 — 상태: 에러

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | SCR-C011 |
| 상태 코드 | `error` |
| 경로 | `/valid-lessons` |
| 우선순위 | P2 |

## 🧩 바이브코딩 프롬프트

```
에러 UI:
<div className="flex flex-col items-center justify-center py-20 gap-4">
  <AlertCircle className="w-12 h-12 text-red-400" />
  <p className="text-base font-semibold text-gray-700">유효 수업 목록을 불러오지 못했습니다.</p>
  <Button variant="outline" onClick={handleRetry}>다시 시도</Button>
</div>
- toast.error 1회
```

## 📝 디스크립션
- 재시도 → `01-로딩`
