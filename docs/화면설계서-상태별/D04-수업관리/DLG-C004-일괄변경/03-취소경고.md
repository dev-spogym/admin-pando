# DLG-C004 일괄변경 — 상태: 취소 경고

## 메타

| 항목 | 값 |
|------|----|
| 화면 ID | DLG-C004 |
| 상태 코드 | `cancel-warn` |
| 부모 화면 | SCR-C002 수업관리 |
| 역할 | manager, owner |
| 우선순위 | P1 |
| 이전 상태 | `02-입력폼` (수업취소 항목 선택) |
| 다음 상태 | 체크박스 확인 후 적용 → `04-처리중` / 다른 항목 선택 → `02-입력폼` |

## 🧩 바이브코딩 프롬프트

```
Next.js 15 App Router + TypeScript + Tailwind v4 기반 취소 경고 UI를 작성하라.
화면: DLG-C004 일괄변경 — 상태: 취소 경고

파일: src/components/classes/BulkChangeModal.tsx (selectedOption='cancel')

빨간 경고박스 (selectedOption==='cancel'):
<div className="rounded-lg border border-red-200 bg-red-50 p-4 space-y-3">
  <div className="flex items-center gap-2 text-red-700">
    <AlertTriangle className="w-5 h-5 flex-shrink-0" />
    <p className="font-medium text-sm">
      {selectedIds.length}개 수업을 취소합니다. 이 작업은 되돌릴 수 없습니다.
    </p>
  </div>
  <label className="flex items-center gap-2 cursor-pointer">
    <input
      type="checkbox"
      checked={cancelConfirmed}
      onChange={e => setCancelConfirmed(e.target.checked)}
      className="w-4 h-4 accent-red-600"
    />
    <span className="text-sm text-red-700">위 내용을 확인했습니다.</span>
  </label>
</div>

적용 버튼:
- cancelConfirmed=false: disabled
- cancelConfirmed=true: enabled → bulkDeleteClasses API
```

## 📝 디스크립션

### 사용 시점 (Trigger)
- `02-입력폼`에서 "수업 취소" 항목 선택

### 인터랙션 (User Actions)
1. 체크박스 미확인 → 적용 버튼 disabled
2. 체크박스 확인 → 적용 버튼 활성 → `04-처리중`
3. 다른 항목 선택 → 경고박스 숨김 → `02-입력폼`

### 비즈니스 룰
- 수업취소는 불가복구 → 반드시 체크박스 확인 후 진행

### 연결 화면
- 확인 후 적용: `04-처리중`
- 항목 변경: `02-입력폼`
